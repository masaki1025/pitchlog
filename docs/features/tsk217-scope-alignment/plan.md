---
feature: tsk217-scope-alignment
status: in-review         # active | in-review(/pr が PR 内で更新。完了は PR 状態・Notion・worktree 除去から導出。codex_run.py implement は active 以外を拒否)
承認: 済(2026-09-14・山田正輝)  # 未 | 済(YYYY-MM-DD・承認者)— codex_run.py が「済」でないと実行を拒否する
重さ分類: 軽微            # 軽微 | 通常 | コア領域 | 機械的軽作業(ADR-001 のモデルをラッパーが自動選択)
worktree: ../../..        # worktree ルート(plan.md からの相対 or 絶対)。/task-start が設定
notion: https://app.notion.com/p/3da93b75e6878197b5cbc203473851f8
branch: feature/tsk217-scope-alignment
created: 2026-09-14
計画レビュー周回: 5        # 指摘反映を伴うレビュー 1 周ごとに +1(収束確認周は数えない。/plan が更新)
確定ゲート周回: 0          # 指摘反映を伴う敵対レビュー 1 周ごとに +1(同前。/finalize-doc が更新)
実行方式: 通常             # 通常 | fast(fast path 適用時に fast へ — 人間の事前 OK 必須。現在地導出が識別)
反映周コミット: 適用       # 適用 | 規約制定前(必須・既定値なし。確定ゲートの反映周コミット突合の適用境界 — 設計書 6.1)
---

# 実装計画書: TSK-217 の射程を裁定 `C` へ追従させる

## 1. 背景・目的

**Notion**: [本タスク](https://app.notion.com/p/3da93b75e6878197b5cbc203473851f8)
**調査メモ**: [research.md](research.md)(2026-09-14 — 調査 2 本の統合。全件典拠)

**TSK-367 からの引き渡し。** TSK-367 は `contract_only` 158 行の受取先を確定し、
**10 owner を `TSK-217` と判定**した。しかし [TSK-217](https://app.notion.com/p/3bc93b75e68781f2a613d791db2231ab) の
カードは **2026-08-14 作成**で、**PO 裁定 `C`(2026-09-12)より前**。
裁定 `C` が与えた役割にカードが追随しておらず、**10 owner のうち 6 件がカードの射程に届かない**。

> **裁定 `C`**(`../../worklog/2026-09-12-merge-gate-api-cycle.md:66`):
> **各 FR の実装タスク**が自経路の HTTP 越境テストごと持つ。
> **TSK-217 は対象横断の判定とハーネスの所有者として残し**、前提「FR-041 の実装が先」を
> 「**FR-041 の実装 PR と同一変更**」へ訂正する

**成果物は Notion のタスク定義**。裁定 `C` 自身が「**正本ではない** — Notion のタスク定義
(設計書 7.4 の作業管理面。**確定ゲート不要**)」と分類済み(同 `:43`)。

## 2. スコープ

### 射程は **カードの射程拡張のみ**(**3 周目で射程縮小 — 人間の裁定 2026-09-14**)

**2 周目までは TSK-367 の `plan.md` の文書是正(射程 B)を同梱していた。3 周目で全件外した。**

| 周 | 判定 | 内訳 | 帰結 |
| --- | --- | --- | --- |
| 1 | 否決 | `P0` 2 / `P1` 2(起因 0)| 是正 6 → 8 件 |
| 2 | 否決 | `P0` 5 / `P1` 4(**起因 4**)| **射程縮小** 13 → 5 件 |
| 3 | 否決 | `P0` 5 / `P1` 3(**起因 7**)| **射程 B を全件撤去** |

**撤去の理由**: **是正の完全性を機械で証明しようとすると、検証器自身が新しい検査面を生む。**
3 周目の `P0` 5 件のうち 4 件が**検証器と射程の不整合**だった(全行ラベル検査を要求しながら
2 行のラベルを外して合格不能 / 検証器が残した是正を検査できない / `#4` が自分で立てた基準に反する)。
**台帳の既存候補「是正の完全性を証明する機構を同一タスクの射程へ入れると、
機構自身が新しい検査面を生み、前周修正起因の輪になる」の型**である(TSK-348 で記録・TSK-363 が 2 例目)。

**外しても危険が残らない条件**(**4 周目 `P0` で是正** — 当初は「PR #3 側に防御がある」で足りるとしていた):

TSK-317 の担当セッションは「宛先の導出は **override 表 + `R-B` 内容表 + `B_SET` 内訳の和**で組み、
**資産の 46 と exact-set で突合してから書き込む。突合が合わなければ red**」と明言している
(2026-09-14 の相互連絡)。**しかし exact-set は「集合の一致」しか見ない。**

**`NFR-012/list_item-003` は `R-B` 内容表で `TSK-217`、override 表で `FR-037` と
2 つの宛先を持つ**(`../contract-only-runtime-handoff/plan.md:134` と `:165`)。
**和を取ると owner としては 1 件だが宛先が 2 つある**状態になり、
**集合の一致は成立したまま矛盾が通る。**

→ **2 つの条件を置く**:

1. **申し送り `#7`(文書是正)の完了を PR #3 の開始条件とする** — 起票時にそう明記する
2. **PR #3 側で「owner → 宛先」を単一の写像として扱い、
   同一 owner に 2 つ以上の宛先があれば fail-closed で red にする**ことを申し送る(`#8`)

**件数の一致だけでは足りない。宛先値の矛盾を拒否する条件が要る。**

### カードの射程は「概要 9 項目」ではなく「ポインタ 2 本」で定義されている

- スコープ節の柱書「**網羅すべき組合せの正は要件書 `NFR-019(b)` の本文**」
- 参照節「要件書 `NFR-019(b)`・**`FR-034` の受け入れ基準**」

| owner | 届くか | 経路 |
| --- | --- | --- |
| `FR-034/heading-004/list_item-001` / `list_item-004` / `list_item-005` | **届く** | 参照節「`FR-034` の受け入れ基準」 |
| `NFR-019/paragraph-001` | **届く** | 柱書「`NFR-019(b)` の本文」(**(a)(c)(d) は概要に無い**)|
| `NFR-010/list_item-004` | **届かない** | カードは `NFR-010` を参照していない |
| `NFR-010/list_item-002` | **届かない** | — |
| `SECTION-1.1/paragraph-002` | **届かない** | — |
| `SECTION-3/heading-001/list_item-001` | **届かない** | — |
| `SECTION-4.0-2/list_item-001` | **届かない** | — |
| `SECTION-8/list_item-002` | **届かない** | — |

> **TSK-367 の報告「10 件中 4 件が明記」は件数としては正しいが内訳が逆だった** —
> `NFR-010/list_item-004` を「明記あり」、`NFR-019/paragraph-001` を「明記なし」としていた。
> **概要 9 項目だけを見て柱書と参照節を見なかった**ため。

### 決めた 2 点(**`TSK-380` の案文と一致を確認済み**)

**① 「判定」だけか「判定**と**ハーネス」か → 本文を正とし「判定 + ハーネス」**

裁定 `C` 本文(`../../worklog/2026-09-12-merge-gate-api-cycle.md:66`)は「**判定と****ハーネス**」だが、
TSK-367 のカードへ入ったコメント(2026-09-12T13:48)は「**対象横断の判定に限られます**」で
**「ハーネス」が落ちている**。**この差は 2 owner の帰属を変える**
(`NFR-019/paragraph-001` と `SECTION-8/list_item-002`)。

→ **裁定 `C` 本文を正とする。** 根拠は当初 3 点だったが、**2026-09-16 に 4 点目が加わった** —
**`TSK-380` のカードの「訂正後(案)」が「本タスクが持つのは対象横断の判定と****ハーネス****である」と
書いており、独立に本文を正としている。**

**② 「ハーネス」は列挙で暫定運用する**

**定義文はリポジトリに存在しない**(全文走査で `worklog:66` の 1 箇所のみ)。
運用例が 2 件 — **(i) 正解ベクタ・比較単位の管理** / **(ii) CI の全ランナー配線**。
→ **定義の確定は PO 裁定へ送る**(申し送り `#2`)。**勝手に広げない。**

### `TSK-380` との分担(**受諾済み 2026-09-16・山田正輝。判定者 = 人間(PO)**)

**同じカード(TSK-217)を 2 タスクが更新する**ため境界を確定した。

| 担当 | 範囲 |
| --- | --- |
| **`TSK-380`** | **「前提・依存」節の訂正**(**役割の宣言を含む**)/ **37 経路の `test_owner` の再割り当て方式** |
| **本タスク** | **「スコープ」節へ `contract_only` 10 owner を ID で列挙**する |

> **本タスクは役割の宣言を書かない**(`TSK-380` と二重記載しないため)。
> **同じことが複数箇所に書かれると片方だけ古くなる** — 本計画が 5 周のレビューで繰り返し踏んだ型であり、
> **ここで自ら再生産しない。** スコープ節からは**裁定 `C` を直接引く**(`TSK-380` の記述には依存しない)。

### やること

- TSK-217 のカードの**「スコープ」節へ 10 owner を ID で個別に列挙**し、
  **裁定 `C` を直接引いて**届かなかった 6 件が受け取れる形にする
- **`TSK-217-A` 7 + `TSK-217-B` 10 = 17** で**互いに素**であることを明記
- カードの「`404`/`400`/存在秘匿の同値性」という説明の訂正
- 終着区分つきの記録を worklog へ書き、`TSK-380` / `TSK-317` / PO へ引き渡す
- **TSK-367 の `plan.md` の不整合の是正を、調査から始める別タスクとして起票**(申し送り `#7`。**件数・内訳は確定しない**)

### やらないこと

- **TSK-367 の `plan.md` の文書是正** — **3 周目で全件撤去**。別タスクへ(申し送り `#7`)
- **検証器の新設** — 射程 B の撤去に伴い不要。**実行可能なコードを 1 行も追加しない**
- **「前提・依存」節の訂正と役割の宣言** — **`TSK-380` の射程**(2026-09-16 に分担を受諾)
- **越境テストの実装本体** — `FR-041` の入口が 1 つも存在しない
  (`backend/src/pitchlog/main.py:8` の `/health` のみ)。裁定 `C` により各 FR の実装タスクが持つ
- **前提「`FR-041` の実装が先」の訂正** — **`TSK-380` の射程**(申し送り `#3`)
- **`http-route-matrix.json` の `test_owner` の再割り当て** — **`TSK-380` の射程**(`ADR-004:46`)
- **`TSK-217` の射程を API 実装へ広げること** — `../merge-gate-api-cycle/plan.md:134` が明示的に禁じている
- **「ハーネス」の定義の確定** — 運用例 2 件の列挙までとし、定義は PO 裁定へ送る
- **正本の変更**

## 3. 影響する正本

| 正本 | 変更内容 | ゲート(PRレビュー / finalize-doc) |
| --- | --- | --- |
| [ハーネス運用評価台帳](../../development/harness-evaluation.md) | **既存候補「是正の完全性を証明する機構を同一タスクの射程へ入れると輪になる」へ 3 例目を追記**(**既存 2 例に無い現れ方** — **申し送りの内訳を列挙する義務**という形で残った)。**`H-*` は与えない**(昇格判断は PO)。変更履歴表へ 1 行(**版は上げない** — 7.6-3 前段)| **PR レビュー** |
| [`docs/README.md`](../../README.md)(索引)| 台帳行の要約と最終更新日を現行化 | —(常に現行化 — 7.2)|
| **上記以外はなし** | 成果物は **Notion のタスク定義**(作業管理面 — 裁定 `C` 自身が「正本ではない・確定ゲート不要」と分類済み)と **worklog** | — |

> **台帳への追記は `/pr` のクローズ処理で判断する**(手順 1-3)。
> 該当する場合はここへ宣言を**先に**追記してから台帳を触る。

## 4. 実装方針

**重さ分類の根拠**: **軽微**。**実行可能なコードを 1 行も追加せず**、正本も変更しない。
`.claude/core-areas.json` のどの `paths` にも当たらない(**変更は Notion と `docs/worklog/` のみ**)。

ただし **`TSK-217` 自身はコア領域(テナント分離)のタスク**であり、その射程を動かすので、
**カードへ書く内容は要件条文の逐語で裏づける**([research.md](research.md) §1 で全件実施済み)。

### `TSK-380` との責任境界(**3 周目 `P1` — 未解決だった**)

`TSK-380` は「**TSK-217 の所有範囲を裁定 `C` に合わせる**」と定義されており
(`../merge-gate-api-cycle/design.md:343`)、**本タスクと同じカードを更新する**。

→ **分担を明文化し、`TSK-380` の受諾を取る**:

| 担当 | 範囲 |
| --- | --- |
| **本タスク** | **`contract_only` 10 owner のカード記載**(裁定 `C` の役割の追記を含む)|
| **`TSK-380`** | **前提「`FR-041` の実装が先」の訂正**と **37 経路の `test_owner` の再割り当て方式** |

**受諾コメントの必須文面**(3 項目を明示させる — 4 周目 `P1`):

- **10 `contract_only` owner のカード記載**は**本タスク**が持つ
- **37 経路の `test_owner` の再割り当て方式**と**前提「`FR-041` の実装が先」の訂正**は **`TSK-380`** が持つ
- **`cells` 12 / `claim-mutant-map` 7 / `boundary-proposal` 1** の扱いは**どちらが持つか**(申し送り `#1`)

**受諾が取れなければ、「TSK-217 のカードの射程拡張(本計画 2 節)」を `TSK-380` へ移管し、
本タスクを取り下げる**(申し送り `#6`)。**「A ごと」という略称は使わない** —
既存の `TSK-217-A`(`no_db_decision_point` 7 行)と紛らわしいため(4 周目 `P1`)。

### 実装ステップ(コミット単位 — 設計書 6.1 段階実装)

| # | ステップ(何を作るか) | 合格条件(このステップの検証方法) |
| --- | --- | --- |
| 1 | **`TSK-380` から分担の受諾を得る**(先行 — 得られなければ以降を実行しない) | `[手動・外部]` **受諾者 = `TSK-380` の担当セッション。判定者 = 人間(PO)**(4 周目 `P1` — 作業者自身のコメントでは通さない)。`TSK-380` のカードに**下記の必須文面**を含む受諾コメントがあり、**受諾後の射程行**が読み取れる。**受諾が無い・拒否・無応答のいずれでも中断し、取り下げの記録を残して人間へ報告** |
| 2 | **カードの更新**(Notion)+ **別タスクの起票** + 終着区分つき記録を worklog へ | `[手動・外部]` カードに **10 owner の ID が個別に列挙**され、届かなかった 6 件が受け取れることが読み取れる / **ハーネスの運用例 2 件**が明記 / **`TSK-217-A` 7 + `TSK-217-B` 10 = 17** と互いに素 / 「`404`/`400`/存在秘匿の同値性」の訂正 / **文書是正の別タスクを起票し ID を申し送り表へ記入**した / **送り先ごとに受取側の確認者・受諾 URL・受諾後の射程行**を worklog へ記録 |

> **ステップ 1 を先行させる理由**: **受諾が取れなければ本タスクは成立しない**(`TSK-380` と同じカードを
> 二重に更新することになる)。**カードを触る前に境界を確定する。**

## 5. DoD(受け入れ基準)

### カードの射程

- [ ] **「スコープ」節が裁定 `C` の役割(対象横断の判定 + ハーネス)を明記している**
- [ ] **10 owner の ID が個別に列挙され**、届かなかった 6 件が受け取れることが読み取れる
- [ ] **ハーネスの運用例 2 件**(正解ベクタ・比較単位の管理 / CI 全ランナー配線)**が列挙で書かれ、
      定義の確定は PO へ送られている**(**勝手に広げていない**)
- [ ] **`TSK-217-A` 7 + `TSK-217-B` 10 = 17 で互いに素**であることがカードから分かる
- [ ] **「`404`/`400`/存在秘匿の同値性」の説明を訂正した**(実データでは存在秘匿は 1 行のみ。
      根拠は `scripts/check_authz_catalog.py:3357-3362` の `_has_db_decision()` による機械分類)

### 共通

- [ ] **`TSK-380` から分担の受諾を得た**(ステップ 1。**取れなければ本タスクを取り下げる**)
- [ ] **リポジトリの変更が本タスクの 3 ファイルだけ**(4 周目 `P1` — `docs/features/**` 全体を許容すると
      **射程 B の再混入を検出できない**)。**判定は次の和集合で行う**(5 周目 `P1` —
      `HEAD` との差分だけでは **staged・unstaged・untracked の再混入を見逃す**):

      ```
      git diff --name-only origin/develop...HEAD      # コミット済み(3 点比較)
      git diff --name-only                            # unstaged
      git diff --name-only --cached                   # staged
      git ls-files --others --exclude-standard        # untracked
      ```

      **重複を除いた和集合が**
      `docs/features/tsk217-scope-alignment/plan.md` / 同 `research.md` /
      `docs/worklog/2026-09-14-tsk217-scope-alignment.md` の **3 件と完全一致**し、
      **`docs/features/contract-only-runtime-handoff/` 配下が 0 件**であること。
      **2 点比較は使わない** — 可動する `origin/develop` との差分を拾い偽陽性になる

### 引き渡し(**送り先の種別で判定条件を分ける**)

- [ ] **タスクへの申し送り**(`#1` `#3` `#4` `#5` `#7` `#8`)は、**単一の実在タスク ID /
      受取側の確認者 / 受諾コメント・更新 URL / 受諾後の射程行**を worklog へ記録した
- [ ] **PO への裁定依頼**(`#2`)は、**裁定依頼の URL / 問い / 裁定者 / 結果**を記録した
      (**実在タスク ID と射程行は求めない** — 送り先が PO でありタスクではないため)

## 6. テスト計画

**実行可能なコードを 1 行も追加しないため、自動テストを新設しない。**
`NFR-019` の **4 種別**((a) 一致性・(b) 越境・(c) E2E 主要分岐・(d) 同期故障系 — 要件書 `:922`)の
いずれにも追加しない。**ルート `pytest` は既存ゲートの回帰確認**として実行する(4 周目 `P1`)。

| 手段 | 内容 |
| --- | --- |
| **既存ゲートの維持確認** | `uv run ruff check .` / `uv run ty check` / `uv run pytest tests/`(ルート)。`ruff format` は未導入 — 走らせない。**backend の DB 必須テストは `PITCHLOG_TEST_ADMIN_DSN` / `PITCHLOG_TEST_ROLE_DSN` のスキームから `+psycopg` を外して実行する**(`../orm-schema-migration/design.md:405-425` の既知の罠。付けたままだと 194 errors)|
| **カード記載の再現性** | 10 owner の ID を**資産から再導出**して、カードの列挙と一致することを worklog に記録する。**件数を定数で持たない** |

## 7. 申し送り(本タスクで決めないもの)

### タスクへの申し送り

**受諾を必須にする** — 「確認者」だけでは**作成者自身の確認でも通る**(2 周目 `P1`)。
**各行に「受取側の確認者」「受諾コメント / 更新 URL」「受諾後の射程行」を求める。**
**受諾が取れなければ別タスクの起票へ切り替える**(同じ条件を課す)。

| # | 事項 | 送り先(単一) | 受取側の射程行 | 状態 |
| --- | --- | --- | --- | --- |
| 1 | **`cells` 12 / `claim-mutant-map` 7 行 / `boundary-proposal` 1(`check_authz_catalog.py:4406` で機械強制)の再割り当て方式** | **TSK-380** | **射程行が無い** — `ADR-004:46` は **37 経路だけ**を送っており 3 資産群を受け取る射程ではない | **受領範囲の追記と受諾が要る** |
| 3 | **前提「`FR-041` の実装が先」の訂正** — 裁定 `C` が定めたが未実施 | **TSK-380** | `../merge-gate-api-cycle/design.md:343` | **射程内・未実施** |
| 4 | **`SECTION-1.1/paragraph-002` の分類差** — 隣接段落 `paragraph-001` は同じ分類器で `out_of_scope` なのに本段落だけ `auth_claim`。**根拠が不明** | **TSK-317**(PR #3)| `../pg-authz-verification-g2/plan.md:711` `S-10` | **受領範囲を PR #3 の計画へ明記させる** |
| 5 | **`plan.md:147` の「全変更系入口」が条文より広い** — 逐語は「**利用者操作はすべて**」 | **TSK-317**(PR #3)| 同上 | 同上。**検査条件の母集団に効く** |
| 6 | **`TSK-380` との責任境界** — 受諾が取れなければ **「TSK-217 のカードの射程拡張(本計画 2 節)」を `TSK-380` へ移管し、本タスクを取り下げる** | **TSK-380** | `../merge-gate-api-cycle/design.md:343` | **ステップ 1 の停止条件** |
| **7** | **TSK-367 の `plan.md` の不整合の是正** — **調査から始める別タスク**(**件数・内訳は本計画では確定しない**)。**PR #3 の開始条件とする** | **[TSK-367 の計画書の不整合を調査して是正する](https://app.notion.com/p/3dc93b75e68781b28214fe6438591c21)**(**2026-09-16 起票**)| カード本文「開始条件・受取先」節 | **起票済み。担当者・DoD・設計方針を設定済み** |
| **8** | **PR #3 で「owner → 宛先」を単一の写像として扱い、同一 owner に 2 つ以上の宛先があれば fail-closed で red にする** — **exact-set は集合の一致しか見ず、`NFR-012/list_item-003` の 2 宛先を通す**(4 周目 `P0`)| **TSK-317**(PR #3)| `../pg-authz-verification-g2/plan.md:711` `S-10`(2)(受取先の許容集合の検査)| **受領範囲を PR #3 の計画へ明記させる** |

> **`#7` の内訳は書かない**(**5 周目で撤去 — 人間の裁定 2026-09-14**)。
>
> **4 周目で内訳 13 件を列挙したが、5 周目に「14 件目」「15 件目」が出た。**
> **TSK-367 の文書を走査するたびに新しい欠陥が出るため、内訳を持つ限り列挙は収束しない。**
> **これは 3 周目に撤去したはずの構造**(是正の完全性を同じ射程で証明しようとする輪)**が、
> 申し送りの内訳という形で残っていたもの**である。
>
> → **別タスクは「調査から始める」形で起票する。** 件数も内訳も本計画では確定しない。
> **確定できないことが 5 周で実証された。**
>
> **起票時に書くのは 3 点だけ**: ① 対象 = `docs/features/contract-only-runtime-handoff/plan.md`
> ② 性質 = **同じ集合の定義が複数の節に分かれ、節ごとに食い違っている**
> ③ **設計方針 = 是正の完全性を機械で証明する機構を同じ射程に入れない**
> (検証器を作るならさらに別タスク。**是正そのものは人間の逐行確認で閉じる**)

### PO への裁定依頼

| # | 論点 | 判定条件 |
| --- | --- | --- |
| 2 | **「ハーネス」の定義** — リポジトリに定義文が無く(`worklog:66` の 1 箇所のみ)、**運用例 2 件からの帰納**しかない。本タスクは**列挙で暫定運用**し、定義を確定しない | **裁定依頼の URL / 問い / 裁定者 / 結果**を記録する(**実在タスク ID は求めない**)|
