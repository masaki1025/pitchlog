---
feature: legacy-column-mapping
type: verification-sheet
date: 2026-09-08
---

# 突合シート: 可変正本への行番号引用 → 安定 ID(ステップ 1)

**用途**: 計画書 4 節ステップ 1 の `[手動・外部]` 合格条件「**貼り替え後の参照先が同じ条項を
指している**」の逐行確認(判定者: 山田正輝)。**代替統制②(人間の逐行確認)の支援資料**。

**読み方**: 1 表は「貼り替え前の行番号引用が指していた**原典の当該行**」と「貼り替え後の
**安定 ID**」の対応。原典の当該行が安定 ID の射程に入っていれば一致。**行番号引用は改訂で腐るため、
安定 ID 側が正**。2 表は本メモ内の変更行の前後比較。

**機械検査の実測**(計画書 4 節):

| 検査 | 是正前 | 是正後 |
| --- | --- | --- |
| ④ 可変正本への行番号引用 | **222 件** | **0 件** |
| `docs/legacy/` への `記号:行` 参照(残すべきもの) | 342 件 | **342 件** |
| ① 88 列の一意網羅 | 88 / `0..87` 一致 | 88 / `0..87` 一致 |
| `scripts/check_docs_status.py` | rc=0 | **rc=0** |

**④ の 222 件の内訳**(是正前・検査の 3 項に対応):

| 項 | 数え方 | 是正前 | 是正後 |
| --- | --- | --- | --- |
| a) 非 `docs/legacy/` のファイルパス形 `<path>:<行>` | 出現数 | 41 | **0** |
| b) 記号形 `REQ:` / `T250P:` / `T250R:` / `CAND:` / `improvements:` | 出現数 | 129 | **0** |
| c) 継続形 `` `:<行>` ``(`docs/legacy/` 記号の後続を除く) | **行数** | 52 | **0** |
| 合計(= ④ の実測) | | **222** | **0** |

本シートの 1 表は b) 129 件に継続形の帰属分を足した **160 出現 / 89 対**、3 表は a) のうち
`docs/legacy/` 配下へ展開される略記(`baseball-apps.md`)を除いた **40 出現 / 34 対**。
c) は行数計上のため合計とは一致しない(同一行に複数ある継続形が 1 と数えられる)。

**3 表の「貼り替え後」列の注記**: 同列は**参照先の直近見出しを機械抽出した値**であり、
本メモの記述は**番号付き節**(例 `docs/design/sync-protocol.md:219` → 本メモは「同期プロトコル
3-2 節」)を使う。抽出値が `####` の小見出しを指す場合も、指す表・条文は同一。
非 Markdown(`.json` / `.py` / `.ts`)は見出しを持たないため `—` と出る — これらは**キー名・
関数名・テストケース名**へ貼り替えた(2 表の前後比較で確認する)。

## 1. 参照写像(原典の当該行 → 安定 ID)

| # | 出所 | 貼り替え前 | 貼り替え後(安定 ID) | 原典の当該行(冒頭) | 本メモの該当行 |
| --- | --- | --- | --- | --- | --- |
| 1 | 要件書 | `REQ:37` | **変更履歴表** | \| 2.4 \| 2026-09-03 \| **NFR-018 例外表の 2 セルを更新（TSK-312 — TSK-233… | 719 |
| 2 | 要件書 | `REQ:39` | **変更履歴表** | \| 2.6 \| 2026-09-03 \| **付録A へ数値表示の書式 binding を追加する改訂を起案(TSK-27… | 719 |
| 3 | 要件書 | `REQ:41` | **変更履歴表** | \| 2.7 \| 2026-09-06 \| **`NFR-020` の前提不成立時の挙動を条文化する改訂を起案(TSK-32… | 719 |
| 4 | 要件書 | `REQ:102` | **2.2 節** | - **経過時間のTag打刻**（旧「▶Tag」ラップ記録。利用実態がないため廃止。試合の開始時刻はFR-001で自動記録される… | 556 |
| 5 | 要件書 | `REQ:126` | **3章** | - **対戦相手チームレコード**: テナントが対戦相手として登録したチーム・選手のデータ。**テナント内のレコードであり、テナ… | 547 |
| 6 | 要件書 | `REQ:157` | **4.0 節** | - **物理削除しない**: 利用者操作はすべて論理削除（非表示化）まで。物理削除はシステム管理者のDB保守作業のみ | 543 |
| 7 | 要件書 | `REQ:159` | **4.0 節** | - **黙殺しない**: 保存・同期・出力の失敗、スキップ、補正は必ず利用者または管理者に顕在化する（NFR-015） | 543 |
| 8 | 要件書 | `REQ:200` | **FR-002** | - Given 試合進行中 / When ストライクゾーン画像をタップ / Then タップ座標が付録Bの座標系（捕手視点・正規… | 403 |
| 9 | 要件書 | `REQ:405` | **FR-039** | - Given 未登録の対戦相手 / When チーム名を登録 / Then テナント内のレコードとして作成され、FR-001の… | 547 |
| 10 | 要件書 | `REQ:409` | **FR-039** | - **補足**: FR-035のチーム登録（テナント発行）とは別概念。本FRは自テナント内の相手チームレコードを扱う | 547 |
| 11 | 要件書 | `REQ:444` | **FR-023** | #### FR-023: 球種分布の表示 | 604 |
| 12 | 要件書 | `REQ:560` | **FR-030** | - Given 記録済み試合 / When 表示・PDF出力 / Then イニング別経過・打席結果・投手成績に加え、**選手交… | 590 |
| 13 | 要件書 | `REQ:563` | **FR-030** | - Given **移行された旧試合**（FR-038） / When 出力 / Then 交代表記は「不明」を許容する（旧デー… | 456・475・668 |
| 14 | 要件書 | `REQ:566` | **FR-031** | #### FR-031: 毎球データのCSV出力 | 467 |
| 15 | 要件書 | `REQ:570` | **FR-031** | - Given 記録済み試合 / When CSV出力 / Then 88列互換フォーマット（付録D）の全プレイ行がダウンロード… | 475・664 |
| 16 | 要件書 | `REQ:573` | **FR-031** | - **補足**: **記録された全データ（交代履歴等の新概念を含む）がCSVで取り出せること**を本質要件とする。88列に収ま… | 467・473 |
| 17 | 要件書 | `REQ:635` | **FR-034** | \| **1球単位の生記録**（FR-031 の88列CSVを含む） \| **付与によらず不可** \| **404** \| | 476 |
| 18 | 要件書 | `REQ:652` | **FR-034** | \| FR-031 毎球データCSV \| **不可（404）** \| 88列の全プレイ行＝生記録そのもの \| | 476 |
| 19 | 要件書 | `REQ:770` | **FR-038** | #### FR-038: 既存データの一括移行 | 516 |
| 20 | 要件書 | `REQ:775` | **FR-038** | - Given 移行完了 / When 検証 / Then 件数突合・代表集計値の新旧一致・**コース分布チャートの右打者のみ/… | 495・554 |
| 21 | 要件書 | `REQ:776` | **FR-038** | - Given 名前→選手IDの名寄せで確定できない行 / When 移行 / Then 解決レポートに列挙され、人が確認して割… | 555 |
| 22 | 要件書 | `REQ:777` | **FR-038** | - Given 旧データの不整合行（編集バグ時代の産物等） / When 移行 / Then 無検閲で取り込み保全され、警告レポ… | 296・543・690 |
| 23 | 要件書 | `REQ:778` | **FR-038** | - Given 新スキーマの制約（参照整合等）を満たせず取り込めない行 / When 移行 / Then 黙って落とさず**隔離… | 296・543・547・690 |
| 24 | 要件書 | `REQ:779` | **FR-038** | - Given 旧データのセンチネル値（`0`・`""`・`"0"`） / When 移行 / Then **列単位のセンチネル… | 61・75・193・290・299・496・503・553・555・625・746・778 |
| 25 | 要件書 | `REQ:780` | **FR-038** | - Given 旧88列の各列 / When 変換層を実装 / Then **`research/data-layer.md` … | 472・535・590・603 |
| 26 | 要件書 | `REQ:781` | **FR-038** | - Given 走者の責任投手（付録A-2） / When 移行 / Then 「走者が出塁した行の投手氏名を責任投手とし、代走… | 554・782 |
| 27 | 要件書 | `REQ:782` | **FR-038** | - Given 先発オーダー / When 移行 / Then **各試合の最初のプレイ行のスタメンJSONを正**とする（`g… | 457・500・604 |
| 28 | 要件書 | `REQ:783` | **FR-038** | - Given カルテ所見の旧4系統（`pitcher_karte_note`/`batter_karte_note` の pa… | 497 |
| 29 | 要件書 | `REQ:784` | **FR-038** | - Given 移行の失敗・検証不一致 / When 再実行 / Then 新DB側を初期化して何度でもやり直せる。**移行は旧… | 511 |
| 30 | 要件書 | `REQ:785` | **FR-038** | - Given 確認用フラグなし / When 実行 / Then 移行は実行されない（誤実行防止） | 512 |
| 31 | 要件書 | `REQ:786` | **FR-038** | - **補足**: 取り込み時の割り当て: 選手=在籍区分「現役」、旧Kind値=「試合区分+大会名」へマッピング、規則=現行判… | 516・668・689 |
| 32 | 要件書 | `REQ:814` | **NFR-005** | - **要件**: 性能目標（NFR-001〜004）の前提負荷は「1テナントあたり同時3試合（各試合=入力1端末+閲覧10端末… | 546 |
| 33 | 要件書 | `REQ:844` | **NFR-010** | - **要件**: チームは自チームが記録したデータと、**FR-041 の共同分析グループで明示的に付与された範囲の集計データ… | 547 |
| 34 | 要件書 | `REQ:865` | **NFR-014** | - **要件**: DB接続情報・JWT署名鍵・管理者パスワード・チーム初期パスワードはリポジトリに含めず環境変数で管理する | 544 |
| 35 | 要件書 | `REQ:885` | **NFR-018** | - **要件**: ドメイン計算（下記**対象**）は**単一の実装コード**とし、UI別・経路別のコピー実装を作らない。**区… | 545 |
| 36 | 要件書 | `REQ:889` | **NFR-018** | - **(α) クライアント・サーバーの双方で実行するもの**: 状況判定・**入力と表示のための座標変換**（画面座標⇄保存座… | 536・559 |
| 37 | 要件書 | `REQ:890` | **NFR-018** | - **(β) サーバーでのみ実行するもの**（**判定規則**: 対象の計算のうち、**クライアントでの実行を要求する条項が存… | 485・499・537・545 |
| 38 | 要件書 | `REQ:891` | **NFR-018** | - **未分類の扱い**（fail-closed）: いずれの区分にも割り当てられない対象計算が生じた場合は、**当該計算の実装… | 545 |
| 39 | 要件書 | `REQ:893` | **NFR-018** | - **(a) 実装の単一性**: 対象の計算ごとに、**人手で保守する実装は1系統**とする。他方の実行体は生成・変換・埋め込… | 545 |
| 40 | 要件書 | `REQ:894` | **NFR-018** | - **(b) 宣言と機械判定**: 対象ごとに**出所（正本の実装）・すべての実行入口・派生関係**を機械可読に宣言し、CIが… | 545 |
| 41 | 要件書 | `REQ:911` | **NFR-018** | \| `frontend/src/lib/courseInputView.ts` の `COURSE_COORDINATE_SI… | 557 |
| 42 | 要件書 | `REQ:914` | **NFR-018** | - **測定方法**: (b)の宣言に対する機械検査（①**出所と派生物の乖離の検出** ②**差分の検出可能性**＝**宣言さ… | 499 |
| 43 | 要件書 | `REQ:915` | **NFR-018** | - **正解ベクタ**: (α)のうち**状態遷移・成績計上は付録Eのゴールデンケース**、**指標算出は付録Aの固定ベクタ**… | 499 |
| 44 | 要件書 | `REQ:919` | **NFR-019** | - **カテゴリ**: 保守性 ／ **優先度**: Must | 485 |
| 45 | 要件書 | `REQ:920` | **NFR-019** | - **要件**: テストランナーを標準化する（バックエンド=pytest・フロントエンド=Vitest・E2E=Playwri… | 485 |
| 46 | 要件書 | `REQ:924` | **NFR-019** | - **入力と表示のための座標変換**＝**画面座標⇄保存座標の相互変換**（往復して元の値に戻ることを含む）**および**保存… | 483・559 |
| 47 | 要件書 | `REQ:977` | **6.1 節** | \| チーム・選手 \| 選手ID・名前・投打・背番号（任意・変更可）・在籍区分/ラベル \| 1チーム数十名×年次入替 \| … | 543 |
| 48 | 要件書 | `REQ:992` | **6.2 節** | - 「削除」はすべて論理削除（4.0-2） | 543 |
| 49 | 要件書 | `REQ:996` | **6.3 節** | - **「全件」の射程**（v2.0 注記）: 「全件」とは **FR-038 が列挙するデータ種別**（毎球・チーム・選手・ス… | 527・531・787 |
| 50 | 要件書 | `REQ:1038` | **8章** | 5. **移行完了**: FR-038 の検証がすべて一致し（件数・集計値・右/左別チャート・**センチネルNULL件数**・所… | 508・553 |
| 51 | 要件書 | `REQ:1080` | **10章** | \| **NFR-018 の正解ベクタの整備**（付録A固定ベクタ・付録E全表・**終了判定ベクタ**・**付録B-3/B-4座… | 559・783 |
| 52 | 要件書 | `REQ:1117` | **付録A-2** | **走者の責任投手**: 各走者には「出塁させた投手」を責任投手として保持する（データ要件6.1に明記）。**フォースアウトによ… | 554・782 |
| 53 | 要件書 | `REQ:1198` | **付録B-2** | 2. **視点と向き**: **捕手視点の物理座標**。原点は左下、x正方向は捕手から見て右、y正方向は上 | 403 |
| 54 | 要件書 | `REQ:1199` | **付録B-3** | 3. **打者左右による変換なし**: 保存値は打者の左右にかかわらず物理位置。表示時のみ打者側に応じた背景画像を切り替える。*… | 404 |
| 55 | 要件書 | `REQ:1201` | **付録B-5** | 5. **移行の変換式（確定）**: 旧263pxスケール座標からの変換は `x_norm = x_old / 263`、`y_… | 405・408・498・553・557・602 |
| 56 | 要件書 | `REQ:1202` | **付録B-6** | 6. **フィールド領域のシード**: 捕球選手の自動推定（FR-004）と打球方向バケット（A-3）に使う守備位置領域・方向区… | 557 |
| 57 | 要件書 | `REQ:1208` | **付録C** | > システム管理者が管理コンソール（FR-037）で変更できる値。変更は以後の表示に反映され、関連キャッシュ・事前集計は無効化→… | 474 |
| 58 | 要件書 | `REQ:1227` | **付録D-1** | 1. **88列契約の実体**: 88列の**列名・順序・型**は現行リポジトリ `config.py` の `COLUMN_N… | 13・471・472・516・544・558・569・590・744 |
| 59 | 要件書 | `REQ:1229` | **付録D-3** | 3. **CSV出力**: 88列互換の本体は不変とし、88列に収まらない新概念（交代履歴等）は**別ファイル方式（例: `su… | 473・478・601・665・699 |
| 60 | 要件書 | `REQ:1236` | **付録D-4** | ※値の表記自体は旧版と同一（88列互換の値レベルの互換性を保つ）。系統はチームが変更可能（4.0-3）。打撃結果・作戦・打球情報… | 349 |
| 61 | 要件書 | `REQ:1238` | **付録D-6** | 6. **名寄せのスコープ規則**: 移行時の名前→選手IDの名寄せは**チーム（対戦相手チームレコード）単位でスコープ**し、… | 547 |
| 62 | 改善台帳 | `improvements:59` | **I-5** | - **pitchlog**: 内部スキーマは正規化・再設計（選手ID参照、項目追加が局所変更で済む形）。88列はCSV出力・既… | 666 |
| 63 | 改善台帳 | `improvements:61` | **I-5** | - **確定（v1.1〜1.4で上書き）**: 88列本体は不変とし、新概念は**別ファイル（サイドカー）方式が正**。サイドカ… | 473・601・666 |
| 64 | 改善台帳 | `improvements:157` | **I-17** | - **なぜ改善が必要か**: 座標の意味が特定の画像サイズに縛られ、UI刷新・画像差し替えで過去データが無言で歪む。暗黙規約は… | 410 |
| 65 | 改善台帳 | `improvements:158` | **I-17** | - **pitchlog**: 座標系を付録で固定（捕手視点の物理座標・正規化0〜1・保存時の左右変換なし・表示時のみ左右対応・… | 408 |
| 66 | 候補案(TSK-248) | `CAND:1423` | **12-1 節** | - **互換出力(FR-031)では列名・順序を維持する** — 88 列は不変の契約であり、列名が中身と乖離した列も | 142・667 |
| 67 | 候補案(TSK-248) | `CAND:1444` | **12-1 節** | \| 17-19 \| 打席の継続 / イニング継続 / 試合継続 \| **導出** \| **旧は DB に試合終了フラグが… | 131 |
| 68 | 候補案(TSK-248) | `CAND:1449` | **12-1 節** | \| 29-31 \| 作戦 / 作戦2 / 作戦結果 \| プレイ行(語彙参照) \| `"0"` は未入力のセンチネル → … | 156 |
| 69 | 候補案(TSK-248) | `CAND:1453` | **12-1 節** | \| 35 \| 捕手 \| プレイ行(捕手 = 選手 ID) \| **旧は全球記録されているが未使用**。新では保持する \… | 173 |
| 70 | 候補案(TSK-248) | `CAND:1464` | **12-1 節** | \| 52-53 \| 牽制の種類 / 牽制詳細 \| プレイ行 \| **旧は未使用**。新では保持する \| | 172 |
| 71 | 候補案(TSK-248) | `CAND:1468` | **12-1 節** | \| 57-58 \| プレス / 偽走 \| プレイ行 \| **旧は未使用**だが列連続性のため保持 \| | 172 |
| 72 | 候補案(TSK-248) | `CAND:1472` | **12-1 節** | \| 63-64 \| 打者登録名 / 打者番号 \| **移行時に使用** \| 名寄せの解決キー。新スキーマには残さない \… | 142・663 |
| 73 | 候補案(TSK-248) | `CAND:1474` | **12-1 節** | \| 71 \| 投手番号 \| **移行時に使用** \| 同上 \| | 142・663 |
| 74 | 候補案(TSK-248) | `CAND:1475` | **12-1 節** | \| 72 \| 入力項目 \| **保持のみ** \| 入力経路の識別値。互換出力のため \| | 142・667 |
| 75 | 候補案(TSK-248) | `CAND:1483` | **12-1 節** | > **守備番号と解決済み選手 ID を混同しない(3 周目 P1-9 の是正)**: 当初は列 47 を「出場区間経由で選手 … | 617 |
| 76 | 候補案(TSK-248) | `CAND:1486` | **12-1 節** | > これは REQ:760「**センチネルだけを NULL 化し、NULL 件数を旧側条件と突合する**」に反する | 555 |
| 77 | 候補案(TSK-248) | `CAND:1491` | **12-1 節** | > - **NULL 件数の突合は「センチネル由来の NULL」だけを数える**(解決不能由来の NULL は別欄) | 555・617 |
| 78 | 候補案(TSK-248) | `CAND:1527` | **12-3 節** | \| **移行の行識別子** \| **旧行の主キー**(取得できない場合は **取り込みバッチ ID + 旧行の読み出し順序*… | 703 |
| 79 | 候補案(TSK-248) | `CAND:1539` | **12-3 節** | \| **移行用の記録権世代** \| **6-1 節の記録権世代表に同居させる**が、**種別を `migration` とし… | 703 |
| 80 | 候補案(TSK-248) | `CAND:1540` | **12-3 節** | \| **イベント連番** \| 旧行の**行識別子の順序**(12-3 節)で連番を振る。これが**論理順序の正**になる(P… | 688・705・769 |
| 81 | TSK-250 計画 | `T250P:4` | **frontmatter** | 承認: 済(2026-09-04・山田正輝) # 初回承認 2026-08-31。2026-09-04 の射程変更(A の申し送… | 636 |
| 82 | TSK-250 計画 | `T250P:38` | **1 節「射程の決定」** | 複数入っており、後段が前段の成果を壊す」— を指摘され、**2 周目の指摘の多くは 1 周目の是正が生んだ新しい欠陥** | 634 |
| 83 | TSK-250 計画 | `T250P:40` | **1 節「射程の決定」** | **独立した確定単位を先行タスクへ切り出す**。 | 634 |
| 84 | TSK-250 計画 | `T250P:46` | **1 節「射程の決定」** | \| **C.** [TSK-271](https://app.notion.com/p/3cc93b75e68781dba1f… | 639・694・745 |
| 85 | TSK-250 計画 | `T250P:48` | **1 節「射程の決定」** | **マージ済みを本タスクの開始条件とする**(人間の裁定 2026-08-31)。 | 634 |
| 86 | TSK-250 計画 | `T250P:67` | **1 節「先行タスク A のマージ実績」** | \| PR / merge commit \| **PR #42** / **`fdda374`**(`feature/doc-… | 636 |
| 87 | TSK-250 計画 | `T250P:166` | **2 節「やらないこと」** | \| 移行のファンアウトの実測・カットオーバー日・`game_lineup_snapshot` の帰属・サイドカーの列定義 \|… | 694 |
| 88 | TSK-250 計画 | `T250P:169` | **2 節「やらないこと」** | \| **要件書の改訂** \| 要件改訂タスク \| 管理者操作ログの Must/Should 矛盾は**計画承認前の裁定**… | 676 |
| 89 | TSK-250 計画 | `T250P:319` | **4 節「実装ステップ」** | \| 26 \| **`core-areas.json` の登録(本タスク分)** — `data-migration` の `… | 714 |

**合計 89 対**(出現回数の合計は 160 件)。

## 3. ファイルパス形の写像(可変正本・一時作業域)

| # | 貼り替え前 | 貼り替え後(安定 ID) | 当該行(冒頭) | 本メモの該当行 |
| --- | --- | --- | --- | --- |
| 1 | `.claude/agents/legacy-analyst.md:14` | **—** | - **補助的な物理構造の調査起点**: `docs/legacy/baseball-scoring-db-struct… | 583 |
| 2 | `.claude/core-areas.json:2` | **—** | "description": "コア領域の機械可読定義(設計書 6.3 / ADR-001 / 敵対レビュー P1-11… | 715 |
| 3 | `charts/batting/calc_stats.py:75` | **—(リポジトリ外)** | — | 406 |
| 4 | `contracts/authz/oracle-seal.lock.json:1` | **—** | { | 734 |
| 5 | `dev-harness-design-2026-08-07.md:365` | **「6.3 レビュー体制（論点B 解決 2026-08-07 — 確定）」節** | \| データ移行 \| **FR-038 の全体**: 88列⇄新スキーマ**変換層をモジュール単位で**(移行/CSV… | 669 |
| 6 | `docs/README.md:35` | **「参照(正本ではない)」節** | - `legacy/` — 旧システム資料(**版固定・変更禁止**。88列の正は `legacy/research/d… | 13・569・584・590 |
| 7 | `docs/design/sync-protocol.md:139` | **「2-6. 識別子参照規約」節** | - **版固定アーカイブ `docs/legacy/` は行番号で引く。** AGENTS.md 絶対規則 3 により変… | 581 |
| 8 | `docs/design/sync-protocol.md:219` | **「(b) 対象外の 38 件(**理由を付けて明示的に外す。件数は (a) 22 + (b) 38 = 60 で一致する**)」節** | \| **データ移行の領域**(移行仕様タスク) \| P-41・P-42・P-43・P-44・P-45・P-46 \| | 694 |
| 9 | `docs/design/sync-protocol.md:255` | **「4-2. 識別子は 5 種を混ぜない(論点 13)」節** | **サイドカー結合キーはこの 3 つの合成で成立する**: 付録D の第 3 項([付録D/D-3](../requir… | 700 |
| 10 | `docs/design/sync-protocol.md:451` | **「4-7. 本節の禁止事項照合」節** | \| **P-02** \| 88 列に列を足して新概念を収める \| **該当なし** \| サイドカー結合キーを *… | 606 |
| 11 | `docs/design/sync-protocol.md:465` | **「5-1. D1 と D2 の役割を分離する」節** | **同期連番(D1)は同期の順序・欠落の判定と undo の逆順に用い、論理順序(D2)はプレイの投影・出場区間の境界・… | 688 |
| 12 | `docs/development/dev-harness-design-2026-08-07.md:560` | **「8.5 agents（サブエージェント — Claude 側の調査部隊）」節** | \| `legacy-analyst` \| 旧システム（Baseball_Scoring）の実挙動・データ構造の事実確… | 583 |
| 13 | `docs/features/doc-check-multi-doc/design.md:290` | **「7. 参照の分類(`reference-class`)」節** | `reference_policy.rules` は順序付き。条件: `source_section`(任意)/ `ta… | 582 |
| 14 | `docs/features/harness-design-review/research.md:59` | **「B. 裁定済み指摘 — P2(節更新・次回改訂で処理)」節** | \| P2-5 \| 版固定アーカイブ内部矛盾: テーブル 12 対 13・CASCADE 有無・接続プール(data-… | 612 |
| 15 | `docs/features/harness-design-review/research.md:105` | **「未解決・申し送り」節** | 4. **版固定アーカイブ内部不整合の優先規定**(P2-5)— req:25 の precedence(88 列限定)… | 591・792 |
| 16 | `docs/features/product-data-model-design/plan.md:159` | **「5. DoD(受け入れ基準)」節** | - [ ] **全 88 列の行き先**が埋まっている(「移行時に導出」「保持のみ」を含む)。**互換出力の列名・順序の… | 671 |
| 17 | `docs/features/product-data-model-design/research.md:186` | **「3-2. 再提案してはいけない案(安定 ID 付き)」節** | **この表が禁止事項の正**。`design.md` は**本表の ID を参照するだけで本文を複製しない**(二重管理… | 567 |
| 18 | `docs/features/sync-event-contract/research.md:259` | **「8. 旧資料内部の矛盾(本タスクでは裁定しない)」節** | **これは先行タスク C(TSK-271)の `DM-LX-01` そのもの**であり、本タスクは裁定しない(イベント契… | 98 |
| 19 | `docs/features/sync-protocol-canonical/plan.md:60` | **「やること」節** | 2. **既知欠陥 40 件の全件是正** — **`SP-01`〜`SP-20`(20)/ `CI-01`〜`CI-1… | 570 |
| 20 | `docs/features/sync-server-apply/research.md:20` | **「結論(要約)」節** | 4. **TSK-250 の完了を開始条件にすると γ は長期停止する。** TSK-250 は承認済み(計画レビュー … | 637 |
| 21 | `docs/features/sync-server-apply/research.md:348` | **「本調査で新たに見つかった論点」節** | \| 2 \| **移行で取り込む旧プレイが適用経路 P1〜P5 を通るのかを規定した記述が無い**。88 列は入出力互… | 695・796 |
| 22 | `docs/improvements-from-baseball-scoring.md:156` | **「I-17: 座標系（コース・打球位置）の明文化と正規化」節** | - **現行**: タップ座標を画像ピクセル準拠の263スケールで保存。ゾーン枠の範囲（53〜210）がマジックナンバー… | 406 |
| 23 | `docs/requirements/requirements-draft-pitchlog.md:442` | **「D-44: v1.7 — 7レンズWorkflow調査+敵対的検証の反映（2026-07-24）」節** | - **最大の発見**: 旧リポ `docs/research/` に9本の調査資料（1,989行）が存在。特に `da… | 580 |
| 24 | `docs/worklog/2026-08-21-product-data-model-design.md:138` | **「敵対レビュー 4 周と「閉じる」判断(2026-08-22〜23)」節** | **PR #23 は敵対レビューで 4 周にわたり否決され、指摘 37 件をすべて反映した。** P0 は 2 周目以降… | 632 |
| 25 | `docs/worklog/2026-08-31-pg-authz-verification.md:17` | **「決定」節** | - **本タスクを先行させる**(人間の判断 2026-08-31)。先行 3 本のうち**スキーマ実装を実際にブロック… | 635 |
| 26 | `docs/worklog/2026-09-02-decision-sheet-ruling.md:135` | **「ステップ 7 — 成果物レビュー(敵対・台帳 v1.2 は対象外)」節** | - **2 周目(差分)**: 否決 P0×0/**P1×1** — **採否記録(採用)**: 設計書 7.2 への … | 584 |
| 27 | `docs/worklog/2026-09-04-sync-event-contract.md:29` | **「着手に至る経緯(2026-09-04 の判断)」節** | B は続き(第 2 群・第 3 群)を **TSK-317** として起票済み、C(TSK-271)は未着手。 | 636 |
| 28 | `frontend/src/lib/sync/prohibitions.spec.ts:1078` | **`P-02: サイドカー結合キーを試合・D4・D1の3要素だけで作る`** | it('P-02: サイドカー結合キーを試合・D4・D1の3要素だけで作る', () => { | 608 |
| 29 | `plan.md:35` | **「やること」節** | - ③ 8.5 legacy-analyst 典拠欄へ `docs/legacy/baseball-scoring-db… | 591 |
| 30 | `requirements-draft-pitchlog.md:443` | **「D-44: v1.7 — 7レンズWorkflow調査+敵対的検証の反映（2026-07-24）」節** | - **P1（移行に集中）**: ①0センチネルのNULL変換規則+専用の件数突合（座標0が有効な隅座標に化ける・両辺同… | 602 |
| 31 | `scripts/check_design_propagation.py:726` | **`check_citation_format`** | 版固定アーカイブ ``docs/legacy/`` への行番号引用は逐語証拠として許容する。 | 581 |
| 32 | `scripts/check_design_propagation.py:986` | **`check_reference_classes`** | if matched_rule["role"] != "normative": | 582 |
| 33 | `sync-protocol.md:465` | **「5-1. D1 と D2 の役割を分離する」節** | **同期連番(D1)は同期の順序・欠落の判定と undo の逆順に用い、論理順序(D2)はプレイの投影・出場区間の境界・… | 706・769 |
| 34 | `tests/test_check_design_propagation.py:4589` | **`test_reference_class_rejects_unapproved_normative_targets`** | """feature・worklog・legacyをnormativeにすると未承認先として拒否する。""" | 582 |

**合計 34 対**(出現回数の合計は 40 件)。**`docs/legacy/` 配下は対象外**(版固定のため行番号のまま維持)。

## 2. 変更行の前後比較

<!-- 左が貼り替え前、右が貼り替え後。行番号は貼り替え前の本文基準 -->

### 差分 1(前 9〜10 行)

**前**:

```
**典拠の記法**: 本メモは調査記録なので `ファイル:行` で典拠を書く(設計書 8.5 の要求)。正本
(`docs/design/` 等)へ反映する際は行番号引用が機構で禁止されているため、節アンカー形式へ貼り替える。
```

**後**:

```
**典拠の記法**: **版固定アーカイブ `docs/legacy/` への典拠だけを `記号:行` で書く**(版が固定され
行番号が腐らないため — 設計書 8.5 は「`ファイルパス:行` **または節番号**」を許容し、同期プロトコル
2-6 節が「版固定アーカイブは行番号で引き、可変な正本本文の引用と扱いを分ける」と定める)。
**可変正本(要件書・設計書・ADR・改善台帳)と一時作業域(候補案・他タスクの計画/調査)への典拠は
安定 ID**(FR/NFR・付録・節番号・決定 ID)**で書く** — 行番号は改訂で腐り、機構
(`scripts/check_design_propagation.py` の `check_citation_format`)も可変正本への行番号引用を禁じている。
```

### 差分 2(前 13〜13 行)

**前**:

```
`DL` = `docs/legacy/research/data-layer.md`(全 423 行 — **88 列の意味の正**。典拠は `docs/README.md:35`・要件書 付録D-1 `REQ:1227`・`docs/legacy/research/README.md:36`。**AGENTS.md にこの規定は無い** — 同書は「版固定・変更禁止」だけを定める)/
```

**後**:

```
`DL` = `docs/legacy/research/data-layer.md`(全 423 行 — **88 列の意味の正**。典拠は `docs/README.md`(索引の `docs/legacy/` 行)・要件書 付録D-1・`docs/legacy/research/README.md:36`。**AGENTS.md にこの規定は無い** — 同書は「版固定・変更禁止」だけを定める)/
```

### 差分 3(前 22〜23 行)

**前**:

```
`CAND` = `docs/features/product-data-model-design/design.md`(候補案 1866 行。88 列対応表は 12-1 節)/
`REQ` = `docs/requirements/requirements-pitchlog-2026-07-22.md` /
```

**後**:

```
`BA` = `docs/legacy/research/baseball-apps.md` /
`CAND` = `docs/features/product-data-model-design/design.md`(候補案。88 列対応表は 12-1 節 — **節で引く**)/
`REQ` = `docs/requirements/requirements-pitchlog-2026-07-22.md`(**FR/NFR・付録・節番号で引く**)/
```

### 差分 4(前 61〜61 行)

**前**:

```
   `REQ:779` の NULL 件数突合の前提が立たない。**残りは本番 Supabase の実データ確認**を要する。
```

**後**:

```
   `FR-038` の NULL 件数突合の前提が立たない。**残りは本番 Supabase の実データ確認**を要する。
```

### 差分 5(前 75〜75 行)

**前**:

```
   `REQ:779`(`x<=0` を NULL 件数に算入)で食い違い、**負値が 1 行でも存在すると V2 の件数突合が
```

**後**:

```
   `FR-038`(`x<=0` を NULL 件数に算入)で食い違い、**負値が 1 行でも存在すると V2 の件数突合が
```

### 差分 6(前 98〜98 行)

**前**:

```
ヒットは `docs/features/sync-event-contract/research.md:259` の 1 件のみで、そこも `DM-LX-01` を
```

**後**:

```
ヒットは `docs/features/sync-event-contract/research.md` 8 節 の 1 件のみで、そこも `DM-LX-01` を
```

### 差分 7(前 131〜131 行)

**前**:

```
→ `CAND:1444`「旧は DB に試合終了フラグがなく列 19 にのみ現れる」は **`SS:131` を根拠に読めば正しい**
```

**後**:

```
→ 候補案 12-1 節「旧は DB に試合終了フラグがなく列 19 にのみ現れる」は **`SS:131` を根拠に読めば正しい**
```

### 差分 8(前 142〜142 行)

**前**:

```
| 候補案内部の非対称 | `CAND:1472`-`:1474`「移行時に使用 … 新スキーマには残さない」vs `CAND:1475` 列 72「**保持のみ** \| 入力経路の識別値。互換出力のため」/ 契約側は `CAND:1423`「互換出力(FR-031)では列名・順序を維持する — 88 列は不変の契約」 |
```

**後**:

```
| 候補案内部の非対称 | 候補案 12-1 節-候補案 12-1 節「移行時に使用 … 新スキーマには残さない」vs 候補案 12-1 節 列 72「**保持のみ** \| 入力経路の識別値。互換出力のため」/ 契約側は 候補案 12-1 節「互換出力(FR-031)では列名・順序を維持する — 88 列は不変の契約」 |
```

### 差分 9(前 156〜156 行)

**前**:

```
| `LG-08` | **成立、かつ `IS` は逆を示す**。`DL:182` 列 30 の値例に **`"0"` は無い**(盗塁系/バント系/エンドラン系の実値のみ)。`IS:82`・`:84`・`:86` の既定値は**盗塁 / バント / HAR**(`"0"` ではない)。対照的に列 29 は `DL:181`「"0"/"盗塁"/…」・`IS:81` 既定 `0`、列 31 は `DL:183`「"0"/"成功"/…」・`IS:83` 既定 `0`。→ **`CAND:1449` が 29-31 を一括して「`"0"` は未入力のセンチネル」とするのは列 30 について裏がない** |
```

**後**:

```
| `LG-08` | **成立、かつ `IS` は逆を示す**。`DL:182` 列 30 の値例に **`"0"` は無い**(盗塁系/バント系/エンドラン系の実値のみ)。`IS:82`・`:84`・`:86` の既定値は**盗塁 / バント / HAR**(`"0"` ではない)。対照的に列 29 は `DL:181`「"0"/"盗塁"/…」・`IS:81` 既定 `0`、列 31 は `DL:183`「"0"/"成功"/…」・`IS:83` 既定 `0`。→ **候補案 12-1 節 が 29-31 を一括して「`"0"` は未入力のセンチネル」とするのは列 30 について裏がない** |
```

### 差分 10(前 172〜173 行)

**前**:

```
→ `CAND:1464`(列 52-53「旧は未使用」)・`CAND:1468`(列 57-58「旧は未使用」)は**分析側の未使用を
入力・格納の未使用と混同**している。一方 `CAND:1453`(列 35 捕手「旧は全球記録されているが未使用」)は
```

**後**:

```
→ 候補案 12-1 節(列 52-53「旧は未使用」)・候補案 12-1 節(列 57-58「旧は未使用」)は**分析側の未使用を
入力・格納の未使用と混同**している。一方 候補案 12-1 節(列 35 捕手「旧は全球記録されているが未使用」)は
```

### 差分 11(前 193〜193 行)

**前**:

```
これが `REQ:779`「新DBのNULL件数＝旧DBで該当条件に一致する行数」の基礎条件である。
```

**後**:

```
これが `FR-038`「新DBのNULL件数＝旧DBで該当条件に一致する行数」の基礎条件である。
```

### 差分 12(前 290〜290 行)

**前**:

```
とくに **53・54 は「該当イベントがない行の値」が不明**で、`REQ:779` の NULL 件数突合の前提が立たない。
```

**後**:

```
とくに **53・54 は「該当イベントがない行の値」が不明**で、`FR-038` の NULL 件数突合の前提が立たない。
```

### 差分 13(前 296〜296 行)

**前**:

```
未知長・不正 JSON は**原本保全・隔離**へ回す(`REQ:777`・`REQ:778`)。
```

**後**:

```
未知長・不正 JSON は**原本保全・隔離**へ回す(`FR-038`・`FR-038`)。
```

### 差分 14(前 299〜299 行)

**前**:

```
→ **`REQ:779`「列単位のセンチネル規則」を旧資料だけでは 88 列全数について確定できない。**
```

**後**:

```
→ **`FR-038`「列単位のセンチネル規則」を旧資料だけでは 88 列全数について確定できない。**
```

### 差分 15(前 349〜349 行)

**前**:

```
→ **`REQ` 側は「値の表記自体は旧版と同一」**(`REQ:1236`)と語彙初期値を付録 D-4 で固定しており、
```

**後**:

```
→ **`REQ` 側は「値の表記自体は旧版と同一」**(`付録D-4`)と語彙初期値を付録 D-4 で固定しており、
```

### 差分 16(前 403〜406 行)

**前**:

```
| **規範上の視点** | **捕手視点の物理座標。原点は左下、x 正方向は捕手から見て右、y 正方向は上** | `REQ:1198`(付録 B-2 — 逐語)。`REQ:200`(FR-002)も「付録Bの座標系(**捕手視点・正規化0〜1**)」 |
| **左右反転するか** | **しない。** 「保存値は打者の左右にかかわらず物理位置」「**左打者をミラー保存しない**」「内角/外角は保存座標と打席左右の組み合わせから導出」 | `REQ:1199`(付録 B-3 — 逐語) |
| **移行変換式** | **`x_norm = x_old / 263`(x は恒等)**、**`y_norm = (263 − y_old) / 263`(y のみ反転)** | `REQ:1201`(付録 B-5 — 「**確定**」と明記) |
| **旧側の視点は本当に不明か** | **`docs/legacy/` には記述なし(legacy-analyst が全 legacy を grep して確認)。しかし改善台帳 I-17 が旧コードを典拠に事実認定している** | `docs/improvements-from-baseball-scoring.md:156`「「**保存値は捕手視点の物理座標・打者左右で変換なし**」という規約は**全経路で一貫している**が**どこにも文書化されていない**(入力UIは左右別画像のため、打者基準保存と誤解しやすい)」(典拠として旧コード `charts/batting/calc_stats.py:75` ほかを名指し) |
```

**後**:

```
| **規範上の視点** | **捕手視点の物理座標。原点は左下、x 正方向は捕手から見て右、y 正方向は上** | `付録B-2`(付録 B-2 — 逐語)。`FR-002`(FR-002)も「付録Bの座標系(**捕手視点・正規化0〜1**)」 |
| **左右反転するか** | **しない。** 「保存値は打者の左右にかかわらず物理位置」「**左打者をミラー保存しない**」「内角/外角は保存座標と打席左右の組み合わせから導出」 | `付録B-3`(付録 B-3 — 逐語) |
| **移行変換式** | **`x_norm = x_old / 263`(x は恒等)**、**`y_norm = (263 − y_old) / 263`(y のみ反転)** | `付録B-5`(付録 B-5 — 「**確定**」と明記) |
| **旧側の視点は本当に不明か** | **`docs/legacy/` には記述なし(legacy-analyst が全 legacy を grep して確認)。しかし改善台帳 I-17 が旧コードを典拠に事実認定している** | 改善台帳 `I-17`「「**保存値は捕手視点の物理座標・打者左右で変換なし**」という規約は**全経路で一貫している**が**どこにも文書化されていない**(入力UIは左右別画像のため、打者基準保存と誤解しやすい)」(典拠として旧コード `charts/batting/calc_stats.py`(旧リポジトリ) ほかを名指し) |
```

### 差分 17(前 408〜408 行)

**前**:

```
| 検証手段 | **右打者のみ / 左打者のみに分けた新旧チャート突合**(全体突合では左打者分の反転が埋もれる) | `REQ:1201`(付録 B-5 末尾)・`improvements:158`(I-17 の pitchlog 欄)・`DB:305` |
```

**後**:

```
| 検証手段 | **右打者のみ / 左打者のみに分けた新旧チャート突合**(全体突合では左打者分の反転が埋もれる) | `付録B-5`(付録 B-5 末尾)・改善台帳 `I-17`(I-17 の pitchlog 欄)・`DB:305` |
```

### 差分 18(前 410〜410 行)

**前**:

```
**`docs/legacy/` の視点記述 0 件を「未確定」と読んではならない**理由: `improvements:157` が
```

**後**:

```
**`docs/legacy/` の視点記述 0 件を「未確定」と読んではならない**理由: 改善台帳 `I-17` が
```

### 差分 19(前 415〜415 行)

**前**:

```
`baseball-apps.md:46`・`:82`・`:123`(他社アプリ調査)であり、**旧 Tsukuba PSS の視点を述べたものは 0 件**。
```

**後**:

```
`BA:46`・`:82`・`:123`(他社アプリ調査)であり、**旧 Tsukuba PSS の視点を述べたものは 0 件**。
```

### 差分 20(前 456〜457 行)

**前**:

```
整合させる必要がある。これは `REQ:563`(FR-030)「移行された旧試合 / Then 交代表記は「不明」を許容する」
および `REQ:782`「両者が食い違う試合は「**交代のあった試合**」として警告レポートに列挙する」に効く。
```

**後**:

```
整合させる必要がある。これは `FR-030`(FR-030)「移行された旧試合 / Then 交代表記は「不明」を許容する」
および `FR-038`「両者が食い違う試合は「**交代のあった試合**」として警告レポートに列挙する」に効く。
```

### 差分 21(前 467〜467 行)

**前**:

```
`REQ:566`-`:573`(FR-031・**Must**)の受け入れ基準に**「列名・列順」の語は無い**。契約の実体は付録 D への委任。
```

**後**:

```
`FR-031`-`FR-031`(FR-031・**Must**)の受け入れ基準に**「列名・列順」の語は無い**。契約の実体は付録 D への委任。
```

### 差分 22(前 471〜478 行)

**前**:

```
| 列名・順序・型の維持 | **要求している** | `REQ:1227`(付録 D-1)「88列の**列名・順序・型**は現行リポジトリ `config.py` の `COLUMN_NAMES` を正とし、その固定コピー(機械可読なスキーマ定義ファイル)を pitchlog リポジトリに収載してバージョン管理する」 |
| 列名と中身の乖離列も列名のまま出す | **要求している(列名契約と意味の二層)** | `REQ:1227`「**各列の「実際の意味」は `../legacy/research/data-layer.md` の全数表を正とし**、スキーマ定義ファイルに意味欄(実際の中身・旧コードの参照箇所・新スキーマの行き先・変換規則)を含める(v1.7。**列名だけを契約にすると移行で意味が失われる**)」/ `REQ:780` |
| 本体の不変性 | **要求している** | `REQ:1229`(付録 D-3)「**88列互換の本体は不変とし**、88列に収まらない新概念(交代履歴等)は**別ファイル方式(例: `substitutions.csv`)を正**として出力する」/ `REQ:573`「**88列本体は不変を保つ**」/ 改善台帳 `improvements:61` |
| 契約値の可変性 | **固定値(設定不可)** | `REQ:1208`「**88列などの契約値**——は設定可能ではなく、**要件の固定値**である」 |
| 対象範囲 | **全記録済み試合。移行試合の免除条項は存在しない** | `REQ:570`「Given **記録済み試合** / When CSV出力」。**免除は FR-030 にのみある** — `REQ:563`「Given **移行された旧試合**(FR-038) / When 出力 / Then 交代表記は「不明」を許容する」。**FR-031 に同種の免除は無い** |
| 共有経路 | **対象外(付与によらず 404)** | `REQ:635`・`REQ:652`「FR-031 毎球データCSV \| **不可(404)** \| 88列の全プレイ行＝生記録そのもの」 |
| 値の一致度 | **要件に記載なし** — バイト一致・列単位一致・往復一致を課す条文は**存在しない**(全文走査) | 要求は ① 列名・順序・型の一致(`:1227`)② 語彙の値表記が旧と同一(`:174`・`:1236`)③ BOM 付き UTF-8 と先頭 5 文字の無害化(`:571`・`:572`・NFR-023 `:962`)の 3 点のみ |
| **本体 88 列の NULL 表現** | **要件に記載なし** — `REQ:1229` の直列化規則(NULL=空文字・日時書式・小数)は**サイドカーの契約**として書かれ、本体へは「エンコーディング/無害化は本体と同一」と逆方向にのみ言及 | 不足として 6-5 に計上 |
```

**後**:

```
| 列名・順序・型の維持 | **要求している** | `付録D-1`(付録 D-1)「88列の**列名・順序・型**は現行リポジトリ `config.py` の `COLUMN_NAMES` を正とし、その固定コピー(機械可読なスキーマ定義ファイル)を pitchlog リポジトリに収載してバージョン管理する」 |
| 列名と中身の乖離列も列名のまま出す | **要求している(列名契約と意味の二層)** | `付録D-1`「**各列の「実際の意味」は `../legacy/research/data-layer.md` の全数表を正とし**、スキーマ定義ファイルに意味欄(実際の中身・旧コードの参照箇所・新スキーマの行き先・変換規則)を含める(v1.7。**列名だけを契約にすると移行で意味が失われる**)」/ `FR-038` |
| 本体の不変性 | **要求している** | `付録D-3`(付録 D-3)「**88列互換の本体は不変とし**、88列に収まらない新概念(交代履歴等)は**別ファイル方式(例: `substitutions.csv`)を正**として出力する」/ `FR-031`「**88列本体は不変を保つ**」/ 改善台帳 改善台帳 `I-5` |
| 契約値の可変性 | **固定値(設定不可)** | `付録C`「**88列などの契約値**——は設定可能ではなく、**要件の固定値**である」 |
| 対象範囲 | **全記録済み試合。移行試合の免除条項は存在しない** | `FR-031`「Given **記録済み試合** / When CSV出力」。**免除は FR-030 にのみある** — `FR-030`「Given **移行された旧試合**(FR-038) / When 出力 / Then 交代表記は「不明」を許容する」。**FR-031 に同種の免除は無い** |
| 共有経路 | **対象外(付与によらず 404)** | `FR-034`・`FR-034`「FR-031 毎球データCSV \| **不可(404)** \| 88列の全プレイ行＝生記録そのもの」 |
| 値の一致度 | **要件に記載なし** — バイト一致・列単位一致・往復一致を課す条文は**存在しない**(全文走査) | 要求は ① 列名・順序・型の一致(付録D-1)② 語彙の値表記が旧と同一(要件書 4.0 節・付録D-4)③ BOM 付き UTF-8 と先頭 5 文字の無害化(FR-031・NFR-023)の 3 点のみ |
| **本体 88 列の NULL 表現** | **要件に記載なし** — `付録D-3` の直列化規則(NULL=空文字・日時書式・小数)は**サイドカーの契約**として書かれ、本体へは「エンコーディング/無害化は本体と同一」と逆方向にのみ言及 | 不足として 6-5 に計上 |
```

### 差分 23(前 483〜483 行)

**前**:

```
**(α) の画面座標⇄保存座標に限られる**: `REQ:924`(NFR-019(a))「**入力と表示のための座標変換**＝画面座標⇄
```

**後**:

```
**(α) の画面座標⇄保存座標に限られる**: `NFR-019`(NFR-019(a))「**入力と表示のための座標変換**＝画面座標⇄
```

### 差分 24(前 485〜485 行)

**前**:

```
(`REQ:890`)、かつ `REQ:919`-`:920`「**NFR-018 の区分(β)は本テストの対象としない**」。
```

**後**:

```
(`NFR-018`)、かつ `NFR-019`-`NFR-019`「**NFR-018 の区分(β)は本テストの対象としない**」。
```

### 差分 25(前 495〜500 行)

**前**:

```
| V1 | 「件数突合・代表集計値の新旧一致・**コース分布チャートの右打者のみ/左打者のみ別の突合**がすべて一致する。**突合の基準**: 代表集計値の一覧(**試合数・プレイ行数・選手数・チーム別通算打率・通算投球数・被安打数・本塁打数を最低限含む**)は**移行計画で確定し**、「移行後の新DBから付録A定義で算出した値」と「旧DBの生データから付録A定義で再計算した値」を**完全一致で比較する**」 | `REQ:775` |
| V2 | 「「**新DBのNULL件数＝旧DBで該当条件(`x<=0`・`球速<=0`・氏名`'0'` 等)に一致する行数**」の件数突合を含める(…**両辺を同じ変換で計算する集計値突合では検出できないため専用の検証項目とする**)」 | `REQ:779` |
| V3 | 「**所見件数の新旧突合を検証に含める**」 | `REQ:783` |
| V4 | 「**テストベクタ**(変換前後の具体値の組)をゴールデンフィクスチャとして収載し、変換後のコース分布チャートが**右打者のみ/左打者のみに分けて**新旧一致することを移行検証(FR-038)に含める」 | `REQ:1201` |
| V5 | 「⑥は付録B-5の変換式に対する変換ベクタ+FR-038の移行突合 / ⑧はFR-038の導出規則に対する単体テスト+同条の移行突合」 | `REQ:890`・`:914`・`:915` |
| V6 | 「両者が食い違う試合は「**交代のあった試合**」として警告レポートに列挙する」 | `REQ:782` |
```

**後**:

```
| V1 | 「件数突合・代表集計値の新旧一致・**コース分布チャートの右打者のみ/左打者のみ別の突合**がすべて一致する。**突合の基準**: 代表集計値の一覧(**試合数・プレイ行数・選手数・チーム別通算打率・通算投球数・被安打数・本塁打数を最低限含む**)は**移行計画で確定し**、「移行後の新DBから付録A定義で算出した値」と「旧DBの生データから付録A定義で再計算した値」を**完全一致で比較する**」 | `FR-038` |
| V2 | 「「**新DBのNULL件数＝旧DBで該当条件(`x<=0`・`球速<=0`・氏名`'0'` 等)に一致する行数**」の件数突合を含める(…**両辺を同じ変換で計算する集計値突合では検出できないため専用の検証項目とする**)」 | `FR-038` |
| V3 | 「**所見件数の新旧突合を検証に含める**」 | `FR-038` |
| V4 | 「**テストベクタ**(変換前後の具体値の組)をゴールデンフィクスチャとして収載し、変換後のコース分布チャートが**右打者のみ/左打者のみに分けて**新旧一致することを移行検証(FR-038)に含める」 | `付録B-5` |
| V5 | 「⑥は付録B-5の変換式に対する変換ベクタ+FR-038の移行突合 / ⑧はFR-038の導出規則に対する単体テスト+同条の移行突合」 | `NFR-018`・`NFR-018`・`NFR-018` |
| V6 | 「両者が食い違う試合は「**交代のあった試合**」として警告レポートに列挙する」 | `FR-038` |
```

### 差分 26(前 503〜503 行)

**前**:

```
`REQ:779`「**列単位のセンチネル規則に従いNULL(未入力)へ変換**する(対象: コースX/Y・打球位置X/Y・球速・
```

**後**:

```
`FR-038`「**列単位のセンチネル規則に従いNULL(未入力)へ変換**する(対象: コースX/Y・打球位置X/Y・球速・
```

### 差分 27(前 508〜508 行)

**前**:

```
`REQ:1038`(8 章 DoD⑤)「**移行着手前に 88列の意味・行き先対応表**(付録D-1のスキーマ定義ファイル)
```

**後**:

```
要件書 8 章(8 章 DoD⑤)「**移行着手前に 88列の意味・行き先対応表**(付録D-1のスキーマ定義ファイル)
```

### 差分 28(前 511〜512 行)

**前**:

```
**再実行・誤実行防止**: `REQ:784`「**新DB側を初期化して何度でもやり直せる**。**移行は旧データを読むだけで
変更しない**(旧システム継続が常に切り戻し手段)」/ `REQ:785`「確認用フラグなし / Then 移行は実行されない」。
```

**後**:

```
**再実行・誤実行防止**: `FR-038`「**新DB側を初期化して何度でもやり直せる**。**移行は旧データを読むだけで
変更しない**(旧システム継続が常に切り戻し手段)」/ `FR-038`「確認用フラグなし / Then 移行は実行されない」。
```

### 差分 29(前 516〜523 行)

**前**:

```
**射程内(写像を直接拘束)**: FR-038 `REQ:770`-`:786`(Must・受入 13 本)/ 付録 D-1 `:1227`(**列定義の正**)/
D-2 `:1228` / D-3 `:1229` / **D-5 `:1237`**(「移行時、旧Kind値は「試合区分+大会名」に機械的に割り当てる」
= 列 2 Kind の変換規則)/ **D-6 `:1238`**(「名寄せは**チーム(対戦相手チームレコード)単位でスコープ**し、
チームをまたいだ同名の統合は行わない」= 列 63-71 の「移行時に使用」の規則)/ D-4 `:1230`-`:1236`
(語彙初期値 10 球種 = **列 44 の値写像の正**)/ 付録 B-1〜B-6 / 付録 A-2 `:1114`・`:1117` /
FR-030 `:563` / FR-031 / FR-014 `:335` / FR-001 `:191`(「任意項目は**移行データとの列の連続性のため**
入力可能とする」)/ FR-004 `:217`(列 57 プレス「**旧88列の毎球項目を継承**」)/ 2.3 `:109`-`:111` /
NFR-018 (β)⑥⑧ / NFR-023 `:962`(移行の解決・警告レポートが XSS 対象経路)/ 2.2 Won't `:96`
```

**後**:

```
**射程内(写像を直接拘束)**: FR-038(Must・受入 13 本)/ 付録 D-1(**列定義の正**)/
D-2 / D-3 / **D-5**(「移行時、旧Kind値は「試合区分+大会名」に機械的に割り当てる」
= 列 2 Kind の変換規則)/ **D-6**(「名寄せは**チーム(対戦相手チームレコード)単位でスコープ**し、
チームをまたいだ同名の統合は行わない」= 列 63-71 の「移行時に使用」の規則)/ D-4
(語彙初期値 10 球種 = **列 44 の値写像の正**)/ 付録 B-1〜B-6 / 付録 A-2 /
FR-030 / FR-031 / FR-014 / FR-001(「任意項目は**移行データとの列の連続性のため**
入力可能とする」)/ FR-004(列 57 プレス「**旧88列の毎球項目を継承**」)/ 2.3 節 /
NFR-018 (β)⑥⑧ / NFR-023(移行の解決・警告レポートが XSS 対象経路)/ 2.2 節 Won't
```

### 差分 30(前 527〜527 行)

**前**:

```
`REQ:996`「**カットオーバー日・移行対象の線引きは移行仕様を策定する別タスクで決定する**」/
```

**後**:

```
要件書 6.3 節「**カットオーバー日・移行対象の線引きは移行仕様を策定する別タスクで決定する**」/
```

### 差分 31(前 529〜529 行)

**前**:

```
FR-017 `:379`(在籍棚卸し)/ 8 章 DoD④⑦ / 9 章 R-4・R-8。
```

**後**:

```
FR-017(在籍棚卸し)/ 8 章 DoD④⑦ / 9 章 R-4・R-8。
```

### 差分 32(前 531〜531 行)

**前**:

```
**「全件」の射程は不変**: `REQ:996`「「全件」とは FR-038 が列挙するデータ種別(毎球・チーム・選手・
```

**後**:

```
**「全件」の射程は不変**: 要件書 6.3 節「「全件」とは FR-038 が列挙するデータ種別(毎球・チーム・選手・
```

### 差分 33(前 535〜537 行)

**前**:

```
**捕球選手推定を列 47 に掛けてはならない**: `REQ:780` が「「捕球選手」＝守備位置番号」と事実認定し、
旧列 47 に既に番号が入っている。NFR-018 は捕球選手推定を **(α)**(`REQ:889`)、移行時変換を **(β)⑥**
(`REQ:890`)に置く。→ 写像で列 47 に推定を掛けるのは**要件区分の混同**。
```

**後**:

```
**捕球選手推定を列 47 に掛けてはならない**: `FR-038` が「「捕球選手」＝守備位置番号」と事実認定し、
旧列 47 に既に番号が入っている。NFR-018 は捕球選手推定を **(α)**(`NFR-018`)、移行時変換を **(β)⑥**
(`NFR-018`)に置く。→ 写像で列 47 に推定を掛けるのは**要件区分の混同**。
```

### 差分 34(前 543〜547 行)

**前**:

```
| **物理削除しない**(`REQ:157`・`:992`・`:977`) | 移行データにも及ぶ。**写像に「捨てる」行き先を作れない** — 取り込めない行は `REQ:778`「黙って落とさず**隔離領域に原本のまま保全**」/ `REQ:777`「無検閲で取り込み保全され、警告レポートで可視化される(**弾かない**)」。`REQ:159`「**黙殺しない**」 |
| **NFR-014**(`REQ:865`) | 写像 JSON は**リポジトリ収載物**(`REQ:1227`)。旧 DB 接続文字列・資格情報を**書けない**。「保存元」欄は**列/テーブルの識別まで**に留める |
| **NFR-018**(`REQ:885`-`:890`・`:893`・`:894`) | **写像 JSON に座標変換式や責任投手導出規則を「実行可能な形」で重複記述すると (a) の「人手で保守する実装は 1 系統」違反になり得る**。**宣言(declaration)として持ち、実装がそれを唯一の出所として読む**構成なら (b)「出所・すべての実行入口・派生関係を機械可読に宣言」に適合。**未分類は fail-closed** — `REQ:891`「いずれの区分にも割り当てられない対象計算が生じた場合は、**当該計算の実装を開始せず、本書の改訂で区分を確定する**」 |
| **NFR-005**(`REQ:814`) | 主語は「**集計**」。**移行処理そのもの(全行の読み取り・変換)には該当条項なし**。ただし **V1 の集計突合は集計に当たり本規律が及ぶ** |
| **テナント分離** | 要件は**旧 `team` の 2 役を新側では別概念へ分解することを明文で要求**している — `REQ:409`「FR-035のチーム登録(テナント発行)とは**別概念**。本FRは**自テナント内の相手チームレコード**を扱う」/ `REQ:126`・`:405`(「**テナント間で共有されない**」)/ NFR-010 `:844` / 付録 D-6 `:1238`。→ **旧 `team` を単一の行き先へ写すと矛盾。** 帰属不明行(`owner_team_id` NULL)の受け皿は `REQ:778` の隔離領域のみで、**「帰属不明のまま新スキーマへ通す」行き先は認められていない** |
```

**後**:

```
| **物理削除しない**(要件書 4.0 節・要件書 6.2 節・要件書 6.1 節) | 移行データにも及ぶ。**写像に「捨てる」行き先を作れない** — 取り込めない行は `FR-038`「黙って落とさず**隔離領域に原本のまま保全**」/ `FR-038`「無検閲で取り込み保全され、警告レポートで可視化される(**弾かない**)」。要件書 4.0 節「**黙殺しない**」 |
| **NFR-014**(`NFR-014`) | 写像 JSON は**リポジトリ収載物**(`付録D-1`)。旧 DB 接続文字列・資格情報を**書けない**。「保存元」欄は**列/テーブルの識別まで**に留める |
| **NFR-018**(`NFR-018`) | **写像 JSON に座標変換式や責任投手導出規則を「実行可能な形」で重複記述すると (a) の「人手で保守する実装は 1 系統」違反になり得る**。**宣言(declaration)として持ち、実装がそれを唯一の出所として読む**構成なら (b)「出所・すべての実行入口・派生関係を機械可読に宣言」に適合。**未分類は fail-closed** — `NFR-018`「いずれの区分にも割り当てられない対象計算が生じた場合は、**当該計算の実装を開始せず、本書の改訂で区分を確定する**」 |
| **NFR-005**(`NFR-005`) | 主語は「**集計**」。**移行処理そのもの(全行の読み取り・変換)には該当条項なし**。ただし **V1 の集計突合は集計に当たり本規律が及ぶ** |
| **テナント分離** | 要件は**旧 `team` の 2 役を新側では別概念へ分解することを明文で要求**している — `FR-039`「FR-035のチーム登録(テナント発行)とは**別概念**。本FRは**自テナント内の相手チームレコード**を扱う」/ 要件書 3 章・`FR-039`(「**テナント間で共有されない**」)/ `NFR-010` / 付録 D-6。→ **旧 `team` を単一の行き先へ写すと矛盾。** 帰属不明行(`owner_team_id` NULL)の受け皿は `FR-038` の隔離領域のみで、**「帰属不明のまま新スキーマへ通す」行き先は認められていない** |
```

### 差分 35(前 553〜559 行)

**前**:

```
| **RQ-A** | **矛盾(最も実害が大きい)** | **負値座標の扱いが `REQ:1201` と `REQ:779` で食い違う**。付録 B-5 のセンチネル条件は「旧値が **`0`(または空・`"0"`)**」で、**範囲外の値は [0,1] にクランプ**。→ 負値は NULL ではなく **0.0 という有効値**になる。一方 `REQ:779` の NULL 件数条件は **`x<=0`** で負値を算入する。**負値が 1 行でも存在すると V2 の件数突合は構造的に不一致となり、`REQ:1038`(8 章 DoD⑤)を満たせない。** 齟齬は付録 B-5 自身の説明文にも残る(`REQ:1201`「**旧集計側も `x<=0` を除外していた**」)。**負値の実在性**は `IS:201`「クリック点→**パディング除去**」(表示 360px に対しパディング 6px)から理論上あり得るが、旧コードでクランプされているかは**旧資料に記述なし** → 実データ確認は移行仕様タスク |
| **RQ-B** | **矛盾リスク(条件付き)** | **責任投手の移行導出 `REQ:781` が付録 A-2 の一般規則 `REQ:1117` を取り込んでいない**。`:781` は「走者が出塁した行の投手氏名を責任投手とし、代走で走者が入れ替わった場合は前任走者の責任投手を継承、導出不能はNULL」。`:1117` は「**フォースアウトによる走者の入れ替わり**…では**前任投手の責任が入れ替わった走者に引き継がれる**(公認規則9.16(g)相当)。**打席途中の投手交代**の帰属は…ボール優勢(2-0, 2-1, 3-0, 3-1, 3-2)で結果が四球の場合はその打者・出塁は**前任投手**の責任」。`REQ:775` は「旧DBの生データから**付録A定義で再計算した値**」との**完全一致**を要求 → **代表集計値に失点系が加わった時点で構造的に不一致**。最低限 7 項目に失点は含まれないため**現時点では潜在** |
| **RQ-C** | **不足** | **NULL の由来を区別する定義が要件側に無い**。`REQ:779` は「新DBのNULL件数＝旧DBのセンチネル条件行数」の完全一致を要求するが、`REQ:776` は名寄せ不能行の人手解決を定めるだけで**全行が解決する保証をしていない**。→ 「センチネル由来の NULL」と「参照解決不能由来の NULL」の分離定義が不在。候補案は設計側で独自に分離している(`CAND:1486`-`:1491` — 7 節 `P1-9`)が**要件側の定義は無い** |
| **RQ-D** | **不足** | **Won't 廃止機能に対応する列の互換出力値が未定義**。`REQ:102`「**経過時間のTag打刻**(旧「▶Tag」ラップ記録。**利用実態がないため廃止**)」= Won't。一方 FR-031 は移行試合を含む全試合に 88 列全列の出力を要求し、88 列には列 75「経過時間」・列 77「現在時刻」が含まれる。→ **廃止した機能に対応する列を、新規記録試合の互換出力で何の値にするかが要件に無い**(6-1 の「本体の NULL 表現が未定義」と同根) |
| **RQ-E** | **不足** | **列 50-51(打球位置)の移行変換の列単位確定が要件条文に無い**。付録 B-5 `REQ:1201` は「旧**263px**スケール座標からの変換」と述べるが**対象列を指定していない**。付録 B-6 `REQ:1202` は「**フィールド画像の元寸法と正規化の対応も同ファイルに記録する**」とシードファイルへ委任。→ 写像に確定値を書くには、**要件条文ではなく改善台帳 I-17 + `contracts/display_geometry_263_v1.json`**(`REQ:911` が契約値として名指しするファイル)を典拠にする必要がある |
| **RQ-F** | **成果物の形の不足(本タスク内で是正)** | **付録 D-1 `REQ:1227` が要求する意味欄と、TSK-250 の受け渡し 4 項目が別集合**。→ 8-2 節で調停案を示す |
| **RQ-G** | **低優先(参照の不整合)** | `REQ:889`・`:924` は**付録 B-3** を「内外角の導出」の定義箇所として参照するが、10 章 `REQ:1080` は「**付録B-3/B-4座標変換ベクタ**」と書く。実際の B-3 は「打者左右による変換なし」、B-4 は「ゾーン枠はデータ」であり、座標変換(正規化・視点)を定めるのは **B-1/B-2**。**仕様への実害なし** |
```

**後**:

```
| **RQ-A** | **矛盾(最も実害が大きい)** | **負値座標の扱いが `付録B-5` と `FR-038` で食い違う**。付録 B-5 のセンチネル条件は「旧値が **`0`(または空・`"0"`)**」で、**範囲外の値は [0,1] にクランプ**。→ 負値は NULL ではなく **0.0 という有効値**になる。一方 `FR-038` の NULL 件数条件は **`x<=0`** で負値を算入する。**負値が 1 行でも存在すると V2 の件数突合は構造的に不一致となり、要件書 8 章(8 章 DoD⑤)を満たせない。** 齟齬は付録 B-5 自身の説明文にも残る(`付録B-5`「**旧集計側も `x<=0` を除外していた**」)。**負値の実在性**は `IS:201`「クリック点→**パディング除去**」(表示 360px に対しパディング 6px)から理論上あり得るが、旧コードでクランプされているかは**旧資料に記述なし** → 実データ確認は移行仕様タスク |
| **RQ-B** | **矛盾リスク(条件付き)** | **責任投手の移行導出 `FR-038` が付録 A-2 の一般規則 `付録A-2` を取り込んでいない**。同条の移行導出は「走者が出塁した行の投手氏名を責任投手とし、代走で走者が入れ替わった場合は前任走者の責任投手を継承、導出不能はNULL」。付録 A-2 は「**フォースアウトによる走者の入れ替わり**…では**前任投手の責任が入れ替わった走者に引き継がれる**(公認規則9.16(g)相当)。**打席途中の投手交代**の帰属は…ボール優勢(2-0, 2-1, 3-0, 3-1, 3-2)で結果が四球の場合はその打者・出塁は**前任投手**の責任」。`FR-038` は「旧DBの生データから**付録A定義で再計算した値**」との**完全一致**を要求 → **代表集計値に失点系が加わった時点で構造的に不一致**。最低限 7 項目に失点は含まれないため**現時点では潜在** |
| **RQ-C** | **不足** | **NULL の由来を区別する定義が要件側に無い**。`FR-038` は「新DBのNULL件数＝旧DBのセンチネル条件行数」の完全一致を要求するが、`FR-038` は名寄せ不能行の人手解決を定めるだけで**全行が解決する保証をしていない**。→ 「センチネル由来の NULL」と「参照解決不能由来の NULL」の分離定義が不在。候補案は設計側で独自に分離している(候補案 12-1 節-候補案 12-1 節 — 7 節 `P1-9`)が**要件側の定義は無い** |
| **RQ-D** | **不足** | **Won't 廃止機能に対応する列の互換出力値が未定義**。要件書 2.2 節「**経過時間のTag打刻**(旧「▶Tag」ラップ記録。**利用実態がないため廃止**)」= Won't。一方 FR-031 は移行試合を含む全試合に 88 列全列の出力を要求し、88 列には列 75「経過時間」・列 77「現在時刻」が含まれる。→ **廃止した機能に対応する列を、新規記録試合の互換出力で何の値にするかが要件に無い**(6-1 の「本体の NULL 表現が未定義」と同根) |
| **RQ-E** | **不足** | **列 50-51(打球位置)の移行変換の列単位確定が要件条文に無い**。付録 B-5 `付録B-5` は「旧**263px**スケール座標からの変換」と述べるが**対象列を指定していない**。付録 B-6 `付録B-6` は「**フィールド画像の元寸法と正規化の対応も同ファイルに記録する**」とシードファイルへ委任。→ 写像に確定値を書くには、**要件条文ではなく改善台帳 I-17 + `contracts/display_geometry_263_v1.json`**(`NFR-018` が契約値として名指しするファイル)を典拠にする必要がある |
| **RQ-F** | **成果物の形の不足(本タスク内で是正)** | **付録 D-1 `付録D-1` が要求する意味欄と、TSK-250 の受け渡し 4 項目が別集合**。→ 8-2 節で調停案を示す |
| **RQ-G** | **低優先(参照の不整合)** | `NFR-018`・`NFR-019` は**付録 B-3** を「内外角の導出」の定義箇所として参照するが、10 章 要件書 10 章 は「**付録B-3/B-4座標変換ベクタ**」と書く。実際の B-3 は「打者左右による変換なし」、B-4 は「ゾーン枠はデータ」であり、座標変換(正規化・視点)を定めるのは **B-1/B-2**。**仕様への実害なし** |
```

### 差分 36(前 567〜567 行)

**前**:

```
| **`P-01`〜`P-60`**(再提案してはいけない案) | **`docs/features/product-data-model-design/research.md:186`-`:251`** — 同節が「**この表が禁止事項の正**。`design.md` は本表の ID を参照するだけで本文を複製しない」と宣言 | **改善台帳 `docs/improvements-from-baseball-scoring.md` ではない**。同書は **`I-1`〜`I-28`** の台帳 |
```

**後**:

```
| **`P-01`〜`P-60`**(再提案してはいけない案) | **`docs/features/product-data-model-design/research.md` 3-2 節** — 同節が「**この表が禁止事項の正**。`design.md` は本表の ID を参照するだけで本文を複製しない」と宣言 | **改善台帳 `docs/improvements-from-baseball-scoring.md` ではない**。同書は **`I-1`〜`I-28`** の台帳 |
```

### 差分 37(前 569〜570 行)

**前**:

```
| **「88 列の意味の正は `data-layer.md`」** | `docs/README.md:35` / 要件書 付録 D-1 `REQ:1227` / `docs/legacy/research/README.md:36` | **AGENTS.md にこの規定は無い**(同書は「版固定・変更禁止」だけ) |
| **`LG-01`〜`LG-08`** | 同期プロトコル正本化タスクにも**別の** `LG-01`〜`LG-08` がある(`docs/features/sync-protocol-canonical/plan.md:60`・`:215`-`:216`) | **`DM-` 接頭辞の有無で区別すること** |
```

**後**:

```
| **「88 列の意味の正は `data-layer.md`」** | `docs/README.md`(索引の `docs/legacy/` 行) / 要件書 付録 D-1 / `docs/legacy/research/README.md:36` | **AGENTS.md にこの規定は無い**(同書は「版固定・変更禁止」だけ) |
| **`LG-01`〜`LG-08`** | 同期プロトコル正本化タスクにも**別の** `LG-01`〜`LG-08` がある(`docs/features/sync-protocol-canonical/plan.md` の「やること」節・「欠陥 ID の体系と検出手段」節) | **`DM-` 接頭辞の有無で区別すること** |
```

### 差分 38(前 580〜584 行)

**前**:

```
- 決定: **D-44 v1.7(2026-07-24)**「9本すべてを `pitchlog/docs/legacy/research/` に版固定同梱し、**移行仕様の事実の正本に据えた**」(`docs/requirements/requirements-draft-pitchlog.md:442`)
- **引用形式は可変正本と分ける**(approved): `docs/design/sync-protocol.md:139`「**版固定アーカイブ `docs/legacy/` は行番号で引く。** …**可変な正本本文の引用と扱いを分ける**」。人間裁定 2026-08-29 / 2026-08-30。機械強制 `scripts/check_design_propagation.py:726`
- **`normative` 分類は機構的に必ず fail する** — `reference-class` は「`normative` のリンク先は `status: approved`」を要求(`docs/features/doc-check-multi-doc/design.md:290`・実装 `scripts/check_design_propagation.py:986`-`:996`・負例テスト `tests/test_check_design_propagation.py:4589`)。**`docs/legacy/` は frontmatter status を持たない**
- **「旧資料間の多数決」は明文で禁じられている(語は違う)**: 設計書 `docs/development/dev-harness-design-2026-08-07.md:560`「**アーカイブ間で矛盾する点は優先順位を定めず「不明」と報告する**」(approved v1.5)。変更履歴 `:41`「**優先規定は新設しない**: 88 列の意味の正は data-layer.md のまま・アーカイブ間の矛盾は「不明」と報告」。エージェント定義側も同旨(`.claude/agents/legacy-analyst.md:14`)。設計書 `:566`「典拠が見つからないことは「不明」と報告する — **推測で埋めない**」
- **`docs/legacy/` の規範性をリポ全体へ拡張する規定は PO が撤回済み** — `docs/worklog/2026-09-02-decision-sheet-ruling.md:135`「**PO 裁定(2026-09-02・山田正輝): 設計書追記を撤回し後続タスクへ**」/ `docs/README.md:35`「規範化は設計書 7.2 の改訂タスク(**TSK-311**)で確定する」
```

**後**:

```
- 決定: **D-44 v1.7(2026-07-24)**「9本すべてを `pitchlog/docs/legacy/research/` に版固定同梱し、**移行仕様の事実の正本に据えた**」(`docs/requirements/requirements-draft-pitchlog.md`(superseded))
- **引用形式は可変正本と分ける**(approved): 同期プロトコル 2-6 節「**版固定アーカイブ `docs/legacy/` は行番号で引く。** …**可変な正本本文の引用と扱いを分ける**」。人間裁定 2026-08-29 / 2026-08-30。機械強制 `scripts/check_design_propagation.py` の `check_citation_format`
- **`normative` 分類は機構的に必ず fail する** — `reference-class` は「`normative` のリンク先は `status: approved`」を要求(`docs/features/doc-check-multi-doc/design.md` 7 節・実装 `scripts/check_design_propagation.py` の `check_reference_classes`・負例テスト `tests/test_check_design_propagation.py` の
  `test_reference_class_rejects_unapproved_normative_targets`)。**`docs/legacy/` は frontmatter status を持たない**
- **「旧資料間の多数決」は明文で禁じられている(語は違う)**: 設計書 8.5「**アーカイブ間で矛盾する点は優先順位を定めず「不明」と報告する**」(approved v1.5)。同書 変更履歴表「**優先規定は新設しない**: 88 列の意味の正は data-layer.md のまま・アーカイブ間の矛盾は「不明」と報告」。エージェント定義側も同旨(`.claude/agents/legacy-analyst.md` の「出典必須」条)。設計書 8.5「典拠が見つからないことは「不明」と報告する — **推測で埋めない**」
- **`docs/legacy/` の規範性をリポ全体へ拡張する規定は PO が撤回済み** — `docs/worklog/2026-09-02-decision-sheet-ruling.md` の「ステップ 7 — 成果物レビュー」節「**PO 裁定(2026-09-02・山田正輝): 設計書追記を撤回し後続タスクへ**」/ `docs/README.md`(索引の `docs/legacy/` 行)「規範化は設計書 7.2 の改訂タスク(**TSK-311**)で確定する」
```

### 差分 39(前 590〜591 行)

**前**:

```
| **88 列の実際の意味・センチネル値・列名の罠** | **`DL` が正**(precedence あり)。`REQ:1227`・`REQ:780`・設計書 `:560`・`docs/README.md:35` |
| **テーブル数(12/13)・`game_lineup_snapshot` の存否・CASCADE・接続プール** | **優先規定なし → 「不明」と報告。precedence の射程拡張は未決** — `docs/features/harness-design-review/research.md:105`(未解決論点 4「req:25 の precedence(**88 列限定**)をテーブル一覧・接続機構へ拡張するか」)・`:136`・`:156`「**未決のまま**」。送り先: 同 plan.md:35・`:43`(第 2 弾以降・PO 判断) |
```

**後**:

```
| **88 列の実際の意味・センチネル値・列名の罠** | **`DL` が正**(precedence あり)。`付録D-1`・`FR-038`・設計書 8.5・`docs/README.md`(索引の `docs/legacy/` 行) |
| **テーブル数(12/13)・`game_lineup_snapshot` の存否・CASCADE・接続プール** | **優先規定なし → 「不明」と報告。precedence の射程拡張は未決** — `docs/features/harness-design-review/research.md` の「未解決・申し送り」節(未解決論点 4「req:25 の precedence(**88 列限定**)をテーブル一覧・接続機構へ拡張するか」)・同「判定表」節・「未解決論点の現況」節「**未決のまま**」。送り先: 同 plan.md の「やること」節・「やらないこと」節(第 2 弾以降・PO 判断) |
```

### 差分 40(前 601〜604 行)

**前**:

```
| `P-02` | 「**88 列に列を足して新概念を収める(サイドカー方式が正)**」(`:193`) | `REQ:1229` / 改善台帳 `improvements:61`(確定日 2026-07-20・要件対話 D-6)。**「キー列を足せない」は P-02 からの導出**であり逐語ではない |
| `P-42` | 「**センチネル `0` をそのまま座標変換する**」(`:233`) | `REQ:1201`「**変換前のセンチネル判定を必ず先に行う**——旧値が `0`(または空・`"0"`)の座標は未入力を意味するため**NULLへ写し、変換対象から除外する**」。原決定 **D-44 v1.7 P1①**(`requirements-draft-pitchlog.md:443`) |
| `P-43` | 「**88 列を列名どおりに読み替える**(実際の意味は `data-layer.md` が正)」(`:234`) | `REQ:780`(逐語は 6-1 節) |
| `P-45` | 「**`game_lineup_snapshot` を先発オーダーとして読む**」(`:236`) | `REQ:782`「**各試合の最初のプレイ行のスタメンJSONを正**とする(`game_lineup_snapshot` は交代で上書きされる「最終オーダー」であり**別に保全する**)」。原決定 **D-44 v1.7 P2×10**(`:444`) |
```

**後**:

```
| `P-02` | 「**88 列に列を足して新概念を収める(サイドカー方式が正)**」 | `付録D-3` / 改善台帳 改善台帳 `I-5`(確定日 2026-07-20・要件対話 D-6)。**「キー列を足せない」は P-02 からの導出**であり逐語ではない |
| `P-42` | 「**センチネル `0` をそのまま座標変換する**」 | `付録B-5`「**変換前のセンチネル判定を必ず先に行う**——旧値が `0`(または空・`"0"`)の座標は未入力を意味するため**NULLへ写し、変換対象から除外する**」。原決定 **D-44 v1.7 P1①**(`docs/requirements/requirements-draft-pitchlog.md` — superseded) |
| `P-43` | 「**88 列を列名どおりに読み替える**(実際の意味は `data-layer.md` が正)」 | `FR-038`(逐語は 6-1 節) |
| `P-45` | 「**`game_lineup_snapshot` を先発オーダーとして読む**」 | `FR-038`「**各試合の最初のプレイ行のスタメンJSONを正**とする(`game_lineup_snapshot` は交代で上書きされる「最終オーダー」であり**別に保全する**)」。原決定 **D-44 v1.7 P2×10**(同書 — superseded) |
```

### 差分 41(前 606〜606 行)

**前**:

```
**`P-02` は既に機械強制済み**: 同期正本 `docs/design/sync-protocol.md:451`「**P-02** \| … \| **該当なし** \|
```

**後**:

```
**`P-02` は既に機械強制済み**: 同期正本 同期プロトコル 4-7 節「**P-02** \| … \| **該当なし** \|
```

### 差分 42(前 608〜608 行)

**前**:

```
コード側テスト `frontend/src/lib/sync/prohibitions.spec.ts:1078`
```

**後**:

```
コード側テスト `frontend/src/lib/sync/prohibitions.spec.ts` の `P-02` ケース
```

### 差分 43(前 612〜612 行)

**前**:

```
**`P-45` の「`DB` のみに依存」懸念は既に判定済みで不成立**: `docs/features/harness-design-review/research.md:59`
```

**後**:

```
**`P-45` の「`DB` のみに依存」懸念は既に判定済みで不成立**: `docs/features/harness-design-review/research.md` の「B. 裁定済み指摘 — P2」節
```

### 差分 44(前 617〜617 行)

**前**:

```
逐語(`CAND:1483`-`:1491`):
```

**後**:

```
逐語(候補案 12-1 節-候補案 12-1 節):
```

### 差分 45(前 625〜625 行)

**前**:

```
→ **候補案(一時作業域)にしかなく正本化されていない**が、論拠は approved 要件 `REQ:779` に直結する。
```

**後**:

```
→ **候補案(一時作業域)にしかなく正本化されていない**が、論拠は approved 要件 `FR-038` に直結する。
```

### 差分 46(前 632〜637 行)

**前**:

```
| 2026-08-21〜23 | **TSK-248**(候補案起草・PR #23)。敵対レビュー 4 周・指摘 37 件全件反映 | `docs/worklog/2026-08-21-product-data-model-design.md:138`-`:145` |
| 2026-08-22 | **人間の裁定**: 「**是正のたびに 4〜5 件の新しい欠陥が生まれる状態**が 3 周続いたため、**周回を止めて申し送る**」。12-7 節に 3 領域(認可機構の物理構成 / イベントの順序 / **移行のファンアウト**)を申し送り。**88 列表はこの 3 領域に入っていない**(= 誤って「閉じた」と宣言された領域) | 同 `:156`-`:166` |
| 2026-08-31 | **人間の裁定 = 射程の分割**。TSK-250 の計画が**計画レビューで 2 周続けて否決**され、A/B/**C** を先行タスクへ切り出し。「**2 周とも同じ構造的理由** — 「1 タスクに独立した確定単位が複数入っており、後段が前段の成果を壊す」」 | `T250P:38`-`:40`・`:48` |
| 2026-08-31 | **人間の判断 = 先行順序**。「**TSK-269 と TSK-271 はブロックしない**ため TSK-270 を先行させる。**製品コードの着手を最短にする**のが目的」 | `docs/worklog/2026-08-31-pg-authz-verification.md:17`-`:19` |
| 2026-09-04 | A(TSK-269)マージ(PR #42 / `fdda374`)。TSK-250 再承認(山田正輝)。**C(TSK-271)は未着手** | `T250P:4`・`:67` / `docs/worklog/2026-09-04-sync-event-contract.md:29` |
| 2026-09-07 | TSK-250 の開始条件は **B・C・人間裁定 4 件が未充足**。`docs/design/data-model.md` も新資産も 1 件も存在しない | `docs/features/sync-server-apply/research.md:20`・`:323` |
```

**後**:

```
| 2026-08-21〜23 | **TSK-248**(候補案起草・PR #23)。敵対レビュー 4 周・指摘 37 件全件反映 | `docs/worklog/2026-08-21-product-data-model-design.md` の「敵対レビュー 4 周と「閉じる」判断」節 |
| 2026-08-22 | **人間の裁定**: 「**是正のたびに 4〜5 件の新しい欠陥が生まれる状態**が 3 周続いたため、**周回を止めて申し送る**」。12-7 節に 3 領域(認可機構の物理構成 / イベントの順序 / **移行のファンアウト**)を申し送り。**88 列表はこの 3 領域に入っていない**(= 誤って「閉じた」と宣言された領域) | 同「4 周目で閉じた(人間の裁定 2026-08-22)」節 |
| 2026-08-31 | **人間の裁定 = 射程の分割**。TSK-250 の計画が**計画レビューで 2 周続けて否決**され、A/B/**C** を先行タスクへ切り出し。「**2 周とも同じ構造的理由** — 「1 タスクに独立した確定単位が複数入っており、後段が前段の成果を壊す」」 | TSK-250 計画 1 節「射程の決定」 |
| 2026-08-31 | **人間の判断 = 先行順序**。「**TSK-269 と TSK-271 はブロックしない**ため TSK-270 を先行させる。**製品コードの着手を最短にする**のが目的」 | `docs/worklog/2026-08-31-pg-authz-verification.md` の「決定」節 |
| 2026-09-04 | A(TSK-269)マージ(PR #42 / `fdda374`)。TSK-250 再承認(山田正輝)。**C(TSK-271)は未着手** | TSK-250 計画 frontmatter・1 節「先行タスク A のマージ実績」 / `docs/worklog/2026-09-04-sync-event-contract.md` の「着手に至る経緯」節 |
| 2026-09-07 | TSK-250 の開始条件は **B・C・人間裁定 4 件が未充足**。`docs/design/data-model.md` も新資産も 1 件も存在しない | `docs/features/sync-server-apply/research.md` の「結論(要約)」節・10 節 |
```

### 差分 47(前 639〜639 行)

**前**:

```
**受け渡し契約の逐語**(`T250P:46`):
```

**後**:

```
**受け渡し契約の逐語**(TSK-250 計画 1 節「射程の決定」):
```

### 差分 48(前 650〜655 行)

**前**:

```
| 全行網羅 + exact-set | 「**`column_index` 0〜87 の全行が本文にあり、C の JSON と 4 項目 + 共通正規化 ID で exact-set 一致**(**C の blob digest を照合し、一致しなければ fail**)」(`:311`) |
| **再解釈の機械検出** | 「**本文の記述のうち C の JSON に無い値が 0 件**」(`:311`)→ **本タスクの JSON に書かれていない値は TSK-250 の正本本文に一切書けない。列 0 の備考「—」のような未記入を残すと、正本側で説明を足せなくなる** |
| 参照分類 | 「**`docs/legacy/` が `normative` として参照されていない**(`reference-class` で `evidence` のみ)」(`:311`) |
| 母集合合流 | 「**B・C の資産の全行が母集合に現れる**(exact-set・**digest 照合つき**)」/「**全行に出典の安定 ID と共通正規化 ID がある**(**該当なしは明示的な空集合 + 理由**)」(`:299`) |
| digest 再照合 | `scripts/verify_handoff_digest.py` が「**先行タスクの成果が後から動いていないことの確認**」を行う(`:277`-`:280`)→ **マージ後に JSON を触ると TSK-250 が落ちる** |
| 件数の固定 | 「**群別件数と ID 集合が 4 節の表と一致**(exact-set)」(`:298`)→ **`DM-LG-*` = 16 件・`DM-LX-*` = 6 件を変えられない**(`unique-owner` / `baseline-digest` が落ちる) |
```

**後**:

```
| 全行網羅 + exact-set | 「**`column_index` 0〜87 の全行が本文にあり、C の JSON と 4 項目 + 共通正規化 ID で exact-set 一致**(**C の blob digest を照合し、一致しなければ fail**)」(同 4 節「実装ステップ」) |
| **再解釈の機械検出** | 「**本文の記述のうち C の JSON に無い値が 0 件**」(同 4 節「実装ステップ」)→ **本タスクの JSON に書かれていない値は TSK-250 の正本本文に一切書けない。列 0 の備考「—」のような未記入を残すと、正本側で説明を足せなくなる** |
| 参照分類 | 「**`docs/legacy/` が `normative` として参照されていない**(`reference-class` で `evidence` のみ)」(同 4 節「実装ステップ」) |
| 母集合合流 | 「**B・C の資産の全行が母集合に現れる**(exact-set・**digest 照合つき**)」/「**全行に出典の安定 ID と共通正規化 ID がある**(**該当なしは明示的な空集合 + 理由**)」(同 4 節「実装ステップ」) |
| digest 再照合 | `scripts/verify_handoff_digest.py` が「**先行タスクの成果が後から動いていないことの確認**」を行う(同 4 節「検証コマンド集合」)→ **マージ後に JSON を触ると TSK-250 が落ちる** |
| 件数の固定 | 「**群別件数と ID 集合が 4 節の表と一致**(exact-set)」(同 4 節「実装ステップ」)→ **`DM-LG-*` = 16 件・`DM-LX-*` = 6 件を変えられない**(`unique-owner` / `baseline-digest` が落ちる) |
```

### 差分 49(前 663〜669 行)

**前**:

```
| 捨てる側 | `CAND:1472`-`:1474`「63-64 \| 打者登録名 / 打者番号 \| **移行時に使用** \| 名寄せの解決キー。**新スキーマには残さない**」 | **候補案(`docs/features/` = 一時作業域)・正本外** |
| 残す側① | `REQ:570`(FR-031・**Must**)「Given 記録済み試合 / When CSV出力 / Then **88列互換フォーマット(付録D)の全プレイ行**がダウンロードされる」 | **approved** |
| 残す側② | `REQ:1229`(付録 D-3)「**88列互換の本体は不変**」 | approved |
| 残す側③ | 改善台帳 I-5 `improvements:59`・`:61`「88列はCSV出力・既存データ取り込みの**互換フォーマットとして維持**」「**88列本体は不変**」 | approved |
| 残す側④ | **候補案自身の非対称** — `CAND:1423`「互換出力(FR-031)では列名・順序を維持する」/ `CAND:1475` 列 72 は逆に「**保持のみ** \| 入力経路の識別値。**互換出力のため**」 | 候補案内部の破れ |
| 「不明」許容の射程 | `REQ:786`・`REQ:563` — 「不明」許容は**交代表記・出場履歴に限る**。**88 列 CSV は射程外** | approved |
| 同一コア領域 | 設計書 6.3 `dev-harness-design-2026-08-07.md:365`「データ移行 \| **FR-038 の全体** … 加えて **FR-031/付録D の 88 列互換出力・サイドカー・直列化契約**」 | approved v1.6(PO 承認 2026-08-17)。**移行と互換出力は同一のコア領域** — 片方の都合で他方を壊す設計が「別領域だから」で逃げられない |
```

**後**:

```
| 捨てる側 | 候補案 12-1 節「63-64 \| 打者登録名 / 打者番号 \| **移行時に使用** \| 名寄せの解決キー。**新スキーマには残さない**」 | **候補案(`docs/features/` = 一時作業域)・正本外** |
| 残す側① | `FR-031`(FR-031・**Must**)「Given 記録済み試合 / When CSV出力 / Then **88列互換フォーマット(付録D)の全プレイ行**がダウンロードされる」 | **approved** |
| 残す側② | `付録D-3`(付録 D-3)「**88列互換の本体は不変**」 | approved |
| 残す側③ | 改善台帳 I-5 改善台帳 `I-5`「88列はCSV出力・既存データ取り込みの**互換フォーマットとして維持**」「**88列本体は不変**」 | approved |
| 残す側④ | **候補案自身の非対称** — 候補案 12-1 節「互換出力(FR-031)では列名・順序を維持する」/ 候補案 12-1 節 列 72 は逆に「**保持のみ** \| 入力経路の識別値。**互換出力のため**」 | 候補案内部の破れ |
| 「不明」許容の射程 | `FR-038`・`FR-030` — 「不明」許容は**交代表記・出場履歴に限る**。**88 列 CSV は射程外** | approved |
| 同一コア領域 | 設計書 6.3「データ移行 \| **FR-038 の全体** … 加えて **FR-031/付録D の 88 列互換出力・サイドカー・直列化契約**」 | approved v1.6(PO 承認 2026-08-17)。**移行と互換出力は同一のコア領域** — 片方の都合で他方を壊す設計が「別領域だから」で逃げられない |
```

### 差分 50(前 671〜671 行)

**前**:

```
**TSK-248 の DoD が既に分離を要求していた**: `docs/features/product-data-model-design/plan.md:159`
```

**後**:

```
**TSK-248 の DoD が既に分離を要求していた**: `docs/features/product-data-model-design/plan.md` 5 節 DoD
```

### 差分 51(前 676〜676 行)

**前**:

```
(`T250P:169`「**要件書の改訂** \| 要件改訂タスク」)。
```

**後**:

```
(TSK-250 計画 2 節「やらないこと」「**要件書の改訂** \| 要件改訂タスク」)。
```

### 差分 52(前 688〜690 行)

**前**:

```
| 7 | **移行行のイベント連番を論理順序の正にする** | **衝突**。`docs/design/sync-protocol.md:465`(approved)「**出場区間の境界または再計算の起点に D1 を使ってはならない**」/ 候補案 `CAND:1540`「イベント連番 \| 旧行の**行識別子の順序**で連番を振る。これが**論理順序の正**になる」が違反 |
| 8 | 交代イベントの旧データからの復元 | **禁止(既決)** `P-44` / `REQ:786` / D-39 |
| 9 | 不整合行を弾く | **禁止(既決)** `P-46` / `REQ:777`・`:778` |
```

**後**:

```
| 7 | **移行行のイベント連番を論理順序の正にする** | **衝突**。同期プロトコル 5-1 節(approved)「**出場区間の境界または再計算の起点に D1 を使ってはならない**」/ 候補案 候補案 12-3 節「イベント連番 \| 旧行の**行識別子の順序**で連番を振る。これが**論理順序の正**になる」が違反 |
| 8 | 交代イベントの旧データからの復元 | **禁止(既決)** `P-44` / `FR-038` / D-39 |
| 9 | 不整合行を弾く | **禁止(既決)** `P-46` / `FR-038`・`FR-038` |
```

### 差分 53(前 694〜695 行)

**前**:

```
| 13 | **`P-42`/`P-43`/`P-45` の裁定者** | **表記の齟齬**。`docs/design/sync-protocol.md:219` は `P-41`〜`P-46` を「**移行仕様タスク**」へ割り当てるが、`T250P:46`・`:166` は TSK-271 と移行仕様タスクを別物として扱う。**明記した決定なし**。ただし同書 (b) 表は「本書が決定を書かない」ことの宣言でしかなく、**本タスクが裁定しても同書の改訂は不要** |
| 14 | 移行取り込みが同期適用経路 P1〜P5 を通るか | **未決**(`docs/features/sync-server-apply/research.md:348`「**正本に明記が無い**」→ 移行仕様タスクへ) |
```

**後**:

```
| 13 | **`P-42`/`P-43`/`P-45` の裁定者** | **表記の齟齬**。同期プロトコル 3-2 節 は `P-41`〜`P-46` を「**移行仕様タスク**」へ割り当てるが、TSK-250 計画 1 節「射程の決定」・2 節「やらないこと」 は TSK-271 と移行仕様タスクを別物として扱う。**明記した決定なし**。ただし同書 (b) 表は「本書が決定を書かない」ことの宣言でしかなく、**本タスクが裁定しても同書の改訂は不要** |
| 14 | 移行取り込みが同期適用経路 P1〜P5 を通るか | **未決**(`docs/features/sync-server-apply/research.md` の「本調査で新たに見つかった論点」節「**正本に明記が無い**」→ 移行仕様タスクへ) |
```

### 差分 54(前 699〜700 行)

**前**:

```
- **要件側**: 結合キーは **(試合ID + 記録権世代 + イベント連番)** で固定(`REQ:1229`)。**移行試合の例外規定は無い**
- **同期側**: `docs/design/sync-protocol.md:255`「サイドカー結合キーは **(試合ID + D4 + D1) の合成**で成立する…**新しいキーを増やさない**(**P-02** と同じ発想)」。**移行行に D4 を与える規則は書かれていない**
```

**後**:

```
- **要件側**: 結合キーは **(試合ID + 記録権世代 + イベント連番)** で固定(`付録D-3`)。**移行試合の例外規定は無い**
- **同期側**: 同期プロトコル 4-2 節「サイドカー結合キーは **(試合ID + D4 + D1) の合成**で成立する…**新しいキーを増やさない**(**P-02** と同じ発想)」。**移行行に D4 を与える規則は書かれていない**
```

### 差分 55(前 703〜703 行)

**前**:

```
- 候補案の未正本化提案: `CAND:1539`「**移行用の記録権世代** \| 6-1 節の記録権世代表に同居させるが**種別を `migration` として区別**する(4 周目 P2-1 の是正)。① 正史の世代連結では通常世代より前に位置する ② 現世代判定から必ず除外する ③ **同期 API はこの世代へのイベントを受け付けない**(移行バッチのみが書ける)④ 各試合に 1 つ」/ `CAND:1527`「**移行の行識別子** \| **旧行の主キー**(取得できない場合は**取り込みバッチ ID + 旧行の読み出し順序**)。**業務上の「プレイ番号」とは別に持つ**」
```

**後**:

```
- 候補案の未正本化提案: 候補案 12-3 節「**移行用の記録権世代** \| 6-1 節の記録権世代表に同居させるが**種別を `migration` として区別**する(4 周目 P2-1 の是正)。① 正史の世代連結では通常世代より前に位置する ② 現世代判定から必ず除外する ③ **同期 API はこの世代へのイベントを受け付けない**(移行バッチのみが書ける)④ 各試合に 1 つ」/ 候補案 12-3 節「**移行の行識別子** \| **旧行の主キー**(取得できない場合は**取り込みバッチ ID + 旧行の読み出し順序**)。**業務上の「プレイ番号」とは別に持つ**」
```

### 差分 56(前 705〜706 行)

**前**:

```
→ **採用されれば結合キーは移行試合でも成立するが、① 正本外であり ② 同じ表の次行(`CAND:1540`)が
`sync-protocol.md:465` と矛盾している**(7-6 #7)。**本タスクの裁定対象**。
```

**後**:

```
→ **採用されれば結合キーは移行試合でも成立するが、① 正本外であり ② 同じ表の次行(候補案 12-3 節)が
同期プロトコル 5-1 節 と矛盾している**(7-6 #7)。**本タスクの裁定対象**。
```

### 差分 57(前 714〜715 行)

**前**:

```
  (他 4 領域で発火する)。**paths 充填は TSK-250 ステップ 26 の所有**(`T250P:319`)。
  触ると **6.3 落とし込み規則⑤(敵対レビュー + 人間承認)** が要る(`.claude/core-areas.json:2`)
```

**後**:

```
  (他 4 領域で発火する)。**paths 充填は TSK-250 ステップ 26 の所有**(TSK-250 計画 4 節「実装ステップ」)。
  触ると **6.3 落とし込み規則⑤(敵対レビュー + 人間承認)** が要る(`.claude/core-areas.json` の `data-migration.paths`)
```

### 差分 58(前 717〜717 行)

**前**:

```
  設計書 `:506`・`:518`・`:847`)。**旧資料の矛盾は旧資料を直して解決できない**
```

**後**:

```
  設計書 8.2・8.3・12.2)。**旧資料の矛盾は旧資料を直して解決できない**
```

### 差分 59(前 719〜721 行)

**前**:

```
  (FR-031・FR-038・付録B・付録D)は v2.4 以降 1 文字も変わっていない**(変更履歴 `REQ:37`・`:39`・`:41`)。
  `T250R` の `REQ:<行>` は旧番号なので読み替えが必要
  (`760→779` / `761→780` / `763→782` / `767→786` / `1154→1201` / `1180→1227` / `1182→1229`)
```

**後**:

```
  (FR-031・FR-038・付録B・付録D)は v2.4 以降 1 文字も変わっていない**(要件書 変更履歴表)。
  なお `T250R` は要件書 v2.4 時点の行番号で引いているため、**本メモは同メモの参照先を現行の
  安定 ID で再照合した**(照合結果は下記「D. 参照経路の課題」)
```

### 差分 60(前 731〜731 行)

**前**:

```
| ディレクトリ・命名 | `contracts/<領域>/` の **1 階層**(**2 階層以上に分けない**)・`<対象>_v<N>.json`(英小文字とアンダースコア)・**ファイル内にも `"version"` を持つ**(ファイル名と一致) | 同 D-12(`:287`-) |
```

**後**:

```
| ディレクトリ・命名 | `contracts/<領域>/` の **1 階層**(**2 階層以上に分けない**)・`<対象>_v<N>.json`(英小文字とアンダースコア)・**ファイル内にも `"version"` を持つ**(ファイル名と一致) | 同 D-12 |
```

### 差分 61(前 734〜735 行)

**前**:

```
| 元 commit・blob digest の先例 | **`contracts/authz/oracle-seal.lock.json` が完全な先例** — `schema_version` / `asset_kind` / `asset_path` / `asset_digest` / `oracle_commit` / `oracle_commit_semantics` / `input_assets[].git_blob_digest` | `contracts/authz/oracle-seal.lock.json:1`-`:25` |
| 安定 ID + entry ごとの digest の先例 | `contracts/authz/auth-catalog.lock.json` — `entry_id` / `aggregate_decision_digest` / entry ごとの `source_text_digest` | 同 `:1`-`:25` |
```

**後**:

```
| 元 commit・blob digest の先例 | **`contracts/authz/oracle-seal.lock.json` が完全な先例** — `schema_version` / `asset_kind` / `asset_path` / `asset_digest` / `oracle_commit` / `oracle_commit_semantics` / `input_assets[].git_blob_digest` | `contracts/authz/oracle-seal.lock.json`(全体) |
| 安定 ID + entry ごとの digest の先例 | `contracts/authz/auth-catalog.lock.json` — `entry_id` / `aggregate_decision_digest` / entry ごとの `source_text_digest` | `contracts/authz/auth-catalog.lock.json`(全体) |
```

### 差分 62(前 744〜746 行)

**前**:

```
| **要件書 付録 D-1** `REQ:1227` | **列名・順序・型** + 意味欄 4 項目(**実際の中身 / 旧コードの参照箇所 / 新スキーマの行き先 / 変換規則**) | **approved(最上位)** |
| **TSK-250 受け渡し契約** `T250P:46` | `column_index = 0..87` をキー + **保存元 / 変換 / 欠損時規則 / 逆変換可能性** + スキーマ版・安定 ID・**共通正規化 ID**・元 commit・blob digest | 承認済み計画(2026-09-04) |
| **`REQ:779`** | **列単位のセンチネル規則**(= 欠損時規則)。かつ `P1-9` により **「センチネル由来の NULL」と「解決不能由来の NULL」を別欄に分ける** | approved + 候補案 |
```

**後**:

```
| **要件書 付録 D-1** `付録D-1` | **列名・順序・型** + 意味欄 4 項目(**実際の中身 / 旧コードの参照箇所 / 新スキーマの行き先 / 変換規則**) | **approved(最上位)** |
| **TSK-250 受け渡し契約** TSK-250 計画 1 節「射程の決定」 | `column_index = 0..87` をキー + **保存元 / 変換 / 欠損時規則 / 逆変換可能性** + スキーマ版・安定 ID・**共通正規化 ID**・元 commit・blob digest | 承認済み計画(2026-09-04) |
| **`FR-038`** | **列単位のセンチネル規則**(= 欠損時規則)。かつ `P1-9` により **「センチネル由来の NULL」と「解決不能由来の NULL」を別欄に分ける** | approved + 候補案 |
```

### 差分 63(前 769〜769 行)

**前**:

```
| A-4 | **移行試合のサイドカー結合キー**(7-7) | **決定が存在しない**。`P-02` が 88 列側へのキー列追加を禁じ、付録 D-3 が結合キーを 3 要素で固定。候補案の `migration` 世代案は正本外かつ `CAND:1540` が `sync-protocol.md:465` に違反 |
```

**後**:

```
| A-4 | **移行試合のサイドカー結合キー**(7-7) | **決定が存在しない**。`P-02` が 88 列側へのキー列追加を禁じ、付録 D-3 が結合キーを 3 要素で固定。候補案の `migration` 世代案は正本外かつ 候補案 12-3 節 が 同期プロトコル 5-1 節 に違反 |
```

### 差分 64(前 778〜778 行)

**前**:

```
| B-1 | **`RQ-A` 負値座標のセンチネル / クランプ衝突** — 付録 B-5 と `REQ:779` の食い違い。**8 章 DoD⑤ が満たせなくなる** | **P0 相当** |
```

**後**:

```
| B-1 | **`RQ-A` 負値座標のセンチネル / クランプ衝突** — 付録 B-5 と `FR-038` の食い違い。**8 章 DoD⑤ が満たせなくなる** | **P0 相当** |
```

### 差分 65(前 782〜783 行)

**前**:

```
| B-5 | **`RQ-B` 責任投手の移行導出 `REQ:781` と付録 A-2 `REQ:1117` の関係** — 代表集計値の確定と同時に裁定 | 中(潜在) |
| B-6 | **`RQ-G` 付録 B の項番参照の不整合**(`REQ:1080` の「付録B-3/B-4座標変換ベクタ」) | 低 |
```

**後**:

```
| B-5 | **`RQ-B` 責任投手の移行導出 `FR-038` と付録 A-2 `付録A-2` の関係** — 代表集計値の確定と同時に裁定 | 中(潜在) |
| B-6 | **`RQ-G` 付録 B の項番参照の不整合**(要件書 10 章 の「付録B-3/B-4座標変換ベクタ」) | 低 |
```

### 差分 66(前 787〜787 行)

**前**:

```
- **カットオーバー日・移行対象の線引き** — `REQ:996` が移行仕様タスクへ明示的に送付済み
```

**後**:

```
- **カットオーバー日・移行対象の線引き** — 要件書 6.3 節 が移行仕様タスクへ明示的に送付済み
```

### 差分 67(前 792〜792 行)

**前**:

```
  (`docs/features/harness-design-review/research.md:105`・`:156`)
```

**後**:

```
  (`docs/features/harness-design-review/research.md` の「未解決・申し送り」節・「未解決論点の現況」節)
```

### 差分 68(前 796〜796 行)

**前**:

```
  (`docs/features/sync-server-apply/research.md:348`)
```

**後**:

```
  (`docs/features/sync-server-apply/research.md` の「本調査で新たに見つかった論点」節)
```

### 差分 69(前 804〜806 行)

**前**:

```
- **`T250R` の `REQ:<行>` は旧番号**(要件書は v2.4 → v2.7)。読み替え:
  `760→779` / `761→780` / `763→782` / `767→786` / `1154→1201` / `1180→1227` / `1182→1229`。
  **ただし 88 列・移行・座標に効く条項は v2.4 以降 1 文字も変わっていない**
```

**後**:

```
- **`T250R` は要件書 v2.4 時点の行番号で `REQ:<行>` を引いている**(現行は v2.7)。本メモは
  同メモの参照先を**現行の安定 ID へ再照合**した: `FR-038`(センチネル規則・意味表・スタメン
  JSON・「不明」許容)/ `付録B-5`(座標変換式)/ `付録D-1`(列定義)/ `付録D-3`(本体不変)。
  **88 列・移行・座標に効く条項は v2.4 以降 1 文字も変わっていない**ため、再照合で意味は動かない
```

**差分ブロック 69 件。**
