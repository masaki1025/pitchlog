---
feature: frozen-baseline-clause
status: active            # active | in-review(/pr が PR 内で更新。完了は PR 状態・Notion・worktree 除去から導出。codex_run.py implement は active 以外を拒否)
承認: 未                  # 未 | 済(YYYY-MM-DD・承認者)— codex_run.py が「済」でないと実行を拒否する
重さ分類: 通常            # 軽微 | 通常 | コア領域 | 機械的軽作業(ADR-001 のモデルをラッパーが自動選択)
worktree: ../../..        # worktree ルート(plan.md からの相対 or 絶対)。/task-start が設定
notion: https://app.notion.com/p/3dd93b75e6878176afdbccfc6ca69788
branch: feature/frozen-baseline-clause
created: 2026-09-16
計画レビュー周回: 9        # 指摘反映を伴うレビュー 1 周ごとに +1(収束確認周は数えない。/plan が更新)
確定ゲート周回: 0          # 指摘反映を伴う敵対レビュー 1 周ごとに +1(同前。/finalize-doc が更新)
実行方式: 通常             # 通常 | fast(fast path 適用時に fast へ — 人間の事前 OK 必須。現在地導出が識別)
反映周コミット: 適用       # 適用 | 規約制定前(必須・既定値なし。確定ゲートの反映周コミット突合の適用境界 — 設計書 6.1)
---

# 実装計画書: ハーネス設計書へ 7.7「凍結基準の更新経路」を新設する(正本のみ)

## 1. 背景・目的

Notion: [本タスク](https://app.notion.com/p/3dd93b75e6878176afdbccfc6ca69788)。

**TSK-386 から正本の部分だけを切り出した。**

TSK-386(PR #63)は「凍結基準を検査器のソースから追い出す」タスクだったが、
**正本の条文と実装を同時に動かしていたため、実装の敵対レビューで 3 周連続 `P0` を受けた。**
あわせて **develop が 104 コミット進み、`oracle_commit` も食い違った**。

**TSK-386 の条文は確定ゲート 8 周 + PO 承認(2026-09-14)を通っていたが、
**本タスクの計画レビュー 4 周で、その条文のうち 3 項が `P0` を出し続けた**
(受理条件の構成・委任の境界・保証する範囲 — **どれも存在しない機構を記述している**)。
**したがって「承認済み本文をそのまま使う」前提は放棄した。** **条文は新しく書き直す。**
**8 周の成果は `fix/oracle-input-baseline` の `design.md` に残り、実装タスクが引き継ぐ。**
**実装の失敗に巻き込んで捨てる理由が無いので、正本だけを先に出す。**

**要件の根拠**: 本件は**開発ハーネスの規律**であり、製品要件の FR/NFR には直接紐づかない。
**制御目的の典拠は運用評価台帳 `H-85`**(oracle の入力凍結が正本の 1 文字の変更を
8 資産 + ハーネステストの更新へ拡大する)。

**解く問題**: **正本に凍結の規律を定めた条文が 1 つも無い。**
規律は資産と検査器の中だけの宣言であり、**基準を動かす作業と検査器を書き換える作業が
区別できない。** 正当な更新と、検査をすり抜けるための書き換えが同じ形になる。

## 2. スコープ

### やること

1. **ハーネス設計書へ `7.7 凍結基準の更新経路` を新設**(**4 項・50 行**)
   - **版は `v1.15 → v1.16`**。**`v1.15` は TSK-355 が取得済み**(develop にマージ済み)
   - **TSK-386 の承認済み本文をそのまま使う部分は無い。** **条文全体を新しく書いた**
   - **確定ゲートは条文全体を対象にする**

### 条文を削った(**計画レビュー 4 周分の帰結**)

**4 周とも `P0` が出て、出どころは常に同じ 3 項だった。**

| 周 | `P0` の出どころ |
| --- | --- |
| 1 | 移行規定の不在 |
| 2 | 移行規定の循環 / 列挙漏れ |
| 3 | 移行規定の発効点 / 「不適合」でなく「検査の同一性」が固定される |
| 4 | **移行規定が文中に残っていた** / **`7.7-6` が状態の規定のまま** / **新 `7.7-2` が中心目的を壊した** |

**`P0` を生んでいたのは、受理条件の構成・委任の境界・保証する範囲の 3 項である。**
**どれも「存在しない機構」を記述している** — **機構が無いので、端の場合が条文だけでは決まらない。**

**機構が無くても真偽が決まる部分だけ残した。**

| 残す | 内容 | 対応する実害 |
| --- | --- | --- |
| **`7.7-1`** | 基準を**新たに置くとき・動かすとき**、検査器のソースへ直書きしない | **基準を動かせない**(`H-85`・TSK-379 で実測)|
| **`7.7-2`** | 動かすとき**事実・理由・承認者・承認日・直前の基準**を追記。**追記のみ** | **正当な更新と改ざんが同じ形になる** |
| **`7.7-3`** | 基準の妥当性を確かめられないとき**緑にしない** | **fail-open で黙って緑**(実測して修正した)|
| **`7.7-4`** | **本節が定めないこと**を明示(委任と非保証)| — |

| 落とす | 理由 | 送り先 |
| --- | --- | --- |
| 受理条件の構成(3 つで尽きる)| **機構の設計であって規範ではない** | 実装タスクの設計 |
| 委任の境界(3 指定・導出)| 同上。**TSK-386 の実装レビューで 3 周 `P0`** | 同上 |
| 保証する範囲(条件 3・非保証 5)| **機構が無いと何を保証するか書けない** | 同上 |

**落とした 3 つは実装タスクが機構を作るときに設計として決める。**
**条文へ昇格させるかはそのあと判断する。**
**素材は `fix/oracle-input-baseline` の `design.md`(8 周のゲートを通った本文)に残る。**

**「動かす」の定義を条文へ入れた**(4 周目 `P0-3` — 一意に読めないという指摘):
**値の変更・別の基準への差し替え・同じ検査への基準の追加・基準の削除の 4 つで尽きる。**

2. **`scripts/check_authz_catalog.py` の fail-open を fail-closed にする**(**8 行** — 9 周目 `P0`)
   - **`7.7-3` は現存の検査にも掛かる**ので、**develop に残る fail-open が条文の発効直後から違反になる**
     (`:4798` — `git rev-parse` 失敗時に黙って続行する。**実測で確認**)
   - **TSK-386 のステップ 6 で書き、変異テストで確認済みの修正を移植する**(`d51a5f1`)
   - **これにより本タスクは「文書のみ」ではなくなり、人間の逐行確認が必要になる**
3. `docs/README.md` の索引を現行化
4. **運用評価台帳へ候補 5 件 + 既存候補 1 件へ実測追記**(**計画レビュー 1 周目 `P1`** —
   **TSK-386 の候補「完了コミットを持たないステップ」は develop の既存候補
   「確定ゲートを含む計画は…欠番」と同じ機構・同じ症状**〔`feature_status.py` の
   `completed != set(range(1, maximum+1))`〕。**新規ではなく実測追記にする**)

### やらないこと

| 対象 | 理由 |
| --- | --- |
| **`check_authz_catalog.py:4798` 以外の実装** — `contracts/` `backend/` と `scripts/` の他の箇所 | **本タスクは正本 + `7.7-3` が要求する最小の修正のみ。** 残りは後継タスクへ |
| **再封印** | 同上 |
| **`H-85` への対応の追記** | **実装が無いので嘘になる。** 後継タスクの範囲 |
| 検査器からの直書き削除 | 同上 |

**実装の差分は `scripts/check_authz_catalog.py` と `tests/test_check_authz_catalog.py` の 2 本に限る。**

### 後継タスクへ送るもの(**永続化の経路を持たせる** — 計画レビュー 1 周目 `P1`)

**後継タスク**: [凍結基準を検査器から台帳へ移す(実装)](https://app.notion.com/p/3dd93b75e68781cd8859dcc2c1e4eb88)
(**起票済み・着手可・担当者あり**)。

**移行規定を置かないので、後継タスクは「例外の受け取り先」ではない。**
**現存する直書きは、触るまで `7.7-1` の対象にならない**(**`7.7-1` に限る** — 条文が明示)。
**`7.7` 全体の対象外になるのではない** — **`7.7-3`(fail-closed)は現存の検査にも掛かる**(6 周目 `P0`)。
**後継タスクは、それらを能動的に台帳へ移す価値ある作業**であって、**規約違反の解消ではない。**

**問題**: **`docs/features/**` は活動中だけ意味を持つ一時領域である**(設計書 7.6-4)。
**`fix/oracle-input-baseline` はマージされないので develop に入らない。**
**枝を消すと失敗記録 43 件と比較の原本に到達できなくなる。**

**永続化の 3 経路**:

| # | 何を | どこへ |
| --- | --- | --- |
| 1 | **設計判断の核心**(自己申告は証拠にならない・`R-1` 型)| **後継タスクの Notion 本文へ転記済み** |
| 2 | **失敗の型 5 つ**(測らずに数を書く / 保証範囲の過大主張 / 母集団を手で列挙 / 訂正の波及漏れ / 新設要素の突合漏れ)| **本タスクで運用評価台帳の候補へ入れる**(正本なので develop に残る)|
| 3 | **比較の原本と実装 38 ファイル**(`4011dd3..fix/oracle-input-baseline` の `docs/` 以外 — 実測)| **`fix/oracle-input-baseline` を残す。** **後継タスクの DoD に「枝を消さない」を書き、本計画にも記す** |

**枝の削除条件**: **後継タスクが完了し、必要な内容を正本・Notion へ移し終えるまで削除しない。**

**とくに引き継ぐべき設計判断**: **自己申告は証拠にならない。**
**3 周の `P0` はすべて「実装に『使った』と言わせる」形で、いずれも no-op で通った。**
**効いたのは `R-1` 型 —「宣言の要素を変えると挙動が変わることを変異で示す」だけである。**

## 3. 影響する正本

| 正本 | 変更内容 | ゲート |
| --- | --- | --- |
| [ハーネス設計書](../../development/dev-harness-design-2026-08-07.md) | **`7.7 凍結基準の更新経路` を新設**(`v1.15 → v1.16`)| **finalize-doc**(7.6-3 **後段** — 合否条件の新設)|
| [ドキュメント索引](../../README.md) | ハーネス設計書の版・要約・最終更新を現行化 | PR レビュー(機械強制)|
| [ハーネス運用評価台帳](../../development/harness-evaluation.md) | **候補 5 件を新規 + 既存候補 1 件へ実測追記** + 変更履歴 1 行。**`H-*` の新規採番なし・版上げなし** | PR レビュー(7.6-3 前段)|
| [データモデル設計](../../design/data-model.md) | **反映なし** | — |
| [要件定義書](../../requirements/requirements-pitchlog-2026-07-22.md) | **反映なし** | — |

**正本体系外**: `docs/features/frozen-baseline-clause/` / `docs/worklog/2026-09-16-frozen-baseline-clause.md`。

## 4. 実装方針

### 重さ分類の根拠 — 通常

**`docs/` 3 本 + `scripts/check_authz_catalog.py`(8 行)+ そのテスト。**
**`check_authz_catalog.py` はコア領域(tenant-isolation)の paths** なので、
**人間の逐行確認が必要**(9 周目 `P0` を受けて射程が広がった)。
**見ていただくのは 8 行とそのテストである。**

### 確定ゲートの扱い

**条文は新しく書き直したので、承認済みの部分は無い。**
**`7.3-1` の適用版を記録し、確定ゲートを条文全体に掛ける。**

**最低 2 周実施する**(**計画レビュー 1 周目 `P1`** — 1 周では `7.3-2` 違反):

| 周 | 範囲 | 根拠 |
| --- | --- | --- |
| **初周** | **全文** | 7.3-4(初周は全文)。**新しい適用版・新しい base に対する初周である** |
| **最終全文確認周** | 全文 | **7.3-2 は「通常周で指摘ゼロ → 別の最終全文確認周へ遷移」と定める。** 初周を兼ねられない |

**条文は 4 項・50 行と小さく、記述しているのは「機構が無くても真偽が決まる部分」だけである。**
**2 周で収束しなければ、そこが次に削る候補になる。**

### 実装ステップ(コミット単位 — 設計書 6.1 段階実装)

| # | ステップ(何を作るか) | 合格条件(このステップの検証方法) |
| --- | --- | --- |
| 1 | **ハーネス設計書を `in-review` 化する** — frontmatter・変更履歴へ**起案行(射程宣言)**・索引 / **同一コミットで適用版(7.3-1)を worklog へ暫定記録** | `check_docs_status.py` exit 0 / **条文コミット SHA = 本コミットの第一親**を確認 / **版が `1.16`**(`1.15` は TSK-355 が取得済み) |
| 2 | **`7.7`(4 項)を書き、確定ゲートを通して approved にする**(`/finalize-doc`)。**反映周のコミットは `反映<r>周目` のみでステップ記法を付けない。本ステップの完了コミットは approved 化コミット** | **条文が 4 項である** / **個別の識別子が 0 件**(40 桁 hex・述語 ID・`contracts/`〜`backend/` 配下のパス。`../design/data-model.md` は除外)/ **`7.7-1` が行為を縛っている**(「新たに置くとき、または動かすとき」)/ **「動かす」が限定列挙である**(値の変更・差し替え・追加・削除)/ **独立した移行規定が無い**(発効点・列挙・受け取り先を持つ条項)/ 収束まで反映(7.3-2)/ **frontmatter・変更履歴・索引の三者が一致** |
| 3 | **`scripts/check_authz_catalog.py` の fail-open を fail-closed にする**(**8 行** — `7.7-3` が現存検査にも掛かるため)| **`git rev-parse` 失敗時に `CatalogError` を上げる** / **エラー文に `oracle_commit`・パス・`git` の stderr を含む** / **fail-open へ戻す変異で `N4` が落ちる**(移植時に実測済み)/ **既存の緑を落としていない** |
| 4 | **運用評価台帳へ候補 5 件 + 既存候補へ実測追記** + 変更履歴 1 行 + 索引の現行化 | **`H-*` の新規採番をしない・版を上げない**(7.6-3 前段)/ **`H-85` への対応の追記をしない**(実装が無いため)/ `check_docs_status.py` exit 0 |
| 5 | **クローズ処理**(`/pr`) | 3 節の宣言と PR 内容が突合 / **実装の差分が 0 ファイル** / CI 全ジョブ green |

## 5. DoD(受け入れ基準)

- [ ] **`7.7 凍結基準の更新経路` が `v1.16` として approved で存在する**
- [ ] **条文が個別の識別子を持たない** — **走査を次で定義する**:
      **40 桁 hex 0 件 / 述語 ID(`F-n`・`G-n`・`N-n`)0 件 /
      `contracts/`・`scripts/`・`tests/`・`backend/` 配下のパス 0 件**。
      **`../design/data-model.md` への参照は除外**(**条文自身が委任の同型を示す典拠として含む**)
- [ ] **`7.7-1` が行為の規定になっている** — **「新たに置くとき、または動かすとき」**
- [ ] **`7.7-2` が「動かすとき」に掛かる**(置くときではない)
- [ ] **独立した移行規定が無い** — **発効点の定義・対象の列挙・受け取り先の指定を持つ条項を置かない**
      (**`7.7-1` の非対象化は行為規定の帰結であり、これは残す**。**`7.7-1` に限り、`7.7-3` は現存の検査にも掛かる**。
      **8 周目 `P0`** — 旧 DoD は「grandfather 条項が無い」と書いており、**条文と正面衝突していた**)
- [ ] **条文が 4 項である** — **受理条件の構成(3 つで尽きる)と、委任先ごとの述語を含まない**。
      **`7.7-4` は「本節が定めないこと」として委任と非保証を明示する** — これは残す
- [ ] **「動かす」が限定列挙である**(値の変更・差し替え・同じ検査への追加・**削除**。
      **削除を含まないと履歴を残さず消す経路が開く** — 5 周目 `P1`)
- [ ] **射程宣言(7.3-7)が変更履歴行にある**
- [ ] **版が `v1.16`**(`v1.15` の重複を避けた)
- [ ] `docs/README.md` の索引が現行
- [ ] **台帳へ候補 5 件 + 既存候補 1 件へ実測追記**(`H-*` の新規採番なし・版上げなし)
- [ ] **`H-85` への対応の追記が無い**(実装が無いため)
- [ ] **`docs/` 以外の差分が `scripts/check_authz_catalog.py` と `tests/test_check_authz_catalog.py` の 2 本のみ**(**計画レビュー 1 周目 `P1`** — 旧 DoD は 4 ディレクトリしか
      列挙しておらず `.github/` `.claude/` `frontend/` ルート設定を通していた)。
      **判定は `git diff --name-only origin/develop...HEAD | grep -v '^docs/'` がその 2 本と一致**
- [ ] **fail-open が消えている** — **fail-closed へ戻す変異で `N4` が落ちる**(移植時に実測済み)
- [ ] **人間の逐行確認を通っている**(コア領域 — 9 周目 `P0` で射程が広がった)
- [ ] `check_docs_status.py` exit 0 / `check_plan_docs_sync.py` exit 0
- [ ] CI 全ジョブ green

## 6. テスト計画

**NFR-019 のテスト種別に足すものは無い。** **コードを 1 行も書かないため。**

| 種別 | 何を足すか |
| --- | --- |
| **機械検査** | `check_docs_status.py`(frontmatter・変更履歴・索引の三者一致)/ `check_plan_docs_sync.py`(計画書の宣言と PR 差分の突合)|
| **静的** | **条文の個別識別子が 0 件**(機械走査)/ **実装の差分が 0 ファイル**(`git diff --name-only`)|

**回すコマンド**

```
uv run ruff check . && uv run ty check && uv run pytest tests/
uv run python scripts/check_docs_status.py
uv run python scripts/check_plan_docs_sync.py --plan docs/features/frozen-baseline-clause/plan.md --base origin/develop
```

**人が読んで確かめること**

| 何を | どう確かめるか |
| --- | --- |
| **`7.7-1` が行為を縛っているか** | **「強制するとき」ではなく「新たに置くとき、または動かすとき」**であることを読んで確認 |
| 既存条文との整合 | **base が 104 コミット動いている**ので、`7.1-1`・`7.3`・`7.6-3`・`10.2` と衝突しないかを最終全文確認周で見る |
| 版番号 | **`v1.16`**。`v1.15` は TSK-355 の「10.1 の意味づけ」 |

## 7. 新設する条文の全文(**契約** — 計画レビュー 6 周目 `P1`)

**本節がステップ 2 で書く条文の正である。** **レビューはこの本文に対して行う。**
**実装時にこれと違う条文を書いた場合、ステップ 2 は不合格である。**

```markdown
### 7.7 凍結基準の更新経路

**本節は、検査が「この資産は変わらない」と主張するときの、基準の置き方と動かし方を定める。**
**何を凍結するかは定めない。**

**背景**: 凍結を謳う検査は、基準となるコミット・版・digest を持つ。**その基準を検査器の
ソースへ直書きすると、基準を動かす作業が「検査器を書き換える作業」と区別できなくなる。**
**正当な更新と、検査をすり抜けるための書き換えが、同じ形になる。**

**7.7-1 凍結基準の外出し**: **凍結の基準を新たに置くとき、または現に置かれている基準を
動かすとき**、**その基準を機械可読な宣言として資産側へ置くこと、および
検査器のソースへその値を直書きしないこと**、**の両方を満たす。**

**本項は行為に掛かる。** **本項より前から検査器のソースに置かれている基準は、
新たに置くか動かすまで本項の対象にならない**(**本項に限る** — 7.7-3 は別である)。

**「基準を動かす」とは次のすべてをいう**(**これで尽きる**):
**値の変更 / 別の基準への差し替え / 同じ検査への基準の追加 / 基準の削除。**
**削除も「動かす」に含む** — **含まないと、履歴を残さず基準を消す経路が開く。**

**7.7-2 更新の記録**: 基準を動かすとき、次を新しい記録として追記する。

1. **新しい基準の識別値**(**基準を取り除くときは、取り除いたことを表す値**)
2. **直前の基準の識別値**(**履歴の先頭では、直前が無いことを表す値**)
3. **動かした事実・理由・承認者・承認日**

**取り除くときも記録を要する** — **記録なしに基準を消せると、凍結そのものを黙って外せる。**

**基準の履歴は追記のみとする。** **既存の記録を書き換える変更・削除する変更を受理しない。**

**7.7-3 fail-closed**: **基準の妥当性を確かめられないとき**(参照先が解決できない、
比較元を読めない、履歴の取得に失敗した等)、**検査は緑にしてはならない。**
**確かめられないことを「異常なし」と扱う検査は、確かめる能力を失ったことを隠す。**

**7.7-4 本節が定めないこと**:

**本節が定めるのは 3 つだけである** — **基準の置き場**(7.7-1)・**動かし方**(7.7-2)・**確かめられないときの扱い**(7.7-3)。
**7.7-1 と 7.7-2 は行為に掛かり、7.7-3 は検査の結果に掛かる**(**現存の検査にも掛かる**)。

**次は各資産の宣言と実装が定める**:

- **何を凍結するか**(凍結対象の集合)
- **凍結対象の同一性をどの粒度・どの識別値で比べるか**
- **宣言と実装の対応をどう保証するか**

(`../design/data-model.md` の「**各項目の検査対象集合と合格述語は実装が確定する**」と同型の委任。)

**本節は、記録された承認者が実際に承認したことを保証しない。**
**7.7-2 が保証するのは「記録を伴わない更新を受理しない」ことまでである。**
**リモートのマージ保護が未達である間**(10.2)、**比較元を直接書き換える経路は本節では塞がらない。**
```
