---
feature: merge-gate-api-cycle
type: design
date: 2026-09-12
---

# 詳細設計: マージゲートと API の循環 — 選択肢の比較

**本書は選択肢の比較と 12-4 の条文判定を持つ。** 調査の事実と典拠は
[research.md](research.md) が正で、本書は内容を複製しない(設計書 7.1-1)。

## 1. 「確定ゲート数」の数え方(柱書)

比較表の「確定ゲート」列は、**設計書 7.3 の確定ゲート(敵対レビュー → 人間承認)を
何回起こすか**を数える。数え方は次の 2 条による。

| 条 | 内容 | 典拠 |
| --- | --- | --- |
| **置き場ごとの要否** | `docs/requirements/`・`docs/design/`・`docs/development/`・`docs/ops/` は**必須**。`docs/adr/` は**必須(軽量版可)**。`docs/worklog/` は**不要**(記録であり正本ではない)。`docs/features/` は**計画レビューのみ**(正本ではない) | 設計書 7.2 の表 |
| **approved の再変更** | 「**approved 済み正本の再変更は新しい版として同じゲートを通す**(軽微な誤字等は人間判断で省略可、ただし変更履歴には残す)」 | 設計書 `dev-harness-design-2026-08-07.md:435` |

したがって:

- **`data-model.md` 12-4 の改訂は 1 ゲート**(approved 済み正本の再変更)。
  ただし**通過条件の変更は「軽微な誤字等」に当たらない**ので省略できない
- **ADR の新設は 1 ゲート。ただし軽量版が使える**(7.2 の表)ため、
  `docs/design/` の全文ゲートより**レビュー面が小さい**
- **worklog への裁定の記録は 0 ゲート**
- **`docs/features/` の `design.md`(本書)は 0 ゲート**

**「人間の手番」列**は PO の裁定・承認の回数を数える。
**全案に共通する本計画の承認 1 回は除く。**

**注意**: ゲート数が少ない案が常に良いわけではない。
**0 ゲートの案は「条文に残らない」という別のコストを持つ**(7.3-5 の終結は当該ゲート内で
のみ有効で、別文書の全文レビュー周には効かない)。**表の「依存」列と併せて読むこと。**

## 2. 選択肢の比較

| 案 | 中身 | 確定ゲート | 人間の手番 | 依存 | 判定 |
| --- | --- | --- | --- | --- | --- |
| **①** | **現状維持** — 裁定しない | 0 | 0 | — | **却下** |
| **②** | **`data-model.md` 12-4 の単独改訂** — 適用単位と判定時点を条文化する | **1**(`data-model.md`) | 承認 1 + ゲート中の裁定 n | `S-4`(12-4 節冒頭の旧文言の現況化)を同乗させたい | 可。ただし実績コストが高い |
| **③** | **`NFR-010` の改訂** — 「API 直叩き含む」を書き換える | **2**(要件書 + 12-4 の追随) | 承認 2 + 裁定 n | — | **却下** |
| **④** | **例外規定の新設** — 初回免除条項を 12-4 へ置く | 1 | 承認 1 + 裁定 n | — | **却下** |
| **⑤** | **ADR-003 型の適用単位** — ゲートを「当該 PR が開く経路」単位で判定する | **単独では 0(乗り物が要る)** | 裁定 1 | ②・⑦・⑩ のいずれか | **中身としては最有力。案としては不完全** |
| **⑥** | **TSK-346(API 契約の正本)へ 12-4 の解釈を畳む** | 名目 1 / **実質 2** | 承認 1 + ゲート中の裁定 n + 後の 12-4 改訂 | — | **却下** |
| **⑦** | **ADR-004 の新設 + 12-4 の追随を同一ゲートで通す** | **1**(ADR は軽量版可) | 承認 1 + 裁定 0〜2 | 先例あり | **推奨(条文化の乗り物)** |
| **⑧** | **裁定のみ** — 条文を変えず、12-4 の literal 読みで運用する | **0** | **裁定 1** | — | **推奨(即時解除)。ただし単独では再燃する** |
| **⑨** | **deny 経路の先行投入** — 常時 404 を返すだけの入口を先に入れる | 0 | 裁定 2 | — | **却下** |
| **⑩** | **予定済みの 12-4 改訂へ相乗りする** | **+0** | 相乗り可否の裁定 1 | TSK-352 / TSK-353 の射程確認が要る | **⑦ の対抗** |

### 却下の根拠(4 件)

| 案 | 却下根拠 | 典拠 |
| --- | --- | --- |
| **①** 現状維持 | **FR の実装が止まる。** ハーネス設計書 v1.13 が同型の詰まりを記録している —「Phase 表が Phase 5 で終わることにより製品実装の位置づけが規範上不明で、**それが着手を止めていた**」 | `dev-harness-design-2026-08-07.md:64` |
| **③** `NFR-010` の改訂 | **`NFR-010` 本文「テナント分離は初日から全機能に適用する」と正面から反する。** 改善台帳 `I-2` の見出し「**マルチテナント分離を初日から完全実装する**」および 「**越境アクセスの自動テストを最初から実装する**(利用が 1 チームの段階から組み込む)」にも反する | 要件書 `:844` / `improvements-from-baseball-scoring.md:32`・`:36` |
| **④** 例外規定の新設 | **③ と同じ。** 加えて旧システムの失敗モード(アプリ層のみの分離で `owner_team_id` がオプショナル引数、省略すると全データにアクセス可能)の再現に向かう | 要件書 `:844` / `docs/legacy/research/data-layer.md:397` |
| **⑥** TSK-346 へ畳む | **射程侵犯。** 12-8 が「越境テスト再実行ゲートの定義(12-4 節)」を **`data-model.md` が確定する範囲**と宣言し(`:2748`)、射程変更を 7.3-6 の PO 裁定に限定している(`:2791`)。加えて 7.1-1「**1 テーマ 1 正本 — 内容を複製しない**」と 7.6 柱書「**同じ規則を別文書で再定義しない**」に反する。**`data-model.md` は「定義節以外が定義節の述語を散文で言い換えて持つ」型で確定ゲートを 13 周・98 件是正して打ち切った履歴**を持ち、99 件目を意図的に作ることになる | `data-model.md:2748`・`:2791`・`:2782-2789` / `dev-harness-design:406`・`:500` |
| **⑨** deny 経路の先行投入 | **常時 404 の経路に対する越境テストは何も証明せず、実装が入った瞬間に測り直しになる。** 7.3-3 の P1 の定義「実装が迷う・規約と矛盾する・**「文言は直したが実効がない」型を含む**」に当たる。加えて「製品機能」の規範的定義が正本に無いので、対象外だと言うために**もう 1 つ裁定が要る** | `dev-harness-design:461` / research.md 12 節 |

### ② の実績コスト

`data-model.md` の単独改訂は安くない。**直近の実績**では、
契約表 4 行の是正に **確定ゲートのレビュー実行 10 回・指摘 16 件・PO 裁定 4 回**を要した
(`docs/README.md` のデータモデル設計 v0.2 の行)。

### ⑦ の先例

**ADR + 正本改訂を 1 ゲートで通す型が確立している。** ADR-003 が
「本 ADR と**同一の確定ゲート**で要件書 **v2.2** として改訂する」と宣言して実施した
(`ADR-003:387`・`:152`)。ADR のゲートは 7.2 の表で「必須(**軽量版可**)」なので、
`data-model.md` 側を 1〜2 行の追記に抑えれば**レビュー面 = 指摘の湧き口が最小**になる。

### ⑩ のリスク

12-4 節には**既に未追随が 1 件あり、受け取り先が決まっている**
(`docs/features/orm-schema-migration/plan.md` の `S-4` — 「**TSK-348 / TSK-353 へ申し送る**」)。
同じ節をどのみち開けるなら 2 度別ゲートで開けるのは不合理である。

**ただし射程膨張のリスクがある。** 台帳の既存候補が
「**是正の完全性を証明する機構を同一タスクの射程へ入れると、機構自身が新しい検査面を生み、
前周修正起因の輪になる**」と記録している。**相乗り可否は TSK-352 / TSK-353 の
現在の射程を Notion で確認してから判断する。**

### 推奨の組

> **⑧ で即時解除 → ⑦ または ⑩ で条文化 → 中身は ⑤。**

**合計 確定ゲート 1 回・PO の手番 2〜3 回。** ⑥ 単独(実質 2 ゲート)より安く、
かつ循環が実際に解ける。

**⑧ を単独で終わらせない理由**: 条文に残らないと、TSK-317 の計画書が書いた
「本タスクの成果を『NFR-010 適合』と主張しない」(`pg-authz-verification-g2/plan.md:157`)
との緊張が条文上に残り、**別文書の全文レビュー周で再燃する**。

## 3. 12-4 の条文判定

(ステップ 2 で記述する)

## 4. PO 裁定 3 件

(ステップ 3 で記述する)
