---
date: 2026-09-01
revised: 2026-09-09
topic: ドメイン計算の宣言モデル(DSL)・生成器・検査基盤(TSK-235)の下調べ
status: draft
---

# 調査: ドメイン計算の宣言モデル・生成器・検査基盤(TSK-235)

調査サブエージェント **4 本**を並列で実施(要件突合 / 決定追跡 / 旧システム事実 / 機構配線)。
**重い指摘は Claude が原典で直接確認した(◎印)**。

> ⚠️ **§0〜§5 は 2026-09-01 時点の調査であり、`ADR-003 v0.1` / 要件書 `v2.5` 以前を典拠としている。**
> `ADR-003 v0.2`(2026-09-03 approved)・要件書 `v2.6`(同日)・`v2.7`(2026-09-07)の確定により
> **一部の前提が失効した**。差分の突合は **[§6 改訂調査(2026-09-09)](#6-改訂調査2026-09-09-adr-003-v02--要件書-v27-差分の突合)** が正で、
> **§0〜§5 と食い違う場合は §6 が新しい**。失効した節の一覧は §6-13。

## 0. 結論(先出し)

### ◎ 0-1. 本タスクの射程は `ADR-003` の「着手順序 1」に限る

ADR-003 の着手順序(`:373-377`)は 4 段階で、段階 1 は逐語で:

> **ベクタの schema と独立に確認した期待値、および検査基盤(生成器・マニフェスト・収集器・CI 配線)を先に確定する**
> (**この段階では対象計算のコードを追加しない**)

段階 2(対象計算 1 件ごとに正本・生成物・呼び出し部・3 層検査を**同一の変更**として追加)には
**TSK-236〜239 の当該契約が必要**であり、本タスクでは着手できない。

→ **本タスクの完了状態は「検査基盤が『対象計算が 0 件・ベクタが未整備』であることを正しく不合格として報告する」状態**である。
要件書 `:900` が「**正解ベクタが未整備の間…不合格として扱う**」(fail-closed)と定めているため、これが正しい完成形。

**「CI が緑」を完了条件に置けない。** ADR `:387`(23 周目で表現を排除):

> **「緑になってからコードを追加する」とは書かない** — 検査は生成物・製品入口・変異対象が存在しないと緑にできず循環するため

### ◎ 0-2. 前提ブロッカ 2 件のうち 1 件は解消済み

| Notion 本文のブロッカ | 現況 |
| --- | --- |
| **TSK-223(backend 骨格)が未着手** | **解消**。`origin/develop` の `backend/` は **15 ファイル**。**ただし `backend/domain/` は存在しない** |
| **ベクタの中身が未整備** | **未解消。しかし着手順序 1 は影響を受けない**(§0-1) |

### ◎ 0-3. 人間の裁定を要する論点 3 件(2026-09-01 に裁定済み)

| # | 論点 | 裁定 |
| --- | --- | --- |
| 1 | `core-areas.json` の paths を本タスクで充填するか | **本タスクが新設するファイルだけを `game-state.paths` へ登録する。全 5 領域の充填は TSK-228 に残す** |
| 2 | 変異テストの時間予算に収まらない場合 | **計画の早い段階で 1 対象計算の実測を行う**(ADR `:220` が「上限そのものは Phase 4 で実測して見直す」と明示)。収まらなければ高速化・並列化で詰め、それでも超えれば ADR 改訂ゲートへ戻す |
| 3 | `D`(履歴文脈の検査上限)の値 | **数字を選ばず、必須 case から下限を導く**。`D ≥ 4`(§3-3)。値の確定は「`D` と `D+1` で挙動が変わらない」ことをプロパティで示す |

---

## 1. 要件書が課す機械判定可能な要求(spec-checker)

### 1-1. ◎ 記号の対応(誤りやすいので先に固定)

| 記号 | 内容 | 典拠 |
| --- | --- | --- |
| NFR-018 (a) | 実装の単一性 | 要件書 `:888` |
| NFR-018 (b) | 宣言と機械判定 | `:889` |
| NFR-018 (c) | 逐語移植対象の例外 | `:902` |
| NFR-018 (d) | 実装分類の列挙の一元性 | `:908` |
| NFR-019 (a) | クライアント/サーバーの一致性 | `:915` |
| NFR-019 (b) | 越境アクセス | `:923` |
| NFR-019 (c) | 主要分岐の E2E | `:923` |
| NFR-019 (d) | 同期プロトコルの故障系 | `:923-927` |

いずれも優先度 **Must**。

### 1-2. ◎ (a) の単一性そのものは機械判定の対象ではない

要件書 `:898`(保証の範囲外・逐語):

> ③**正本と常に同一の値を返す再実装が存在する事実**(値が同一である限り本項の対象となる差は生じない。
> ただし**上位要件のコピー実装の禁止と(a)の単一性には違反する**ため、**(a)とレビューで抑止する**)

→ **「同一値を返す再実装の存在」は (b)② の保証範囲外**であり、抑止手段は**人手レビュー**。
**本タスクの DoD に「(a) を機械判定する」を置けない。**

### 1-3. ◎ (b)② の「経路一致」は要件書に直接の典拠がある

要件書 `:892`(逐語):

> **あわせて、同一入力に対する「実行入口を通した出力」と「正本の生成物を直接呼んだ出力」の一致そのものを合否条件とする**
> (**期待値と一致していても両者が食い違えば不合格**。製品経路が正本以外の実装を使っている事実を、値の面で直接検出するため)

→ **合否条件は 3 つ**: ①入口経由 = 期待値 / ②生成物直呼び = 期待値 / **③① = ②**。
③は独立の条件であり、期待値一致では代替できない。ADR は 9 周目でこれを **5 判定**へ拡張(§2-4)。

### 1-4. (b) の 3 層と fail-closed

**3 層**(`:894-897`)— 「**いずれか 1 層でも欠ければ本要件を満たさない**」:

| 層 | 要求 |
| --- | --- |
| 網羅的な正解ベクタ | 入力範囲を**全件**覆う(「代表例だけで済ませない」) |
| プロパティテスト | **不変条件**と**正本から生成した各実行体どうしの等価性**の**両方**。「区分ごとの対象は設計フェーズの ADR が定める」 |
| 変異テスト | 上位 2 層の**実効性そのものを測定**。**非等価変異の生存を認めない(生存 0)**。等価変異は**人手で判定**して分母から除外し、**除外の記録を残す** |

**fail-closed は 2 箇所にある**:

- `:886`(未分類の対象計算)— 「**当該計算の実装を開始せず、本書の改訂で区分を確定する**」「**自動的に(α)へ倒さない**」「**対象外として逃がすこともしない**」
- `:900`(判定)— 「**未分類・判定不能は合格としない**」「**正解ベクタが未整備の間、変異テストの生存率が閾値を超える間は、いずれも不合格**」

**用語の不統一(実害は小)**: 合否基準は `:897`・`:909` が「**非等価変異の生存 0**」と明記する一方、
`:900`・`:1027`・`:1046` は「生存率」「閾値」と書く。**基準値は 0 と読むのが唯一整合する。**

**退路**(`:901`): 「**①〜③のいずれかが実現不能と ADR が結論した場合は実装を開始せず、本書を `in-review` へ戻して再改訂する**」

### 1-5. (b)③ 構成の完全性 — 9 点(`:899`)

1. 本番到達可能な実行入口を宣言する
2. **宣言外の入口を拒否する**(検出ではなく拒否)
3. 探索対象に **Web Worker・Service Worker・別 HTML・静的配信配下**を含む
4. 本番のモジュール・生成物の集合が宣言と一致する
5. **サーバー側も同等に閉じる**
6. 動的コード生成の禁止と違反検出
7. 非リテラルな動的読み込みの禁止と違反検出
8. 外部スクリプト実行の禁止と違反検出
9. **検査手段を複数重ね、かつ実行時の制限も併用する**(静的検査のみは不適合)

### 1-6. 対象計算の列挙 — (α)5 件・(β)①〜⑧(`:883-885`)

**柱書の 5 名称(状況判定・座標変換・捕球選手推定・成績集計の前処理・終了判定)は分類名であり (α)5 件と一対一ではない。**

**(α)**(クライアント・サーバー双方で実行 — NFR-019(a) の対象):

1. 状況判定
2. **入力と表示のための座標変換** — 画面座標⇄保存座標の相互変換 + 内外角の導出(付録B-3)**に限る**。
   「高低等の別の導出を対象に加えるには**本書の改訂を要する**」。**打球方向の導出は含めない**((β)⑦)
3. 捕球選手推定
4. 終了判定
5. **断中にクライアントで実行する成績集計の前処理**(FR-020・FR-021・FR-022・FR-023)

**(β)**(サーバーのみ。判定規則 = 「**クライアントでの実行を要求する条項が存在しないもの**」):
①投手/打者/作戦分析の集計 ②カルテ掲載指標 ③シーズン通算 ④共同分析グループの共有集計
⑤チーム単位の分析 ⑥**移行時の座標変換** ⑦**打球方向の導出** ⑧**移行時の責任投手・成績帰属の導出**

**区分の用途は 2 つに限られる**(`:883`)— 「上記の要件と NFR-019(a) の対象判定に限る。
**コア領域の境界〔設計書 6.3〕には用いない**」。

**生成先の言語 matrix は要件書に無い**。決めているのは ADR-003 D-1(§2-1)。

### 1-7. ◎ NFR-019(a) の比較面 — 5 対象の入力・出力(`:917-921`)

| 対象 | 入力 | 出力 |
| --- | --- | --- |
| **状況判定** | **事前状態 + 履歴文脈 + 次イベント 1 件** | 回・表裏・得点・S/B・アウト・打順・走者 + **スコアボード全欄** + **成績計上フラグ全欄** + **遷移後の履歴文脈(スタック全体・各要素が種別と差分)** + **操作結果の区分** |
| **終了判定** | プレイ入力列 + **規則スナップショット** | 終了条件の成立・入力ロック・終了宣言の促し(**試合状態が自動遷移しないことを含む**)・コールド・サヨナラ・X 表記 |
| **座標変換** | 画面座標 / 保存座標 + 打席左右 | 相互変換の結果(**往復して元に戻ることを含む**)と内外角の導出値 |
| **捕球選手推定** | 打球位置座標 | 守備番号 |
| **断中の前処理** | プレイ入力列 + **語彙スナップショット** | FR-021 の全指標・FR-022 の全列・FR-023 の球種割合と併記する球数 |

**比較面から項目を除外するには要件書の改訂を要する**(`:915`)。

### 1-8. ◎ FR-006 / FR-040 と履歴文脈

**◎ 要件書に「連鎖」の語は 0 件。** 「スタックが空になるまで続く」「実行時のスタック長に上限はない」は
**ADR-003 と worklog にあり、要件書には無い**。

要件書が履歴文脈に触れるのは **2 行だけ**:

- `:234`(FR-006)「取消可能な操作が 1 件も無い(**履歴文脈が空**)/ When undo /
  Then **状態を変えず、取り消す対象が無いことを利用者へ示す**」
- `:917`(NFR-019(a))「**遷移後の履歴文脈(取消可能な操作のスタック全体 — 各要素は種別と差分。先頭 1 件だけではない)**」

**深さの規定は無い。**

**FR-006 補足**(`:236`): 「**undo を適用した後は、取り消された操作の 1 つ前の取消可能な操作が次の対象になる**
(例: 状態補正 → プレイ → undo は当該プレイを取り消し、続く undo は状態補正を取り消す)」

**FR-040 は Should**(`:333`)で、**採否の決定記録が要件書に存在しない**(10 章にも行が無い)。
4.0-4(`:173`)は「**採用した場合のみ第 7 の種別として加わる**」「**未採用の Should を実装したことにする根拠にはならない**」。

### 1-9. ◎ 語彙と表示規則

**4.0-3 の「挙動が紐づく語彙」は 3 件**(`:163`): **打撃結果(状況自動更新と連動)・打球情報・シーズン**。
管理主体は**システム管理者**。「**改名は表示名の変更**として ID 経由で全履歴表示に反映される」(`:166`)。

→ **宣言モデルは打撃結果を ID で参照し、表示名を挙動の分岐に使ってはならない。**

**機械判定できる規律 2 件**:

- `:168`「**球種名のハードコードによるグループ分けを実装しない**」(系統属性を軸とする)
- `:167`「チーム追加の作戦は傾向集計のみ。**成否自動判定は付録A-4 の既定義作戦に限る**」

**◎ 合成表記の規則本体は要件書に無い。** `:433` は「見三振／空三振／`{捕球選手}{打球タイプ}` **等**」の
例示 3 件で止まる。一方 `:921` は「**完成した表示値として比較する**」ことを要求し `:915` が
「一部だけを比較面に採ることはできない」と定める。**規則本体が無いまま完成値の一致を求める構造。**

**◎ 「表示規則」という名前の節・付録は要件書に存在しない**(語の出現は `:917` の 1 箇所)。
実体は付録A-1 の個別規則のみ(`:1084`「0除算となる指標は「−」を表示する(エラー・0.00にしない)」)。

### 1-10. ◎ 触ってはならない領域(要件書の改訂を要する)

| 行為 | 典拠 |
| --- | --- |
| NFR-018 の対象列挙に追加・削除 | `:885`「追加・削除には本書の改訂を要する」 |
| (α) 座標変換に高低等の導出を加える | `:884` |
| **(c) 例外表の行を追加・削除・失効** | `:902`「実装判断で増やさない・消さない」 |
| NFR-019(a) の比較面から項目を除外 | `:915` |
| 区分未確定の対象計算の実装開始 | `:886`(fail-closed) |
| `contracts/display_geometry_263_v1.json` の移動 | `:906` + ADR D-12(要件書改訂と同一 PR で原子的に) |

**(c) 例外表の唯一の行は現在「有効化待ち」= 違反状態**(`:26`)。**本タスクでは触らない。**

### 1-11. Won't(2.2)— 設計に入れてはならないもの

| Won't | 典拠 | 衝突する設計 |
| --- | --- | --- |
| 防御率・自責点の算出・表示 | `:91` | 対象計算に自責点/防御率を含める |
| 勝敗投手・セーブの自動判定 | `:92` | 終了判定の出力に含める |
| イレギュラー判定の専用自動化 | `:93` | 宣言モデルに実装する |
| **チーム別カスタムの挙動付き語彙** | `:95` | **宣言モデルにテナント別の挙動分岐を持たせる** |
| 多言語対応 | `:100` | 表示名解決に言語軸を持たせる |

---

## 2. ADR-003 の決定(decision-tracer)

**D-6(契約表)・D-8(比較面)・D-12(`contracts/` 構造)は TSK-233 の下調べで全行を写済み。**
本節はそれ以外の D-* と決定の空白。

### 2-1. D-1(正本の実装方式)の全項目

| 項目 | 決定 | 行 |
| --- | --- | --- |
| **正本** | **制約された型付きの宣言モデル**。人手で保守するのはこのモデルだけ | `:95` |
| **射程** | 状態 / イベント / ガード / 次状態 / **整数・boolean・enum** / 四則・比較・min・max / **明示的な丸め** / **有限の** reducer / 入出力の型・範囲・単位・scale / **nullable** / **enum → 表示名の写像テーブル**。**これ以外の計算を正本に書かない**(表せないものが出たら**見直しトリガー**) | `:96` |
| **生成物(target matrix)** | (α) = **Python + TypeScript** /(β)①〜⑤⑦ = **SQL 式 + Python の型付き受け口**(**TypeScript は作らない**)/(β)⑥⑧ = **Python のみ**。全生成先に共通で hash 埋め込み・再生成差分の検出・結果型の構築規則 | `:97` |
| **生成前検査** | 遷移の**重複・欠落・到達不能** / 演算の型と整数範囲 / **除算・丸め規則の指定漏れ** | `:98` |
| **生成物への埋め込み** | 生成元の位置 + **正本の内容 hash と生成器のバージョン**((b)① の乖離検出に使う) | `:99` |
| **置き場** | 正本と生成器は **`backend/` 配下**。生成物は backend 側と **`frontend/src/lib/generated/`**。**`contracts/` には置かない** | `:100` |

**枝番 3 件**(a は無い):

| 枝番 | 内容 |
| --- | --- |
| **D-1-b** | **`contracts/` に正本・生成器・生成物を置かない**(設計書 4 章の限定と衝突させない。「本 ADR は同章に触れない」) |
| **D-1-c** | **状態テーブルを両言語の手書き interpreter で読む方式は採らない**(interpreter 自体が 2 つの人手保守実装になり (a) を満たさない) |
| **D-1-d** | **(β)①〜⑤⑦ の集計は宣言モデルから DB 側集計の式(SQL 式)を生成する**(NFR-005 が全件読み込みを禁じる) |

### 2-2. ◎ 表示規則の限定と、旧の実体が収まらない可能性

D-1 `:96`(逐語):

> **自由な文字列処理は正本の射程に含めない**が、**宣言的な表示規則は含める**。
> **表示規則として許すのは 2 種だけ**: **①enum → 表示名の写像テーブル** /
> **②プレースホルダの差し込みだけを行う宣言的テンプレート**(例: `{捕球選手}{打球タイプ}`。
> **条件分岐・部分文字列操作・数値整形ロジックは書けない**)

さらに:
- **①の写像テーブルは「管理者管理でない安定した enum」に限る**
- **管理者が改名できる語彙(打撃結果・打球情報・シーズン)の表示名は写像テーブルに焼き込まない**
- 18 周目(`:28`): **データ参照による名称解決は対象計算の出力ではない(比較面にも入らない)**
- **対象計算は「①構造化値」と「②表示規則を適用した表示値」の両方を返す**
- **0 除算時は構造化値 `null` + 表示値「−」**(**表示規則で対応づける** — 比較器で吸収しない)
- 責任投手は**選手 ID(または `null`)を返し、氏名の解決は射程外**
- **写像テーブルとテンプレートも生成対象**であり、(b)① の乖離検出・直接呼び出し対象・変異テストの対象に含まれる

**◎ 旧の実体で 2 種に収まりにくいもの(§4-4 参照)**:

| 実体 | 難点 |
| --- | --- |
| `{捕球選手}ライナー併` | 同じ「打球タイプ = L」を、併殺打では `L`、ライナー併殺では `ライナー` と**別表記でレンダリング** |
| 打者状況の読み替え(長打時「走塁死」→「二塁アウト」) | **打撃結果を条件とする写像** — 単一キーの写像テーブルでは表現できない |
| 投球回 3 表現(`1.1` / `1.333` / `1⅓`) | 数値 → 記号の変換 |
| 構え(aim 9 分類 → ゾーン番号 `[7,6,1,2]`) | **1 対多写像** |
| **打率等の数値書式**(`.333`) | 「数値整形は書けない」と「完成した表示値を返す」の**帰属先が決まっていない** |

→ **D-1 の見直しトリガーに触れる可能性がある。計画で正面から扱う。**

### 2-3. 語彙スナップショットを入力に取る対象は 1 件だけ

21 周目(`:31`)で一意化:

| 対象 | 語彙スナップショット |
| --- | --- |
| **断中の成績集計の前処理** | **取る(唯一)** |
| **状況判定** | **取らない**(21 周目で外した。出力に表示名を含まないため — D-8 `:308`) |
| 終了判定 | 取らない(第 2 の軸は**規則スナップショット**) |

### 2-4. D-11 — 3 層・経路一致検査・マニフェスト

**② 保証する対象**(`:185`):

> **宣言された実行入口を通した**対象計算の出力。**生成物を直接呼ぶだけの検査にはしない** —
> 旧システムの事故(責任投手モデルを新設したのに表示される失点は旧方式のまま)は
> **製品経路が別の実装を使っていた**形であり、生成物を直接叩く検査では捕まらない

**◎ この事故記述の典拠について**: `docs/legacy/` に「責任投手」の語は **0 件**(Claude が確認)。
典拠は `docs/features/nfr018-domain-calc-adr/research.md:101` と
`docs/features/req-v2-legacy-parity/research.md:119`(いずれも**調査文書であって正本ではない**)。
→ **ADR の最重要決定の根拠が版固定アーカイブに無い**。事実として記録する(ADR の当否は判定しない)。

**経路一致検査の変遷**:

| 版 | 内容 |
| --- | --- |
| 3 判定(8 周目) | 「①=期待値」「②=期待値」「**①=②**」 |
| **5 判定(9 周目・現行)** | 「**①実行済み・②実行済み・①=expected・②=expected・①=②**」。**いずれか不成立で fail**。**製品経路だけを実行した case で「実行済み」にできない** |
| **要求集合(10 周目・現行)** | **`(calculation, vector, case, runner, entrypointId, directTargetId)` の直積**。**要求集合と証跡集合の差が 1 件でもあれば fail**。**入口が 2 つあり片方だけ実行した場合も差分として検出** |

**`comparison` は lossless に限る**(`:231`): 禁止 = フィールドの除外・`NULL` と既定値の同値化・
値を丸めて差を吸収する変換。許すのは行の全順序による並べ替えと情報を落とさない型の統一だけ。
**比較器は値を丸めない**。**未知フィールド・欠落フィールドはいずれも fail**。

**◎ 独立収集器**(`:235`):

> **証跡はアダプタの自己申告ではなく、CI 側の独立した収集器が生成する** — 収集器はアダプタから
> **生の出力・実行した artifact の hash・(β) では実際に発行された SQL 文または AST** を受け取り、
> **等値判定は収集器が計算する**(アダプタが `true` を 5 個返すだけで通る経路を閉じる)。
> **受け取った hash が `generated[]` の現在の hash と一致しない場合も fail**

**実装・配置・入出力形式は未決**(「証跡ファイルの具体形式」として送り出し済み)。

**マニフェスト `backend/domain/manifest.json` の全フィールド**(`:231`):

`calculation` / `source` / `generated[]` / **`entrypoints[]`** / **`directTargets[]`** / `comparison` /
`normalization[]` / **`vectors[]`** / **`properties[]`** / **`mutation`** / `propertyCatalog`

**空を許さない**: `entrypoints[]` / `directTargets[]` / `vectors[]` / `properties[]` / `mutation`。
`properties[]` は**対象計算ごとに `invariant` と `equivalence` を各 1 件以上**。

**◎ 記述の非対称**: `propertyCatalog` は `:205` と変更履歴 `:20` にあるが、
**`:231` の「宣言の正」の列挙に現れない**。schema 設計時に取りこぼす危険。

**3 層の登録漏れ検出**(`:235`): マニフェストの `calculation` 集合と**各層が実際に実行した対象計算の集合**を
突合し、**差が 1 件でもあれば fail**。層ごとの実行集合の定義:

| 層 | 実行集合の定義 |
| --- | --- |
| 正解ベクタ | 消費されたベクタファイルが覆う対象計算 |
| **プロパティ** | **ランナーの機械可読な実行証跡**から集計した**実際に完走したテスト**。**skip・xfail・todo・0 ケース生成は「実行済み」に数えない**。`calculation` / `propertyId` / `propertyKind` / `generatedCases` / `status` の **5 項目を出力しないテストは実行済みに数えない**(pytest = JUnit XML の `properties` / Vitest = reporter の JSON) |
| 変異 | 変異が生成・実行された対象計算(**生成変異数 0 は fail**) |

**不変条件カタログ**(`:205`): 要件・付録由来の**独立述語**で、**生成器が読むことを禁止**。典拠必須・
`propertyCatalog` 登録・**allowlist とファイルアクセス証跡の突合** + 生成器依存グラフにカタログが
現れないことの静的検査。

対象計算ごとの不変条件: 状況判定/終了判定 = S≤2・B≤3・アウト≤2・同一塁の走者重複なし・
**通常の前進プレイに限った**得点の単調性(状態補正・undo は対象外)/ 座標変換 = 定義域内で全射・単調・
**クランプの冪等性** / 捕球選手推定 = 出力が閉じた集合に属し**タイブレーク規則適用後の帰属が一意** /
集計 = 分母 0 で構造化値 `null`・表示値「−」・**計数と率の非負性**(FIP・wRAA など負値を取り得る指標には課さない)・
部分和の総和一致 / 移行導出 = 出力が入力の値域に閉じ NULL 条件が排他。

**変異テストの運用規則 8 項目**(`:212-219`):

| 項目 | 決定 |
| --- | --- |
| 言語別の変異演算子 | Python/TS = 算術・比較の境界移動・真偽値反転・定数の増減・条件分岐の除去。**SQL 式** = 集計関数の置換・`WHERE` 述語の反転・`GROUP BY` キーの欠落・`JOIN` 条件の緩和 |
| 等価変異 | **人手で判定したものだけ**を分母から除外し理由と判定者を記録(**記録のないものは非等価扱い**) |
| 未対応変異 | **未対応が 1 件でもあれば fail**。**生成された変異数が 0 の対象計算も fail**。未対応箇所の一覧を毎回出力 |
| 判定の単位 | **対象計算ごと**(全体集約にしない) |
| 全面が PR 必須になる発火条件 | 宣言マニフェスト / 生成器 / 正本の宣言モデル / 変異器の設定 / 等価変異台帳 / 正解ベクタ・プロパティテストの基盤 / 実行入口の構成。**影響範囲を逆引きできない変更も全面**(fail-closed) |
| kill 要因の記録 | 必須。**プロパティ層の kill が 0 件の対象計算は fail**。**(b)① 乖離検出による kill は分子に数えない** |
| 等価変異の記録先 | **`backend/domain/mutation-equivalents.json`**(識別子・理由・判定者・判定日)。**判定者は PO** |
| **実行時間の上限** | **PR 必須分の合計 10 分** / **全面発火 PR は 30 分**。**超過はタイムアウト = fail** |

### 2-5. ◎ D-7 の実行契機 — 6 機構すべて PR 必須・マージ不可

| # | 検証機構 | 実行契機 | 失敗時 |
| --- | --- | --- | --- |
| 1 | 網羅的な正解ベクタ(D-6 の①契約の全行) | **PR 必須** | **マージ不可** |
| 2 | 差分テスト(両実行系の出力突合) | **PR 必須** | **マージ不可** |
| 3 | プロパティテスト | **PR 必須(seed 固定)** + 夜間(拡大) | PR = マージ不可 / 夜間 = **起票して追跡し、縮小した再現ケースを正解ベクタへ case として追加** |
| 4 | 変異テスト | **PR 必須**(差分 10 分 / 全面 30 分) | **マージ不可**(生存 1 件・上限超過はいずれも fail) |
| 5 | (b)① 乖離検出 | **PR 必須** | **マージ不可** |
| 6 | (b)③ 構成の完全性 | **PR 必須** | **マージ不可**。「**これをもって要件書 v2.1 の (b)② を満たしたとはしない**」 |

**◎ 経路一致検査は D-7 の独立行になっていない**(D-11 では 1 層目に同梱)。**CI ジョブとしての帰属先が一意でない。**

**◎ 時間上限の見直し規定**(`:220` 末尾・逐語):

> **上限そのものは Phase 4 で実測して見直す**

かつ:

> 収まらない場合は**変異器の高速化・並列化・CI ランナーの増強**で対処し、
> **必須範囲の縮小でも夜間への移送でも対処しない**(夜間枠は補助検査に限る)。
> それでも超える場合は**上限の見直し自体を本 ADR の改訂ゲートで行い、上限を超えたまま暫定でマージする経路は設けない**

→ **実測は迂回ではなく ADR が定めた経路。**

### 2-6. D-3(生成物のコミット規律)

- 生成物は**生成器の実行結果のみ**をコミットする(= **コミットする**)
- **手で直した生成物は (b)① が検出して CI を失敗させる**
- 正本を変更した PR は**生成物の再生成を同一コミットに含める**(片側コミットを作らない)
- 生成器のバージョンを上げた PR も**全生成物を再生成する**

**◎ 「コミット単位」で片側性を検査する機構は存在しない**(規範文のみ)。実効は **PR 単位の (b)① 乖離検出**。

### 2-7. ◎ 決定の空白(いずれも「存在しない」を確認)

| 論点 | 状態 |
| --- | --- |
| **DSL の記述形式**(JSON / YAML / Python DSL / 独自) | **決定が存在しない**。`contracts/` の版付き JSON は**ベクタ契約の形式**で DSL 正本の形式ではない |
| **生成器の実装言語** | **決定が存在しない**(置き場「`backend/` 配下」のみ) |
| **プロパティテストのライブラリ** | **決定が存在しない**(調査文書に hypothesis / fast-check の名前があるだけ) |
| **変異テストのツール** | **決定が存在しない**。**SQL 式の変異器の実現手段も未決** |
| **独立収集器の実装・配置・入出力形式** | **決定が存在しない** |
| **生成物の backend 側の具体パス** | **決定が存在しない** |
| **`D` の値** | **決定が存在しない**(候補値の記述もなし) |
| **CI ジョブ名・ジョブ分割** | 設計書 10.1 に `consistency` の行はあるが他の 5 機構の行が無い |
| **`propertyCatalog` の schema** | 未定 |
| **契約自身の JSON Schema の置き場** | 未決 |

**◎ 調査文書に残る旧記述の誤誘導**: `docs/features/nfr018-domain-calc-adr/research.md:85`
「変異テストは**毎 PR でなく夜間 CI で可**」は **ADR 反映 3 周目の決定(PR 必須)と矛盾**する。
research は正本ではないが、本タスクの入力として読むと誤る。

### 2-8. 見直しトリガー(2 系統)

**送り出しに付随**(`:238`): 「細部の確定が本 ADR の決定(**特に D-1 の射程・D-6 の契約分類・
D-11 の 3 層と経路一致検査**)と両立しないと判明した時点で、**本 ADR を改訂ゲートへ戻す**」

**帰結節の 5 件**(`:392-398`): ①**DSL の射程で表せないドメイン計算が現れたとき**
②**生成器の保守コストが実測で見合わなくなったとき**(1 名開発体制で維持できない)
③要件書 v2.2 の (b)② が再び改訂されたとき ④付録A 契約の実装が完了したとき ⑤対象技術の世代交代

**加えて**: D-1 表内(`:96`)「表せないものが出たら D-1 の見直しトリガー」/ 変異テストの時間上限超過(`:220`)。

---

## 3. `D`(履歴文脈の検査上限)— 導出

### 3-1. ADR が課している制約

- 深さは **0 / 1 / 2 / `D`** の 4 点(D-6 `:255`)
- 構成は**スタック各要素の種別による等価分割 4 通り**(先頭が確定プレイ / 先頭が状態補正 /
  先頭と 2 件目で種別が異なる / 全要素が同種)
- シナリオ case の長さは **1 / 2 / `D` / `D`+1**
- **`D`+1 は「不正入力」ではなく「(b)② の保証範囲外」**。**製品は `D` を超える履歴でも拒否せず動作する**
- **`D` は検査が保証する範囲の上限であって実行時の上限ではない**
- **履歴文脈は全列挙の軸に入れない**
- **深さは固定値ではなく宣言モデルの schema が上限 `D` を定める**(`:29`)

### 3-2. ◎ 要件書側に典拠が無い

要件書 `:893` は「有限にならない対象(連続値の変換等)は**境界値・等価分割・範囲の端点を明示的に列挙した集合**とし、
**その集合を要件・付録から導出して固定する**」と要求する。
一方、要件書に履歴の深さの記述は無い(§1-8)。

### 3-3. ◎ 必須 case から下限を導く

`ADR-003` D-6 `:255` が必須と定める undo の case 3 本:

| # | シナリオ | イベント数 |
| --- | --- | --- |
| ① | 補正 → undo → 補正前 | 2 |
| ② | 補正 → プレイ → undo → 補正後 | 3 |
| **③** | **補正 → プレイ → undo → undo → 補正前** | **4** |

シナリオ長を `1 / 2 / D / D+1` で固定する規定と併せると、**必須シナリオを表現できる下限は `D` ≥ 4**。

**この下限は FR-006(`:236` の「1 つ前が次の対象になる」)と FR-040(`:337` の「補正が取り消され補正前に戻る」)
から導かれるため、要件書 `:893` の「要件・付録から導出して固定する」を満たす。**

### 3-4. 値の確定方法(提案)

`D` = **max(必須シナリオ長 = 4, 等価分割 4 通りを実現できる最小深さ)** とし、
**プロパティテストで「深さ `D` と `D`+1 で出力が変わらない」ことを示す**。
これにより `D` は恣意的な定数ではなく**測られた境界**になる。

---

## 4. 旧システムの事実(legacy-analyst)

**`docs/legacy/` は版固定アーカイブ — 読み取りのみ。**

### 4-1. ◎ 旧に「複数実装が同じ値を返すことを固定する機構」は無かった

**決定的な証拠は旧システム自身の要件書** `requirements-tsukuba-pss-v0.2.md:421-425`:

- 既知の多重実装: **捕球選手推定(Streamlit 版・API 内コピー・フロント TS 移植の 3 実装)**、
  **分析前処理(plays_cache と API 側の 2 実装)**、**カルテ PDF(2 実装)**
- **測定方法: 「コードレビューと共有モジュールのテスト」= 人手**
- 優先度: **Should**

得点計算については資料が「**両者の一致が暗黙の前提**」と明記(`domain-logic.md:113`・`:174`)。

**全 12 本を通読・検索したが、2 つ以上の実装の出力値を突き合わせるテストの記載は存在しない。**

### 4-2. ◎ ADR-003 の「固定機構は 3 種類」は 2 点で不正確

**(1) 4 つ目が本文から落ちている。** `ADR-003 旧システムが示した事実` は「3 種類」とするが、
**ADR 自身の着手前調査は 4 行の表**を持ち、4 行目に「**定義の固定**」がある
(`docs/features/adr003-domain-calc-method/research.md:129`)。
**しかもこれだけが明示的に多重実装向け** — `domain/batting_results.py` は
「成績計算 **3 実装**(charts/batting・charts/pitching・analytics)の**定義統一用**」(`domain-logic.md:85`)。

→ **本タスクにとって重要**: 4 つ目こそが「**入力側の正規化規則を契約に含める**」という D-6 の決定の実例。

**(2) 「2 DDL 同期(唯一の実装横断固定)」は実際は 1 テーブル分しか働いていない。**
`ADR-003 旧システムが示した事実` は「別実装間の突合テスト」とするが、
`baseball-scoring-db-structure.md:284` は「**parity テストは play_player_link の断片のみ**」と明記。

### 4-3. ◎ 前処理が指標値を左右していた — 裏づけられる

**最も強い証拠**(`docs-tests-ops.md:180`・`:51`):

> **成績計算の公式定義**: 三振略記の正規化…**監査で「表示成績が信頼できない」状態から立て直した経緯があり**、
> これらの定義テストは新実装でもそのまま契約として移植すべき

監査(2026-06-12・57 件の指摘)で **P0「三振略記不一致」**が挙がり、
是正手段は**共有定数の新設 + 定義テスト**(`test_batting_result_definitions.py` 7 本)。

**前処理の実体**(いずれも指標値を決めていた):

| 前処理 | 実体 |
| --- | --- |
| 派生列の付与 | `コースYadj = 263 − コースY` / 守備チーム / 攻撃チーム / `_date`。**`plays_cache` と API 側の 2 実装** |
| センチネル除外 | `0`(int)・`"0"`(str)・`""`・NULL が同居。集計は `x<=0` / `球速>0` / `氏名 != "0"` で除外 |
| 行の種別で絞る | **旧集計は一貫して `プレイの種類 == '投球'` で絞る** |
| 列名と中身の乖離を前提にした集計 | エラー数を **`タイムの種類.isin([1..9])`** で数える(列名は「タイムの種類」だが中身は守備番号) |
| 結果球ベースの紐付け | 打者指標は**打席完了行ベース** |
| 対左右分類 | **両打ちは投手の逆をプリフィル**、打席ごとに上書き可 |
| 欠損球速の除外 | `compose_pitch_speed` が `00`→`0`(未入力)、集計は `球速>0` で除外 |
| ゾーン率の閾値 | `コースX/Yadj ∈ [53,210]`(**反転済み派生列**を前提) |

**同じ元データが経路によって別の値になる実例**:
帳票だけの合成座標生成 `synthetic_batted_ball`(`analytics-reports.md:86`)/
打球位置の `(0,0)` 強制上書き(`input-screen.md:210`)/ `_row_to_db` の空文字→NULL 変換。

**投球回は同じ元データが 3 つの数値になる**: `1.1`(表示)/ `1.333`(率計算)/ `1⅓`(スコアブック)。

**◎ 0 除算の旧の扱いは `docs/legacy/` に記述が無い(不明)。** ADR `:96` の
「構造化値 `null` + 表示値「−」」は**新システム側の決定**であり、旧の事実としての典拠は無い。

**◎ 旧に年度境界の概念は無い。** 「シーズン通算」= 全期間累計または利用者指定の任意日付範囲。
列 `Season` は「春季/夏季/秋季/冬季」の試合属性で、集計の境界として使われた記載は無い。

### 4-4. 旧の純粋性 — おおむね成立、例外 2 件

`domain/` 12 モジュールは Streamlit 非依存の純粋関数群で `st.session_state` 参照は **0(grep 検証済み)**。
docstring に **"Pure game-state transitions"**。**素の Python 3.14 で 95 本パス**。

**例外**:
- **`scoreboard.apply_scoreboard_event`** — 「16 要素スコアリスト×2(**in-place 破壊的変更**)」
- **`team_theme.py`** — ドメインロジックではなく UI テーマ。**CSS linear-gradient 文字列を生成**
- `input_validation.py` / `pitch_speed.py` は純粋だが**現 UI の入力手順に意味的に結合**
- **pandas 依存は `pitching_innings.count_pitcher_outs` の 1 箇所のみ**(再利用分類 B)

### 4-5. ◎ 旧テストは `docs/legacy/` に同梱されていない

`docs/legacy/` 配下は **`.md` 12 本のみ**(`.py` / `.json` / `.sql` は 0 件・Claude が確認)。

旧テストの規模(**資料間で数字が割れている**): 38 ファイル・300 テストメソッド(`docs-tests-ops.md:69`)vs
37 ファイル・300 テスト関数(`domain-logic.md:162`)。

**◎ 重い事実**: `requirements-tsukuba-pss-v0.2.md:505`
「**pytest 形式テスト 74 件超が未実行・1 件が恒常 FAIL**」。
→ 「300 件が契約を固定していた」という理解はこの事実で限定される。

**テストが無かった領域**: `game_end_rules.py` に専用テストなし / **カバレッジ計測なし** /
**CI 設定なし** / リプレイ恒等式は 2 本かつ **UI 未使用** / DDL parity は 1 テーブル分。

### 4-6. ◎ 3 層検査で防げない旧の失敗が 3 つある

| 旧の失敗 | 3 層検査で防げるか |
| --- | --- |
| 三振略記の表記ゆれで指標値がずれた | **防げる**(正規化規則を契約に含める) |
| 得点の二重管理 | **防げる**(正本 1 系統からの生成) |
| リプレイ検証が UI から未使用だった | **防げる**(経路一致検査がまさにこれ) |
| **`SELECT *` + 位置ベース復元** | **防げない** — 列がずれても偶然同型なら値は一致する |
| **PG の実列名が小文字化し「偶発的整合」で通っていた** | **防げない** — 出力が偶然一致する間は検出されない |
| **移行スクリプトが構造テストを通りながら全件失敗** | **防げない** — 構造の検査と実行結果の検査は別 |

**不変条件ガードの不在**: `update_list` は **S=3 / B>3 / 同一塁走者重複 / 4 アウトを検出しない**。
**B=4→四球の自動変換もない**(UI 層の `_resolve_continue_batting_result` にだけある)。
→ **同じ不変条件が正本の外にあり、UI テストだけが通っていた**型。

---

## 5. 既存ハーネスの実体(機構調査)

### 5-1. ◎ `backend/domain/` は現状どの型検査にも入らない

| 設定 | 値 | `backend/domain/**` は |
| --- | --- | --- |
| ルート `ruff` の `extend-exclude` | `["backend", ".claude"]` | **対象外** |
| ルート `ty` の `include` | `["scripts", "tests"]` | **対象外** |
| **backend `ty` の `include`** | **`["src", "tests"]`** | **対象外 — 型検査がゼロ** |
| backend `pytest` の `testpaths` | `["tests"]` | **収集されない** |
| backend `packages.find` の `where` | `["src"]` | **インストール対象外 → import が通らない** |
| backend `ruff check` / `format --check` | cwd=backend・exclude なし | 対象(唯一かかる) |

**docstring 規約の差**: ルートは `ignore = ["D403","D415"]`、backend は `["D415"]` のみ。
`select` はどちらも `["E","F","I","D"]`・`pydocstyle convention = "google"`。
ルート `scripts/` は **`D103`(public function の docstring)が効いたまま**
(`per-file-ignores` は `tests/**` のみ)→ **新規検査スクリプトの全関数に Google スタイル日本語 docstring が必須**。

### 5-2. ◎ CI の全体像(9 ジョブ)

| ジョブ | paths filter | 実測 |
| --- | --- | --- |
| `secrets` | なし | 10s |
| `docs-lint` | なし | 12s |
| `core-guard` | なし | 11s |
| **`harness`** | **なし** | **283s(4m43s)— 最長** |
| `nfr021-append-only` | **意図的になし** | 13s |
| `frontend-changes` / `frontend` | `frontend/**`・`contracts/**`・`ci.yml`・`mise.toml` | 6s / 28s |
| `backend-changes` / `backend` | `backend/**`・`contracts/**`・`ci.yml` | 7s / 37s |

**Action は全て SHA ピン留め**。`uv` = `0.8.13` 固定・`mise` = `2026.8.6`。

**◎ `nfr021-append-only` に paths filter を付けない理由が明記されている**(`ci.yml:87-92`):

> **フィルタ自体が誤ると検査が黙って通り fail-closed に反するため**

**◎ `pull_request` では `base:` 入力が無視される**(実測のログ警告)。
GitHub API の PR ファイル一覧が使われる。**フィルタが under-report すればジョブが黙って skip = fail-open**。

### 5-3. ◎ `check_authz_catalog.py` は CI から直接呼ばれていない

`ci.yml` が呼ぶスクリプトは **4 本だけ**(`check_docs_status` / `check_doc_coverage` /
`check_design_propagation` / `core_guard`)。

**TSK-270 の計画書は「`docs-lint` へ配線」と書いていたのに配線されなかった**
(`docs/features/pg-authz-verification/plan.md:140`)。
いま効いているのは `harness` の `pytest tests/` が `tests/test_check_authz_catalog.py` を収集する**間接経路**。

→ **本タスクでは `ci.yml` の step として明示配線する。**

### 5-4. 流用すべき既存の型

**検査スクリプトの共通型**(9 本でほぼ一致):

- 日本語 1 行 docstring → `from __future__ import annotations` → **stdlib のみ**(第三者依存ゼロ)
- モジュール定数で `DEFAULT_*: Path`、閉じた語彙は `frozenset` / `tuple`
- **専用例外 1 個**。`parse_args(argv=None)` + **必ず `--root`(default `Path.cwd()`)**
- `main(argv=None) -> int` + `if __name__ == "__main__": raise SystemExit(main())`
- **exit code 3 値**: `0` = 違反なし / `1` = 検査違反 / `2` = 入力不正・検査不能(fail-closed)
- 違反は **stderr** に `{識別子}: [{検査ID}] {理由}`。スキップ理由は **stdout**
- **選択実行引数は CI では渡さない**。`tests/test_ci_wiring.py` の
  `FORBIDDEN_SELECTORS = ("--defects", "--checks")` が `ci.yml` を機械検査する

**`check_authz_catalog.py`(3,958 行)から流用する型**:

| 型 | 実体 |
| --- | --- |
| **独立収集器** | `extract_source` — 「**内容による選択をせず、空行以外を全数採取する**」。**行番号を ID にしない** |
| **第 2 の収集器** | `collect_pytest_node_ids` — `pytest --collect-only -q` を起動し、資産の `status: implemented` が**実在テストを名指ししていること**を証明 |
| **canonical JSON** | `json.dumps(v, ensure_ascii=False, sort_keys=True, separators=(",", ":"))` + `sha256`。**書き込みは別形式**(`indent=2` + 末尾改行・`sort_keys` なし) |
| **git blob digest** | `sha1(b"blob {len}\0" + data, usedforsecurity=False)` |
| **厳密キー集合** | `_expect_keys` — **未知キーも欠落キーも拒否**。「フィールドを足して情報を忍ばせる」が不可能 |
| **bool 罠** | `not isinstance(x, int) or isinstance(x, bool)` |
| **exact-set** | **28 対**。必ず `不足=` と `未登録=` の**両方向を報告** |
| **「宣言させず導出する」** | `_expected_cell_decision` が規則の唯一の実装で、資産の `cells` はこれと突合 → **資産がコードと食い違えない** |
| **provenance 逐語検査** | `"".join(extracted.split()) not in "".join(source.split())` で空白無視の実在確認 |
| **パス脱出検査** | `(root / rel).resolve().relative_to(root.resolve())` |
| **否定部分文字列走査** | 全資産を canonical JSON で直列化し、禁止語が現れたら reject。**NFR-018 の重複実装禁止に応用できる** |
| **commit アンカー** | `_verify_manifest_commit` — `git rev-parse <commit>:<path>` と `source_blob_digest` を突合。**正本の変更と oracle の追随を同一コミットにできなくする**(代償は `H-85`) |
| **oracle-seal** | 「**黙って再封印しない**」という規則そのものが封印データになっている(`review_policy` / `reseal_policy` を dict 丸ごとリテラル一致で検査) |

**テストの型**:

| 型 | 実体 |
| --- | --- |
| **スクリプトの読み込み** | `importlib.util.spec_from_file_location`(`sys.path` を触らない) |
| **フィクスチャ** | `tests/fixtures/<name>/` の「小さな本物のリポジトリ」+ `shutil.copytree(FIXTURE_ROOT, tmp_path/"repository")` |
| **全葉変異** | 全 JSON 葉 × {変更, 削除} で `escaped == []`。**型保存変異**(未対応型は `AssertionError`) |
| **列挙器の空虚化防止** | **`assert attempts == sum(leaf_counts)*2`** または `assert leaf_paths` |
| **他ガードの意図的修復** | `_refresh_manifest` / `_refresh_decision_digest` — さもないと全変異が blob digest で捕まって**何も証明しない** |
| **検査器自身の効力検査** | `test_docs_lint_rejects_selective_check_option` — `ci.yml` に禁止引数を注入した変異が `AssertionError` になること |
| **0 件収集 / 0 件実行を fail** | `backend/tests/db/conftest.py` の `pytest_collection_modifyitems` / `pytest_runtest_logreport` / `pytest_sessionfinish` |

**「期待値を資産として凍結する」型**(`backend/tests/db/environment-expectations.json`):

- 各期待値が **`{expected, comparison, provenance}` の 3 点セット**。`comparison` は閉じた語彙
- **`provenance` = `{path, extracted_text}`**。空白無視で実在を検査
- **導出過程を書く欄**(`derivation`)— 「17*10000+11=170011」まで書く
- **`oracle_policy` を資産自身に埋める**(`observed_values_must_not_reseal_this_asset: true`)
- **否定形の許可フラグ**(`skip_allowed: false` 等)も葉として変異対象にする
- テストが **`EXPECTED_EXPECTATION_COUNT` / `EXPECTED_PROVENANCE_COUNT` と厳密一致**を要求

### 5-5. ◎ 時間予算の実測

| 実測 | 値 |
| --- | --- |
| `harness` ジョブ全体 | **283s** |
| `test_check_authz_catalog.py` の全葉変異 2 本 | **124.55s**(ローカル 214.67s の **58%**) |
| **全葉変異のスループット** | 9,065 葉 × 2 操作 ÷ 105.68s ≒ **172 回/秒** |
| 30 分での検査呼び出し可能数(純 JSON 検証型) | **約 31 万回** |
| **ADR の変異テスト**(mutant 適用 + テストスイート再実行) | backend 37s 基準で **30 分 ≒ 48 mutant** |

**◎ `claim-mutant-map.json` は 216 mutants を宣言している。**同規模なら **30 分に収まらない**。

### 5-6. ◎ 依存の現状 — 必要なツールがすべて未導入

`hypothesis` / `mutmut` / `cosmic-ray` / `jsonschema` / `fast-check` を
ルート・backend の `pyproject.toml` と両 `uv.lock`・`frontend/package.json`・`pnpm-lock.yaml` で検索 →
**ヒット 0 件**。

`uv sync --locked` / `pnpm install --frozen-lockfile` なので **lock を同一 PR でコミット必須**。
`uv add` / `pnpm add` は `ask` 権限。

### 5-7. frontend 側の配線

- `@contracts` エイリアスは **2 箇所必要**(`tsconfig.app.json` の `paths` + `vite.config.ts` の
  `resolve.alias`〔絶対パス〕+ `server.fs.allow`)。**`../backend/` は allow に入っていない**
- **`frontend/src/lib/generated/` は ESLint・Prettier・`vue-tsc` すべての対象に入る**。
  `noUnusedLocals` / `noUnusedParameters` が true・`eslint-plugin-vue` の severity は一律 `error`
  → **生成コードが規約違反なら red**
- **既存 spec はコロケート**(テスト専用ディレクトリは存在しない)
- **`contracts/` は Prettier / ESLint の対象外**(frontend ジョブは `working-directory: frontend`)

### 5-8. ジョブを 1 本足すだけで同時更新が必要な箇所

| 更新先 | 該当 |
| --- | --- |
| `docs/development/github-setup.md` | 2 章手続 2 / 手続 4 の「CI 5 ジョブ」の列挙・3 章 Ruleset JSON の `required_status_checks` 5 件・3 章注記「**必須ジョブの追加・削除・改名時は本書と Ruleset を同時更新する**」+ 変更履歴 |
| **設計書 10.1** | `consistency` の行は**既にある**(導入時期「実装期」)。**変異テスト / 乖離検出 / 経路一致 / `manifest.json` / 宣言モデル の語は 0 件**(Claude が確認)→ **行の新設が必要** |
| `.claude/core-areas.json` | `guard_paths`(足すなら `tests/test_core_guard.py:40-52` の `NEW_GUARD_PATHS` リテラル tuple も**同時更新** — `:340` が **exact-set** で検査) |
| `docs/README.md` | 索引 |

**`ci.yml` は既に `guard_paths` に入っている** → **本タスクの PR は自動的に core-guard の対象**になる。

### 5-9. ◎ その他の落とし穴

- **真の paths filter の穴はクロスの依存**: 「DSL 正本(`backend/domain/`)を変えたが
  `frontend/src/lib/generated/` を再生成していない」を `frontend` ジョブに置くと
  **`backend/domain/` だけの変更で発火しない**。→ **乖離検出は paths filter のないジョブに置くのが唯一の fail-closed な形**
- **`backend/.coverage` が git に追跡されている**(53 KB)。`.gitignore` に `.coverage` が無い。
  **backend のテストを回すたびに worktree が dirty になる**
- **`git` が PATH に無いと traceback**: `_verify_manifest_commit` は `subprocess.run(["git", ...])` を
  `try` なしで呼ぶ。**新スクリプトでは `except OSError` を入れる**(`core_guard.py:190-191` が正しい形)
- **CI 判定の env ガードがどこにもない**: `check_authz_catalog.py` に `os.environ` の使用がない。
  `--reseal*` が CI で走らないのは「CI がそのフラグを渡さないから」だけ
- **母集合の件数をテストにハードコードすると保守税になる**(`total=1062` 等が 4 箇所)
- **git_guard / secret_guard の heredoc**: 本文の引用符不整合で `shlex` 解析失敗 → **ブロック**。
  **ファイルへ書いてから `git commit -F <file>` にする**
- **`tsconfig.node.json` は孤立している**(`references` されていない)

---

## 6. 調査の限界(隠さず記録する)

- **旧リポジトリのコード本体を読んでいない**。§4 はすべて `docs/legacy/` の調査レポートを典拠とする二次情報
- **`docs/legacy/` 内の資料間に 12 件の不一致がある**(テーブル数 12 vs 13 / CASCADE の有無 /
  接続プールの種類 / DDL 系統数 2 vs 3 / DDL の一致 / 2 DDL 同期テストの有効性 / テストファイル数 37 vs 38 /
  テストの実行状況 / 球種の保存値〔日本語 vs コード〕/ 牽制の種類の値域〔漢数字 vs 算用数字〕/
  `status_labels` の所在 / ADR 内部の 3 種 vs 4 行)。**優先規定が未設定なので判定せず報告に留める**
- **変異テストの実測は「純 JSON 検証型」のスループットのみ**。
  **mutant 適用 + テストスイート再実行の実測は未実施**(本タスクのステップで行う)
- **`vocab.ts`(旧の新 UI 側)の語彙集合を確認できていない**

---

# 6. 改訂調査(2026-09-09)— ADR-003 v0.2 / 要件書 v2.7 差分の突合

**§0〜§5 は 2026-09-01 時点の調査で、ADR-003 v0.1・要件書 v2.5 以前を典拠としている。**
TSK-278 による `ADR-003 v0.2`(2026-09-03 approved)・要件書 `v2.6`(同日 approved)、および
`v2.7`(2026-09-07 approved・TSK-322)の確定により、**計画書の前提が実際に壊れている箇所がある**。
本節はその差分の突合結果であり、**§0〜§5 と本節が食い違う場合は本節が新しい**。

調査サブエージェント **3 本**を並列で実施(要件突合 / 決定追跡 / 機構配線・所有境界)。
legacy-analyst は外した — 本タスクは旧システムの挙動を 1 件も追加しない基盤タスクであり、
旧の事実は §4 で確認済みのものをそのまま使う。
**重い指摘は Claude が原典で直接確認した(◎印)。エージェント間で判断が分かれた 3 件はすべて原典で裁定した。**

## 6-0. 版の実測(前提の訂正)

| 正本 | 計画書・§0〜§5 の前提 | 実測(2026-09-09) |
| --- | --- | --- |
| `docs/adr/ADR-003-domain-calc-method.md` | v0.1 | **v0.2 approved 2026-09-03**(`:9`。敵対レビュー 27 周・指摘 88 件全件採用・P0 全周ゼロ) |
| 要件書 | v2.5 以前 | **v2.7 approved 2026-09-07**(`:42`)。v2.6 が本タスクに効く版(`:40`)/ v2.7 の射程は `NFR-020` の条文化のみで本タスクの計算面に波及なし |
| worktree のブランチ | — | 調査開始時点で **develop から 44 コミット遅れ**。`origin/develop` へ rebase 済み(旧 `c363b9d` → `75b6cd3`) |

**計画書は `v0.2` / `v2.6` を 1 文字も参照していない**(実測 grep = 0 件。ヒット 1 件は旧要件書 `requirements-tsukuba-pss-v0.2.md` の別物)。

## 6-1. ◎ P0 — ADR v0.2 が要求する機構が計画書に無い(6 件)

### P0-A. (b)③ の 4 つ目の不変条件「表外既定の表示対応の突合」が計画に無い

`ADR-003 D-11 ③ 構成の完全性表`(D-7 の (b)③ 機構表・v0.2 新設行)逐語:

> **表外既定の表示対応の突合(v0.2)** | **表外既定(要件書 A-1)の表示項目 ↔ 製品表示呼出箇所 ↔ 生成 formatter の対応をマニフェストへ宣言し、静的解析で実測した呼出箇所の集合と突合する**(登録漏れ・formatter を経由しない表示経路 = 差分として fail)。**対象集合の正は宣言モデル schema の閉包**(要件書 A-1)。宣言フィールドの細部は検査基盤タスク〔TSK-235〕で確定する | **fail**

`ADR-003 D-11 ③ 確定範囲と送り出す範囲`(v0.2 追記・PO 裁定 2026-09-03)も「**宣言フィールド・静的解析器の具体・等価変異の判定手順はベクタ整備・検査基盤タスク(TSK-235)で実機とともに確定する**」と本タスクを名指しする。
`ADR-003 D-7 実行契機表 変異テスト行` により (b)③ は **PR 必須・マージ不可**の機構。

**計画書ステップ 4(マニフェスト schema 凍結)・18・19((b)③ 閉域)・DoD のいずれにも該当記述が無い。**
現行の 18・19 は入口・モジュール・動的機構だけを見ており、表示対応の突合を持たない。

→ **効く箇所**: ステップ 1(schema 閉包の設計)/ 4(宣言フィールド)/ 18・19(静的解析器)/ 13・15(等価変異の判定手順)/ DoD。

### P0-B. マニフェスト「11 フィールドの exact-set」を凍結できない

計画書ステップ 4 は「D-11 `:232` の **10 フィールド + `propertyCatalog` = 11** の exact-set」を課す。実測 3 点:

1. **`:232` は移動済み。現行の「宣言の正」列挙は `ADR-003 D-11 ③ 動的機構の禁止行`**。フィールドは
   `calculation` / `source` / `generated[]` / `entrypoints[]` / `directTargets[]` / `comparison` /
   `normalization[]` / `vectors[]` / `properties[]` / `mutation` = **10 件で、すべて「対象計算ごとに」持つ**。
2. **`propertyCatalog` は `:243` の列挙に無い**(ADR 内の出現は `:10`・`:23`〔変更履歴〕と `:216` のみ)。
   `ADR-003 D-11 ② 3 層表` 逐語「**`propertyCatalog` はマニフェストのトップレベル・キー**であり、対象計算ごとの
   `properties[]` とは階層が異なる — v0.2 改訂で記載階層を明記」。
   → **10 件は per-calculation・1 件は top-level。フラットな 11 件の exact-set は型として誤り。**
3. **`ADR-003 D-11 ③ 構成の完全性表` が新フィールドの命名を TSK-235 に委任している**(P0-A)。
   → **「11 件の exact-set」を凍結する行為そのものが v0.2 と両立しない。**

→ **是正**: exact-set を「対象計算エントリ(10)」と「トップレベル(`propertyCatalog` + 対象計算コンテナ + 表示対応宣言)」の
**2 本に分割**し、件数は本タスクで確定する。
**「空を許さない 5 フィールド」は v0.2 でも維持**(`vectors[]` / `properties[]` / `mutation` = `ADR:243`、`entrypoints[]` / `directTargets[]` = `ADR:247`)。
**`components[]` の最小要素数は ADR に規定が無い(不明)** — 本タスクの確定事項。

### P0-C. composite target が schema・収集器・証跡・fixture・生成器に無い

`ADR-003 D-11 ③ 動的機構の禁止行` 逐語(要旨):

> **composite target(v0.2 改訂)** — 1 つの `directTargets[]` 要素(`kind: composite`。既存の単一 target は `kind: single`)が
> **順序固定の構成生成物リスト `components[]`**(各要素が `generated[]` への参照と内容 hash を持つ。段数は対象による —
> **(β)①〜⑤ = 3 段〔sql → typed-receiver → formatter〕/ (β)⑦ = 2 段〔sql → typed-receiver(写像①適用)〕**)と、
> **宣言された全段を通す `invocation`** を持ち、最終段の出力までを 1 つの直接呼び出し対象とする。
> **証跡直積の軸は従来どおり `directTargetId` 単位**(要素を 3 つに分けない)

`ADR-003 D-11 ③ 経路一致検査の理由` は収集器の入力契約の拡張を定め、「**この拡張は「収集器の入力契約」の共通契約の一部であり、
composite を持つ対象計算では必須 — 局所規定ではない**」と明記。各段の入力と出力を渡し、
**前段出力 = 後段入力の値の連鎖を収集器が独立に検証**する。

**5 判定と 6 次元要求集合 `(calculation, vector, case, runner, entrypointId, directTargetId)` は不変**(`ADR:247`)。
つまり**ステップ 8 の骨格はそのまま生きる**が、次が足りない:

| ステップ | 追加要求 |
| --- | --- |
| 4 | `kind` / `components[]`(段数可変)/ `invocation` の schema |
| 7 | 段別の入出力を採取する収集器の入力契約 |
| 8 | 段別入出力を持つ証跡 schema + **値の連鎖の独立検証** + **表示値は文字列の完全一致(正規化を置かない)** |
| 9 | hash 検査を **段数分に多重化**(現行は単一 hash 前提)+ **値の連鎖不整合**の fixture |
| 16 | target matrix に **生成 formatter**((β)①〜⑤ の 3 生成物目・および (α) の Python/TS 双方)と **(β)⑦** を追加 |

### P0-D. ステップ 9 に「恣意的迂回を検出する敵対 fixture」を置いてはならない

`ADR-003 D-11 ③ 動的機構の禁止行` の PO 裁定(2026-09-03)逐語:

> アダプタが**恣意的に整合する値と現行 hash を提出して中段・終段を迂回する場合の完全排除は保証範囲外**
> (残余リスクとして明示 — 任意コードの実行証明は意味判定に帰着するため保証しない)(hash は「宣言どおりの生成物か」・
> 連鎖は「提出された値が宣言された全段の入出力として整合しているか」をそれぞれ検証する — **迂回・自前整形の実装は
> 連鎖の欠落・不一致と構成検査〔(b)③〕の差分で検出される**(分担は確定のとおり — **変異テストは formatter 等の
> 生成物自体の実効性を担い、scale=0 では迂回が等価変異になり得るため迂回の検出手段には数えない**))

→ **是正**: ステップ 9 の敵対 fixture は「①偽 SQL ②**段別** hash 不一致 ③期待値直返し ④**値の連鎖不整合**」の 4 種とし、
**⑤恣意的迂回は「保証範囲外として明示的に不検出であることを記録する」形にする**。
**「迂回を検出する」合格条件を書くと決定と矛盾する。**

### P0-E. プロパティ層の等価性の終端が延びた

`ADR-003 D-11 ② 3 層表`(D-11 3 層表・プロパティ層)逐語(要旨):

> **(β)①〜⑤** = 複合 target の**最終表示文字列**と、同じ宣言モデルから**表示まで生成した**テスト専用 Python 参照実装の結果
> (**構造化数値段の比較も従来どおり保持する — 置換ではなく終端の延長**。v0.2 改訂)/
> **(β)⑦** = **受け口が写像①を適用した区分の表示名まで** / **(β)⑥⑧** = 生成 Python と同参照実装
> (**参照実装は生成物であり手書きではない。製品経路には載せない**)

`ADR-003 D-7 実行契機表 網羅ベクタ行`(D-7 実行契機表)も同旨。加えて `ADR-003 D-1 表示規則 3 類`・`:326` により **(α) も表示値を返す**。

計画書ステップ 11 は「(α) Python ⇄ TypeScript /(β) SQL ⇄ テスト専用生成 Python 参照実装」で
**(β) を一括に扱い、formatter 段を持たない**。
→ **是正**: (β) を **3 区分**((β)①〜⑤ / (β)⑦ / (β)⑥⑧)に分け、区分別の比較面を持つ runner 契約にする。
**構造化段と表示段の両方を持つ**(片方に置き換えると v0.2 に違反)。

### P0-F. 表示系変異演算子と同値分割 8 軸

`ADR-003 D-11 ② 決定表` 逐語(要旨): 変異演算子に **表示系(v0.2)** を追加 —
文字列リテラルの置換 / テンプレート placeholder の削除と入替 / 写像テーブルの値と順序の変異 /
**表示 primitive のパラメータ変異**(scale・丸め・先頭 0・記号・剰余 0 の形)/
**formatter 呼出しの削除と言語既定の文字列化への置換**。
かつ「**表示生成物(formatter・写像・テンプレート)を変異対象に持つ対象計算は表示系演算子を 1 種以上適用する**」。
kill 判定の限定: **scale=0 の整数表示では出力が同一になり得るため等価変異の規則(人手判定・記録必須)で扱う**。

`ADR-003 D-11 ② 入力範囲表 座標変換行`(および `:203`)逐語 — **軸は 8 件**:

> **丸め境界の直前・一致・直後 / scale の上下限 / 正・負・0 / 分母 0 /
> 集計対象 0 件(全欠損 — 分母 0 と別 case)/ 剰余 0・1・分母−1(投球回)/ nullable / オーバーフロー**。
> **軸の組合せ要件(v0.2)**: **丸め境界(直前・一致・直後)× 符号(正・負)は直積で必須**とし、
> **負値の丸め境界一致(負のちょうど中間値)を必ず含める**(片側の符号だけで境界軸を消費した false green を許さない)

**◎ 訂正**: 調査指示に書いた「7 軸」は誤り。`docs/features/adr003-display-primitives/design.md:59-60` が
7 軸で書かれているが、これは feature 文書であり **「集計対象 0 件」を欠く**(確定ゲート 11 周目の反映で追加 —
`docs/worklog/2026-09-01-adr003-display-primitives.md:246`)。**規範の正は ADR。**
丸め方向の正は要件書 `:1099`「四捨五入(round half up)。**負値のちょうど中間値は絶対値で丸めて符号を戻す**(away from zero)」。

→ **効く箇所**: ステップ 3(表示 primitive のパラメータ値域と整数範囲上限の schema 化 — `ADR:101` が
「1 つでも欠けた宣言は fail」を要求)/ 10(**8 軸 × 丸め境界×符号の直積を「要求 case 集合」として持ち、
証跡集合との差を fail**)/ 13(表示系演算子を第 4 系統として追加)。

## 6-2. ◎ P0 — 計画書の記述が現行決定と矛盾している(4 件)

### P0-G. トリガー 2 が現行文のままでは空振りする

計画書 §4-(7) トリガー 2「**表示規則(数値書式 `.333`・条件付き写像・1 対多写像)が D-1 の 2 種に収まらない**」。

| 部分 | 判定 | 典拠 |
| --- | --- | --- |
| 「D-1 の **2 種**」 | **存在しない**。v0.2 の D-1 は **3 類** | `ADR-003 D-1 表示規則 3 類`「**表示規則として許すのは次の 3 類だけ**」 |
| 「数値書式 `.333`」 | **解消済み**。既に発火し TSK-278 で正規経路により解消 | `docs/worklog/2026-09-01-domain-calc-dsl.md:15-64`・`ADR:9` |
| 「条件付き写像・1 対多写像」 | **残存**。依然として射程外 | `ADR-003 D-1 表示規則 3 類`「**条件分岐・部分文字列操作・③の宣言の外の数値整形ロジックは引き続き書けない**」 |

→ **是正**: トリガー 2 を**再スコープ**する。現行文のままステップ 1 で評価すると、
既に解消した論点で停止するか、逆に**残存する制約(条件付き写像・1 対多写像)を見落とす**。

### P0-H. §4-(8)② の前提が偽 — 前提ゲートは 2 件から 1 件へ

計画書 §4-(8)② は「数値表示規則(打率 `.333`・投球回「5 回 2/3」)は**要件に規則本体が無い**。
**DSL 資産で決めると利用者可視仕様を実装側で新設することになる**」とする。**v2.6 でこれは成立しない**:

- 付録A の各表に「表示書式」列が入った。投球回 = 混合分数(分母3・整数部「回」・**剰余0は分数省略**・半角 `n/3`)= 要件書 `:1104` /
  打率 = 小数3桁・四捨五入・**先頭0なし** + `({打数}打数)` = `:1133` / 球種割合 = `:1123` / WHIP・FIP = `:1112`・`:1113`
- A-4 柱書 `:1156`「**本表の「表示書式」列が binding の正**」
- A-1 共通規則 `:1099`(丸め・文字種・符号・先頭0・0除算表記)
- `ADR-003 D-1 表示規則 3 類`「**書式の実引数の正は要件書 付録A の「表示書式」列(binding)であり、正本の宣言は binding の値を参照する
  (表示の都合ではなく要件として与えられる)**」
- 要件書変更履歴 `:39` の射程宣言「**実装時に確定する範囲 = なし(書式は本改訂で閉じる)**」

→ **是正**: §4-(8) は **1 件(成績計上フラグの外延のみ)** へ縮む。
実装側に残るのは **binding の値の参照と、宣言時パラメータの充足検査**(`ADR:101`)だけ。

**なお §4-(8)① は有効**: 付録E-1 の成績計上フラグは現況でも
「打席・打数・安打・塁打・犠打・犠飛・四球・死球・奪三振・被本塁打・打点**等**の計上有無」(要件書 **`:1257`**)で
**「等」が開いたまま**。柱書 `:1244`「全表の完成は設計フェーズの必須成果物」も維持。

### P0-I. ステップ 22 の `[手動]` が正本より軽い

計画書ステップ 22 の `[手動]` は「**7.6-3 のゲート分類**(判定者: 山田正輝)」。
設計書 6.3-⑤ 逐語(`docs/development/dev-harness-design-2026-08-07.md:368`):

> ⑤ **paths の追加・削除・縮小は敵対レビュー + 人間承認の対象**
> (core-areas.json は guard_paths でもあるため PR 必須チェックが機械強制される)

→ **是正**: ステップ 22 の手動条件を「**敵対レビュー + 人間承認(PR 作成者以外の逐行確認)**」へ引き上げる。

### P0-J. §2「やらないこと」と ADR v0.2 が衝突 — PO 裁定要

計画書 §2「やらないこと」: 「**実ベクタの中身・対象別 property・実 mutant を作らない**」。
一方 `ADR-003 D-11 ② 入力範囲表 状況判定行`・`:206` はいずれも「**具体ケース値**と証跡形式は検査基盤タスク〔TSK-235〕で確定 — 本行は軸の正」と
本タスクを名指しする。

→ **「具体ケース値」を「軸の具体化規則」と読むか「実ベクタの case 値」と読むかで射程が変わる。**
**PO 裁定が必要**(§6-8 に申し送り)。現状では計画書 §2 が ADR v0.2 と矛盾している。

## 6-3. ◎ P0 — 2 周目指摘のうち残存かつ悪化した 2 件

典拠 = `docs/worklog/2026-09-01-domain-calc-dsl.md:86-100`。
**全文 `rev235b-full.txt` はスクラッチパッドに置かれていたため消失している**(同 `:98`)。
P1 の 4〜13・P2 の 14・15 は 1 行要約しか残っていない。§4-(7) 手順 5 が
「敵対レビューを最初から再実施」を求めるため実害は小さいが、**要旨から判定できない粒度は「不明」とする**。

### 2 周目 P0-1(案 A の三すくみ)— 残存・悪化

- **是正案 `bootstrapDebt`(承認時点の既知債務を全件列挙して封印 + 追加・再導入・降格をコミット履歴単位で禁止 +
  債務が空になったら自動かつ不可逆に昇格)は、どこにも採り込まれていない**
  (実測: `bootstrapDebt` の出現は worklog `:92` の 1 箇所のみ。ADR・計画書・要件書・`.claude/` にゼロ)。
- **NFR-018(c) 例外表の当該行は「有効」化された**(要件書 `:911`・v2.4 行 `:37`)が、
  例外は **(a) 単一性の適用除外**であって **(b)③ の入口宣言義務を外さない**(`:907`)。
- **座標変換の呼び出し元は実測で `StrikeZone.vue` のみ・4 箇所**
  (`frontend/src/components/zone/StrikeZone.vue:11,:12` で import、`:51`・`:60`・`:70`・`:105` で呼び出し。
  テスト・stories・scripts から 0 件)。到達経路は
  `index.html:19` → `src/main.ts` → `App.vue` → `StrikeZoneCompareHost.vue` → `StrikeZone.vue` の**静的 import 4 ホップ**。
  **Web Worker・Service Worker は 0 件**、`vite.config.ts` に `build` セクションが無く **input は `index.html` 1 本のみ**。
- **悪化要因**: P0-A の (b)③ 新設行が**もう 1 系統の未整備 → 常時 fail 源**を増やした。三すくみの穴が 1 つ増えている。

### 2 周目 P0-3(網羅性の証明が無い)— 残存・悪化

- ADR の要求は不変。`ADR-003 D-11 ② 3 層表`「上記の入力範囲を**全件覆う**」。
- 計画書ステップ 10 は依然「**全 case 消費**」のみで、**要求 case 集合と証跡集合の差を fail する機構が無い**。
- **不変条件 runner の本体も依然として不在**: ステップ 11 = 等価性 / 12 = カタログ**独立性** / 7 = 証跡採取であり、
  **述語を生成 case へ適用して偽なら fail する runner のステップが 22 件中に無い**。
  `ADR-003 D-11 ③ 経路一致検査の理由` は「対象計算ごとに ①**不変条件のテスト**と ②実行体どうしの等価性のテストの双方が 1 件以上完走し、
  生成ケース数が 1 以上であること」を課す。
- **悪化要因**: v0.2 が入力範囲へ **8 軸 + 丸め境界×符号の直積**を追加し、不変条件カタログへも
  「集計対象 0 件で構造化値 null・完成表示値は値の位置に「−」」を追加(`ADR:216`)。**覆うべき空間が拡大した。**

## 6-4. ◎ P0 — 並行タスクとの資産の三重所有(TSK-343)

`tests/test_ci_wiring.py:1272` 実物:

```python
assert {"sqlalchemy", "alembic"}.isdisjoint(locked_versions)
```

`backend/uv.lock` に `sqlalchemy` / `alembic` が現れた瞬間に red になる。
**TSK-343 はこの 3 ファイルを明示的に自分の所有と宣言している**
(`docs/features/orm-schema-migration/plan.md:106`「`tests/test_ci_wiring.py`(現行 `:1272` の ORM 不在検査)| **本タスク**」・
`:110`「**`backend/uv.lock`** + `backend/pyproject.toml` | **本タスク**」)。
さらに `:149`・`:193` に**裁定 `Q-4`「マージ順序 = TSK-348 → TSK-317 → 本タスク」**がある。

**TSK-343 の「TSK-317 との衝突 6 箇所」の表に TSK-235 は 1 行も無い。相互に無認識。**
逆に TSK-235 の計画書・§0〜§5 も TSK-343 / TSK-348 / TSK-317 を 1 度も参照していない
(実測: `research.md:616` の `pg-authz-verification`〔前身タスク〕1 件のみ)。

**ただしこの衝突は既に下りている裁定で解ける。** Notion タスクの 2026-09-08 コメント(人間の裁定)が
「**実装は TSK-342〔→ 343〕のマージ後に開始**」と決めている。
→ **順序は `348 → 317 → 343 → 235` で確定済み。計画書がそれを記録していないだけ。**

**計画書への是正 3 点**:

1. ステップ 21 は**現行の `:1272` ではなく、TSK-343 が置き換えた後の形**
   (ORM スタックの厳密固定検査・判定を純関数へ抽出済み — `orm-schema-migration/plan.md:59`)の上に載せる前提で書く
2. `backend/uv.lock` は行マージ不能。**TSK-235 側は必ず再生成**し、`uv sync --locked` が通ることを合格条件に持つ
3. `.claude/core-areas.json` は TSK-343 が `tenant-isolation.paths`(同 `plan.md:88`)、
   TSK-235 が `game-state` + `data-migration` で**別配列だが同一 JSON**。逐次前提

### 衝突マトリクス(実測 — `git diff origin/develop...<branch>`)

3 ブランチはいずれも merge-base = `1424d67`。**§3 が挙げた 16 資産に対する実測ヒットは `docs/README.md` の 1 件のみ**で、
残りは 3 ブランチとも現時点で未変更。以下は**宣言(各 plan.md)ベースの衝突**。

| 資産 | TSK-235 | TSK-317 | TSK-348 | TSK-343 | 判定 |
| --- | --- | --- | --- | --- | --- |
| `tests/test_ci_wiring.py` | 宣言:変更(ステップ 21) | 触れない(改訂 3 のみ) | 反映なし | **宣言:置き換え** | **P0** 二重所有・相互無認識 |
| `backend/pyproject.toml` | 宣言:変更(ステップ 21) | — | 反映なし | **宣言:変更**(SQLAlchemy 2.x・Alembic) | **P0** 同一 `dependencies` 配列 |
| `backend/uv.lock` | 宣言:変更 | — | 反映なし | **宣言:変更** | **P0** 行マージ不能 |
| 設計書 **10.1** | 宣言:変更・**版は上げない**(実装追随) | 反映なし(`plan.md:171`「追随は TSK-343」) | **10.1 差分 0 を DoD** | **宣言:変更 + 版繰り上げ v1.14** | **P1** 行域は別(表 `:668-679` vs 箇条 `:695`)だが**変更履歴表 `:9` を同位置で争う**・版の扱いが背反 |
| `docs/README.md` | 宣言:変更(`:14`・`:20`) | 宣言:変更・実測なし | **実測変更(`:13`)** | 宣言:変更(`:14`) | **P1** `:13` と `:14` は連続行 = 同一 hunk |
| `.claude/core-areas.json` | 宣言:変更(`game-state` + `data-migration`) | 宣言が内部矛盾(改訂 3 へ送り) | **差分 0 を DoD** | **宣言:変更**(`tenant-isolation`) | **P1** 別配列・同一 JSON |
| `tests/test_core_guard.py` | 宣言:変更(期待値方式の改造) | ファイル表のみ | 反映なし | **宣言:変更**(ステップ 17) | **P1** TSK-343 が `EXPECTED_AREA_PATHS` に追記 → 235 の基線が崩れる |
| `.github/workflows/ci.yml` | 宣言:変更(`consistency` + 変異ジョブ) | — | 差分 0 を DoD | 宣言の穴(表に無いが `:325` で `backend` job へ相乗り) | **P1** 別 hunk だが両方 `guard_paths` 該当 |
| ルート `tests/` の実行時間 | **前提として使用**(§4-(11)「既に 283s」) | **実測 +460 行の root テスト 2 本** | — | — | **P1** 予算基線が無音で動く |
| `github-setup.md` / `frontend/*` 5 点 / `.gitignore` / `backend/.coverage` | 宣言:変更 | — | — | 言及ゼロ | **P2** 単独所有 |

**実測の補正 3 件**(申し送り・SessionStart の表示との差):

| 表示 | 実測 |
| --- | --- |
| TSK-317 実装 8/21 | コミット上は **9/21** 完了(`96c8ef9 …(ステップ 9)`) |
| TSK-348 確定ゲート 6 周目 | コミットは 6 周目だが plan frontmatter は `確定ゲート周回: 7` |
| TSK-343 が `core-areas.json` を「触る可能性」 | **確定的に触る**(ステップ 17 で `tenant-isolation.paths` へ完全列挙) |

## 6-5. ◎ P1 — ステップ 22 の設計に 2 つの穴

### 穴 1. core-guard は領域を見ない

`scripts/core_guard.py:124-131` が**全領域の `paths` を 1 本の tuple へ平坦化**してから
`fnmatch.fnmatchcase` で判定する(`:224-227`。`guard_paths` は完全一致・area paths は `fnmatch`)。

→ **core-guard は「どの領域に登録されたか」を一切見ない。**
計画書ステップ 22 の `[機械]`「新規各パスの変更で core-guard が発火」は、
**`game-state` と `data-migration` の片方だけに登録しても等しく成立する**。
「両方へ登録する」という要求をこの機械条件は検証できない。
→ 合格条件を「**JSON の両 area 配列の包含**」または「/pr の領域帰属出力」で書き直す。

実測(照合結果): `backend/domain/manifest.json` / `frontend/src/lib/generated/x.ts` /
`backend/src/pitchlog/authz/catalog.py` はいずれも**現状どの core パターンにも一致しない**。

### 穴 2. 「他領域は byte-for-byte 不変」の基線機構が存在しない

期待値は `tests/test_core_guard.py:44-218` の Python リテラル `EXPECTED_AREA_PATHS`(検査は `:613-620`)と
`:225-258` の `EXISTING_REAL_GUARD_PATHS` + `NEW_GUARD_PATHS`(検査は `:643-646`)。

- **「変更前の姿」を保持する機構が無い**(外部基線も封印 digest も無い)。したがって
  「他領域が不変であること」は**同じコミットで編集可能なリテラルとの一致**としか表現できず、**delta 検査になっていない**。
- **`EXPECTED_AREA_PATHS` は 5 領域が単一の dict リテラル**。「他領域だけを byte-for-byte 固定する」には
  ①**封印**(git blob digest — `research.md:641` が `sha1(b"blob {len}\0" + data)` の既存型を確認済み)か
  ②**`guard_paths` が既に採っている二層方式**(据え置き分 + 追加分)の area paths への導入が要る。**②が最小変更**。

**計画書の記述の誤り 2 件**: 行参照 `:310` / `:340` は失効(実際は `:44`/`:613` と `:225`/`:643`)。
「**他 4 領域**」は誤りで、areas は 5 件・変更対象が 2 件なので **他 3 領域**。

### 2 周目の指摘「自己作成の期待値で完全性を証明」は現コードに完全に該当

develop の `03c41ca`(「core-areas.json へデータモデル正本と検査資産を登録 (ステップ 11/13)」)が**同一コミット**で:

- `.claude/core-areas.json` に `docs/design/data-model.md` を **5 領域すべてへ**追加(+5 行)
- `tests/test_core_guard.py` の `EXPECTED_AREA_PATHS` に**同じ 5 行**を追加
- `NEW_GUARD_PATHS` に**同じ 4 件**を追加(`guard_paths` 26 → **30**)

`test_actual_core_area_paths_are_exact_expected_set` は `03c41ca` より前から存在していた。
つまり既存の完全一致テストは、**登録漏れを検出せず、登録者が期待値を同時に書き換えれば必ず green になる**。
同時追加された `has_expected_data_model_registrations`(`:558-585`)も
テスト内の自己申告リテラル(`:31-37`・`:38-43`)への**包含**判定のみで、独立導出は無い。

**基線の移動 3 点**(ステップ 22 の期待 delta に効く):
`guard_paths` = **30 件が新基線**(計画書の「30 件」は現況と一致)/ `data-migration` の paths が **5 → 6 件**・
配列先頭が `docs/design/data-model.md` になった / `03c41ca` が 4 領域で `*CORE_DOCUMENT_PATHS` の展開を
**手展開の 3 要素へ書き換えた**ため、共有定数による「1 か所直せば全領域に効く」性質が失われた。

**リポジトリ内に既にある「宣言させず導出する」型**: `check_authz_catalog.py` の `_expected_cell_decision`
(規則の唯一の実装で資産と突合 — `research.md:645`)/ commit アンカー型(`research.md:649`)。
**これらを core-guard 側へ持ち込むのがステップ 22 の実体**であり、計画書の粒度(「期待 delta 方式に改める」)では射程が過小。

## 6-6. P1 — CI と機構の実測

| 項目 | 計画書の記述 | 実測 |
| --- | --- | --- |
| 現存ジョブ | 「9 ジョブ」 | **一致**(`secrets` / `docs-lint` / `core-guard` / `harness` / `nfr021-append-only` / `frontend-changes` / `frontend` / `backend-changes` / `backend`。workflow は `ci.yml` 1 本のみ) |
| `consistency` | 「現状これらの語は 0 件」 | `ci.yml` には**存在しない**が、**設計書 `10.1:672` にジョブ表の行として既に宣言済み**(導入時期 = 実装期)。`e2e`(`:673`)も同様。→ 10.1 の追随は「行の新設」ではなく「**導入時期の現行化 + 6 機構行の追加**」 |
| `timeout-minutes` | 「変異処理内の 10 分 / 30 分タイムアウト(GitHub job の `timeout-minutes` **とは別**)」 | ◎ **9 ジョブすべてで未設定**(grep 0 件)。**区別する相手が存在しない**。GitHub 既定 360 分が効いているだけ。新設ジョブに設定するかは計画書に記述なし(**不明**) |
| `harness` 283s | §4-(11) の前提 | 典拠は**自ブランチの `research.md:596`・`:677` のみで run id・実測日なし**。develop 側 docs に記録 0 件。**独立検証不能**。かつ TSK-317 が root テストを +460 行入れるので**近い将来失効する** |
| 全葉変異の母集団 | ステップ 21「YAML 契約木の全葉変異で escape 0」 | 機構は存在するが(`_leaf_paths`・`_mutate_contract_value`・`_set_node_at`・`_delete_node_at`)、`_backend_ci_contract_tree`(`:286-316`)が**`backend` ジョブの `services`〜`defaults` 手前だけ**を採取。**新設する `consistency`・変異ジョブは覆えない** |
| 列挙器の空虚化防止 | ステップ 5 は強い側(`assert attempts == 期待件数`)を要求 | `test_ci_wiring.py` は**弱い側(`assert leaf_paths`)しか使っていない**。既存資産の格上げが要る点は計画書に未記載 |
| `backend/pyproject.toml` | §4-(10)「`packages.find where=["src"]` は src-layout の設定」 | **正しい**(`:24-25`)。`[tool.ty.src] include = ["src", "tests"]`(`:42-43`)= **`backend/domain/` は現状 `ty check` の対象外**。`testpaths = ["tests"]`(`:48-52`)。`dependencies` は fastapi・psycopg の **2 件のみ** |
| `backend/.coverage` | ステップ 21「index 除去」 | ◎ **index に入っている**(`git ls-files` が出力・53,248 B のバイナリ)。`.gitignore`(27 行)に `.coverage` にマッチするパターンが**無い**(`coverage/` はディレクトリ) |
| `github-setup.md` | ステップ 21 で必須ジョブ列挙 + Ruleset を更新 | 2 章 `:39` に 3 分類・3 章 `:80-91` の Ruleset JSON に **9 context を列挙**。`:101`「追加・削除・改名時は本書と Ruleset を同時更新」。`consistency` + 変異ジョブで **9 → 11 context** になる |
| frontend の既存機構 | ステップ 18 が前提 | **3 点すべて未整備** — `src/lib/generated/` は**存在しない**(リポジトリ全体で `generated` ディレクトリ 0 件)/ `eslint.config.js`(40 行)に `no-restricted-imports` 等が **0 件**・`eslint-plugin-import` も依存に無い / `index.html`(21 行)に **CSP が 0 件** |

**ステップ 18 の負例が実リポジトリから採れない(P2)**: input 1 本・Worker 0・SW 0 のため、
「解析不能例」は**合成 fixture が必須**。計画書は 3 通りを求めるが調達元を書いていない。

**`.prettierignore` との将来衝突(P2)**: `frontend/.prettierignore:8-12` が `courseInputView.ts` ほか 4 本を
「旧コミット `dd03160` との**逐語比較を維持する** TypeScript」として prettier 対象外にしている。
本タスクは製品生成物を作らないので当面は非該当だが、**段階 2 で衝突する**。

## 6-7. P1 — 行番号参照の陳腐化(全 18 件)

TSK-278 のステップ 8(`docs/worklog/2026-09-01-adr003-display-primitives.md:180`)は行番号参照を洗い出したが、
「活動中文書の 3 件」だけを置換し、**残り 2 件を「クローズ済み feature」として据え置いた**。
その 2 件は実は `status: active` の本計画書である。実測対応:

| 計画書の参照 | 現行の正しい行 | | 計画書の参照 | 現行の正しい行 |
| --- | --- | --- | --- | --- |
| ADR `:96`(D-1 射程) | **`:99`** | | REQ `:236`(FR-006 undo) | **`:241`** |
| ADR `:100`(frontend 生成先) | **`:103`** | | REQ `:883-885`(対象集合) | **`:888-890`** |
| ADR `:206`(プロパティ層) | **`:216`** | | REQ `:893`(集合を固定する) | **`:898`** |
| ADR `:232`(マニフェスト列挙) | **`:243`** | | REQ `:900`(未整備は不合格) | **`:905`** |
| ADR `:238`(見直しトリガー) | **`:406-412`** | | REQ `:902`((c) 例外表) | **`:907-911`** |
| ADR `:256`(FR-040 非採用) | **`:201`**(または `:269`) | | REQ `:910`(判定禁止) | **`:915`** |
| ADR `:374`(段階 1) | **`:388`** | | REQ `:915`(NFR-019 要件文) | **`:920`** |
| ADR `:375`(段階 2) | **`:389`** | | REQ `:917`(履歴スタック全体) | **`:922`** |
| ADR `:380`(既存座標変換) | **`:393`** | | REQ `:1240`(E-1「等」) | **`:1257`**(柱書 `:1244`) |

**とくに危険な 1 件**: ステップ 3 の `[機械]` 合格条件「**典拠(FR-006 `:236`)の実在検査**」は、
引用文の実在行が `:241` であるため(`:236` はユーザーストーリー行)**別行を検査する**。
ステップ 4 の「provenance の逐語実在」も同様。

→ **是正**: provenance を**行番号ではなく節番号・条項 ID**(`NFR-018 (b)②`・`FR-006 補足`・`付録A-2 WHIP 行`)で持つ。
**v0.2 の兄弟タスクが先例を確立している** — `docs/features/adr003-display-primitives/plan.md` は
「6 節に「機械条件の判定方法」表を新設(**節限定差分・同一コミット・行番号参照 0 件・母集合計測・文言存在**の 5 型)」+ 4 型追加で対応
(worklog `2026-09-01-adr003-display-primitives.md:108`・`:115`)。**この先例を本計画書へ移植するのが最短**
(2 周目 P1「`[機械]` に意味判断が混入」への解法も同じ)。

## 6-8. 見直しトリガー 12 件の v0.2 再評価

**横断的な欠陥(P1)**: トリガー表の「評価するステップ」欄が**ステップ 12→22 の再番号付けに追随していない**
(トリガー 5 → 「12・19」だが変異器は 13・コスト実測は 20 / トリガー 6 → 「16・17」だが (b)③ は 18・19 /
トリガー 8 → 「14」だが生成器は 16 / トリガー 12 → 「19」だが 20)。
加えて **§4-(9) は「12 → 20 になった」・§7 は「12 → 22 になった」で自己矛盾**
(これが 2 周目 P2「内部の数値不整合」の実体)。

| # | トリガー | v0.2 での判定 | 典拠 |
| --- | --- | --- | --- |
| 1 | 型語彙で表現できない | **変質・残存**。型語彙が拡張(`NumericValue` 3 形 + nullable / `DisplayAtom` 不透明型)。評価対象に**表示値の型語彙**を追加 | `ADR:99` |
| 2 | 表示規則が D-1 の 2 種に収まらない | **P0・再スコープ必須**(§6-2 P0-G) | `ADR:99` |
| 3 | SQL と参照実装の意味保存 | **変質・残存**。参照実装も **formatter まで**生成が必要 / **(β)⑦ に SQL `CASE` 式 + 写像①適用受け口**の新形 | `ADR:216`・`:107` |
| 4 | lossless 比較で統一できない | **P0・範囲拡大**。**表示文字列の完全一致**が追加(文字種・先頭0・負号・剰余表記まで)。トリガー文へ「表示文字列」を追加要 | `ADR:243`・REQ `:1092`・`:1099` |
| 5 | SQL / TS 変異が予算内で実装できない | **変質・残存**。**表示系が第 4 系統として追加**され予算対象が増えた | `ADR:223` |
| 6 | 入口全列挙・CSP を fail-closed にできない | **変質・残存**。**(b)③ に 4 つ目の不変条件が新設**され対象が増えた | `ADR:239` |
| 7 | FR-040 除外宣言・D-6 契約分類・段階的マージ | **残存(変化なし)**。FR-040 は Should のまま | `ADR:269`・REQ `:338` |
| 8 | 生成器の保守コストが 1 名体制に見合わない | **変質・残存(リスク増)**。生成物が増えた。ただし **v0.2 は D-2 へ切り替えない 3 理由を明文化済み**(`ADR:120-126`)= このトリガーで D-2 へ倒す判断は先取りして否定されている | `ADR:100`・`:120-126` |
| 9 | `D` の履歴深さとシナリオ長が衝突 | **残存(変化なし)** | `ADR:269` |
| 10 | `propertyCatalog` の ADR 内不整合 | **残存(生きている)**。`:243` の列挙に依然入っていない。ただし**不整合の性質が「未列挙」から「階層分離の意図か否か」へ変質** — v0.2 が行ったのは階層の明記だけで「決定の追加ではない」(`adr003-display-primitives/design.md:83`) | `ADR:216`・`:243` |
| 11 | D-6 の契約分類とベクタ schema の分担 | **P0・発火しやすくなった**。D-6 側の schema が拡張((β)ベクタ expected = 構造化値 + 完成表示値 `:271` / 打球方向ベクタ = 構造化区分 + 表示名 `:275` / A-1 共通規則に完成表示値・集計対象 0 件 `:265`) | `ADR:265`・`:271`・`:275` |
| 12 | 変異テストの時間上限超過 | **変質・残存(超過リスク増)**。上限自体は不変 | `ADR:230`・`:223` |

**v0.2 が生んだ新しい停止条件(トリガー追加が必要 — P0)**:

- **13(候補)**: 表外既定の「対象集合 = 宣言モデル schema の閉包」を機械導出できない(REQ `:1099`・`ADR:239`)
- **14(候補)**: composite target の**値の連鎖検証**を収集器が独立に計算できない(`ADR:243`)
- **15(候補)**: 表示系変異の kill 条件と等価変異の切り分けが実機で一意にならない(`ADR:223`・`:322`)

## 6-9. 2 周目指摘 15 件の解消・残存判定(一覧)

| 指摘(要旨) | 判定 |
| --- | --- |
| **P0-1** 案 A の三すくみ | **残存・悪化**(§6-3)。`bootstrapDebt` は未採り込み |
| **P0-2** 数値表示規則 | **解消**(TSK-278 の正規経路)。ただし計画書 §4-(8)②・トリガー 2 の**削除・再スコープが必要**(§6-2) |
| **P0-3** 網羅性の証明が無い / 不変条件 runner 本体が未実装 | **残存・悪化**(§6-3) |
| P1 依存の逆行 | **残存**(v0.2 は計画書の順序に触れていない) |
| P1 `D` と case 集合が一意に固定されていない | **残存**。加えて**典拠行 `:236` の陳腐化**という新欠陥(§6-7) |
| P1 D-6 の「13 対象 ↔ 契約ちょうど 1 件」が未明示 | **残存**。規則自体は `ADR:262` に明示(「(α)5 件・(β)①〜⑧ のすべてが**ちょうど 1 つの契約で発効する**ことを CI が検査する」)だが、**「13」という数値は ADR・要件書のどこにも無い**(実測: ヒットは worklog と計画書の自己記述のみ) |
| P1 変異テストの D-7 運用が未完 | **P0 へ格上げ**。v0.2 で表示系演算子・適用必須化・kill 条件の分担が追加 |
| P1 トリガー 12 件では不十分 | **P0 へ格上げ**。v0.2 で 3 件以上の新規停止条件(§6-8) |
| P1 前提ゲート 1 の設計不足 | **残存**。付録E-1 は依然「等」で開いている(REQ `:1257`) |
| P1 `[機械]` に意味判断が混入 | **残存**。ただし**兄弟タスクが 5 型 + 4 型の先例を確立**(§6-7) |
| P1 core-area paths の完全性を自己作成の期待値で証明 | **残存 + P0 追加**。現コードに完全に該当(§6-5)。加えて**手動ゲートが正本より軽い**(§6-2 P0-I) |
| P1 段階的 CI 状態モデルのゲート分類が軽すぎる | **残存**。v0.2 は設計書へ波及なし(`adr003-display-primitives` worklog `:172-175` が「結論: 波及なし」で PO 承認済み) |
| P1 PR が大きすぎ・乙案にステップ 17 が無い | **残存・悪化**。実測で乙案は ①(1・16)②(2〜15)③(18・19)④(20〜22) で **17 が欠落**。v0.2 で内容が増えたため「13pt は過小」はより強く成立 |
| P2 内部の数値不整合 | **残存**。実測 2 件(§6-8 冒頭) |
| P2 research §1-10 と実コードの事実差 | **不明**。差の内容が要旨に記録されておらず全文も消失。`research.md:189` の §1-10 は現存するが当時の指摘は復元できない |

## 6-10. 着手順序と位置づけ — 変わっていない

- **ADR の 4 段階は内容不変**。場所が `:373-377` → **`:387-391`** へ移動しただけ。
  「何が変わるか」節・「後続タスクへ送るもの」節・「見直しトリガー」節に **v0.2 の改訂痕跡は 1 件も無い**
  (v0.2 の置換はすべて D-1/D-6/D-7/D-8/D-10/D-11 と変更履歴)。
- **設計書 13 章は v1.13 のまま**(`dev-harness-design-2026-08-07.md:867-873`)。`:872` 逐語
  「**検査基盤の未整備を理由に対象計算のコードを先に追加することはできない**」。
  v0.2 の確定ゲート中に設計書への波及を明示的に検査し「結論: 波及なし」で PO 承認済み。
- **「本タスクは段階 1 の②のみ」という位置づけは今も正しい**(行番号 `:374` → `:388` の更新のみ)。
  **ただし②の外延が拡大した** — composite 証跡 schema と値の連鎖検証を行う収集器 / 表示系変異演算子 /
  表外既定の表示対応の突合を行う静的解析器 / 表示 primitive の生成(Python・TS・(β)Python)と生成前検査 **3 種 → 5 種**
  (`ADR:101` = 遷移 / 型・整数範囲 / 除算・丸め / **表示 primitive 全パラメータ** / **NumericValue 直接差し込み**)。
  → **§7 の「13pt は過小」の再算定が必要。**

## 6-11. 要件突合で「変わっていない」と確認できたもの(適合)

計画に手を入れる必要がない項目。**再調査の重複を避けるため明示的に記録する。**

| 項目 | 判定 | 典拠 |
| --- | --- | --- |
| **付録A の書式 binding 全 36 件**(表 33 行 + A-1 表外 3 規定)が D-1 の primitive 4 種 + 写像① + 閉じた template で表現できるか | **全件表現可能。トリガー 2 は再発火しない** | REQ `:1090-1183`。合成は 3 系統だけ(単一 primitive / primitive + fixed-decimal(0) + template の母数併記 / template に別 primitive の `DisplayAtom` を入れる入れ子〔WHIP・FIP のみ〕)。条件分岐に見える 2 箇所(剰余0省略・null 時の「−」)は primitive のパラメータ/専用 primitive で吸収 |
| 対象集合 **(α)5 + (β)8 = 13 件** | **不変**。v2.6 の射程宣言に区分・対象列挙の改訂は含まれない | REQ `:888-890`・`:39` |
| v2.6 新設 3 行の帰属 | **新対象計算ではない** — A-3 本塁打 = (β)③ + FR-038 移行検証 / A-2 被安打 = (α) 断中前処理・シーズン通算は (β)③ / A-4 状況別割合 = (β)① | REQ `:1132`・`:1109`・`:1156-1160` |
| `D` の導出(FR-006 単独・長さ 4) | **成立**。「取消可能な操作は確定プレイと FR-040 採用時の状態補正(**未採用なら確定プレイのみ**)。**undo 自身は取消可能な操作としない**」 | REQ **`:241`** |
| 「`D+1` に出力同値を課さない」 | **正しい**。`:922` が遷移後の履歴スタック全体を出力に要求するため深さが違えば出力も違う | REQ `:922`・`ADR:322` |
| NFR-019(a) v2.6 例外の影響 | **`D` の導出には無影響**。例外が外すのは表外既定の**表示値**だけで、履歴文脈・操作結果は構造化値として比較面に残る | REQ `:920`・`:1099`・`ADR:322` |
| 主要フラグ 9 項目 | **不変**(回・表裏・得点・S/B・アウト・打順・走者・タイブレーク中・試合終了済み) | `ADR:201` |
| 空を許さない 5 フィールド | **維持** | `ADR:243`・`:247` |
| 5 判定・6 次元要求集合・証跡直積 | **不変** | `ADR:247`・`:243` |
| DoD の「充足を宣言しない」「例外表を触らない」「未整備は不合格」 | **すべて内容は適合**(行番号のみ陳腐化) | REQ `:915`・`:907-911`・`:905` |
| §4-(10) の配置(src-layout・`packages.find` を広げない) | **正しい** | `backend/pyproject.toml:24-25` |
| 「空なのは `backend/domain/` とマニフェスト登録」 | **事実**。`backend/domain/` は存在せず、`backend/src/pitchlog/` は 2 ファイル 12 行 | 実測 |
| 既存の手書き座標変換入口が実在し未宣言 | **事実**(§6-3) | `StrikeZone.vue:11,:12,:51,:60,:70,:105` |

## 6-12. 未解決・PO 裁定を要するもの

| # | 論点 | なぜ人間の判断が必要か |
| --- | --- | --- |
| 1 | **§2「やらないこと」と `ADR:203`/`:206` の衝突** — 「具体ケース値は TSK-235 で確定」を「軸の具体化規則」と読むか「実ベクタの case 値」と読むか | **段階 1 の①/②の分界そのもの**。読み方で本タスクの射程が変わる(§6-2 P0-J) |
| 2 | **§7 の甲/乙**(再ポイント or 4 分割)。v0.2 反映で機構がさらに増えたため**再算定が必要** | 見積りと分割は PO の裁定事項。Claude の推奨は引き続き**甲**(乙は 4 タスク揃わないと段階 1 が閉じず、中間状態が長い) |
| 3 | **マージ順序への TSK-235 の明記** | 既に 2026-09-08 の裁定で「実装は TSK-343 マージ後」と決まっているが、**TSK-343 の裁定 `Q-4` の表に本タスクが無い**。`348 → 317 → 343 → 235` を両計画書に記録するかは調整事項 |
| 4 | **`consistency`・変異ジョブへ `timeout-minutes` を設定するか** | 現在 9 ジョブすべて未設定。計画書に意図が書かれていない(**不明**) |
| 5 | **打球方向の等角5分割区分が 4.0-3 の「打球情報」語彙(管理者管理)に含まれるか** | 含まれると `ADR:99` の写像①適格性(「管理者管理でない安定した enum に限る」)と衝突する。REQ `:1137`・`:1202`・`:1236`・`:168` に明示なし(**不明**)。TSK-236〜239 のベクタ契約で判明するため本タスクの schema 凍結には効かない見込み |
| 6 | **TSK-317 の改訂 3(承認範囲外)が `core-areas.json` / `test_ci_wiring.py` をいつ触るか** | ステップ 22 の期待値リテラルの失効リスク。TSK-317 は検査資産 4 本(scripts 2 + root tests 2)を **`guard_paths` 未登録**で新設しており(実測)、これは台帳 `H-12` 再発の 6 件目に当たる。**不明** |
| 7 | **`harness` 283s の測定日と run id** | 元の 283s は自ブランチ `research.md:596`・`:677` にしか記録がなく run id・測定日が無い。**2026-09-09 に本 worktree(`75b6cd3` = `origin/develop` 1424d67 + 計画書起票 1 件)で再実測: `uv run pytest tests/` = 1144 passed / 285.07s**(ローカル実行・CI runner ではないので CI 時間の代理値として使う場合はその旨を明記する)。**TSK-317 の root テスト +460 行で失効見込みなのは変わらない** — §4-(11) の予算基線は TSK-317 マージ後に測り直す |
| 8 | **`components[]` の最小要素数** | ADR に規定が無い(**不明**)。本タスクの確定事項 |
| 9 | **(β) formatter 生成物の言語・配置**(Python 受け口内の関数か独立モジュールか) | `adr003-display-primitives/design.md:102` は feature 文書であり正本の送り出しではない。ADR 側に規範記述は**不明** |

## 6-13. §0〜§5 のうち陳腐化した節

| 節 | 状態 |
| --- | --- |
| §0-1(射程 = 着手順序 1) | **有効**。行番号のみ更新要(`:373-377` → `:387-391`) |
| §0-2(前提ブロッカ 2 件) | **有効**。`backend/` は存在・`backend/domain/` は未作成 |
| §0-3(2026-09-01 の裁定 3 件) | **有効**。ただし**2026-09-08 の裁定(今回は計画段階まで)を追記要** |
| §1-9(語彙と表示規則) | **陳腐化**。v2.6 の書式 binding と v0.2 の primitive で前提が変わった → §6-1・§6-2 が正 |
| §2-1(D-1 の全項目) | **陳腐化**。3 類・`NumericValue` / `DisplayAtom` が未反映 → §6-1 が正 |
| §2-2(表示規則の限定と旧の実体が収まらない可能性) | **解消済み**。TSK-278 で正規経路により決着 |
| §2-4(D-11 — 3 層・経路一致・マニフェスト) | **陳腐化**。composite target・段別 hash・値の連鎖が未反映 → §6-1 P0-B・P0-C が正 |
| §2-7(決定の空白) | **一部解消**。表外既定の担保機構は v0.2 で方式確定・細部が本タスクへ委任された |
| §2-8(見直しトリガー 2 系統) | **陳腐化** → §6-8 が正 |
| §3(`D` の導出) | **有効**。ただし典拠行 `:236` → `:241` の訂正要 |
| §4(旧システムの事実) | **有効**(v0.2 は旧システムに触れていない) |
| §5(既存ハーネスの実体) | **一部陳腐化**。`03c41ca` で `core-areas.json` / `test_core_guard.py` の基線が動いた → §6-5・§6-6 が正 |

---

# §7. 実装着手前の再調査(2026-09-18・`/investigate` 3 並列)

> **§6 は 2026-09-09(要件書 v2.7 時点)の実測。** 以後、**要件書 v2.9 / `ADR-003` v0.3 /
> 開発ハーネス設計書 v1.15 / `ADR-004` 新設 / TSK-363(23 実装単位)/ TSK-387(U-00 API 器)/
> U-01 DTO 基盤**が入った。**計画は 2026-09-16 に承認済み**で、ステップ 1 の着手前に
> **追跡範囲を閉じた 3 観点**(要件突合 / 決定追随 / コード基盤の現況)を並列で当てた。
>
> **エージェントの報告は全件、原典で裏を取った**(`verify-peer-claims-at-source` の規律)。
> **裏取りの方法が報告と異なる場合は、本節の実測を正とする。**

## 7-1. 要件書 v2.7 → v2.9 の差分 — **機械で全件確認**

**spec-checker は Bash を持たず `git diff` が取れないと限界を明示した**ため、**本セッションで差分を取り直した。**

**`git diff 23425c5..HEAD -- docs/requirements/...`(v2.7 approved 時点 → 現行)= 8 挿入 / 3 削除の 9 行のみ**:

| # | 変更 |
| --- | --- |
| 1〜4 | **変更履歴表 4 行**(v2.8 起案・approved / v2.9 起案・approved) |
| 5 | **`NFR-018` 達成条件 (e) の新設**(1 行) |
| 6〜7 | **`NFR-019` 柱書の 1 行変更**(= `BOOT-NO-CLAIM` の追補) |
| 8〜9 | **10 章 未決事項表の 1 行**(`NFR-018` の正解ベクタ整備行の現況化) |

→ **`NFR-018` の「対象」欄・付録 A・付録 A-1・付録 A-2〜A-5・付録 E は 1 行も変わっていない。**
**宣言ではなく差分で確定した。**

| 本タスクの前提 | 判定 |
| --- | --- |
| **対象計算 13 件**((α)5 + (β)①〜⑧) | **無変更**。封印集合 136 の分母は動かない |
| **付録 A の書式 binding 群** | **無変更**。型語彙の前提は v2.6 確定時のまま |
| **`NFR-019(a)` の対象 5 行と比較面** | **無変更**(柱書へ `BOOT-NO-CLAIM` の 1 文が入っただけ) |
| **付録 E-1「成績計上フラグ」の「等」** | **開いたまま** → **前提ゲート 1 件は残る**(design.md §13) |

> **◎ 正本側の小さなねじれ(事実として記録・本タスクの射程外)**: **要件書 v2.9 の変更履歴行は
> 「本改訂で変更しない範囲」に `NFR-019` の柱書を挙げているが、実際には柱書へ `v2.9追補` の 1 文が
> 挿入されている**(上表の 6〜7)。**宣言と本文が厳密には一致していない。**
> **本タスクに実害は無い**(追補は本タスクが依拠する `BOOT-NO-CLAIM` そのもの)。

## 7-2. 決定の追随 — **`ADR-003` D-1・D-11 とも無変更**

| 対象 | 判定 | 典拠 |
| --- | --- | --- |
| **`ADR-003` D-1 の射程行** | **無変更**。v0.3 の射程宣言は D-7 併記 / D-13 / 封印集合の規律 / トリガー追加の 4 点で、**D-1 を含まない**。本文の `v0.3` 表記は変更履歴・D-7 の表 6 行・D-13・見直しトリガーにしか現れず、**D-1 節の改訂表記はすべて `v0.2`** | `ADR-003` 変更履歴 0.3 行・D-1 節 |
| **`ADR-003` D-11 の per-calculation 宣言 10 種** | **無変更**。`calculation` / `source` / `generated[]` / `entrypoints[]` / `directTargets[]` / `comparison` / `normalization[]` / `vectors[]` / `properties[]` / `mutation`。v0.3 の「変更しない範囲」に **D-11 の 4 不変条件**が明記 | `ADR-003` D-11 ③ |
| **トップレベル宣言 3** | **無変更**(`propertyCatalog` / コンテナ / 表示対応宣言) | 同上 |
| **`ADR-003` D-13**(v0.3 新設) | **本タスクへ新たな義務を足していない**。識別子 `BOOT-ADR-STAGE`・内容は「移行状態は段階 1 と段階 2 にまたがる」の 1 点のみ | `ADR-003` D-13 |
| **`ADR-004`**(新設) | **本タスクへ課したものは無い**。射程外表の受け取り先 5 件に **TSK-235 は含まれない**。発効条項を置いたのは **`NFR-019(b)`(越境)**で、本タスクが関わる **`NFR-019(a)`(一致性)ではない** | `ADR-004` 射程宣言・D-3 |

→ **封印集合 136 = 対象 13 × 宣言 10 + トップレベル 3 +(b) 由来 3 は、現行の正本と整合している。**

**◎ D-13 の「規範を持たない」は実測されている**: `ADR-003` 変更履歴 0.3 の approved 行が
「**D-13 が『規範を持たない』と自称しながら規範を持っていた(13〜14 周目)→ 検証条件を削除テストへ
差し替え**」と記録し、**台帳が「15 周目にレビュアが実際に適用して『通る』と判定した」**と記録している。
**現行の検証条件は「本決定を全文削除しても、`NFR-018` (e) と帰結 1 だけで適用区間・合否判定・量化が
一意に決まること」。** → **本タスクは D-13 を依拠条項に挙げない**(挙げると規範を持たない条項へ依拠することになる)。

**◎ 実効的に掛かるのは D-7 実行契機表の v0.3 併記のほう**(6 機構すべての「失敗時の扱い」欄)。
**ただし述語はすべて要件書 `BOOT-GRANT` の逐語**であり、**ADR 側に新規義務は無い**。
**承認済み計画のステップ 17・19・21・22 が既に覆っている。**

## 7-3. 後続タスクの所在(TSK-363 の 23 実装単位)

**TSK-363 は 5 単位を「凍結帯」に置き、着手不可の典拠に `ADR-003` と `NFR-018` を挙げている。**

| 単位 | 凍結の性質 | 本タスクとの関係 |
| --- | --- | --- |
| **`U-X1`** 状況計算核 | **条文凍結**(`ADR-003`「この段階では対象計算のコードを追加しない」) | **直接の後続**(段階 2) |
| **`U-X2`** 分析・集計 (β) | **条文凍結** | **直接の後続**(段階 2) |
| **`U-X5`** 一括移行 | **混在** — **⑥座標変換と⑧責任投手・成績帰属の 2 面のみ**条文凍結 | **直接の後続**(当該 2 面のみ) |
| `U-X3` スコアカード | **条文凍結ではない**(`U-X1` 依存) | 間接 |
| `U-X4` 分析資料 PDF | **条文凍結ではない**(`U-X2` 依存) | 間接 |

→ **本タスクの完了が解凍の前提**として記録されている単位は **`U-X1` / `U-X2` / `U-X5`(⑥⑧)の 3 件。**

> **◎ 典拠が取れなかったもの(不明)**: **TSK-363 の帰属は FR 単位**であり、**`NFR-018` の 13 対象計算を
> 実装単位へ割り当てた表は存在しない。** 具体的に **(β)③ シーズン通算**((β) だが FR-021/022 は `U-X1` 主所有)と
> **(β)⑦ 打球方向導出**の所有が一意に決まらない。**TSK-363 の design.md 自身が「210 セルすべてを証明しない。
> 各単位の計画書が自分の FR について再確認する」と完全性を後続へ送っている。**
> → **本タスクの射程外**(段階 2 の各タスクが決める)。**引き渡し時に明示する**(§16-13)。

## 7-4. コード基盤の現況 — **配置の前提は生きている。ただし 4 件の食い違い**

| # | 実測 | 影響するステップ | 判定 |
| --- | --- | --- | --- |
| 1 | **`backend/domain/` は JSON 資産であり `ty` は Python しか見ない。`domaincheck` / `domaingen` / `domainmut` は `src` 配下なので既存の `[tool.ty.src] include = ["src","tests"]` で自動的に対象** | **53** | **design.md §1-3 の「`backend/domain/` を `[tool.ty.src] include` へ追加する」は誤り** → 是正 |
| 2 | **`.dependency-cruiser` は不在**。`frontend/package.json` の devDependencies にも無い。**import 境界を強制するプラグインが 1 つも入っていない**(lint は eslint + eslint-plugin-vue + typescript-eslint + prettier のみ) | **44** | **新規導入が要る**(devDependency + 設定)。**§3 は `frontend/package.json` / `pnpm-lock.yaml` を宣言済みなので射程内** |
| 3 | **`backend/tests/db/conftest.py` の `pytest_sessionfinish` は「通常の全件実行で DB テストが 0 件なら失敗にする」**(docstring 逐語)。**`backend/tests/db/` を収集対象に含む実行でのみ有効** | **52** | **新設ジョブが `backend/tests/` を DB なしで回すと red**。**§14-1(`services: postgres` の書き忘れ)と同型の暗黙結合**。**計画に無かった** → 追加 |
| 4 | **`backend/pyproject.toml` に `package-data` も `MANIFEST.in` も不在**。既存の前例は **`contracts/authz/ddl-elements.json` をリポジトリルート相対パスで読む**方式(`backend/src/pitchlog/authz/ddl.py`) | **53** | **同方式に倣えば `pyproject.toml` の変更は不要**。**design.md §1-3 の「package-data または明示パスの契約」のうち後者を採る** |

**整合が確認できたもの**:

- **`backend/src/pitchlog/` の兄弟は `api` / `authz` / `db`**。**`backend/src` 配下に `domain` を含む識別子は 1 件も無い** → **3 パッケージの命名衝突なし**
- **`frontend/` は存在**(Vue 3.5 + Vite 8.2 + vitest 4.1・pnpm)。**`src/lib/generated/` は不在** = 新設どおり。**`index.html` 存在・`vite.config.ts` の `rollupOptions.input` は明示指定なし**(既定の `index.html` が入力)→ **ステップ 10・44・45 の前提は生きている**
- **`tests/domain/` と `backend/tests/domain/` は不在**・近名衝突なし
- **DB fixture は `backend/tests/db/` 配下に完全に閉じている**(`backend/tests/conftest.py` は AnyIO 設定のみで DB 非依存)→ **検査器テストは DB 非依存で置ける**

> **◎ 「JSON 規範資産が `contracts/` に集約済みなのに `backend/domain/` を作ると 2 系統に分かれる」は
> 決定済みであって食い違いではない。** **`ADR-003` D-1-b が逐語で「`contracts/` には正本・生成器・生成物を
> 置かない」と定め**、**`backend/domain/manifest.json` も `ADR-003` が名指ししている。**
> **意図した分岐である** — **再燃させないためにここへ記録する。**

## 7-5. 本タスクの文書に見つかった残滓 — **2 件**

| # | 内容 | 型 |
| --- | --- | --- |
| 1 | **`plan.md` に撤回済み語彙が 6 箇所残存**(「未達 / 違反 / 債務」「`bootstrapDebt`」「債務台帳の負例 5 種」)。**§2 スコープ / §4-(2) 契約として固定する 6 点 / §6 テスト計画**。**design.md §4 は §17 是正 `D` で改稿したが、plan.md を取り残した** | **§16-9**(引き写しの取り残し)。**§17 是正 `D` の不完全な履行** |
| 2 | **「付録A の書式 binding 全 36 件」(§6-11)の数え方が定義されておらず、再現できない** | **母集合の取り違え**(4〜6 周目の型 A) |

**2 の実測**: **付録A の範囲(柱書〜付録B の直前)で「表示書式」列を持つ表の行数は 32**
(A-2 = 12 / A-2b = 4 / A-3 = 7 / A-4 = 1 / A-5 = 8)。**§6-11 は「表 33 行 + A-1 表外 3 規定 = 36」と書くが、
本セッションの計測は 32 行**で一致しない。**複合行 4 件**(`最速・平均球速` / `勝 / 敗 / 分` /
`得点 / 失点` / `イニング別得点・失点`)**を分解するかどうかで数が動く。**
**要件書に「36」という件数の記載は無い**(spec-checker も同じ結論)。

→ **ステップ 1 の合格条件は「36 件」を前提にしない。** **母集合の数え方そのものを資産に定義し、
そこから機械導出する**(**独立導出** — 「複合行を分解するか」の規則を明示し、
その規則で数えた件数を `assert` する)。**§6-11 の「全 36 件」は本節で失効させる。**

## 7-6. §6 のうち本節で更新したもの

| §6 の節 | 本節での扱い |
| --- | --- |
| §6-11(要件突合で「変わっていない」と確認できたもの) | **「対象欄・付録 A・付録 E は無変更」は §7-1 で機械確認して**強化**。ただし「書式 binding 全 36 件」は §7-5 で**失効** |
| §6-10(着手順序と位置づけ) | **無変更**。§7-3 で**後続単位の所在を追加** |
| §6-12(未解決・PO 裁定を要するもの) | **成績計上フラグの外延は開いたまま**(§7-1)。**前提ゲート 1 件は残る** |
