---
feature: data-model-canonical
type: research
date: 2026-08-30
---

# 調査メモ: データモデル設計の正本化(候補案 → `docs/design/` 正本)

**典拠の記法**: 本メモは調査記録なので `ファイル:行` で典拠を書く(設計書 8.5 の要求)。
**正本 `docs/design/` 側では行番号引用が機構で禁止**されている(2-2 節)ため、正本化時は
`[FR-012](../requirements/requirements-pitchlog-2026-07-22.md#FR-012)` 形式へ貼り替える。

**◎印** = 調査サブエージェントの報告のうち **Claude が原典を直接確認したもの**。
略記: `CAND` = `docs/features/product-data-model-design/design.md`(候補案) /
`SYNC` = `docs/design/sync-protocol.md` / `REQ` = `docs/requirements/requirements-pitchlog-2026-07-22.md` /
`HARNESS` = `docs/development/dev-harness-design-2026-08-07.md` /
`DL` = `docs/legacy/research/data-layer.md` / `DB` = `docs/legacy/baseball-scoring-db-structure.md`。

## 問い

前タスク(TSK-248)の候補案 `CAND`(1866 行)を `docs/design/` の正本へ昇格させるにあたり、

1. 候補案の「形」をそのまま採ってよい部分と、**採ってはいけない部分**はどこか
   (候補案 12-7 節が「形は提案したが検証はしていない」と宣言した 3 領域)
2. 直後に正本化された **同期プロトコル設計 `SYNC` が返した決定**により、候補案のどこが
   **否決・書き換え・追加**になるか
3. 確定ゲート(設計書 7.3)を通すために、文書として**どんな「形」を満たす必要があるか**
4. DoD の「実 PostgreSQL での越境テスト 4 件」は、この環境で実際に何が要るか
5. 「要件に明示がなく導出で決めた 5 件」は、本当にそのまま人間裁定へ上げるのか
6. 88 列の行き先・移行の境界は、旧システムの事実と一致しているか

## 結論(要約)

1. **候補案 4-2 節の用途対応表は正本へそのまま移送できない**。同期正本が「D1(同期連番)を出場区間の
   境界・再計算の起点に使ってはならない」と確定したのに、候補案は **4 箇所に旧文言が残存**(◎)。
   候補案自身の 411 行と自己矛盾しており、同期側タスクが申し送った既知の欠陥である。
2. 同期正本との突合で **矛盾 15 件 / 欠落 20 件 / 書き換え 12 件**。とくに**変更イベント(P3 — 参加区分
   #10〜#12)の実体が候補案に無い**。同期正本 11-2 節「データモデル影響差分」は**丸ごと本タスク宛**(◎)。
3. **確定ゲートの実体は文書だけではない**。先行正本 `SYNC` には表間伝播マニフェスト・要件母集合の
   全数帰属表・典拠台帳・専用 CI 検査が付き、**行番号引用は機構で禁止**されている(◎)。候補案は全典拠が
   `REQ:<行>` 形式なので**全件貼り替え**が要る。
4. **越境テストの置き場が無い**(◎)。`backend/` は骨組みのみで DB ドライバ・Alembic 未導入、CI にも
   postgres サービスが無い。**生 SQL で書けば ORM 選定と独立**にでき、DoD の順序依存を解ける。
5. 導出 5 件は**そのまま 5 件ではない**。要件書側で既に決着している部分があり、裁定に上げる論点は縮む。
   加えて **要件書内部の矛盾を 1 件発見**(管理者操作ログの Must/Should — ◎)。
6. 88 列表は**列名の転記ミス 0 件**だが、**旧資料との食い違い 16 件**。うち最大は
   **列 63-71 を「新スキーマに残さない」としながら互換出力で 88 列を再現する契約と両立しない**点(◎)。
   さらに **`game_lineup_snapshot` は正本(`DL`)に存在しない**(◎ — 候補案 7-4・P-45 は `DB` のみに依存)。

## 詳細と典拠

### 1. 同期プロトコル正本との突合(最重要)

`SYNC`(approved v0.1・2026-08-30)は候補案 12-7 節 (2) が送った「イベントの順序」に**全件回答した**。
その結果、候補案の記述の一部が**否決**されている。

#### 1-1. 同期正本が確定した内容(◎)

| 事項 | 決定 | 典拠 |
| --- | --- | --- |
| **D1(同期連番)の意味** | **クライアントが操作を受理し端末内の永続キューへ新規追記した順**。サーバー受信順ではない | `SYNC:37` |
| 「到着順」の扱い | **概念として存在しない**。禁止語に指定 | `SYNC:41`・`:55` |
| **D1 の用途制限** | **出場区間の境界・再計算の起点に D1 を使ってはならない** | `SYNC:456` |
| **D2(論理順序)の方式** | **整数の間隔(gap)に確定**。有理数・LexoRank・連結リストは却下理由つきで否決 | `SYNC:476`-`:479` |
| 再採番 | **あり**(隙間が尽きたら採番と同じ原子的操作の中で局所再採番 — O2) | `SYNC:605`-`:606` |
| **D5(べき等キー)の一意範囲** | **`(テナント, D5)`**。「グローバル一意にするかを TSK-250 へ送る」という初版の委譲は**撤回** | `SYNC:381`・`:383` |
| **W1 / W2** | 確定済みイベントの修正・削除は**追記型の変更イベント**。**墓標・改訂を流用しない**。「削除」は 2 つあり同じ経路にしない | `SYNC:304`-`:305`・`:356` |
| **変更イベント(P3)** | 参加区分 **#10 プレイの修正 / #11 プレイ行の論理削除 / #12 交代イベントの修正**。V11(期待版)による楽観ロック。変更版順は D1 でも D2 でもない第 3 の順序 | `SYNC:589`-`:591`・`:306`-`:308` |
| **11-2 データモデル影響差分** | **丸ごと TSK-250 へ渡す**と明記 | `SYNC:1728`・`:2077` |

#### 1-2. 候補案に残っている矛盾(◎ — P0 相当)

| # | 候補案の記述 | 同期正本の決定 |
| --- | --- | --- |
| C1 | `CAND:408`「**イベント連番は同期の到着順**」/ `:1722`「同期連番が『到着順』であることを明記し」 | `SYNC:41`「サーバー到着順という概念は本設計に存在しない」— **申し送り文自体が誤り** |
| C2 | `CAND:404`「**出場区間の境界** \| **イベント連番**で持つ」 | `SYNC:456`「D1 を使ってはならない」 |
| C3 | `CAND:584`「出場区間(投影)… 有効開始と有効終了(**イベント連番**またはプレイ番号)」 | 同上 |
| C4 | `CAND:405`「**再計算の起点**(状態補正・undo) \| **イベント連番**」 | 同上・`SYNC:1772`(起点は D2) |
| C5 | `CAND:419`「**任意位置への挿入はプレイ番号の採番(投影側)**で表す」 | `SYNC:490` 挿入は **D2(イベント側)の隙間**へ採番し O1 で直列化 |
| C6 | `CAND:417`「**プレイ行の論理削除は投影側のフラグ**であり、**正は墓標・改訂イベント**」 | `SYNC:305` W2・`:356`(削除は 2 つ・同じ経路にしない) |
| C7 | `CAND:420`・6-4 節「退避イベントの取り込みは**現行世代の連番として採番し直す**」 | `SYNC:2057` U-2「**取り込みの規則は一切決めない。本書から類推してはならない**」→ TSK-267 |
| C8 | `CAND:1540`(移行)「イベント連番 \| 旧行の行識別子の順序で連番を振る。**これが論理順序の正になる**」 | `SYNC:456`・`:1772` |
| C9 | `CAND:438`・`:534` べき等キーの一意範囲が未確定 | `SYNC:381`(`(テナント, D5)`)・`:383`(委譲を撤回) |
| C10 | `CAND:439`・`:525`「(試合, 記録権世代)内で**単調増加**」 | `SYNC:25`(新規割り当てだけが連続。D6・D7 は同じ D1 を再利用) |
| C11 | `CAND:530` 操作イベントの属性行に「**適用水位**」 | `SYNC:645` R1(D3 は `(試合, D4)` ごと)・`:1541`(置き場の決定は TSK-250) |
| C12 | `CAND:541` 履歴文脈 =「未取消の取消可能イベントを**連番降順**」 | `SYNC:1110`(「世代の連結 + 世代内 D1」で導出) |
| C13 | `CAND:564`「試合 / **世代番号** \| (試合, 世代番号)で一意」 | `SYNC:28`(ロールバック後も D4 を再利用しない — 番号方式は最大値+1 で再利用が起きる) |
| C14 | `CAND:503`・`:385` 楽観ロックの版を**プレイ行(投影)側**に置く | `SYNC:307` W3-a・`:1781`(対象 = イベント側に「確定版」列) |
| C15 | `CAND:614`-`:617` 退避は「旧世代 / 元の連番」限定 + 「取り込み先」参照 | `SYNC:32` D8・`:1512`(端末資料を正史へ適用する入力構造にしない) |

**C2・C4 は既知**: 同期側タスクが調査中に検出し「本タスクは 411 行を正として起草し、**表の修正は
正本化タスクへ申し送る**」と記録している(`docs/worklog/2026-08-26-sync-protocol-design.md:24`)。
**C3・C8 は今回新たに検出**した同型の残存(worklog は 4-2 節しか挙げていない)。
**候補案自身の中で矛盾している**: `CAND:411`「境界と再計算の起点は『論理順序』側を使う。**同期連番は
同期と undo に限る**」が正で、`CAND:404`-`:405` の表が旧文言(◎ 直接確認)。

#### 1-3. 欠落(同期正本が要求し、候補案に対応物が無い) — 20 件

代表: **D2 の列そのもの**(`SYNC:1764`)/ **D2 の部分一意と O4 の同値検出**(`:609`)/
**変更イベント P3 の実体 #10〜#12**(`:589`-`:591`)/ **V10 の複合参照列 `(試合, 対象の D4, 対象の D1)`**(`:1771`)/
**V11 期待版と対象側の確定版列**(`:1781`)/ **D5 ごとの確定結果台帳(T6)**(`:1182`)/
**T9 の拒否原本の一体保存**(`:1185`)/ **復旧世代**(`:265`)/ **RG1 復元調整状態**(`:1768`)/
**I6(P3 受理結果の端末保持)**(`:1769`)/ **無効化意図(outbox)の原子保存**(`:1183`・`:1321`)/
**端末側キュー状態 4 種**(`:1504`-`:1508`)/ **D3 の後退禁止**(`:1766`)/
**V1・V4・V5・V7 の NOT NULL**(`:1780`)。

#### 1-4. 同期正本が「TSK-250 の確定を待っている」もの — 8 件

D3 の物理表現・保持場所(`SYNC:1541`)/ 退避イベントの列定義(`:1542`)/ D2 の数値型(`:1544`)/
U-3 退避の保持期間(構造側)(`:2058`)/ U-7 D1・D2 用途分離の伝播(`:2061`)/ U-8 クラッシュ注入点
(構造側)(`:2062`)/ **無効化トリガー × 対象範囲マトリクスの正本化**(`:1314`)/ 9-2 境界表の
「永続化の正本」= TSK-250 の 17 行(`:1355`)。
加えて **禁止事項 P-01・P-03・P-04・P-10・P-11・P-22 の判定**を同期側が明示放棄して本タスクへ送っている(`:206`)。

#### 1-5. 本タスクが作ってはいけないもの(同期正本が射程外と宣言)

退避イベントの現行世代への取り込み構造(TSK-267 待ち・**類推禁止** — `SYNC:2057`・`:2069`)/
U-1 の 3 経路(`:2056`)/ 復元前受理イベントを正史へ戻す経路(`:1512`)/ RR-3 の回収機構
(`:2071`「v0.1 は保護しない」)。

### 2. 確定ゲートと文書の「形」

#### 2-1. ゲート手順(◎)

`HARNESS:395`-`:405`: draft → in-review(`codex_run.py review adversarial`・**収束するまで反復**)
→ 人間承認(対話で明示)→ approved(版数確定・変更履歴追記・`docs/README.md` 索引更新)。
`docs/design/` は確定ゲート**必須**(`HARNESS:387` の表)。`/finalize-doc` が敵対レビュー未実施の
approved 化を手順上不可能にする(`HARNESS:404`)。反映周 1 周ごとに `plan.md` の `確定ゲート周回` を +1、
**反映周コミットは件名に `反映<r>周目` を含めステップ記法を付けない**
(`.claude/skills/finalize-doc/SKILL.md:17`-`:18`)。

#### 2-2. 先行正本が確立した「形」(◎)

- **行番号引用の禁止**: `scripts/check_design_propagation.py:980` `check_citation_format` が
  **`REQ:<行番号>` / `<パス>.md:<行番号>` / 裸の行番号**を検出して失格させる。
  **`docs/legacy/` への行番号引用のみ逐語証拠として許容**(同 `:983`)。
  先行正本は `[FR-012](../requirements/requirements-pitchlog-2026-07-22.md#FR-012)` + 引用文の形(`SYNC:25` ほか)。
  → **候補案は全典拠が `REQ:<行>` なので全件貼り替えが要る**。なお候補案の `REQ:<行>` は要件書 v2.4 の
  追記で **+9〜+30 行ずれている**(例: 候補案 `REQ:487` → 現行 `REQ:500`)。**行番号を直すのではなく形式ごと変える**。
- **述語の単一定義 + 禁じる別名の表**: `SYNC:2`-`:55`(D1〜D9 の定義と、各述語で使ってはならない語の
  一覧 = 残存検査の入力)。前タスクの教訓「**定義を集約しただけでは伝播漏れは直らない。残存を検出する
  検査が別に要る**」(台帳 H-78)への対処。
- **機械検査**: `scripts/check_design_propagation.py`(表間伝播マニフェスト
  `scripts/design_relations/sync-protocol.json`・12 検査)/ `scripts/check_doc_coverage.py`(要件母集合
  `req-universe.json` に対する全数帰属表 + 典拠台帳・判定は 支持 / 不支持 / 射程過大 / 誤典拠)。
  **いずれも既定の対象文書が `docs/design/sync-protocol.md` にハードコード**(`scripts/check_doc_coverage.py:15`)で、
  CI は引数なしで実行。**新正本を同じ機構に載せる規範は正本に無い(不明)**。載せるなら CI 配線と
  設計書 10.1 の追随が増える。
- **`guard_paths` に検査機構が入っている**: `.claude/core-areas.json:3`-`:22` に
  `scripts/design_relations/*`・`check_design_propagation.py`・`check_doc_coverage.py` が列挙されており、
  マニフェストを足す PR は **core-guard の必須チェック(人間の逐行確認)**が発火する。

#### 2-3. 索引・変更履歴の規律

`docs/README.md:5`-`:23` が正本表。**索引セルは frontmatter と一致しないと docs-lint が失敗**する
(`HARNESS:380`。正規化は「太字除去 → 半角 `(` より前を抽出 → trim」)。先行事例の索引行
(`docs/README.md:12`)は**同じゲートで一括検証した相手方を注記に書く**運用。

### 3. 設計書 5.1 の追随(DoD)

- 「設計フェーズで最終確定」と書かれているのは **`HARNESS:205` の Alembic 行 1 行のみ**(◎)。
  **ORM 行・DB ドライバ行は 5.1 に存在しない**ため、追随は**書き換え + 行の新設**になる。
- `backend/pyproject.toml` の依存は `fastapi[standard]` のみで **SQLAlchemy・psycopg・Alembic は未導入**(◎)。
  今回の追随は**純粋に文書上の確定**で実装を伴わない。
- **ゲート区分が未裁定**: 7.6-3 の決定表は「実装追随の節更新(PR レビュー)」と「版繰り上げを伴う
  構造的変更(7.3)」の 2 分岐しかなく、**「留保条項の解除 + 行の新設」がどちらかは正本に明文が無い**。

### 4. コア領域 `paths` の材料(DoD)

`.claude/core-areas.json:24`-`:28`(◎): 5 領域のうち **4 領域**に `SYNC` + 要件書が登録済みで、
**`data-migration` のみ `[]`**。コードパスは 0 件。`guard_paths` は**完全一致集合で glob が効かない**
(`docs/worklog/2026-08-28-sync-protocol-canonical.md:178`)。6.3 の落とし込み規則⑤(`HARNESS:361`)により
paths の追加は敵対レビュー + 人間承認が要るが、先行事例は「`.claude/` は 7.2 の確定ゲート対象外なので
`/finalize-doc` の対象にせず、**本タスクのゲートが規則⑤を満たす**」と整理した(同 worklog `:189`)。
**注意**: 台帳 H-12 の事象欄は「5 領域すべてが `paths: []`」のままで現況と食い違う
(`docs/development/harness-evaluation.md:427`・状態は `:432` で未対応)。

### 5. 要件突合

#### 5-1. 導出 5 件は「そのまま 5 件」ではない

| 導出 | 要件側の状況 | 裁定に上げるべき残り |
| --- | --- | --- |
| (a) 再参加時の付与 | 記載なし。ただし `REQ:500`「離脱後の再参加は**新規参加として計数**」・`REQ:493`「**既定はすべて非共有**」・`REQ:716`「無効化中は参加を維持し再有効化でそのまま復帰(**離脱とは異なる**)」が候補案の方向と整合(◎) | 「付与を復活させない」を要件へ明記するか、設計側の決定に留めるか |
| (b) カルテ世代上限超過 | 世代数は 3 箇所で定義済み(`REQ:534`・`:972`・`:1199`)。**保存の可否は決着**(`REQ:972`「無期限(物理削除なし)」・`:152` 4.0-2) | **可視性の線引きだけ**(11 世代目以降を復元対象・表示から外すか) |
| (c) 取り込みバッチ識別子 | 記載なし。**候補案の導出理由が原文より強い** — `REQ:779` の原文は「**新 DB 側を初期化**して何度でもやり直せる」でありバッチ粒度を要求していない(◎) | 理由の書き直し + バッチ粒度を採るかの裁定 |
| (d) 非表示の復元可否 | **一律ではない**。試合のゴミ箱期限経過は「DB 残存・**管理者がフラグ復旧できる**」と明記(`REQ:391`・`:148`・`:115`) | **選手・対戦相手チーム・プレイ行・プレイ 0 件試合の 4 種**に限定 |
| (e) 移行由来で未検証 | 「**移行由来の区別**」は要件から必須(`REQ:558`・`:776`・`:781` — 出力で「不明」「移行データのため帰属不明」と表示し分ける)。「未検証/不整合」の**行属性**は記載なし(要件は警告レポートに置く — `REQ:772`・`:1030`) | 後者のみ。棚卸し状態の保持先(行 or レポート)も同時に |

#### 5-2. FR-030 勝敗投手・セーブの手動指定

要件は **Could**・「指定は任意・後から変更可」・「**自動判定は行わない**(2.2 Won't)」まで
(`REQ:559`・`:92`)。**保持先・未指定の表現・変更履歴の要否は記載なし**。候補案が実体を置かなかったのは
Won't 違反ではない(Won't は自動判定のみ)。

#### 5-3. 要件書内部の矛盾を 1 件検出(◎)

**管理者操作ログの優先度**: `REQ:724`(FR-035 柱書)「操作ログは **Should**」・`REQ:975`(6.1)「**Should**」に対し、
`REQ:734`(受入基準)は「破壊的操作(**Must** に v1.7 で昇格)」。変更履歴 v1.7(`REQ:17`)も昇格と書く。
候補案 8-4 節は **Must** で設計しており受入基準に従っている。**要件書側の追随漏れ**であり、
人間裁定 or 同一 PR での節更新(版は上げない追随)が要る。

#### 5-4. 6.1 の表を超える実体は 3 群に分かれる

6.1 は**サーバー側の永続化スキーマの網羅表ではない**(「未同期キュー」「P3 受理結果の端末保持」は
**端末内**の行 — `REQ:966`-`:978`)。候補案 12-6 が 1 つの箱に入れた 9 実体は、
**① 他条項が Must で永続化を要求(7 件: 操作イベント・記録権・退避イベント・PDF 出力実績・隔離領域と
移行レポート・トークン/セッション・レート制限カウンタ)② 採用可 = 任意(事前集計 — `REQ:809` NFR-005)
③ Could 1 条のみ(勝敗投手の手動指定 — `REQ:559`)** に分けるべき。

#### 5-5. 越境テストの母集合

NFR-019(b) の原文(`REQ:923`)は**合否を「越境が 1 件でもあれば fail」ではなく「FR-034 の認可行列どおりに
通り、行列外はすべて 404」で判定する**と定め、網羅すべき組合せを **付与の粒度 3 種 × 相互性 /
終了後・離脱後・撤回後・無効化後 / 複数グループ同時参加 / 資源種別ごと / 制御資源の読み取り 3 ケース /
7 操作 / 再有効化後 / 既定拒否 / 単一テナント名指し / 参加上限の同時受諾 / 同時比較上限超過の 400 /
自テナントを含む集合 / 在籍区分変更後のキャッシュ失効**と列挙している。
**DoD の 4 件は NFR-019(b) の全体ではなく、認可機構の物理構成を検証する最小集合**である。
要件から落ちる「データモデルが満たすべき検証可能条件」は 22 件(C-1〜C-22 — 5-6 節)。

#### 5-6. データモデルが満たすべき検証可能条件(要件からの機械的な落とし込み・抜粋)

所有テナントの一意決定(`REQ:595`・`:634`・`:839`)/ 許可リスト 4 経路以外に他テナント行への到達経路が
モデル上存在しないこと(`REQ:599`-`:608`)/ 認可判定に要る 6 前提が永続化属性として引けること
(`REQ:612`-`:619`)/ 付与が (グループ × 自テナント) × 粒度 3 種で、**行なし = 非共有に倒れる**こと
(`REQ:493`)/ 参加は履歴を持ち `active` 最大 1 件(`REQ:500`)/ 実効 `admin` 1 名以上(`REQ:501`・`:717`)/
招待はハッシュのみ保持(`REQ:491`)/ 共有対象は自テナント所有 ∧ 在籍 `active`(`REQ:496`・`:497`)/
共有指標は付録A 定義のみ・率は分子分母から再計算(`REQ:494`・`:690`)/ 撤回 5 経路 + 在籍区分変更が
無効化トリガーとして引け、**招待の失効は含めない**(`REQ:702`-`:708`・`:374`)/ 認証情報はソルト付き
bcrypt のみ(`REQ:845`)/ レート制限カウンタはプロセス外(`REQ:1003`)。

### 6. 旧システムとの突合(88 列・移行境界)

**列名の転記は 88 列すべて `DL:150`-`:231` と一致し、転記ミス 0 件。** 一方、**行き先・意味の食い違いが
16 件**。移行変換に直接効くものを挙げる。

#### 6-1. 明確な誤り(◎)

| # | 候補案 | 旧資料の事実 |
| --- | --- | --- |
| D-1 | `CAND:1444` 列 17-19「旧は DB に試合終了フラグがなく**列 19 にのみ現れる**」 | 「試合終了」は**列 18 イニング継続の値域にも現れる** — `DL:170`「"イニング継続"/"イニング開始"/**"試合終了"**」 |
| D-2 | `CAND:1478` 列 76 開始時刻「旧は手動ボタン未押下で **"0:00" のまま**」 | "0:00" のままになるのは**列 75 経過時間** — `docs/legacy/research/services-shell.md:122`・`DL:222`。列 76 の未押下時の値は `DL:223` で値例が空欄 = **旧資料に記述なし**。列の取り違え |

#### 6-2. 候補案内部の整合性の破れ(◎ — 正本化前に潰すべき最大の点)

**D-14**: `CAND:1472`-`:1474` は列 63-71(打者登録名/番号・一走〜三走の登録名/番号・投手番号)を
「**移行時に使用 … 新スキーマには残さない**」とする。一方 `CAND:1423` は「**互換出力(FR-031)では
列名・順序を維持する** — 88 列は不変の契約」。旧の CSV 出力は 88 列の列名・列順そのもの
(`docs/legacy/research/docs-tests-ops.md:173`)で、列 63-71 には実値が入る(`DL:215`-`:218`)。
**捨てると移行試合の互換出力で 63-71 を再現できず、再導出も不可**(移行試合の出場区間は「不明」)。
隣の列 72 は「**保持のみ … 互換出力のため**」と逆の扱いになっており、**候補案自身が列 47/55 について
是正した P1-9(守備番号を原本事実として保持)と同型の未是正**である。同じ理由で「導出」に分類された
列 62・77 も互換出力の再現性が未証明(D-4・D-5)。

#### 6-3. 旧資料で裏が取れない主張(5 件)

列 86-87「0 点の回と未消化 `""` を区別できず情報喪失」(`CAND:1506`)→ 旧資料は「未消化回は空文字」と
書くのみで **0 点の回の表現は記述なし** / 列 62 Result_col「導出」→ `DL:214` に生成規則・値例なし /
列 77 現在時刻「導出」→ `DL:224` は記録値 / 列 33 投手左右「選手属性から導出」→ 旧 `player` の左右は
**投打兼用の単一列で "両" を含む**(`DL:38`)ため復元不能な場合がある(候補案は列 28 打席左右を
「導出できない」としており**非対称**)/ 列 30 作戦2 の `"0"` センチネル → `DL:182` に記載なし。

#### 6-4. 旧資料にある事実の欠落(移行変換に効く・7 件)

列 52 牽制の種類は**投手牽制と捕手牽制で上書き共有され表記体系が不統一**(「一塁牽制」vs「1塁牽制」)/
列 7/8 の先攻後攻逆転は**「スタメン確定前の行のみ」**(`DL:412` — 全行一律スワップすると正しい行を壊す)/
列 0 は名が「試合日時」だが**実データは日付のみ**(`DL:152` — P-43 該当なのに備考が「—」)/
列 27/32/35 と top/bottom_names の**初期値がプレースホルダ文字列**(`DL:179`「先攻1」・`DL:184`「後攻P」—
そのまま名寄せすると架空選手を生む)/ 列 60 コメントは Streamlit 入力画面では**常に空** /
**センチネル `0`/`"0"` の列単位の列挙が不完全**(未言及: 列 44・45・46・47・52・57・58・72・73・74 —
`REQ` の「NULL 件数を旧側条件と突合」は列単位の定義がないと実行できない)/
**Streamlit 期の試合には `game_lineup_snapshot` 行がそもそも存在しない**(`DB:310`)。

#### 6-5. 座標(付録B)

保存形式は **0〜263 の float(共通スケール)**で、素材の実サイズは 401×401 / 421×421・表示は 360px / 344px
(`docs/legacy/research/input-screen.md:199`-`:210`)。したがって「263px」は画像のピクセル数ではなく
**正規化前の共通スケール**。**未クリックは (0,0)**、集計側の除外条件は `x<=0`(`DB:208`)。
**Y は下向き**で分析側が `Yadj = 263 - Y` と反転していた(`docs/legacy/research/analytics-reports.md:18`)
→ **P-42(センチネル判定を座標変換より先に)は旧資料から正しく導ける**(先に反転すると 0 → 263 になり
未入力が有効値に化ける)。**ただし「捕手視点」は旧資料のどこにも記述が無い**(旧の視点は不明)。
左打者用 `Plate_L.png` と右打者用 `Plate_R.png` が鏡像かも記述なし → **移行変換が恒等か左右反転かは
旧資料だけでは確定できない**。`DB:305` は検証手段を「右/左打者別チャート突合(I-17)」と指定している。

#### 6-6. ファンアウトと `game_lineup_snapshot`

**複製が必要になる構造的根拠は旧資料で完全に裏が取れる**: 旧 `team` は**テナントと対戦相手が同居**し
(`DB:168`)、**チーム名がグローバル一意**(`DL:28`)。所有境界は試合側の `owner_team_id` だけで **NULL 可**
(`DL:55`)。選手は `UNIQUE(チーム_id, 背番号)` でチーム行に従属(`DL:35`-`:39`)、`stamem` は
**1 チーム 1 行・名前ベース JSON**(`DL:41`-`:45`)。**倍率・名寄せ閾値・所有テナント不明行の実件数は
本リポジトリでは確認不能**(移行の読み取り元は本番 Supabase)で、候補案もそう明記しており扱いは適切。

**`game_lineup_snapshot` の重大な問題(◎)**: **`DL` は「テーブルは全12種」と書き、`game_lineup_snapshot`
への言及が 0 件**。一方 `DB:17` は「13 個」で 4 箇所に記述がある。**88 列・データ層の正は `DL`**
(AGENTS.md)であるにもかかわらず、**候補案 7-4・P-45・`CAND:1503`-`:1504` は `DB` のみに依存**している。
また `DB:310` は「交代ごとに上書きされる**試合終了時のオーダー**であり、**先発オーダーは各試合の最初の
プレイ行のスタメン JSON を正とする**」と訂正しているが、旧には **`apply_lineup_snapshot_from_play`
(交代入力忘れの遡及反映)**があり(`docs/legacy/research/services-shell.md:152`)、**先頭行が指定されていれば
最初のプレイ行の JSON も上書きされている** — 「最初のプレイ行 = 先発オーダー」は無条件には成り立たない。

#### 6-7. サイドカーの境界

「88 列に列を足さず別表へ」は**旧システムが `play_player_link` で実際に採った方式**であり前例がある
(`docs/legacy/research/docs-tests-ops.md:45`)。**ただし `CAND:1514` のサイドカー結合キー
「試合ID + 記録権世代 + イベント連番」に対応する値は旧側に一つも存在しない**(記録権は新概念、旧の行内
キーは列 9 プレイの番号のみで**非一意** — `DL:402`)。**88 列側にはキー列を足せない(P-02)ため、移行試合で
互換出力の本体 CSV とサイドカー CSV を突合する手段が 12-2 節に書かれていない**。旧行 PK(`play_data.id`)も
88 列の外(`DL:58`-`:63`)。

#### 6-8. 旧資料内部の矛盾(判定は保留・6 件)

`DL` と `DB` が食い違う: **テーブル数 12 / 13 と `game_lineup_snapshot` の存在**(◎ 上記)/ 接続プール
(`SimpleConnectionPool(1,3)` vs `ThreadedConnectionPool` min1/max10)/ **列 44 球種の保存値**(コード
"FB" vs **日本語名**)/ 列 86-87 の末尾 4 集計(R/H/E/K 系 vs H/E/K/BB)/ 列 56 球速の合成規則 /
列 42-43・50-51 の保存形式(「ピクセル座標」vs 0〜263 float)。**`DL` を正とする規定の射程内で衝突**して
おり、88 列の意味・値域に直結する。移行仕様タスクへ送る前に、**正本化で「どちらを採るか」を明示するか
「未確定」と書くか**を決める必要がある。

### 7. 実機検証の足回り(◎ 自分で確認)

| 事実 | 典拠 |
| --- | --- |
| 開発 DB は **postgres:17.11-bookworm** 固定・loopback バインド・healthcheck 付き | `docker-compose.yml` |
| docker は稼働(29.1.3)。ただし worktree 側で `docker compose ps` は **`POSTGRES_USER` 未設定で失敗**(`.env` は主ツリー側・Claude は参照禁止 — NFR-014) | 実行確認 2026-08-30 |
| `psql` クライアントは**未インストール** | 実行確認 |
| `backend/` は骨組みのみ(`main.py` + health/package テスト)。**DB ドライバ・SQLAlchemy・Alembic なし** | `backend/pyproject.toml`・`backend/src/pitchlog/` |
| CI の backend ジョブに **postgres サービスが無い**(`uv run pytest --cov` のみ) | CI ワークフロー(backend ジョブ) |

→ **越境テスト 4 件は「書けば動く」状態ではない**。(1) 接続情報の与え方(`.env` を使わずテスト専用の
DSN 環境変数を人間に設定してもらうか、テスト側でコンテナを起こす)(2) ドライバ導入
(3) CI での実行可否(サービスコンテナを足すか、DSN 未設定時 skip か)を計画で決める。
**越境テストを生 SQL(psycopg)で書けば ORM 選定と独立**にでき、DoD の順序依存を解ける。

## 未解決・申し送り

### 人間の裁定が要るもの

1. **導出 5 件**(5-1 節の「残り」欄に縮めた形で)
2. **FR-030 勝敗投手・セーブの手動指定**の持ち場・未指定の表現・変更履歴の要否(要件は Could のみ)
3. **管理者操作ログの Must/Should 矛盾**(`REQ:724`・`:975` と `:734`・`:17`)をどう処置するか
4. **設計書 5.1 追随のゲート区分**(PR レビュー / 版繰り上げ + 7.3)— 正本に明文が無い
5. **要件書 6.1 の表を触るか**(同期側は触って母集合 211→212 になりテストが落ちた前例:
   `docs/worklog/2026-08-28-sync-protocol-canonical.md:283`)。候補案は「改訂は提案しない」と宣言済み
6. **新正本を `check_doc_coverage.py` / `check_design_propagation.py` の対象に加えるか**(規範なし)
7. **`core-areas.json` の paths を本タスクで触るか**(触ると core-guard の人間逐行確認が発火。
   `data-migration` が空である点も含めて)
8. **旧資料内部の矛盾 6 件**(6-8 節)を正本でどう扱うか(採択を明示 / 未確定と書く / 移行仕様タスクへ送る)

### 実測・別タスク待ち

移行のファンアウト(複製倍率・名寄せ閾値・所有テナント不明行の実件数・隔離領域の運用)は**旧 DB の実データ**が
要るため移行仕様タスク。座標の視点(恒等か左右反転か)は**右/左打者別チャート突合**での確認が要る。
索引の具体形・事前集計の採否は実装期の実測後。

### 次の一歩

`/plan` で計画書を作る。**本調査の帰結として、計画は少なくとも次を分離したステップに落とす必要がある**:
① 候補案の移送 + 典拠形式の全件貼り替え ② 同期正本の 11-2 反映(矛盾 15・欠落 20・書き換え 12)
③ 88 列表の是正(16 件)④ 認可機構の実機越境テスト ⑤ 設計書 5.1 追随 ⑥ 確定ゲート(7.3)。
