---
feature: data-model-schema-orm
status: active            # active | in-review(/pr が PR 内で更新。完了は PR 状態・Notion・worktree 除去から導出。codex_run.py implement は active 以外を拒否)
承認: 未                  # 未 | 済(YYYY-MM-DD・承認者)— codex_run.py が「済」でないと実行を拒否する
重さ分類: コア領域          # 軽微 | 通常 | コア領域 | 機械的軽作業(ADR-001 のモデルをラッパーが自動選択)
worktree: ../../..        # worktree ルート(plan.md からの相対 or 絶対)。/task-start が設定
notion: https://app.notion.com/p/3d593b75e68781958074ed04dff7dab7
branch: feature/data-model-schema-orm
created: 2026-09-08
計画レビュー周回: 1        # 指摘反映を伴うレビュー 1 周ごとに +1(収束確認周は数えない。/plan が更新)
確定ゲート周回: 0          # 指摘反映を伴う敵対レビュー 1 周ごとに +1(同前。/finalize-doc が更新)
実行方式: 通常             # 通常 | fast(fast path 適用時に fast へ — 人間の事前 OK 必須。現在地導出が識別)
反映周コミット: 適用       # 適用 | 規約制定前(必須・既定値なし。確定ゲートの反映周コミット突合の適用境界 — 設計書 6.1)
---

# 実装計画書: データモデル設計の正本化(`docs/design/data-model.md` の新設 + 7.3 の確定ゲート)

## 1. 背景・目的

**Notion タスク**: [TSK-342](https://app.notion.com/p/3d593b75e68781958074ed04dff7dab7)(高)
**前タスク**: [TSK-335](https://app.notion.com/p/3d493b75e687817eb966e0b5511ebd31)([PR #49](https://github.com/masaki1025/pitchlog/pull/49) マージ済み `f0eeae1`)
**切り出した後続**: [TSK-343](https://app.notion.com/p/3d593b75e68781469990ca4e61fca9d2)(ORM の導入と `models`・`migration`)
**要件**: `FR-038`(移行)/ `FR-015`(選手登録)/ `FR-030`(勝敗投手の手動指定・Could)/ `NFR-010`(テナント分離)/
`NFR-005`(性能規律)/ `NFR-018`(ドメイン計算の単一実装)/ `4.0-2`(物理削除しない)/ 7.1(技術制約)
**下調べ**: [research.md](research.md)(488 行・全事実に典拠)

### なぜやるか

**データモデルの正本が存在しない。** 候補案 `docs/features/product-data-model-design/design.md`(1,866 行)は
**一時作業域に置かれたまま**で、`docs/design/` には `sql`... ではなく **`sync-protocol.md` の 1 本しかない**(実測)。
同期プロトコル正本が `WAIT-01`〜`WAIT-08` を送り、要件書 6 章がデータ要件を課し、
[TSK-250](https://app.notion.com/p/3c593b75e6878152b3edd6cf4f26b30b)・[TSK-330](https://app.notion.com/p/3d493b75e68781ce8417d9ed882ece5b)・
[TSK-343](https://app.notion.com/p/3d593b75e68781469990ca4e61fca9d2) が**いずれもこの正本を待っている**。

### なぜ独立タスクか / なぜ実装と分けたか

TSK-250 の承認済み計画を表どおり実行すると**後段が前段と同じ成果物を再変更する**構造だった。
**人間の裁定 2026-09-08(`R-1`・`R-2`・`R-5`)**でスキーマ本体・ORM・5.1/10.1 が本タスクへ切り出された。

さらに**本タスクの計画レビュー 1 周目 `P0-1`** で、**TSK-335 が移管した旧ステップ
`11`・`12`・`13`・`15`・`17`・`20` と旧 `14`・`16` の一部(= 欠陥 ID 群 56 件の是正)が
計画から欠落していた**と判定された。移管表を守ると **8 → 15 ステップ(うち 10 が正本の文書執筆)**になる。
→ **人間の裁定(2026-09-08)で文書と実装を 2 本に分けた。**
**本タスク** = 正本の新設 + 欠陥群の是正 + 7.3 の確定ゲート /
**[TSK-343](https://app.notion.com/p/3d593b75e68781469990ca4e61fca9d2)** = 5.1/10.1 の改訂 + `models` + `migration` + 依存追加。

**裁定 `R-2` と矛盾しない** — `R-2` が禁じたのは「`models` と 10.1 改訂を**別タスクにすること**」
(`models` を書くと lock が変わり 10.1 の機械検査に当たるため)であり、**TSK-343 はその 2 つを同じタスクに保っている**。

### 人間の裁定(取得済み — 2026-09-08・山田正輝)

| # | 論点 | 裁定 |
| --- | --- | --- |
| **A-1** | `game_lineup_snapshot`(最終オーダーのスナップショット)の受け皿を作るか | **作る。** 要件書 `FR-038`(approved)が名指しで「別に保全する」と要求。`DL` の precedence は **88 列の意味に限定**でテーブル一覧は射程外。旧側に存在しなくても害はない(移行 0 件で終わる) |
| **A-2** | 「実スキーマ適用後に越境テストを再実行するゲート」は誰が持つか | **定義は本タスク・実行は後。** **越境テスト自体がまだ存在しない**(TSK-270 第 1 群は DB テスト基盤まで。正例・拒否例・DDL 適用器は TSK-317 第 2 群 — 未着手)。**RLS ポリシーとロールの DDL・越境テストの実行は TSK-317 の実機確定後へ送る**(裁定 `R-1`「DDL の実機確定だけ TSK-317 を待つ」と一致) |
| **A-3** | 移行バッチの書き込み経路 | **移行バッチ専用ロールへ `BYPASSRLS` を与える。** 所有者バイパスに頼らないので**全テーブルに `FORCE ROW LEVEL SECURITY` を掛けたまま移行できる**。候補案 3-2 は「所有者がバイパス」と書きながら同節で `FORCE` を推奨しており**内部矛盾していた**(research.md 6-4) |
| **A-4** | `NFR-018` (b)③「動的コード生成の禁止」は ORM の実行時 SQL 生成に及ぶか | **及ばない。** 条文の適用範囲は (b)「宣言と機械判定」= **対象計算の出所・実行入口・派生関係**。**判定の根拠と適用対象の列挙を正本へ書き**(ステップ 10)、**確定ゲートの敵対レビューに掛ける** |

## 2. スコープ

### やること

1. **`docs/design/data-model.md` の新設**(候補案 1〜12 節を素材に移送)+ **引用 286 件を安定 ID 参照へ貼り替え** + **`citation-map-data-model.json`(旧参照 → 新安定 ID の対応表)** + **退行 fixture と SHA-256** + 索引登録
2. **RLS を防御層とする設計判断の明文化**と、そこから導出される**テーブル制約**(テナント内一意 + 複合 FK + 制約違反詳細の一律秘匿)
3. **裁定 A-1・A-3 の反映** — `game_lineup_snapshot` の保全先 / 移行バッチ専用ロールへ `BYPASSRLS`
4. **移管された欠陥 ID 群の是正**(TSK-335 裁定 `R-1` の移管表どおり):
   - **順序系の写像**(`C01`〜`C05`・`C08`)+ `WAIT-05` — 旧 11
   - **削除経路の分離**(`C06`・`M06`)— 旧 12
   - **識別子と一意性**(`M01`〜`M04`・`M07`・`M08`・`M17`)+ `WAIT-03` — 旧 13
   - **`D5` 一意範囲・`D1` 連続性・適用水位・履歴文脈・楽観ロック** + `WAIT-01` — 旧 14 の TSK-342 側
   - **原子境界と結果保持**(`M05`・`M09`・`M10`・`M15`・`M18`・`M20`)+ `WAIT-06`・`WAIT-08` — 旧 15
   - **退避と復元の構造側**(`C07`・`C15`・`C16`・`M11`〜`M14`・`M16`・`M19`)+ `WAIT-02`・`WAIT-04` — 旧 16 の TSK-342 側
   - **キャッシュ無効化の物理化**(`B01`〜`B05`)+ `WAIT-07` — 旧 17
   - **要件由来の写像**(`RQ-01`〜`RQ-09`・`SP-01`〜`SP-06`)+ FR-030・管理者操作ログ — 旧 20
5. **`NFR-018` の境界の明文化** — (b)③ の判定(裁定 A-4)と (a) の発火点((β) 集計クエリ)
6. **越境テスト再実行ゲートの定義**(裁定 A-2)+ **non-serving 宣言と DB 利用機能のマージ停止条件** + **送り先の起票**
7. **`.claude/core-areas.json` へ新設正本を登録**(6.3 規則⑤)
8. **7.3 の確定ゲート通過**(`/finalize-doc` — 正本の新設)

### やらないこと

| 対象外 | 受け取り先 | 理由 |
| --- | --- | --- |
| **設計書 5.1・10.1 の改訂 / `models` / `migration` / 依存追加 / `test_psycopg_...` の更新** | **[TSK-343](https://app.notion.com/p/3d593b75e68781469990ca4e61fca9d2)** | **人間の裁定 2026-09-08**(文書と実装を 2 本に分ける)。開始条件 = **本タスクの完了**(正本が `approved`)。裁定 `R-2` は保たれる(TSK-343 が 10.1 と `models` を同じタスクに持つ) |
| **RLS ポリシー・ロールの DDL の実機確定**(`BYPASSRLS` / `NOLOGIN` / 関数 ACL / `search_path` / `FORCE RLS` の組合せ) | **[TSK-317](https://app.notion.com/p/3d193b75e687815b83a1faed4848dba2)** | 裁定 `R-1`「DDL の実機確定だけ TSK-317 を待つ」。本タスクは**設計判断**を書く |
| **越境テストの実行**(実スキーマに対する再実行) | **本タスクが起票する送り先**(裁定 A-2) | **越境テスト自体が未作成**(TSK-317 第 2 群) |
| **88 列写像の値**(行き先・変換・逆変換可能性)/ **フィールド別センチネル変換** | **TSK-250** | 2026-09-07 の PO 裁定と `R-9`。本タスクは**受け皿(物理実体名と型)を用意する側** |
| **記録権世代の生成アルゴリズム・物理形式** | **論点 17 の受け取り先**(実装計画 = 同期・記録権の最初の実装タスク) | 裁定 `R-6` の除外 |
| **`WAIT-04` の保持期間そのもの** | **要件改訂タスク** | **要件書に行が無い**(同期正本 11-4 U-3)。9-4 も「保持期間そのものは決めない」と明記。**構造側だけを本タスクが持つ** |
| **`waiting-data-model.json` の `resolved` 記録** / **`data-model` プロファイル・レジストリ entry** | **TSK-250** | 資産の新設と文書検査機構への登録は TSK-250 の射程。→ **本タスクは `check_citation_format` を単発で呼んで自前で検査する**(4 節 検査 ①) |
| **要件書の改訂** | 要件改訂タスク | AGENTS.md 絶対規則 4。**本タスクで踏む要件は無い**(research.md 3 節で `FR-015` の衝突が成立しないことを確認) |
| **候補案 13 節・未解決メモの移送** | — | TSK-250 の `DM-MT-01` が **`absent-section: "13"`** で**正本に 13 節が無いこと**を検査する |
| **索引の具体形・事前集計の採否** | 実装期の性能実測 | `NFR-005` は「毎球参照画面に限り採用可」とし具体形を定めない |

## 3. 影響する正本

| 正本 | 変更内容 | ゲート(PRレビュー / finalize-doc) |
| --- | --- | --- |
| [`docs/design/data-model.md`](../../design/data-model.md) | **新設**(`0.1` → `in-review` → `approved`)。候補案 1〜12 節を素材に移送し、**引用を安定 ID 参照へ貼り替え**、RLS の設計判断・テーブル制約・裁定 A-1/A-3 の反映・**欠陥 ID 群 56 件の是正**・`WAIT-*` の物理表現・`NFR-018` の境界・越境テスト再実行ゲートの定義を持たせる | **finalize-doc**(7.3 — **正本の新設**) |
| [`docs/README.md`](../../README.md)(索引) | **設計正本の行を新設**(`draft` → `approved`) | —(常に現行化 — 7.2) |
| [`docs/development/dev-harness-design-2026-08-07.md`](../../development/dev-harness-design-2026-08-07.md) / `docs/requirements/**` / `docs/adr/**` / `docs/ops/**` / `contracts/**` / `backend/**` / `frontend/**` | **反映なし**(**5.1・10.1 の改訂は TSK-343**) | — |

### 正本体系外だが同一 PR で運ぶもの(/pr 突合の別枠宣言)

| ファイル | 変更内容 |
| --- | --- |
| `scripts/design_relations/citation-map-data-model.json`(**新設**) | **旧参照 → 新安定 ID の対応表**(**286 件の exact-set**・置換前後の規範の同一性判定を行ごとに記録) |
| `tests/fixtures/data-model-source.txt`(**新設**) | **退行 fixture**(本文のバイト単位のコピー。**拡張子は `.txt`**) |
| `scripts/design_relations/fixture-sha256-data-model.txt`(**新設**) | **ソース fixture 専用の SHA-256**(欠陥台帳用の digest とは**別資産**) |
| `.claude/core-areas.json` / `tests/test_core_guard.py` | **新設正本を paths へ登録**(6.3 規則⑤) |
| `docs/features/data-model-schema-orm/{plan,research}.md` / `docs/worklog/2026-09-08-data-model-schema-orm.md` | feature 作業ディレクトリ(記録・正本ではない) |

## 4. 実装方針

### 重さ分類の根拠 — **コア領域(テナント分離・データ移行・記録権)**

設計書 6.3 の境界定義表がテナント分離に「認可境界・テナント列・越境経路」、データ移行に
「`FR-038` の全体」、記録権に「世代の永続化」を含める。**本タスクはスキーマ本体そのもの**であり、
意味範囲上 3 領域に該当する。→ **`sol xhigh`・敵対レビュー必須・人間の逐行確認必須**。

### 機構の発火判定 — **`guard_paths` のみ該当**(領域 `paths` は非該当)

| ファイル | 該当 |
| --- | --- |
| `.claude/core-areas.json` / `tests/test_core_guard.py` | **`guard_paths`** に登録済み |
| **`docs/design/data-model.md`**(新設)/ `docs/README.md` / `scripts/design_relations/citation-map-data-model.json`(新設)/ `tests/fixtures/data-model-source.txt`(新設) | **どの領域の `paths` にも無い** |

→ **PR テンプレは「逐行確認チェックのみ」を有効化する**(`guard_paths` 該当。CI の core-guard が
`- [x]` を機械検査する)。**adversarial レビュー項目はテンプレ上は不要だが、本タスクはコア領域なので
7.3 の確定ゲートで敵対レビューを通す。**
→ **新設正本は 6.3 規則⑤で `paths` へ登録する**(ステップ 11。**登録しないと台帳 `H-12` の再発**)。

**TSK-250 への申し送り**: T250P の新ステップ 17 は「既存 4 領域へ本正本を追加」を持つ。
**`docs/design/data-model.md` の登録は本タスクが行う**ので、**T250P 側は重複しないよう再承認時に調整する**。

### 設計判断 1 — **RLS を防御層として採る**(要件からは導出されない)

**要件書・改善台帳に `RLS` は 0 hits**(research.md 2-1 で独立に再確認)。
`NFR-010` が課すのは**結果**(内容・存在を含め参照できない)・**適用範囲**(初日から全機能)・
**検証**(自動テスト)だけで、**実現層を指定していない**。改善台帳 `I-2` が指名する手段は `team_id` 列。
→ **これは本タスクが決める設計判断である。**

**採る理由**:

1. **7.1「標準 PostgreSQL の範囲で使用し、ホスティング固有機能に依存しない」** — RLS は PostgreSQL 9.5 の
   コア機能なので**採ってよい**。**Supabase の認可機能に寄せる案は禁止事項 `P-04` 違反**で採れない
2. **`NFR-010`「テナント分離は初日から全機能に適用する」** — アプリ層のみの強制は、
   管理画面・バッチ・将来のリポジトリ層・手書き SQL で条件を落とすと隔離を失う。
   **旧システムがまさにその状態だった** — テナント境界は `game.owner_team_id` 1 列 + アプリ層ガードのみで、
   **所有権チェックはオプショナル引数で省略すると全データにアクセス可能**だった(research.md 5-2)
3. **`NFR-019(b)` の越境テストが「API 直叩きを含む全経路」を要求**する — DB 側に既定拒否があれば
   経路の追加漏れが構造的に塞がる

**この判断の帰結(避けられない)**: **一意制約と外部キーは RLS を必ず貫通する**
(PostgreSQL 公式 `ddl-rowsecurity.html`)。`NFR-010` が「内容・存在を含め参照できない」を要求するため、
**素直に設計すると制約違反エラーが他テナントの行の存在を漏らす**。
→ **テナント内一意 + 複合 FK + 制約違反詳細の一律秘匿**が従属して決まる(候補案 3-4)。
**要件単独からは導かれない**(research.md 2-5)。

### 設計判断 2 — **`NFR-018` (b)③ は ORM の実行時 SQL 生成に及ばない**(裁定 A-4)

**条文の適用範囲**: `NFR-018` (b) は「**宣言と機械判定**」であり、(b)③ の「動的機構の禁止」は
**対象計算の出所・実行入口・派生関係の宣言**に対する検査である。
**`NFR-018` の対象は本項の列挙が正**(達成条件 (d))= 「状況判定・座標変換・捕球選手推定・
成績集計の前処理・終了判定」で、**`models` と `migration` はどれにも当たらない**(research.md 4-3)。
→ **及ばないと判定する。要件書に適用範囲の明示が無い点は「不明」として記録し、確定ゲートの敵対レビューに掛ける。**

**ただし発火点が 1 つ残る**: **(β) の集計クエリを ORM で人手記述する段で `NFR-018` (a)
「人手で保守する実装は 1 系統」が発火する**。→ **本タスクと TSK-343 の射程外**。
**この境界を `docs/design/data-model.md` に明記する**(ステップ 10)。

### 設計判断 3 — **本 migration は non-serving**(裁定 A-2 の帰結・1 周目 `P1-3` の反映)

TSK-343 が作る `migration` は **RLS ポリシー / ロール DDL を含まない**ため、
**適用しただけでは DB レベルのテナント分離が効かない**。
`NFR-010`「初日から全機能に適用」は**製品の要件**であり、**RLS 未適用のスキーマが develop に入ることを
禁じる条項は存在しない**(research.md 2-3・TSK-335 の調査でも同じ判定)。
しかし **TSK-330 が本タスクの完了直後に開始できる**ため、**止める条件が無いと穴になる**。

→ **正本へ次を書く**(ステップ 12 の合格条件):

1. **本 `migration` は non-serving である**(利用経路を開かない)
2. **RLS ポリシー / ロール DDL の適用と、実スキーマに対する越境テストが green になるまで、
   TSK-330 を含む DB 利用機能をマージまたは有効化しない**
3. **暫定的にアプリ層分離で先行する場合も、同じ越境テストの green を条件とする**

### 検査(**機械 3 本に絞る** — 前タスクの教訓)

**前タスク(TSK-335)は計画レビュー 3 周・成果物レビュー 3 周でいずれも収束せず、
指摘の多くが「自分で書いた bash 検査の欠陥」だった**(台帳の候補へ実測を追記済み)。
→ **機械検査は「既存機構が検査主体を持つ」か「負例で red になることを本計画の中で実測済み」のものだけ。**
**すべての検査に「是正前の実測値」を併記する。**

```bash
# 本ブランチの worktree で実行する
D=docs/design/data-model.md
C=docs/features/product-data-model-design/design.md

# ---- ① 引用形式(可変正本への行番号引用が 0 件)----
#    ※ check_design_propagation.py は**レジストリ駆動**で、現在の登録は sync-protocol 1 本だけ
#      (実測)。`data-model` プロファイルの作成は TSK-250 の射程なので、**本タスクは
#      check_citation_format を単発で呼んで自前で検査する**(登録待ちにすると腐ったまま通る)
#    ※ **これは「禁止書式の不在」しか見ない**(1 周目 `P0-2`)。本文削除や誤った貼り替えでも
#      通るので、**`citation-map-data-model.json` の 286 件 exact-set と fixture のバイト一致を
#      同じステップの合格条件に置く**
uv run python -c "
import importlib.util, sys, pathlib
spec = importlib.util.spec_from_file_location('cdp','scripts/check_design_propagation.py')
m = importlib.util.module_from_spec(spec); sys.modules['cdp']=m; spec.loader.exec_module(m)
print('正本  :', m.check_citation_format(pathlib.Path('$D').read_text(encoding='utf-8')))
print('候補案:', m.check_citation_format(pathlib.Path('$C').read_text(encoding='utf-8')))
"
# → 正本 = ()  /  候補案 = ('REQ:<行番号>', '<パス>.md:<行番号>', '裸の行番号')
#   **是正前の実測**: 候補案に 3 形式すべてが検出される(= 負例が現物として存在する)
#   **`$D` が未作成の間は FileNotFoundError で fail-closed**(1 周目 `P2-10` の確認)

# ---- ② 文書検査(既存機構)----
uv run python scripts/check_docs_status.py                # → rc=0
uv run python scripts/check_design_propagation.py         # → rc=0(レジストリ列挙)
uv run python scripts/check_doc_coverage.py               # → rc=0
#   **是正前の実測**: 3 本とも rc=0(= 番人型)
#   ※ **索引漏れを検出するのは ① ではなく ② の check_docs_status.py**(1 周目 `P2-10` の是正)

# ---- ③ 品質ゲート(ステップ 11)----
uv run pytest tests/test_core_guard.py -q                 # → green
#   **是正前の実測**: green(= 番人型。新設資産を登録し忘れても現状は落ちない → 負例で確認する)
```

**`[手動・外部]` に置くもの**(機械判定しない): 本文の意味の正しさ / 候補案からの移送の網羅 /
**欠陥 ID 群 56 件の是正が原典と整合するか** / `WAIT-*` と同期正本の 1 対 1 照合 /
RLS 採用の設計判断の妥当性 / 制約の形が `NFR-010` を満たすか / **貼り替えた 286 件が指す条項の正しさ**。
**いずれも確定ゲートの敵対レビューと人間の逐行確認で見る。**

### 実装ステップ(コミット単位 — 設計書 6.1 段階実装)

**ステップ 1〜10・12 は文書(コード変更 0 行)なので Claude が直接編集する**
(CLAUDE.md「Git 操作・PR・ドキュメントは Claude の役割」)。
**ステップ 11 は JSON 設定とテストを変更するので Codex へ委任する**(設計書 6.3 — 1 周目 `P1-9` の反映)。

| # | ステップ(何を作るか) | 実行者 | 合格条件(このステップの検証方法) |
| --- | --- | --- | --- |
| 1 | **正本の新設・素材移送・引用の貼り替え・3 資産の固定**(**同一コミット** — 分けると腐ったまま通る)— `docs/design/data-model.md` を新設し候補案 **1〜12 節**を移送(**13 節と未解決メモは移送しない**)+ frontmatter 3 行 + 変更履歴表 + **`REQ:<行>` 引用 286 件を安定 ID 参照へ貼り替え** + **`citation-map-data-model.json`**(旧参照 → 新安定 ID・**置換前後の規範の同一性判定を行ごとに記録**)+ **`tests/fixtures/data-model-source.txt`**(退行 fixture)+ **`scripts/design_relations/fixture-sha256-data-model.txt`** + `docs/README.md` へ行を新設 | Claude | `[機械]` **検査 ① の正本側が `()`**(**是正前 = 候補案に 3 形式検出**)/ **`## 13.` で始まる節が 0 件** / **対応表の件数が 286 でありキー集合が本文の旧参照集合と exact-set 一致** / **fixture が本文とバイト一致** / **SHA-256 が fixture の実値と一致** / 検査 ② が 3 本とも rc=0。`[手動・外部]` **候補案 1〜12 節の全節が移送されている**(節見出しの集合を突合)/ **貼り替えた 286 件が指す条項が正しい**(判定者: 山田正輝) |
| 2 | **RLS の設計判断とテーブル制約** — 4 節「設計判断 1」を本文へ明文化(**理由 3 点 + 却下案 3 件**)/ **テナント内一意 + 複合 FK + 制約違反詳細の一律秘匿**を規則として置く | Claude | `[機械]` **`tenant_id` を先頭に含まない業務的一意制約が 0 件**(**グローバル一意の記述が 0 件**)/ 検査 ①② green。`[手動・外部]` **RLS 採用の理由 3 点と却下案 3 件が本文にある** / **制約の形が `NFR-010` を満たす**(判定者: 山田正輝) |
| 3 | **裁定 A-1・A-3 の反映** — **`game_lineup_snapshot` の保全先**(A-1)/ **移行バッチ専用ロールへ `BYPASSRLS`**(A-3)。**候補案 3-2 の「テーブル所有者がバイパス」の記述を除去**し、**全テーブルに `FORCE ROW LEVEL SECURITY` を掛けたまま移行できることを明記** | Claude | `[機械]` **「テーブル所有者」+「バイパス」が同一文に現れる箇所が 0 件**(**是正前 = 候補案に 1 件**)/ **`FORCE ROW LEVEL SECURITY` と移行経路の両立が本文にある** / **`game_lineup_snapshot` の保全先が本文にある**(是正前 0 件)/ 検査 ①② green |
| 4 | **順序系の写像**(`C01`〜`C05`・`C08`)+ **`WAIT-05`**(U-7 の D1/D2 用途分離)— 用途対応表・6-2 相当節・**移行の節**の**4 箇所すべて**で境界・起点を **D2** へ改め、**「到着順」の語を全廃** | Claude | `[機械]` **`C01`〜`C05`・`C08`・`WAIT-05` の 7 個の ID が本文にある**(是正前 0 件)/ **「到着順」が 0 件** / 検査 ①② green。`[手動・外部]` **4 箇所すべてで D2 へ改まっている**(同期正本 11-4 U-7 と逐行照合し結果を worklog へ) |
| 5 | **削除経路の分離**(`C06`・`M06`)— 確定済み記録の修正・削除を**変更イベント(参加区分 #10〜#12)**として実体化し、**墓標・改訂の流用記述を撤去** | Claude | `[機械]` **`C06`・`M06` が本文にある** / **「正は墓標・改訂イベント」が 0 件** / 検査 ①② green。`[手動・外部]` **#10〜#12 に対応する実体・属性・制約がある** |
| 6 | **識別子と一意性**(`M01`〜`M04`・`M07`・`M08`・`M17`)+ **`WAIT-03`**(D2 の数値型)— D2 の列・部分一意・**索引の禁止**、V10 の複合参照列、V11 と確定版列を物理写像として置く | Claude | `[機械]` **7 個の `M*` と `WAIT-03` が本文にある** / **`(試合, D4, D1)` の部分一意がある** / **D2 に不変前提の索引を張らない旨が索引原則にある** / 検査 ①② green。`[手動・外部]` **`WAIT-03` の数値型と根拠が本文にある** |
| 7 | **一意範囲・連続性・世代の TSK-342 側** + **適用水位・履歴文脈・楽観ロック** + **`WAIT-01`**(D3 の物理表現)— **`D4` の永続化写像は書かない**(裁定 `R-6` — TSK-250 の所有) | Claude | `[機械]` **`WAIT-01` が本文にある** / **`D5` の一意範囲が `(テナント, D5)`** / **`D1` の連続性が新規割り当てに限られる** / **`D4` の非再利用・世代の物理化の記述が 0 件**(TSK-250 の所有)/ 検査 ①② green |
| 8 | **原子境界と結果保持**(`M05`・`M09`・`M10`・`M15`・`M18`・`M20`)+ **`WAIT-06`**(U-8 構造側)・**`WAIT-08`**(9-2 境界表 17 行の永続化写像) | Claude | `[機械]` **6 個の `M*` と `WAIT-06`・`WAIT-08` が本文にある** / **9-2 境界表の 17 行すべてに物理写像がある**(exact-set)/ **拒否原本が受理済み行と混ざらない旨がある** / 検査 ①② green。`[手動・外部]` **`P1`〜`P5` と `T1`〜`T9` の対応表がある** / **クラッシュ注入点が定義できる配置**になっている |
| 9 | **退避と復元の構造側**(`C07`・`C15`・`C16`・`M11`〜`M14`・`M16`・`M19`)+ **`WAIT-02`**(退避の列定義)・**`WAIT-04`**(U-3 構造側)— **退避原本・端末キュー状態 4 種・保持期間の器**を置く。**`C16`: 管理関数クラスから「取り込み」を分離**し、現版で許可する「閲覧・書き出し」だけを残す。**復旧世代・`RG1`・`I6` の永続化写像は書かない**(裁定 `R-6`)。**`WAIT-04` は「構造側のみ・保持期間は要件改訂待ち」と理由つきで明記** | Claude | `[機械]` **11 個の ID(`C07`・`C15`・`C16`・`M11`〜`M14`・`M16`・`M19`・`WAIT-02`・`WAIT-04`)が本文にある** / **管理関数の記述に「取り込み」が残っていない** / **「復旧世代」「`RG1`」「`I6`」の永続化写像の記述が 0 件** / 検査 ①② green。`[手動・外部]` **`WAIT-04` の理由が要件改訂待ちであることと整合** |
| 10 | **キャッシュ無効化の物理化**(`B01`〜`B05`)+ **`WAIT-07`** + **要件由来の写像**(`RQ-01`〜`RQ-09`・`SP-01`〜`SP-06`)+ **FR-030・管理者操作ログ** + **`NFR-018` の境界の明文化**(裁定 A-4 の判定・理由・適用対象の列挙と、(a) の発火点) — 無効化は**同期側のトリガー ID を参照**し本書は**物理的な無効化先だけ**を決める | Claude | `[機械]` **`B01`〜`B05`・`WAIT-07`・`RQ-01`〜`RQ-09`・`SP-01`〜`SP-06` の 26 個の ID が本文にある**(是正前 0 件)/ **要件の 14 トリガーが全数対応づいている**(**招待の失効を含めない**)/ **発火条件の再記述が 0 件** / **`NFR-018` (b)③ の判定と (a) の発火点の両方が本文にある** / 検査 ①② green。`[手動・外部]` **`RQ-*` 各件に裁定 ID・決定者・決定日・反映節がある** |
| 11 | **`core-areas.json` へ新設正本を登録**(6.3 規則⑤)— `docs/design/data-model.md` と本タスクの新設資産(`citation-map-data-model.json` / 退行 fixture / SHA)を**完全列挙**で該当領域へ / **実設定を読む回帰テスト** | **Codex** | `[機械]` **検査 ③ が green** / **新設資産の全パスが該当領域の `paths` に包含されている**(**`guard_paths` は完全一致集合でディレクトリ登録では発火しない** — `scripts/core_guard.py`)/ **負例: 1 パスを外すと red** / `/check` 3 層 green。`[手動・外部]` **paths 追加の敵対レビュー + 人間承認**(6.3 規則⑤・逐行確認は PR 作成者以外) |
| 12 | **越境テスト再実行ゲートの定義と送り先の起票**(裁定 A-2)+ **non-serving 宣言とマージ停止条件**(4 節「設計判断 3」)— 「**誰が・何に対して・いつ回すか**」を本文へ書く。**RLS ポリシー / ロール DDL の実装 + 越境テストの実行**を Notion へ起票し**開始条件を TSK-317 の完了**とする。**起票の前に必ず Notion をタスク名で検索する** | Claude | `[機械]` **本文にゲートの定義がある** / **non-serving 宣言と「越境テストが green になるまで TSK-330 を含む DB 利用機能をマージまたは有効化しない」が本文にある**(是正前 0 件)/ **本書と本文に起票した実 ID(URL 付き)がある**。`[手動・外部]` **重複起票していないこと**(検索結果を worklog へ)/ **ゲートの定義が「実スキーマに対して」「TSK-317 の資産で」回すことを一意に指している** |

**ステップ外ゲート(DoD で担保)**: **7.3 の確定ゲート通過**(下記)/ PR 本文の**必須チェック行**
(`guard_paths` 該当で core-guard が自動付与・機械検査する)と**実施記録行の記入** /
Notion の DoD 現行化と PR URL 記録。

### 確定ゲート(ステップ 12 完了後 — 表外手順)

1. `plan.md` の `status` を `in-review` へ / 索引と `docs/design/data-model.md` の frontmatter を `in-review` へ
2. **開始条件(機械検査)**: 検査 ①②③ がすべて期待値どおり
3. `codex_run.py review adversarial` で**本 PR の全差分を一括検証**
4. 指摘を採用/不採用に分類して反映。**反映周ごとに 1 コミット**(件名に `反映<r>周目`。**ステップ記法を付けない**)。**`確定ゲート周回` を +1**
5. **反映周コミットは文書の変更に限る** — コードの修正が要るなら `status` を `active` へ戻し、**承認済み実装ステップを追加してから**直す
6. **人間の逐行確認**(コア領域)— **RLS の設計判断 / テーブル制約の形 / 欠陥 ID 群 56 件の是正 / `WAIT-*` の 8 件 / 貼り替えた 286 件 / non-serving 宣言とマージ停止条件**を対象に含む。確認者・日付・対象 commit・結果・範囲を worklog へ
7. 逐行確認で欠陥が出たら手順 3 へ戻る
8. **停止条件**: **2 周連続で「直近の是正が原因の P0/P1」が増えたら人間へ 3 案を提示して打ち切る**(前タスクの実測 — 台帳の候補)
9. **人間承認** → `approved`・版数確定・変更履歴追記・索引を `approved` へ
10. approved 化コミットに対して 4 節の検査を再実行し、結果と最終 SHA を worklog へ記録する

## 5. DoD(受け入れ基準)

- [ ] **`docs/design/data-model.md` を新設し、7.3 の確定ゲートを通して `approved` にした**
- [ ] **候補案 1〜12 節を移送した**(**13 節と未解決メモは移送していない**)
- [ ] **`REQ:<行>` 引用 286 件を安定 ID 参照へ貼り替えた** — **`check_citation_format` が `()`**
- [ ] **`citation-map-data-model.json` が 286 件の exact-set を覆い、置換前後の規範の同一性判定を行ごとに持つ**
- [ ] **退行 fixture が本文とバイト一致し、SHA-256 が一致する**
- [ ] **RLS を防御層とする設計判断を明文化した**(理由 3 点 + 却下案 3 件)。**要件から導出されないことを明記した**
- [ ] **テナント内一意 + 複合 FK + 制約違反詳細の一律秘匿**を規則として置いた(**グローバル一意が 0 件**)
- [ ] **`game_lineup_snapshot` の保全先がある**(裁定 A-1)
- [ ] **移行バッチ専用ロールへ `BYPASSRLS` を与える形にした**(裁定 A-3)。**「テーブル所有者がバイパス」が 0 件**で、**`FORCE ROW LEVEL SECURITY` と両立している**
- [ ] **移管された欠陥 ID 群を全件是正した** — `C01`〜`C08`・`C15`・`C16`(**`D4` と復旧世代を除く**)/ `M01`〜`M20`(**同**)/ `B01`〜`B05` / `RQ-01`〜`RQ-09` / `SP-01`〜`SP-06`。**各 ID に反映節がある**
- [ ] **`WAIT-01`〜`WAIT-08` の 8 件の物理表現がある**(**`WAIT-04` は構造側のみ・保持期間は要件改訂待ちと明記**)
- [ ] **記録権世代の生成アルゴリズム・物理形式を書いていない** / **`D4`・復旧世代・`RG1`・`I6` の永続化写像を書いていない**(裁定 `R-6` — TSK-250 の所有)
- [ ] **`NFR-018` (b)③ の判定(裁定 A-4)と (a) の発火点((β) 集計クエリ)の境界を本文に書いた**
- [ ] **越境テスト再実行ゲートを定義し、送り先を起票した**(裁定 A-2。開始条件 = TSK-317 の完了)
- [ ] **non-serving 宣言と「越境テストが green になるまで TSK-330 を含む DB 利用機能をマージまたは有効化しない」を本文に書いた**
- [ ] **`core-areas.json` へ新設正本を登録した**(6.3 規則⑤の敵対レビュー + 人間承認)
- [ ] **コア領域として敵対レビュー + 人間の逐行確認を通っている**。**逐行確認は PR 作成者以外**が行い、**実施記録行を確認者本人が記入**した
- [ ] **`/check` が 3 層で green**(backend の DB 必須テスト 3 件は環境要因の既知事象)
- [ ] **[TSK-343](https://app.notion.com/p/3d593b75e68781469990ca4e61fca9d2) を起票し、開始条件を本タスクの完了とした**

## 6. テスト計画(NFR-019)

| 種別 | 追加内容 |
| --- | --- |
| **単体 / 一致性 / 越境 / E2E / 故障系** | **追加なし。** 本タスクは**コード変更 0 行の文書タスク**であり、`backend/` `frontend/` `scripts/**.py` を 1 ファイルも触らない(**唯一の例外はステップ 11 の `tests/test_core_guard.py`** — 下記) |
| **回帰(実設定)** | `tests/test_core_guard.py` — **新設正本と新設資産の全パスが該当領域の `paths` に包含されていること**。**負例: 1 パスを外すと red** |
| **既存 green の維持確認** | `/check` を実行し、**harness / backend / frontend の各層が着手前と同じ結果**であることを確認する。**backend の DB 必須テスト 3 件は `PITCHLOG_TEST_ADMIN_DSN` / `PITCHLOG_TEST_ROLE_DSN` 未設定の環境要因で fail-closed に落ちる**既知事象 |
| **文書検査** | 4 節 検査 ①(自前呼び出し)/ ②(既存機構 3 本)/ ③(`test_core_guard.py`) |

**越境テストは TSK-317 の第 2 群が作る**(裁定 A-2 — 本タスクは**ゲートの定義**まで)。
**models・migration のテストは [TSK-343](https://app.notion.com/p/3d593b75e68781469990ca4e61fca9d2)**。
一致性ベクタは TSK-235〜239。E2E・故障系は Phase 4。

## 検証(このタスクが終わったことの確認方法)

```bash
WT=../pitchlog-worktrees/feature-data-model-schema-orm

# 1. 4 節の検査 ①②③ をすべて実行する(上記のコマンド集)

# 2. 正本の状態(索引と frontmatter が approved で一致)
uv run python scripts/check_docs_status.py

# 3. 品質ゲート一括(/check 相当 — 既存 green の維持確認)
#    harness / backend / frontend の 3 層

# 4. 正本反映の突合(3 節の宣言と差分の双方向突合)
uv run python scripts/check_plan_docs_sync.py \
  --plan docs/features/data-model-schema-orm/plan.md --base origin/develop

# 5. core-guard が新設資産にマッチすること
uv run pytest tests/test_core_guard.py
```

**人間が確認すること**: 貼り替えた 286 件 / RLS の設計判断 / テーブル制約の形 /
**欠陥 ID 群 56 件の是正** / `WAIT-*` の 8 件と同期正本の 1 対 1 / non-serving 宣言とマージ停止条件。

## 進め方

1. **人間の裁定 4 件は取得済み**(2026-09-08・山田正輝)— 結果は 1 節の `A-1`〜`A-4` に記録した
2. **[TSK-343](https://app.notion.com/p/3d593b75e68781469990ca4e61fca9d2) は起票済み**(2026-09-08。文書と実装を分ける裁定の反映)
3. **Notion TSK-342 の DoD を本書 5 節と逐項照合する** — 照合結果を worklog へ **3 列表**で記録し、**更新後の URL を併記**する
4. **コア領域なので敵対レビュー**: `python .claude/scripts/codex_run.py review adversarial -`
5. 指摘を反映(1 周ごとに `計画レビュー周回` を +1)→ 収束したら**人間の承認**を求める
6. **承認後**: ステップ 1〜10・12 は **Claude が直接編集**(文書)/ **ステップ 11 は `/implement` で Codex へ委任**
   (件名は `(ステップ k/12)`)
7. **ステップ 12 完了後に 7.3 の確定ゲート**(表外手順)
