---
feature: harness-design-review
status: active            # active | in-review(/pr が PR 内で更新。完了は PR 状態・Notion・worktree 除去から導出。codex_run.py implement は active 以外を拒否)
承認: 済(2026-08-16・徳光 尋弥)  # 未 | 済(YYYY-MM-DD・承認者)— codex_run.py が「済」でないと実行を拒否する
重さ分類: 通常            # 軽微 | 通常 | コア領域 | 機械的軽作業(ADR-001 のモデルをラッパーが自動選択)
worktree: ../../..        # worktree ルート(plan.md からの相対 or 絶対)。/task-start が設定
notion: https://app.notion.com/p/3bd93b75e6878194a949d648160fe49a
branch: feature/harness-design-review
created: 2026-08-16
計画レビュー周回: 1        # 指摘反映を伴うレビュー 1 周ごとに +1(収束確認周は数えない。/plan が更新)
確定ゲート周回: 0          # 指摘反映を伴う敵対レビュー 1 周ごとに +1(同前。/finalize-doc が更新)
実行方式: 通常             # 通常 | fast(fast path 適用時に fast へ — 人間の事前 OK 必須。現在地導出が識別)
反映周コミット: 適用       # 適用 | 規約制定前(必須・既定値なし。確定ゲートの反映周コミット突合の適用境界 — 設計書 6.1)
---

# 実装計画書: ハーネス設計レビュー是正 第1弾(追随修正+調査記録の保存)

## 1. 背景・目的

/investigate(エージェントチーム討論モード初試行)で開発ハーネス設計書をレビューし P1×7 / P2×18 を検出した([research.md](research.md))。着手時に調査ベースライン(fcbd1de)の陳腐化が判明したため生存突合を実施し、現況(HEAD 2f220ea)は **既決 7 / 生存 14 / 変質して生存 1 / 部分既決 3**(research.md「追補: 生存突合」が現況の正)。

本タスクは第 1 弾として、(a) 調査記録の保存(履歴としてコミット)、(b) 「実装追随の節更新」で処理できる生存指摘の是正、(c) 構造的な生存指摘のハーネス運用評価台帳への起票、に限定する。版繰り上げ(確定ゲート)を要する改訂とコード変更は第 2 弾以降。

- Notion タスク: https://app.notion.com/p/3bd93b75e6878194a949d648160fe49a
- 関連要件(生存指摘が担保に関わるもの): NFR-018・NFR-019(P1-5 付録A 契約化・P2-1 CI ジョブ表)/ FR-038・NFR-009(P1-6 実データ統制)/ NFR-010・FR-041(P1-4 テナント分離境界)。本タスク自体は開発プロセス文書の是正であり、製品要件は変更しない。

## 2. スコープ

### やること

- **調査記録の保存**: research.md(追補込み)・調査 worklog(2026-08-16-harness-design-review-team-investigate.md)・タスク worklog をコミット。あわせて 6.4 ベースライン worklog へ完了追記 1 行(P2-18 — マージ実施は git 実機確認済み: a4dc517 = f06e2f2 + cbe1371)
- **設計書の節更新(版は上げない)**:
  - ① 6.3 に保護未適用の縮退注記を追記(P1-2)。**文言は「リモート保護は未適用(10.2 のリスク受容)— 保護未適用の間は github-setup.md 2 章の管理手続で補償」に限定**する(paths 未定義時の判定者・新しい代替手続・FR-041 を含む境界には言及しない — それらは H-12/新規 H 項目の未決事項であり本タスクで解かない)
  - ② 13 章 Phase 1 完了条件セルの固定テスト件数を除去し、8.3 の既定方針どおり「CI harness ジョブの実行結果を正とする」へ置換(P2-12)。**変更履歴表 v0.17 行の「103件」は当時の事実として保持する**(歴史記録の全文置換はしない)
  - ③ 8.5 legacy-analyst 典拠欄へ `docs/legacy/baseball-scoring-db-structure.md` を**「補助的な物理構造の調査起点」として追加するのみ**(P1-7 残余)。**優先規定は新設しない** — 88 列の意味の正は data-layer.md のまま、アーカイブ間の矛盾(テーブル数・CASCADE・接続方式)は「不明」と報告する既存規律(出典のない回答は受け取らない)に従う。優先規定の新設は未解決論点 4(要 /finalize-doc)として第 2 弾以降
  - 各変更を変更履歴表へ追記(版は上げない)
- **`.claude/agents/legacy-analyst.md` の典拠列挙を 8.5 と同期**(P1-7 残余 — 同じ典拠集合の同期なので 8.5 と同一ステップ)
- **評価台帳への起票(版は上げない)**: H-12 の対応案補強(P1-3)+ **新規 H 項目 4 件**(P1-4 境界 / P1-5 付録A / P1-6 実データ / P2-1 CI ジョブ表 — 分類・優先度は 4 節の固定表)+ **候補 3 件**(N-1 鮮度 / P2-9 付録E 完成手順 / P2-10 リプレイ整合性 — 各 3 要素〔事象・留める理由・昇格条件〕)+ 変更履歴表追記

### やらないこと(各項目の行き先を明記 — 未追跡にしない)

- コア領域境界(6.3 領域定義)の**改訂** — 版繰り上げ+確定ゲート案件。**行き先: 新規 H 項目(P1-4)として台帳で追跡 + 未解決論点 3 の PO 判断**
- 未解決論点 1(NFR-019 逸脱の要件正本転記)・4(アーカイブ precedence の射程拡張)の是正 — 要件書改訂案件。**行き先: research.md 追補「未解決論点の現況」で追跡・第 2 弾で PO 判断**
- **P2-13(codex_run fast path 判定)・P2-14(notion-map 対 11.1)** — コード・設定変更を伴う。**行き先: 第 2 弾へ別タスク化**(research.md 追補が典拠を保持)
- **P2-15(2.3 の NFR-005/023 行)・P2-16(I-8 根拠注記)・P2-8 残余(決定記録の変更履歴表)** — 軽微な文書修正。**行き先: 第 2 弾の節更新パックへ別タスク化**(同上)
- 設計書・要件書・台帳の版繰り上げ / docs/README.md(索引)の変更(3 節参照 — 差分不要)

### 変更ファイル一覧(正本体系外 — 3 節の突合対象外)

`.claude/agents/legacy-analyst.md`(ハーネス設定)/ `docs/features/harness-design-review/plan.md`・`research.md`(計画・調査)/ `docs/worklog/2026-08-16-harness-design-review-team-investigate.md`・`2026-08-16-harness-design-review.md`(新規)・`2026-08-10-harness-baseline-merge.md`(完了追記 1 行)。

## 3. 影響する正本

| 正本 | 変更内容 | ゲート(PRレビュー / finalize-doc) |
| --- | --- | --- |
| [開発ハーネス設計書](../../development/dev-harness-design-2026-08-07.md)(v1.5) | 6.3 縮退注記の追記(限定文言)・13 章 Phase 1 固定件数の是正・8.5 legacy-analyst 典拠欄への補助起点追加 + 変更履歴表追記(**版は上げない**) | **PRレビュー**(7.6-3 前段 — 根拠は 4 節) |
| [ハーネス運用評価台帳](../../development/harness-evaluation.md)(v1.0) | H-12 対応案の補強 + 新規 H 項目 4 件 + 候補 3 件 + 変更履歴表追記(**版は上げない**) | **PRレビュー**(H-43〜46 の前例 = 様式・運用規則不変の項目追記) |
| [docs/README.md(索引)](../../README.md) | **反映なし**(設計書・台帳の最終更新欄は既に 2026-08-16 であり、本タスクは版・最大日付を進めないため差分不要) | — |
| 要件定義書・改善台帳・決定記録・ADR-001/002・github-setup.md・onboarding.md | **反映なし** | — |

## 4. 実装方針

- **重さ分類 = 通常** の根拠: 全変更が文書・ハーネス設定であり機械的複写ではない(台帳起票は 8 要素・分類・優先度判定の判断を伴う)。軽微(誤字・単純追随)では足りず、コア領域でもない。
- **コア領域判定: 触れない** — CLAUDE.md 列挙の 5 領域(同期プロトコル・状況計算・記録権・テナント分離・データ移行)の実装コードは存在せず対象外。core-guard の guard_paths(core-areas.json・ci.yml・core_guard.py・PR テンプレ・pr/SKILL.md)にも触れない。
- **実行主体**: 全ステップが文書変更のため、役割分担(設計書 3 章)どおり **Claude が直接編集・コミット**する(/implement の Codex 委任は使わない)。品質は反対側レビューで担保: 本計画書のレビュー(codex_run.py review normal)+ /pr 時のレビュー。
- **ゲート判定の根拠(7.6-3 前段 = 節更新で可)**: ① 6.3 注記 = v1.1 で確定ゲート済みのリスク受容(10.2)と github-setup.md 管理手続への**参照の伝播漏れ是正に限定**(新規統制・境界定義を含めない — 含めるなら確定ゲート)② 13 章件数 = v1.3 確定済み方針「固定件数は腐るため書かない」(8.3)の**追随**(歴史記録は保持)③ 8.5 典拠欄 = **優先規定を新設しない補助起点の追加**のみ(D-39・D-44 が同文書を調査に使用した事実への追随。優先順位の新設・拡張に踏み込む場合は本タスクから切り出して /finalize-doc)④ 台帳追記 = H-43〜46 と同型の項目追記。※ H-19(節更新判定が確定ゲートへ覆る前例)を踏まえ、レビューで判定が覆った場合は /finalize-doc へ切り替える。
- **台帳起票の固定表**(ステップ 5・6 はこの表のとおり起票する — レビュー可能な期待値):

| 事象 | 起票先 | 分類 / 追跡優先度 | 根拠 |
| --- | --- | --- | --- |
| P1-3 paths 充填手続・記入基準の不在 | **H-12 対応案の補強**(新規項目にしない — 同一事象の深掘り) | 既存(機構の穴 / 通常)を維持 | 1 件 = 1 事象。H-12 が既に「実質空」を記録済み |
| P1-4 コア領域境界の未定義(FR-041 認可モデルの新サブケース込み) | 新規 H | プロセス設計 / 通常 | 文書・ゲート区分の設計に起因し機構実体を伴わない(台帳の分類境界)。プロセス設計は通常固定 |
| P1-5 付録A 集計定義の契約化欠落 | 新規 H | プロセス設計 / 通常 | 同上 |
| P1-6 移行実データ統制の不在 | 新規 H | プロセス設計 / 通常 | 同上 |
| P2-1 CI ジョブ表に NFR-019 (b)(d) 行がない | 新規 H | プロセス設計 / 通常 | 制御目的の典拠 = 10.1 表が「NFR-019 の実体」を自称 + 要件書の (b)(d) 要求。文書(将来 CI 設計)の欠落で機構実体なし |
| N-1 調査系スキルの鮮度確認欠如 | **候補** | —(3 要素で記載) | /investigate に鮮度確認の制御目的が未明文(SKILL.md は典拠付き調査のみ)= 条件①不成立。昇格条件 = スキルへ鮮度確認を明文化する改訂の起票時 |
| P2-9 付録E 完成手順の未規定 | **候補** | — | 完成手順の制御目的が現行文書に未明文。昇格条件 = 設計フェーズで付録E 完成作業を定義する時点 |
| P2-10 リプレイ整合性検証の CI 化未検討 | **候補** | — | 現行要件に対応する制御目的なし(旧資産の転用提案)。昇格条件 = Phase 4 CI 設計での採否判断 |

- 新規 H 項目の ID は起票時点の台帳最大番号(現在 H-53)の続番で採番する。各エントリの典拠 = research.md 追補と各原典(要件書・設計書・core-areas.json 等)。
- design.md は**作成しない**(詳細・典拠は research.md 追補が既に保持 — 複製しない。設計書 7.1-1)。

### 実装ステップ(コミット単位 — 設計書 6.1 段階実装)

| # | ステップ(何を作るか) | 合格条件(このステップの検証方法) |
| --- | --- | --- |
| 1 | 調査記録の保存: research.md(追補込み)・調査 worklog・タスク worklog をコミットし、6.4 ベースライン worklog へ完了追記 1 行(P2-18) | `uv run python scripts/check_docs_status.py` green / 追補が 25 指摘+未解決論点 6 件+N-1 を網羅(目視)/ diff が 2 節「変更ファイル一覧」の該当 4 点のみ |
| 2 | 設計書 6.3 へ縮退注記を追記(P1-2 — 2 節の限定文言)+ 変更履歴表に節更新行(器)を追加 | 6.3 節内に「リスク受容」(10.2 参照)と「github-setup」への言及が入る(`rg` で確認)/ 注記が判定者・代替手続・FR-041・境界定義に言及しない(目視)/ check_docs_status.py green |
| 3 | 設計書 13 章 Phase 1 完了条件セルの固定件数を除去し「CI harness ジョブの実行結果を正とする(8.3)」へ置換(P2-12)+ 変更履歴行を拡張 | `rg -n '103件'` のヒットが**変更履歴表 v0.17 行の 1 箇所のみ**(Phase 1 セルから消え、歴史記録は保持)/ check_docs_status.py green |
| 4 | 設計書 8.5 legacy-analyst 典拠欄へ baseball-scoring-db-structure.md を補助起点として追加(優先規定は新設しない)+ `.claude/agents/legacy-analyst.md` の典拠列挙を同期 + 変更履歴行を拡張 | 8.5 の legacy-analyst 行と agents/legacy-analyst.md の**双方**に `rg 'baseball-scoring-db-structure'` がヒット / agents 側に「88列(の意味)は data-layer.md が正」の記述が**保持**される(`rg`)/ 両者の典拠列挙が一致(目視)/ check_docs_status.py green |
| 5 | 台帳 H-12 の対応案を補強(充填の承認手続・記入基準〔変換層 2 契約の切り分け〕の未規定と対応方向を追記 — 分類・優先度・既存 8 要素は不変)+ 台帳変更履歴表に行(器)を追加 | H-12 対応案に「承認手続」「記入基準」の語が入る(`rg`)/ 既存 8 要素・分類・優先度が不変(diff 目視)/ check_docs_status.py green |
| 6 | 台帳へ新規 H 項目 4 件 + 候補 3 件を 4 節の固定表どおり起票 + 変更履歴行を拡張 | 新規 H 4 件が 8 要素完備・候補 3 件が 3 要素完備(台帳冒頭の様式規則と突合)/ 分類・優先度・起票先が 4 節の固定表と一致 / check_docs_status.py green |

## 5. DoD(受け入れ基準)

<!-- Notion タスクの DoD と同期(承認時にコメントで現行化する) -->

- [ ] research.md(生存突合の追補込み)・調査 worklog・タスク worklog がコミットされ、PR がマージされる。6.4 ベースライン worklog に完了追記が入る(P2-18)
- [ ] 設計書の節更新 3 件(6.3 限定縮退注記・13 章固定件数・8.5 補助起点追加)が反映され、変更履歴表に追記される。「103件」は Phase 1 セルから消え、変更履歴 v0.17 行には保持される
- [ ] 8.5 典拠欄と `.claude/agents/legacy-analyst.md` の典拠列挙が一致し、優先規定は新設されていない(88 列の正 = data-layer.md の記述が保持)
- [ ] 評価台帳に H-12 補強 + 新規 H 4 件(8 要素完備)+ 候補 3 件(3 要素完備)が 4 節の固定表どおり起票され、変更履歴表に追記される
- [ ] `check_docs_status.py` green(全ステップ)・`/pr` で plan を `status: in-review` にした後の `check_plan_docs_sync.py`(宣言突合)が成功する

## 6. テスト計画

- NFR-019 のテスト種別(単体・一致性・越境・E2E・故障系)への**追加なし** — コード変更を伴わない文書・設定タスクのため。
- 検証の担保: ① `uv run python scripts/check_docs_status.py`(CI docs-lint 相当 — F2 強化後の変更履歴表検査・索引↔変更履歴表突合を含む)を各ステップ後に実行し全 green ② `uv run pytest tests/`(ハーネス回帰 — 本タスクの変更はテスト対象外だが退行検知として実行)③ `/pr` 時の宣言突合(`check_plan_docs_sync.py` — 3 節の宣言と diff の一致。plan を in-review 化した後に実行し成功を確認)。
