---
feature: harness-design-review
type: research
date: 2026-08-16
---

# 調査メモ: ハーネス設計レビュー(エージェントチーム討論モード試行)

## 問い

開発ハーネス設計書 v1.3(approved)は、(1) 要件書 v1.8・改善台帳、(2) 旧システム Baseball_Scoring の実態・移行制約、(3) 過去の決定(D-xx・ADR・変更履歴)と実装済み機構、のそれぞれと整合しているか。あわせて、エージェントチーム(spec-checker / legacy-analyst / decision-tracer をチームメイト化・相互反証 1 周)による調査方式の有効性を検証する。

## 結論(要約)

> **追補あり(2026-08-16)**: 本文は develop = fcbd1de(2026-08-10)基準の調査記録であり、行番号・版表記はその時点の証拠として保存する。着手時(/task-start)に origin/develop の前進(設計書 v1.5・要件書 v2.0・評価台帳新設)が判明し、全指摘を新 HEAD 2f220ea で再突合した — **現況の正は末尾「追補: 生存突合」**(既決 7 / 生存 14 / 変質して生存 1 / 部分既決 3)。

設計書 v1.3 の骨格(コア領域 5 領域の選定・codex_run.py 一本化・hooks/CI の機構化・ADR 整合)は要件・旧システム事実・決定経緯のいずれからも支持された。一方、**P1 級 7 件**が確認された: NFR-019 文言の v1.8 追随漏れ(2.3・5.1)/ 6.3 の縮退注記漏れ / コア領域 paths の充填ゲート不在 / コア領域 5 名称の境界未定義(座標変換・捕球選手推定・出力経路が宙に浮く)/ contracts の射程欠落(付録A 集計定義 — FR-038 が 2 実装並立を強制)/ 移行実データ検証の受け皿不在 / 8.5 調査エージェント典拠欄の失効。いずれも設計書の節更新+ハーネス設定の追随で是正可能で、実装フェーズ(Phase 4)着手前に処理すべき。相互反証により初回指摘 35 件中、重大度是正 6 件・自己訂正/撤回 3 件・部分反証採用 5 件が事前解消された。

## 詳細と典拠

出典表記: design = `docs/development/dev-harness-design-2026-08-07.md` / req = `docs/requirements/requirements-pitchlog-2026-07-22.md` / imp = `docs/improvements-from-baseball-scoring.md` / draft = `docs/requirements/requirements-draft-pitchlog.md`。指摘元: S = spec-checker / L = legacy-analyst / D = decision-tracer(番号は各初回報告)。

### A. 裁定済み指摘 — P1(実装フェーズ着手前に是正)

**P1-1. NFR-019 の文言が v1.7 のまま(2.3・5.1)** — S1 = D2、三者一致
design:91「テストは pytest に一本化」・design:180「NFR-019『pytest 一本化』に整合」に対し、req:679 は v1.8 で「ランナー標準化(バックエンド=pytest・フロントエンド=Vitest・E2E=Playwright)」へ改訂済み(改訂記録 req:18)。この改訂を起案したのは設計書自身(design:18 v0.10 行)。AGENTS.md は追随済みで、設計書だけが古い。

**P1-2. 6.3 に縮退注記がない** — D1、S 補強
design:297 は「未チェックのままマージしない」と無条件に断定するが、ブランチ保護は未適用(リスク受容記録 design:571-572、承認者: 徳光 尋弥)。v1.1 の縮退注記伝播(design:28)は 2.3・6.2・10.1・12.1・12.2 を列挙し 6.3 だけ漏れた。2.3:90・6.2:278・10.1:564・12.2:687 には注記があることを対比確認済み。

**P1-3. コア領域 paths の充填ゲートが未規定** — S4 = L3 を D の部分反証で再構成
現状 `.claude/core-areas.json:11-15` は 5 領域とも `paths: []`。ただし「機構的に空振り/黙って通る」は不成立 — core_guard は paths 未定義時に「Phase 4 で定義予定 — 検査対象なし」を明示出力し(scripts/core_guard.py:19・:269-271)、この挙動は PO 承認済み計画に明記(docs/features/ci-foundation/plan.md:3・:79)= **記録された受容**(リーダー実機確認)。成立するのは: ①13 章 Phase 4 完了条件(design:699)に paths 充填が入っていない ②「誰の承認・どのゲートで埋めるか」の決定がどこにもない ③記入基準も未定義(88列⇄新スキーマ変換層は移行と CSV 出力で契約が 2 本 — imp:49-51・req:911-912 — どこまで data-migration に入れるか判断を要する)。R-3・R-5(req:783・:785、影響「高」)の担保が手続き未規定のまま実装フェーズに入り得る。

**P1-4. コア領域 5 名称の境界未定義** — S11 + L5 + L4 + L 確認事項(NFR-023/I-27)を統合、反証ラウンドで P1 へ引き上げ
コア領域は名称のみで定義され(design:291、core-areas.json:11-15)、次が「入るか」が決まらない:
- **座標変換・捕球選手推定**: NFR-018 のドメイン計算列挙(req:674)と NFR-019(a) の一致性対象(req:679)に明記されるが、4.0-4 の「状況計算」定義(req:152)には含まれない。捕球選手推定は旧で実際に 3 箇所コピーされた前例(imp:40-41)があり、NFR-018 新設の原因そのもの。座標系は「左打者ミラー保存で内外角逆転 — エラーが出ない静かなデータ破壊」(imp:141-143)が新規入力の保存経路でも起こり、これは「データ移行」にも「状況計算」にも属さない。しかも両側が揃ってミラー保存すれば一致性テストは緑のまま — CI では原理的に検出できない(L の補強)。
- **出力経路/管理者画面**: I-27 の「管理者画面経由でテナント分離を迂回する」経路(imp:214-216)に人間逐行確認が効かない。
- 是正の方向(未解決論点 3 と対): 個別に paths で吸収するか、6.3 の領域定義自体を改訂するか(後者は版繰り上げ+確定ゲート)。

**P1-5. contracts/ の射程に付録A 集計定義が欠落** — L6、S 強補強、D の「承認済み先送り」反証は部分採用
design:168 は contracts/ を NFR-019a のゴールデンベクタ置き場と定義し、「NFR-018 の実現方式は Phase 4 着手前に ADR で確定」と申し送る(req:674 と逐語対応 = 承認済みの先送り — D の反証は事実)。ただし: 付録A は「実装・テストはこの定義に対して合否判定する」と明示要求し(req:811)、FR-038 は「新DBから付録A定義で算出」対「旧DB生データから付録A定義で再計算」の完全一致を要求する(req:567)— **付録A 計算は必ず 2 実装並立し、これを裁く固定ベクタの計画がどこにもない**。旧システムで実際に事故が起きたのは成績定義側(WHIP/FIP の二重実装 — `docs/legacy/research/analytics-reports.md:80・:121`、旧テストが契約として固定 — `docs/legacy/research/docs-tests-ops.md:90-92・:180`)。裁定: 先送り自体は覆さないが、**Phase 4 ADR の必須スコープに「付録A 集計定義の契約化」を含める**ことを計画要件とする。

**P1-6. 移行実データ検証の受け皿がない** — L10、S・D 補強
FR-038 は全件移行・移行結果レポート・名寄せ解決・隔離保全を要求(req:566-571)、DoD④⑤は実データ量での性能実測・警告レポート棚卸しを要求(req:766-767)。設計書に「実データ」「本番データ」「ダンプ」は 0 件(D が全文検索で追認)、13 章の Phase 計画(design:691-700)にも該当項目なし。worktree + sandbox + .env 非コピーの統制下では移行検証が実行できない。要件側は取り扱い規律の明文化を既に要求している(NFR-009 補足 req:630、6.4 req:730)。精度注記(S): docs/ops/ という枠は確保済み(design:325)— 欠けるのは中身と、開発時のデータ持ち込み・保管・破棄の統制。

**P1-7. 8.5 調査エージェントの典拠欄が失効** — L2(P1)+ S2(P2 へ自己訂正)+ D 補強
- legacy-analyst の典拠(design:443、`.claude/agents/legacy-analyst.md:10-15`)に `docs/legacy/baseball-scoring-db-structure.md` が無い。同文書にしか無い情報: game_lineup_snapshot・CAS トークン・列単位 NULL 変換規則(同 :208・:227)。「あえて外した決定」は不在 — 決定記録自身が同文書を根拠に使っている(D-39 draft:413・D-44 P2 draft:444)ため列挙漏れと確定(D の追跡)。
- spec-checker の典拠(design:442、`.claude/agents/spec-checker.md:10`)は「v1.7・I-1〜I-24」に固定(実体は v1.8・I-27)。ただし S が自己反証: 本調査で当の焼き込み下の spec-checker が I-27 を検出・引用しており、範囲ラベルは打ち切りとして機能しなかった → この部分は P2。
- 裁定: ファイル自体の欠落(legacy 側)は探索起点を狭めるため P1、範囲ラベル(spec 側)は P2。**8.5 の表と agents/ frontmatter を一括で現行化**するのが是正。

### B. 裁定済み指摘 — P2(節更新・次回改訂で処理)

| # | 指摘 | 出典 | 経緯 |
| --- | --- | --- | --- |
| P2-1 | NFR-019(b) 越境・(d) 同期故障系の行が 10.1 ジョブ表にない(表は「NFR-019 の実体」を自称 design:549。2.3:91 は両者を継承済みで表だけ欠落)。consistency 行の説明も捕球選手推定・座標導出を落とす(design:555 対 req:679) | S6 | D 同意(1対1対応の決定は不在)・L 補強〔(b) は旧の実弱点 data-layer.md:397 への直接対処〕。(d) は L 判定不能。裁定: (b)(d) はコア領域 2 領域の CI 検証が計画から不可視になるため P1 寄りの P2 — Phase 4 CI 拡張時に必ず行を追加 |
| P2-2 | v1.7 版ラベルとリンク先内容の不一致(design:38・:86・:748、AGENTS.md:5)。v1.8 本文が現物・v1.7 本文はリポジトリに不在。ただし v1.8 は確定ゲート未通過(frontmatter in-review) | S5 = D3 | 両者が反証で歩み寄り交差(裁定経緯②参照)。P2 で合意 |
| P2-3 | 範囲ラベルの系統的失効: 「D-1〜D-41」(実体 D-44・自己記述 D-1〜D-37 — draft:7・:429-440)、「I-1〜I-24」(実体 I-27 — imp:211)。要件書自身も両方古い(req:23・:40) | D4 + S2 + L 補強 | I-25=D-41・I-26=D-43(imp:197・:204)で切断面が一致 = 改訂時に範囲表記を追随させる手順の不在が根本原因(S)。是正: 範囲表記の廃止(「最新版を参照」化)を推奨 |
| P2-4 | 改善台帳 I-4 が「FastAPI + React + TS」のまま(imp:42 対 ADR-002・req:739)。設計書 2.4 のフォローアップ(design:100-102)に台帳追随が無い | S3 = L11 | S が P1→P2 へ自己訂正(I-4 の規範「単一UI・単一実装」imp:38 は Vue 化で毀損しない)。更新時は変更履歴表の扱い(P2-8)も同時に決める(D) |
| P2-5 | 版固定アーカイブ内部矛盾: テーブル 12 対 13・CASCADE 有無・接続プール(data-layer.md:17・:21・:249 対 baseball-scoring-db-structure.md:17-18・:47・:88・:177)。リーダーが両原典行を実機確認 | L1 | D の部分反証採用: game_lineup_snapshot は req:574 で移行規則に名指し済みのため「黙って落ちる」は不成立。残余 = precedence 規定(req:25)の射程が 88 列限定でテーブル一覧・接続機構に及ばない(未解決論点 4) |
| P2-6 | NFR-021 の WSL2 解釈が要件正本へ未転記(req:689-690・:769 は Windows のまま)+ 10.1 win-setup ジョブ(design:561)が WSL2 決定(design:710)と不整合 | S7 + S9 = D11 | D の部分反証採用: 解釈自体は確定ゲートを通った PO 決定(design:19・:26・:710)— 「決定の不在」ではなく「未転記」。要件改訂の要否は未解決論点 6 |
| P2-7 | 移行センチネル除外規則(x<=0・球速>0)の帰属先誤り: data-layer.md に規則は無く(:400 は UI の "0" 多用のみ)、req:571・:886 が同ファイルへ誤帰属。実際の所在は research/README.md:17・baseball-scoring-db-structure.md:208 | L13 | S は P1 引き上げを提案・D は P2。裁定: P2 — 規則本文は要件書(付録B-5)に現存し情報は失われていない。引用先の是正は v1.8 確定ゲートに同乗させる |
| P2-8 | 変更履歴表の欠如: 改善台帳・決定記録に 7.1-3(design:313)の表が無く、ADR×2 も更新に対応する行が無い。台帳は要件書の版番号で自らの履歴を語る構造(imp:51・:66) | D8 | L が P2 据え置きを支持(項目ごとの確定日で実質追跡可能)。S が構造の深さを補強 |
| P2-9 | 付録E 完成手順の未規定: 正解ベクタの出所は付録E と規定済み(req:927・:186)だが全表は未完成で、完成時に何を典拠に拾うか・旧バグ(5 件 — domain-logic.md:174-179)と仕様の仕分けゲートが無い。UI 層が担っていた規則(B=4→四球 — domain-logic.md:117 対 docs-tests-ops.md:101)が落ちるリスク | L7 + L8 | S の前提反証を採用(L7 の「旧テストをベクタ化する設計」という前提は要件が採らない)→ P2 に再構成。L に再反論機会なし(1 周ルール)のため反証採用を明記 |
| P2-10 | 旧のリプレイ整合性検証(validate_replay_consistency — docs-tests-ops.md:104)に相当する CI ジョブ・受け入れ基準が無い。移行データ健全性検証に転用可能な資産 | L9 | D 同意(決定の不在)。改善提案として Phase 4 CI 設計のインプットに |
| P2-11 | 8.4 /setup-dev 行が Windows sandbox 前提のまま(design:418 対 :668)。実装スキルは WSL2 追随済み(setup-dev/SKILL.md:19)— v0.14 の掃除(design:22)から 8.4 が漏れた | D5 | S・L 異議なし |
| P2-12 | 13 章 Phase 1 完了条件の固定件数「103件」(design:696)が 8.3 の「固定件数は書かない」方針(design:407)と矛盾。実測 213 件(worklog 2026-08-10-codex-plan-status-guard.md:41) | D6 | 異議なし |
| P2-13 | codex_run.py fast path の worktree 判定が部分文字列一致(codex_run.py:307 対 :273)。実害はブランチ検証で抑止済み | D9 | 異議なし |
| P2-14 | notion-map.json のプロパティ表が 11.1(design:596)と乖離(タスクID 無・見積ポイント増 — notion-map.json:13-23) | D10 | 異議なし |
| P2-15 | 2.3 継承表に NFR-005・NFR-023 が無い(NFR-018 と同じ収載理由が成立するのに — design:86-96 対 req:604-607・:692-696)。AGENTS.md では稼働済み | S10 | D 同意(収載基準の決定は不在) |
| P2-16 | I-8 の根拠(GameScreen.tsx:1257 — imp:72)が版固定アーカイブ外。ただし同期プロトコルの規範は要件書が独立に定義(req:266-279・:615-618・:151)しており依存なし | L12 | D・S の影響限定の反証を採用 |
| P2-17 | NFR-019 リモート強制未達の受容が要件正本(9 章リスク表 req:779-790・10 章 req:794-805)から不可視。設計書側の統制記録は完備(design:571-572) | S8 | D: 転記を要求/免除した決定はどちらも不在 → 未解決論点 1 へ |
| P2-18 | 6.4 ベースラインマージの worklog が未締め(2026-08-10-harness-baseline-merge.md は全 39 行、末尾:36-39 が「未決・次の一歩」のまま)。**マージ自体は実施済みと確定** — リーダーが git 実機検証: a4dc517 = Merge PR #4、親 = f06e2f2(main 側アンカー)+ cbe1371 = 6.4 プロトコルどおり | D7 | 両エージェントとも Bash 不可で判定不能だった点をリーダーが確定(裁定経緯⑦)。worklog は記録であり正本でない(証跡の正 = PR 本文 + Notion — design:303)ため P2/低 |

### C. 討論の裁定経緯(誰が異議 → どの原典で決着)

1. **spec-4/legacy-3(paths 空)— decision が反証**: 「機構的に空振り」に対し core_guard.py:269-271 の明示メッセージと ci-foundation plan.md:79(PO 承認済み)を提示。リーダーが両原典を実機確認して反証採用。ただし「充填ゲート不在」は三者一致で残存 → 指摘を再構成して P1-3 採用。
2. **spec-5 対 decision-3(v1.7 ラベル)— 立場が交差**: decision は「承認済み版は v1.7」の但し書きを自ら撤回(規約の不在を全文検索で確認・7.1-5 frontmatter 原則・時系列)。spec は逆に「弁護不成立」を撤回(design:26「残るゲート項目: 要件書 v1.8」・:340 により「拘束力ある最終承認版は v1.7」の読みは原典に支えられる)。リーダー裁定: 事実は「版ラベルとリンク先内容の不一致」P2 で両者合意どおり。背後の状態モデル問題は未解決論点 5。
3. **legacy-6(contracts 射程)— decision が P2 へ引き下げ提案、spec が P1 補強**: decision の「承認済み先送り(design:168 = req:674 と逐語対応)」は事実として採用。しかし spec の補強(req:811 の合否判定要求・req:567 の 2 実装並立強制)により、先送り先 ADR の必須スコープ指定として P1-5 採用。
4. **legacy-7(旧テスト由来ベクタ)— spec が前提を反証**: req:927・:186 で正解ベクタの出所は付録E と確認。反証採用、P2-9 へ再構成。legacy は 1 周ルールで再反論なし — 反証採用の旨を明記して確定。
5. **spec-11(状況計算の範囲)— legacy が P1 引き上げを要求**: 旧の 3 箇所コピー前例(imp:40-41)と「一致性テストで検出不能な破壊経路」の論証を採用し、境界未定義クラスタ(P1-4)へ統合。
6. **legacy-1(12 対 13)— decision が影響を部分反証**: req:574 の名指し保全を確認し「黙って落ちる」を棄却、P2-5 へ。矛盾の事実自体はリーダーが両原典行を実機確認。
7. **decision-7(ベースラインマージ)— 両者判定不能 → リーダーが確定**: エージェントは Bash 不可のため、リーダーが `git log a4dc517` で親構成(f06e2f2 + cbe1371)を検証し「実施済み・プロトコル準拠」と確定。
8. **legacy-13 の重大度 — spec P1 提案 対 decision P2**: 規則本文が要件書に現存する(情報喪失なし)ことを重視し P2。是正は v1.8 確定ゲートに同乗。
9. **自己訂正 3 件**: spec-2(P1→P2 — 自セッションの実挙動が反例)、spec-3(P1→P2 — I-4 の規範は毀損していない)、decision-3 但し書き撤回。相互反証の直接成果。
10. **decision の「問題なし」への訂正 2 件(legacy 指摘)**: ①調査エージェント 3 本の clearance は実行既定のみで典拠欄(design:442-444)未検証 — 実際に失効(P1-7)。②ADR-002 clearance は改善台帳を対象外にしていた(P2-4)。「問題なし」の記載は範囲限定つきで読むこと。

### D. 確認して問題なしだった点(三者の検証済みクリアランス)

- **コア領域 5 領域の選定根拠**: 旧側の事実と全領域で整合(データ移行 = 列名乖離・位置依存復元・センチネル同居ほか data-layer.md:147-149・:234・:396-404 / 同期 = sync_missing_plays と重複 INSERT 余地 services-shell.md:132・:207 / 記録権 = 複数ブラウザ非対応前提 docs-tests-ops.md:184 / テナント分離 = owner ガード省略可能 data-layer.md:397)。R-3・R-5 との対応(design:296)も要件書 9 章(req:783・:785)と一致
- **ADR-001 モデル対応表**: codex_run.py:30-39 = ADR-001:29-37 = design:533-541 で三者一致
- **hooks 6 本・skills 13 本・調査エージェント実行既定・CI 4 ジョブ・core-guard 文言・plan status 機構強制・plan テンプレ拡張キー**: いずれも設計書の記述と実装が一致(D 検証。ただし典拠欄は上記のとおり除外)
- **索引と frontmatter の状態が全 8 文書で一致**(docs-lint の動的検査 check_docs_status.py:185-237 と併せ D 検証)
- **AGENTS.md の NFR 参照(005/014/018/019/023)はすべて v1.8 の実在番号・実内容と一致**(S 検証)— NFR-019 は AGENTS.md の方が設計書本文より正確
- **6.4 例外の統制内容と worklog の決定記録が一致**(design:303 対 worklog:23-28)、要件書 8 章とも非衝突(リリース定義は設計書側の管轄 — req:761-773)
- **docs/legacy/ の版固定・書き込み禁止機構**(design:401・:686)は移行仕様の前提を満たす
- **14 章論点の現況**: A(Vue)・B(レビュー体制)・C(WSL2)・F は決着済みで本文反映、D(本番環境)・E(ty)は未決のまま — 記述と現況が整合

## 未解決・申し送り

相互反証 1 周で決着しなかった論点(いずれも PO 判断または後続タスクの設計判断が必要):

1. **NFR-019 リモート強制未達の受容を要件正本(9 章リスク表)にも転記するか**(P2-17)。転記を要求/免除した決定がどちらも不在。
2. **コア領域 paths の充填手続**(P1-3 の是正設計)— 誰の承認・どのゲートで埋めるか、記入基準(変換層の 2 契約の扱いを含む)。
3. **コア領域 5 名称の意味範囲の定め方**(P1-4)— 個別 paths 判断で吸収するか、6.3 の領域定義自体を改訂するか(後者は版繰り上げ+確定ゲート必須)。
4. **版固定アーカイブ内部不整合の優先規定**(P2-5)— req:25 の precedence(88 列限定)をテーブル一覧・接続機構へ拡張するか。
5. **frontmatter 単一値の状態モデルが「承認済み v1.7 + 検証中 v1.8」を表現できない**問題 — 要件書 v1.8 の確定ゲート通過で自然消滅する見込み。
6. **NFR-021/WSL2 の要件改訂**(P2-6)— v1.8 確定ゲートに同乗させるか、v1.9 として別途起票するか。

**是正の推奨パッケージ**(後続の /task-start → /plan 向け):
- 設計書 v1.4 改訂(節更新群: P1-1・P1-2・P2-11・P2-12 + 8.5 典拠欄 P1-7)— 実装追随の節更新として PR レビューで可(7.6 決定表)か、境界定義(P1-4)を含めるなら版繰り上げ+確定ゲート
- ハーネス設定の追随(agents/ frontmatter・notion-map 対応表の扱い)
- 要件書 v1.8 確定ゲートへの同乗項目: P2-2・P2-7(+ 判断次第で P2-17・P2-6)
- Phase 4 計画への予約項目: paths 充填ゲート(P1-3)・NFR-018 ADR のスコープに付録A 契約化(P1-5)・CI ジョブ表の (b)(d) 行(P2-1)・移行実データ統制の ops 文書(P1-6)・リプレイ整合性検証の検討(P2-10)

---

## 追補: 生存突合(2026-08-16・リーダー実施・HEAD 2f220ea)

本文の調査は develop = fcbd1de(2026-08-10)基準で実施した。/task-start 時の fetch で origin/develop が 2f220ea まで前進済み(PR #7〜#12: 要件書 v1.9 → v2.0 / 設計書 v1.4 → v1.5 / ハーネス運用評価台帳 v1.0 新設 / docs 検査強化 F2 / 現在地導出 F5 / ガード修正 F1)と判明したため、全 25 指摘と未解決論点 6 件を新 HEAD で再突合した。本文の行番号は fcbd1de 時点の証拠として保存し、**現況は本節を正とする**(行番号は 2f220ea 基準)。

### 判定表

| 指摘 | 判定 | 現況の典拠(2f220ea) |
| --- | --- | --- |
| P1-1(NFR-019 文言) | **既決** | v1.4 で 2.3 を 3 ランナー + (a)〜(d) へ(design:96、変更履歴 design:35)。「pytest 一本化」は全文残存なし |
| P1-2(6.3 縮退注記) | **生存** | design:328 が無条件断定のまま |
| P1-3(paths 充填ゲート) | **生存(台帳 H-12 と重複)** | paths 全空(core-areas.json:11-15 不変)・Phase 4 完了条件に充填なし(design:760 — v1.4 改訂後も)。台帳 H-12(harness-evaluation.md:368)は「実質空」の事実と「Phase 4 で埋める」対応案(:376)のみで、**充填の承認手続・記入基準は未規定のまま** → H-12 の補強として起票 |
| P1-4(コア領域境界未定義) | **変質して生存** | 座標サブ項は v2.0 が保存規約を明文化して縮小(req:1100-1108 — 0〜1 正規化・捕手視点・左右変換なし・ゾーン枠データ化・移行変換式確定・領域シード)。**境界問題の核心は残存**(5 領域名称は不変)。さらに v2.0 の FR-041 共同分析グループ・認可モデル新設(req:22・:104 — 「共有グループの参加テナント」という新アクター)で**テナント分離の境界に新サブケース**が発生 — 指摘はむしろ強化 |
| P1-5(付録A 契約化) | **生存** | 付録A 柱書「実装・テストはこの定義に対して合否判定する」不変(req:1002)、FR-038 の新旧突合も 2 実装並立を強制(req:748)、consistency は状況計算のみ(design:587)。付録A 由来の成果物計画は依然なし |
| P1-6(実データ統制) | **生存** | 設計書に「実データ」「本番データ」「ダンプ」0 件(再確認) |
| P1-7(8.5 典拠欄) | **半分既決** | spec-checker / decision-tracer 側は v1.4 で「全 D-*/全 I-*」化(design:35)。**legacy-analyst の典拠に baseball-scoring-db-structure.md が無い点は生存**(.claude/agents/legacy-analyst.md 不変) |
| P2-1(10.1 の (b)(d) 行) | **生存(縮小)** | 2.3 は (a)〜(d) を明記済み(design:96)。**ジョブ表は依然 consistency(a)・e2e(c) のみ**(design:583-591)、consistency の説明も捕球選手推定・座標導出を含まず(:587) |
| P2-2(v1.7 ラベル) | **既決** | v1.4 で参照を v1.9 へ・過去事実の v1.7 は保存と明記(design:35) |
| P2-3(範囲ラベル) | **既決(残余は台帳追跡中)** | design 8.5 は「全 D-*/全 I-*」化(v1.4)、要件書本文 2 箇所は F2 で D-44 / I-27 へ是正(req:23)。残余 = 決定記録自身の冒頭「D-1〜D-37」(draft:7)— 台帳の候補 (8) で追跡済み |
| P2-4(I-4 React) | **既決** | v1.9 波及追随で解消。現存の React 言及は I-2「現行」欄の旧実装説明のみ(imp:47 — 正当) |
| P2-5(アーカイブ内部矛盾) | **生存(性質上不変)** | 版固定のため矛盾自体は消えない。precedence は「88列の実際の意味・センチネル値・列名の罠」限定のまま(req:30)。ただし座標変換式が要件に直接確定(req:1106)しアーカイブ依存は縮小 |
| P2-6(NFR-021 WSL2 / win-setup) | **既決** | 要件 v1.9 で改訂 + design v1.4 で win-setup を onboarding.md 再現へ(design:35)。**未解決論点 6 も同時に解消** |
| P2-7(センチネル帰属誤り) | **既決** | v2.0 改稿で「旧集計側も x<=0 を除外していた → data-layer.md」の誤帰属記述が消滅(「除外していた/旧集計側」全文 0 件)。現行の参照(req:752-753)は列単位規則・意味表の話で data-layer.md:400 と整合 |
| P2-8(変更履歴表欠如) | **部分既決** | 改善台帳は v1.0 で変更履歴表新設(imp:11)+ F2 が docs-lint に変更履歴表検査を追加。残余 = 決定記録(draft:1-10 に表なし)・ADR の追記行 |
| P2-9(付録E 完成手順) | **生存** | 柱書「スキーマと代表例を定める(全表の完成は設計フェーズ)」構造は不変(req:1149-1150)。完成時の典拠選定・旧バグ仕分けゲートは依然未規定 |
| P2-10(リプレイ整合性) | **生存** | 10.1 に該当ジョブなし(design:583-591)・台帳にも言及なし |
| P2-11(8.4 Windows 残存) | **既決** | v1.4「8.4 /setup-dev — Windows sandbox 設定の言及を除去」(design:35) |
| P2-12(Phase 1 固定件数) | **生存** | design:757 に「tests/・103件」残存(実測は 213 件) |
| P2-13(fast path 部分文字列) | **生存** | codex_run.py:273(親ディレクトリ名判定)対 :307-308(部分文字列)— F1 の改修は guard 側のみで本箇所は不変 |
| P2-14(notion-map 対 11.1) | **生存** | design:655 実査行 = タスクID あり・見積ポイントなし / notion-map.json:13-23 = タスクID なし・見積ポイントあり — 非対称は不変 |
| P2-15(2.3 の NFR-005/023 欠落) | **生存** | v1.4 の 2.3 改稿後も両 NFR の行なし(全文 grep で 2.3 範囲ヒットなし) |
| P2-16(I-8 の根拠が版固定外) | **生存(影響限定のまま)** | imp:80 に GameScreen 参照残存 |
| P2-17(リモート強制の要件正本記載) | **生存** | v2.0 でも「リモート強制/保護未適用」0 件 — 未解決論点 1 は未決のまま |
| P2-18(6.4 worklog 未締め) | **生存** | 2026-08-10-harness-baseline-merge.md は 39 行のまま(マージ実施は本文 P2-18 のとおり git で確定済み) |

### 未解決論点の現況

1. NFR-019 逸脱の要件正本転記 — **未決のまま**(P2-17)
2. paths 充填手続 — **未決のまま**(H-12 補強として起票する)
3. コア領域境界の定め方 — **未決・変質**(座標保存規約は v2.0 で明文化済み。残るは強化レビュー境界の問題で、FR-041 認可モデルの新サブケースが加わった)
4. アーカイブ precedence の射程拡張 — **未決のまま**(req:30)
5. frontmatter 状態モデル — **解消**(要件書は v2.0 approved。以後の改訂は「起案 in-review 行 + 確定 approved 行」の 2 行方式で運用が確立 — design:35-36 が実例)
6. NFR-021 の要件改訂 — **解消**(要件 v1.9 として確定ゲート通過済み)

### 新規指摘(本突合で発生)

**N-1. 調査系スキルにベースライン鮮度の確認がない**(P2 / プロセス設計)
本調査は git fetch を経ずにローカル develop(6 日古い fcbd1de)基準で実施され、指摘 25 件中 7 件が既決だった。/investigate・調査サブエージェントの手順にベースライン確認(fetch + origin/develop との乖離検知)が無い。討論モードのコスト($27.98)の一部が陳腐化した指摘に費やされた。→ 評価台帳へ起票対象(事象典拠: docs/worklog/2026-08-16-harness-design-review-team-investigate.md・本追補)

### 是正推奨パッケージ(改訂版 — 本文版を置き換え)

- **本タスク(第 1 弾)の節更新**: 6.3 縮退注記(P1-2)/ 13 章 Phase 1 固定件数(P2-12)/ 8.5 legacy-analyst 典拠欄 + `.claude/agents/legacy-analyst.md` の現行化(P1-7 残余)+ 設計書変更履歴表追記
- **本タスクの台帳起票**: H-12 補強(P1-3 = 充填手続・記入基準)/ 境界未定義(P1-4 — FR-041 サブケース込み)/ 付録A 契約化(P1-5)/ 実データ統制(P1-6)/ N-1。候補判断: (b)(d) ジョブ行(P2-1)・付録E 完成手順(P2-9)・リプレイ整合性(P2-10)・fast path(P2-13)・notion-map 対 11.1(P2-14)・2.3 欠落行(P2-15)
- **PO 判断待ち(第 2 弾以降)**: 未解決論点 1・3・4。境界の 6.3 改訂を選ぶ場合は版繰り上げ+確定ゲート
- **軽微・随時**: P2-16(台帳の根拠注記)・P2-18(worklog 追記 1 行)・P2-8 残余(決定記録の表)
