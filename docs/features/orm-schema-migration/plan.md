---
feature: orm-schema-migration
status: active            # active | in-review(/pr が PR 内で更新。完了は PR 状態・Notion・worktree 除去から導出。codex_run.py implement は active 以外を拒否)
承認: 済(2026-09-10・山田正輝)  # 未 | 済(YYYY-MM-DD・承認者)— codex_run.py が「済」でないと実行を拒否する
重さ分類: コア領域          # 軽微 | 通常 | コア領域 | 機械的軽作業(ADR-001 のモデルをラッパーが自動選択)
worktree: ../../..        # worktree ルート(plan.md からの相対 or 絶対)。/task-start が設定
notion: https://app.notion.com/p/3d593b75e68781469990ca4e61fca9d2
branch: feature/orm-schema-migration
created: 2026-09-09
計画レビュー周回: 6        # 指摘反映を伴うレビュー 1 周ごとに +1(収束確認周は数えない。/plan が更新)
確定ゲート周回: 0          # 指摘反映を伴う敵対レビュー 1 周ごとに +1(同前。/finalize-doc が更新)
実行方式: 通常             # 通常 | fast(fast path 適用時に fast へ — 人間の事前 OK 必須。現在地導出が識別)
反映周コミット: 適用       # 適用 | 規約制定前(必須・既定値なし。確定ゲートの反映周コミット突合の適用境界 — 設計書 6.1)
---

# 実装計画書: ORM の導入と models・migration(設計書 5.1・10.1 の改訂 + SQLAlchemy 2.x・Alembic・依存追加)

> **引用の基準**(TSK-348 のマージ後に整理 — 2026-09-10)
>
> - **`docs/design/data-model.md` は v0.2**(blob `2a78b78`)。**本書の説明文中の行番号引用は v0.1**
>   (blob `fc573b6`)**を基準に採取したもので、v0.2 では旧行 +2〜+5 ずれる**
>   (旧 10-238 → **+2** / 旧 241-528 → **+3** / 旧 529-2749 → **+4** / 旧 2750- → **+5**)。
>   **同じ表・段落の中に収まるずれ**であり、指す条項は変わらない。
> - **実装ステップ表と DoD は行番号を使わず、節番号 + 要求 ID で引用している**
>   (例: 3-4 節の業務的一意性全数表 / `M07` / `B02` / 12-1 節の行き先の区分)。
>   **実装者と逐行確認者が手を動かす箇所はこちらが正**。
> - **運用評価台帳は `H-nn`**、**要件書は安定 ID**(`#FR-nnn` / `#NFR-nnn`)で引用している。
> - **機械照合の結果**: v0.1 → v0.2 のずれ計算で **273 行参照のうち 272 件が完全一致**。
>   不一致 1 件は **TSK-348 が是正した 3-2 節の `search_path` 契約**(旧 `:238-239`)で、
>   **v0.2 で内容が変わっている**(「または全オブジェクトを完全修飾」→「`search_path` の設定は**必須**」)。
>   → 本書の当該引用は **v0.1 時点の記述**であり、**現行の契約は v0.2 の 3-2 節が正**。
> - **設計書・要件書・`tests/test_ci_wiring.py`・`docs/legacy/research/data-layer.md`・
>   他 feature の計画書は TSK-348 で変更されていない**ので、それらへの行番号引用は現行のまま有効。

## 1. 背景・目的

**Notion タスク**: [TSK-343](https://app.notion.com/p/3d593b75e68781469990ca4e61fca9d2)(優先度 高・コア領域)

[TSK-342](https://app.notion.com/p/3d593b75e68781958074ed04dff7dab7)から**人間の裁定(2026-09-08)で
切り出された実装タスク**。TSK-342 は「正本の新設 + 欠陥群の是正 + 7.3 の確定ゲート」を持ち、
**本タスクが「設計書 5.1/10.1 の改訂 + models + migration + 依存追加」を持つ**。

**目的**: `docs/design/data-model.md`(**v0.2**・**approved**)が定めた**認定済みの論理スキーマを物理スキーマへ写す**。
これにより「backend が書ける状態」に到達する。

**開始条件は充足済み** — `docs/design/data-model.md` は `status: approved`。
**TSK-342 の v0.1**(確定ゲート 13 周・PO 承認 2026-09-09・PR #50 = `1424d67`)に加え、
**TSK-348 の v0.2**(search_path 契約の是正・PO 承認 2026-09-10・**PR #51** = `4ed54fc`)も**マージ済み**。
**本タスクが写す対象は v0.2 の論理スキーマ**(v0.2 はエンティティ・一意性・制約の節に触れていないので設計内容は変わらない — [research.md](research.md) 3-1 節)。

**対応する要件**(突合の全数と典拠は [research.md](research.md) 4-1 節):

- [NFR-010](../../requirements/requirements-pitchlog-2026-07-22.md#NFR-010) テナント分離(初日から全機能)
  — 本タスクは**テナント列・テナント内一意・複合 FK の物理化**まで。**実現層(RLS)は要件が指定していない**
- [NFR-014](../../requirements/requirements-pitchlog-2026-07-22.md#NFR-014) シークレット管理
- [NFR-018](../../requirements/requirements-pitchlog-2026-07-22.md#NFR-018) ドメイン計算の単一実装
  — **models / migration は対象計算に当たらない**(裁定 `A-4` = `RQ-09`)
- [NFR-005](../../requirements/requirements-pitchlog-2026-07-22.md#NFR-005) DB 側絞り込み・ページング
- [NFR-019](../../requirements/requirements-pitchlog-2026-07-22.md#NFR-019) テスト規約の CI 強制
- [NFR-011](../../requirements/requirements-pitchlog-2026-07-22.md#NFR-011) パスワードは bcrypt
- [NFR-015](../../requirements/requirements-pitchlog-2026-07-22.md#NFR-015) 失敗の黙殺防止
- [NFR-017](../../requirements/requirements-pitchlog-2026-07-22.md#NFR-017) 一般ログ(**管理者操作ログとは別**)
- [FR-015](../../requirements/requirements-pitchlog-2026-07-22.md#FR-015) 選手の登録 — **背番号に一意制約を張らない**
- [FR-018](../../requirements/requirements-pitchlog-2026-07-22.md#FR-018) 選手の非表示化 — **系統の割り当てと述語の器まで**
- [FR-038](../../requirements/requirements-pitchlog-2026-07-22.md#FR-038) 旧データの移行 — **受け皿まで**
- 要件書 4.0-2 **物理削除しない** / 4.0-3 語彙の 3 層 / 6.2 無効化マトリクス / 7.1 **標準 PostgreSQL の範囲**

**下調べ**: [research.md](research.md)。**詳細設計**: [design.md](design.md)。

## 2. スコープ

### やること

| 群 | 内容 |
| --- | --- |
| **① 設計書 5.1・10.1 の改訂** | 5.1 へ **ORM 行・DB ドライバ行を新設**し留保を除去 / **規範本文に採用理由と却下案を残す**(design.md 6 節)/ 10.1 の「**ORM は入れない**」箇条を改訂 / **版を v1.14 へ繰り上げ**、`/finalize-doc` を通す(裁定 `Q-1`)/ `docs/README.md` の索引を現行化 |
| **② 依存追加** | `backend/pyproject.toml` + `backend/uv.lock` へ **SQLAlchemy 2.x・Alembic を直接依存として厳密固定**。**① と同一コミット**(4-2 節) |
| **③ 機械検査の更新** | `tests/test_ci_wiring.py` の ORM 不在検査を **ORM スタックの厳密固定検査へ置き換える**(design.md 8 節)。**② と同一コミット** |
| **④ 設定契約** | **アプリ用 URL・Alembic 用 URL・pooler フラグの 3 キーへ分離**し**相互 fallback を禁止**(design.md 7-3 節)+ 接続 URL の正規化 |
| **⑤ スキーマ契約 manifest** | `contracts/db/schema-manifest.json` を新設。**カタログで表現可能な構造の期待値 + 正本への追跡可能性**(正本節・要求 ID・**正本の blob SHA へ束縛**)。**「全数オラクル」とは主張しない** — 機械で閉じない範囲は **design.md 10 節の受入証跡**で閉じる |
| **⑥ models** | `backend/src/pitchlog/db/` に SQLAlchemy models。**領域ごとにモジュール分割**(design.md 9 節) |
| **⑦ migration** | `backend/migrations/` に Alembic。**テーブル・制約・テナント列・複合 FK まで**。**不変列マトリクスのトリガ**(design.md 5 節) |
| **⑧ テスト** | models・migration・型境界 3 層・スキーマ契約の全数突合(6 節) |
| **⑨ core-areas.json 登録** | 新設資産を **4 領域**へ登録(6.3 規則②③⑤ — design.md 9 節)。**`areas[].paths` は glob**(`fnmatch` の `*` は `/` を跨ぐ)/ **`guard_paths` に足す場合は完全一致なので完全列挙** |
| **⑩ CI** | 10.1 の `backend` ジョブへ **`alembic` 3 段**を追加 + **job env へ `PITCHLOG_MIGRATION_DATABASE_URL` を供給**(レビュー 3 周目 `P0-2`)+ **`tests/test_ci_wiring.py` の CI コマンド契約を更新**(design.md 8-3 節) |
| **⑪ 受入証跡** | **`N1`・`N3`・`N4` の突合シートを機械生成**(判定は人間・**判定区分と是正遷移つき**)+ **`N2`・`N5`・`N6` は受け取り先で閉じる**(design.md 10 節) |

### やらないこと

| 対象 | 受け取り先 | 根拠 |
| --- | --- | --- |
| **RLS ポリシー・ロール DDL** | **TSK-317** | 裁定 `A-2`。`data-model.md:2418`・`:2750` |
| **越境テストの作成・実行** | **TSK-317 / TSK-344** | `:2439`。**NFR-019(b) の充足義務は本タスクに無い** |
| **88 列写像の値** / **フィールド別センチネル変換** / **座標正規化** / **球速の扱い** | **TSK-250** | 裁定 `R-9` + 2026-09-07 の PO 裁定 |
| **移行元最終オーダーの解決列** | **TSK-250**(または別途裁定) | ****12-3 節の移行元最終オーダーの「列」(裁定 `R-1`)** は「解決結果を持ちたい場合は別の nullable 列として足す」= 任意**。作ると裁定なしに射程を広げる(レビュー 2 周目 `P0-1`) |
| **`D4` の非再利用の保証機構 / 復旧世代・`RG1`・`I6` の永続化写像** | **TSK-250** | **`:2758`・裁定 `R-6`**「本書は `D4` の列までを持ち、非再利用を保証する機構を持たない」 |
| **`D2` の局所再採番の手順** / **記録権世代の生成アルゴリズム・物理形式** | **同期・記録権の最初の実装タスク** | `:2757` 付近・裁定 `R-6` の除外 |
| **`D2` の初期間隔・再採番の閾値** | **実装期の実測** | `:2756`。**本書は型だけを決めた** |
| **原子境界 `T1`〜`T9` の実装**(台帳行と原本行の原子書き込み) | **同期・記録権の実装タスク** | 本タスクが構造で保証するのは **`D5` の「高々 1 件」まで**(design.md 4-4 節) |
| **FR-018 の具体的なロック方式・SQL** | **[TSK-354](https://app.notion.com/p/3d693b75e68781748725cab8ecc14780)**(**起票済み** 2026-09-10) | `:2023`「実装計画へ送る((B))」が**タスクを名指ししていない**ため所有が空白だった。レビュー 2 周目 `P1-8` の是正として起票(4-10 節) |
| **enum・参照テーブルの値**(とくに列 44 球種) | **TSK-334 / TSK-250** | 値域が衝突しており裁定が必要(research.md 4-6 節)。**器まで作る** |
| **(β) の集計クエリ** | 実装期 | **NFR-018(a) の発火点**(`:2564-2570`) |
| **索引の具体形・事前集計の採否 / 実行計画の確認** | **実装期の性能実測** | `:2753`・`:2181`。**`EXPLAIN` はカタログ検査へ置き換える** |
| **GRANT の設計** | **実機確定の受け取り先**(12-7 (1)) | `:430-431` |
| **`WAIT-04` の退避の保持期間** | **TSK-345** | `:2760`。**本書は器だけを持つ** |
| **`docs/design/data-model.md` の編集** | — | 正本は本タスクに更新を要求していない(`:2394` は**設計書**の追随のみ)。**TSK-348 は v0.2 でマージ済み(PR #51)。次の改訂は TSK-353** |
| **`tests/fixtures/data-model-source.txt` / `scripts/design_relations/fixture-sha256-data-model.txt`** | **TSK-353** | 触ると TSK-348 / TSK-353 と 3 者競合になる |
| **ADR の起票** | — | **起票しない**(design.md 6 節) |

## 3. 影響する正本

| 正本・資産 | 変更内容 | ゲート |
| --- | --- | --- |
| [開発ハーネス設計書](../../development/dev-harness-design-2026-08-07.md) | **5.1**: ORM 行・DB ドライバ行の新設 + 留保除去 + **採用理由と却下案を規範本文へ** / **10.1**: 「ORM は入れない」箇条の改訂 + **`alembic` 3 段の CI 記述** / 変更履歴へ **v1.14** 行 | **finalize-doc**(裁定 `Q-1`(A)= 7.6-3 後段。**版を繰り上げる**) |
| [ドキュメントマップ](../../README.md) | 設計書の状態・版・最終更新を **v1.14** へ現行化 | PRレビュー |
| **`tests/test_ci_wiring.py`** | ① ORM 不在検査 → ORM スタックの厳密固定検査 ② **backend ジョブの CI コマンド契約を「既存 6 件 + Alembic 3 件」へ更新**(design.md 8-3 節) | PRレビュー + **core-guard**(`guard_paths`) |
| **`.github/workflows/ci.yml`** | `backend` ジョブへ **`alembic upgrade head` / `current --check-heads` / `check`** を追加 + **job env へ `PITCHLOG_MIGRATION_DATABASE_URL`**(既存の `PITCHLOG_TEST_*_DSN` とは別キー)。**新しいジョブは起こさない** | PRレビュー |
| **`.claude/core-areas.json`** | **4 領域**の `areas[].paths` へ新設資産を **glob** で登録(design.md 9 節) | **6.3 規則⑤ = 敵対レビュー + 人間承認** |
| **`contracts/db/schema-manifest.json`**(新設) | スキーマ契約の期待値(カタログで表現可能な範囲)+ 正本への追跡可能性 | PRレビュー + **core-guard**(4 領域へ登録) |
| **`docs/features/orm-schema-migration/acceptance-sheets/`**(新設・機械生成) | **`N1`・`N3`・`N4` の突合シート**(design.md 10 節)。**判定区分 `一致` / `差分` / `対象外` を人間が付ける** | PRレビュー(**判定は人間**) |
| 設定正本テンプレート(`.env` の見本) | **3 キーへ分離**(アプリ用 URL / Alembic 用 URL / **pooler フラグ**)+ `postgresql+psycopg://` の形 | PRレビュー(**編集は人間・ステップ 3** — 4-7 節) |
| [要件定義書](../../requirements/requirements-pitchlog-2026-07-22.md) | **反映なし** | — |
| [改善台帳](../../improvements-from-baseball-scoring.md) | **反映なし** | — |
| [データモデル設計](../../design/data-model.md) | **反映なし** | — |
| [同期プロトコル設計](../../design/sync-protocol.md) | **反映なし** | — |
| [ハーネス運用評価台帳](../../development/harness-evaluation.md) | **反映なし**(候補の起票判断は /pr のクローズ処理) | — |
| ADR-001 / ADR-002 / ADR-003 | **反映なし** | — |
| [オンボーディング](../../development/onboarding.md) / [GitHub 設定手順](../../development/github-setup.md) | **反映なし** | — |
| [NFR-021 受入証跡の運用](../../ops/nfr021-acceptance/README.md) 他 ops 4 本 | **反映なし**(ただし**依存追加で既存の受入証跡が失効する** — 4-5 節) | — |

### 別枠宣言 — 共有資産の所有(TSK-317 と TSK-235)

実測は [research.md](research.md) 1 節。**実衝突は 1 箇所だけで、6 番目が新たに判明した。**

**順序は裁定 `Q-4` の `TSK-348 → TSK-317 → 本タスク` に統一する**(レビュー 2 周目 `P1-6` の是正 —
1 周目の表は「本タスクが先に入る」と書いており 4-6 節と読みが逆転していた)。
**本タスクは TSK-317 の本体 PR より後、TSK-317 の改訂 3 より前**に入る。

| # | 箇所 | 所有 | 根拠 |
| --- | --- | --- | --- |
| 1 | `tests/test_ci_wiring.py` | **本タスク**(TSK-317 の**改訂 3 より前**) | TSK-317 の実 diff に含まれない。改訂 3(`S-8`)でのみ触る予定(`pg-authz-verification-g2/plan.md:189`) |
| 2 | 設計書 **10.1**(`:695`) | **本タスク** | TSK-317 が計画書 `:171` で「**反映なし**」と宣言。**衝突しない** |
| 3 | `backend/tests/db/conftest.py` | **TSK-317**(本タスクは**マージ後に追記**) | TSK-317 が大改造済み。**`Q-4` の順序で衝突が解消する** |
| 4 | `backend/tests/db/environment-expectations.json` | **どちらも触らない** | TSK-317 の合格条件が「**差分 0**」(`plan.md:313`) |
| 5 | **`backend/uv.lock`** + `backend/pyproject.toml` | **本タスク** | TSK-317 の実 diff に含まれない。**ORM 検査が見るのは `backend/uv.lock`**(`:27`) |
| 6 | **`.claude/core-areas.json`** | **本タスク**(TSK-317 の**改訂 3 より前**) | **申し送りに無い衝突面**。**逐次になるので競合しない** |
| **7** | **設計書 10.1** の**後続改訂** | **本タスク → その後 TSK-235** | **TSK-235 も 10.1 を触る**(`domain-calc-dsl/plan.md:136` — `D-7` の 6 機構の行 + 段階的 CI 状態モデル + `consistency`/`e2e` の導入時期)。**本タスクの v1.14 の上に乗る**(同 `:138`「設計書行が TSK-343 の v1.14 に追随済みかを確認してからマージする」) |

#### TSK-235(`feature/domain-calc-dsl`)との共有 4 ファイル(2026-09-10 追記)

**TSK-235 のセッションからの申し送りで判明した**(承認後の追記 — 4-13 節)。
**TSK-235 の計画書 3 節が、本タスクが「本タスクの所有」と宣言した 4 ファイルを同じく変更資産に挙げている**
(`domain-calc-dsl/plan.md:15,17,38-43`)。**相互に無認識だった**(本計画書の TSK-235 参照は 0 件だった)。

| 共有ファイル | 本タスク | TSK-235 | 競合するか |
| --- | --- | --- | --- |
| `tests/test_ci_wiring.py` | ORM スタックの厳密固定検査 + CI コマンド契約(ステップ 1・26) | **CI 配線の追加**(`consistency` ジョブ・変異ジョブ) | **しない**(順序で逐次。**TSK-235 は本タスクの純関数抽出後の形の上に足す前提**) |
| `backend/pyproject.toml` | SQLAlchemy・Alembic の厳密固定(ステップ 1) | `ty` include・`testpaths`・package-data・依存追加 | **しない**(逐次) |
| `backend/uv.lock` | 同上 | 依存追加(property / 変異テスト系・**ライブラリ選定は未決**) | **しない**(逐次)。**ただし lock は行マージ不能**なので、**後から入る側が必ず再生成する**(TSK-235 の合格条件に `uv sync --locked` がある) |
| `.claude/core-areas.json` | **`tenant-isolation.paths`**(4 領域へ登録 — ステップ 28) | **`game-state` + `data-migration`** の配列 | **しない**(**別配列・同一 JSON**。逐次なら競合しない) |

**本タスクの作業は変わらない** — いずれも本タスクが先に入る前提で TSK-235 が計画を書いている。

**開始条件に追加**(レビュー 2 周目 `P1-6`): **TSK-317 の本体 PR がマージされた時点で、
上表 6 箇所の差分を再調査する**(基準が動くため)。

## 4. 実装方針

### 4-1. 重さ分類の根拠 — コア領域(4 領域に重複帰属)

- **`backend/pyproject.toml`・`backend/uv.lock`・`backend/tests/db/*` が `tenant-isolation.paths` に既登録**、
  **`tests/test_ci_wiring.py` が `guard_paths`**(`.claude/core-areas.json:204-206`・`:26`)
  → **core-guard が確実に発火する**
- **意味範囲は 4 領域に跨る** — `sync-protocol` / `recording-rights` / `tenant-isolation` /
  **`data-migration`**(design.md 9 節)。**6.3 規則③が領域間の重複を許す**
- **6.3 の除外(「旧データと接しない新スキーマ内部の通常マイグレーション」)には当たらない** —
  本タスクは**移行の受け皿**(隔離領域・移行世代・移行レポート)を作る
- → **`sol xhigh`・敵対レビュー・人間の逐行確認必須(PR 作成者以外)**。
  **有効確認者**(逐行確認・実施記録行・approve・マージの 4 役の一元担当)が要る(台帳 `H-12` の「承認手続の決着」)

### 4-2. ①②③ を同一コミットにする理由(人間決定 2026-09-09)

Notion タスクが「**② 依存追加は ① と同一コミット必須**」と定める。分けると**依存を足した瞬間に
現行 `tests/test_ci_wiring.py:1272` の `assert {"sqlalchemy","alembic"}.isdisjoint(locked_versions)` が
必ず落ちる red の中間コミット**ができ、6.1 の段階実装(各ステップに合格条件を要求)に対して
**合格条件を書けない状態**が生まれる。

`:1272` は **lock の `[[package]]` 全件**(直接・推移とも)を見ているので、
**推移依存で SQLAlchemy が入っても red**。逃げ道はない。

**裁定 `R-2` とも整合**(`:2394`・`:2752`)。**1 周目・2 周目とも「維持してよい」と評価された。**

### 4-3. 人間の裁定 4 件(2026-09-09・山田正輝)

| ID | 裁定 | 帰結 |
| --- | --- | --- |
| **`Q-1`** | **(A) 設計書自身を `/finalize-doc` で回す**(7.6-3 後段)。**版を v1.14 へ繰り上げる** | **裁定 `R-5` の変更**(4-4 節) |
| **`Q-2`** | **型の境界は本タスクで線を引く** | design.md 1 節。**レビューで層を 3 つに分けた** |
| **`Q-3`** | 典拠のない DoD 2 条件を**維持し「自主基準」と明記する** | 5 節 |
| **`Q-4`** | **マージ順序 = TSK-348 → TSK-317 → 本タスク**(**+ 4 本目に TSK-235** — 4-6 節の追記) | 4-6 節 |

### 4-4. `Q-1` は裁定 `R-5` の変更である(記録)

`R-5`(TSK-335・2026-09-08)は「**版は上げず**、差分を確定ゲートの敵対レビュー対象に含める」という折衷
(`data-model-handoff-revision/plan.md:59`)。**相乗り先は `docs/design/data-model.md` の確定ゲート**
(`data-model-canonical/plan.md:724`)だったが、**TSK-342 が 13 周で通して approved 化したため受け皿が消滅した**。

**`Q-1`(A)は「版は上げず」を外し、7.6-3 後段(版繰り上げ + 7.3 の確定ゲート)を採る** —
`R-5` より**厳しい側**へ寄せた判断。7.6-3 前段と 7.3 本文 `:412` の**調停を明文で行った決定は無い**ので、
**本裁定がその先例になる**。**1 周目・2 周目とも「正しい」と評価された。**

**帰結**:

- 設計書の変更履歴表へ **v1.14** 行、`docs/README.md` の索引を現行化
- **`/finalize-doc` を回す** — `in-review` 化 → 敵対レビュー → 収束 → 人間承認 → `approved`
- **反映周コミットは `反映<r>周目` 記法**(ステップ記法を付けない — 設計書 `:307`)
- **台帳 `H-58` への対処**: 反映周コミットが**非文書パスを含むと現在地導出が「不明」へ縮退する**
  (台帳 `H-58`)。指摘が `tests/` / `backend/` の変更を要求した場合は
  **ステップ表の末尾へ番号付きステップとして起こす**。
  **`/<N>` を付けないのでステップを足しても既存コミットが不整合にならない**

**確定ゲートの実施位置**: **ステップ 3 の完了後**(ステップ 2 で Alembic が動き、ステップ 3 で設定契約が
確定した時点)。**ステップ 2 は `target_metadata` が本番 Base を参照する構造検査と、
test-only metadata に差分を作ると `alembic check` が red になる負例まで含む**
(レビュー 2 周目 `P2-1` — **空の `target_metadata` と no-op revision でも 3 段は通る**ため、
それだけでは配線の証拠にならない)。

### 4-5. 手続き上の影響

- **`backend/pyproject.toml`・`backend/uv.lock`・設定正本テンプレートは NFR-021 証跡の失効対象パス**
  (設計書 `:720`)。**依存追加で既存の受入証跡が失効する。**
- **NFR-014 の「URL リテラル 0 件」は要件の逐語ではない**(要件は「リポジトリに含めず環境変数で管理する」)。
  かつ `.github/workflows/ci.yml` の job env に**接続 URL リテラルが実在する**(run 単位の一時資格情報)。
  → **DoD は「製品コードと Alembic 設定」に範囲を限定して書く**。
- **`SP-06`(`:2604`)と 12-4 の射程外宣言が同一文書内で衝突して読める。**
  **本計画は 12-4 の non-serving 宣言を優先根拠に置く**。字面の差は TSK-348 / TSK-353 へ申し送る。
- **RLS 未適用スキーマの上に製品機能が乗ることを機械的に止める仕組みは不在**(`:2439-2440`)。
  **本タスクはこの穴を塞がない**が、/pr のクローズ処理で運用評価台帳の候補として起票を検討する。

### 4-6. マージ順序(裁定 `Q-4`)

**TSK-348(完了)→ TSK-317 → 本タスク → TSK-235 → (TSK-317 の改訂 3)。**

**第 1 矢は完了した** — TSK-348 は **PR #51 として develop へマージ済み**(2026-09-10・`4ed54fc`)。
**残るのは TSK-317 → 本タスク → TSK-235**。

**4 本目の TSK-235 は 2026-09-10 に追記した**(承認後の追記 — 4-13 節)。
**根拠は既存の人間の裁定**であり、本計画が新たに決めたものではない:

- **Notion TSK-235 の 2026-09-08 のコメント(人間の裁定)**「**実装は ORM タスクのマージ後に開始**」
  (`domain-calc-dsl/plan.md:164` が引用)
- **PO 裁定 2026-09-10** で TSK-235 の**開始条件**として明示された —
  「**TSK-343 の計画書が承認され、裁定 `Q-4`(マージ順序)が確定していること**」(同 `:166,168`)
  → **本計画の承認(2026-09-10)でこの開始条件が満たされた**

- **TSK-348 → TSK-317 は既存の明文の制約**(`data-model-v0-2/plan.md:29`・`:289`)
- **TSK-348 は論理スキーマを変えない**ので、本タスクの**設計内容**は TSK-348 を待たない。待つのは**マージだけ**
- **順序の副作用(有利)**: 本タスクが TSK-317 の後になるので **マージ済み `conftest.py` の上に追記できる** —
  唯一の実衝突が解消する。`.claude/core-areas.json` も TSK-317 の改訂 3 と逐次になる
- **TSK-344** は**本タスクと TSK-317 の両方**が入って初めて通過判定できる(`:2436`)。
  **本タスクのマージが TSK-344 の再実行契機を発火させる**(`:2439`)
- **本タスクの実装着手は TSK-317 の本体 PR のマージ後**。着手時に**衝突 6 箇所を再調査する**(3 節)

### 4-7. 設定正本テンプレートの追随は番号付きステップにする(レビュー 2 周目 `P1-5` の是正)

**編集は Claude / Codex が実施できない**(`.claude/hooks/secret_guard.py` がブロック —
NFR-014 / AGENTS.md 絶対規則 2)。

**1 周目は「手動ゲート・ステップ記法なし」としたが、これは誤りだった** —
**設定テンプレートは `.md` でも feature 文書でもない設定資産**であり、
**無記法コミットが混ざると現在地導出が「不明」に落ちる**(設計書 `:280` 後半)。

→ **是正**: **ステップ 3 として番号付きで扱い、人間がコミットに `(ステップ 3)` を付ける。**

| 事項 | 内容 |
| --- | --- |
| **実施者** | **人間**(セッションで `!` プレフィックス実行、または直接編集) |
| **内容** | **3 キーへ分離**(`PITCHLOG_DATABASE_URL` / `PITCHLOG_MIGRATION_DATABASE_URL` / **`PITCHLOG_DATABASE_POOLED`**)+ `postgresql+psycopg://` の形。**`POOLED` の欠落は 4 周目 `P1-7` の是正** |
| **コミット** | **人間が作る。件名に `(ステップ 3)` を含める** |
| **検証** | 設定契約の**負例 3 種**(design.md 7-3 節: 相互 fallback / `POOLED` 未設定 / `POOLED=true` で `prepare_threshold=None` 欠落)が red になること。**正規化関数は裸の形でも動くので機能要件ではない**が、**3 キーの分離は機能要件**である |

### 4-8. 計画レビューの反映

#### 1 周目(2026-09-09・sol xhigh)— 指摘 18 件を全件採用

`P0` 4・`P1` 11・`P2` 3。**2 周目で 12 件が閉鎖**と評価された。
主要な反映: 型境界の 2 層分割 / `D5` に `NOT NULL` + `MATCH FULL` / 全 FK の複合参照検査 /
`D2` の禁止対象の是正 / schema manifest 新設 / `D3` トリガの補償テスト強化 / core-areas 4 領域登録 /
`/<N>` を付けない / `ci.yml` の宣言追加 / `D4` と FR-018 をやらないことへ / カタログ検査へ置換 /
DoD 対応表 / 5.1 の規範本文へ理由 / 最小 revision / 一意制約の分類。

#### 2 周目(2026-09-10・sol xhigh)— 指摘 14 件を全件採用

`P0` 3・`P1` 8・`P2` 3。不採用 0 件。

| ID | 指摘 | 反映 |
| --- | --- | --- |
| **`P0-1`**(1 周目 `P0-1` の未解決) | 型境界に**「移行イベントの原本ペイロード」層が無い**。`保持のみ` の列(例: 列 72 — `:2256`「互換出力のため」)は**保存先が要る**のに、B4 が原本列を禁止するだけで**保存先の有無を検査していない**。移行元最終オーダーの解決列は **12-3 節の移行元最終オーダーの「列」(裁定 `R-1`)** で**任意**なのに必須化していた | **層を 3 つに分けた**(design.md 1-2 節)。**層 (ii) を新設**し**行き先区分との exact-set 照合**で保存先を検査。**必須ペアを列 47・55 に限定**し、**最終オーダーの解決列を作らない**(やらないこと表へ) |
| **`P0-2`** | **削除系統 exact-set が追記専用ログを論理削除可能にする** — 管理者操作ログは「保持は無期限」「**追記のみ(論理削除の対象にしない)**」(`:1642-1643`) | manifest のライフサイクルを**判別共用体**にした(`4 系統` \| **`削除対象外・追記専用`**)。**削除列を持たせず `UPDATE`/`DELETE` を拒否**し、構造・挙動・downgrade を検査(design.md 3-4 節) |
| **`P0-3`** | **CI 3 段を追加すると既存 CI 配線検査が必ず落ちる** — `tests/test_ci_wiring.py:577-585` が backend のコマンド列を**既存 6 件と完全一致**要求している | **`ci.yml` の 3 段追加と同一ステップで検査の契約も更新する**(design.md 8-3 節)。**期待列を「既存 6 件 + Alembic 3 件」の完全一致**にし、**負例 4 種**(欠落・重複・順序違い・別ジョブ追加) |
| **`P1-1`**(1 周目 `P1-2` の未解決) | **manifest は正本に対する全数オラクルではない** — 同じ表を manifest と実装の両方から落とすと green。人手転記の腐敗経路が残る | **manifest を正本へ機械的に束縛した**(design.md 3-2 節): **要求 ID と正本節の保持** / **`data-model.md` の blob SHA 束縛(変われば `stale` で red)** / **正本の 2 表(業務的一意性全数表・行き先区分表)をパースして exact-set 照合**(実 DB を経由しない独立経路)/ **機械で閉じない範囲は `手動転記` と明示**して逐行確認へ |
| **`P1-2`**(1 周目 `P1-11` の未解決) | **D14 は「全構造要求」を閉じない** — manifest にペイロード契約・トリガ・追記専用性・`MATCH`・default 値・述語の全文が無かった | **manifest の項目を拡張**(`MATCH`・削除動作・**述語の全文**・列順・**不変列マトリクス**)。**D14 を「カタログで表現可能な構造集合の一致」に狭め**、**D15(不変列)・D16(ペイロード契約)を分離**(5 節) |
| **`P1-3`**(1 周目 `P2-3` が生んだ矛盾) | **一意制約の排他的 3 分類が破れる** — イベントスロットの PK(**3-4 節の全数表のイベントスロット行(`M07` の FK 参照先)**)は**業務的一意 27 行の 1 行かつ主キーかつ `M07` の FK 参照先** | **非排他的な役割タグ**へ変更(`business_unique` / `primary_key` / `fk_target`)。**27 行照合は `business_unique` タグの集合で行う**(design.md 3-3 節) |
| **`P1-4`**(1 周目 `P1-6` の未解決) | **direct/pooler 分離が設定契約まで届いていない** — キー名・消費者・fallback 禁止が未確定で、同じ設定を両方が読む実装でも受入可能 | **2 キーを確定**(`PITCHLOG_DATABASE_URL` / `PITCHLOG_MIGRATION_DATABASE_URL`)+ 消費者・未設定時の扱い・**相互 fallback 禁止**+ **負例 3 種**(design.md 7-3 節) |
| **`P1-5`** | **手動ゲートの無記法コミットが現在地を「不明」にする** | **ステップ 3 として番号付きにし、人間コミットに `(ステップ 3)` を付ける**(4-7 節) |
| **`P1-6`** | **TSK-317 との順序が同一計画内で逆転している** | **全記述を `TSK-348 → TSK-317 → 本タスク` に統一**。**着手時に衝突 6 箇所を再調査**することを開始条件へ(3 節・4-6 節) |
| **`P1-7`** | **不変列の強制機構が `D3` 以外で閉じていない** — `D5` は「トリガまたは権限」と曖昧、**チームレコードの `kind` も不変**(**7-1 節のチームレコードの可変性(`kind` は不変)**)なのに強制が無い | **不変列マトリクス**を新設(design.md 5 節)。4 表(記録権世代 / `D5` 台帳 / チームレコード / 管理者操作ログ)の**保護列と許可更新列**を manifest に持ち、**トリガに確定**して `D3` と同水準で検査 |
| **`P1-8`**(1 周目 `P1-9` の未解決) | **FR-018 の受取先が存在しない** — 「起票を検討」では所有が確定しない | **4-10 節の承認前条件にした**(起票して本計画からリンクする) |
| **`P2-1`**(1 周目 `P2-2` の未解決) | **最小 revision の検証が空疎** — 空の `target_metadata` と no-op revision でも 3 段は通る | ステップ 2 に **`target_metadata` の構造検査**と **test-only metadata の差分で `alembic check` が red になる負例**を追加 |
| **`P2-2`** | **隔離領域の「制約 0 件」と ORM mapping の両立方法が未定** | **「raw ペイロードの内容へ意味制約・FK・CHECK を掛けない」に限定**し、**取り込み単位識別子と技術的 PK は許す**(design.md 1-6 節) |
| **`P2-3`** | **ステップ 8・11・13・16 が 1 論理変更として大きすぎる** | **4 ステップを 8 ステップへ分割**(ステップ表。`/<N>` を付けていないので分割で既存文法が壊れない) |

#### 3 周目(2026-09-10・sol xhigh)— 指摘 10 件

`P0` 2・`P1` 5・`P2` 3。**レビュー自身の内訳申告は「設計が破れるもの」8 件 / 「残滓」2 件。**
**2 周目の 14 件のうち 7 件が閉鎖・3 件が部分解決・3 件が未解決・1 件が「妥当だが未履行」。**

**4-11 節の収束方針(述語を弱める)に従って処理した。**

| ID | 指摘 | 処理 |
| --- | --- | --- |
| **`P0-1`**(2 周目 `P0-1` の未解決) | 型境界の層 (ii) の列単位の網羅が閉じていない | **弱めた** — **区分の集合の一致までを機械で閉じ、列単位は TSK-250 の写像確定後へ送る**(design.md 1-5 節 B・10 節 `N2`) |
| **`P0-2`** | **CI に `alembic` を足しても migration URL を供給していない** | **是正した** — `ci.yml` の job env へ **`PITCHLOG_MIGRATION_DATABASE_URL`** を追加(design.md 7-3 節末) |
| **`P1-1`**(2 周目 `P1-1` の未解決) | **manifest は正本に対する全数オラクルではない**(同じ表を両方から落とすと green) | **弱めた** — **「全数オラクル」という主張を降ろし**、機械で閉じる範囲(2 表のパース照合)と閉じない範囲(表の全数性)を分けて明示(design.md 3-2 節・10 節 `N1`) |
| **`P1-2`**(2 周目 `P1-7` の未解決) | 不変列マトリクスの全数性が閉じていない | **弱めた** — **4 表を「本計画が拾う範囲」と明示**し、全数性は**語の機械抽出 + 逐行確認**で閉じる(design.md 5 節・10 節 `N3`) |
| **`P1-3`** | ライフサイクルの割り当ての全数性が閉じていない | **弱めた** — **5 値は「本計画が拾う範囲」**。割り当ての正しさは**突合シート + 逐行確認**(design.md 3-4 節・10 節 `N4`) |
| **`P1-4`**(2 周目 `P1-4` の部分未解決) | **2 キーの同値禁止と pooler の識別方法が未定** | **是正した** — **`PITCHLOG_DATABASE_POOLED` を明示設定にし、URL からの自動判定をしない**。**2 URL キーの同値を負例にした**(design.md 7-3 節) |
| **`P1-5`** | **スタメンの正本が 3 区分**(スタメン時 / 途中交代時 / 出場区間 = 導出。`:1442`)なのにステップ 7 が一括していた | **是正した** — ステップ 7 を 3 区分の検査に分け、**出場区間に複製列を置かない**ことを合格条件へ |
| **`P2-1`**(2 周目 `P2-3` の部分未解決) | ステップ 20 が横断的 / ステップ 5 の全表条件が空疎 | **是正した** — **取り込み識別子と退役列を各表の作成ステップへ移し、全数 audit だけをステップ 23 へ**。**ステップ 20 を 2 つに分割** |
| **`P2-2`** | 残滓: 「3 層」の表に第 4 層がある | **是正した** — `(iv)` を**層外の処理**へ改称し、層番号は `(i)`〜`(iii)` に限定 |
| **`P2-3`** | 残滓: Notion DoD 対応が「1 対 1」でない | **是正した** — 見出しを「**全数対応**」に変更 |

#### 4 周目(2026-09-10・sol xhigh)— 指摘 10 件を全件採用

`P0` 1・`P1` 7・`P2` 2(申告内訳は「破れ」8 件 / 「残滓」2 件)。
**レビューは 4-11 節の収束方針そのものを「妥当」と評価し、
「より強い exact-set への復帰は要求しません」と明記した** — 残りは**新機構の具体的な穴**である。

| ID | 指摘 | 反映 |
| --- | --- | --- |
| **`P0-1`** | **突合シートの完了条件が「未判定行なし」だけで、差分が出たときの処理と是正権限が無い**。人間が欠落を見つけても確認済みにして `D18` を通せる | **判定区分を `一致` / `差分` / `対象外`(理由と典拠が必須)に固定**し、**`D18` を「未判定 0 かつ未解消の差分 0」**へ。**差分は末尾へ番号付き是正ステップを追加して解消し、シートを再生成して再判定**(design.md 10-1 節) |
| **`P1-2`** | **`N3` の抽出語が既知の不変要求を漏らす** — 正本の**システム固定語彙は「変更不可」**(**10-3 節の 3 層表のシステム固定行**) | **抽出語に「変更不可」「更新しない」「上書きしない」を追加**し、**語彙のシステム固定層を不変列マトリクスへ追加**(4 表 → **5 表**)。ステップ 14 の合格条件にも追加 |
| **`P1-3`** | **ライフサイクルの 5 値では全表を分類できない**(`D5` 台帳は退役状態を更新でき、4 系統でも追記専用でもない)。**「人手転記だから」は閉じない理由になっていない** | **3 つの独立した軸へ分離**(業務オブジェクトの削除系統 / 追記専用性 / 移行バッチ退役)+ 削除系統に **`対象外` / `親に従う`** を追加。**`N4` の理由を「**11-1 節の削除 4 系統の表** は業務オブジェクトへの割り当て表で物理表の全数写像を持たないため」へ訂正**し、**突合の対象を同表が挙げる業務オブジェクトに対応する表だけに限定** |
| **`P1-4`** | **`N5` の受け取り先が存在しない**(FR-031 は **Must** なのにタスク ID・開始条件・DoD が無く、`D18` と自己矛盾) | **4-10 節の承認前条件に追加**(FR-018 と同型。**未実施**) |
| **`P1-5`**(3 周目 `P1-5` の未解決) | **出場区間の背番号列を逆に禁止していた** — **7-3 節の保持場所表の出場区間(投影)行** は「**投影行にも背番号を持つが、それは導出値である**」 | **ステップ 7 を是正**。**出場区間は導出値として背番号を持つ**。合格条件は「正本はスタメン/交代イベント側」「そこから再生成できる」「出場区間だけを正本として更新する経路が無い」 |
| **`P1-6`**(3 周目 `P1-4` の是正が生んだ破れ) | **有効な direct/direct 構成を拒否していた** — アプリ URL は direct も許されるので**同値は pooler の証拠にならない** | **無条件の同値禁止を削除**。**`POOLED=false` + 同一 direct URL を正例**にし、**負例を 3 種へ限定**。migration の direct 性は **CI の明示 URL と運用設定の確認**で担保 |
| **`P1-7`** | **`PITCHLOG_DATABASE_POOLED` が人間編集手順と最終 DoD に届いていない** | **4-7 節の編集内容へ追加**し、**負例を 3 種へ統一**、**`D3` に 3 キーを記載** |
| **`P2-1`**(3 周目 `P2-1` の未解決) | **取り込み識別子・退役属性が各作成ステップの合格条件へ移っていない**(ステップ 5 の将来宣言と横断 audit にしかない) | **「表を作るステップ(6〜22)の共通の合格条件」を新設**し、**各表の作成コミットで同時に適用**することを要求。**横断 audit は追加修正を行わない検査に限定** |
| **`P2-2`** | 残滓: 「5 事項の突合シート」と「3 シート + 2 受取先」が不一致 | **「`N1`・`N3`・`N4` の突合シート + `N2`・`N5` の受取記録」に統一** |
| **`P2-3`** | 残滓: 反映履歴の audit のステップ番号が古い(22 → 23) | **訂正した** |

**3 周目の `P0-2`(CI の migration URL)・旧 `P2-2`(第 4 層表記)・旧 `P2-3`(1 対 1 表記)は解消済みと評価された。**

#### 5 周目(2026-09-10・sol xhigh)— 指摘 6 件を全件採用

**(a) 実行を止める破れ 2 件 / (b) 射程・所有の穴 3 件 / (c) 残滓 1 件。**
**指摘総数の推移: 18 → 14 → 10 → 10 → 6。**

| ID | 区分 | 指摘 | 反映 |
| --- | --- | --- | --- |
| **`P0-1`** | (a) | **判定区分に適格条件が無く、`対象外` を対象内の欠落に使って `D18` を通せる** | **区分の適格条件を追加**(`一致` = 正本側と実装側の両方を特定できる / `差分` = 欠落・余剰・内容不一致 / **`対象外` は当該 `N` の対象・範囲の外にある生成候補だけ。対象内の欠落に使うことを禁止**)+ **完了条件へ「不適格な `対象外` 0」**(design.md 10-1 節) |
| **`P1-1`**(4 周目 `P1-3` の未解決) | (a) | **3 軸へ直したのに、ステップ 5・16 と manifest 項目に旧「判別共用体」が現役で残っていた** — `D5` 台帳を表せない旧構造を実装し得る | **ステップ 5・16 と design.md 3-5 節を 3 フィールドへ統一**し、**`削除対象外・追記専用` という複合値を現役規定から除いた** |
| **`P1-2`** | (b) | **`N3` の抽出語が **5-3 節「既存イベントを書き換えない」**「既存イベントを書き換えない」を拾わない** — 操作イベントの不変性の所有判定が証跡に現れない | **抽出語へ「書き換えない」を追加**し、**ステップ 9 で「DB トリガによる全列保護」か「アプリ層所有として受け取り先へ送る」かを判定・記録**する合格条件を追加 |
| **`P1-3`**(4 周目 `P1-4` の未解決) | (b) | **`N5` の受け取り先が依然として未起票**(承認前条件の未履行) | **[TSK-356](https://app.notion.com/p/3d693b75e687810fb8abf3426efe51b6) を起票**し、ID・開始条件・受入条件を記録(4-10 節) |
| **`P1-4`**(4 周目 `P1-6` の反映で生じた穴) | (b) | **migration URL の direct 性を「運用設定の確認」へ送ったが、証跡形式・実施者・受取先が無い**。`N1`〜`N6` にも含まれていない | **`N6` を新設**(運用環境の migration endpoint の direct 性)。**秘密値を記録しない形の手動証跡**(実施者・確認時点・endpoint 種別のみ。**URL 自体は書かない** — NFR-014)。**`D3` の対象を「コード配線と CI 環境」に限定** |
| **`P2-1`** | (c) | 残滓: 3 キー契約に旧「2 キー」表記が残る | **「3 キー(2 つの URL + pooler フラグ)」へ統一** |

#### 6 周目(2026-09-10・sol xhigh)— 指摘 9 件のうち 7 件を反映し、**打ち切った**

**(a) 実行を止める破れ 2 件 / (b) 射程・所有の穴 4 件 / (c) 残滓 3 件。**
**指摘総数が 6 → 9 へ戻った**(推移: 18 → 14 → 10 → 10 → 6 → 9)。

**反映した 7 件**:

| 区分 | 指摘 | 反映 |
| --- | --- | --- |
| (a) | **是正ステップを「末尾へ追加」と定めたため、是正で新設した資産が `core-areas.json` に未登録のまま残る**(台帳 `H-12`「コードを入れたのに paths が未充填で非発火」の再発型) | **是正ステップは core-areas 登録のステップより前に挿す**。**是正が資産を新設したら core-areas 登録をその後に置き直す**(design.md 10-1 節) |
| (b) | **`N6` の受け取り先が不確定**(`github-setup.md` は GitHub 設定と CI secrets の手順書で migration endpoint の確認手順ではない) | **証跡の保存先 = PR の実施記録行**・**判定根拠 = pooler エンドポイントでないことの運用者確認**(**URL と資格情報は書かない** — NFR-014)・**実施責任者 = 有効確認者**・**不適合時 = 当該環境への適用保留**。**`github-setup.md` へは送らない** |
| (b) | **`N2` の証跡形式・判定条件・差分時の遷移が無い** | **対象 = 88 列の全数**・**完了条件 = 各列のペイロードキーが写像 JSON に記録され区分と矛盾しないこと**・**差分は TSK-250 の確定ゲートで解消し本タスクへ差し戻さない** |
| (b) | **投影行 ID の安定性が実装条件にも受け取り先にも無い**(**12-3 節「投影行 ID の安定性」** は再投影での同一性を要求) | **ステップ 10 へ追加** — **旧行識別子から決定的に導ける形**と**再投影テスト** |
| (c) | 残滓: 「判別共用体」の旧名称が節見出しと計画概要に残る | **「ライフサイクルの 3 独立軸」へ統一** |
| (c) | 残滓: `N6` 追加後も「5 事項」「`N1`〜`N5`」が残る | **「6 事項(`N1`〜`N6`)」へ統一** |
| (c) | 残滓: 3 キー契約に旧「2 キー」表記が残る | **「3 キー(URL 2 つ + pooler フラグ)」へ統一** |

**未反映の 2 件(申し送り — 4-12 節)**: **(a) 1 件・(b) 1 件**。
**レビュー出力の末尾のみを保存したため本文を取り落とした**(Claude 側の保存ミス)。
**推測で修正すべきでないため未着手のまま残す。**

### 4-12. 打ち切りの記録と申し送り(人間決定 2026-09-10)

**計画レビューを 6 周で打ち切った。** 指摘総数の推移は **18 → 14 → 10 → 10 → 6 → 9**(全件採用・不採用 0 件)。
**6 周目で件数が戻り、収束していない。**

**打ち切りの根拠**:

- **`P0` は 4 → 3 → 2 → 1 → 1 → (a)2 と低位で推移**しており、**計画の骨格**
  (射程・所有境界・28 ステップ・DoD の対応・受入証跡の枠組み)**は 6 周を経て安定している**
- **残る指摘は「細部の整合」と「正本の散文から属性を拾い切る作業」**であり、
  **実スキーマという具体物に当たる実装ステップの合格条件で拾うほうが確実**である
- **6 周目の残滓 3 件はすべて Claude の改名の伝播漏れ**で、**検査層(計画書)自体が欠陥源になっている**
  — 台帳が記録する「**検査層が欠陥源になる輪**」(`harness-evaluation.md`)。
  **レビューは 5 周を通じて Claude の反映そのものの誤りを 3 回検出した**
  (出場区間の背番号を逆に書いた / 有効な direct 構成を拒否した / 是正ステップの順序で core-areas を飛ばす)
- **実装着手は TSK-317 の本体 PR のマージ後**(裁定 `Q-4`)なので、**待ち時間に人間の逐行確認を充てられる**

**申し送り(実装時に拾う)**:

| # | 事項 | 拾い方 |
| --- | --- | --- |
| **S-1** | **6 周目の未反映 2 件**((a) 1・(b) 1)。**本文不明**(Claude の保存ミス) | **実装着手時に計画レビューを 1 周だけ回し直して同じ指摘を再取得する**。**着手は TSK-317 マージ後なのでその時点で実施できる** |
| **S-2** | **正本の散文から不変性・ライフサイクル・必須属性を拾い切る作業** | **`N1`・`N3`・`N4` の突合シート**(ステップ 27)。**`差分` は番号付き是正ステップで解消し、core-areas 登録より前に挿す** |
| **S-3** | **Claude の反映誤りの再発**(正本と逆を書く / 有効な構成を拒否する型) | **コア領域なので人間の逐行確認が必須**(PR 作成者以外)。**逐行確認では「正本と逆になっていないか」を明示の観点にする** |
| **S-4** | **`SP-06`(`:2604`)と 12-4 の射程外宣言の字面の衝突** | **TSK-348 / TSK-353 へ申し送る**(本タスクでは `data-model.md` を触らない) |
| **S-5** | **RLS 未適用スキーマの上に製品機能が乗ることを機械的に止める仕組みが不在** | **/pr のクローズ処理で運用評価台帳の候補として起票を検討** |
| **S-6** | **DB テストが強制終了されると teardown が飛び、使い捨てクラスタのコンテナと `pitchlog_test_role` が残る**。**ロールの残存は fixture が fail-closed で弾くため以後の全 DB テストが error になる**。**復旧手順はどの正本にも書かれていない**(復旧 = `docker rm -f` と `DROP OWNED BY` → `DROP ROLE`)。**TSK-317 が 2026-09-10 に 3 回踏んだ** | **本タスクは DB テストを多用する**(ステップ 2・9〜26)。**踏んだら上記手順で復旧する**。**運用評価台帳の候補として /pr のクローズ処理で起票を検討**(復旧手順の正本化)。**もう 1 つの罠(TSK-317 が実測)**: **`ALTER TABLE ... OWNER TO` を含む DDL は、対象表へ触った接続が 1 つでも `idle in transaction` で残っていると永久に待つ**(ACCESS EXCLUSIVE 待ち。**読み取りだけの接続でも同じ**)。TSK-317 は 1 点 10 分 48 秒の「処理時間」が実は待ち時間だったと `pg_blocking_pids` で確定した。**診断**: `pg_stat_activity` で `pg_blocking_pids(pid)` が空でない行を探し、`state = idle in transaction` が犯人。**migration を書く本タスクに直接効く** |
| **S-7** | **`feature_status.py` の縮退** — **develop 取り込みマージを入れると、ステップコミットが 1 本も無い段階で現在地が「不明」へ落ちる**。設計書 6.1 は develop 取り込みマージを「**正当(記法不要)**」と定めているのに、`derive_progress` の最終ガード `any(kind not in {"planning","documentation"})` が **`merge_allowed` を弾く**(`scripts/feature_status.py:998-1000`)。**同型が `feature/data-model-canonical` でも発生中**(マージ `128be83` を持ち「実装状況: 不明」) | **表示の縮退であり実害は無い**(ステップ 1 のコミットが入れば復帰する見込み)。**運用評価台帳の候補として /pr のクローズ処理で起票**。**台帳 `H-58` と同型**(反映周コミットの縮退)だが**別事象**(こちらは develop 取り込みマージ)。**分類漏れであって仕様ではない根拠**(TSK-235 が追認): **同じ関数のすぐ上に `note="許可されないマージコミット混在"` 専用の分岐が別にある** — **「許可されないマージ」を名指しで弾く分岐が既にある以上、許可マージは通す設計意図だった**と読める。**行番号参照を安定参照へ寄せる前例**は TSK-278 が確立済み(`adr003-display-privatives` ではなく `docs/features/adr003-display-primitives/plan.md` 6 節の「機械条件の判定方法」表に**「行番号参照 0 件」型**があり、provenance を節番号・条項 ID で持って**文言存在**で実在検査する)。**台帳へ出すときはこの前例を引く** |

### 4-13. 承認後の追記(2026-09-10)

**承認(2026-09-10)後に判明した事実を追記した。射程・ステップ・DoD は変えていない。**

| # | 追記 | 契機 |
| --- | --- | --- |
| 1 | **引用の基準の明記**(本書冒頭の引用注記)+ **実装ステップ表と DoD の `data-model.md` 引用 20 件を節番号 + 要求 ID へ貼り替え** + **台帳引用を `H-nn` へ寄せ**。**develop を取り込んだ**(`4ed54fc`) | **TSK-348 が PR #51 でマージされ `data-model.md` が v0.2 になった**ため、v0.1 基準の行番号引用が +2〜+5 ずれた |
| 2 | **共有 4 ファイルの所有表(TSK-235)と設計書 10.1 の後続改訂**(3 節)+ **`Q-4` の 4 本目に TSK-235**(4-6 節) | **TSK-235 のセッションからの申し送り**。**本計画書の TSK-235 参照が 0 件で相互に無認識だった**。順序の根拠は**既存の人間の裁定**(2026-09-08 / PO 裁定 2026-09-10) |
| 3 | **申し送り `S-6`(DB テストの強制終了後の復旧手順)**(4-12 節) | **TSK-317 が今日 3 回踏んだ**という共有。**復旧手順がどの正本にも無い** |
| 4 | **申し送り `S-7`(`feature_status.py` の縮退)**(4-12 節) | **develop 取り込みマージを入れた直後に現在地が「不明」へ落ちた**(本セッションで実測) |

### 4-9. 詳細設計(design.md へ分離)

| # | 決定 | design.md |
| --- | --- | --- |
| 1 | **型境界を 3 層に分ける** — 隔離領域 raw(恒等写像が正解)/ 移行イベントのペイロード(保存先の網羅)/ 正規スキーマの原本・解決列(列 47・55 のみ必須) | 1 節 |
| 2 | **置き場** — models / migration / manifest | 2 節 |
| 3 | **manifest を正本へ追跡可能に束縛**(要求 ID・blob SHA・正本 2 表のパース)。**「全数オラクル」とは主張しない**。**一意制約は非排他の役割タグ**。**ライフサイクルは 3 独立軸** | 3 節 |
| 4 | **`D5` の強制機構** — `NOT NULL` + `CHECK` + **`MATCH FULL` の複合 FK**。保証は**高々 1 件** | 4 節 |
| 5 | **不変列マトリクス**(**5 表**)+ トリガ + 構造・挙動・downgrade の検査 | 5 節 |
| 6 | **ADR は起票しない**。**5.1 の規範本文に理由と却下案を残す** | 6 節 |
| 7 | **設定契約を 3 キーに分離**(2 つの URL + pooler フラグ・相互 fallback 禁止)+ URL 正規化 | 7 節 |
| 8 | **機械検査の新形** + **CI コマンド契約の更新** | 8 節 |
| 9 | **core-areas.json へ 4 領域へ重複登録** | 9 節 |
| 10 | **機械で閉じない範囲(`N1`〜`N6`)と受入証跡** — 収束方針の実体 | **10 節** |

**計画レビュー 4 周目の重点検証観点**(4-11 節の収束方針に照らして見てほしい点):

1. **design.md 10 節の 6 事項(`N1`〜`N6`)が「弱めた範囲」を漏れなく覆っているか。**
   弱めた主張(表の全数性 / 列単位の写像 / 不変列の全数性 / ライフサイクル割り当て)に対して、
   **受入証跡の形と受け取り先が全部与えられているか**
2. **弱め方が「逃げ」になっていないか** — 各箇所に「なぜ機械で閉じないか」の理由が付いているか
3. **弱めていない箇所**(`P0-2` / `P1-5` / `P2-1` / 残滓 2 件)**の是正が正しいか**

### 4-10. 承認前条件 — **2 件とも履行済み**

- [x] **FR-018 の「具体的なロック方式・SQL」の受け取りタスクを起票し、本計画からリンクした**
      — **[TSK-354](https://app.notion.com/p/3d693b75e68781748725cab8ecc14780)**
      「選手非表示化(FR-018)の削除可否判定 — ロック方式と同一トランザクションの実装」
      (**2026-09-10 起票**・`未着手`・優先度 中)。
      **開始条件 = 本タスクのマージ**、**阻止関係 = 選手削除経路の実装を TSK-354 の完了まで開始しない**。
      TSK-343 側にも相互リンクのコメントを記録した。

- [x] **`N5`(FR-031 互換出力の 88 列往復)の受け取りタスクを起票し、本計画からリンクした**
      (レビュー 4 周目 `P1-4` / 5 周目 `P1-3`)—
      **[TSK-356](https://app.notion.com/p/3d693b75e687810fb8abf3426efe51b6)**
      「88 列互換出力(FR-031)の往復検証 — 『保持のみ』の列が復元できることを閉じる」
      (**2026-09-10 起票**・`未着手`・優先度 中)。
      **開始条件 = TSK-343 と TSK-250 の完了**、**受入条件 = 88 列の往復**(列名・順序・値の復元)。
      **`N5` は「互換出力の実装タスクへ送る」と書くだけでタスク ID が無く、`D18` と自己矛盾していた。**

### 4-11. 収束方針 — 述語を弱める(人間決定 2026-09-10)

**計画レビューが 3 周で収束しなかった**(指摘 18 → 14 → 10。`P0` は 4 → 3 → 2)。
3 周目のレビュー自身の総括は「**正本に対する独立した全数集合がまだ作れていない** —
3 層、manifest、不変列、ライフサイクルの各定義を**増やしただけでは閉じず**、
正本から生成した期待集合または明示的な逐行受入証跡が必要」だった。

**これは運用評価台帳が記録している既知のループ型である** —
`harness-evaluation.md` の TSK-335 の項が「**強い述語が細部を無限に呼ぶ輪**」と
「**述語を弱めることが収束手段になった 1 例目**」を実測として記録している
(同タスクの計画レビューは 8 → 11 → 9 で収束せず 2 度打ち切られた)。
**本計画は毎周より強い exact-set 要求を足すことで新たな細部指摘を呼んでいた。**

→ **人間決定(2026-09-10)**: **述語を弱めて収束させる。**

| 方針 | 内容 |
| --- | --- |
| **機械検査の射程を限定する** | **「カタログで表現可能な構造集合」+「正本の機械可読な 2 表(業務的一意性全数表・行き先区分表)」までを機械で閉じる** |
| **閉じない範囲を隠さず列挙する** | **design.md 10 節に 6 事項(`N1`〜`N6`)を明示**し、**受入証跡の形(対象・範囲・方法)と受け取り先を必ず与える** |
| **突合シートは機械生成する** | 人間の目視を数十件以上の逐行に晒さない。**シート生成は機械・判定は人間** |
| **「全数オラクル」という主張を降ろす** | manifest は期待値と追跡可能性を持つが、**全数性の保証は主張しない**(3-2 節・design.md) |
| **主張を弱めた箇所は理由を書く** | 弱めた各箇所に「なぜ機械で閉じないか」を明記する(**逃げと区別できる形にする**) |

**弱めなかったもの**(具体的で実行を止める指摘は**そのまま是正した**):
`P0-2`(CI の migration URL 欠落)/ `P1-5`(スタメンの正本 3 区分)/
`P2-1`(取り込み識別子と退役列を各表の作成ステップへ)/ 残滓 2 件。

### 実装ステップ(コミット単位 — 設計書 6.1 段階実装)

**全ステップにステップ記法が必要**(`backend/` のコード・`tests/`・設定資産を触るので文書系・計画系のどちらでもない
— 設計書 `:280` 後半)。**`/<N>` は付けない**(任意。反映周や分割で総数が動くと既存コミットが不整合になる)。
件名は **`(ステップ k)`** の形。

**共通の合格条件**(全ステップ): ルートで `uv run ruff check .` / `uv run ty check` / `uv run pytest tests/`、
`backend/` で `uv run ruff format` / `uv run ruff check --fix` / `uv run ty check` / `uv run pytest` が green。

**表を作るステップ(6〜22)の共通の合格条件**(レビュー 4 周目 `P2-1` の是正 —
**各表の作成コミットで同時に適用し、後付けにしない**):

- **テナントに属する表は `tenant_id NOT NULL` とテナント内一意・複合 FK を同時に持つ**
- **移行が行を作り得る表は取り込みバッチ識別子を同時に持つ**(`:2358`「**全表へ通す**」。
  **後から列を足すと全表 DDL 変更になる**)
- ****12-3 節の不変条件 5(退役述語)** の 7 件に当たる表は退役述語を同時に持つ**
- **ライフサイクルの 3 軸**(削除系統 / 追記専用性 / 移行バッチ退役)**を manifest へ同時に記入する**
- → **ステップ 23〜25 の横断 audit は「これらが全表で揃っていること」を検査するだけで、追加修正は行わない**

| # | ステップ(何を作るか) | 合格条件 |
| --- | --- | --- |
| 1 | **設計書 5.1・10.1 の改訂 + 依存追加 + 機械検査の更新**(4-2 節により**同一コミット必須**) | 設計書に「ORM は入れない」が **0 件** / 5.1 に「設計フェーズで最終確定」が **0 件** / **5.1 の規範本文に採用理由と却下案 3 件** / 変更履歴の最大版が **v1.14** かつ `docs/README.md` と**一致**(`scripts/check_docs_status.py` green)/ lock の `sqlalchemy`・`alembic` が pyproject の固定版と一致し **major = 2** / **負例 5 種で red** |
| 2 | **Alembic の骨組み・URL 正規化・最小 revision・`target_metadata` の配線** | 裸 → `postgresql+psycopg://` / 既に正しい形は不変 / **別ドライバ明示は拒否** / `dialect.driver == "psycopg"` / **実接続 green** / **空 DB へ `upgrade head` 成功** / `current --check-heads` が単一 head / **`alembic check` の差分 0** / **`target_metadata` が本番 Base を参照する構造検査** / **負例: test-only metadata に差分を作ると `alembic check` が red** / **負例: 正規化を外すと driver が `psycopg2` で red** |
| 3 | **設定契約の 3 キー分離**(design.md 7-3 節)。**設定正本テンプレートの編集は人間が実施**(4-7 節) | `PITCHLOG_DATABASE_URL` はアプリの engine のみが読む / `PITCHLOG_MIGRATION_DATABASE_URL` は `env.py` のみが読む / **`PITCHLOG_DATABASE_POOLED` を明示設定にし URL から自動判定しない** / **3 キーとも未設定時に失敗する** / **正例: `POOLED=false` で 2 つの URL が同一の direct URL でも通る**(4 周目 `P1-6` — 同値は pooler の証拠にならない)/ **負例 3 種で red**(相互 fallback / `POOLED` 未設定 / `POOLED=true` なのに `prepare_threshold=None` が無い) |
| — | **⟨設計書 v1.14 の確定ゲート⟩** `/finalize-doc docs/development/dev-harness-design-2026-08-07.md`(4-4 節)。**反映周コミットは `反映<r>周目` 記法**。非文書パスの変更を要求する指摘は**末尾へ番号付きステップとして起こす** | 敵対レビューが 7.3-2 の終端遷移で収束 / **人間承認** / 設計書 frontmatter が `approved` / `確定ゲート周回` を反映周ごとに +1 |
| 4 | **スキーマ契約 manifest の新設**。`contracts/db/schema-manifest.json` を **`data-model.md` から人手で起こし、正本へ機械的に束縛する**(design.md 3 節) | manifest の各要素が**正本節と要求 ID** を持ち**全件解決する** / **`data-model.md` の blob SHA を記録**し**変わると `stale` で red** / **業務的一意性全数表(**3-4 節の業務的一意性全数表**)と行き先区分表(**12-1 節の行き先の区分**)をパースして該当集合と exact-set 一致** / **機械で閉じない範囲が `手動転記` と明示されている** / 突合テストが**未実装の表を「欠落」として red にする** |
| 5 | **models の基盤・領域別分割・不変列マトリクスの器** | `ty check` green / **テナント列 mixin・ライフサイクルの 3 フィールド**(**削除系統**〔`ゴミ箱`/`非表示`/`無効化`/`終了・離脱`/`対象外`/`親に従う`〕・**追記専用性**・**移行バッチ退役**)**・取り込みバッチ識別子 mixin・退役述語 mixin が存在し、単体で検査される**(**`削除対象外・追記専用` という複合値は使わない** — 5 周目 `P1-1`) / **モジュールが 4 領域に対応して分かれている**。**この時点で「全表が〜を持つ」型の検査は置かない**(対象表がまだ無く検査が空疎になる — レビュー 3 周目 `P2-1`)。**各 mixin の適用は以降の各表の作成ステップで行い、全数 audit はステップ 23 に置く** |
| 6 | **テナント・チームレコード・選手・在籍区分**(7 章系)+ migration | `UNIQUE (tenant_id) WHERE kind = 'self'` / **背番号に一意制約が 0 件**(FR-015)/ 背番号の原本事実列が `text` / **チーム名に一意制約が 0 件** / **`kind` の更新が拒否される**(**7-1 節のチームレコードの可変性(`kind` は不変)** — 不変列マトリクス) |
| 7 | **試合・スタメン記憶・背番号スナップショット・出場区間**(5-1 / 6-2 / 7-4 / **7-3 節**)+ migration | スタメン記憶 `UNIQUE (tenant_id, チームレコード) WHERE 退役していない` / 出場区間 `UNIQUE (tenant_id, 試合, 枠種別, 枠, 有効開始 D2) WHERE 退役していない` / **試合の業務的一意性が 0 件** / **背番号の保持場所が **7-3 節の保持場所表** の 3 区分どおり**(**先発 = 試合のスタメンに当時値を焼き込む・試合単位で不変** / **途中出場 = 交代イベントのペイロードが正本** / **出場区間 = 上記 2 つから再生成する投影**)/ **出場区間は背番号を持つが、それは導出値である**(**7-3 節の保持場所表の出場区間(投影)行**。**4 周目 `P1-5` の是正 — 3 周目に「複製列を置かない」と書いたのは正本と正反対だった**)/ **出場区間だけを正本として更新する経路が無い** |
| 8 | **規則セットと適用規則スナップショット**(6-5 節)+ migration | manifest と exact-set 一致 / 適用スナップショットが試合へ紐づく |
| 9 | **イベントスロット・操作イベント**(5-3)+ migration | イベントスロット `PRIMARY KEY (tenant_id, 試合, D4, D1)`(**部分でない**)/ **役割タグが `business_unique` + `primary_key` + `fk_target` の 3 つ**(**3-4 節の全数表のイベントスロット行(`M07` の FK 参照先)**)/ **`V10` の FK がイベントスロット表を指す** / `C12` の `NOT NULL` 切り分けが種別ごとの `CHECK` / **`V12`・入力者識別列が 0 件** / **操作イベントの不変性の所有を判定して記録した** — **5-3 節「既存イベントを書き換えない」**「確定済みイベントは新イベントで訂正し、**既存イベントを書き換えない**」に対し、**DB トリガによる全列保護**か**アプリ層所有として受け取り先へ送る**かを決め、`N3` の証跡に載せた(5 周目 `P1-2`) |
| 10 | **プレイ行・走者・一時ID→正式ID写像表**(5-2 / **5-3 節の一時 ID 写像(`M17`)**)+ migration | `D2` の部分一意が **3-4 節の全数表の `D2` 行(`M02`)** と一致 / **`D2` が PK・FK 参照先・他表の複製列になっていない。`D2` の特定値・範囲を固定した述語が 0 件**(`D2 IS NOT NULL` は許す — **11-3 節の原則 8(`M03`)**)/ 走者 `UNIQUE (tenant_id, プレイ, 塁) WHERE 退役していない` / 一時 ID 写像が `uuid` で `UNIQUE (tenant_id, 一時 ID)` / **投影行 ID の安定性を可能にする物理キーがある** — **12-3 節「投影行 ID の安定性」** が「**再投影で同じ投影行が同じ識別子になる**」ことを要求する(サイドカーの結合先が壊れるため)。**旧行識別子から決定的に導ける形にし、再投影テストで同一性を確かめる**(6 周目 `P1-6`) |
| 11 | **`D5` 台帳と参照元 3 表**(5-4 / 6-4・design.md 4 節)+ migration | 台帳に `UNIQUE (tenant_id, D5)` と **役割タグ `fk_target` の `UNIQUE (tenant_id, D5, 種別)`** / 参照元 3 表に `UNIQUE (tenant_id, D5)` / **4 表すべての種別列が `NOT NULL`** / 参照元は `CHECK (種別 = literal)` / **複合 FK が `MATCH FULL`** / **負例 4 種で拒否** / **確定結果・種別・先着原本の更新が拒否され、退役状態の更新は通る** / **`(tenant_id, 試合, D5)` になっていない** |
| 12 | **記録権世代・`D3`・退避原本**(5-3 / `C11` / 6-4・design.md 5 節)+ migration | 現世代 `UNIQUE (tenant_id, 試合) WHERE 種別 = 通常 AND 失効していない` / 発行順 `UNIQUE (tenant_id, 試合, 発行順)` / `D3` は `bigint NOT NULL`・新世代で `0` / 退避原本 `UNIQUE (tenant_id, 試合, 旧世代, 元の連番)` / **`D3` の後退拒否・同値許可・前進許可・他列更新許可の 4 ケース** / **保持期限の既定が NULL** |
| 13 | **カルテ所見と変更履歴・PDF 出力実績**(10-1 **10-1 節**)+ migration | カルテ所見 `UNIQUE (tenant_id, 選手, カルテ種別) WHERE 退役していない` / 変更履歴と PDF 出力実績が manifest と一致 |
| 14 | **語彙 3 層・システム設定値**(10-3 / 10-4)+ migration | チーム拡張層 `UNIQUE (tenant_id, キー名)`・管理者層と固定層 `UNIQUE (キー名)` / **native enum が 0 件**(参照テーブル + FK)/ **参照テーブルに値を入れていない** / **語彙は無効化のみで削除不可**(`P-12`)/ **システム固定層(試合区分・在籍区分)は「変更不可」なので更新が拒否される**(**10-3 節の 3 層表のシステム固定行** — 不変列マトリクス。4 周目 `P1-2` の是正) |
| 15 | **認証主体・認証情報・管理者資格情報・管理者セッション・通常トークン**(8-2 / 8-2-A)+ migration | `UNIQUE (認証主体)` / **平文・可逆保持の列が 0 件**(bcrypt ハッシュ列のみ — NFR-011)/ **管理者の資格情報がテナントと別系統** |
| 16 | **管理者操作ログ**(8-4)+ migration | **削除系統 = `対象外`・追記専用性 = `追記専用`・移行バッチ退役 = `持たない`**(**3 フィールドで表す** — 5 周目 `P1-1`)/ **削除列を持たない** / **`UPDATE` と `DELETE` が拒否され `INSERT` が通る** / **保持が無期限**(保持期限列を持たない)/ **NFR-017 の一般ログとは別の表** / **対象がグループの場合を表せる** |
| 17 | **選手統合イベント・移動記録・レート制限表**(8-4 **8-4 節**)+ migration | manifest と exact-set 一致 / 論理削除の系統が割り当てられている |
| 18 | **グループ・参加・付与・招待**(9 章系)+ migration | 参加 `UNIQUE (グループ, 参加テナント) WHERE 状態 = active` / 付与 `UNIQUE (参加行)` / **招待 `UNIQUE (コードのハッシュ)` がテナントに閉じない** / **越境 FK が制御資源の明示的な例外集合と exact-set 一致** |
| 19 | **キャッシュ無効化意図表**(11-2 系)+ migration | `UNIQUE (tenant_id, 意図 ID)` / **5 種の対象範囲の物理キー要素を全部保持できる**(**11-2 節の物理的な無効化先(`B02`)** と exact-set。共有集計キーが**要求元・対象テナント**を含む)/ **`(tenant_id, 配信状態)` の索引が `pg_index` に存在し列順が正しい**(**`EXPLAIN` は使わない**)/ **物理削除経路が 0 件** / **発火規則を焼き込んでいない** |
| 20 | **隔離領域**(10-6 節)+ migration | **raw ペイロードに意味制約・FK・CHECK が 0 件**(**技術的 PK と取り込み単位識別子は許す** — design.md 1-6 節)/ **88 列の生の行をそのまま保持できる** / **この表だけは正規化していない** |
| 21 | **移行世代・移行元最終オーダー・移行由来未検証列**(12-3 節)+ migration | 移行世代 `UNIQUE (tenant_id, 試合) WHERE 種別 = migration AND 退役していない` / **`D3` を持ち `NOT NULL`**・保持端末は NULL 可 / 移行元最終オーダー `UNIQUE (tenant_id, 試合, チームレコード) WHERE 退役していない` + 複合 FK / **解決列を作っていない**(**12-3 節の移行元最終オーダーの「列」(裁定 `R-1`)** は任意)/ **プレイ行に「移行由来で未検証」列がある** / **移行の行識別子が業務上のプレイ番号と別列**で一意性を仮定していない |
| 22 | **移行結果・解決・警告レポート**(10-6 **10-6 節**)+ migration | 3 表が manifest と exact-set 一致 / **重複した旧行が 2 行として取り込まれ警告レポートへ列挙される**器がある |
| 23 | **スキーマ契約の全数突合と横断 audit**(manifest ↔ 実スキーマ ↔ **正本 2 表**) | **manifest と実スキーマが exact-set 一致** / **`business_unique` タグの集合が **3-4 節の業務的一意性全数表** の 27 行と exact-set 一致**・**グローバル一意が `➖` 4 行以外に 0 件** / **`pg_constraint` の全 FK について、テナント所有表間の FK がすべて `tenant_id` を先頭に含む** / **越境 FK が例外集合と exact-set 一致** / **`MATCH` と削除動作が manifest と一致し `ON DELETE CASCADE` が 0 件** / **`C12` の列単位 NULL 性が exact-set 一致** / **禁止列集合が 0 件** / **一意制約の述語の全文が一致** / **横断 audit**: **テナントに属する全表が `tenant_id NOT NULL` を持つ** / **移行が作る全表が取り込みバッチ識別子を持つ** / **退役述語が 7 件の全数に入っている**(**12-3 節の不変条件 5(退役述語)**)/ **`deleted_at` 単一列に畳んだ表が 0 件** / **負例: manifest から 1 行落とすと red・実スキーマに 1 表足すと red・正本の blob SHA を変えると `stale` で red**。**表の全数性はここでは閉じない**(design.md 10 節 `N1` の受入証跡へ)。**本ステップは追加修正を行わない検査に限る** — 差分の是正はステップ 27 以降の是正ステップで行う(design.md 10-1 節) |
| 24 | **型境界の exact-set テスト**(design.md 1-5 節・**3 層**) | **A(隔離領域)**: 4 形態 + 境界値が期待出力表と exact-set 一致・4 形態が**相互に区別できる**・桁の大きい数値文字列/先頭ゼロ/前後空白/全角数字が**バイト同値** / **B(移行イベントのペイロード)**: **行き先区分表(**12-1 節の行き先の区分**)をパースし `保持のみ` の列集合がペイロードの区分定義と一致**・**`導出` の列が入っていない**・**`移行時に使用` の列が新スキーマに残っていない**(**列単位の写像は TSK-250 へ送る** — design.md 10 節 `N2`)/ **C(正規スキーマ)**: 列 47・55 の原本/解決が分離・原本列の型が規則どおり・解決列が nullable で既定値なし / **負例で red** / **センチネル変換の実装が 0 件**(`R-9`) |
| 25 | **不変列マトリクスの検査**(design.md 5 節) | **5 表(記録権世代 / `D5` 台帳 / チームレコード / 管理者操作ログ / 語彙のシステム固定層)の全行**について **`pg_get_triggerdef` の全文・対象表と対象列・`tgenabled`・関数定義**が manifest と一致 / **保護列の更新を拒否し許可更新列は通る** / **追記専用表は `UPDATE`/`DELETE` を拒否し `INSERT` を通す** / **downgrade でトリガと関数が消える**。**5 表は本計画が拾う範囲であり全数性はここでは閉じない**(design.md 10 節 `N3` の受入証跡へ)。**追加修正は行わない** |
| 26 | **migration の往復 + CI 3 段 + CI コマンド契約 + migration URL の供給** | 空 DB へ `upgrade head` → `downgrade base` → `upgrade head` が通る(**`downgrade` は自主基準**)/ `current --check-heads` が単一 head / **`alembic check` の差分 0** / **`CREATE POLICY`/`CREATE ROLE`/`ALTER ROLE` が 0 件** / **`create_all()` が 0 件** / **DDL と DML を統合していない** / **`ci.yml` の `backend` ジョブが「既存 6 件 + Alembic 3 件」で新しいジョブを起こしていない** / **job env に `PITCHLOG_MIGRATION_DATABASE_URL` がある**(レビュー 3 周目 `P0-2`)/ **`tests/test_ci_wiring.py` の期待列が更新され、負例 4 種(欠落・重複・順序違い・別ジョブ追加)で red** / **`environment-expectations.json` の差分が 0** |
| 27 | **受入証跡の突合シート生成と判定**(design.md 10 節・10-1 節) | **`N1`・`N3`・`N4` の突合シートが機械生成される**(`docs/features/orm-schema-migration/acceptance-sheets/`)/ **`N1`**: manifest の表一覧 ↔ `data-model.md` 5〜12 章の節見出し / **`N3`**: 「不変」「変えない」「昇格させない」「後退させない」「追記のみ」「**変更不可**」「更新しない」「上書きしない」の語の機械抽出 ↔ 不変列マトリクス / **`N4`**: ****11-1 節の削除 4 系統の表** が挙げる業務オブジェクトに対応する表だけ** ↔ 軸 1 の値(**それ以外の表は「正本に直接割当なし」として個別典拠を添える**)/ **各行が `一致` / `差分` / `対象外`(理由と典拠が必須)のいずれかで判定されている** / **未判定 0 かつ未解消の `差分` 0** / **`差分` があれば末尾へ番号付きの是正ステップを追加し、是正後にシートを再生成して再判定した** / **`N2` の受け取り先が TSK-250** / **`N5` の受け取り先が承認前条件で確定済み**(4-10 節) |
| 28 | **`.claude/core-areas.json` へ 4 領域へ登録**(6.3 規則②③⑤・design.md 9 節)+ 回帰テスト | 新設資産の**全パスが該当領域の `paths` の glob に包含される**(**`fnmatch` の `*` は `/` を跨ぐ**ので `backend/src/pitchlog/db/*` で配下を覆える — 実証済み)/ **後から足すファイルも同じ glob に入る**(**新規ファイルの登録漏れ = `H-12` 再発を構造で防ぐ**) / **Alembic revision が 4 領域すべてに登録**(混在ファイル — 規則②)/ **負例: 1 パスを外すと red** / `uv run pytest tests/test_core_guard.py` green / `[手動・外部]` **paths 追加の敵対レビュー + 人間承認**(**逐行確認は PR 作成者以外**) |

## 5. DoD(受け入れ基準)

### 5-1. Notion タスクの DoD 10 項目との全数対応

**「1 対 1」ではない**(レビュー 3 周目 `P2-3` の是正)— Notion の ④⑤⑥ はそれぞれ **2 つの D へ分割**しており、
本計画が**追加した自主基準・工程ゲートは別欄**に分類してある。**全数対応**(Notion の 10 項目に
対応する D が漏れなくある + 追加分がどれか判別できる)であることが要求水準である。

| Notion | 本計画の DoD | 備考 |
| --- | --- | --- |
| ① 5.1 の留保解除・10.1 改訂(版は上げない) | **D1** | **裁定 `Q-1` で「版を上げる」へ変更**(4-4 節) |
| ② `test_psycopg_...` の更新 | **D2** | 関数を改名(design.md 8-1 節) |
| ③ 接続 URL を `postgresql+psycopg://` に統一 | **D3** | **範囲を限定** + **3 キー分離**(design.md 7-3 節) |
| ④ models は型を損失なく受け取る境界に限定・センチネル変換を実装しない・exact-set テスト | **D4**(境界)+ **D5**(exact-set 3 層) | **2 項目に分けた** |
| ⑤ `upgrade`/`downgrade` 両方通る・`CREATE POLICY`/`ROLE` が 0 件 | **D6**(往復)+ **D7**(DDL 0 件) | **2 項目に分けた**。`downgrade` は自主基準 |
| ⑥ FR-015 背番号に一意制約なし・テナント内一意が `tenant_id` 先頭 | **D8**(背番号)+ **D9**(テナント内一意・複合 FK) | **典拠が 2 系統なので分けた** |
| ⑦ NFR-014 ハードコード 0 件 | **D10** | **範囲を限定**(4-5 節) |
| ⑧ core-areas.json へ登録 | **D11** | **4 領域へ** |
| ⑨ 敵対レビュー + 人間逐行確認 | **D12** | |
| ⑩ TSK-342 / TSK-330 と相互リンク | **D13** | TSK-344 の契機記録を追加 |
| —(本計画が追加) | **D14** | **manifest ↔ 実スキーマ ↔ 正本 2 表の一致**(**カタログで表現可能な構造集合**に限る。**表の全数性は含まない**) |
| —(本計画が追加) | **D15** | **不変列マトリクス(5 表)が構造で効いている**(**全数性は含まない**) |
| —(本計画が追加) | **D16** | **移行イベントのペイロードの区分定義が行き先区分表と一致する**(**列単位の写像は含まない**) |
| —(本計画が追加) | **D17** | **CI コマンド契約が更新され、負例で red になる** + **job env に migration URL がある** |
| —(本計画が追加) | **D18** | **機械で閉じない 6 事項の受入証跡が揃っている**(design.md 10 節。**突合シートは機械生成・判定は人間**) |

### 5-2. DoD

- [ ] **D1**: **設計書 5.1 の ORM 留保を解除し、10.1 の箇条を改訂した**。**規範本文に採用理由と却下案を残した**。
      **版を v1.14 へ繰り上げ、`/finalize-doc` を通した**(裁定 `Q-1`(A))。`docs/README.md` を現行化した
- [ ] **D2**: **機械検査を ORM スタックの厳密固定検査へ置き換えた**(psycopg の厳密固定は維持 +
      SQLAlchemy/Alembic の厳密固定 + **SQLAlchemy の major = 2**)。**負例 5 種で red**
- [ ] **D3**: **SQLAlchemy / Alembic が消費する接続 URL を `postgresql+psycopg://` へ正規化した**。
      **設定を 3 キーに分離**(アプリ用 URL / Alembic 用 URL / **`PITCHLOG_DATABASE_POOLED`**)し
      **相互 fallback を禁じた**(**Alembic は direct connection のみ**・
      **`POOLED=true` なら `prepare_threshold=None`**)。**pooler かどうかを URL から自動判定していない**。**`D3` の対象は「コード配線と CI 環境」であり、運用環境の direct 性は `N6` の運用証跡で閉じる**(5 周目 `P1-4`)。
      **設定正本テンプレートに 3 キーが揃っている**。**負例 3 種で red**。
      **psycopg へ直接渡るテスト DSN は変更していない**
- [ ] **D4**: **型境界が 3 層で、層ごとに射程が限定されている** — 隔離領域は raw を保持、
      移行イベントのペイロードはセンチネル判定後の値、**正規スキーマの原本/解決の必須ペアは列 47・55 のみ**。
      **フィールド別センチネル変換を実装していない**(`R-9`)。**移行元最終オーダーの解決列を作っていない**
- [ ] **D5**: **exact-set テストが 3 層である**。**負例で red**。
      **⚠ 正本の典拠なし・本計画の自主基準**(`Q-3`)
- [ ] **D6**: **`upgrade` / `downgrade` が両方通る**。**単一 head**。**`alembic check` の差分が 0**。
      **⚠ `downgrade` は正本の典拠なし・本計画の自主基準**(`Q-3`。正本が置くのは 3 段 — `:2404-2405`)
- [ ] **D7**: **migration に `CREATE POLICY` / `CREATE ROLE` / `ALTER ROLE` が 0 件**(裁定 `A-2`)。
      **`create_all()` が 0 件**。**DDL と DML を統合していない**
- [ ] **D8**: **FR-015 の背番号に一意制約を置いていない**(チーム名・試合の業務的一意性も 0 件)
- [ ] **D9**: **`business_unique` の集合が 27 行と exact-set 一致**し、**グローバル一意が `➖` 4 行以外に 0 件**。
      **テナント所有表間の FK がすべて複合参照**で、**越境 FK が例外集合と exact-set 一致**。
      **`ON DELETE CASCADE` が 0 件**
- [ ] **D10**: **NFR-014** — **製品コードと Alembic 設定に接続文字列のハードコードが 0 件**
      (**設定正本テンプレートと CI の隔離されたテスト値は対象外として分類した**)
- [ ] **D11**: **`.claude/core-areas.json` へ新設資産を 4 領域へ登録した**(**`areas[].paths` は glob**・**`guard_paths` に足すなら完全一致なので完全列挙**)
      (**6.3 規則⑤ の敵対レビュー + 人間承認**。負例: 1 パスを外すと red)
- [ ] **D12**: **コア領域として敵対レビュー + 人間の逐行確認(PR 作成者以外)を通した**。実施記録行を記入した
- [ ] **D13**: **TSK-342 / TSK-330 と相互リンクした**。**TSK-344 の再実行契機が発火することを PR に記録した**
- [ ] **D14**: **`contracts/db/schema-manifest.json` が正本へ追跡可能な形で束縛され、
      manifest ↔ 実スキーマ ↔ 正本 2 表が一致**している(**カタログで表現可能な構造集合**に限る)。
      **正本の blob SHA が変われば `stale` で red**。
      **⚠ 表の全数性は D14 に含まない** — **`N1` の受入証跡(D18)で閉じる**(4-11 節の収束方針)
- [ ] **D15**: **不変列マトリクス(5 表)が構造で効いている** — `pg_get_triggerdef`・対象列・`tgenabled`・
      関数定義が manifest と一致し、**保護列は拒否・許可列は通り・downgrade で消える**。
      **⚠ 5 表は本計画が拾う範囲であり全数性は D15 に含まない** — **`N3` の受入証跡(D18)で閉じる**
- [ ] **D16**: **移行イベントのペイロードの区分定義が行き先区分表(**12-1 節の行き先の区分**)と一致**し、
      **`導出` の列を含まず、`移行時に使用` の列が新スキーマに残っていない**。
      **⚠ 列単位の写像は D16 に含まない** — **TSK-250 の 88 列写像の確定ゲートで閉じる**(`N2`)
- [ ] **D17**: **`ci.yml` に Alembic 3 段が入り、job env に `PITCHLOG_MIGRATION_DATABASE_URL` があり、
      `tests/test_ci_wiring.py` の CI コマンド契約が更新され、
      負例 4 種(欠落・重複・順序違い・別ジョブ追加)で red になる**
- [ ] **D18**: **機械で閉じない 6 事項(`N1`〜`N6`)の受入証跡が揃っている**(design.md 10 節・10-1 節)—
      **`N1`・`N3`・`N4` は突合シートが機械生成され、全行が `一致` / `差分` / `対象外` で判定され、
      未判定 0 かつ未解消の `差分` 0 かつ不適格な `対象外` 0**(`対象外` は**当該 `N` の対象・範囲の外にある生成候補だけ**に使え、**理由と典拠が必須**。**対象内の欠落に使うことは禁止** — 5 周目。**判定は人間**)。
      **`差分` は末尾の是正ステップで解消し、シートを再生成して再判定した**。
      **`N2` の受け取り先は TSK-250**、**`N5` は 4-10 節の承認前条件で確定済み**、**`N6` は運用証跡**(実施者・確認時点・endpoint 種別のみ。**URL 自体は記録しない** — NFR-014)。
      **実施記録行**(対象 / 範囲 / 方法)を 6.3 の形式(設計書 `:355`)で PR に記入した

## 6. テスト計画

**NFR-019 の種別は 4 種**((a) 一致性 /(b) 越境 /(c) E2E 主要分岐 /(d) 同期故障系 — 要件書 `:920`・`:928`)。
**「単体」は同項の列挙に無く、ランナー標準(バックエンド = pytest)の側にある**(research.md 4-1 節)。

| 種別 | 本タスクが足すもの |
| --- | --- |
| **ランナー標準(pytest 単体)** | 接続 URL 正規化の純関数 / **設定契約(3 キー)の負例 3 種** / 機械検査の判定純関数と**負例 5 種** / **CI コマンド契約の負例 4 種** / models の宣言的検査 / **manifest の自己整合検査と正本 2 表とのパース照合** |
| **(a) 一致性** | **該当なし**(対象計算は models / migration に当たらない — 裁定 `A-4`) |
| **(b) 越境** | **本タスクの義務ではない**。作成は TSK-317・実行は TSK-344(`:2439`・`:2751`)。**本タスクのマージが再実行契機を発火させる**ことを PR に記録する |
| **(c) E2E 主要分岐** | **該当なし**(初回 migration は **non-serving** — `:2415-2425`) |
| **(d) 同期故障系** | **該当なし**(同期の適用経路は TSK-330 の射程) |

**実 DB を要するテスト**(`requires_db` marker・`backend/tests/db/` 配下):

- Alembic の実接続 / `dialect.driver == "psycopg"` / **Alembic が direct URL を使う** / **pooler の `prepare_threshold=None`**
- **空の PostgreSQL へ `alembic upgrade head`**(**`create_all()` を使わない** — `:2404`)+ 往復
- **`target_metadata` の配線**(test-only metadata の差分で `alembic check` が red)
- **スキーマ契約の全数突合**(`pg_constraint` / `pg_index` / `pg_attribute` / `pg_trigger` ↔ manifest ↔ 正本)
- **`D5` の負例 4 種** + **上書き拒否と退役更新の許可**
- **不変列マトリクス 5 表**(構造 4 点 + 挙動 + downgrade で消える)
- **型境界の exact-set(3 層)**
- **無効化意図表の索引のカタログ検査**(`pg_index` の存在と列順。**`EXPLAIN` は使わない**)

**規約の遵守**(`tests/test_ci_wiring.py` が機械強制):

- `backend/tests/db/test_*.py` の**全件に `pytestmark = pytest.mark.requires_db`**(`:1216-1228` の非再帰 glob)
- **`SET ROLE` を conftest に書かない**(`:1237`)
- **`environment-expectations.json` に差分を出さない**
- **`backend/tests/db/conftest.py` への追記は TSK-317 のマージ後**。**既存の使い捨てクラスタ factory を利用し、
  新しいテスト用 DSN 環境変数を増やさない**(design.md 7-3 節の 3 キー(URL 2 つ + pooler フラグ)は**製品の設定**でテスト DSN とは別系統)

**CI**: 10.1 の `backend` ジョブ(`services: postgres`)に相乗りする。**新しいジョブは起こさない**。
`alembic upgrade head` + `current --check-heads` + `check` の **3 段**を入れ、
**`tests/test_ci_wiring.py` の CI コマンド契約を同時に更新する**(design.md 8-3 節)。
**`alembic check` が見ないトリガと enum 値は専用のスキーマ検査テストで補う**(design.md 8-5 節)。
