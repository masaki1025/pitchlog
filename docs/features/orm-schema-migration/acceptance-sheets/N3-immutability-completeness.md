# N3 — 不変列マトリクスの全数性

<!-- scripts/generate_orm_acceptance_sheets.py により生成。判定欄と理由欄だけを人間が記入する。 -->

## 対象・範囲・方法

- 対象: data-model.md 全文にある設計指定の 9 語の全出現
- 範囲: 不変・変えない・昇格させない・後退させない・追記のみ・変更不可・更新しない・上書きしない・書き換えない。
- 方法: 各語を文書順に機械抽出し、同じ正本節を持つ manifest の immutability 候補と人間が突き合わせる。

## 判定区分と適格条件

| 区分 | 適格条件 |
| --- | --- |
| `一致` | 正本側と対応する manifest / 実装側を両方特定できる。片方しか特定できない行は一致にできない。 |
| `差分` | 欠落・余剰・内容不一致のいずれか。未解消のまま完了にできない。 |
| `対象外` | 当該 N の対象・範囲外にある生成候補だけ。対象内の欠落には使えず、理由と典拠（ファイル:節）が必須。 |

完了条件は、未判定 0・未解消の `差分` 0・不適格な `対象外` 0。

## 差分時の是正遷移

`差分` が出たら番号付きの是正ステップを追加する。是正ステップは `core-areas.json` 登録ステップ（現ステップ 29）より前へ挿入し、登録ステップを後ろへ繰り下げる。是正後にシートを再生成し、全行を再度判定する。

## 突合行（正本の文書順）

| 対象 | 正本側 | 実装側 | 判定 | 理由と典拠 |
| --- | --- | --- | --- | --- |
| 出現 001: 不変 | data-model.md §変更履歴 — 版 0.2 の行（語: 不変） | manifest immutability: 該当候補なし | 対象外 | 正本の変更履歴表(確定ゲートの周回記録)の中の出現であり、列の不変性を述べた契約ではない docs/features/orm-schema-migration/design.md:10 |
| 出現 002: 不変 | data-model.md §変更履歴 — 版 0.2 の行（語: 不変） | manifest immutability: 該当候補なし | 対象外 | 正本の変更履歴表(確定ゲートの周回記録)の中の出現であり、列の不変性を述べた契約ではない docs/features/orm-schema-migration/design.md:10 |
| 出現 003: 不変 | data-model.md §変更履歴 — 版 0.2 の行（語: 不変） | manifest immutability: 該当候補なし | 対象外 | 正本の変更履歴表(確定ゲートの周回記録)の中の出現であり、列の不変性を述べた契約ではない docs/features/orm-schema-migration/design.md:10 |
| 出現 004: 不変 | data-model.md §変更履歴 — 版 0.1 の行（語: 不変） | manifest immutability: 該当候補なし | 対象外 | 正本の変更履歴表(確定ゲートの周回記録)の中の出現であり、列の不変性を述べた契約ではない docs/features/orm-schema-migration/design.md:10 |
| 出現 005: 不変 | data-model.md §変更履歴 — 版 0.1 の行（語: 不変） | manifest immutability: 該当候補なし | 対象外 | 正本の変更履歴表(確定ゲートの周回記録)の中の出現であり、列の不変性を述べた契約ではない docs/features/orm-schema-migration/design.md:10 |
| 出現 006: 不変 | data-model.md §変更履歴 — 版 0.1 の行（語: 不変） | manifest immutability: 該当候補なし | 対象外 | 正本の変更履歴表(確定ゲートの周回記録)の中の出現であり、列の不変性を述べた契約ではない docs/features/orm-schema-migration/design.md:10 |
| 出現 007: 不変 | data-model.md §変更履歴 — 版 0.1 の行（語: 不変） | manifest immutability: 該当候補なし | 対象外 | 正本の変更履歴表(確定ゲートの周回記録)の中の出現であり、列の不変性を述べた契約ではない docs/features/orm-schema-migration/design.md:10 |
| 出現 008: 不変 | data-model.md §1-1 — #### 定義の所在(同じ不変条件を 2 箇所で定義しない) | manifest immutability: 該当候補なし | 対象外 | 「定義の所在」表で不変条件の定義節を一本化する記述であり、列の不変性ではない data-model.md:1-1 |
| 出現 009: 不変 | data-model.md §1-1 — 確定ゲートの 4 周のうち、指摘の多くが「同じ不変条件が複数の節に散っていて片方だけ直った」型だった | manifest immutability: 該当候補なし | 対象外 | 確定ゲートの指摘傾向の記述であり、列の不変性ではない data-model.md:1-1 |
| 出現 010: 不変 | data-model.md §1-1 — 「定義の一本化」を行い、次の不変条件については定義節を 1 つに固定した。 | manifest immutability: 該当候補なし | 対象外 | 定義の一本化の宣言であり、列の不変性ではない data-model.md:1-1 |
| 出現 011: 不変 | data-model.md §1-1 — \| 不変条件 \| 定義節(ここだけに書く) \| 参照する節 \| | manifest immutability: 該当候補なし | 対象外 | 定義の所在表の表頭であり、列の不変性ではない data-model.md:1-1 |
| 出現 012: 不変 | data-model.md §1-1 — \| D5 台帳(全経路一意の所在・確定結果・不変性・経路排他・照合位置) \| 5-4 \| 3-4・5-3・6-4 の直後 \| | manifest immutability: 該当候補なし | 対象外 | 定義の所在表の行(D5 台帳の定義節の指定)であり、列の不変性ではない data-model.md:1-1 |
| 出現 013: 不変 | data-model.md §3-0 — 直して不変条件・制御資源・付与・招待へ反映しなかった)。以下を唯一の定義とし、各節は参照する。 | manifest immutability: 該当候補なし | 対象外 | 述語の唯一定義の宣言であり、列の不変性ではない data-model.md:3-0 |
| 出現 014: 不変 | data-model.md §3-0 — \| 9-4 / 9-4-A(不変条件・グループ終了) \| 実効 admin \| | manifest immutability: 該当候補なし | 対象外 | 述語の定義節の対応表であり、列の不変性ではない data-model.md:3-0 |
| 出現 015: 不変 | data-model.md §3-2 — \| スキーマ検査 \| 関数所有者 / ACL(PUBLIC が含まれないこと)/ search_path / 名前解決経路への注入可能性(下記「名前解決経路の非注入」の不変条件)/ 所有ロールの NOLOGIN・BYPASSRLS / ロール所属を検査するテストを置く。NFR-019(b) の越境テストとは別に、構成そのものを検査する。各項目の検査対象集合と合格述語は実装が確定する(範囲確定 — PO 裁定 2026-09-09・7.3-6。受け取り先は 12-8 節) \| | manifest immutability: 該当候補なし | 対象外 | スキーマ検査の対象列挙であり、列の不変性ではない data-model.md:3-2 |
| 出現 016: 不変 | data-model.md §3-2 — \| 名前解決経路の非注入(不変条件) \| 関数の名前解決経路へ、所有者以外のロールがオブジェクトを注入できてはならない。呼び出しロールに限定しない — 未信頼ロールが探索対象スキーマへオブジェクトを置ければ、正規の呼び出しを経由して解決させられる。守り方は 2 つで、pg_temp は上記「末尾に置く」契約で守り、それ以外の列挙スキーマは CREATE を所有者以外へ与えないことで守る。本書はこの不変条件までを定め、検査の対象集合の厳密な列挙と合格述語は実装が確定する (範… | manifest immutability: 該当候補なし | 対象外 | 「名前解決経路の非注入」という業務上の不変条件であり、列の不変性ではない data-model.md:3-2 |
| 出現 017: 不変 | data-model.md §3-2 — \| 名前解決経路の非注入(不変条件) \| 関数の名前解決経路へ、所有者以外のロールがオブジェクトを注入できてはならない。呼び出しロールに限定しない — 未信頼ロールが探索対象スキーマへオブジェクトを置ければ、正規の呼び出しを経由して解決させられる。守り方は 2 つで、pg_temp は上記「末尾に置く」契約で守り、それ以外の列挙スキーマは CREATE を所有者以外へ与えないことで守る。本書はこの不変条件までを定め、検査の対象集合の厳密な列挙と合格述語は実装が確定する (範… | manifest immutability: 該当候補なし | 対象外 | 同上(名前解決経路の非注入) data-model.md:3-2 |
| 出現 018: 変えない | data-model.md §3-2 — → 常時経路は関数へ一本化する原則は変えない。 移行バッチは NOLOGIN にできないため関数一本化の形を | manifest immutability: 該当候補なし | 対象外 | 「関数へ一本化する原則は変えない」という方針の記述であり、列の不変性ではない data-model.md:3-2 |
| 出現 019: 不変 | data-model.md §3-2 — \| 書き込み先の限定 \| 移行が書く表へのみ GRANT。対象の集合は 12-3 節「移行バッチの退役」の不変条件 1 が正(本節では列挙しない — 7 周目 P1-2 の是正。同じセル内で再列挙していた) \| | manifest immutability: 該当候補なし | 対象外 | 移行バッチの書き込み先の限定であり、列の不変性ではない data-model.md:3-2 |
| 出現 020: 不変 | data-model.md §3-4 — \| 12-3 \| 移行元最終オーダーは試合 × チームレコードで 1 件 \| UNIQUE (tenant_id, 試合, チームレコード) WHERE 退役していない \| ✅ テナント内(裁定 A-1 で追加。述語は 12-3 節のバッチ退役の不変条件 5 — 4 周目 P1-3) \| | manifest immutability: 該当候補なし | 対象外 | 一意制約の全数表の行であり、列の不変性ではない data-model.md:3-4 |
| 出現 021: 不変 | data-model.md §3-4 — \| 5-3 \| D2 は「同時に有効で自身の D2 を持つイベント」の間で 1 件 \| UNIQUE (tenant_id, 試合, D2) WHERE D2 IS NOT NULL AND 置換されていない AND 退役していない \| ✅ テナント内(M02。D4 を含めない — 正史は世代の連結。退役していない は 12-3 節の不変条件 5 — 4 周目 P1-3) \| | manifest immutability: 該当候補なし | 対象外 | 同上(一意制約の全数表) data-model.md:3-4 |
| 出現 022: 不変 | data-model.md §3-4 — \| 5-3 \| イベントスロットは (試合, D4, D1) で 1 件 \| PRIMARY KEY (tenant_id, 試合, D4, D1)(退役済みの行も残るので、再投入は新しい D4〔新しい移行世代〕で行う — 12-3 節の不変条件 6) \| ✅ テナント内(M07 の FK 参照先。部分でない完全な一意性) \| | manifest immutability: 該当候補なし | 対象外 | 同上(一意制約の全数表) data-model.md:3-4 |
| 出現 023: 不変 | data-model.md §3-4 — \| 7-4 \| スタメン記憶はチームレコードにつき 1 件 \| UNIQUE (tenant_id, チームレコード) WHERE 退役していない \| ✅ テナント内(述語は 12-3 節の退役の不変条件 5 — 8 周目 P1-7) \| | manifest immutability: 該当候補なし | 対象外 | 同上(一意制約の全数表) data-model.md:3-4 |
| 出現 024: 不変 | data-model.md §4-3 — \| エンティティの内部 ID \| サーバー \| テーブル内(代理キー。業務的一意性ではない — 3-4 節) \| 選手 ID は不変・内部。名前・背番号は表示用属性 \| FR-015 \| | manifest immutability: 該当候補なし | 一致 | §4-3 冒頭(:637)「本節は役割の一覧(列・制約の定義は他節)」。選手 ID 不変の定義節は §7-2(:1416)で出現 047 と同一事実。`players.immutability.protected_columns=['id']` data-model.md:4-3,7-2 |
| 出現 025: 不変 | data-model.md §5-2 — - 一意性: UNIQUE (tenant_id, プレイ, 塁) WHERE 退役していない(述語は 12-3 節の退役の不変条件 5 — 7 周目 P1-7 の是正) | play_rows: protected=['id', 'source_event_id', 'legacy_row_identifier']; allowed=['version', 'hidden_at'] / play_runners: protected=[]; allowed=['status', 'status_source', 'retired_at'] | 対象外 | 走者の一意性述語(退役していない)の説明であり、列の不変性ではない data-model.md:5-2 |
| 出現 026: 不変 | data-model.md §5-3 — \| D2(論理順序) \| bigint。NULL 可(自身の D2 を持たない種別がある — 下記「参加区分と D2 の有無」)。整数の間隔(gap)方式(5-2 節) \| UNIQUE (tenant_id, 試合, D2) WHERE D2 IS NOT NULL AND 置換されていない AND 退役していない の部分一意(退役していない は 12-3 節の退役の不変条件 5)。同期プロトコル設計 5-2 の「D2 の一意性の射程」(「同時に有効で、かつ自身の D2 … | event_slots: protected=['tenant_id', 'game_id', 'generation', 'd1']; allowed=['confirmed_version'] / operation_events: protected=['tenant_id', 'id', 'game_id', 'generation', 'd1', 'd5', 'event_kind', 'payload', 'state_diff']; allowed=['d2', 'replaced_at', 'retired_at'] / temporary_player_id_mappings: protected=['temporary_id', 'player_id']; allowed=[] / recording_generations: protected=['generation', 'kind', 'issuance_order', 'holder_device', 'granted_at']; allowed=['confirmed_watermark', 'applied_prefix', 'revoked_at', 'retired_at'] | 対象外 | D2 の部分一意制約の説明であり、列の不変性ではない data-model.md:5-3 |
| 出現 027: 不変 | data-model.md §5-3 — \| D5(べき等キー) \| 全イベントで無条件(V1) \| 一意性・確定結果・不変性・経路排他の定義は 5-4 節(1-1 節の定義の所在表。6 周目 P1-4 の是正) \| — \| | event_slots: protected=['tenant_id', 'game_id', 'generation', 'd1']; allowed=['confirmed_version'] / operation_events: protected=['tenant_id', 'id', 'game_id', 'generation', 'd1', 'd5', 'event_kind', 'payload', 'state_diff']; allowed=['d2', 'replaced_at', 'retired_at'] / temporary_player_id_mappings: protected=['temporary_id', 'player_id']; allowed=[] / recording_generations: protected=['generation', 'kind', 'issuance_order', 'holder_device', 'granted_at']; allowed=['confirmed_watermark', 'applied_prefix', 'revoked_at', 'retired_at'] | 一致 | `operation_events.immutability.protected_columns` に `d5`。D5 の一意性・不変性の定義節 5-4 の台帳は `idempotency_ledger`(protected に確定結果・種別・先着原本) data-model.md:5-3,5-4 |
| 出現 028: 不変 | data-model.md §5-3 — (1-1 節の定義の所在表。5 周目 P1-4 の是正 — 本節にあった全経路一意・不変性・再掲・照合位置の | event_slots: protected=['tenant_id', 'game_id', 'generation', 'd1']; allowed=['confirmed_version'] / operation_events: protected=['tenant_id', 'id', 'game_id', 'generation', 'd1', 'd5', 'event_kind', 'payload', 'state_diff']; allowed=['d2', 'replaced_at', 'retired_at'] / temporary_player_id_mappings: protected=['temporary_id', 'player_id']; allowed=[] / recording_generations: protected=['generation', 'kind', 'issuance_order', 'holder_device', 'granted_at']; allowed=['confirmed_watermark', 'applied_prefix', 'revoked_at', 'retired_at'] | 一致 | §5-3(:772-774)は D5 の不変性の定義を §5-4 へ寄せた旨の所在参照(定義節ではない)。定義 §5-4(:1058-1060)「先着の原本と確定結果を上書きしない・種別の変更もしない」に対し `idempotency_ledger.immutability.protected_columns=['kind','source_fingerprint','result','reason']`(allowed は退役状態 `retired_at` のみ)。出現 039 と同一事実 data-model.md:5-3,5-4 |
| 出現 029: 後退させない | data-model.md §5-3 — \| 単調非減少 \| 後退させない。墓標を越えて前進する \| 影響差分 5 \| | event_slots: protected=['tenant_id', 'game_id', 'generation', 'd1']; allowed=['confirmed_version'] / operation_events: protected=['tenant_id', 'id', 'game_id', 'generation', 'd1', 'd5', 'event_kind', 'payload', 'state_diff']; allowed=['d2', 'replaced_at', 'retired_at'] / temporary_player_id_mappings: protected=['temporary_id', 'player_id']; allowed=[] / recording_generations: protected=['generation', 'kind', 'issuance_order', 'holder_device', 'granted_at']; allowed=['confirmed_watermark', 'applied_prefix', 'revoked_at', 'retired_at'] | 一致 | design.md 5 節マトリクス 1 行目「記録権世代 — D3 の後退(減少方向のみ拒否)」。実装 = migration 0008(:129-131)BEFORE UPDATE トリガ、検査 = backend/tests/db/test_immutability_matrix.py:443(同値許可・前進許可・後退拒否)。manifest immutability は protected/allowed の 2 値で単調性を表現しないが挙動・検査は存在(表現追加は申し送り) data-model.md:5-3 |
| 出現 030: 後退させない | data-model.md §5-3 — \| 後退禁止の機構 \| CHECK 制約では表せない(行の更新前後を比較する必要がある)。更新文を WHERE D3 <= :new_d3 の条件付き UPDATE に限定するか、BEFORE UPDATE トリガで後退を拒否する。本書は「後退させない責務を DB 側に置く」ことを規則として定め、どちらの機構を採るかは実装で選ぶ(いずれも標準 PostgreSQL の範囲 — 7.1) \| 影響差分 5「後退しない制約を機構で表せるか」 \| | event_slots: protected=['tenant_id', 'game_id', 'generation', 'd1']; allowed=['confirmed_version'] / operation_events: protected=['tenant_id', 'id', 'game_id', 'generation', 'd1', 'd5', 'event_kind', 'payload', 'state_diff']; allowed=['d2', 'replaced_at', 'retired_at'] / temporary_player_id_mappings: protected=['temporary_id', 'player_id']; allowed=[] / recording_generations: protected=['generation', 'kind', 'issuance_order', 'holder_device', 'granted_at']; allowed=['confirmed_watermark', 'applied_prefix', 'revoked_at', 'retired_at'] | 一致 | 正本が実装へ送った機構選択は BEFORE UPDATE トリガで確定(design.md 5-1 節・migration 0008:129-131)。出現 029 と同じ検査で覆われる data-model.md:5-3,12-8 |
| 出現 031: 不変 | data-model.md §5-3 — 本書が定める不変条件: | event_slots: protected=['tenant_id', 'game_id', 'generation', 'd1']; allowed=['confirmed_version'] / operation_events: protected=['tenant_id', 'id', 'game_id', 'generation', 'd1', 'd5', 'event_kind', 'payload', 'state_diff']; allowed=['d2', 'replaced_at', 'retired_at'] / temporary_player_id_mappings: protected=['temporary_id', 'player_id']; allowed=[] / recording_generations: protected=['generation', 'kind', 'issuance_order', 'holder_device', 'granted_at']; allowed=['confirmed_watermark', 'applied_prefix', 'revoked_at', 'retired_at'] | 対象外 | 「本書が定める不変条件:」という見出しであり、列の不変性ではない data-model.md:5-3 |
| 出現 032: 不変 | data-model.md §5-3 — \| # \| 不変条件 \| | event_slots: protected=['tenant_id', 'game_id', 'generation', 'd1']; allowed=['confirmed_version'] / operation_events: protected=['tenant_id', 'id', 'game_id', 'generation', 'd1', 'd5', 'event_kind', 'payload', 'state_diff']; allowed=['d2', 'replaced_at', 'retired_at'] / temporary_player_id_mappings: protected=['temporary_id', 'player_id']; allowed=[] / recording_generations: protected=['generation', 'kind', 'issuance_order', 'holder_device', 'granted_at']; allowed=['confirmed_watermark', 'applied_prefix', 'revoked_at', 'retired_at'] | 対象外 | 不変条件表の表頭であり、列の不変性ではない data-model.md:5-3 |
| 出現 033: 不変 | data-model.md §5-3 — #### D2 に不変前提の索引を張らない(M03 — 索引原則へ) | event_slots: protected=['tenant_id', 'game_id', 'generation', 'd1']; allowed=['confirmed_version'] / operation_events: protected=['tenant_id', 'id', 'game_id', 'generation', 'd1', 'd5', 'event_kind', 'payload', 'state_diff']; allowed=['d2', 'replaced_at', 'retired_at'] / temporary_player_id_mappings: protected=['temporary_id', 'player_id']; allowed=[] / recording_generations: protected=['generation', 'kind', 'issuance_order', 'holder_device', 'granted_at']; allowed=['confirmed_watermark', 'applied_prefix', 'revoked_at', 'retired_at'] | 対象外 | D2 に不変前提の索引を張らないという索引方針であり、列の不変性ではない data-model.md:5-3 |
| 出現 034: 不変 | data-model.md §5-3 — - D2 を含む索引は「範囲・ソート用」に限る。D2 が不変であることを前提にした構造 | event_slots: protected=['tenant_id', 'game_id', 'generation', 'd1']; allowed=['confirmed_version'] / operation_events: protected=['tenant_id', 'id', 'game_id', 'generation', 'd1', 'd5', 'event_kind', 'payload', 'state_diff']; allowed=['d2', 'replaced_at', 'retired_at'] / temporary_player_id_mappings: protected=['temporary_id', 'player_id']; allowed=[] / recording_generations: protected=['generation', 'kind', 'issuance_order', 'holder_device', 'granted_at']; allowed=['confirmed_watermark', 'applied_prefix', 'revoked_at', 'retired_at'] | 対象外 | 同上(索引方針) data-model.md:5-3 |
| 出現 035: 書き換えない | data-model.md §5-3 — 確定済みイベントの修正・削除は、新しい変更イベントとして追記する。既存イベントを書き換えない | event_slots: protected=['tenant_id', 'game_id', 'generation', 'd1']; allowed=['confirmed_version'] / operation_events: protected=['tenant_id', 'id', 'game_id', 'generation', 'd1', 'd5', 'event_kind', 'payload', 'state_diff']; allowed=['d2', 'replaced_at', 'retired_at'] / temporary_player_id_mappings: protected=['temporary_id', 'player_id']; allowed=[] / recording_generations: protected=['generation', 'kind', 'issuance_order', 'holder_device', 'granted_at']; allowed=['confirmed_watermark', 'applied_prefix', 'revoked_at', 'retired_at'] | 一致 | operation_events.protected_columns に payload・state_diff・event_kind があり、確定済みイベントを書き換えない契約に対応する |
| 出現 036: 不変 | data-model.md §5-4 — \| 不変性 \| 先着の原本と確定結果を上書きしない(同 影響差分 14)。種別の変更もしない — 一度「拒否」で確定した D5 を後から「受理」に変えない \| | operation_events: protected=['tenant_id', 'id', 'game_id', 'generation', 'd1', 'd5', 'event_kind', 'payload', 'state_diff']; allowed=['d2', 'replaced_at', 'retired_at'] / idempotency_ledger: protected=['kind', 'source_fingerprint', 'result', 'reason']; allowed=['retired_at'] / rejected_event_originals: protected=['d5', 'kind', 'payload']; allowed=[] | 一致 | operation_events.protected_columns に d5・payload・state_diff があり、先着の原本と確定結果を上書きしない契約に対応する |
| 出現 037: 変えない | data-model.md §5-4 — \| 不変性 \| 先着の原本と確定結果を上書きしない(同 影響差分 14)。種別の変更もしない — 一度「拒否」で確定した D5 を後から「受理」に変えない \| | operation_events: protected=['tenant_id', 'id', 'game_id', 'generation', 'd1', 'd5', 'event_kind', 'payload', 'state_diff']; allowed=['d2', 'replaced_at', 'retired_at'] / idempotency_ledger: protected=['kind', 'source_fingerprint', 'result', 'reason']; allowed=['retired_at'] / rejected_event_originals: protected=['d5', 'kind', 'payload']; allowed=[] | 一致 | 同上(operation_events.protected_columns の d5・payload・state_diff) |
| 出現 038: 上書きしない | data-model.md §5-4 — \| 不変性 \| 先着の原本と確定結果を上書きしない(同 影響差分 14)。種別の変更もしない — 一度「拒否」で確定した D5 を後から「受理」に変えない \| | operation_events: protected=['tenant_id', 'id', 'game_id', 'generation', 'd1', 'd5', 'event_kind', 'payload', 'state_diff']; allowed=['d2', 'replaced_at', 'retired_at'] / idempotency_ledger: protected=['kind', 'source_fingerprint', 'result', 'reason']; allowed=['retired_at'] / rejected_event_originals: protected=['d5', 'kind', 'payload']; allowed=[] | 一致 | 同上(operation_events.protected_columns の d5・payload・state_diff) |
| 出現 039: 不変 | data-model.md §5-4 — → D5 台帳に「確定結果」を不変で持つ。確定結果を書く T 要素は経路で違う — | operation_events: protected=['tenant_id', 'id', 'game_id', 'generation', 'd1', 'd5', 'event_kind', 'payload', 'state_diff']; allowed=['d2', 'replaced_at', 'retired_at'] / idempotency_ledger: protected=['kind', 'source_fingerprint', 'result', 'reason']; allowed=['retired_at'] / rejected_event_originals: protected=['d5', 'kind', 'payload']; allowed=[] | 一致 | §5-4(:1058-1060)「D5 台帳に確定結果を不変で持つ / 先着の原本と確定結果を上書きしない / 種別の変更もしない」→ `idempotency_ledger.immutability.protected_columns=['kind','source_fingerprint','result','reason']`。確定結果 = result・reason、先着原本 = source_fingerprint、種別 = kind。design.md 5 節マトリクス 2 行目(D5 台帳)と一致 data-model.md:5-4 |
| 出現 040: 不変 | data-model.md §5-4 — - D5 台帳の確定結果は上書きしない — 先着の原本と確定結果を不変にする(同 影響差分 14) | operation_events: protected=['tenant_id', 'id', 'game_id', 'generation', 'd1', 'd5', 'event_kind', 'payload', 'state_diff']; allowed=['d2', 'replaced_at', 'retired_at'] / idempotency_ledger: protected=['kind', 'source_fingerprint', 'result', 'reason']; allowed=['retired_at'] / rejected_event_originals: protected=['d5', 'kind', 'payload']; allowed=[] | 一致 | 同上(operation_events.protected_columns の d5・payload・state_diff) |
| 出現 041: 上書きしない | data-model.md §5-4 — - D5 台帳の確定結果は上書きしない — 先着の原本と確定結果を不変にする(同 影響差分 14) | operation_events: protected=['tenant_id', 'id', 'game_id', 'generation', 'd1', 'd5', 'event_kind', 'payload', 'state_diff']; allowed=['d2', 'replaced_at', 'retired_at'] / idempotency_ledger: protected=['kind', 'source_fingerprint', 'result', 'reason']; allowed=['retired_at'] / rejected_event_originals: protected=['d5', 'kind', 'payload']; allowed=[] | 一致 | 同上(operation_events.protected_columns の d5・payload・state_diff) |
| 出現 042: 不変 | data-model.md §6-2 — 種別で区別する)。述語の 退役していない は 12-3 節の退役の不変条件 5。 | participation_intervals: protected=[]; allowed=['valid_until_d2', 'retired_at'] | 対象外 | 退役の不変条件(12-3 節)への参照であり、列の不変性ではない data-model.md:6-2 |
| 出現 043: 不変 | data-model.md §6-4 — \| 同期済み \| 初回確定から 24 時間。RG1 中と確認不能な間は期限清掃を止める \| D5 台帳の確定結果(不変)。サーバー側に 24 時間の期限を持たない — 端末側の清掃規律である \| | evacuated_event_originals: protected=['game_id', 'old_generation', 'original_d1', 'd5', 'kind', 'origin']; allowed=['status', 'imported_event_id', 'retention_deadline', 'discarded_at'] | 一致 | §6-4 保持期間表(:1245)「同期済み — D5 台帳の確定結果(不変)」は §5-4 の不変性の再掲で、サーバー側に新たな列・期限を要求しない(「サーバー側に 24 時間の期限を持たない」)。実装側は出現 039 と同じ `idempotency_ledger.protected_columns`(result・reason) data-model.md:6-4,5-4 |
| 出現 044: 不変 | data-model.md §7-1 — \| 可変性 \| kind は不変。対戦相手行を後から自テナント行へ昇格させない(共有範囲が遡って変わるため) \| | team_records: protected=['kind']; allowed=['name', 'hidden_at'] | 一致 | team_records.immutability.protected_columns = [kind] |
| 出現 045: 昇格させない | data-model.md §7-1 — \| 可変性 \| kind は不変。対戦相手行を後から自テナント行へ昇格させない(共有範囲が遡って変わるため) \| | team_records: protected=['kind']; allowed=['name', 'hidden_at'] | 一致 | team_records.immutability.protected_columns = [kind](昇格させない = kind を更新させない) |
| 出現 046: 不変 | data-model.md §7-1 — kind + 部分一意索引なら循環せずに同じ不変条件を表現できる | team_records: protected=['kind']; allowed=['name', 'hidden_at'] | 対象外 | 同じ不変条件を kind + 部分一意索引で表現できるという構造の説明であり、列の不変性ではない data-model.md:7-1 |
| 出現 047: 不変 | data-model.md §7-2 — \| 内部 ID \| 不変。プレイ・スタメン・カルテはすべてこれを参照する \| FR-015 \| | players: protected=['id']; allowed=['name', 'throws', 'bats', 'uniform_number', 'roster_status_key', 'roster_label_key', 'hidden_at'] | 一致 | players.immutability.protected_columns = [id] |
| 出現 048: 不変 | data-model.md §7-3 — \| 先発: 試合のスタメン(7-4 節) \| 確定時に当時の背番号を焼き込む。試合単位で不変 \| | game_lineups: protected=['entries_with_uniform_number_snapshot']; allowed=[] / participation_intervals: protected=[]; allowed=['valid_until_d2', 'retired_at'] | 一致 | game_lineups.immutability.protected_columns = [entries_with_uniform_number_snapshot]、allowed_update_columns は空 |
| 出現 049: 不変 | data-model.md §7-4 — - スタメン記憶の一意性: UNIQUE (tenant_id, チームレコード) WHERE 退役していない。テナント内に閉じている(3-4 規則 1)。述語は 12-3 節の退役の不変条件 5(8 周目 P1-7 — 移行は旧 stamem を「移行由来の記憶」として保全する〔7-1 節〕ので移行が行を作る) | lineup_memories: protected=[]; allowed=['lineup', 'retired_at'] / game_lineups: protected=['entries_with_uniform_number_snapshot']; allowed=[] / migrated_final_lineups: protected=['game_id', 'team_record_id', 'raw_lineup', 'legacy_row_identifier', 'import_batch_id']; allowed=['retired_at'] | 対象外 | スタメン記憶の一意性述語であり、列の不変性ではない data-model.md:7-4 |
| 出現 050: 追記のみ | data-model.md §8-4 — - 追記のみ(論理削除の対象にしない)。旧システムは「管理者の破壊的操作(試合削除等)に操作記録がない」 | admin_operation_logs: protected=['id', 'occurred_at', 'operation_kind', 'target', 'tenant_id', 'group_id']; allowed=[] | 一致 | admin_operation_logs は lifecycle.append_mode = 追記専用、protected_columns が全 6 列 |
| 出現 051: 不変 | data-model.md §9 — 制御資源のテーブル構成と越境経路は 3-5・3-6 節。本節はライフサイクルと不変条件を決める。 | analysis_groups: protected=['id']; allowed=['status', 'terminated_at', 'termination_reason'] / group_memberships: protected=['id', 'group_id', 'tenant_id', 'joined_at']; allowed=['role', 'status', 'left_at'] / sharing_grants: protected=['membership_id']; allowed=['grant_flags'] / group_invitations: protected=['id', 'group_id', 'code_hash', 'initial_role']; allowed=['status'] | 対象外 | 本節がライフサイクルと不変条件を決めるという宣言であり、列の不変性ではない data-model.md:9 |
| 出現 052: 不変 | data-model.md §9-1 — - 設定値の不変条件(FR-041): 同時比較表示上限 ≤ 参加テナント上限。これを破る設定値は | group_memberships: protected=['id', 'group_id', 'tenant_id', 'joined_at']; allowed=['role', 'status', 'left_at'] | 対象外 | 設定値の不変条件(比較表示上限 ≤ 参加テナント上限)であり、列の不変性ではない data-model.md:9-1 |
| 出現 053: 不変 | data-model.md §9-4 — ### 9-4. 不変条件: 実効 admin が 1 名以上 | group_memberships: protected=['id', 'group_id', 'tenant_id', 'joined_at']; allowed=['role', 'status', 'left_at'] | 対象外 | 節見出し「不変条件: 実効 admin が 1 名以上」であり、列の不変性ではない data-model.md:9-4 |
| 出現 054: 不変 | data-model.md §9-4 — 「active な admin が 1 名以上存在することを不変条件とし、最後の admin は離脱も降格もできない」(FR-041 の文言。本書ではこれを 3-0 節の実効 admin として読む — テナント無効化を含めるため) | group_memberships: protected=['id', 'group_id', 'tenant_id', 'joined_at']; allowed=['role', 'status', 'left_at'] | 対象外 | 実効 admin が 1 名以上という業務上の不変条件であり、列の不変性ではない data-model.md:9-4 |
| 出現 055: 不変 | data-model.md §9-4-B — 不変条件(実効 admin が 1 名以上)の検証経路を離脱・降格に限定していたため、 | analysis_groups: protected=['id']; allowed=['status', 'terminated_at', 'termination_reason'] / sharing_grants: protected=['membership_id']; allowed=['grant_flags'] | 対象外 | 同上(実効 admin の不変条件の検証経路) data-model.md:9-4-B |
| 出現 056: 不変 | data-model.md §9-4-A — 9-4 の不変条件(実効 admin が 1 名以上 — 述語の定義は 3-0 節が正)は離脱・降格に掛かるものであり、 | analysis_groups: protected=['id']; allowed=['status', 'terminated_at', 'termination_reason'] | 対象外 | 同上(実効 admin の不変条件) data-model.md:9-4-A |
| 出現 057: 不変 | data-model.md §9-4-A — - 9-4 の不変条件との関係: terminated のグループは不変条件の対象外(admin 0 名で終了する)。 | analysis_groups: protected=['id']; allowed=['status', 'terminated_at', 'termination_reason'] | 対象外 | 同上(終了済みグループは不変条件の対象外) data-model.md:9-4-A |
| 出現 058: 不変 | data-model.md §9-4-A — - 9-4 の不変条件との関係: terminated のグループは不変条件の対象外(admin 0 名で終了する)。 | analysis_groups: protected=['id']; allowed=['status', 'terminated_at', 'termination_reason'] | 対象外 | 同上(終了済みグループは不変条件の対象外) data-model.md:9-4-A |
| 出現 059: 不変 | data-model.md §10-1 — - 一意性: UNIQUE (tenant_id, 選手, カルテ種別) WHERE 退役していない(述語は 12-3 節の退役の不変条件 5)。テナント内に閉じている(選手がチームレコード経由で | medical_notes: protected=['player_id', 'note_kind']; allowed=['content', 'version', 'retired_at'] / medical_note_versions: protected=['medical_note_id', 'version', 'content', 'origin', 'recorded_at']; allowed=['retained_for_restore'] | 対象外 | カルテ所見の一意性述語であり、列の不変性ではない data-model.md:10-1 |
| 出現 060: 変更不可 | data-model.md §10-3 — \| システム固定 \| 試合区分・在籍区分 \| 変更不可 \| | system_vocabularies: protected=['key', 'category', 'display_name', 'disabled']; allowed=[] / admin_vocabularies: protected=['key', 'category']; allowed=['display_name', 'disabled'] / tenant_vocabularies: protected=['key', 'category']; allowed=['display_name', 'pitch_family', 'abbreviation', 'disabled'] | 一致 | system_vocabularies.immutability.protected_columns = [key, category, display_name, disabled]、allowed_update_columns は空 |
| 出現 061: 不変 | data-model.md §11-1 — \| 同一トランザクション \| 紐づけ判定と削除を同一トランザクションで行い、判定後に紐づいた場合は失敗する(FR-018) \| 本書で決める(不変条件) \| | tenants: protected=[]; allowed=['name', 'enabled', 'disabled_at'] / team_records: protected=['kind']; allowed=['name', 'hidden_at'] / players: protected=['id']; allowed=['name', 'throws', 'bats', 'uniform_number', 'roster_status_key', 'roster_label_key', 'hidden_at'] / games: protected=[]; allowed=['status', 'started_at', 'trashed_at', 'hidden_at'] / medical_notes: protected=['player_id', 'note_kind']; allowed=['content', 'version', 'retired_at'] / system_vocabularies: protected=['key', 'category', 'display_name', 'disabled']; allowed=[] / admin_vocabularies: protected=['key', 'category']; allowed=['display_name', 'disabled'] / tenant_vocabularies: protected=['key', 'category']; allowed=['display_name', 'pitch_family', 'abbreviation', 'disabled'] | 対象外 | 削除の前提条件を同一トランザクションで判定するという不変条件であり、列の不変性ではない data-model.md:11-1 |
| 出現 062: 不変 | data-model.md §11-1 — - 具体的なロック方式・SQL は実装計画へ送る((B))。述語と不変条件だけ今決める | tenants: protected=[]; allowed=['name', 'enabled', 'disabled_at'] / team_records: protected=['kind']; allowed=['name', 'hidden_at'] / players: protected=['id']; allowed=['name', 'throws', 'bats', 'uniform_number', 'roster_status_key', 'roster_label_key', 'hidden_at'] / games: protected=[]; allowed=['status', 'started_at', 'trashed_at', 'hidden_at'] / medical_notes: protected=['player_id', 'note_kind']; allowed=['content', 'version', 'retired_at'] / system_vocabularies: protected=['key', 'category', 'display_name', 'disabled']; allowed=[] / admin_vocabularies: protected=['key', 'category']; allowed=['display_name', 'disabled'] / tenant_vocabularies: protected=['key', 'category']; allowed=['display_name', 'pitch_family', 'abbreviation', 'disabled'] | 対象外 | 述語と不変条件だけを本書で決めるという射程の記述であり、列の不変性ではない data-model.md:11-1 |
| 出現 063: 変えない | data-model.md §11-2 — 変えないため。逆に 1〜7 は元データが変わるので全範囲に効く | system_settings: protected=['key']; allowed=['value', 'updated_at'] / invalidation_intents: protected=['tenant_id', 'intent_id', 'scope_kind', 'game_id', 'player_id', 'group_id', 'requesting_tenant_id', 'target_tenant_id', 'period', 'chart_kind']; allowed=['delivery_status', 'delivered_at'] | 対象外 | 索引の効き方の説明(元データが変わらない範囲)であり、列の不変性ではない data-model.md:11-2 |
| 出現 064: 不変 | data-model.md §11-3 — 8. D2 に不変前提の索引を張らない(M03 — 5-3 節)。D2 は O2 の局所再採番で値が動くため、 | manifest immutability: 該当候補なし | 対象外 | D2 に不変前提の索引を張らないという索引原則であり、列の不変性ではない data-model.md:11-3 |
| 出現 065: 不変 | data-model.md §12-1 — - 互換出力(FR-031)では列名・順序を維持する — 88 列は不変の契約であり、列名が中身と乖離した列も | play_rows: protected=['id', 'source_event_id', 'legacy_row_identifier']; allowed=['version', 'hidden_at'] | 対象外 | 88 列互換出力の契約(列名・順序を維持する)であり、新スキーマの列の不変性ではない data-model.md:12-1 |
| 出現 066: 不変 | data-model.md §12-2 — - 88 列本体は不変。収まらない新概念(交代履歴・状態補正・責任投手など)はサイドカー方式が正 | manifest immutability: 該当候補なし | 対象外 | 88 列本体を増やさないというサイドカー方針であり、列の不変性ではない data-model.md:12-2 |
| 出現 067: 不変 | data-model.md §12-3 — \| # \| 不変条件 \| | tenants: protected=[]; allowed=['name', 'enabled', 'disabled_at'] / team_records: protected=['kind']; allowed=['name', 'hidden_at'] / players: protected=['id']; allowed=['name', 'throws', 'bats', 'uniform_number', 'roster_status_key', 'roster_label_key', 'hidden_at'] / games: protected=[]; allowed=['status', 'started_at', 'trashed_at', 'hidden_at'] / lineup_memories: protected=[]; allowed=['lineup', 'retired_at'] / game_lineups: protected=['entries_with_uniform_number_snapshot']; allowed=[] / participation_intervals: protected=[]; allowed=['valid_until_d2', 'retired_at'] / event_slots: protected=['tenant_id', 'game_id', 'generation', 'd1']; allowed=['confirmed_version'] / operation_events: protected=['tenant_id', 'id', 'game_id', 'generation', 'd1', 'd5', 'event_kind', 'payload', 'state_diff']; allowed=['d2', 'replaced_at', 'retired_at'] / play_rows: protected=['id', 'source_event_id', 'legacy_row_identifier']; allowed=['version', 'hidden_at'] / play_runners: protected=[]; allowed=['status', 'status_source', 'retired_at'] / temporary_player_id_mappings: protected=['temporary_id', 'player_id']; allowed=[] / idempotency_ledger: protected=['kind', 'source_fingerprint', 'result', 'reason']; allowed=['retired_at'] / evacuated_event_originals: protected=['game_id', 'old_generation', 'original_d1', 'd5', 'kind', 'origin']; allowed=['status', 'imported_event_id', 'retention_deadline', 'discarded_at'] / recording_generations: protected=['generation', 'kind', 'issuance_order', 'holder_device', 'granted_at']; allowed=['confirmed_watermark', 'applied_prefix', 'revoked_at', 'retired_at'] / medical_notes: protected=['player_id', 'note_kind']; allowed=['content', 'version', 'retired_at'] / migration_quarantine: protected=['id', 'import_batch_id', 'source_read_order', 'raw_payload']; allowed=[] / migrated_final_lineups: protected=['game_id', 'team_record_id', 'raw_lineup', 'legacy_row_identifier', 'import_batch_id']; allowed=['retired_at'] / migration_runs: protected=['id', 'started_at', 'source_counts']; allowed=['completed_at', 'generated_copy_counts', 'validation_results', 'retired_at'] / migration_warning_reports: protected=['id', 'import_batch_id', 'source_kind', 'legacy_row_identifier', 'warning_kind', 'details']; allowed=[] | 対象外 | 移行が要求する不変条件表の表頭であり、列の不変性ではない data-model.md:12-3 |
| 出現 068: 不変 | data-model.md §12-3 — \| 4 \| D5 は退役後も消費済みのままである((テナント, D5) の一意性は台帳が持ち、退役で解放しない — 5-4 節「不変性」)。→ やり直しは新しいバッチ識別子と新しい D5 で行う。同じ D5 を再利用して再投入しない \| | tenants: protected=[]; allowed=['name', 'enabled', 'disabled_at'] / team_records: protected=['kind']; allowed=['name', 'hidden_at'] / players: protected=['id']; allowed=['name', 'throws', 'bats', 'uniform_number', 'roster_status_key', 'roster_label_key', 'hidden_at'] / games: protected=[]; allowed=['status', 'started_at', 'trashed_at', 'hidden_at'] / lineup_memories: protected=[]; allowed=['lineup', 'retired_at'] / game_lineups: protected=['entries_with_uniform_number_snapshot']; allowed=[] / participation_intervals: protected=[]; allowed=['valid_until_d2', 'retired_at'] / event_slots: protected=['tenant_id', 'game_id', 'generation', 'd1']; allowed=['confirmed_version'] / operation_events: protected=['tenant_id', 'id', 'game_id', 'generation', 'd1', 'd5', 'event_kind', 'payload', 'state_diff']; allowed=['d2', 'replaced_at', 'retired_at'] / play_rows: protected=['id', 'source_event_id', 'legacy_row_identifier']; allowed=['version', 'hidden_at'] / play_runners: protected=[]; allowed=['status', 'status_source', 'retired_at'] / temporary_player_id_mappings: protected=['temporary_id', 'player_id']; allowed=[] / idempotency_ledger: protected=['kind', 'source_fingerprint', 'result', 'reason']; allowed=['retired_at'] / evacuated_event_originals: protected=['game_id', 'old_generation', 'original_d1', 'd5', 'kind', 'origin']; allowed=['status', 'imported_event_id', 'retention_deadline', 'discarded_at'] / recording_generations: protected=['generation', 'kind', 'issuance_order', 'holder_device', 'granted_at']; allowed=['confirmed_watermark', 'applied_prefix', 'revoked_at', 'retired_at'] / medical_notes: protected=['player_id', 'note_kind']; allowed=['content', 'version', 'retired_at'] / migration_quarantine: protected=['id', 'import_batch_id', 'source_read_order', 'raw_payload']; allowed=[] / migrated_final_lineups: protected=['game_id', 'team_record_id', 'raw_lineup', 'legacy_row_identifier', 'import_batch_id']; allowed=['retired_at'] / migration_runs: protected=['id', 'started_at', 'source_counts']; allowed=['completed_at', 'generated_copy_counts', 'validation_results', 'retired_at'] / migration_warning_reports: protected=['id', 'import_batch_id', 'source_kind', 'legacy_row_identifier', 'warning_kind', 'details']; allowed=[] | 対象外 | D5 は退役後も消費済みという業務上の不変条件であり、列の不変性ではない data-model.md:12-3 |
| 出現 069: 不変 | data-model.md §12-3 — \| 5 \| 退役したバッチが作った業務的一意性の述語へ 退役していない を加える(4 周目 P1-3 / 5 周目 P1-9 の是正)。対象は 3-4 節の全数表のうち「移行が行を作り得る」もの全数であり、述語を加えるのは次の 7 件である: D2(5-3)/ 走者(5-2)/ 出場区間(6-2 — 投影なので再生成で作り直す)/ カルテ所見(10-1)/ スタメン記憶(7-4 — 移行は旧 stamem を「移行由来の記憶」として保全する〔7-1 節〕ので移行が行を作る。… | tenants: protected=[]; allowed=['name', 'enabled', 'disabled_at'] / team_records: protected=['kind']; allowed=['name', 'hidden_at'] / players: protected=['id']; allowed=['name', 'throws', 'bats', 'uniform_number', 'roster_status_key', 'roster_label_key', 'hidden_at'] / games: protected=[]; allowed=['status', 'started_at', 'trashed_at', 'hidden_at'] / lineup_memories: protected=[]; allowed=['lineup', 'retired_at'] / game_lineups: protected=['entries_with_uniform_number_snapshot']; allowed=[] / participation_intervals: protected=[]; allowed=['valid_until_d2', 'retired_at'] / event_slots: protected=['tenant_id', 'game_id', 'generation', 'd1']; allowed=['confirmed_version'] / operation_events: protected=['tenant_id', 'id', 'game_id', 'generation', 'd1', 'd5', 'event_kind', 'payload', 'state_diff']; allowed=['d2', 'replaced_at', 'retired_at'] / play_rows: protected=['id', 'source_event_id', 'legacy_row_identifier']; allowed=['version', 'hidden_at'] / play_runners: protected=[]; allowed=['status', 'status_source', 'retired_at'] / temporary_player_id_mappings: protected=['temporary_id', 'player_id']; allowed=[] / idempotency_ledger: protected=['kind', 'source_fingerprint', 'result', 'reason']; allowed=['retired_at'] / evacuated_event_originals: protected=['game_id', 'old_generation', 'original_d1', 'd5', 'kind', 'origin']; allowed=['status', 'imported_event_id', 'retention_deadline', 'discarded_at'] / recording_generations: protected=['generation', 'kind', 'issuance_order', 'holder_device', 'granted_at']; allowed=['confirmed_watermark', 'applied_prefix', 'revoked_at', 'retired_at'] / medical_notes: protected=['player_id', 'note_kind']; allowed=['content', 'version', 'retired_at'] / migration_quarantine: protected=['id', 'import_batch_id', 'source_read_order', 'raw_payload']; allowed=[] / migrated_final_lineups: protected=['game_id', 'team_record_id', 'raw_lineup', 'legacy_row_identifier', 'import_batch_id']; allowed=['retired_at'] / migration_runs: protected=['id', 'started_at', 'source_counts']; allowed=['completed_at', 'generated_copy_counts', 'validation_results', 'retired_at'] / migration_warning_reports: protected=['id', 'import_batch_id', 'source_kind', 'legacy_row_identifier', 'warning_kind', 'details']; allowed=[] | 対象外 | 退役述語を一意性へ加えるという不変条件であり、列の不変性ではない data-model.md:12-3 |
| 出現 070: 不変 | data-model.md §12-3 — \| 5 \| 退役したバッチが作った業務的一意性の述語へ 退役していない を加える(4 周目 P1-3 / 5 周目 P1-9 の是正)。対象は 3-4 節の全数表のうち「移行が行を作り得る」もの全数であり、述語を加えるのは次の 7 件である: D2(5-3)/ 走者(5-2)/ 出場区間(6-2 — 投影なので再生成で作り直す)/ カルテ所見(10-1)/ スタメン記憶(7-4 — 移行は旧 stamem を「移行由来の記憶」として保全する〔7-1 節〕ので移行が行を作る。… | tenants: protected=[]; allowed=['name', 'enabled', 'disabled_at'] / team_records: protected=['kind']; allowed=['name', 'hidden_at'] / players: protected=['id']; allowed=['name', 'throws', 'bats', 'uniform_number', 'roster_status_key', 'roster_label_key', 'hidden_at'] / games: protected=[]; allowed=['status', 'started_at', 'trashed_at', 'hidden_at'] / lineup_memories: protected=[]; allowed=['lineup', 'retired_at'] / game_lineups: protected=['entries_with_uniform_number_snapshot']; allowed=[] / participation_intervals: protected=[]; allowed=['valid_until_d2', 'retired_at'] / event_slots: protected=['tenant_id', 'game_id', 'generation', 'd1']; allowed=['confirmed_version'] / operation_events: protected=['tenant_id', 'id', 'game_id', 'generation', 'd1', 'd5', 'event_kind', 'payload', 'state_diff']; allowed=['d2', 'replaced_at', 'retired_at'] / play_rows: protected=['id', 'source_event_id', 'legacy_row_identifier']; allowed=['version', 'hidden_at'] / play_runners: protected=[]; allowed=['status', 'status_source', 'retired_at'] / temporary_player_id_mappings: protected=['temporary_id', 'player_id']; allowed=[] / idempotency_ledger: protected=['kind', 'source_fingerprint', 'result', 'reason']; allowed=['retired_at'] / evacuated_event_originals: protected=['game_id', 'old_generation', 'original_d1', 'd5', 'kind', 'origin']; allowed=['status', 'imported_event_id', 'retention_deadline', 'discarded_at'] / recording_generations: protected=['generation', 'kind', 'issuance_order', 'holder_device', 'granted_at']; allowed=['confirmed_watermark', 'applied_prefix', 'revoked_at', 'retired_at'] / medical_notes: protected=['player_id', 'note_kind']; allowed=['content', 'version', 'retired_at'] / migration_quarantine: protected=['id', 'import_batch_id', 'source_read_order', 'raw_payload']; allowed=[] / migrated_final_lineups: protected=['game_id', 'team_record_id', 'raw_lineup', 'legacy_row_identifier', 'import_batch_id']; allowed=['retired_at'] / migration_runs: protected=['id', 'started_at', 'source_counts']; allowed=['completed_at', 'generated_copy_counts', 'validation_results', 'retired_at'] / migration_warning_reports: protected=['id', 'import_batch_id', 'source_kind', 'legacy_row_identifier', 'warning_kind', 'details']; allowed=[] | 対象外 | 同上(退役述語を一意性へ加える) data-model.md:12-3 |
| 出現 071: 不変 | data-model.md §12-5 — 具体的なロック方式・SQL は実装計画へ送る((B))。述語と不変条件は本書で決める。 | manifest immutability: 該当候補なし | 対象外 | 述語と不変条件を本書で決めるという射程の記述であり、列の不変性ではない data-model.md:12-5 |
| 出現 072: 不変 | data-model.md §12-6 — \| ID \| 禁止事項 \| 判定 \| 該当節 \| 対応する構造不変条件 \| | manifest immutability: 該当候補なし | 対象外 | 禁止事項照合表の表頭であり、列の不変性ではない data-model.md:12-6 |
| 出現 073: 不変 | data-model.md §12-6 — 4. 参加上限と実効 admin の不変条件は DB 制約で表現できない(9-1・9-4 節。3-0 節の述語が集合条件を含むため)→ 直列化が必要 | manifest immutability: 該当候補なし | 対象外 | 参加上限と実効 admin の不変条件は DB 制約で表現できないという判定であり、列の不変性ではない data-model.md:12-6 |
| 出現 074: 不変 | data-model.md §12-8 — 4. 同期プロトコルの値の物理写像(列・型・NULL 性・一意制約の定義は 5-3 節。ただし D5 の一意性・確定結果・不変性・経路排他は 5-4 節 — 13 周目 P1-5 の是正) — D1・D2・D3・D4・D5・V10・V11・確定版・ | manifest immutability: 該当候補なし | 対象外 | 射程宣言(同期プロトコルの物理写像の受け取り先)であり、列の不変性ではない data-model.md:12-8 |
| 出現 075: 不変 | data-model.md §12-8 — \| 3-2 節「名前解決経路の非注入」の検査対象集合の厳密な列挙と合格述語 \| TSK-317 \| 範囲確定(PO 裁定 2026-09-09・山田正輝 — 7.3-6)。本書は不変条件までを定める(pg_temp は末尾明示で守り、それ以外の列挙スキーマは CREATE を所有者以外へ与えないことで守る)。確定ゲート 3 周とも本行の集合定義をめぐって指摘が回り、起因が 2 周連続で過半になった — D3 の後退禁止・D2 の局所再採番と同じ線引き \| | manifest immutability: 該当候補なし | 対象外 | 射程宣言(名前解決経路の非注入の検査対象集合)であり、列の不変性ではない data-model.md:12-8 |
| 出現 076: 後退させない | data-model.md §12-8 — \| D3 の後退禁止の機構(条件付き UPDATE か BEFORE UPDATE トリガか) \| 実装 \| どちらも標準 PostgreSQL の範囲。「後退させない責務を DB 側に置く」ことだけを本書が定めた(5-3 節) \| | manifest immutability: 該当候補なし | 対象外 | 射程宣言(D3 の後退禁止の機構は実装で選ぶ)であり、列の不変性ではない data-model.md:12-8 |
| 出現 077: 不変 | data-model.md §12-8 — \| D2 の局所再採番の手順(退避値域の二段更新 / 更新順の保証 のいずれか。別表化は 5-3 節で不採用) \| 同期・記録権の最初の実装タスク \| PO 裁定「範囲確定」(2026-09-08 — 7.3-6)。本書は 4 つの不変条件(T5 の内側で完結・各文の完了時点で一意・値に意味を持たせない・局所)を定め、手順は実装が選ぶ(5-3 節)。D3 の後退禁止と同じ線引き \| | manifest immutability: 該当候補なし | 対象外 | 射程宣言(D2 の局所再採番の手順)であり、列の不変性ではない data-model.md:12-8 |
