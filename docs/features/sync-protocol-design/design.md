---
feature: sync-protocol-design
type: design
date: 2026-08-28
---

# 詳細設計: 同期プロトコル(イベント順序・断中記録・記録権の境界)

**本書は「候補案」であり「決定」ではない。** ハーネス設計書 7.6-4 は `docs/features/<slug>/` を「活動中のみ意味を持つ一時ディレクトリ」と定めている。したがって本書は**推奨と根拠・却下案・トレードオフ**を書くにとどめ、**本書自身を approved / 最終決定として表現しない**。正本化(`docs/design/` への新設 + 7.3 の確定ゲート)は別タスク。

> **ドメイン用語の「確定」は使う。** 要件書が「確定イベント」「確定記録」「確定プレイ」を必須語彙として定めているため(REQ:289 — FR-012「サーバー結果が確定記録となり」)、これらは通常どおり用いる。禁じるのは**本書の結論を「確定した」と称すること**である。

契約(機構が読む状態・実装ステップ表・論点の (A)/(B) 分類)は [plan.md](plan.md) が正。調査の参照先は [research.md](research.md)。**本書はそれらを複製せず参照する**(ハーネス設計書 7.1-1)。

## 1. 本書の構成と、どのステップが何を書くか

| 節 | 内容 | 対応する論点 | 記入 |
| --- | --- | --- | --- |
| **2** | **述語の単一定義**(本書で唯一の定義)+ 禁じる別名 + 適用先 | — | **本ステップ** |
| **3** | 論点の一覧(ID 参照)と禁止事項照合表の枠 | — | ステップ 3 |
| **4** | イベント契約の骨格 | 13・14・15 | ステップ 4 |
| **5** | 順序の 2 系統と射程 | 1・2・3・4 | ステップ 5 |
| **6** | prefix コミットと、拒否・再開の決定表 | 5・6 | ステップ 6 |
| **7** | ACK とキューの状態遷移・クライアント規律・undo・非常口 | 7・8・9・16 | ステップ 7 |
| **8** | サーバー適用の原子性・再計算・補正通知 | 10 | ステップ 8 |
| **9** | 記録権との境界表、退避と取り込み | 12・11 | ステップ 9 |
| **10** | (B) の決めない理由と受け取り先 + NFR-019(c)(d) の観点 | (B) 全件 | ステップ 10 |
| **11** | 性能の構造表 + データモデル影響差分 + 未解決・申し送り | — | ステップ 11 |
| **12** | 検証記録(引用台帳・別名残存検査・禁止事項照合の結果) | — | ステップ 12 |

## 2. 述語の単一定義(**定義はここだけに置く。他の節は参照する**)

**同じ述語を各節で言い直すと伝播漏れが起きる。** 前タスク(製品データモデル設計)では、述語を集約する節を新設してもなお**残存を検出する機械検査が別に必要**だった(台帳 **H-78**・`docs/development/harness-evaluation.md:548`)。さらに、是正時に**正規語の文字列だけで走査して別名を取りこぼす**事故が繰り返し観測されている(台帳 **H-79**・同 `:555`・`:563`)。

したがって本節は **① 唯一の定義 ② 禁じる別名(残存検査の入力)③ 適用先** の 3 点を持つ。**以降の節は本節を参照し、定義を書き直さない**。

### 2-1. 定義

| # | 述語 | 定義 | 典拠 |
| --- | --- | --- | --- |
| D1 | **同期連番** | **クライアントが操作を受理し、端末内の永続キューへ不可分に追記した順**に、**記録権世代の中で単調増加**に採番される整数。**サーバーが受信した順とは無関係**。用途は**欠番検知・再送の重複排除・undo の逆順**に限る | REQ:284(FR-012「クライアント生成のべき等キーと**記録権世代内で単調増加の連番**」)/ REQ:288(同「**追記と連番の採番は不可分に永続化**」)/ REQ:301(FR-013「**連番(FR-012)は記録権世代ごとに採番され、試合の正史は「世代の連結」として定義される**」) |
| D2 | **論理順序** | **プレイの投影・出場区間の境界・下流再計算の起点**に用いる順序。**同期連番とは別の値**として持つ。**後から生成したイベントが過去の位置へ入る**ことがある(任意位置挿入) | REQ:238(FR-007「**任意位置への挿入**」)/ 候補案 `docs/features/product-data-model-design/design.md:411`(「**境界と再計算の起点は「論理順序」側**を使う。**同期連番は同期と undo に限る**」) |
| D3 | **prefix 水位**(適用水位) | サーバーがある**記録権世代**について「**この同期連番までは欠番なく確定した**」と判定した上限値。**確定イベントまたは墓標を越えて前進する**。欠番があればそれ以降は適用しない | REQ:286(FR-012「**連番に欠落があればそれ以降を適用しない**」)/ REQ:290(同「**prefix は確定イベントまたは墓標を越えて前進する**」) |
| D4 | **記録権世代** | 記録権に付与される世代番号。**引き継ぎのたびに更新**され、**同期連番のスコープ**になる。試合の正史は**世代の連結**として定義される | REQ:301(FR-013「記録権には世代番号を持たせ、**引き継ぎのたびに更新する**」) |
| D5 | **べき等キー** | **クライアントが生成する**、操作イベントごとに一意な識別子。**再送が重複到達しても二重適用させない**ために用いる。**改訂版は新しいべき等キーを持つ** | REQ:284(FR-012「**クライアント生成のべき等キー**」)/ REQ:286(同「**べき等キーにより再送が重複到達しても二重適用されず**」)/ REQ:290(同「**新しいべき等キーで送信し**」) |
| D6 | **墓標** | 拒否されたイベントの**同期連番を正式に消費する空イベント**。内容を持たず、**prefix 水位を前進させる**ためだけに存在する | REQ:290(FR-012「**墓標イベント**(当該連番を正式に消費する空イベント)」) |
| D7 | **改訂** | 拒否されたイベントを**同一の同期連番のまま**、**新しいべき等キー**で作り直したイベント。**サーバーは旧版を置換する**。**新しい連番では表さない** | REQ:290(FR-012「**同一連番の改訂版**として新しいべき等キーで送信し、サーバーは旧版を置換する」) |
| D8 | **退避** | **現世代でない記録権世代**のイベントを、**適用せず、かつ破棄せずに保存**すること。閲覧・書き出しの対象になり、**取り込みは現行世代で採番し直す** | REQ:305(FR-013「イベントは適用されないが**破棄されず退避され**」)/ REQ:307(同「**操作者が挿入位置を指定**…現行世代の連番として採番し直し」) |
| D9 | **ACK** | サーバーが同期要求に対して返す**受理の応答**。**何を保証するか**(どの範囲が確定したか・prefix 水位がどこまで進んだか)は **7 節**で定義する。**利用者向けの完了通知とは別物** | REQ:285(FR-012「自動再送成功 / キューが空になり、**どの試合の何球分が送信されたか**が操作者に通知される」)/ REQ:228(FR-006「クライアントの「未送信/同期済み」判定に依存する分岐を置かない(**ACK 消失時の誤判定**…)」) |

### 2-2. 「同期連番」が誤読されやすい点(**本タスクの中核**)

受け取り元の申し送りは「**同期連番が「到着順」であることを明記**し、論理順序を別に持つ前提を共有する」と書かれている(候補案 `docs/features/product-data-model-design/design.md:1774-1775`)。**この「到着順」を「サーバーが受信した順」と読んではならない。**

- 要件書は**イベント生成時にクライアントが採番**することを定めている(REQ:284)。採番は**キュー追記と不可分**である(REQ:288)
- **サーバー受信順は入れ替わり得る**(再送・並行送信・部分到達)。それを連番の意味に採ると、**prefix コミット**(REQ:286 = 欠落があればそれ以降を適用しない)が成立しない — 受信順で採番するなら定義上欠落が生じないため、欠番検知そのものが意味を失う
- したがって **D1 の「到着」はクライアント内のキューへの到着**を指す。**サーバー到着順という概念は本設計に存在しない**

### 2-3. 禁じる別名

**各述語について、本書で使ってはならない語**を列挙する。**「正規語だけを検索して別名を取りこぼす」型の事故**(H-79)を防ぐため、**検査の入力を人手の記憶ではなく表にする**。

**検査の除外区間**: **2-2 節と 2-3 節は検査対象から外す** — この 2 節は**その語を禁じる理由を述べるために当該語を使う**ため、検査すると必ず引っかかる。**除外を隠れた例外にしないよう、ここに明記する**(検査スクリプトはこの見出しを見て除外区間を決める)。**2-1 と 2-4、および 3 節以降は検査対象**。

**表を 2 つに分ける。** 初版では 1 つの表に混ぜていたが、**残存検査を実際に回したところ「再送」「応答」「空イベント」で誤検出が出た** — これらは**それ自体が正しい概念**であり、無条件に禁止できない。**機械検査にかけられるのは無条件の禁止語だけ**であり、文脈依存の語は**人間の逐行確認の対象**である(計画書 6 節が「意味の支持は人間レビューへ分離する」と定めている)。

#### (a) 無条件の禁止語(**機械検査の入力**)

| 述語 | 使わない語 | 理由 |
| --- | --- | --- |
| **D1 同期連番** | サーバー到着順 / サーバー受信順 / 受信順 / 通し番号 / シーケンス番号 | 2-2 の誤読そのもの。**サーバー到着順という概念は本設計に存在しない** |
| **D2 論理順序** | 操作順 / 記録順 / 表示順 | 「操作順」は候補案が **D1 側**の呼び名として使っており(`docs/features/product-data-model-design/design.md:1767`)、**逆の意味に取られる** |
| **D3 prefix 水位** | 適用済み位置 / 同期位置 / オフセット / カーソル | 位置と水位は別概念(水位は「欠番なく確定した上限」であって単なる位置ではない) |
| **D4 記録権世代** | ロック世代 / エポック / リース | 記録権は排他ロックではなく**世代の連結で正史を定義する**機構(REQ:301) |
| **D5 べき等キー** | リクエスト ID / 冪等トークン / 重複判定キー | 「重複判定キー」は旧システムの `プレイの番号` の役割名(`docs/legacy/research/data-layer.md:161`) |
| **D6 墓標** | tombstone(和文中で)/ 欠番埋め / 削除マーカー | 「削除マーカー」は**論理削除**(4.0-2)と混同される |
| **D7 改訂** | リビジョン / 改版 | 要件書の語は「**改訂版**」(REQ:290) |
| **D8 退避** | 待避 / 保留 / デッドレター / アーカイブ | 「待避」は誤字。他は保持期間・復帰経路の含意が異なる |
| **D9 ACK** | 受信確認 / 成功レスポンス / 同期完了通知 | 「同期完了通知」は**利用者へ示す通知**(REQ:285)であって ACK ではない |

#### (b) 条件付きの語(**人間の逐行確認の対象。機械検査にはかけない**)

いずれも**その語自体は正しい**が、**当該述語の意味で使ってはならない**。

| 語 | 正しい用法 | 当該述語の意味で使わない |
| --- | --- | --- |
| **到着順** | 「**クライアントのキューへの**到着順」と限定するときのみ | **単独では使わない**(D1 — 2-2 の誤読を招く) |
| **再送** | **同一イベントを送り直すこと**(REQ:286) | **D7 改訂**の意味で使わない(改訂は内容を作り直した別のイベント) |
| **応答** | HTTP 応答一般 | **D9 ACK** の意味で使わない |
| **空イベント** | **D6 の定義文**(REQ:290 の逐語)でのみ | 定義以外の節で D6 の代わりに使わない |
| **修正 / 訂正** | **FR-007 の利用者操作**(REQ:238-244) | **D7 改訂**の意味で使わない |
| **上書き / スキップ** | 旧システムの挙動の記述(`docs/legacy/research/services-shell.md:150`) | D7・D6 の意味で使わない |
| **UUID** | **一時 ID**(FR-015 の断中登録 — REQ:343) | **D5 べき等キー**の意味で使わない |
| **プレイ番号 / プレイの番号** | **旧システムの 88 列の列名**(`docs/legacy/research/data-layer.md:161`) | D1・D2 の意味で使わない |
| **トークン** | **認証トークン**(NFR-011・REQ:293) | **D4 記録権世代**の意味で使わない |
| **隔離** | **移行の隔離領域**(FR-038) | **D8 退避**の意味で使わない |
| **チェックポイント** | **再計算の粒度**((B) 論点 21) | **D3 prefix 水位**の意味で使わない |

**正本の語との対応(禁じるのではなく、同一物であることを明示する)**:

| 本書の語 | 正本での表記 | 扱い |
| --- | --- | --- |
| **D1 同期連番** | 要件書の「**連番**」(REQ:284・REQ:301)/ 付録D の「**イベント連番**」(REQ:1186 — サイドカー結合キー) | **同一物**。本書では **D1** に統一し、正本を引用するときのみ正本の表記を使う |
| **D3 prefix 水位** | 要件書の「**prefix**」(REQ:286・REQ:290)/ 候補案の「**適用水位**」(同 `:530`) | **同一物**。本書では **D3** に統一する |
| **D8 退避** | 要件書の「**退避**」(REQ:305-307)/ 「**退避イベント**」(REQ:306) | 同一。**イベント単位を指すときは「退避イベント」**と書く |

### 2-4. 適用先(**この定義を使う節**)

| 節 | 使う述語 |
| --- | --- |
| **4**(イベント契約の骨格) | **D1・D4・D5**(サイドカー結合キーは D1 + D4 で成立する — REQ:1186)/ **D7**(改訂が新しい D5 を持つこと) |
| **5**(順序の 2 系統と射程) | **D1・D2**(分離そのもの)/ **D6・D7**(これらが D2 に参加するかの判定) |
| **6**(prefix と、拒否・再開の決定表) | **D3**(前進の規則)/ **D6・D7**(再開の 2 択)/ **D4**(旧世代の拒否)/ **D8**(記録権起因の拒否の行き先) |
| **7**(ACK とキュー状態遷移・undo) | **D9**(保証内容)/ **D1**(欠番検知)/ **D5**(重複排除)/ **D3**(どこまで進んだかの通知) |
| **8**(サーバー適用の原子性・再計算) | **D3・D5**(単一トランザクションの構成要素 — REQ:287)/ **D2**(再計算の起点) |
| **9**(記録権との境界・退避と取り込み) | **D4**(世代更新とフェンシング)/ **D8**(退避と取り込み)/ **D1**(取り込み時の再採番)/ **D2**(挿入位置の指定) |
| **10**((B) の受け取り先) | **D3**(物理表現は (B) 論点 23)/ **D5**(生成方式は (B) 論点 17) |
| **11**(性能・影響差分・申し送り) | **D1・D3・D5・D6・D7・D8**(いずれもデータモデルへの構造上の帰結を持つ) |

**適用先に挙げていない節で述語を使わない。** 使う必要が生じた場合は**本表へ追記してから**使う(表と本文の乖離を防ぐ)。

## 3. 論点の一覧と禁止事項照合表

### 3-1. 論点の一覧(**ID 参照のみ。分類と扱いの正は [plan.md](plan.md) 4 節**)

**同じ表を 2 か所に置かない**(ハーネス設計書 7.1 の単一正本原則)。**(A)/(B) の分類・(A) に入れた根拠・(B) の「決めない理由」は [plan.md](plan.md) 4 節が正**であり、本書は**どの節がどの論点に答えるか**だけを示す。

| 論点 | 見出し(識別用の短い呼び名) | 答える節 |
| --- | --- | --- |
| 1 | 同期連番の意味の正 | **5** |
| 2 | 論理順序の表現方式 | **5** |
| 3 | 挿入時の再採番の可否と、任意位置挿入の実行前提 | **5** |
| 4 | 論理順序の射程と tie-break | **5** |
| 5 | prefix コミットと適用水位の規則 | **6** |
| 6 | 拒否と再開の決定表(応答分類の全域化) | **6** |
| 7 | サーバー応答・ACK の意味とキューの状態遷移 | **7** |
| 8 | 追記と採番の不可分性・単一書き手の条件・キューの可視化義務 | **7** |
| 9 | undo の同期上の表現 | **7** |
| 10 | サーバー適用の原子性と再計算/補正通知 | **8** |
| 11 | 退避と取り込みの手順、保持期間・破棄可否の意味規則 | **9** |
| 12 | 記録権との境界の割り当て(4 列方式) | **9** |
| 13 | イベントの意味上必須な値と不変条件 | **4** |
| 14 | べき等キーの再利用禁止範囲と衝突時の扱い | **4** |
| 15 | FR-040(状態補正)の条件付き扱い | **4** |
| 16 | キューのローカル書き出しと復旧後取り込みの同期上の規則 | **7** |
| 17〜26 | **(B) 10 件** — 物理方式 | **10**(決めない理由と受け取り先) |

### 3-2. 禁止事項照合表の枠

**安定 ID は既存のものを参照し、新規採番しない。** 出所は `docs/features/product-data-model-design/research.md:192-251`(**P-01〜P-60** の 60 件)。**同じ番号空間へ本書が追記すると、並行タスク間で衝突する**(台帳 H-77 と同型)。

> **典拠の行番号に注意**: 出所の表が持つ `REQ:<行>` は**要件書 v2.2 時点**のものである(例: P-25 の `REQ:299` は現行 `:301`、P-26 の `REQ:288` は現行 `:290`)。**本書は現行 v2.3 の行で照合する**。出所は他 feature の一時ディレクトリなので**本タスクでは書き換えない**([plan.md](plan.md) 2 節)。

#### (a) 本書で判定する 22 件(**各ステップが「該当なし / 遵守」を記入する**)

| ID | 禁止事項(要旨) | 現行の典拠 | 判定する節 | 判定 |
| --- | --- | --- | --- | --- |
| **P-02** | 88 列に列を足して新概念を収める(**サイドカー方式が正**) | REQ:1186 | 4 | 未記入 |
| **P-05** | サーバープロセス内に試合状態・横断状態を持つ | REQ:977 | 8 | 未記入 |
| **P-06** | 全件読み込み型集計・ページングなし一覧 | REQ:797(NFR-005) | 8・11 | 未記入 |
| **P-07** | 集計結果を「再計算不能な正本」として持つ | REQ:956 | 8 | 未記入 |
| **P-08** | 事前集計を無効化トリガー表なしで置く | REQ:957 | 8 | 未記入 |
| **P-09** | 物理削除経路・CASCADE 削除 | REQ:145(4.0-2) | 6 | 未記入 |
| **P-20** | `users` テーブル・入力者識別列の実装 | REQ:81(2.2 Won't) | 4 | 未記入 |
| **P-24** | 未同期キューを「プレイ行の列」として設計する | 決定記録 D-14 | 7 | 未記入 |
| **P-25** | 試合グローバルの単一連番(**記録権世代内スコープが正**) | REQ:301 | 5 | 未記入 |
| **P-26** | 墓標なしの欠番放置 / 改訂を新連番で表現する | REQ:290 | 6 | 未記入 |
| **P-27** | 交代をオーダー表の上書きで表す | 決定記録 D-12 | 5 | 未記入 |
| **P-28** | 履歴に残らない状態補正(直接上書き) | 決定記録 `:437` | 4・5 | 未記入 |
| **P-29** | 終了条件成立で試合状態を自動遷移させる | REQ:216 | 5 | 未記入 |
| **P-30** | 規則をマスタ参照のみで持つ(**スナップショット必須**) | REQ:316 | 5 | 未記入 |
| **P-31** | 退避イベントを末尾採番で自動取り込み | REQ:307 | 9 | 未記入 |
| **P-32** | 全毎球行に投球項目を必須にする(**イベント種別で分ける**) | REQ:150 | 4 | 未記入 |
| **P-50** | ドメイン計算を手書き Python / TypeScript で持つ | ADR-003 D-1 | 8 | 未記入 |
| **P-51** | 状態テーブルを両言語の手書き interpreter で読む | ADR-003 D-1 | 8 | 未記入 |
| **P-52** | (β) を「全行を Python で畳み込む」実装にする | ADR-003 D-1 | 8 | 未記入 |
| **P-53** | Pyodide / Rust+WASM / TS↔Py トランスパイル | ADR-003 D-2 | 8 | 未記入 |
| **P-57** | 履歴文脈から差分を落として種別だけ持つ | ADR-003 D-6(`:255`) | 4 | 未記入 |
| **P-58** | FR-040(状態補正)を Must 前提として構造に組み込む | REQ:119・166 | 4 | 未記入 |

**P-50〜P-53 は「本書が再決定しない」ことの確認**である。サーバー再計算(8 節)は ADR-003 が確定した実現方式の上に載るため、**方式を書き換える記述を持たないことを judgement として記録する**。

#### (b) 対象外の 38 件(**理由を付けて明示的に外す。件数は (a) 22 + (b) 38 = 60 で一致する**)

| 理由 | 該当 ID |
| --- | --- |
| **DB スキーマ・物理構成の領域**(TSK-250) | P-01・P-03・P-04・P-10・P-11・P-22 |
| **選手・チームの識別と在籍の領域** | P-13・P-14・P-15・P-16・P-17・P-18・P-19 |
| **語彙・マスタの領域** | P-12・P-34・P-35・P-36・P-37・P-38・P-40 |
| **認可・共同分析グループの領域** | P-21・P-23・P-47・P-48・P-49 |
| **データ移行の領域**(移行仕様タスク) | P-41・P-42・P-43・P-44・P-45・P-46 |
| **座標・成績の導出の領域** | P-33・P-39 |
| **`contracts/` の構造とドメイン計算の検査方式** | P-54・P-55・P-56 |
| **要件改訂を要する事項**(Won't 維持) | P-59・P-60 |

**対象外の判定基準**: 本書が**その事項について決定を書かない**こと。**書く必要が生じたら (a) へ移す**(表を先に直してから書く)。


## 4. イベント契約の骨格(論点 13・14・15)

<!-- ステップ 4 で記入 -->

## 5. 順序の 2 系統と射程(論点 1・2・3・4)

<!-- ステップ 5 で記入 -->

## 6. prefix コミットと、拒否・再開の決定表(論点 5・6)

<!-- ステップ 6 で記入 -->

## 7. ACK とキューの状態遷移・クライアント規律・undo・非常口(論点 7・8・9・16)

<!-- ステップ 7 で記入 -->

## 8. サーバー適用の原子性・再計算・補正通知(論点 10)

<!-- ステップ 8 で記入 -->

## 9. 記録権との境界表、退避と取り込み(論点 12・11)

<!-- ステップ 9 で記入 -->

## 10. (B) の決めない理由と受け取り先 + NFR-019(c)(d) の観点

<!-- ステップ 10 で記入 -->

## 11. 性能の構造表 + データモデル影響差分 + 未解決・申し送り

<!-- ステップ 11 で記入 -->

## 12. 検証記録

<!-- ステップ 12 で記入(引用台帳・別名残存検査・禁止事項照合の結果とスクリプト) -->
