---
feature: oracle-input-baseline
type: design
date: 2026-09-13
---

# 詳細設計: 凍結資産の更新経路と digest 連鎖の短縮

**本書は設計の中身を持つ。** 調査の事実と典拠は [research.md](research.md) が正で、
本書は内容を複製しない(設計書 7.1-1)。**契約(射程・ステップ表・DoD)は
[plan.md](plan.md) が持つ。**

**計画レビュー 1 周目で `P1` 7 件・`P2` 1 件を受け、設計を作り直した。**
**とくに ① 宣言の置き場 ② 承認の機械的検証 ③ 上げ忘れの検出 の 3 点は初版から全面的に変えている。**
初版の何が誤りだったかは 8 節に残す。

## 1. 解くべきこと — 3 つの層

| 層 | 現状 | 望む状態 |
| --- | --- | --- |
| **規範** | **正本に条文が 1 つも無い**。凍結の規律は資産と検査器の中だけの宣言 | **ハーネス設計書に「凍結基準の更新経路」の条文がある** |
| **機構** | **コミット SHA を検査器へ直書き**(`check_authz_catalog.py:107`)。更新は**検査器の編集** | **検査器は SHA を持たない**。基準は**追記のみの台帳**から読む |
| **連鎖** | **母集合 → 派生 3 資産が blob digest を 6 値**持つ | **版参照 3 値**へ。**手で計算する digest が 6 → 0** |

**3 層は独立に失敗しうる。**

## 2. 規範 — ハーネス設計書へ何を書くか

### 2-1. 置き場と版

**7 章の末尾へ `7.7 凍結基準の更新経路` を新設する。** **版は `v1.14 → v1.15`**。
**合否条件を新設するので 7.6-3 後段(版繰り上げ + 7.3 の確定ゲート)を通す**
(先例: ハーネス設計書 `:720` の v1.14・裁定 `Q-1`)。

### 2-2. 条文の骨子

1. **凍結基準の外出し**: ある資産集合の不変性を検査で強制するとき、
   **その基準(コミット・版・digest)は機械可読な宣言として資産側へ置く**。
   **検査器のソースへ基準の値を直書きしない。**
2. **更新経路の必置**: **凍結を謳う検査は、正当な更新経路をちょうど 1 本、
   宣言の中に持たなければならない。** 経路を持たない凍結は置けない。
3. **追記のみ**: **基準の履歴は追記のみとする。** 既存の記録を書き換える変更を検査は受理しない。
4. **更新の記録**: 基準を動かすとき、**動かした事実・理由・承認者・承認日・直前の基準**を
   新しい記録として追記する。**直前の基準が履歴の末尾と一致しない追記を受理しない。**
5. **fail-closed**: 基準の妥当性を確かめられないとき(参照先が解決できない等)、
   **検査は緑にしてはならない。**
6. **本節が定めるのは 1〜5 まで**であり、**何を凍結するか・どの粒度で比べるかは
   各資産の宣言と実装が定める**(`data-model.md:244` と同型の委任)。

**3 と 4 が `P1-2` の反映**である。**追記のみ + 直前基準の連鎖**によって、
**前回の承認記録を使い回す経路を機械で塞ぐ**。

**5 は本タスクで実際に踏んだ欠陥**(`check_authz_catalog.py:4798` の fail-open)から来ている。

### 2-3. 適用範囲の明示(`P1-3` の反映)

**条文は「凍結を謳う検査」全般に掛かる。** したがって**導入と同時に既存の違反を解消する**。
**リポジトリに現存する凍結基準の直書きは 3 つ**(research.md 5 節・実測):

| 直書き | 何の基準か | 本タスクでの扱い |
| --- | --- | --- |
| `scripts/check_authz_catalog.py:107` `ORACLE_INPUT_BASELINE_COMMIT` | **入力ベースライン** | **台帳へ移す** |
| `backend/tests/db/authz/mutation_composition.py:36` `STEP2_BASE_REVISION` | **意味本文の固定基準** | **台帳へ移す** |
| `tests/test_check_authz_catalog.py:38` `AUTHZ_STEP2_BASE_REVISION` | 同上(**同じ値の重複**) | **台帳へ移す**(重複も解消) |

**3 つは種類の違う基準ではなく、「承認された基準点」という同じものの 3 つの写し**である。
**1 つの台帳に 2 種類(入力ベースライン / 意味本文の基準)として持てば足りる。**

**`contracts/authz/function-bodies/manifest.json` の `source_commit` は射程外**
(関数本体スナップショットの基準で、oracle 封印とは別の資産群)。**申し送りにする。**

## 3. 機構 — 追記のみの基準台帳

### 3-1. 置き場(`P1-1` の反映)

**`contracts/authz/frozen-baselines.json` を新設する。**

**初版では `oracle-seal.lock.json` の中へ置こうとしたが、これは誤りだった。**
**seal 自身が backend の意味本文比較の対象**であり(`_oracle_seal_meaning_body`)、
**キーを 1 つ足すだけで `verify_frozen_oracle_unchanged` が落ちる**。
**基準を、その基準で検査される資産の中に置くのは自己言及である。**

**新設しても digest 連鎖は増えない。** 本ファイルは
**`input_assets` にも `sealed_assets` にも入れない**。**誰の digest も持たず、誰からも digest で参照されない。**
**連鎖の外に置くことが要件**である。

### 3-2. 構造

```
{
  "schema_version": 1,
  "asset_kind": "authz_frozen_baselines",
  "baselines": {
    "oracle_input": [ <記録>, … ],      // 追記のみ・末尾が現行
    "oracle_meaning": [ <記録>, … ]     // 同上
  }
}
```

記録 1 件:

```
{
  "commit": "<40hex>",
  "supersedes": "<直前の commit。初回は null>",
  "approved_by": "<人名>",
  "approved_at": "<YYYY-MM-DD>",
  "reason": "<なぜ動かしたか>"
}
```

### 3-3. 検査(`P1-2` の反映)

| # | 述語 | 何を防ぐか |
| --- | --- | --- |
| F-1 | 各系列の**末尾の `commit`** が、対応する検査の基準として使われる | 基準の一元化 |
| F-2 | 各系列で **`記録[i].supersedes == 記録[i-1].commit`**、先頭は `null` | 履歴の連鎖 |
| F-3 | **`approved_by` / `approved_at` / `reason` が空でない**。`approved_at` は ISO 日付 | 空の記録 |
| F-4 | **追記のみ** — **PR の base に存在した記録が 1 件も書き換わっていない・削除されていない** | **承認記録の使い回し・改ざん** |
| F-5 | **`commit` が実在し、到達可能**(`git cat-file -e`)。**解決できなければ red** | **fail-open**(`P1-5` 相当) |
| F-6 | 同一系列に**同じ `commit` が 2 度現れない** | 巻き戻し |

**`F-4` が要**である。**先例がある** — `scripts/check_nfr021_append_only.py` が
**PR の base と HEAD を比べて既存レコードの不変を検査**している(同ファイル `:397` `validate_existing_record_history`)。
**同じ型を使う。**

**これで「前回の承認記録をそのまま使い回す」経路が塞がる**: 基準を動かすには
**新しい記録を追記するしかなく**、追記には `supersedes` の連鎖と非空の承認欄が要る。
**既存記録の書き換えは `F-4` で落ちる。**

### 3-4. 残余リスク(明示する)

**`approved_by` に書かれた人物が実際に承認したかは、機械では検証できない。**
`core_guard.py` はチェックボックスしか見ておらず、実施記録行も承認者も検証しない
(`scripts/core_guard.py:200-209`・`:274-280` — レビュー実測)。

**したがって本設計が機械的に保証するのは次までである**:

- **基準を動かすには、追記の形で承認欄を埋めた新しい記録が要る**(自己申告だが**痕跡が残る**)
- **既存の記録を書き換えて誤魔化す経路は無い**(`F-4`)
- **記録なしに基準だけ動かす経路は無い**(`F-1`・`F-2`)

**「実際に人間が承認したか」は人手統制であり、機械的防御としては数えない。**
**初版はここを取り違えていた**(8 節)。

### 3-5. 検査器の変更

| 現在 | 変更後 |
| --- | --- |
| `ORACLE_INPUT_BASELINE_COMMIT = "0cf994f4…"`(`:107`) | **削除** |
| `if oracle_commit != ORACLE_INPUT_BASELINE_COMMIT`(`:4525`) | **`!= frozen_baselines.oracle_input[-1].commit`** |
| `if result.returncode == 0 and …`(`:4798`) | **`if result.returncode != 0: raise`** を先に置く |
| `STEP2_BASE_REVISION`(backend `:36`) | **台帳の `oracle_meaning[-1].commit` を読む** |
| `AUTHZ_STEP2_BASE_REVISION`(harness `:38`) | 同上(**重複を解消**) |

## 4. 連鎖 — `H-85` 対応案②

### 4-1. 何を「段」と数えるか(`P1-5` の反映)

**初版は「4 段 → 3 段」と書いたが、これは測れない主張だった。** 依存の段は減らない。
**測れる形へ言い直す。**

**定義**: **digest 辺** = 資産 X が資産 Y の内容 digest(`git_blob_digest` / `canonical_sha256` /
`sha256`)を**自分の中に保持している**関係。**辺の数を数える。**

**測り方**: `contracts/` の全 JSON を走査し、`(保持している資産, 参照先のパス, キー名)` の
三つ組を全数列挙する。**変更前後で数える。**

### 4-2. 変える辺

| 現在 | 変更後 |
| --- | --- |
| 派生 3 資産が `requirement_claims_blob_digest` と `requirement_claims_lock_blob_digest` を各自持つ = **6 辺** | 派生 3 資産が **`corpus_version` を 1 つ持つ = 0 辺**(版参照は digest 辺ではない) |

**期待値**: **digest 辺が 6 本減る。手で計算する digest が 6 → 0。**
**これが DoD の測定対象である**(「段数」ではない)。

### 4-3. 上げ忘れの検出(`P1-4` の反映)

**初版は「母集合が版と canonical digest を対で持つ」としたが、これでは防げない。**
**同じ可変資産に両方があり、両方を再封印すれば自己整合する。**

**台帳を使う。** `frozen-baselines.json` へ 3 系列目を足す:

```
"corpus_versions": [
  { "version": 1, "canonical_sha256": "<母集合の digest>",
    "approved_by": …, "approved_at": …, "reason": … },
  …
]
```

**検査**:

| # | 述語 |
| --- | --- |
| G-1 | 母集合の `corpus_version` == 台帳の `corpus_versions[-1].version` |
| G-2 | 母集合の現在の canonical digest == 台帳の `corpus_versions[-1].canonical_sha256` |
| G-3 | **追記のみ**(`F-4` と同じ機構) |
| G-4 | `version` は 1 から連番・重複なし |

**上げ忘れは `G-2` で落ちる**: 母集合を変えると digest が変わり、**台帳の末尾と一致しなくなる**。
**台帳を書き換えて合わせようとすると `G-3` で落ちる**(追記のみ)。
**追記すれば `G-1` が版の不一致で落ちる**ので、**母集合の版も上げるしかない。**

**独立した anchor は「追記のみ」が与える。** これが初版に欠けていた。

## 5. 二段コミット(`P1-6` の反映)

**入力資産を変えると、seal が指すコミット上の blob と一致しなくなる。**
**新 blob を含むコミットを先に作り、その SHA を台帳へ追記して再封印する**必要がある。
**今日 TSK-379 で手で踏んだ二段構造**である。

**ステップ表で 2 つのステップに分ける**(plan.md 4 節のステップ 9・10)。
**「確認」ではなく「成果物」として宣言する。**

## 6. 負例

| # | 負例 | 期待 | 種別 |
| --- | --- | --- | --- |
| N1 | 封印資産の意味本文を改ざん + 再封印 | **red** | **退行**(現在も red) |
| N2 | `oracle_commit` を、8 入力 blob が同一の別コミットへ + 再封印(台帳は据え置き) | **red** | **退行**(現在も red) |
| N3 | 台帳へ追記するが `approved_by` が空 | **red** | 新設(`F-3`) |
| N4 | `oracle_commit` が到達不能なコミット | **red** | 新設(`F-5`) |
| N5 | **既存の記録の `approved_by` を書き換えて基準だけ動かす** | **red** | **新設(`F-4`)— 最重要** |
| N6 | 追記するが `supersedes` が末尾と一致しない | **red** | 新設(`F-2`) |
| N7 | 母集合を変えたが `corpus_version` を上げない | **red** | 新設(`G-2`) |
| N8 | `corpus_version` を上げたが派生 3 資産が追随していない | **red** | 新設(`G-1` 経由) |

**N1・N2 は変更前に測り、変更後も同じ結果になることを確かめる。**
**朝にこの領域で防御を落としているため、物差しを先に作る。**

## 7. 却下・不採用(`P2-8` の反映 — 無宛先の申し送りを作らない)

| 案 | 判定 |
| --- | --- |
| **`ci.yml` へ authz 系スクリプトの実行ステップを足す** | **不採用(閉じる)**。**harness ジョブが `pytest tests/` を無条件実行し、`test_repository_catalog_covers_the_entire_requirements_file` が実スクリプトを subprocess 実行している**(レビュー実測)。**検査の実効に差が無い** |
| **harness 側 `test_oracle_reseal_preserves_inputs_and_changes_only_two_asset_digests` の整理** | **不採用(閉じる)**。backend に包含され弱いが、**無条件実行 vs パスフィルタ依存という差がある**ので**消すと弱くなる場合がある**。**現状維持が安全** |
| `function-bodies/manifest.json` の `source_commit` | **本タスクの射程外**。**台帳の候補へ記録する**(受け取り先を持たせる) |
| **定数を削除して backend の設計に一本化** | **却下**。**(d) の性質を失う**。台帳 `H-85`「**凍結そのものを緩めてはならない**」 |
| **基準を `oracle-seal.lock.json` の中へ置く** | **却下(初版の誤り)**。**自己言及**(3-1) |

## 8. 初版の誤り(記録として残す)

**計画レビュー 1 周目で 7 件の `P1` を受けた。** うち設計を作り直したのは 3 件。

| 初版 | 何が誤りだったか |
| --- | --- |
| **基準を `oracle-seal.lock.json` の中へ置く** | **seal 自身が意味本文の比較対象**で、キーを 1 つ足すだけで backend が落ちる。**自己言及**。「新しいファイルを作ると連鎖が増える」を理由にしたが、**digest を持たないファイルは連鎖に入らない**。**理由の立て方が誤っていた** |
| **「`input_baseline` の編集はコア領域の逐行確認で守られる」** | **`core_guard.py` はチェックボックスしか見ていない**と自分で書いておきながら、**人手統制を機械的防御の代替に数えた**。**朝と同じ型の誤り** |
| **「母集合が版と canonical digest を対で持てば上げ忘れを捕まえられる」** | **同じ可変資産に両方があり、両方を再封印すれば自己整合する**。**独立した anchor が無かった** |
