---
feature: contract-only-runtime-handoff
status: in-review         # active | in-review(/pr が PR 内で更新。完了は PR 状態・Notion・worktree 除去から導出。codex_run.py implement は active 以外を拒否)
承認: 済(2026-09-13・山田正輝)  # 未 | 済(YYYY-MM-DD・承認者)— codex_run.py が「済」でないと実行を拒否する
重さ分類: コア領域        # 軽微 | 通常 | コア領域 | 機械的軽作業(ADR-001 のモデルをラッパーが自動選択)
worktree: ../../..        # worktree ルート(plan.md からの相対 or 絶対)。/task-start が設定
notion: https://app.notion.com/p/3d993b75e6878144bd42ee08d678b68f
branch: feature/contract-only-runtime-handoff
created: 2026-09-13
計画レビュー周回: 2        # 指摘反映を伴うレビュー 1 周ごとに +1(収束確認周は数えない。/plan が更新)
確定ゲート周回: 0          # 指摘反映を伴う敵対レビュー 1 周ごとに +1(同前。/finalize-doc が更新)
実行方式: 通常             # 通常 | fast(fast path 適用時に fast へ — 人間の事前 OK 必須。現在地導出が識別)
反映周コミット: 適用       # 適用 | 規約制定前(必須・既定値なし。確定ゲートの反映周コミット突合の適用境界 — 設計書 6.1)
---

# 実装計画書: contract_only 158 行の runtime テスト受取先を決める(TSK-367)

## 1. 背景・目的

**Notion**: [TSK-367](https://app.notion.com/p/3d993b75e6878144bd42ee08d678b68f)
**調査メモ**: [research.md](research.md)(2026-09-13 — 調査 3 本並列 + 設計レビュー 1 本の統合。全件典拠)

`docs/design/data-model.md` 12-4 のマージゲートが backend の製品機能を止めている。律速の連鎖は
**TSK-344 ←(開始条件)← TSK-317 の PR #3 ←(律速)← 本タスク**である。

`contracts/authz/claim-mutant-map.json` の `claims` のうち、受取先がプレースホルダ
`TSK-270-GROUP-2` のまま残る **158 行**(一意 `runtime_test_owner.id` **152** 個)について、
runtime 検査の受取先を決める。決まらないと PR #3 が「受取先が空白の claim を封印で固定する」
ことになり封印できない(裁定 `D-15` — `../pg-authz-verification-g2/plan.md:90`)。

**関連する要件**: [NFR-010](../../requirements/requirements-pitchlog-2026-07-22.md)(チーム間データ分離)/
`NFR-019`(b)(越境アクセステスト)/ `FR-034`(データ所有権制御)/ `FR-041`(共同分析グループ)。

**方向は既に決まっている。** PO 裁定 `C`(2026-09-12・山田正輝 —
`../../worklog/2026-09-12-merge-gate-api-cycle.md:66`)が
「**各 FR の実装タスクが自経路の HTTP 越境テストごと持つ。TSK-217 は対象横断の判定と
ハーネスの所有者として残す**」と決め、「単一の API 実装タスク」を不採用にした。
**2026-09-12 13:48 に Notion カードのコメントで本タスクへ引き渡し済み**。
→ **本タスクの残作業は方向の決定ではなく、規則の設計・例外の処理・引き渡しである。**

> **上流非依存(重要)**: 本規則は裁定 `C`(作業管理面・非正本)のみに依拠し、
> **`ADR-004` / `data-model.md` 12-4 / 要件書 `NFR-019(b)` の発効条項に依存しない**。
>
> **【2026-09-13 更新】3 正本は [PR #60](https://github.com/masaki1025/pitchlog/pull/60) で
> develop へマージされた**(`0bc05b8`)。**依存していないので本規則は影響を受けない**が、
> **引用は develop の版を正とする**(旧 worktree 版ではない)。
> **リベース後に全数値を再検算し、158 / 152 / 178 / 共有 6 組 / override 46 の全件一致を確認した。**
> `contracts/authz/claim-mutant-map.json` の変更は `oracle_commit` の 1 行のみで、`claims` は不変。

## 2. スコープ

### 母集団の宣言(**規則より先に書く**)

`receiving_task_id == "TSK-270-GROUP-2"` は **178 行**あり、158 ではない。

| 区分 | 件数 | 受取先 | 典拠 |
| --- | --- | --- | --- |
| `TSK-270-GROUP-2` × **`contract_only`** | **158 行 / 152 owner** | **本タスクが決める** | — |
| `TSK-270-GROUP-2` × **`probe_executable`** | **20 行** | **TSK-317**(決定済み・**本タスクの射程外**) | `../pg-authz-verification-g2/plan.md:710` `S-9` ② |

`S-9` は**「残存 0 件」を条件にする**。母集団を宣言しないと、PR #3 の実装者が規則を
178 行へ適用する経路を取りうる。**適用域を機械可読に閉じる**:

> 本規則の適用域は `contracts/authz/claim-mutant-map.json` の `claims[]` のうち
> **`execution_class == "contract_only"` かつ `receiving_task_id == "TSK-270-GROUP-2"`** の行に限る。

### 規則(**本節が規則の正**)

**2 段構造** — 「claim 行 → ベース ID が多対 1」+「ベース ID(= 論理 ID)→ 受取先が多対 1」。
**割り当ては論理 ID(152)単位で行い、claim 行では分けない**(`S-9` 確定事実 ① — `../pg-authz-verification-g2/plan.md:710`)。

#### 規則の構造 — **振り分けは 2 集合の直和。注記は直交させる**

**全 `contract_only` 行は非空の `receiving_task_id` と `runtime_evidence_kind: handoff_runtime_test` を
必須とする**(`scripts/check_authz_catalog.py:3864-3868`)。`execution_classes` は
**閉じた 2 値**(`probe_executable` / `contract_only`)で、`kill_contract.contract_only_runtime_rule` は
**`runtime_kill_forbidden_handoff_test_required`**。
→ **「runtime テストを持たない終着」は契約上存在しない。受取先を返さない規則は書けない。**

したがって振り分けは **2 集合の直和**とする:

```
U       = 152 論理 ID(execution_class == "contract_only" かつ receiving_task_id == "TSK-270-GROUP-2")
B_SET   = R-B が明示列挙する owner の集合
A_SET   = U − B_SET
```

**`R-C` は振り分けの規則ではなく注記**である(改訂 1 で格下げ — 敵対レビュー `P0-1`)。
`A_SET` / `B_SET` のいずれかに属したうえで、**runtime テストが退化する理由**を添える。

**行から論理 ID へ畳むとき、行ごとの判定が割れたら即エラーとする**(優先順位で黙って解決しない)。

#### `R-A′`(既定)— 認可判定が**執行される入口を開く FR** の実装タスクが受ける

> 受取先は、その claim の**認可判定が執行される入口を開く FR** の実装タスクとする。
> **ここでいう「入口」は `data-model.md` 12-4 の定義**(**製品の外からの要求を受け取り
> DB のデータへ到達する 1 本の口。HTTP なら method と path の組**)**であり、
> UI の所在でも認可機構の所在でもない。**
> `claim_id` の接頭辞は**既定値**であり、**その FR が入口を開かない場合(受け入れ基準が
> 他 FR の入口に掛かる場合)は執行先の FR へ寄せる。**

**根拠**: 要件書 `:602`「**FR-041 が新設する権限・経路によって得られるアクセスは、下の許可リストに
明示的に列挙されたものだけである**」。`FR-034` は 4 つの受け入れ基準見出しを持つが、
**4 つとも `FR-041` が開く入口に掛かる**(`:602` 共有の認可 / `:661` 制御資源の読み取り粒度 /
`:685` 共有集計エクスポート / `:703` 撤回の経路と効果)。**`FR-034` 自身は入口を持たない。**

**`FR-034` の 51 owner の内訳**(実測。**53 行 / 51 owner** — heading 配下 50 行 / 48 owner + 直下 3 行 / 3 owner):

| 集合 | owner 数 | 宛先 |
| --- | --- | --- |
| heading 配下 48 − `heading-004/list_item-001` | **47** | **`FR-041`** |
| `FR-034/list_item-002`(ユーザーストーリー・`AUTH_ACCESS_SCOPE`)| **1** | **`FR-041`** |
| `FR-034/list_item-004`・`-005`(URL/API 直叩き・単一テナント名指し)| **2** | **`TSK-217`**(`B_SET`) |
| `FR-034/heading-004/list_item-001` | **1** | **`TSK-217`**(`B_SET`) |
| **合計** | **51** | — |

→ **`FR-041` へ 48 owner / `TSK-217` へ 3 owner。**
**改訂 1 で是正**(旧表は 49 + 2 + 1 = 52 で、`heading-004/list_item-001` を heading 群と
二重計上し、`list_item-002` を落としていた — 敵対レビュー `P0-3`)。

**`NFR-nnn` 接頭辞も同様に執行先へ寄せる**(「`NFR-010` の実装タスク」は存在しない)。

#### `R-B`(`B_SET`)— **内容で列挙する**。機械分類だけで一括しない

**`layer: verification_contract` の 7 owner を一律 `TSK-217` へ送るのは根拠が広すぎる**
(敵対レビュー `P1-9`)。裁定 `C` が `TSK-217` に残したのは
「**対象横断の判定とハーネスの所有者**」であり、`layer` はその射程と一致しない。
**本文を読んで内容別に再裁定した**:

| owner | 内容(`source_text` 要旨)| 宛先 | 根拠 |
| --- | --- | --- | --- |
| `FR-034/heading-004/list_item-001` | 存在判別の境界。「越境アクセスの自動テストは `NFR-019`(b) に含む」 | **`TSK-217`** | 越境行列そのもの |
| `NFR-010/list_item-004` | 測定方法: 越境アクセスの自動テスト(API 直叩き含む) | **`TSK-217`** | 対象横断の判定 |
| `NFR-012/list_item-003` | 測定方法: 権限テスト | **`TSK-217`** | 対象横断の判定 |
| `NFR-019/paragraph-001` | 比較単位と正解ベクタの定義 | **`TSK-217`** | **ハーネスの所有者**(裁定 `C` の後半) |
| `SECTION-8/list_item-002` | リリース判定基準 2「CI 全グリーン」(全ランナー) | **`TSK-217`** | 同上(CI ハーネス) |
| `NFR-011/list_item-003` | 測定方法: DB・コード・ログのレビュー。**失効の自動テスト** | **`FR-036`**(`A_SET`) | 失効の執行入口はパスワード変更(`FR-036`) |
| `SECTION-8/list_item-006` | **バックアップ復元リハーサル**(記録権世代の失効・D1 退避) | **未確定 → 申し送り** | **越境行列でもハーネスでもない。運用手順である** |

**`B_SET` はこの表だけでは閉じない。** `R-A′` 表の 2 件と、横断不変条件 2 件も入る。

| `B_SET` の出所 | owner | 根拠 |
| --- | --- | --- |
| 上表の `verification_contract` 由来 | **3**(`FR-034/heading-004/list_item-001` / `NFR-010/list_item-004` / `NFR-019/paragraph-001`)+ `SECTION-8/list_item-002` = **4** | 越境行列 2 + ハーネス 2 |
| `R-A′` 表の `FR-034/list_item-004`・`-005` | **2** | 受け入れ基準(基本)`:599-600` = 対象横断の存在秘匿 |
| **横断不変条件** | **3**(`SECTION-1.1/paragraph-002` / `SECTION-3/heading-001/list_item-001` / `NFR-010/list_item-002`)| いずれも**全経路・全機能**に掛かり、特定の入口を持たない |
| **横断不変条件(追加)** | **1**(`SECTION-4.0-2/list_item-001`「物理削除しない」)| **PO 裁定 2026-09-13** — 全変更系入口に掛かり 1 owner で分配できない。`NFR-010` の「全機能適用」と同性格 |
| **`B_SET` 合計** | **10** | — |

> **改訂 2 で `NFR-012/list_item-003`(測定方法: 権限テスト)を `B_SET` から外した**
> (敵対レビュー 2 周目 `P0-3`)。親要件が**管理者操作の分離**なので **`FR-037` へ**。
> **`SECTION-4.0-2/list_item-001`(物理削除しない)は PO 裁定(2026-09-13)で `B_SET` へ戻した** —
> 要件書 4.0 節の「原則」であり、`NFR-010`「全機能に適用」やテナントの用語定義と同性格。
> **それらを `TSK-217` へ入れて本件だけ外すのは不整合**である。

#### `R-A′` の override 表(**FR 接頭辞が宛先を返さない owner の全件**)

**母数は 46 owner**(実測 — `SECTION-*` 30 + `APPENDIX-*` 8 + `NFR-*` 8)。
**改訂 2 で 32 → 46 へ是正**(敵対レビュー 2 周目 `P1-5`。旧数は `B_SET` と保留へ回した分を数え落としていた)。
**46 件すべて `source_text` を読んで確定した**(空欄 0)。

| 宛先 | owner |
| --- | --- |
| **`FR-041`**(17)| `SECTION-1.1/paragraph-003` / `SECTION-3/table_row-004` / `SECTION-3/heading-001/list_item-002` `-003` `-004` `-005` / `SECTION-6.4/list_item-001` / `SECTION-9/table_row-011` `-012` `-013` / `APPENDIX-ITEM-A-5/paragraph-003` `blockquote-002` `blockquote-006` / `APPENDIX-C/table_row-009` `-010` |
| **`FR-037`**(6)| `SECTION-2.2/list_item-008` / `SECTION-3/table_row-002`(**改訂 2 で `FR-041` から変更** — 本文が管理者権限の全体〔チーム登録・無効化・PW リセット・語彙・設定・選手統合・ゴミ箱復旧・DB 保守・ログ〕を列挙しており `FR-041` では受け取れない。2 周目 `P0-4`)/ `SECTION-4.0-1/code_line-011` / `SECTION-4.0-3/table_row-002` / `APPENDIX-C/blockquote-001` / `NFR-012/list_item-002` / `NFR-012/list_item-003` |
| **`FR-013`**(4)| `SECTION-2.2/list_item-002` / `SECTION-3/heading-001/list_item-006` / `SECTION-4.0-1/code_line-003` / `SECTION-7.1/list_item-004` |
| **`FR-033`**(4)| `SECTION-2.2/list_item-001` / `SECTION-3/table_row-001` / `APPENDIX-C/table_row-004` / `APPENDIX-C/table_row-006`(**値が「未決(10 章)」** — 閾値確定まで測れない旨を注記)|
| **`FR-036`**(2)| `NFR-011/list_item-002` / `NFR-011/list_item-003` |
| **`FR-035`**(1)| `SECTION-2.2/list_item-012` |
| **`FR-039`**(1)| `SECTION-3/table_row-003` |
| **`FR-019`**(1)| `SECTION-9/table_row-007` |
| **`TSK-217`**(4)| `SECTION-1.1/paragraph-002` / `SECTION-3/heading-001/list_item-001` / `NFR-010/list_item-002` / `NFR-010/list_item-004` |
| **`TSK-217`**(+1)| `SECTION-4.0-2/list_item-001`(**PO 裁定 2026-09-13** — 横断不変条件)|
| **`FR-041`**(+1)| `SECTION-6.2/list_item-003`(**PO 裁定 2026-09-13** — 本文が「**うち後半 6 件は共同分析グループ〔`FR-041`〕に伴うもの**」と明記し、在籍区分変更を含める理由も「OB 化した選手のデータが失効前のキャッシュから返り得る」と `FR-041` 基準で説明している。`layer: cache_authorization` / `AUTH_REVOCATION_CACHE`)|
| **`PENDING:TASK-RECOVERY`**(2)| `SECTION-8/list_item-006`・`NFR-009/list_item-004`(**PO 裁定 2026-09-13** — どちらも**記録権世代の失効と新世代発行 + `FR-012` の端末イベント回収**という同じ復旧ライフサイクルを記述し単一 FR に閉じない。**12-4 の定義上「入口を開く」に当たらない**ので裁定 `C` の規則が当たらず、越境行列でも横断ハーネスでもないので `TSK-217` にも入らない。→ **[復旧手順タスクを新規起票](https://app.notion.com/p/3da93b75e687818da3aac516470d8c40)**)|
| **`PENDING:TASK-REQ-LABEL`**(1)| `SECTION-4.0-3/table_row-003`(**要件書に対応する FR 条項が無い** → **[要件書改訂タスクを新規起票](https://app.notion.com/p/3da93b75e68781fb978fedba7ef499e0)**)|

> **改訂 2 で保留へ移した 2 件**(2 周目 `P0-3` `P0-4`):
> **`SECTION-4.0-2/list_item-001`**(物理削除しない)= 全変更系入口に掛かるが単一 owner で分割不能。
> **`NFR-009/list_item-004`** = バックアップ取扱い + 復元後の記録権フェンス + `FR-012` の端末イベント回収を
> **1 つの復旧手順**として要求しており単一 FR に閉じない(**`SECTION-8/list_item-006` を保留した判断と揃える**)。

#### 集合の実測(**PO 裁定 2026-09-13 を反映して全量再検算**)

```
U = 152 owner / 158 claim 行
  B_SET(TSK-217)  =  10
  A_SET           = 142
  保留             =   0
確定 = 152 / 152      未解決 = 0
```

**`A_SET` の内訳**: `FR-041` 74 / `FR-037` 14 / `FR-013` 13 / `FR-035` 13 / `FR-033` 6 /
`FR-039` 4 / `FR-012` 4 / `FR-036` 4 / `FR-019` 3 / `FR-007` 2 / `FR-014` 1 / `FR-017` 1 /
**`TASK-RECOVERY` 2** / **`TASK-REQ-LABEL` 1**。

> **`FR-041` が 74 owner(約半分)。** `TSK-363` が `route_kind` で 3 実装単位へ分割済み
> (`U-C1` management_operation 8 経路 / `U-C2` control_read 4 / `U-C3` shared_data 12)。
> **本タスクは分割に触らない。**
>
> **新規起票 2 本は FR ではないので `PENDING_REF` の文法を拡張する** —
> `PENDING_REF ::= "PENDING:" (("FR"|"NFR") "-" [0-9]{3} | "TASK-" [A-Z-]+)`。
> **起票済みなので、PR #3 の時点では実タスク ID へ解決できる。**

#### `R-C`(**注記** — 振り分けではない)

**改訂 1 で「例外規則」から「注記」へ格下げした**(敵対レビュー `P0-1`)。
**全 owner は `A_SET` か `B_SET` に属し、受取先を必ず持つ。**
`R-C` は**その runtime テストがなぜ退化するか**を添えるだけである。

> **形式で切らない**(旧 `R-C` ③ の「ユーザーストーリー」は撤回 — 敵対レビュー `P1-4`)。
> **改訂 2 で `C-PRECONDITION` を分離した**(2 周目 `P1-11`)。`SECTION-9` の区分欄を実測すると
> `table_row-007` は「**前提**」、`-011` `-012` `-013` は「**受容済みリスク**」で、旧表は**逆に入れていた**。
>
> **形で切らない**(旧 `R-C` ③ の「ユーザーストーリー」は撤回 — 1 周目 `P1-4`)。
> 実測すると `FR-013/list_item-002` は書式こそユーザーストーリーだが
> `AUTH_OPERATION_PERMISSION` で「**自分の端末が試合の唯一の記録端末であること**」という
> **検査可能な述語**を持つ。`FR-034/list_item-002` も同様。**形式で除外すると検査可能な主張を落とす。**

| 注記 | 意味 | 該当 owner と宛先(実測) |
| --- | --- | --- |
| **`C-ABSENCE`** | **不在を主張する**。テストは「その入口が無い / その操作ができない」ことを主張する | `SECTION-2.2/list_item-001`(個人アカウント無し)→ **`FR-033`** / `SECTION-2.2/list_item-002`(同時入力マージ無し。本文が「記録権 `FR-013` が機構として保証」と名指し)→ **`FR-013`** / `SECTION-2.2/list_item-008`(挙動付き語彙の変更は管理者のみ)→ **`FR-037`** / `SECTION-2.2/list_item-012`(セルフサインアップ無し)→ **`FR-035`**(手動オンボーディング)/ `SECTION-3/table_row-003`(対戦相手チームは利用しない)→ **`FR-039`** / `FR-019/list_item-009`(チーム管理者ロール導入しない)→ **`FR-019`** |
| **`C-ACCEPTED-RISK`** | **受容済みリスク**。テストは「塞がれていないこと」を主張する(**塞ぐと red**) | **`SECTION-9/table_row-011`・`-012`・`-013`** — いずれも区分欄が逐語で「**受容済みリスク**」(実測)→ **`FR-041`** |
| **`C-PRECONDITION`** | **前提**。機構で塞ぐ対象ではない | `SECTION-9/table_row-007`(論理削除の蓄積 — 区分欄が逐語で「**前提**」)→ **`FR-019`** |

**注記を付けた owner も `A_SET` に属し、通常どおり受取先を持つ。**
`contract_only_reason_code` の新設は**本タスクでは行わない**(申し送り #4)。

#### 係争点だった 1 行の解決

`FR-041/list_item-014#request-validation` は `R-A′` で **`FR-041`** へ行く。共有相手の
`#participant-authorization` も同じベース ID なので**同一の受取先に揃う**。
共有 ID を解消せず、不変条件も破らない。**PO 裁定は不要。**

> ただし **`no_db_decision_point` → `TSK-217` の 100% 一致が崩れる**(8 行目が `FR-041` へ行くため)。
> 崩す判断は正しい([research.md](research.md) `C-4` — 写像を定めた文書が存在しない)が、
> **崩すという事実を明示しないと `_has_db_decision` を見た実装者が矛盾と誤認する。**

#### 受取先の表現 — **型付きプレースホルダ**

`receiving_task_id` を **`PENDING:FR-041`** のような型付きリテラルにする。
`TSK-270-GROUP-2` は**宛先を持たない**(既に解散した群を指す)のに対し、`PENDING:FR-nnn` は
**宛先の feature を指す**ため、`S-10`(2) の許容集合検査が書けるようになる。

> **前例の位置づけを正確にする**(敵対レビュー `P1-6`): `scripts/check_authz_catalog.py:4396-4406` の
> 混在リテラルは**別資産の別フィールド**であり、`receiving_task_id` の前例ではない。
> **`receiving_task_id` の現行検査は `_expect_string`(`:511`)= 非空文字列のみ**で、
> `PENDING:FR-999` も `x` も誤字の FR も素通りする。
> → **文法と参照整合の仕様は第 2 段で書く**(下記の「第 2 段へ送る」)。

**置換義務**(改訂 1 で是正 — 旧文「当該 FR の最初の実装 PR が置換する」には根拠が無かった。
FR は複数 PR・複数入口に分割されうる。敵対レビュー `P0-2`):

> **入口を開く PR は、`allocation` 表で自分が開いた入口に対応づけられた owner の
> `receiving_task_id` だけ**を実タスク ID へ置換し `--reseal-oracle` する。
> **1 つの PR が当該 FR の全 owner を受け取るのではない。**
> **どの owner がどの入口に対応するかは `allocation` 表が持つ。**

#### PR #3 へ渡す仕様(**改訂 2 で射程縮小 — 本タスクは実装しない**)

**敵対レビュー 2 周目が「`PENDING:` の文法検査は現在の変更範囲では実装不能」と指摘した**(`P1-6`)。
資産には `TSK-270-GROUP-2` が **178 件**残っており、**本タスクは `contracts/` を変更しない**ので、
既存 checker に文法検査を入れると**即 red** になる。**検査と資産置換は同一コミットでしか成立しない。**

→ **人間の裁定(2026-09-13)で射程を A(縮小)に確定した。**
**本タスクは「決めて渡す」まで**とし、**実装・検査・資産置換はすべて PR #3 が同一コミットで行う。**

**渡す仕様**(PR #3 が実装する。本タスクは条文として書くだけ):

**① `receiving_task_id` の閉じた文法**

```
receiving_task_id ::= TASK_ID | PENDING_REF
TASK_ID     ::= "TSK-" [0-9]{3}
PENDING_REF ::= "PENDING:" ("FR" | "NFR") "-" [0-9]{3}
```

**`TSK-270-GROUP-2` は文法から外れる**(置換後に残っていたら red)。
**文法の導入と 158 行の置換は同一コミットに置く。**

**② 参照整合** — `PENDING_REF` の FR/NFR 部分は要件書に見出しが実在するものに限る。
母集団は `^#### (FR|NFR)-[0-9]{3}` = **65 件**(実測)。**件数は定数で持たず要件書から導出する。**
本タスクが使う 12 個は全件実在を確認済み。

**③ 対象集合** — `execution_class == "contract_only"` の行のみ。
**`probe_executable` に `PENDING:` があったら red**(`S-9` ② で受取先が `TSK-317` に確定)。

**④ 置換義務**(2 方向で閉じる。片方では「置き忘れ」と「余分」を分離できない)

> **入口を開く PR は、本計画書 2 節の割り当てで自分が開いた入口に対応する owner の
> `receiving_task_id` だけ**を実タスク ID へ置換し `--reseal-oracle` する。

- **過少**: `PENDING:FR-nnn` を残したまま `FR-nnn` の入口を開く PR をマージしようとしたら red
- **過剰**: その PR が開いていない入口の owner を置換していたら red

**⑤ `contract_only_reason_code` の新設は定数追加では足りない** — 値集合は閉じた 3 値
(`check_authz_catalog.py:118`)で、**検査器は期待値を導出する**(`:3818` `_has_db_decision`)。
**対象 owner の明示集合を入力にして導出ロジックまで変える**必要がある。

**⑥ `S-10` 9 論点の可否** — 本タスクが答えを持つのは **(2)(3)(4)(5)(8)(9)** の 6 件。
**(1) 比較元と基準コミット /(6) 許可パスと許可フィールド /(7) CI の浅い履歴**の 3 件は
**TSK-317 の封印手順の話であり本タスクの射程外**。**射程外と未検討を区別して記録する。**

### やること

- 母集団の宣言・規則 `R-A′` / `R-B` / `R-C` の条文化(2 節)
- **FR 接頭辞が宛先を返さない 46 owner** の行ごと確定(2 節 override 表)
- **PR #3 へ渡す仕様**の条文化(文法・参照整合・対象集合・置換義務・理由コード・`S-10` 可否)
- 終着区分つき裁定結果表を worklog へ記録し、`TSK-217` / `TSK-317` / `TSK-380` / PO へ引き渡す

### やらないこと(**射程 A** — 人間の裁定 2026-09-13)

- **`allocation.json` / `allocation.md` / 導出器 / テストを作らない。** 実装は **PR #3**
- **`PENDING:` の文法検査を実装しない。** 資産に `TSK-270-GROUP-2` が 178 件ある以上、
  **検査の導入と置換は同一コミットでしか成立しない**(敵対レビュー 2 周目 `P1-6`)→ **PR #3**
- **`.claude/core-areas.json` を変更しない**(登録対象の新設ファイルを作らないため)→ **PR #3**
- **`contracts/authz/claim-mutant-map.json` への書き込み** — **PR #3 の射程**
  (`../pg-authz-verification-g2/plan.md:226, :231`)。**凍結資産**(`oracle-seal.lock.json:54`)で
  `--reseal-oracle` + `human_review_required: true` + `ORACLE_STEP5_REREVIEW` を伴う
- **`http-route-matrix.json` の 37 経路の `test_owner`** — **`TSK-380` の射程**(`ADR-004:46`)
- **non-serving の意味** — **`TSK-382` の射程**(`ADR-004:41-47`)
- **`FR-041` の実装単位の分割** — **`TSK-363` の射程**(`route_kind` で 3 単位へ分割済み)
- **共有 ID の解消** — カードが明示的に禁止。`S-9` 確定事実 ① にも反する

## 3. 影響する正本

| 正本 | 変更内容 | ゲート(PRレビュー / finalize-doc) |
| --- | --- | --- |
| `../../development/harness-evaluation.md` | **既存候補「母集団を人が列挙する検査は、射程が動くたびに黙って古くなる」へ実測 2 件を追記**(18・19。**4 タスク目**)。**`H-*` は与えない**(昇格判断は PO)。変更履歴表へ 1 行(**版は上げない** — 7.6-3 前段)| **PR レビュー** |
| `../../README.md` | 台帳行の最終更新日と寄与の現行化 | **PR レビュー** |
| **上記以外はなし** | 産出する規則と割り当ては**作業管理面**であり、裁定 `C` 自身が「正本ではない」と分類済み(`../../worklog/2026-09-12-merge-gate-api-cycle.md:43`)| — |

> **制約**: `docs/features/` は正本ではない。`scripts/check_design_propagation.py:103` の
> `NONCANONICAL_PATH_RE` が**正本から `docs/features/` への規範参照を禁じる**(`:1590`)。
> **PR #3 は値を資産へ書き写すのであって、資産から本計画書を参照させない。**

**`ADR` にしない**: `../../development/dev-harness-design-2026-08-07.md:235-236`
「**ADR を起票すると同じ決定が 3 箇所に散り**、7.1-1 の『内容を複製しない』に反する」。

## 4. 実装方針

**重さ分類の根拠**: `contracts/authz/*` が `.claude/core-areas.json` の対象 = **コア領域**。
資産そのものは触らないが、**規則が封印資産の値空間を定義する**ため実質的な影響は同等。
**敵対レビュー + 人間の逐行確認が必須**(設計書 6.3)。

前提となる調査結果は [research.md](research.md) が正(**内容を複製しない** — 設計書 7.1-1)。
とくに次の 4 点を受ける:

- **`C-1`** 受取先を入口単位で決めることは現在の資産では不可能 → **DoD 1 は「群ごとの規則」で閉じる**
- **`C-2`** `runtime_test_owner.id` は `claim_id` から導出される**恒等式**(158 行で反例 0)。
  **「接頭辞キーなら共有 ID が割れない」は自明であってキーの正しさの証拠ではない**
- **`C-3`** `#facet` を持つ **12 行**が `requirement-claims.json` と join できず、
  **共有 6 組と完全に同一集合**。ベース ID 側は `layer: null`
  → **正規化は fail-closed**(ベース ID が `layer: null` なら `R-B` の対象外とする)
- **`C-4`** 既定 7 行の `TSK-217` 割り当て根拠は「404/存在秘匿」ではなく
  `_has_db_decision()`(`scripts/check_authz_catalog.py:3357-3362`)の機械分類

### 成果物の置き場(**改訂 2 で 2 層へ縮小**)

| 内容 | 置き場 |
| --- | --- |
| **規則・override 表・集合の実測・PR #3 へ渡す仕様** | **本計画書 2 節**(`plan.md` が契約 — 設計書 `:302`) |
| **裁定の事実と終着区分・引き渡しの実値** | `../../worklog/2026-09-13-contract-only-runtime-handoff.md`(前例: `2026-09-02-decision-sheet-ruling.md`)**= DoD の証跡** |

**`allocation.json` / `allocation.md` / 導出器 / テストは作らない**(射程 A)。
**`decision.md` / `decision-sheet.md` という名も使わない** — 前例
(`../legacy-frontend-coverage/decision-sheet.md`)が「提示まで」の意味を持ち衝突する。

### 変更・新設するファイル

| パス | 種別 |
| --- | --- |
| `docs/features/contract-only-runtime-handoff/plan.md` | 改訂(**規則の正**)|
| `docs/worklog/2026-09-13-contract-only-runtime-handoff.md` | 追記(**終着区分つき裁定結果表** = DoD の証跡)|
| `docs/features/contract-only-runtime-handoff/research.md` | 作成済み(291 行・全件典拠)|

**`contracts/` も `scripts/` も `tests/` も `.claude/core-areas.json` も変更しない。**
→ **本タスクは実行可能なコードを 1 行も追加しない。**

### 実装ステップ(コミット単位 — 設計書 6.1 段階実装)

| # | ステップ(何を作るか) | 合格条件(このステップの検証方法) |
| --- | --- | --- |
| 1 | **規則 `R-A′`/`R-B`/`R-C`・override 表 46 件・集合の実測・PR #3 へ渡す仕様**を本計画書 2 節へ確定 | `[機械]` 152 owner / 158 claim 行 / **`B_SET` 10 / `A_SET` 142 / 保留 0** が資産から導出でき**件数を定数で持たない** / **override 表の母数 46 が「FR 接頭辞で宛先を返さない owner」と exact-set 一致** / 178 = 158 + 20 の内訳が `S-9` ② 典拠つきで射程外と宣言されている `[手動・外部]` **46 件の `source_text` を逐行で確認**した |
| 2 | **worklog へ終着区分つき裁定結果表**を書き、`TSK-217` / `TSK-317` / `TSK-380` / 新規 2 タスクへ引き渡す | `[手動・外部]` 全群が **(a) 既存正本アンカー /(b) 本 PR で反映 /(c) 後続タスク**のいずれかに分類 / **`TSK-217` が `TSK-217-A` 7 + `TSK-217-B` 10 = 17** と書かれている / **送り先ごとに単一の実在タスク ID・射程行・追記 URL / コミット・確認者**を記録 / **PO 裁定 2026-09-13 の 5 件が解消として記録されている** |

> **ステップが 2 つしかないのは射程 A の帰結である。** 実装・検査・資産置換は PR #3 が行う。
> **`core-areas.json` への登録も PR #3 の射程**(登録対象の新設ファイルを本タスクが作らないため)。

### 引き渡しの作法(**台帳が記録した失敗型を踏まないため**)

`../../development/harness-evaluation.md:2365-2410` の候補「**送り先が空手形になる**」が
2026-09-12 に実害 1 件を記録し、型を「**送り先の射程が受け取れる形か確認されていない**」と特定している。
**本タスクは同じ台帳項目に「反例」として名指しされている**(同 `:2398`)。
**ここで空手形を出すと反例が反転する。**

| 送り先 | 射程確認すべきこと | 懸念 |
| --- | --- | --- |
| **`TSK-217`**(`TSK-217-B` 9 owner)| 射程が「対象横断の判定とハーネス」で検証契約を受け取れるか | **計画書・DoD がリポジトリに存在しない**。Notion カード本文でしか確認できない |
| **`TSK-317` PR #3** | `S-9`・`S-10` が本成果物を**入力として受け取る形か** | **分担が未記載**(`../pg-authz-verification-g2/plan.md` に `TSK-367` の出現 0 件)。**先に分担を書かせる** |
| **未起票の FR 実装タスク** | 起票時に規則が届く形か | **前例あり** — `../course-coordinate-contract/plan.md:350`「**画面受入タスクは未起票なので相互リンクを求めない**」 |

## 5. DoD(受け入れ基準)

- [ ] **158 行の受取先が群ごとの規則で決まっている** — **`A_SET`(142)と `B_SET`(10)が直和で
      152 owner を覆う**(`R-C` は注記であり振り分けではない)。**保留 0・未解決 0**
- [ ] **override 表が 46 owner を覆う**(**FR 接頭辞で宛先を返さない owner と exact-set 一致**。空欄 0)
- [ ] **共有 ID を解消していない**(**6 共有組が同一受取先に揃う**。`FR-041/list_item-014` の 2 facet を含む)
- [ ] **全 owner が非空の受取先を持つ**(`contract_only` の契約 —
      `scripts/check_authz_catalog.py:3864-3868`。**「受取先を返さない終着」を作っていない**)
- [ ] **母集団 178 = `contract_only` 158 + `probe_executable` 20 を宣言し、20 件を射程外と明示した**
- [ ] **実行可能なコードを 1 行も追加していない**(射程 A)。
      **`git diff --name-only origin/develop...HEAD` が `docs/` 配下のみ**
      (**3 点比較**。2 点比較は可動する `origin/develop` との差分を拾い偽陽性になる — 2 周目 `P1-12`)
- [ ] **PR #3 へ渡す仕様 6 件が条文として書かれている**(文法・参照整合・対象集合・置換義務・理由コード・`S-10` 可否)
- [ ] **申し送りがすべて「単一の送り先 + 射程行 or 起票責任者」つきで記録されている**(空手形を作らない)
- [ ] **申し送り「TSK-317 との分担の明文化」について、先方が受け取れる形になっていることを確認した**
      (**本タスク完了の前提** — 1 周目 `P1-10`)
- [ ] **コア領域の敵対レビューと人間の逐行確認を通した**(実施記録行を埋めた)

## 6. テスト計画

**本タスクは実行可能なコードを 1 行も追加しないため、自動テストを新設しない**(射程 A)。
`NFR-019` の 5 種別(単体・一致性・越境・E2E・故障系)のいずれにも追加しない。

**検証は 2 つで行う。**

| 手段 | 内容 |
| --- | --- |
| **既存ゲートの維持確認** | `uv run ruff check .` / `uv run ty check` / `uv run pytest tests/`(ルート)。`ruff format` は未導入 — 走らせない。**backend の DB 必須テストは `PITCHLOG_TEST_ADMIN_DSN` / `PITCHLOG_TEST_ROLE_DSN` のスキームから `+psycopg` を外して実行する**(`../orm-schema-migration/design.md:405-425` の既知の罠。付けたままだと 194 errors) |
| **規則の再現性** | 2 節の数値(152 / 158 / `B_SET` 10 / `A_SET` 142 / 保留 0 / override 46)を**資産から再導出して一致**することを、レビュー時に再実行できる形で worklog に記録する |

> **PR #3 が実装する検査の正例・負例は、2 節「PR #3 へ渡す仕様」が条文として定める。**
> **本タスクはそれをテストしない**(テスト対象のコードが存在しないため)。

## 7. 申し送り(本タスクで決めないもの)

**各行は「単一の実在タスク ID」+「受取側の射程行(その射程が当該項目を受け取れる根拠)」を持つ**
(改訂 1 — 敵対レビュー `P1-10`。**責任主体が 2 つの行・タスク ID を持たない行を作らない**)。
**射程が確認できない行は「未起票・起票責任者つき」と明記する**(空手形との違いを残すため)。

| # | 事項 | 送り先(単一) | 受取側の射程行 | 状態 |
| --- | --- | --- | --- | --- |
| 1 | `S-10` の機械条件 — (2)(3) は `PENDING:` の型で書ける。**(8)(9) は `PENDING:` 行について成立しない**(read-back の相手も DoD も無い)。「`PENDING:` でない行への exact-set 突合」+「入口を開く PR が同一 PR で自分の owner を置換する」義務の検査へ置き換える | **TSK-317**(PR #3) | `../pg-authz-verification-g2/plan.md:231`(`S-10` = PR #3) | **射程内** |
| 2 | **`../pg-authz-verification-g2/plan.md:231` の「受取タスクは起票済み」は `S-10` については誤り**(`../merge-gate-api-cycle/research.md:362`「製品 HTTP 経路(API)の実装 = なし」) | **TSK-317**(PR #3) | 同上 | **射程内** |
| 3 | **`S-10`(4)の「`TSK-217` の 7 件」→ `TSK-217-A` 7 + `TSK-217-B` 9 = 16** へ是正。**`S-10`(4) は既存 7 件のみを指しており、本タスクが足す 9 件を含まない** | **TSK-317**(PR #3) | 同上 | **射程内** |
| 4 | **`contract_only_reason_code` の新設は定数追加では足りない** — 値集合は閉じた 3 値(`check_authz_catalog.py:118`)で、**検査器は期待値を導出する**(`:3818` `_has_db_decision` → `expected_reasons`)。**対象 owner の明示集合を入力にして導出ロジックまで変える**必要がある(敵対レビュー `P1-6`) | **TSK-317**(PR #3) | 同上 | **射程内** |
| 5 | **「TSK-367 が決め、PR #3 が適用する」分担の明文化**(`../pg-authz-verification-g2/plan.md` に `TSK-367` の出現 0 件) | **TSK-317**(PR #3) | **射程行が無い** | **要確認 — 本タスク完了の前提**(下記) |
| 6 | **本タスクは `FR-034` の受け入れ基準を `FR-041` の入口の執行として扱った**(37 経路には `ROUTE:SHARED`/`CONTROL`/`MANAGEMENT` の **24 本の FR-041 経路**が含まれ、同じ入口が 2 資産で別方式に割られうる) | **TSK-380** | `ADR-004:46`(37 経路の `test_owner` 再割り当て方式) | **射程内** |
| 7 | ~~`SECTION-6.2/list_item-003` の受取先~~ | — | — | **解消**(PO 裁定 2026-09-13 = **`FR-041`**)|
| 8 | **要件書の穴** — `SECTION-4.0-3/table_row-003`「ラベル語彙は各チーム拡張可」に**対応する FR 条項が存在しない** | **[要件書改訂タスク](https://app.notion.com/p/3da93b75e68781fb978fedba7ef499e0)**(新規起票)| カード本文「やること」節 | **解消**(**起票済み**。要件書の改訂は確定ゲートが要る)|
| 9 | `no_db_decision_point` → `TSK-217` の写像を定めた文書が**存在しない**([research.md](research.md) `C-4`)。本タスクはこの一致を意図的に崩す | **TSK-317**(PR #3) | `../pg-authz-verification-g2/plan.md:711` `S-10`(4) | **射程内** |
| 10 | `SECTION-8/list_item-006` + `NFR-009/list_item-004` の受取先 | **[復旧手順タスク](https://app.notion.com/p/3da93b75e687818da3aac516470d8c40)**(新規起票)| カード本文「受け取る 2 owner」節 | **解消**(PO 裁定 2026-09-13。**起票済み・受領範囲をカードへ記載済み**)|
| 11 | ~~`SECTION-4.0-2/list_item-001` の受取先~~ | — | — | **解消**(PO 裁定 2026-09-13 = **`TSK-217`**)|
| 12 | ~~`NFR-009/list_item-004` の受取先~~ | — | — | **解消**(#10 と統合。同じ復旧ライフサイクル)|
| 13 | **射程 A で PR #3 へ移した実装一式** — `allocation` 資産 / 導出器とテスト / `PENDING:` の文法検査 / `core-areas.json` への登録。**検査の導入と 158 行の置換は同一コミットでしか成立しない**(2 周目 `P1-6`)| **TSK-317**(PR #3) | `../pg-authz-verification-g2/plan.md:226`(`S-9` = PR #3) | **射程内** |

### 保留の解消(**PO 裁定 2026-09-13・山田正輝**)

**敵対レビュー `P1-5`** が「裁定待ちの owner を残したまま被覆を主張できない」と指摘したため
保留 5 owner を分離していたが、**PO 裁定で全件解消した**。

| owner | 裁定 |
| --- | --- |
| `SECTION-4.0-2/list_item-001` | **`TSK-217`**(横断不変条件。`NFR-010` の全機能適用と同性格)|
| `SECTION-6.2/list_item-003` | **`FR-041`**(本文が後半 6 件を `FR-041` に伴うものと明記)|
| `SECTION-8/list_item-006` | **[復旧手順タスク](https://app.notion.com/p/3da93b75e687818da3aac516470d8c40)**(新規起票)|
| `NFR-009/list_item-004` | 同上(**同じ復旧ライフサイクル**)|
| `SECTION-4.0-3/table_row-003` | **[要件書改訂タスク](https://app.notion.com/p/3da93b75e68781fb978fedba7ef499e0)**(新規起票)|

→ **確定 152 / 152・保留 0・未解決 0。**

**#5(TSK-317 との分担の明文化)は本タスク完了の前提**として残る(敵対レビュー `P1-10`)。
**先方がこの分担を受け取れる形になっていることを確認できるまで `/task-done` へ進まない。**
