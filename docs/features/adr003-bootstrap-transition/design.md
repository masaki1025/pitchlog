---
feature: adr003-bootstrap-transition
type: design
date: 2026-09-13
---

# 詳細設計: 検査基盤の導入期の移行状態

本書は [plan.md](plan.md) 4 節から参照される詳細設計である(設計書 6.1 の 3 ファイル役割分担 —
plan = 契約 / research = 調査 / design = 詳細設計)。**機構が読む状態と実装ステップ表は plan.md のみに置く**
(設計書 7.1-1)。実測値の典拠は [research.md](research.md) が正で、本書は結論だけを引用する。

**参照規約**: 正本の箇所は**条項名で参照し、行番号を使わない**。ただし**走査の証跡**(§2)は
**走査時点の行番号を証跡として記録する** — これは「どこを見たか」の再現性のためであり、
**規範の参照ではない**(行が動いたら証跡を取り直す。§2-5 に取り直しの規約を置く)。

---

## 1. 本書が答えを出す範囲

plan.md §4-10 が「**design.md で確定する**」と定めた事項に、本書で答えを出す。

| 区分 | 事項 | 本書の節 |
| --- | --- | --- |
| **走査** | 強制点の exact-set(全数走査の基準と証跡) | §2 |
| **語彙** | 4 型の書き分け(未達 / 違反 / 債務 / 判定不能) | §3 |
| **状態** | 移行状態の二軸の定義と遷移条件 | §4 |
| **機械条件** | `M-1`〜`M-5` の答え(必須命題・正例・負例・不成立条件) | §5 |
| **封印** | 既知債務の授権 — 封印の 4 規律 | §6 |
| **原子性** | 発効原子性(何が同一変更に揃ったときに発効するか) | §7 |
| **ID** | 条項 ID の体系(名前空間・粒度・正規化規約・対応表) | §8 |
| **条文** | 3 正本へ入れる条文案 | §9 |
| **配置** | `D-xx` の配置決定と選定理由 | §10 |
| **写像** | 写像表(各回答の着地先) | §11 |

**本書はゲート対象外である**(feature 文書)。したがって**本書に書いたことは、正本の条項へ着地して
初めて規範になる** — §11 の写像表がその対応を閉じる。**写像漏れ・着地漏れはいずれも停止条件**
(plan.md §4-10 の機構 2)。

---

## 2. 強制点の exact-set — 走査の基準と証跡

### 2-1. 走査基準を 2 度作り直した(記録)

plan.md §4-11 は**マーカー 6 種**(`マージ不可` / `不合格` / `合格としない` /
`合格と判定してはならない` / `満たしたと判定` / `fail`)を挙げ、「**マーカー自体の一覧は
design.md で確定する**」としていた。**実測の結果、この 6 種では全数を覆えないことが判明した。**

**第 1 版(マーカー 6 種)の欠陥**: 計画書 §4-3 自身が強制点として挙げた
「**1 層でも欠ければ満たさない**」(要件書 `NFR-018` 達成条件 (b)②)が、**どのマーカーにも当たらない**。
実測で未捕捉の行数:

| 語 | 要件書 | ADR-003 | 設計書 | 6 マーカーで未捕捉の行数 |
| --- | --- | --- | --- | --- |
| `満たさない` | 6 | 2 | 1 | **7** |
| `拒否` | 22 | 3 | 17 | **38** |
| `禁止` | 13 | 11 | 19 | **36** |
| `許さない` | 4 | 5 | 0 | **7** |
| `認めない` | 1 | 1 | 2 | **3** |
| `設けない` | 3 | 1 | 0 | **3** |
| `失敗させ` | 1 | 2 | 0 | **3** |
| `実装を開始せず` | 2 | 0 | 0 | **1** |
| `違反として扱う` | 1 | 0 | 0 | **1** |
| `例外は成立しない` | 1 | 0 | 0 | **1** |

**第 2 版(強制語 17 種 × 射程語フィルタ)の欠陥**: 強制語を 17 種へ拡張したうえで、
「本タスクが触れうる主題の語」(`NFR-018` / `正解ベクタ` / `変異` / `入口` ほか 25 語)を
**第 2 段のフィルタ**にかけた。432 行 → 77 行まで絞れたが、**`ADR-003` D-7 の表 6 行のうち
1 行(差分テストの行)が脱落した** — 当該行は「差分テスト(両実行系の出力突合)| PR 必須 |
(α)| **マージ不可**」であり、**射程語をひとつも含まない**ためである。

→ **主題の語によるフィルタは、強制点そのものを落とす。**

### 2-2. 確定した走査基準 — **節単位 × 強制語**

**射程節を条項単位で確定し、その節の内側にある強制語のヒットを全数分類する。**
**節の外側は、節単位で「非波及」を証明する**(行単位ではなく節単位で閉じる)。

**走査対象文書**(plan.md §4-11 が固定した 6 種):

```
docs/requirements/requirements-pitchlog-2026-07-22.md
docs/adr/ADR-003-domain-calc-method.md
docs/development/dev-harness-design-2026-08-07.md
docs/development/github-setup.md
docs/improvements-from-baseball-scoring.md
docs/design/*.md
```

**強制語(17 種)** — 「判定の帰結を定める語」。**主題を問わない**:

```
マージ不可 / 不合格 / 合格としない / 合格と判定してはならない / 満たしたと判定 /
満たしたとはしない / 満たさない / fail / 拒否 / 禁止 / 認めない / 設けない /
許さない / 失敗させ / 実装を開始せず / 例外は成立しない / 違反として扱う
```

**除外規則**: **変更履歴表の行を除く**(正規表現 `^\|\s*\*{0,2}\d+\.\d+\*{0,2}\s*\|`)。
変更履歴は**改訂の記録であって規範ではない**ため。**この除外は機械的であり、判断を挟まない。**

### 2-3. 射程節の確定

**射程節 = 本改訂が定める移行状態が、その節の判定に影響しうる節。**

| 文書 | 射程節 | 強制語ヒット |
| --- | --- | --- |
| 要件書 | `NFR-018`(ドメインロジックの単一実装) | **14** |
| 要件書 | `NFR-019`(テスト規約の CI 強制) | **2** |
| 要件書 | 7.3(法規制・社内ルール・コンプライアンス — 開発プロセスと 10 章未決事項を含む) | **4** |
| 要件書 | `NFR-021`(開発環境) | **1** |
| 要件書 | A-1 共通規則 | **2** |
| `ADR-003` | D-1(実現手段) | 3 |
| `ADR-003` | D-3(正本の版と生成物の同期規則) | **1** |
| `ADR-003` | D-4 不変条件ごとの判定 / 結論の根拠 | **3** |
| `ADR-003` | D-5 (a)(実現可能な検査)/ (c)(補助手段) | **4** |
| `ADR-003` | D-11 ②(差分の検出可能性) | **9** |
| `ADR-003` | D-11 ③(宣言された構成の完全性) | **8** |
| `ADR-003` | D-6(正解ベクタと契約) | **4** |
| `ADR-003` | D-12(`contracts/` の構造) | 1 |
| `ADR-003` | **D-7(検証機構の実行契機)** | **6** |
| `ADR-003` | D-8(比較面) | 1 |
| `ADR-003` | D-10(付録A 契約の実装完了の判定基準) | **1** |
| `ADR-003` | 帰結「何が変わるか」(**着手順序 4 段階**) | **1** |
| 設計書 | **10.1(`ci.yml` — `NFR-019` の実体)** | **14** |

**合計 79 件**(うち `ADR-003` D-1・D-12・D-8 の 5 件は下記の理由で非波及)。

**射程外と判定した節**(節単位で閉じる):

| 文書・節 | 非波及の理由 |
| --- | --- |
| 要件書 `FR-005` / `FR-007` / `FR-012` / `FR-013` / `FR-033`〜`FR-035` / `FR-041` / `NFR-020` / `NFR-023` / A-5 / E-2 | **製品の挙動に関する規定**であり、検査基盤の充足判定とマージ可否の関係に触れない。移行状態はこれらの判定を一切変えない |
| 要件書 用語定義 / ブロック1 | 定義のみ。判定を持たない |
| 設計書 6.1 / 6.2 / 6.3 / 6.4 / 7.1 / 7.3 / 8.x / 9.x / 12.x / 13 章 | **開発プロセスの規定**。6.3 の fail-closed は**レビュー範囲**の規則であり本改訂の対象と別物(plan.md §4-5 で確認済み)。13 章の禁止は主語が「**対象計算のコード**」に限定され、検査基盤 PR 自身のマージは対象外 |
| 設計書 2.3 / 3.2 / 4 章 / 10.2 | 制約の継承・原則・構成・ブランチ保護。判定の内容に触れない |
| `github-setup.md` | **必須チェックの数と名称**に関する運用手続。**本改訂はジョブ名・個数を変えない**ため波及しない(ステップ 5 で実測して確認する) |
| 改善台帳 | 強制語のヒット 7 件はいずれも**過去の記録**であり規範ではない |
| `docs/design/data-model.md` / `sync-protocol.md` | **それぞれ別領域の設計正本**。強制語のヒットは当該領域の判定(DB 制約・同期プロトコル)であり、`NFR-018` の検査機構に触れない |

### 2-4. 分類の割り当て

**§2-3 の射程節にあるヒットを、plan.md §4-3 の 3 分類へ割り当てる。**

**分類表は §2-6 に置く。** 全数の突合は §2-6-4、走査基準の限界は §2-7 が記録する。
**2026-09-13 時点で 79 件すべてが分類済みであり、未分類は 0 件である。**

### 2-5. 行番号証跡の取り直し規約

本節の証跡は**走査時点(2026-09-13・`develop` = `0bc05b8` へリベース後)の行番号**である。
**正本を編集すると行が動く**ため、**ステップ 3 の改訂後に走査を再実行し、証跡を取り直す**。

**取り直しの判定**: 再走査の**ヒット件数と分類の内訳が一致すること**を確認する。
**一致しなければ、改訂が意図しない強制点を作った/消したことを意味する**ため、差分を特定して
分類表を更新する。**件数が合っていることだけを確認して終えない**(plan.md §4-2 の契約 5 の自己点検)。

---

## 3. 語彙 — 4 型の書き分け

**新語を作らず既存語に寄せる**(plan.md §4-2 の契約 4)。既存の正本に実在する語を確認したうえで、
**移行状態が扱う 4 つの型を別語で呼び分ける**。

### 3-1. 4 型の定義

| # | 型 | 定義 | 移行状態での扱い | 典拠となる既存条文 |
| --- | --- | --- | --- | --- |
| 1 | **未達** | **宣言された対象について、要求された層・検査が未整備**である状態 | **計上し続ける。マージ可否の判定のみ分ける** | 要件書 `NFR-018` (b)②「いずれか 1 層でも欠ければ本要件を満たさない」 |
| 2 | **違反** | **宣言と実在が食い違う**状態(宣言外の入口が存在する・生成物が手編集されている・派生物が古い) | **救わない。従来どおり fail** | `NFR-018` (b)①③ / `ADR-003` D-3・D-11 ③ |
| 3 | **債務** | **移行開始時点で既に未宣言であった対象**を、**全件列挙して封印したもの** | **授権の対象。追加・再導入・復活は fail** | 本改訂で新設(§6) |
| 4 | **判定不能** | **解析器が「解析できない」と報告した**状態 | **救わない。従来どおり合格としない** | `ADR-003` D-11「未分類・判定不能は合格としない(fail-closed)」 |

### 3-2. なぜ 4 型か — 「未達」と「違反」を分ける理由

plan.md の射程 6 項目のうち②が「**『未達』と『違反』の区別**」である。**この区別が本改訂の要である**:

- **未達は「まだ無い」** — 検査基盤の導入期には**必然的に発生する**。これを理由にマージを止めると
  **検査基盤そのものを作れない**(`ADR-003` 帰結 1 が「緑になってからコードを追加するとは書けない」と
  述べた循環と同じ構造)
- **違反は「あるべきでないものがある」** — 導入期であるか否かと無関係に**常に誤り**である。
  宣言外の入口・手編集された生成物・古い派生物は、**移行状態であっても許されない**

**移行状態が緩めるのは①未達のみ**であり、**②違反・④判定不能には一切触れない**。
③債務は「**開始時点で確定した未達の部分集合を、封印して増やさないことと引き換えに授権する**」ものである。

**research.md §8-7 の切り分けを条文へ写す**: 「移行状態が緩めるのは**宣言と実在の一致部分だけ**であり、
拒否・禁止・手段の重ねは緩めない」。

### 3-3. 既存語との対応(新語を作らない)

| 本書の語 | 正本で使う表現 | 新語か |
| --- | --- | --- |
| 未達 | **「満たさない」「不合格として扱う」**(既存表現をそのまま使う) | **新語ではない** |
| 違反 | **「違反として扱う」**(要件書 `NFR-018` (c) に実在) | **新語ではない** |
| 債務 | **「既知債務」** | **新設**(§6 で定義を置く。**移行状態の授権に固有の概念であり、既存語に対応物がない**) |
| 判定不能 | **「判定不能」**(`ADR-003` D-11 に実在) | **新語ではない** |

**「充足宣言」は使わない**(正本に 0 件 — plan.md §4-2 の契約 4 で確定済み)。
**「満たしたと判定してはならない」「合格と判定してはならない」という既存の書き方に固定する。**

### 3-4. **`TSK-379` が導入した「未発効」との衝突を避ける**(2026-09-13)

**要件書 v2.8(`TSK-379`)が `NFR-019(b)` へ発効条項を置き、「未発効」を
「対象経路が実装されていない状態」の意味で要件書に導入した。**

本改訂は plan.md §4-4 論点 3 で「**定義済みだが未発効**」という表現を
**「移行状態の判定機構が CI の必須経路へ接続されていない状態」**の意味で使う設計であった。
**同じ語が 2 つの意味を持つ。**

**採る方針**: **本改訂では「未発効」を使わない。**

| 語 | 意味 | 所有者 |
| --- | --- | --- |
| **未発効**(既存) | `NFR-019(b)` の越境テストについて、**対象経路が実装されていない** | **要件書 v2.8 / TSK-379** |
| **未接続**(本改訂) | 移行状態の**判定機構が CI の必須経路へ接続されていない** | 本改訂 |

**「未接続」は新語だが、既存語の再利用が誤読を生む場面では新語のほうが安全である**
(plan.md §4-2 の契約 4 は「新語を作らず既存語に寄せる」だが、**寄せ先が既に別の意味で埋まっている
場合は例外とする** — 本書がその判断を明示的に記録する)。

**あわせて両者の関係を条文に書く**(要件書 v2.9 の射程宣言で宣言済み):
**`NFR-019(b)` の発効条項と本改訂の移行状態は別の事柄であり、共有するのは
「実体が無いものについて判定を下さない」という原則のみ**である。

**向きの非対称を引くときは非対称ごと引く**(`TSK-379` からの助言 — 2026-09-13):

| 条項 | 向き |
| --- | --- |
| `NFR-018` 正解ベクタ欄 | **未完成のあいだ「合格」と判定してはならない**(未整備を適合と偽らない) |
| `NFR-019(b)` 発効条項(v2.8) | **未発効の経路を理由に「不合格」と判定してはならない**(実体の無い経路の試験は書けない) |
| **本改訂の移行状態** | **`NFR-018` 側の向きを維持する** — **充足判定は従来どおり不合格**であり、**変えるのはマージ可否の判定のみ** |

---

## 4. 移行状態の二軸

### 4-1. なぜ二軸か

**同じ状態に 2 つの終了条件を持たせない**(plan.md 3 周目 P1-4)。
「未達 0 かつ債務 0 で終わる」と「停滞したら失効する」は**別の事象**であり、
1 つの状態に両方を持たせると**どちらで終わったのかが判定できない**。

| 軸 | 名称 | 何を表すか | 終了 |
| --- | --- | --- | --- |
| ① | **移行プログラム** | **検査基盤の整備が完了していない**という事実 | 「未達 0 かつ債務 0」の成立で**必須ゲートへ自動かつ不可逆に昇格** |
| ② | **緩和されたマージ授権** | **その事実を理由にマージを止めない**という授権 | **有効 → 失効 → 人間の明示的な再承認**を遷移する |

**①が終了すれば②も終了する。②が失効しても①は継続する**(失効中も未達・債務は計上され続ける)。

### 4-2. 軸①: 移行プログラム

| 項目 | 定義 |
| --- | --- |
| **開始** | **移行開始を承認したコミット**(固定 SHA)。§6-1 で定義する |
| **継続条件** | `未達数 + 債務数 > 0` |
| **終了** | `未達数 + 債務数 = 0` の成立をもって**自動的に**終了し、**必須ゲートへ昇格**する |
| **不可逆性** | **昇格後に再び移行状態へ戻る経路を設けない**。戻すには**正本の改訂**を要する |
| **失効との関係** | **②が失効しても①は継続する**。未達・債務の計上は止まらない |

**「自動かつ不可逆」の理由**: 昇格を人間の操作にすると、**未達 0 になっても昇格されないまま
授権が残り続ける**経路ができる。**条件の成立と昇格を同一にする**ことで、この経路を塞ぐ。

### 4-3. 軸②: 緩和されたマージ授権

| 状態 | 定義 | 遷移先 |
| --- | --- | --- |
| **有効** | 停滞エポック内にあり、かつ①が継続中 | **失効**(停滞)/ **終了**(①の昇格) |
| **失効** | 停滞エポック内で `未達数 + 債務数` が 1 度も減少しないまま **N マージ**が経過した | **有効**(人間の明示的な再承認) |
| **終了** | ①が昇格した | — |

**`N = 50`**(PO 裁定 B-1'・2026-09-11)。

**失効中の扱い**: **既存の fail-closed が無条件に復帰する。**
移行状態を理由としたマージ可否の別判定は行われず、`NFR-018`(b)・`NFR-019`(a) の
判定どおりにマージ不可となる。

**「無期限の緩和にしない」の担保**(plan.md 射程④): **停滞を時間ではなくマージ本数で測る。**
時間で測ると**作業していない期間も経過する**ため、「進んでいないこと」の測定にならない。

---

## 5. 機械条件 `M-1`〜`M-5`

plan.md §4-10 が「**design.md が答えを出さなければならない**」と定めた 5 論点。
**各論点に「必須命題・正例・負例・不成立条件」を書く**(「決める」と書いただけの回答を認めない —
plan.md §4-10 の機構 1)。

> **⚠ 射程縮小(PO 裁定 2026-09-13)により、本節の位置づけが変わった。**
>
> **確定ゲートで起因過半が 2 周連続**(7.3-6)。**3 周かけて「未履行と違反の切り分け」を 3 度書き直し、
> そのたびに別の穴が開いた**(初周: 既知債務が違反にも該当 → 優先順位を追加 / 2 周目: 封印が対象単位で
> 別種の違反まで免除 → 宣言の有無で切り分け / 3 周目: 「宣言外の入口」は `entrypoints[]` 宣言が無いので
> 未履行にも該当 → **振り出し**)。
>
> → **正本は「性質による分類」を免除の判定に用いるのをやめ、`BOOT-SEAL`(封印集合への列挙)のみで決める。**
> **本節の `M-1`〜`M-5` は、正本が確定する機械条件ではなく、`TSK-235` が実装で満たすべき要求**となる。
> **正本は充足すべき条件を定め、判定方法を実装へ委ねる**(要件書 `NFR-018` (e) の末尾「本規定が実装へ委ねる事項」)。
>
> **本節の内容は破棄しない** — **委任先が満たすべき条件(必須命題・正例・負例・不成立条件)として `TSK-235` へ渡す。**
> **空手形にしないための材料である**(plan.md §4-10 の機構 1)。

**用語**: 以下で「**対象**」は要件書 `NFR-018` の「対象」欄が列挙する対象計算を指し、
「**段階**」は `ADR-003` 帰結 1 の着手順序 4 段階を指す。

### 5-1. `M-1`: 段階 2 の PR の判定方法

**問題**(plan.md 2 周目 P0-1): **paths 接触では判定できない。**
発効原子性を満たす段階 1 の基盤 PR は必然的にマニフェストと債務台帳へ触れるため、
paths 判定だと**発効 PR 自身が段階 2 と誤判定され、厳密減少に失敗して発効できない**。

#### 必須命題

> **`M-1-a`** 段階 2 の PR とは、**base から head へのあいだに、安定した対象 ID が
> ちょうど 1 件、実在集合へ新規に加わった** PR をいう。
>
> **`M-1-b`** 「実在集合へ加わった」とは、当該対象について
> **①宣言モデルの正本 ②生成物 ③製品経路の呼び出し部 ④その対象の 3 層検査**
> の **4 つがすべて同一の PR に揃っている**ことをいう(`ADR-003` 帰結 1 の段階 2 の逐語
> 「対象計算 1 件ごとに、宣言モデルの正本・生成物・製品経路の呼び出し部・その対象の 3 層検査を
> 『同一の変更』として追加する」)。
>
> **`M-1-c`** 判定は **paths ではなく意味差分**で行う。**マニフェスト・債務台帳・検査器への
> 接触は判定に用いない。**
>
> **`M-1-d`** **2 件以上が同時に加わった PR は段階 2 ではない**(段階 2 は「1 件ごと」を要求する)。
> この場合は **fail** とし、PR の分割を求める。

#### 正例

| # | PR の内容 | 判定 |
| --- | --- | --- |
| 正-1 | 対象「終了判定」について ①〜④ を揃えて追加。他の対象に触れない | **段階 2** |
| 正-2 | 正-1 と同じ内容に加え、当該対象の債務 ID を解消済み集合へ移す | **段階 2**(債務の解消は段階 2 に付随する) |

#### 負例(**段階 2 に分類されてはならないもの**)

| # | PR の内容 | 判定 | 理由 |
| --- | --- | --- | --- |
| 負-1 | **発効 PR** — 判定器・未達レポート・観測機構・負例 3 種を CI へ接続する。対象は 1 件も加わらない | **段階 2 ではない** | 実在集合の差が 0。**paths ではマニフェストに触れるため誤判定される** |
| 負-2 | **台帳保守** — 債務台帳の書式・コメントを整える。対象は加わらない | **段階 2 ではない** | 実在集合の差が 0 |
| 負-3 | **reseal** — 封印資産の digest を追随させる | **段階 2 ではない** | 実在集合の差が 0 |
| 負-4 | **宣言だけの変更** — マニフェストに対象を宣言するが、生成物・呼び出し部・3 層検査が無い | **段階 2 ではない**(かつ **fail**) | `M-1-b` の 4 点が揃っていない。**宣言だけで未達数を減らせる経路を塞ぐ**(`M-5` と対) |
| 負-5 | 対象を 2 件同時に追加 | **段階 2 ではない**(かつ **fail**) | `M-1-d` |

#### 不成立条件(**この条件下では `M-1` の判定自体が成立しない → fail-closed**)

- **安定した対象 ID の体系が存在しない**(§8 の ID 規約が未確定)
- **base と head のいずれかで実在集合が算出できない**(マニフェストの解析に失敗する)
- **base が移行開始の固定 SHA の祖先でない**(履歴が繋がらない)

→ **いずれも「判定不能」として扱い、`ADR-003` D-11 の既定どおり合格としない**(§3-1 の型 4)。

---

### 5-2. `M-2`: 停滞エポックの定義と「1 マージ」の母集合

**問題**(plan.md 2 周目 P0-2・3 周目 P1-3): 「連続 50 マージで減少 0 なら失効」を測るには、
**何を 1 マージと数えるか**と**いつエポックが始まるか**を一意に決める必要がある。

#### 必須命題

> **`M-2-a`(エポックの開始)** 停滞エポックは次の 3 時点で開始(リセット)する:
> **①移行状態の発効時**(= 判定機構が CI の必須経路へ接続された時点。**正本の approved 時点ではない**)
> **②`未達数 + 債務数` が減少した時点** **③失効後の正当な再承認が成立した時点**。
>
> **`M-2-b`(1 マージの母集合)** 「1 マージ」は **`develop` の第一親をたどった列に現れる
> PR 統合コミット**を数える。**feature 側が `develop` を取り込むマージコミットは数えない。**
>
> **`M-2-c`(候補 PR の評価)** 判定対象の PR は、**固定された base に対する次の 1 本**として
> 評価する。**base が評価中に前進した場合は再評価する。**
>
> **`M-2-d`(観測の開始時点)** **観測は移行状態の開始と同時に始まる。**
> ②の失効判定は①の昇格判定より**先に必要になる** — ①の昇格(未達 0 かつ債務 0)は遠い先だが、
> **②の 50 マージのカウントは開始直後から走る**ため。
>
> **`M-2-e`(履歴の保持場所)** `未達数 + 債務数` の履歴は **CI が生成する機械可読な未達レポート**に
> 記録し、**エポック開始時点の値と現在値の比較**で減少を判定する。
>
> **`M-2-f`(優先順位)** **50 本目のマージと「前進」または「自動昇格」が同一の変更で成立した場合、
> 前進・昇格を優先する**(失効させない)。**減少が起きている以上、停滞ではないため。**

#### 正例

| # | 状況 | 判定 |
| --- | --- | --- |
| 正-1 | エポック開始から 49 マージ経過、50 本目で未達が 1 減った | **失効しない**。エポックが②でリセットされる |
| 正-2 | 50 本目のマージで最後の未達と債務が同時に 0 になった | **失効しない。①が昇格して終了する**(`M-2-f`) |

#### 負例

| # | 状況 | 判定 | 理由 |
| --- | --- | --- | --- |
| 負-1 | 50 マージのあいだ、宣言だけを増やして未達数を見かけ上減らした | **減少に数えない → 失効する** | `M-5` の原子単位が実在集合との差分を要求する |
| 負-2 | feature ブランチが `develop` を 10 回取り込み、それを 10 マージと数える | **数えない** | `M-2-b` |
| 負-3 | 移行状態が**正本で approved になった時点**からカウントを始める | **誤り** | `M-2-a` ①は**発効時**。approved は発効ではない(§7) |

#### 不成立条件

- **履歴が不足している**(エポック開始時点のレポートが存在しない)
- **base が前進していて固定できない**
- **`develop` の第一親をたどれない**(履歴の書き換え等)

→ **いずれも fail-closed**。**停滞していないことを示せない場合は失効扱いとする**
(「測れないから継続」にしない)。

---

### 5-3. `M-3`: 固定 SHA と再承認エポックの分離

**問題**(plan.md 2 周目 P0-3・3 周目 P0-1): 再承認で固定 SHA を更新できると、
**それまでに追加・再導入・復活した債務を新しい開始集合へ吸収でき、比較から消せる。**

#### 必須命題

> **`M-3-a`** **2 つの基準を分離する**:
>
> | 基準 | 用途 | 更新可能性 |
> | --- | --- | --- |
> | **初回開始 SHA** | **債務比較の基準** | **不変**。更新には**正本の改訂**を要する |
> | **再承認エポック** | **50 マージを数える起点** | **再承認で更新できる** |
>
> **`M-3-b`** **再承認が更新できるのは再承認エポックのみ**であり、**初回開始 SHA・初回対象集合・
> 初回債務 ID 集合には触れられない。**
>
> **`M-3-c`** **解消済み債務 ID 集合は追加のみ可能な単調集合**とする
> (**復活の検知に必要な履歴を保つため** — 削除を許すと「解消 → 復活 → 解消済みから削除」で
> 復活が見えなくなる)。
>
> **`M-3-d`** **初回開始 SHA を書き換える PR は無条件に fail** とする。

#### 正例

| # | 状況 | 判定 |
| --- | --- | --- |
| 正-1 | 失効後に人間が再承認し、再承認エポックが更新された。初回開始 SHA は不変 | **正しい** |
| 正-2 | 債務 A を解消し、解消済み集合へ A を追加した | **正しい**(単調に増える) |

#### 負例

| # | 状況 | 判定 | 理由 |
| --- | --- | --- | --- |
| 負-1 | 再承認と同時に初回開始 SHA を現在の `develop` へ更新した | **fail** | `M-3-b`・`M-3-d`。**増えた債務が吸収される** |
| 負-2 | 解消済み集合から ID を削除した | **fail** | `M-3-c`。復活が検知できなくなる |
| 負-3 | 債務 A を解消済みにしたあと、再び A が未宣言になった | **fail** | §6 の規律③(解消済みからの復活) |

#### 不成立条件

- **初回開始 SHA が到達不能**(履歴の書き換え・浅いクローン)
- **初回債務 ID 集合が読めない**

→ **fail-closed**。

---

### 5-4. `M-4`: 再承認の機械的経路

**問題**(plan.md 2 周目 P1-4): 失効後は既存 fail-closed が無条件復帰するため、
**再承認を反映する PR 自体が通常ならマージ不可**になる。

#### 必須命題

> **`M-4-a`(承認者)** **要件書 8 章の判定者**(システム管理者 = プロダクトオーナー)に固定する。
> **既存の承認主体を流用し、本改訂で新しいロールを作らない。**
>
> **`M-4-b`(記録場所)** 再承認は**移行状態の宣言ファイル**に記録する
> (**ファイル名・スキーマ・フィールド名は本改訂で決めない** — `TSK-235` の射程。plan.md「やらないこと」)。
>
> **`M-4-c`(単回性)** **1 回の再承認は 1 つの再承認エポックのみを開始する。**
> 同一の承認記録で 2 度エポックを開始できない。
>
> **`M-4-d`(失効中に許される変更範囲)** **失効中にマージできるのは、次のいずれかに限る**:
> **①`未達数 + 債務数` を減少させる変更** **②再承認そのものを記録する変更**
> **③移行状態と無関係な変更**(`NFR-018` の対象計算・検査基盤に一切触れない PR)。
>
> **`M-4-e`(再承認 PR の扱い)** **②の PR は、移行状態の授権ではなく `M-4-d` ②の規定によって
> マージ可能となる。** この経路は**再承認の記録のみを含む PR に限る**(他の変更を同梱した PR は
> この経路を使えない)。
>
> **`M-4-f`(自動昇格後の禁止)** **①が昇格した後の再承認は受け付けない**(fail)。
>
> **`M-4-g`(有効期限)** **再承認エポックに時間的な期限は設けない。**
> 期限は `M-2` のマージ本数で測る(§4-3 の「時間で測らない」理由と同じ)。

#### 正例

| # | PR | 判定 |
| --- | --- | --- |
| 正-1 | 再承認の記録のみを含む PR | **マージ可**(`M-4-d` ②・`M-4-e`) |
| 正-2 | 失効中に、対象を 1 件実在させて未達を減らす PR | **マージ可**(`M-4-d` ①) |
| 正-3 | 失効中に、フロントエンドの表示文言を直す PR(検査基盤に触れない) | **マージ可**(`M-4-d` ③) |

#### 負例

| # | PR | 判定 | 理由 |
| --- | --- | --- | --- |
| 負-1 | 再承認の記録と、検査器の変更を同梱した PR | **マージ不可** | `M-4-e`。**再承認を口実に他の変更を通す経路を塞ぐ** |
| 負-2 | 昇格後に再承認を記録する PR | **fail** | `M-4-f` |
| 負-3 | 同じ承認記録を参照して 2 度目のエポックを開始する | **fail** | `M-4-c` |

#### 不成立条件

- **承認記録が機械的に読めない**(自由記述のみ)
- **承認者が要件書 8 章の判定者と一致しない**

→ **fail-closed**。

---

### 5-5. `M-5`: `未達数 + 債務数` の原子単位

**問題**(plan.md 2 周目 P1-5): 行の統合・ワイルドカード化・改名・分類変更で
**対象計算を整備せずに値を減らせる。**

#### 必須命題

> **`M-5-a`(原子単位)** **`未達数` は、安定した対象 ID の単位で数える。**
> 1 対象 = 1 カウント。**行・表・ファイルの単位では数えない。**
>
> **`M-5-b`(実在集合との差分)** `未達数` は
> **「要件書 `NFR-018` の対象欄が列挙する対象 ID の集合」から「実在集合」を引いた差**である。
> **宣言集合ではなく実在集合を引く**(plan.md §4-2 の契約 5 — 宣言と実測を別語で呼ぶ)。
>
> **`M-5-c`(実在の条件)** ある対象が実在集合に属するのは、
> **`M-1-b` の 4 点が揃い、かつその対象の 3 層検査が緑である**ときに限る。
>
> **`M-5-d`(行構造の変更は減少に数えない)** 対象 ID の**統合・ワイルドカード化・改名・分類変更**は
> **減少に数えない**。改名は §8 の ID 規約で**禁止**する。
>
> **`M-5-e`(減少の要件)** 減少を主張するには、**独立して実測した入口集合と、必要層の充足**を
> 示さなければならない。**宣言の更新だけでは減少しない**(`M-1` 負-4 と対)。
>
> **`M-5-f`(債務数)** `債務数` は**初回封印された債務 ID 集合の要素数から、解消済み集合の要素数を
> 引いた値**である(§6)。

#### 正例

| # | 状況 | `未達数` の変化 |
| --- | --- | --- |
| 正-1 | 対象「捕球選手推定」について 4 点を揃え、3 層検査が緑になった | **−1** |
| 正-2 | 債務 ID を 1 件解消した(当該対象を宣言し実在させた) | **`未達数` −1 / `債務数` −1** |

#### 負例

| # | 状況 | `未達数` の変化 | 理由 |
| --- | --- | --- | --- |
| 負-1 | 対象 A と対象 B を 1 行に統合した | **変化なし** | `M-5-a`・`M-5-d`。ID の単位で数えるため |
| 負-2 | 対象欄の記述をワイルドカード(「座標変換一般」)へ変えた | **変化なし** かつ **fail** | `M-5-d`。要件書の対象欄の改訂は本改訂の射程外(plan.md「やらないこと」) |
| 負-3 | マニフェストに対象 C を宣言したが、生成物と呼び出し部が無い | **変化なし** | `M-5-c`・`M-5-e` |
| 負-4 | 3 層検査のうち変異層だけが緑で、他の 2 層が無い | **変化なし** | `M-5-c`。`NFR-018` (b)②「いずれか 1 層でも欠ければ満たさない」 |

#### 不成立条件

- **対象 ID の体系が無い**(§8 未確定)
- **実在集合が算出できない**
- **3 層検査の結果が読めない**

→ **fail-closed**。

---

## 6. 既知債務の授権 — 封印の 4 規律

**債務 = 移行開始時点で既に未宣言であった対象を、全件列挙して封印したもの。**
**封印は「増やさないこと」と引き換えに授権を与える機構**である。

### 6-1. 規律① 比較元は移行開始を承認した既存コミットの固定 SHA

> **`BOOT-SEAL-BASE`** 債務の比較元は、**移行開始を承認した既存コミットの完全な commit OID**とする。
> **merge-base を用いてはならない。**

**理由**: merge-base は **base が進むと動く**ため、「開始時点の集合と比べて増えていないこと」を
見る用途に適さない(`TSK-235` から訂正を受領 — plan.md 1 周目 P0-6)。

### 6-2. 規律② 当該 PR から変更不能

> **`BOOT-SEAL-IMMUTABLE`** 固定 SHA は**当該 PR から変更できない**。
> **固定 SHA を書き換える PR は無条件に fail** とする。
> **通常の再承認では更新できず、更新には正本の改訂を要する。**

**理由**: 更新できると `M-3` の吸収が成立する(§5-3 負-1)。

### 6-3. 規律③ 追加・再導入・復活は fail

> **`BOOT-SEAL-MONOTONE`** 次のいずれも **fail** とする:
> **①封印集合に無い対象が新たに未宣言になること**(追加)
> **②一度解消した対象が再び未宣言になること**(復活)
> **③封印集合から要素を削除すること**(再導入の隠蔽)
>
> **解消済み集合は追加のみ可能な単調集合**とする(`M-3-c`)。

### 6-4. 規律④ 債務の対象範囲

> **`BOOT-SEAL-SCOPE`** 債務の対象は、**`ADR-003` D-11 が要求する宣言のうち、
> 移行開始時点で未宣言であったものすべて**とする。
> **実行入口に限定しない** — **表外既定の表示対応の突合**(D-11 ③・v0.2 新設)を含む。

**理由**: 入口だけに限ると、**入口以外の宣言漏れが債務にも未達にも数えられず、
どちらの計上からも落ちる**(plan.md §4-4 論点 4 ④)。

### 6-5. 封印の自己点検 — **片側検査になっていないか**

plan.md §4-2 の契約 5 が要求する自己点検を、本節自身に当てる。

| 検査 | 宣言側 | 実測側 | 両側にあるか |
| --- | --- | --- | --- |
| 封印集合の重複拒否 | ✅ 封印集合の要素は一意 | ✅ 実在集合の要素も一意 | **両側** |
| 追加の検出 | ✅ 封印集合に無い | ✅ 実測で未宣言 | **両側** |
| 復活の検出 | ✅ 解消済み集合に在る | ✅ 実測で未宣言 | **両側** |

**`TSK-317` が踏んだ型**(`check_authz_catalog.py` は `sealed_assets` 側に重複拒否があるのに
`input_assets` 側に無かった)を、**本改訂の封印では両側に置く**。

---

## 7. 発効原子性

### 7-1. 何が揃ったときに発効するか

> **`BOOT-ACTIVATION`** 移行状態は、**本規定の各条項を機械的に判定する機構**と、
> **各条項が不合格とする事象がいずれも不合格となることを示す負例**とが、
> **すべて同一の変更(= `develop` へ統合される 1 つの PR)で CI の必須経路へ接続された時点**をもって発効する。
>
> **機構と負例の具体的な構成は `TSK-235` が定める**(射程縮小)。**ただし次を満たさなければならない**:
>
> | # | 要求 |
> | --- | --- |
> | ① | **本規定のすべての条項について、その違背を検出する機構が存在すること** |
> | ② | **各条項が不合格とする事象について、それが実際に不合格となることを示す負例が存在すること** |
> | ③ | **軸①の自動昇格および軸②の失効が、実際にその条件で成立することを示す正例が存在すること** |
>
> **接続前は「定義済みだが未接続」であり、既存の fail-closed が維持される。**

**当初 7 点を列挙し、2 周目で 10 点へ拡張したが、3 周目で「列挙が閉包でない」と指摘された**
(自動昇格を「不合格とする負例」へ誤って入れていた — **残務 0 での昇格は成功させるべき正遷移**。
また⑩が「違反・判定不能を免除しない」検査なのに負例は違反だけで判定不能を捕捉していなかった)。

→ **射程縮小により、点の列挙をやめ「全条項に機構と負例がある」という要求へ置き換えた。**
**列挙は条項が増減するたびに追随を要し、追随漏れが構造的に生じる。**

**7 点では閉じていなかった**(初周 指摘 5): 当初の 7 点は**判定器の責務を二軸判定に限定**しており、
**`BOOT-SEAL-*`・`BOOT-NO-CLAIM`・`BOOT-REAPPROVAL` を検査する機構も負例も発効条件に無かった。**
→ **7 点を形式上満たしながら、債務の追加や充足の誤表示を止める検査が未実装のまま発効できた。**

**「未接続」の語については §3-4 を参照する**(`TSK-379` の「未発効」と区別する)。

### 7-2. なぜ観測機構(4)を同一変更に含めるか

**②の失効判定は①の昇格判定より先に必要になる** — ①の昇格(未達 0 かつ債務 0)は遠い先だが、
**②の 50 マージのカウントは開始直後から走る**。**発効時に観測機構が動いていなければ
失効判定が成立しない**ため、両者を同一変更に含めることを条文で要求する
(`TSK-235` からの指摘 — **先方の実装順序が一意に決まる**)。

### 7-3. 発効と approved の区別

| 時点 | 何が起きるか | 移行状態 |
| --- | --- | --- |
| 3 正本が **approved** になる | **条文が確定する** | **定義済み・未接続**。**既存の fail-closed が維持される** |
| **10 点 + 必須の負例**が CI の必須経路へ接続される | **発効する** | **有効**。停滞エポックの観測が開始する(`M-2-a` ①) |

**approved は発効ではない。** この区別を条文に明記する(§5-2 負-3 が塞ぐ経路)。

### 7-4. コードは本改訂で書かない

**10 点と必須の負例の実装は `TSK-235` の射程**である(plan.md「やらないこと」)。
**本改訂が拘束するのは「揃っていなければ発効しない」という条件のみ**であり、
**ファイル名・スキーマ・フィールド名は正本に書かない。**

---

## 8. 条項 ID の体系

### 8-1. 名前空間 — **`BOOT-` を採る**

**既存 ID との衝突を実測した**(2026-09-13・`docs/**/*.md`・`docs/legacy/` 除外):

| 候補接頭辞 | 既存の出現 |
| --- | --- |
| **`BOOT-`** | **0 件** |
| `TRANS-` | 0 件 |
| `MIGR-` | 0 件 |
| `BT-` / `MG-` / `TR-` | 0 件 |

**既存の主要接頭辞**(3 件以上の実測・上位): `TSK`(4883)/ `FR`(4223)/ `NFR`(2994)/
`P1`(1543)/ `REQ`(1307)/ `ADR`(1015)/ `P0`(676)/ `P2`(407)/ `DL`(319)/ `SP`(259)/
`AUTH`(41)/ `CATALOG`(18)/ `REJ`(23)/ `PROP`(50)ほか。

→ **`BOOT-` を採る**(bootstrap = 導入期。**2 文字略号は将来の衝突余地が大きい**ため 4 文字を採る)。

### 8-2. ID の規約(`TSK-235` の 4 要求を受け入れる)

| # | 規約 | 根拠 |
| --- | --- | --- |
| 1 | **粒度は条項単位**(節単位にしない) | 節単位だと、単調性だけに依拠するステップが「移行状態の定義」全体を指すことになり、**単調性の条文が消えても実在検査が通ってしまう** |
| 2 | **意味的な安定 ID**(節番号を含めない) | 番号ベースは**改訂で節が動いた瞬間に照合が全滅**する |
| 3 | **一度付けた ID は改訂で改名しない**(追加は自由) | 改名は**偽の発火**をする(授権は生きているのに「実在しない」と判定する) |
| 4 | **削除・失効時は ID を消してよい** | その場合の発火は**正しい発火**(依拠していた授権が撤回されたなら `TSK-235` は止まるべき) |

**非対称は 3 段**(`TSK-235` が §12-1-b-1 で確定した照合方式):
**改名 = 偽発火 → 条文側が禁止** / **削除 = 真発火 → 停止** / **内容変化 = 再確認 → 停止しない**。

### 8-3. digest 計算前の正規化規約

> **`BOOT-ID-NORMALIZE`** 条項 digest の計算前に、**空白の連続を単一空白へ畳み、
> 行末空白と改行を正規化する。それ以外は無加工。**

**理由**: 正本は日本語 Markdown で**行を手で折り返している**ため、**折り返し位置を変えるだけ(reflow)で
生テキストの digest が変わる**が**規範は変わっていない**。そのままでは `TSK-235` の②が
**偽の再確認を大量に出す**。この正規化により **reflow では発火せず、語句の変更では発火する**。

> **混同注意**: `TSK-235` の §7-3 に「**比較器は正規化しない**」という規約があるが、
> **あれは表示値の比較**(`ADR-003` の lossless 原則)で**別物**。**両者を別の名前で呼ぶ** —
> **表示値比較 = lossless / 条項 digest = 正規化あり**。

### 8-4. 条項 ID 対応表の必須列

| 列 | 用途 |
| --- | --- |
| **条項 ID** | 実在照合の鍵 |
| **規範の所在**(正本と条項名) | 人間の追跡 |
| **範囲の切り出し規則** | digest の対象範囲。**表形式の条項では「規範を担うセル」に限る**(行全体だと列を 1 つ足すだけで全条項が一斉に再確認になる) |
| **版と `status`**(**行ごと**) | 「依拠先は `approved` 版に限る」検査の入力。**表全体で 1 つでは不足** |
| **`superseded_by`** | **「撤回」と「分割・統合」の区別**。分割・統合を②の再確認(停止しない)へ落とす |
| **`kind`**(規範 / 補足) | 「依拠先が `規範` でない行なら fail」の機械検査 |
| 一行の要旨 | **人間向け。本機構の検査の入力にはしない**(表に明記する) |

**対応表そのものの不変条件**: **条項 ID の重複を許さない**(`TSK-235` からの要求・2026-09-12)。
**対応表の生成時に一意性を機械検査する** — §4-2 の契約 5 の自己点検を**本タスク自身の成果物にも当てる**。

### 8-5. 本改訂が定義する条項 ID(暫定一覧)

**§9 の条文案と対応する。ステップ 3 の改訂で正本へ着地させ、§11 の写像表で照合する。**

| 条項 ID | 規範の内容 | 所在(予定) |
| --- | --- | --- |
| `BOOT-STATE` | 移行状態の定義(二軸) | 要件書 `NFR-018` |
| `BOOT-PROGRAM` | 軸①移行プログラムの開始・継続・終了・不可逆性 | 要件書 `NFR-018` |
| `BOOT-GRANT` | 軸②緩和されたマージ授権の有効・失効・再承認 | 要件書 `NFR-018` |
| `BOOT-VOCAB` | 4 型の書き分け(未達 / 違反 / 債務 / 判定不能) | 要件書 `NFR-018` |
| `BOOT-NO-CLAIM` | **移行状態では `NFR-018(b)②` / `NFR-019(a)` を満たしたと判定できない** | 要件書 `NFR-018` |
| `BOOT-REPORT` | 未達レポートの出力義務(黙って緑にしない) | 要件書 `NFR-018` |
| `BOOT-SEAL-BASE` | 比較元は固定 SHA(merge-base ではない) | 要件書 `NFR-018` |
| `BOOT-SEAL-IMMUTABLE` | 固定 SHA は当該 PR から変更不能 | 要件書 `NFR-018` |
| `BOOT-SEAL-MONOTONE` | 追加・再導入・復活は fail / 解消済みは単調 | 要件書 `NFR-018` |
| `BOOT-SEAL-SCOPE` | 債務の対象範囲(入口に限定しない) | 要件書 `NFR-018` |
| `BOOT-ACTIVATION` | 発効原子性(**10 点 + 必須の負例**・「同一の変更」= PR) | 要件書 `NFR-018` |
| `BOOT-COUNT` | **状態変数(未達数・既知債務数)の定義と原子単位** | 要件書 `NFR-018` |
| `BOOT-REAPPROVAL` | **再承認の統制**(承認者・単回性・専用 PR・失効中の変更範囲・昇格後の禁止) | 要件書 `NFR-018` |
| `BOOT-STALL` | 停滞エポックと N マージ(`N = 50`) | 要件書 `NFR-018` |
| `BOOT-ID-NORMALIZE` | 条項 digest の正規化規約 | **本書**(正本には置かない — §10 で理由) |
| `BOOT-ADR-STAGE` | 移行状態が帰結 1 の段階 1〜2 にまたがること | `ADR-003` |
| `BOOT-ADR-TRIGGER` | 見直しトリガー「移行状態が機能しない場合」 | `ADR-003` |
| `BOOT-CI-MEANING` | 段階ゲートの green / `NFR-019(a)` の充足判定 / 全体適合判定の関係 | 設計書 10.1 |

**このうち `BOOT-ID-NORMALIZE` だけは正本へ置かない。** 理由は §10-3。

---

## 9. 条文案

**経過規定として書く**(plan.md §4-2 の契約 1)。「例外ではない」とは書かず、
**優先関係・両軸の遷移条件・授権が有効でない間の無条件復帰**を明示する。

### 9-1. 要件書 `NFR-018` — 達成条件 **(d) の後**に (e) として置く経過規定

**配置の決定**: 既存の達成条件は (a) 実装の単一性 /(b) 宣言と機械判定 /(c) 逐語移植対象の例外 /
(d) 実装分類の列挙の一元性 の 4 つである。**経過規定は (e) として末尾に置く** —
**番号順を保ち、既存 4 条件の構造を壊さないため**。**(b) への適用関係は条文の中で明示する**
(「本項の達成条件 (b) が要求する検査基盤が未整備である期間について」)。

> **(e) 検査基盤の導入期における経過規定**
>
> **本項の達成条件 (b) が要求する検査基盤が未整備である期間について、次を定める。
> 本規定は (b) の充足判定を変更しない — 変更するのはマージ可否の判定のみである。**
>
> **`BOOT-VOCAB`(語の定義)** 本規定において:
> **「未達」**とは、対象欄が列挙する対象のうち、(b) が要求する層・検査が整備されていないものをいう。
> **「違反」**とは、宣言と実在が食い違う状態(宣言外の入口の存在・生成物の手編集・派生物の陳腐化)をいう。
> **「既知債務」**とは、移行開始時点で既に未宣言であった対象を全件列挙して封印したものをいう。
> **「判定不能」**は [ADR-003](../adr/ADR-003-domain-calc-method.md) D-11 の定義による。
> **本規定が扱うのは「未達」と「既知債務」のみであり、「違反」と「判定不能」には一切触れない**
> — これらは本規定の期間中も従来どおり不合格として扱う。
>
> **`BOOT-STATE`(移行状態)** 検査基盤の導入期を**移行状態**と呼び、次の二軸で定める。
> **同じ状態に 2 つの終了条件を持たせない。**
>
> **`BOOT-PROGRAM`(軸① 移行プログラム)** **未達数と既知債務数の合計が 0 より大きいあいだ継続**する。
> **合計が 0 になった時点をもって自動的に終了し、以後は本項の達成条件をそのまま適用する**
> (**必須ゲートへの昇格**)。**昇格は不可逆であり、移行状態へ戻る経路を設けない** —
> 戻すには本書の改訂を要する。
>
> **`BOOT-GRANT`(軸② 緩和されたマージ授権)** 軸①が継続しているあいだ、
> **未達および既知債務を理由としてマージを妨げない**。ただし次のいずれかで**失効**する:
> **①停滞エポック内で未達数と既知債務数の合計が 1 度も減少しないまま 50 回のマージが経過したとき**
> **②軸①が終了したとき**(この場合は失効ではなく終了)。
> **①による失効中は、本項の達成条件が無条件に復帰する** — 移行状態を理由としたマージ可否の
> 別判定は行われない。**失効は人間の明示的な再承認によってのみ解除される。**
>
> **`BOOT-NO-CLAIM`(充足判定の禁止)** **移行状態にあるあいだ、本項の達成条件 (b)② を
> 満たしたと判定してはならない。また NFR-019(a) を合格と判定してはならない。**
> **これは軸②が有効であるか失効しているかを問わない。**
> **移行状態における CI の緑は「移行状態の要求を満たしている」ことのみを意味し、
> 本項または NFR-019(a) の充足を意味しない。**
>
> **`BOOT-REPORT`(未達レポート)** **移行状態にあるあいだ、CI は未達および既知債務の一覧を
> 機械可読な形式で出力しなければならない。** 出力のない緑は本規定の充足とみなさない。
>
> **`BOOT-SEAL-BASE`(比較元)** 既知債務の比較元は、**移行開始を承認したコミットの
> 完全な commit OID** とする。**merge-base を用いてはならない。**
>
> **`BOOT-SEAL-IMMUTABLE`(比較元の不変性)** 比較元の commit OID は**当該 PR から変更できない**。
> **これを書き換える変更は無条件に不合格とする。** 更新には本書の改訂を要し、
> **軸②の再承認では更新できない。**
>
> **`BOOT-SEAL-MONOTONE`(単調性)** 次のいずれも**不合格**とする:
> **①封印集合に無い対象が新たに未宣言となること ②一度解消した対象が再び未宣言となること
> ③封印集合から要素を削除すること。**
> **解消済みの記録は追加のみ可能とし、削除してはならない。**
>
> **`BOOT-SEAL-SCOPE`(債務の範囲)** 既知債務の対象は、
> **ADR-003 D-11 が要求する宣言のうち移行開始時点で未宣言であったものすべて**とする。
> **実行入口に限定しない。**
>
> **`BOOT-STALL`(停滞の測定)** 停滞エポックは
> **①移行状態の発効時 ②未達数と既知債務数の合計が減少した時点 ③再承認の成立時点**
> のいずれかで開始する。**経過は時間ではなくマージ回数で測る。**
> **50 回目のマージと減少または昇格が同一の変更で成立した場合は、減少・昇格を優先する。**
>
> **`BOOT-ACTIVATION`(発効)** **本規定における「同一の変更」とは、`develop` へ統合される
> 1 つの PR をいう**(PR 内のコミットの分割は妨げない)。本規定は **10 点がすべて同一の変更で
> CI の必須経路へ接続され、かつ必須の負例がすべて当該変更に含まれた時点をもって発効する。**
> **接続前は本規定を適用せず、本項の達成条件をそのまま適用する。**
> **本書が approved となることは発効ではない。**

### 9-2. 要件書 `NFR-019` — 柱書は変えず、(a) へ 1 文を足す

**柱書「全グリーンでないとマージ不可」は変更しない**(plan.md「やらないこと」)。
**(a) の末尾に次を加える**:

> **NFR-018 の経過規定 (e) が発効しているあいだ、本項の合格判定は同規定 `BOOT-NO-CLAIM` に従う**
> — **移行状態における CI の緑は本項の合格を意味しない。**

### 9-3. `ADR-003` — D-7 の (b)③ 行への併記と、移行状態の決定条文

**D-7 の実行契機表の (b)③ 行(構成の完全性)の「失敗時の扱い」欄へ併記する**:

> **マージ不可**。**これをもって要件書 v2.1 の (b)② を満たしたとはしない**(D-4)。
> **要件書 NFR-018 (e) の移行状態が発効しているあいだは、同規定 `BOOT-GRANT` により
> 未達および既知債務を理由とするマージ不可は適用されない。違反(宣言と実在の食い違い)は
> 移行状態においても従来どおりマージ不可である。**

**あわせて決定条文 `BOOT-ADR-STAGE` を新設する**:

> **D-13: 移行状態が帰結 1 の着手順序に対して占める位置**
>
> **要件書 NFR-018 (e) の移行状態は、帰結 1 の着手順序のうち段階 1 と段階 2 にまたがる。**
> 段階 3 は「当該対象について全検査が緑であることをマージの条件とする」と対象単位で
> マージ条件を定めており、**対象が 1 件も存在しない段階 1 のマージ条件だけが未定義であった。**
> **本決定はその区間に経過規定を与えるものであり、段階 3 以降の条件を変更しない。**
>
> **根拠は段階 3 の括弧書きの延長線上にある** — 逐語「**未完成のあいだ適合と判定しないであって、
> 実装を禁じるものではない**」。**移行状態はこの区別を、対象が存在しない区間へ及ぼす。**
>
> **D-4 の「対象範囲を縮小して②を満たしたことにはできない」は維持する** —
> **移行状態は対象を縮小しない。債務台帳に載せた対象も未達として計上し続ける。**

**見直しトリガーへ 1 件追加する**(`BOOT-ADR-TRIGGER`):

> - **移行状態が機能しないとき** — 停滞による失効が繰り返される、あるいは既知債務が
>   解消されないまま推移する場合。**移行状態の設計そのもの(二軸・停滞の閾値・債務の範囲)を
>   見直す**(本 ADR 自身が後で判断する事象)

### 9-4. 設計書 10.1 — ジョブ状態の意味づけ

**ジョブ名と個数は変更しない**(plan.md「やらないこと」)。**箇条書きへ 1 項を加える**:

> - **移行状態における CI 全ジョブ green の意味**(`BOOT-CI-MEANING`・要件書 `NFR-018` (e)):
>   **要件書 NFR-018 の経過規定が発効しているあいだ、`consistency` を含む全ジョブの green は
>   「移行状態の要求を満たしている」ことを意味し、`NFR-019(a)` の合格および `NFR-018(b)②` の
>   充足を意味しない。** 3 者の関係は次のとおり:
>
>   | 判定 | 意味 | 成立時点 |
>   | --- | --- | --- |
>   | **段階ゲートの green** | 当該 PR が移行状態の要求を満たしている | PR ごと |
>   | **`NFR-019(a)` の充足** | (α) の全対象のベクタが完成し一致性が成立した | 移行状態の終了後 |
>   | **全体適合判定** | `NFR-018(b)②` を満たした | 移行状態の終了後 |
>
>   **移行状態にあるあいだ、CI は未達レポートを出力する**(同規定 `BOOT-REPORT`)。

---

## 10. `D-xx` の配置決定と選定理由

### 10-1. 規範をどこへ置くか

| 規範 | 置き場 | 理由 |
| --- | --- | --- |
| **移行状態の定義・二軸・語彙・封印・発効** | **要件書 `NFR-018` (e)** | **`NFR-018` 自身の達成条件の適用を時限的に調整する規定**であり、**調整される条項と同じ場所に置く**のが追跡可能性が高い。別文書に置くと「上位の義務を下位が解除する」形になる(`TSK-379` が `NFR-019(b)` で踏んだ構造 — 12-4 は下位文書なので上位の義務を解除できない) |
| **着手順序に対する位置づけ** | **`ADR-003` D-13(新設)** | **帰結 1 の段階構造は ADR-003 の決定**であり、そこへの位置づけは同書が持つ |
| **CI ジョブ状態の意味** | **設計書 10.1** | **`consistency` の定義を持つのが 10.1** であるため |

### 10-2. `ADR-003` に規範を置かない原則との関係

`TSK-379` が ADR-004 で確立した原則(**ADR は争点・決定・理由・却下案を持ち、規範は持たない**)と、
本改訂の **D-13 新設**は矛盾しないか。

**矛盾しない。** D-13 が置くのは**位置づけの決定**であり、**適用条件・合格述語・量化子を持たない**。
**規範は要件書 `NFR-018` (e) 側にあり、D-13 はそれを参照する。**

**検証可能な境界**(ADR-004 の型を借りる): **D-13 から条項参照(`NFR-018` (e))を外すと、
規範として読めなくなること。** 読めてしまうなら規範を持っている。

### 10-3. `BOOT-ID-NORMALIZE` を正本へ置かない理由

**条項 digest の正規化規約は、`TSK-235` の照合機構の実装詳細**である。
**正本へ置くと、先方の実装方式が変わるたびに正本の改訂が要る。**

→ **本書に置き、`TSK-235` へ申し送る。** ただし **`TSK-235` は本書の記述に依拠できない**
(design.md はゲート対象外)ため、**先方が自身の design.md で確定する**。
**本改訂が保証するのは「条項 ID の安定」のみ**である(plan.md §4-10 の合意どおり)。

---

## 11. 写像表

**§5〜§9 の各回答が、どこへ着地するか。**
**ステップ 3 では、本表の各行が実際に正本の条項へ着地したことを検証する。**
**写像漏れ・着地漏れはいずれも停止条件**(plan.md §4-10 の機構 2)。

| # | 回答 | 着地先 | 種別 |
| --- | --- | --- | --- |
| 1 | §3-1 4 型の定義 | 要件書 `NFR-018` (e) `BOOT-VOCAB` | **正本へ条文化** |
| 2 | §4-2 軸①の定義 | 要件書 `NFR-018` (e) `BOOT-PROGRAM` | **正本へ条文化** |
| 3 | §4-3 軸②の定義 | 要件書 `NFR-018` (e) `BOOT-GRANT` | **正本へ条文化** |
| 4 | §5-1 `M-1` 段階 2 の判定 | 要件書 `NFR-018` (e) **`BOOT-COUNT`**(4 点が揃い 3 層が緑であること)+ **`BOOT-GRANT`**(段階 2 の新規対象には本規定を適用しない) | **正本へ条文化**(初周 指摘 2 により段階 2・3 の空文化を塞いだ) |
| 5 | §5-2 `M-2` 停滞エポック**とマージ回数の母集合** | 要件書 `NFR-018` (e) `BOOT-STALL` | **正本へ条文化**(初周 指摘 4 により母集合を正本へ移した) |
| 6 | §5-3 `M-3` 固定 SHA と再承認の分離 | 要件書 `NFR-018` (e) `BOOT-SEAL-IMMUTABLE` | **正本へ条文化** |
| 7 | §5-4 `M-4` 再承認の経路 | 要件書 `NFR-018` (e) **`BOOT-REAPPROVAL`** | **正本へ条文化**(初周 指摘 6 により統制を正本へ移した。**記録の形式と承認の認識条件のみ委任**) |
| 8 | §5-5 `M-5` 原子単位 | 要件書 `NFR-018` (e) **`BOOT-COUNT`** | **正本へ条文化**(初周 指摘 3 により状態変数の定義を正本へ移した) |
| 9 | §6-1〜6-4 封印の 4 規律 | 要件書 `NFR-018` (e) `BOOT-SEAL-*` 4 条項 | **正本へ条文化** |
| 10 | §7-1 発効原子性(**7 点 → 10 点**) | 要件書 `NFR-018` (e) `BOOT-ACTIVATION` | **正本へ条文化**(初周 指摘 5 により封印・充足判定・再承認の検査と負例を追加) |
| 11 | §7-3 approved と発効の区別 | 要件書 `NFR-018` (e) `BOOT-ACTIVATION` 末文 | **正本へ条文化** |
| 12 | §3-4 「未発効」との衝突回避 | 要件書 v2.9 の**射程宣言**(変更履歴行)+ 本書 §3-4 | **記録**(条文には「未発効」を使わないことで実現) |
| 13 | §9-2 `NFR-019(a)` への追記 | 要件書 `NFR-019` (a) 末尾 | **正本へ条文化** |
| 14 | §9-3 **D-7 表の 6 行すべて**への併記(正解ベクタ / 差分 / プロパティ / 変異 = 未達側 4 行、(b)① 乖離検出 / (b)③ 構成の完全性 = 違反側 2 行) | `ADR-003` D-7 表 **6 行** | **正本へ条文化** |
| 15 | §9-3 段階構造への位置づけ | `ADR-003` **D-13(新設)** `BOOT-ADR-STAGE` | **正本へ条文化** |
| 16 | §9-3 見直しトリガー | `ADR-003` 見直しトリガー `BOOT-ADR-TRIGGER` | **正本へ条文化** |
| 17 | §9-4 CI ジョブ状態の意味 | 設計書 10.1 `BOOT-CI-MEANING` | **正本へ条文化** |
| 18 | §8-1〜8-2 ID 名前空間と規約 | **条項 ID として全条文に付与**(着地は 1〜17 の各条項) | **正本へ条文化**(ID の形で) |
| 19 | §8-3 digest 正規化規約 | **本書のみ**(§10-3) | **申し送り**(`TSK-235`) |
| 20 | §8-4 対応表の必須列 | **本書 §12**(ステップ 2 で生成) | **成果物** |

**委任は 2 件に減った**(初周 指摘 3・4・6 により #4・#5・#7・#8 を正本へ移した):
**#19(digest 正規化規約)と、各条項が明示的に委任する実装詳細**
(**義務の件数の算出 / 承認記録の形式と認識条件**)**のみ**。
**正本は「何を満たすべきか」と「粒度」を定め、「どう数えるか」だけを委任する。**
**委任であることは正本の条文に明示する。空手形にしないため、委任先が満たすべき条件を §5 に残す。**

---

## 未解決・検討メモ

- **`M-1`〜`M-5` の答えは出したが、`TSK-235` との突合が済んでいない。**
  plan.md §4-12 の約束により、**ステップ 2 完了時に先方へ渡す**。渡すものは
  **二軸の判定条件 / 封印の 4 規律の最終形 / `M-1`〜`M-5` の答え / 条文と条項 ID 対応表 /
  ID の名前空間規約と digest 正規化規約**の 5 点。
- **`BOOT-` 接頭辞の衝突検査は 2026-09-13 時点の実測である。** `TSK-235` が独自の ID 体系を
  導入する場合、**先方の接頭辞と重ならないことを渡す時点で再確認する**(plan.md §4-12
  「伝える時点で測り直す」)。
- **条項 ID 対応表(§12)の `版と status` 列は、ステップ 6 の approved 化で確定する。**
  現時点では全条項が `in-review` であり、**`TSK-235` は本改訂の条項へまだ依拠できない**
  (先方の照合機構は `approved` 版に限って依拠する — plan.md §4-10)。

### 2-6. 分類表 — 射程節の全ヒット

**走査時点**: 2026-09-13・`develop` = `0bc05b8` へリベース後(§2-5 の取り直し規約を適用する)。
**分類の 3 種は plan.md §4-3 の定義による** — **直接改訂**(既存箇所を編集する)/
**明示参照**(新設条項から参照し既存箇所は無変更)/ **非波及**(無変更 + 逐語証明)。

#### 2-6-1. 要件書(23 件)

| 行 | 強制語 | 規範の要旨 | 分類 | 根拠 |
| --- | --- | --- | --- | --- |
| 894 | fail / 実装を開始せず | 未分類の対象計算は実装を開始せず本書の改訂で区分を確定 | **非波及** | **移行状態は「未分類」を救わない**(§3-1 の型 4 と同じ扱い)。区分の確定は本改訂の射程外(plan.md「やらないこと」= 区分 (α)(β) を変えない) |
| 897 | 満たさない | ②を欠く担保は本要件を満たさない | **明示参照** | **充足判定の条項**。`BOOT-NO-CLAIM` から参照し、**移行状態でも②の充足判定は変わらない**ことを明示する |
| 899 | 許さない | 保証範囲内では取りこぼしを許さない | **非波及** | **保証範囲の定義**であり充足判定の帰結を持たない。移行状態は範囲を変えない |
| 900 | 不合格 | 実行入口経由の出力と生成物直接呼び出しの一致が破れたら不合格 | **明示参照** | **これは「違反」の検出**(§3-1 の型 2)。`BOOT-VOCAB` から参照し**移行状態でも不合格**であることを明示する |
| 902 | 満たさない | いずれか 1 層でも欠ければ本要件を満たさない | **明示参照** | **「未達」の定義の典拠**。`BOOT-VOCAB` がこの条項を参照して未達を定義する |
| 905 | 認めない | 非等価変異の生存を認めない(生存 0 が合否基準) | **非波及** | **変異層の合否基準**。移行状態は層の基準を緩めない(緩めるのはマージ可否のみ) |
| 906 | 禁止 | コピー実装の禁止と (a) の単一性 | **非波及** | **上位要件そのもの**。plan.md「やらないこと」= fail-closed 原則を緩めない |
| 907 | 拒否 / 禁止 | 宣言外の入口を拒否・動的機構の禁止 | **非波及** | **「違反」の禁止**(型 2)。research.md §8-7 の切り分け逐語「**拒否・禁止・手段の重ねは緩めない**」 |
| 908 | 不合格 / 合格としない / fail | 未分類・判定不能は合格としない。正解ベクタ未整備・変異生存率超過は不合格 | **直接改訂** | **本改訂の中核**。「不合格として扱う」は**維持したまま**、直後に (e) を新設して**マージ可否の別判定**を置く。**この行自体の文言は変えない**が、(e) が同じ達成条件 (b) の内側に入るため**直接改訂の節に属する** |
| 909 | 実装を開始せず | ①〜③が実現不能と ADR が結論した場合は実装を開始せず in-review へ戻す | **非波及** | **「実現不能」は「未整備・既知債務」と区別される**(plan.md 6 周目 P2-1)。**移行状態は実現不能を救わない**。逐語証明: 本条は ADR の結論を発火点とし、未達数・債務数を発火点としない |
| 910 | 禁止 / 例外は成立しない / 違反として扱う | (c) 逐語移植の例外表の境界 | **非波及** | plan.md「やらないこと」= **(c) の例外表を触らない**。逐語: 除外されるのは「本項の要件および達成条件 (a)」であり **(b)③ の入口宣言義務には波及しない** |
| 917 | 禁止 | 測定方法(動的機構の禁止を含む) | **非波及** | 907 と同じ(測定方法欄での再掲) |
| 918 | 合格と判定してはならない / 満たしたと判定 | 正解ベクタの発効条件((α) は `NFR-019(a)` を合格としない /(α)(β) は (b)② を満たしたとしない) | **明示参照** | **`BOOT-NO-CLAIM` の典拠**。**移行状態でもこの判定は維持される**ことを新設条項から明示参照する |
| 919 | 禁止 | 申し送り(ADR の確定事項の列挙) | **非波及** | **申し送りの記述**であり判定を持たない |
| 923 | マージ不可 | `NFR-019` 柱書「全グリーンでないとマージ不可」 | **直接改訂** | plan.md「やらないこと」により**柱書は変えない**が、**(a) の末尾に 1 文を足す**(§9-2)。**柱書そのものは無変更**であり、変更は (a) に限る |
| 931 | 不合格 / 合格と判定してはならない / fail | `NFR-019(b)` の発効条項(v2.8・TSK-379) | **非波及** | **別の事柄**(§3-4)。**共有するのは原則のみで条文は独立**。逐語証明: 本条は「経路の実装」を発効条件とし、本改訂は「検査基盤の接続」を発効条件とする |
| 1029 | 禁止 | 開発プロセス(直接コミット禁止・PR ベース + CI 全グリーン必須) | **明示参照** | **CI 全グリーン必須の再掲**。`BOOT-GRANT` が「マージを妨げない」と定める対象がこの要求であるため、**新設条項から参照する**。**本行は無変更** |
| 1038 | fail | 8 章 リリース判定基準 2((b) の合否) | **非波及** | **リリース判定**であり開発期のマージ可否ではない。移行状態は `BOOT-PROGRAM` の昇格で終了しており、リリース時点では適用されない |
| 1066 | 設けない | グループ数の上限は設けない | **非波及** | **製品仕様**(FR-041)。検査基盤に触れない |
| 1083 | 満たしたと判定 | 10 章 未決事項「未完成の間の扱いは区分で分かれる」 | **直接改訂** | **移行状態の記述を追記する**。本行は「未完成の間の扱い」を定めており、**移行状態はまさにその期間の規定**であるため |
| 961 | 禁止 | `NFR-021` 証跡の改変禁止 | **非波及** | **受入証跡の保護**。検査基盤の充足判定に触れない |
| 1100 | fail | A-1 `NumericValue` の直接差し込みは生成前検査で fail | **非波及** | **表示 binding の生成前検査**。型 2(違反)の検出であり移行状態は救わない |
| 1102 | 禁止 | A-1 言語既定の文字列化の禁止 | **非波及** | 1100 と同じ |

**要件書の内訳**: 直接改訂 **3** / 明示参照 **5** / 非波及 **15** = **23**(未分類 **0**)。

#### 2-6-2. `ADR-003`(42 件)

| 行 | 強制語 | 規範の要旨 | 分類 | 根拠 |
| --- | --- | --- | --- | --- |
| 100 / 102 | fail / 禁止 | D-1 表示 binding の生成前検査(`NumericValue` 直挿し・欠けた宣言) | **非波及** ×2 | **型 2(違反)の検出**。宣言の欠落を検出する検査であり、移行状態は宣言と実在の一致部分のみを扱う |
| 110 | 満たさない | D-1 手書き 2 実装は達成条件 (a) を満たさない | **非波及** | **(a) 単一性**。plan.md「やらないこと」= fail-closed 原則を緩めない |
| 131 | 失敗させ | D-3 手編集した生成物は (b)① が検出して CI を失敗させる | **明示参照** | **型 2(違反)の代表例**。`BOOT-VOCAB` が「違反」を定義する際に参照する |
| 145 | 合格としない / fail | D-4 未分類・判定不能は合格としない(fail-closed) | **明示参照** | **型 4(判定不能)の典拠**。`BOOT-VOCAB` から参照し**移行状態は救わない**ことを明示 |
| 146 | 禁止 | D-4 対象範囲を縮小して②を満たしたことにはできない | **明示参照** | **D-13 が「移行状態は対象を縮小しない」と述べる際の典拠**(§9-3) |
| 160 | fail | D-4 単一ツールでは fail-closed にならない(根拠の記述) | **非波及** | **調査結果の記録**であり規範ではない |
| 170 / 172 / 173 | 拒否 / 禁止 ×3 | D-5 (a) 入口の全列挙・動的機構の禁止と検出 | **非波及** ×3 | research.md §8-7 逐語「**拒否・禁止・手段の重ねは緩めない**」 |
| 185 | 拒否 | D-5 (c) ディレクトリ限定・lint で拒否 | **非波及** | 同上(補助手段) |
| 194 / 207 | 許さない ×2 | D-11 ② 保証範囲・境界軸の false green | **非波及** ×2 | **保証範囲の定義**。移行状態は範囲を変えない |
| 216 / 217 | fail ×2 | D-11 ② 経路一致検査・生成器の独立性 | **非波及** ×2 | **型 2(違反)の検出**(製品経路が別実装を使う事実の検出) |
| 218 | 認めない | D-11 ② 非等価変異の生存を認めない | **非波及** | **層の合否基準**。移行状態は層の基準を緩めない |
| 226 / 228 / 229 / 231 | fail ×4 | D-11 ② 変異テストの運用規則(未対応残・変異数 0・発火条件・上限超過) | **明示参照** ×4 | **plan.md §4-3 が強制点として列挙**。`BOOT-VOCAB` の「未達」がこれらの層の未整備を指すため、**新設条項から参照して「未達として計上し続ける」ことを明示** |
| 239 / 240 / 241 / 242 | fail / 禁止 ×4 | D-11 ③ 入口差分・表示対応の突合・モジュール突合・動的機構 | **明示参照** ×4 | **`BOOT-SEAL-SCOPE` が「D-11 が要求する宣言」と述べる際の対象**。**債務の範囲を入口に限定しない**ことの典拠 |
| 244 | 満たさない / fail | D-11 ③ 「いずれか 1 層でも欠ければ満たさない」の再掲 | **明示参照** | 要件書 897・902 と同じ条項の ADR 側の再掲 |
| 248 | fail / 許さない | D-11 ③ 3 層の登録漏れ検出(要求集合と実行集合の差) | **明示参照** | **plan.md §4-3 が強制点として列挙**。未達の測定と直結する |
| 252 | fail | D-11 ③ 方式の確定と後続への委任の記述 | **非波及** | **委任の記録**であり判定ではない |
| 254 | 禁止 / 失敗させ | D-11 ③ 判定不能は合格にしない・CI を失敗させて人が判断する | **明示参照** | **型 4 の典拠**(145 と対) |
| 263 / 270 / 283 / 286 | fail ×3 / 禁止 | D-6 契約の発効・宣言のない除外・schema 不一致・数値規約 | **非波及** ×4 | **契約の形式と発効の規定**。移行状態は契約の形式を変えない。263 の「発効」は**契約ごとの発効**であり移行状態の発効(`BOOT-ACTIVATION`)とは別概念 |
| 297 | fail | D-12 `schemaVersion` 不一致を fail | **非波及** | **契約のバージョニング**。検査基盤の充足判定に触れない |
| **310** | マージ不可 | **D-7 網羅的な正解ベクタ = PR 必須 / マージ不可** | **直接改訂** | **D-7 の 6 行すべてに移行状態の適用関係を併記する**(§9-3) |
| **311** | マージ不可 | **D-7 差分テスト** | **直接改訂** | 同上。**本行は射程語をひとつも含まず、主題フィルタでは脱落した**(§2-1 の第 2 版の欠陥) |
| **312** | マージ不可 | **D-7 プロパティテスト** | **直接改訂** | 同上 |
| **313** | マージ不可 / fail | **D-7 変異テスト** | **直接改訂** | 同上 |
| **314** | マージ不可 | **D-7 (b)① 乖離検出** | **直接改訂** | 同上。**ただし (b)① は型 2(違反)の検出**であり、併記は「**移行状態でもマージ不可**」となる |
| **315** | マージ不可 / 満たしたとはしない | **D-7 (b)③ 構成の完全性** | **直接改訂** | **plan.md 射程⑥が名指しした行**(§9-3 の併記文がこの行に入る) |
| 323 | fail | D-8 formatter を経由しない表示経路を fail | **非波及** | **型 2(違反)の検出** |
| 361 | マージ不可 | D-10 付録A 契約の実装完了の判定基準(D-7 への配線) | **明示参照** | **D-7 を参照する条項**であり、D-7 側の併記で意味が変わる。**本行は無変更のまま、D-7 の改訂が効く**ことを確認する |
| 393 | fail | 帰結「FR-022 の列定義で比較面の fail-closed が解ける」 | **非波及** | **過去の改訂の記録** |

**`ADR-003` の内訳**: 直接改訂 **6** / 明示参照 **15** / 非波及 **21** = **42**(未分類 **0**)。

#### 2-6-3. 設計書 10.1(14 件)

| 行 | 強制語 | 規範の要旨 | 分類 | 根拠 |
| --- | --- | --- | --- | --- |
| 694 | fail | `backend` ジョブの DSN 未設定・DB テスト 0 件は fail | **非波及** | **ジョブの内部条件**。本改訂はジョブ名・個数・内部条件を変えない |
| 702 | 禁止 | `nfr021-append-only` の逆順の禁止 | **非波及** | **`NFR-021` 受入証跡の機械化**。検査基盤の充足判定に触れない |
| 719 | fail | DB 障害時も `backend` 全体を fail させる | **非波及** | 694 と同じ(実装追随の記録) |
| 735 / 738 / 739 / 740 / 741 / 742 / 745 / 746 / 747 / 749 / 752 | fail / 不合格 / 禁止 / 認めない / 満たさない(計 11 件) | **`NFR-021` 受入ゲート**(実施順序・証跡・排他と採番・失効・保存先・残余リスク) | **非波及** ×11 | **人手の受入手続**であり、`NFR-018` の検査機構に触れない。**10.1 の射程内にあるが、`NFR-021` 受入ゲートは独立した小節**である |

**設計書 10.1 の内訳**: 直接改訂 **0** / 明示参照 **0** / 非波及 **14** = **14**(未分類 **0**)。

> **注意**: **設計書 10.1 への追記(§9-4)は、強制語のヒットが無い箇所へ新設する。**
> 10.1 の箇条書き「**CI 全ジョブ green をマージ条件にする(10.2)— 要件書 7.3 / NFR-019 の直接要求**」と
> `consistency` ジョブの行(**強制語を含まない**)が対象である。
> → **強制語による走査だけでは、この 2 箇所を拾えない。** plan.md §4-3 が
> 「`consistency` の定義」「CI 全ジョブ green の位置づけ」を**強制点として明示的に列挙していた**ため
> 補足できた。**走査基準の限界として §2-7 に記録する。**

#### 2-6-4. 全数の突合

| 文書 | 直接改訂 | 明示参照 | 非波及 | 合計 | 未分類 |
| --- | --- | --- | --- | --- | --- |
| 要件書 | **3** | **5** | 15 | **23** | **0** |
| `ADR-003` | **6** | **15** | 21 | **42** | **0** |
| 設計書 10.1 | 0 | 0 | 14 | **14** | **0** |
| **合計** | **9** | **20** | **50** | **79** | **0** |

**加えて、強制語を含まないが plan.md §4-3 が列挙した強制点 2 件**(設計書 10.1 の
`consistency` 定義・CI 全ジョブ green の位置づけ)を **直接改訂**へ追加する。

→ **直接改訂の総数 = 11**(要件書 3 + `ADR-003` 6 + 設計書 2)。

### 2-7. 走査基準の限界(記録)

**強制語による走査は「判定の帰結を明示した箇所」しか拾わない。**
**定義だけを置いている箇所**(設計書 10.1 の `consistency` 行 — 「ゴールデンベクタ一致性テスト
(`NFR-019a`: 同一入力列 → クライアント/サーバー同一状況)」)は、**強制語を含まないが、
その定義が変わればマージ条件の意味が変わる。**

→ **走査は 2 系統で行う**:
**①強制語による機械走査**(§2-2)**②plan.md §4-3 が列挙した既知の強制点との突合**。
**②で拾えて①で拾えなかったものが 2 件あった。**

**ステップ 3 の検証では、両系統の結果を突合する**(片方だけで閉じない — plan.md §4-2 の契約 5)。

---

## 12. 条項 ID 対応表

**§8-4 が定めた必須列で作る。`TSK-235` はこの表から依拠宣言を機械生成できる。**

### 12-1. 範囲の切り出し規則(2 周目 指摘 8・3 周目 指摘 9 の是正)

**要件書 `NFR-018` (e) の `BOOT-*` 条項は、すべて同一の箇条書き行の中にある**(Markdown 見出しではない)。

**条項 ID マーカー**を次のとおり定める:

```
**`BOOT-XXX`（見出し語）**:
```

| 境界 | 規則 |
| --- | --- |
| **開始** | **当該条項のマーカーの直後**(マーカー自身を digest に含めない — ID と見出し語は対応表が別に保持しており、二重に digest へ入れると**見出し語の表現調整で digest が動く**) |
| **終端(最終条項以外)** | **次の条項のマーカーの直前** |
| **終端(最終条項)** | **`(e)` の箇条書き行の末尾** |

**開始側が対応表と食い違っていた**(3 周目 指摘 9): §12-1 は「マーカーの先頭」、対応表は「見出し全体の直後」と
書いており、**見出しを digest に含める実装と含めない実装で値が変わった**。→ **マーカーの直後(含めない)に統一。**

**最終条項の終端**は **`(e)` の箇条書き行の末尾**とする(2 周目 指摘 8)。
**`NFR-018` 全体の末尾ではない** — 測定方法・正解ベクタ・申し送りの各欄を含めると、
**それらの改訂で最終条項の digest が動いてしまう**。

**この規則は `TSK-235` の照合機構が実装する。本表はその入力である。**

**この表の `一行の要旨` 列は人間向けであり、本機構の検査の入力にしない**
(要旨方式は `TSK-235` が §12-1-b-1 で却下済み — 更新が気づき依存になるため。
**明記が無いと後から誰かが検査へ配線して同じ問題が戻る**)。

| 条項 ID | 規範の所在 | 範囲の切り出し規則 | 版 | status | superseded_by | kind | 一行の要旨(検査入力にしない) |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `BOOT-SEAL` | 要件書 `NFR-018` (e) | §12-1(同一箇条内・マーカーの直後から次のマーカーの直前まで) | 2.9 | **in-review** | — | **規範** | 封印集合 — 免除の唯一の根拠。性質による分類を用いない |
| `BOOT-STATE` | 要件書 `NFR-018` (e) | §12-1(同一箇条内・マーカーの直後から次のマーカーの直前まで) | 2.9 | **in-review** | — | **規範** | 移行状態を二軸で定める |
| `BOOT-PROGRAM` | 要件書 `NFR-018` (e) | §12-1(同一箇条内・マーカーの直後から次のマーカーの直前まで) | 2.9 | **in-review** | — | **規範** | 軸①の継続・終了・不可逆な昇格・解消の要件 |
| `BOOT-GRANT` | 要件書 `NFR-018` (e) | §12-1(同一箇条内・マーカーの直後から次のマーカーの直前まで) | 2.9 | **in-review** | — | **規範** | 軸②の授権範囲・失効・終了 |
| `BOOT-REAPPROVAL` | 要件書 `NFR-018` (e) | §12-1(同一箇条内・マーカーの直後から次のマーカーの直前まで) | 2.9 | **in-review** | — | **規範** | 再承認(失効後の承認に限る・単回性・専用 PR・昇格後の禁止) |
| `BOOT-NO-CLAIM` | 要件書 `NFR-018` (e) | §12-1(同一箇条内・マーカーの直後から次のマーカーの直前まで) | 2.9 | **in-review** | — | **規範** | 移行状態では (b)② / `NFR-019(a)` を満たしたと判定できない |
| `BOOT-REPORT` | 要件書 `NFR-018` (e) | §12-1(同一箇条内・マーカーの直後から次のマーカーの直前まで) | 2.9 | **in-review** | — | **規範** | 未解消レポートの出力義務 |
| `BOOT-SEAL-BASE` | 要件書 `NFR-018` (e) | §12-1(同一箇条内・マーカーの直後から次のマーカーの直前まで) | 2.9 | **in-review** | — | **規範** | 比較元は固定 commit OID(merge-base 不可) |
| `BOOT-SEAL-IMMUTABLE` | 要件書 `NFR-018` (e) | §12-1(同一箇条内・マーカーの直後から次のマーカーの直前まで) | 2.9 | **in-review** | — | **規範** | 比較元は当該 PR から変更不能 |
| `BOOT-SEAL-MONOTONE` | 要件書 `NFR-018` (e) | §12-1(同一箇条内・マーカーの直後から次のマーカーの直前まで) | 2.9 | **in-review** | — | **規範** | 追加・再発・削除は不合格 / 解消済みは単調 |
| `BOOT-STALL` | 要件書 `NFR-018` (e) | §12-1(同一箇条内・マーカーの直後から次のマーカーの直前まで) | 2.9 | **in-review** | — | **規範** | 停滞エポックと 50 マージ・マージ回数の母集合 |
| `BOOT-ACTIVATION` | 要件書 `NFR-018` (e) | §12-1(同一箇条内・マーカーの直後から次のマーカーの直前まで) | 2.9 | **in-review** | — | **規範** | 発効(機構と負例の同一 PR 接続・「同一の変更」= PR) |
| `BOOT-ADR-STAGE` | `ADR-003` D-13 | 見出し `### D-13:` から次の `###` まで | 0.3 | **in-review** | — | **補足** | 移行状態は帰結 1 の段階 1〜2 にまたがる(**D-13 は規範を持たない** — §10-2。**`TSK-235` はこの ID に依拠してはならない**) |
| `BOOT-ADR-TRIGGER` | `ADR-003` 見直しトリガー | 当該箇条 1 項 | 0.3 | **in-review** | — | **規範** | 見直しトリガー「移行状態が機能しないとき」 |
| `BOOT-CI-MEANING` | 設計書 10.1 | 当該箇条 1 項(表を含む) | 1.15 | **in-review** | — | **規範** | 段階ゲートの green / `NFR-019(a)` の充足 / 全体適合の関係 |
| `BOOT-ID-NORMALIZE` | **本書 §8-3** | — | — | — | — | **補足** | digest 計算前の正規化規約(**正本に置かない** — §10-3) |

**一意性の機械検査**(§8-4 の不変条件): **本表の条項 ID に重複がないことを検査する。**
2026-09-13 時点で **16 行・重複 0**(実測。射程縮小により 18 → 16)。

**`kind` が `補足` の行への依拠は fail** とする(`TSK-235` の要求)。
**`kind` が `補足` の行は 2 件**(`BOOT-ADR-STAGE` と `BOOT-ID-NORMALIZE`)。**いずれも依拠先にできない** — 前者は **D-13 が規範を持たない**ため(§10-2)、後者は**正本に着地しない**ため(§10-3)。

**status について**: 本表は**全行が `in-review`** である。
**`TSK-235` の照合機構は `approved` 版に限って依拠する**ため、
**ステップ 6 の approved 化までは先方は本改訂の条項へ依拠できない。**
approved 化の時点で `版` と `status` を更新し、**先方が digest を pin する**。


### 2-8. 改訂後の再走査(ステップ 3・4 完了時点・2026-09-13)

**§2-5 の取り直し規約に従い、改訂後に走査を再実行した。**

| 射程節 | 改訂前 | 改訂後 | 差 | 説明 |
| --- | --- | --- | --- | --- |
| 要件書 `NFR-018` | 14 | **15** | **+1** | **新設した (e) 自身が強制語を含む**(「不合格として扱う」「不合格とする」ほか)。**意図した増加** |
| 要件書 `NFR-019` | 2 | 2 | 0 | (a) への追記は強制語を含まない(「合格を意味しない」) |
| `ADR-003` D-7 | 6 | 6 | 0 | **6 行への併記は既存行の内側**であり、行数もヒット数も変わらない |
| **`ADR-003` D-13(新設)** | — | **0** | — | **下記参照** |
| `ADR-003` 見直しトリガー | 0 | 0 | 0 | 追加した 1 件は強制語を含まない |

**`D-13` のヒットが 0 件であることは、§10-2 の主張と整合する。** ただし**証明ではない**。

**当初ここに「規範を持つ条項は必ず強制語を含む。したがって『規範を持たない』は強制語の不在で
検証できる」と書いたが、これは誤りである**(確定ゲート 初周 指摘 8)。
**§2-7 が記録したとおり、強制語を含まない規範点が実際に 2 件あった**(設計書 10.1 の
`consistency` 定義・CI 全ジョブ green の位置づけ)。**「規範 → 強制語を含む」が偽である以上、
その対偶も使えない。**

→ **強制語の不在は「規範を持たない」ことの必要条件ですらない。**
**D-13 が規範を持たないことの根拠は §10-2 の検証可能な境界**
(**条項参照を外すと規範として読めなくなること**)**のみ**であり、
**本節のヒット 0 件はそれと矛盾しないという以上のことを示さない。**

### 2-9. 走査の完全性について(確定ゲート 初周 指摘 8 の是正)

**§2-6-4 の「79 件・未分類 0」は、候補集合の全数性を示さない。**
示しているのは**取得済み候補の分類が完了したこと**のみである。

**既知の限界を 3 つ明記する**:

| # | 限界 | 実測 |
| --- | --- | --- |
| 1 | **行単位の走査であり、条項単位ではない** | 要件書 `NFR-018` (e) は**同一行に 14 条項**を持つが、走査上は **1 件**と数えられる |
| 2 | **強制語を含まない規範点を取りこぼす** | **実際に 2 件取りこぼし**、計画書 §4-3 の既知一覧から手動で回収した(§2-7) |
| 3 | **走査は 2 系統の併用でしか閉じない** | ①強制語による機械走査 ②計画書 §4-3 の既知リストとの突合。**①だけでは閉じない**ことが②で実証された |

**したがって本書は「全数を走査した」とは主張しない。**
**主張するのは「2 系統で拾った候補をすべて分類し、未分類を残していない」ことである。**
**候補集合の全数性は、ステップ 6 の敵対レビューが全文を読むことで補う**
(設計書 7.3-4 — 差分外の欠陥クラスは全文レビューの周が受け持つ)。

**分類表(§2-6)は改訂前の既存強制点を対象とするため更新しない。**
**新設条項 (e) は本改訂が作ったものであり、分類の対象(既存の強制点)ではない。**
**ステップ 6 以降で条文が変わった場合は、本節を再度取り直す。**
