---
feature: phase4-6-acceptance
status: active            # active | in-review(/pr が PR 内で更新。完了は PR 状態・Notion・worktree 除去から導出。codex_run.py implement は active 以外を拒否)
承認: 済(2026-08-26・山田正輝 — 前提 PR へ再スコープ)  # 未 | 済(YYYY-MM-DD・承認者)— codex_run.py が「済」でないと実行を拒否する
重さ分類: 通常            # 軽微 | 通常 | コア領域 | 機械的軽作業(ADR-001 のモデルをラッパーが自動選択)
worktree: ../../..        # worktree ルート(plan.md からの相対 or 絶対)。/task-start が設定
notion: https://app.notion.com/p/3c793b75e68781cb8aa1d50dc7403237
branch: feature/phase4-6-acceptance
created: 2026-08-25
計画レビュー周回: 4        # 指摘反映を伴うレビュー 1 周ごとに +1(収束確認周は数えない。/plan が更新)
確定ゲート周回: 2          # 指摘反映を伴う敵対レビュー 1 周ごとに +1(同前。/finalize-doc が更新)
実行方式: 通常             # 通常 | fast(fast path 適用時に fast へ — 人間の事前 OK 必須。現在地導出が識別)
反映周コミット: 適用       # 適用 | 規約制定前(必須・既定値なし。確定ゲートの反映周コミット突合の適用境界 — 設計書 6.1)
---

# 実装計画書: Phase 4 完了時受入の前提 PR(第 2 号) — 設計書 v1.10 + onboarding v1.1

> **本タスクは再スコープされた。** 当初は「Phase 4-6(win-setup 選定 + 受入判定)」として起票したが、**確定ゲート 2 周目の P0** により、正本の改訂と受入判定を同一 PR で行うと前提と結論が循環することが判明した(下記 1 節)。本タスクは**前提 PR(第 2 号)まで**を担い、**4-6 の受入判定は別タスク・別ブランチ**で行う(PO 決定 2026-08-26)。
>
> **slug(`phase4-6-acceptance`)と branch 名は `/task-start` 時点の名称を引き継いでいる**(再スコープ前の命名)。混乱を避けるため、4-6 の受入タスクは別の slug で起票する。

## 1. 背景・目的

- **Notion タスク**: [TSK-255](https://app.notion.com/p/3c793b75e68781cb8aa1d50dc7403237)
- **要件**: [NFR-021 開発環境](../../requirements/requirements-pitchlog-2026-07-22.md)(`:915-923`)/ 同 10 章 未決事項「NFR-021 の継続検証基盤」(`:1037`)/ 8 章 判定者(`:1007`)
- **設計**: [ハーネス設計書](../../development/dev-harness-design-2026-08-07.md) 13 章「Phase 4 完了時受入の前提 PR(第 2 号)」(本 PR で新設)/ 10.1「NFR-021 受入ゲート」

**なぜ前提 PR として切り出すか。** 10.1 の実施順序 ① は「**`develop` に統合済みの approved 版**の `onboarding.md` で受入を完走する」ことを要求し、v1.9 は**判定単位を `develop` の状態**と定めている(feature ブランチの中間コミットではない)。本 PR が改訂する `onboarding.md` を 4-6 と同一 PR で approved 化し、その PR の `T` で受入すると、**v1.9 が「前提と結論が循環する」として閉じたのと同じ経路**が再発する。**検証器は `T` 内の blob と `status: approved` は検査するが、その版が受入前に `develop` へ統合済みだったかは検査しない**ため、機械検証を通っても Must を満たさない穴が残る。

**なぜ 13 章の改訂を伴うか。** v1.9 の歯止めは「**本例外を他の前提へ類推適用してはならず、別の前提をスロット外へ置く場合は本書の版繰り上げと 7.3 の確定ゲートで対象・順序・マージ条件・本数を改めて確定する**」と定める。本 PR はまさにその手続に従って第 2 号を定義する(類推適用ではない)。

下調べの結論は [調査メモ](research.md) を正とする(本書は複製せず参照する — 設計書 7.1-1)。**ただし同メモの E 節・F-1 は本再スコープ以前の 1 PR 構成を前提としており、現行の構成は本書 4 節が正**である。

## 2. スコープ

### やること

1. **win-setup ランナー選定の確定と、その記録**(設計書 10.1)。**選定結果は (d) = GitHub-hosted ランナーを採用せず、WSL 固有の継続検証は要件書 NFR-021 が定める 2 時点の手動再現に一本化する**(PO 決定 2026-08-25 — research.md G-1)
2. **13 章へ「Phase 4 完了時受入の前提 PR(第 2 号)」を新設**し、対象・順序・マージ条件・本数(7 本 → 8 本)を確定する
3. **`onboarding.md` v1.1** — 2 章へ「NFR-021 の受入判定を実施する場合」の節を新設(候補コミットの取得手順)
4. **受入 README v1.2** — ブートストラップ手順を前提 PR 第 2 号へ対応させ、**onboarding の版の焼き込みを撤去**する
5. **台帳への follow-up 登録**(self-hosted runner の再検討経路 / scheduled workflow の制約)+ 索引の現行化
6. **通常 PR 要件でマージする**(受入判定は課さない)
7. **4-6 の受入判定タスクを別途起票**し、本タスクと相互リンクする

### やらないこと

| # | 除外するもの | 理由 |
| --- | --- | --- |
| 1 | **NFR-021 の受入の実施・結果証跡・検証器の実行・排他区間・マージ後検査** | **すべて 4-6 のタスクの責務**。本 PR が `develop` へ統合されてから開始する(10.1 実施順序 ①) |
| 2 | **`win-setup` の `ci.yml` への実装** | 選定結果が (d) = GitHub-hosted 不採用のため、載せるジョブが無い。**選定の後送りではない**(設計書 `:810` が禁じるのは**選定**の後送り) |
| 3 | **`/release` 手順 0 の無条件中断の解除** | `P4-後` の責務 |
| 4 | **13 章の現況追随・Phase 4 完了証跡の記録** | `P4-後` の責務(設計書 `:835`・`:811`)。**本 PR が改訂するのは 13 章の前提 PR 規定・順序・本数であって、Phase 4 の完了記録ではない** |
| 5 | **要件書 10 章 未決事項行の決着記録** | 3 節で理由を述べる。**別タスクへ切り出す** |
| 6 | **論理スロットと Notion タスクの対応の機械化 / マージ後検査の新しいキー・媒体 / 再試行タスクの初期化項目の追加** | v1.9 で「増やさない」と確定済み。**再提案にあたる**(設計書 `:829-832`) |
| 7 | **プロダクト実装(backend / frontend / contracts)** | 前提 PR の適用範囲外(1 変更集合に限る — 13 章の歯止め) |

## 3. 影響する正本

| 正本 | 変更内容 | ゲート(PRレビュー / finalize-doc) |
| --- | --- | --- |
| [ハーネス設計書](../../development/dev-harness-design-2026-08-07.md) | **10.1**: CI ジョブ表の `win-setup` 行を**不採用**へ / 「CI との関係」行を恒常の規定へ / **「NFR-021 の継続検証基盤の選定結果」を新設** / **実施順序 ① の版焼き込みを撤去** / ブートストラップ経路へ第 2 号を追加。**13 章**: 4-6 行の根拠を現況化(+ H-69 (c) の実施主体を明記)/ **「前提 PR(第 2 号)」節を新設** / 順序へ第 2 号を挿入 / 本数を 7 → 8 本。変更履歴表へ追記。**版繰り上げ v1.9 → v1.10** | **/finalize-doc** |
| [オンボーディング](../../development/onboarding.md) | **2 章へ「NFR-021 の受入判定を実施する場合」の節を新設**(候補コミットの取得手順・ブランチ名を `develop` に保つ必須理由)。**版繰り上げ v1.0 → v1.1** | **/finalize-doc**(設計書と**同一ゲート**) |
| [NFR-021 受入証跡の運用](../../ops/nfr021-acceptance/README.md) | ブートストラップ手順 5 を**第 1 号 → 第 2 号の順**へ改め、**onboarding の版の焼き込みを撤去**する。**版繰り上げ v1.1 → v1.2** | **/finalize-doc**(同上) |
| [ハーネス運用評価台帳](../../development/harness-evaluation.md) | follow-up を 2 件追記 — ① self-hosted runner の再検討経路 ② scheduled workflow は default branch でしか走らず `main` に検証対象が無いという制約 | **PR レビュー**(`H-*` の追記では版を上げない — 7.6-3 前段) |
| [docs/README.md](../../README.md)(索引) | 上記 3 正本の行を **① 起案時に `in-review`・新版・日付** → **② `/finalize-doc` の承認後に `approved`・最終日付**の 2 段階で更新 + **台帳行の最終更新日**を現行化 | **①②は /finalize-doc と一体** / 台帳行は **PR レビュー** |
| [要件定義書](../../requirements/requirements-pitchlog-2026-07-22.md) | **反映なし** | — (理由は下記) |
| [ADR-001](../../adr/ADR-001-codex-model-selection.md) / [ADR-002](../../adr/ADR-002-frontend-vue.md) / [ADR-003](../../adr/ADR-003-domain-calc-method.md) | **反映なし** | — |
| [改善台帳](../../improvements-from-baseball-scoring.md) | **反映なし** | — |
| [GitHub リポジトリ設定手順](../../development/github-setup.md) | **反映なし** | — |
| [NFR-021 受入証跡のテンプレート 3 種](../../ops/nfr021-acceptance/reservation-template.md) | **反映なし** | — |

### 設計書を「版繰り上げ + 確定ゲート」と判定した根拠

**当初は「実装追随(版は上げない)」と判定していたが、計画レビュー 1 周目の指摘を受けて改めた。**

- 当初の根拠は前例 3 件(v1.8 の `nfr021-append-only` 行**追加**、v1.8 の `harness` 行**現行化**、v1.3 の `codex-plan-status-guard`)だったが、**いずれも「確定済み規範の実装・現行化」であり本件とは性質が違う**
- 本件は「CI の定期ジョブ候補」から「**GitHub-hosted を採らず手動運用へ一本化**」への変更であり、**当該行の意味と実行主体そのものを変える**。さらに 13 章の前提 PR・順序・本数という**構造規定を変える**
- **PO 裁定(2026-08-25)**: 版繰り上げ + `/finalize-doc`

### 選定結果の記載内容(10.1 に一意化して書く)

| 項目 | 記載内容 |
| --- | --- |
| ランナー | **GitHub-hosted は採用しない**(Windows Server 2025 であって受入プロファイル〔Windows 11 x64〕の再現にならず、入れ子仮想化は公式サポート外、**hosted runner 上で `Ubuntu-26.04` をサポート済み・再現確認済みとして導入する経路を確認できない**) |
| 継続検証の媒体 | **`docs/development/onboarding.md` の 0〜8 章そのもの**(別のチェックリスト文書を新設しない) |
| 実施者 | **要件書 8 章の判定者**(システム管理者 = プロダクトオーナー) |
| 時点 | **要件書 NFR-021 が定める 2 時点**(Phase 4 完了時・リリース候補時)。**それ以外の定期検証は置かない** |
| self-hosted runner | **将来の選択肢として台帳へ送る**(本タスクでは採用しない) |

### 要件書 10 章を本 PR から外した理由

1. **7.6-3 が「要件改訂」を確定ゲート側に明示列挙している**(`:425`)。要件書 v1.9 で当該行を**追加**したときも版繰り上げ + 確定ゲートを通しており、**閉じる側だけ軽いゲートで通すのは非対称**である
2. **13 章の前提 PR の歯止めは「1 変更集合に限る」**と定めており、要件改訂を抱き合わせると適用範囲を超える
3. **決定そのものは設計書 10.1 に記録されるため失われない**

**残余リスク**: 本 PR のマージ後、要件書 10 章は「未決・暫定=手動再現」のまま残り、設計書 10.1 は「決定済み」となって**一時的に併存する**。**この間は要件書側の暫定方針が上位である**ことを設計書本文へ明記した(確定ゲート 1 周目 P1-2 の反映)。**別タスクを起票し、`P4-後` の着手前提として明記する**ことを DoD に含める。

### 正本体系外だが同一 PR で更新するもの(別枠)

| パス | 内容 | 備考 |
| --- | --- | --- |
| `docs/features/phase4-6-acceptance/plan.md` | 本計画書 | feature 作業ディレクトリ(一時) |
| `docs/features/phase4-6-acceptance/research.md` | 調査メモ | 同上 |
| `docs/worklog/2026-08-25-phase4-6-acceptance.md` | 作業ログ | |

> **本 PR は受入証跡を含まないため、台帳 H-72(受入証跡で `/pr` が中断する)を踏まない。** H-72 の対処は 4-6 のタスクの計画書で行う。

## 4. 実装方針

**前提となる調査**: [調査メモ](research.md)。**ただし同メモの E 節「実行順序の制約」と F-1 は本再スコープ以前の 1 PR 構成を前提としており、現行の構成は本節が正**である(調査時点の記録として残す)。

### 重さ分類の根拠 = 通常

- **コア領域に触れない。** CLAUDE.md が列挙する 5 領域のいずれにも該当せず、プロダクトのコードを 1 行も変更しない
- **`.claude/core-areas.json` の `guard_paths` にも触れない**(同ファイルが列挙するのは `core-areas.json` / `ci.yml` / `core_guard.py` / PR テンプレ / `/pr`・`/release`・`/task-done` の各 SKILL.md)。`ci.yml` を触らない判断(2 節「やらないこと」2)がここでも効く
- **軽微ではない。** 正本 3 本の版繰り上げと確定ゲートを含み、13 章の構造規定を変える

### 本 PR の位置づけと、4-6 との接続

```
4-5(完了) → 前提 PR 第 1 号(onboarding v1.0 — TSK-253・完了)
          → 【本 PR】前提 PR 第 2 号(設計書 v1.10 + onboarding v1.1 + 受入 README v1.2)
          → 4-6(別タスク・別ブランチ: 受入判定のみ)
          → マージ後検査 → P4-後
```

- **本 PR のマージ条件は通常 PR 要件 + 本 PR に含まれる確定ゲート**。**`gate_kind: phase4` の判定は課さない**(受入を実施するのは 4-6 だけ)
- **本 PR がマージされた後の `develop`** が、4-6 の `T`(試験対象コミット)の候補になる。**`T` の確定は 4-6 のタスクで行う**
- **4-6 側は `onboarding.md` v1.1 の 2 章 2-1 節に従い、候補コミットを OID で固定して取得する。** 前提 PR のマージから実機受入までは日単位で空きうるため、`develop` の先端をそのまま使うと**別のツリーを試験してしまう**

### 実装ステップ(コミット単位 — 設計書 6.1 段階実装)

**確定ゲートの反映は本表に含めない**(反映周コミットには実装ステップ番号を割り当てない — `/finalize-doc`)。

| # | ステップ(何を作るか) | 合格条件(このステップの検証方法) |
| --- | --- | --- |
| 1 | 設計書 10.1 の `win-setup` 行と「CI との関係」行を選定結果へ改訂し、「NFR-021 の継続検証基盤の選定結果」の箇条を追加、変更履歴表へ 1 行追記して **v1.10 を起案**する(frontmatter は `in-review`)。**同一コミットで `docs/README.md` の設計書行を `in-review`・版欄 `1.10`・日付へ更新する** | **10.1 の節内に限定した検査**(`awk '/^### 10\.1/,/^### 10\.2/'` の出力)に対し `ランナーは未決` / `ランナー未決` / `Phase 4 で選定` が **0 件**、かつ **`win-setup`(定期)というジョブ表記が残っていない** / 3 節「選定結果の記載内容」の 5 項目がすべて本文に現れる / 変更履歴表の末尾が v1.10 / `uv run python scripts/check_docs_status.py`・`uv run pytest tests/`・`uv run ruff check .`・`uv run ty check` がいずれも終了コード 0 |
| 2 | 台帳 `harness-evaluation.md` へ follow-up を 2 件追記し(① **self-hosted runner の再検討経路** — **再検討トリガー / 判断者 / 最低限の安全条件 / hosted と self-hosted のどちらを再検討するか の 4 項目を必ず持たせる**〔確定ゲート 1 周目 P2〕② scheduled workflow は default branch でしか走らず `main` に検証対象が無いという制約 ③ **`onboarding.md` の確定ゲート中はハーネスの pytest が赤になる** — `tests/test_verify_nfr021_evidence.py::test_real_repository_onboarding_is_approved` が**リポジトリ HEAD の同書が `approved` であること**を不変条件として検査するため、`in-review` の間は必ず失敗する〔実測〕。`/pr` は `/check` の成功を要求するので、**確定ゲートの承認・approved 化を完了するまで PR を出せない**という順序が機構的に強制される)、`docs/README.md` の**台帳行の最終更新日**を現行化する | 台帳の既存記法どおり ID・追跡優先度・典拠が埋まっている / **① の 4 項目がすべて記載されている** / **③ の観測に実測の典拠(テスト名とエラーメッセージ)が付いている** / 台帳の変更履歴表に 1 行追記され**版は上がっていない** / `check_docs_status.py`・`uv run pytest tests/` が終了コード 0 |

**変更履歴表と 13 章の歴史記述は検査対象にしない。** 「ランナーは未決」「Phase 4 で選定」は**過去の決定を記録した歴史的事実**として変更履歴表に残る。**歴史は消さない**(7.1-4)。是正対象は現行規範だけである。

**ステップ 1 の後に `/finalize-doc`(確定ゲート)を通す。** 反映周コミットには**ステップ記法を付けず** `反映<r>周目` だけを含める(設計書 6.1)。**承認後に 3 正本を `approved` 化し、変更履歴を追記し、索引を `approved`・日付へ現行化する**(同スキル手順 6)。

## 5. DoD(受け入れ基準)

- [ ] **win-setup ランナーの選定が確定し、設計書 10.1 に一意化して記録されている**(ランナー / 媒体 / 実施者 / 時点 / self-hosted の 5 項目)。10.1 の現行規範に「未決」表現が 1 件も残っていない
- [ ] **13 章に「Phase 4 完了時受入の前提 PR(第 2 号)」が新設され**、対象・順序・マージ条件・本数(**7 本 → 8 本**)・適用範囲の歯止めが定まっている
- [ ] **設計書が確定ゲートを通過し v1.10 approved になっている**
- [ ] **`onboarding.md` が同じ確定ゲートを通過し v1.1 approved になっている**(2 章に受入判定時の候補コミット取得手順が含まれる)
- [ ] **受入 README が同じ確定ゲートを通過し v1.2 approved になっている**(前提 PR 第 2 号への対応 + 版焼き込みの撤去)
- [ ] 台帳へ follow-up が 2 件登録され(① は 4 項目つき)、`docs/README.md` の索引が 4 行とも現行化されている
- [ ] **通常 PR 要件**(`/check` 全グリーン + CI + 反対側レビュー + 人間マージ)を満たしてマージした。**`gate_kind: phase4` の判定は課していない**
- [ ] **4-6 の受入判定タスクを別 slug で起票し**、本タスクと相互リンクした(DoD に「本 PR マージ後の `develop` を起点に `T` を確定し、`onboarding.md` v1.1 の 2 章 2-1 節に従って受入を完走する」ことを書く)
- [ ] **要件書 10 章 未決事項行の決着記録を別タスクとして起票し**、本タスクと相互リンクし、`P4-後` の着手前提として明記した

## 6. テスト計画

**NFR-019 のテスト種別へ新規に足すものは無い。** 本タスクはコードを追加せず、変更対象は正本ドキュメント(設計書・onboarding・受入 README・台帳・索引)に限られるため。

| 検証手段 | 対象 | 実行者 |
| --- | --- | --- |
| `uv run pytest tests/` / `uv run ruff check .` / `uv run ty check` / `check_docs_status.py` | ハーネス(既存テストの非退行)・正本 frontmatter の固定文法・索引との一致 | Claude(`/check`) |
| `codex_run.py review adversarial`(`/finalize-doc`) | 設計書 v1.10・onboarding v1.1・受入 README v1.2 の確定ゲート | Claude が実行し、**承認は PO** |
| `check_plan_docs_sync.py`(`/pr` 手順 2-2) | 3 節の宣言と差分の双方向突合 | Claude(`/pr`) |
| CI 全ジョブ | PR 全体(**文書のみの変更のため `frontend`・`backend` は skipped になりうる**。success 必須は `secrets` / `docs-lint` / `core-guard` / `harness` / `nfr021-append-only` / `frontend-changes` / `backend-changes`) | CI |

**NFR-021 の受入 5 項目は本 PR の対象外**である(実施するのは 4-6 のタスク)。
