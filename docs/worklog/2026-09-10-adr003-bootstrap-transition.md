---
date: 2026-09-10
topic: 検査基盤の導入期の移行状態を条文化する(TSK-355 — ADR-003 + 要件書)
branch: feature/adr003-bootstrap-transition
---

# 作業ログ: 2026-09-10 検査基盤の導入期の移行状態の条文化(TSK-355)

## やったこと

- `/task-start` — worktree を `origin/develop`(`4ed54fc`)から作成。計画書雛形と本ログを起票。

## 決定

- スラッグ = `adr003-bootstrap-transition`(前例 TSK-278 = `adr003-display-primitives` に倣う)。
- 重さ分類 = **コア領域**。要件書 `docs/requirements/requirements-pitchlog-2026-07-22.md` は
  `.claude/core-areas.json` の `game-state` / `sync-protocol` / `recording-rights` / `tenant-isolation` の
  paths に登録済み(実測)。ADR-003 自体は paths 未登録だが、要件書側で該当するためコア領域として扱う。
- PO 裁定(2026-09-10・山田正輝)で**甲 = 正本へ移行状態を明記する改訂**が確定済み。方式の再検討はしない。

- `/investigate` — 調査サブエージェント 3 本(spec-checker / decision-tracer / Explore)を並列で実行。
  あわせて発生源セッション `tsk235-dsl` へ設計意図を照会し、制約 6 件・閉じ条件 5 件の回答を得た。
  結果を `docs/features/adr003-bootstrap-transition/research.md`(864 行)へ統合。

## 調査で判明した重要事実(research.md が正)

- **矛盾の実体は「例外の不在」ではなく「段階 1 のマージ条件が未定義」**。ADR-003 の帰結 1 は着手順序 4 段階を持ち、
  段階 3 が「当該対象の全緑をマージ条件」と**対象単位で**切っているため、対象 0 件の段階 1 だけが未定義(research §7-3)。
- **触るアンカーは 6 つ**(要件書 = fail-closed 行 / 正解ベクタ欄の発効条件 / 10 章未決事項行、
  ADR-003 = D-7 (b)③ 行 / D-11 ③ 入口全列挙 / D-11 判定不能の扱い)。**1 ステップで同時に触らないと必ず追随漏れ**
  (TSK-278 の 27 周で最頻だった欠陥型 — 7 回。research §7-9)。
- **fail-closed に経過措置を認めた前例は無い**(全数確認)。本タスクが初めて(research §7-4)。
- **「無期限の緩和にしない」は日付なしで満たせる** — 要件書 8 章 DoD ② と NFR-021 リリース候補時が
  「NFR-018(b) の機械検査が成功していること」を要求しており、**移行状態はリリース判定前に必ず終端する**(research §6-5)。
- **D-7 に「(b)② 行」は存在しない**(前提の訂正)。(b)② は上 4 行が分担して担う(research §7-1)。
- **「充足宣言」という語は正本に 0 件**。既存は「満たした / 合格と判定してはならない」の否定形(research §6-4)。
- **既存 approved の ADR へ後から D-xx を足した前例は無い**(research §7-5)。

## 未決・次の一歩

- **人間の判断を仰ぐ論点 5 件**を計画書に載せる(research の「未解決・申し送り」— 許容期間の書き方 /
  D-13 新設の可否 / 射程に発効条件を含めるか / 語彙 / 10 章の扱い)。**私は確定しない。**
- **`/plan` で停止した(機構による)。** `.claude/skills/plan/SKILL.md` は frontmatter に
  `disable-model-invocation: true` を持つため、**Claude 側から起動できない**(「人が都度判断する」ためのゲート)。
  人間の指示は「`/plan` の敵対レビューまで進めて承認待ちで止める」であったが、
  **スキルのワークフローを別手段で再現することは明示的に禁止**されているため再現しない。
  → **人間が `/plan adr003-bootstrap-transition` を打った時点で、調査済みの状態から即座に起草へ入れる。**

## 計画書の敵対レビュー 1 周目(2026-09-10・sol xhigh・**否決**)

`codex_run.py review adversarial`。**判定: 否決 / P0 6 件・P1 3 件・P2 2 件 / 全件 起因**。
**指摘の典拠はすべて原典で裏取りした**(下表の「裏取り」列)。

### 採否記録

| # | 重大度 | 分類 | 要旨 | 裏取り | 採否 |
| --- | --- | --- | --- | --- | --- |
| 1 | **P0** | 起因 / (-) | **「未定義の空枠を埋める」という立て方は成立しない**。ADR-003 の段階 1 は作業順序を定めているだけで既存のマージ条件を解除していない。段階 1 にも NFR-018 の「未整備は不合格」と D-7 の「マージ不可」が既に適用される。「例外ではない」と書くと**優先関係が定義されない** | ADR-003 D-7 / 帰結 1・要件書 NFR-018 (b) を再読して確認 | **採用**。§1-2 と §4-2 契約 1 を撤回し「**充足判定は従来どおり不合格だが、閉じた移行状態に限りマージ可否を別判定する経過規定**」へ改める |
| 2 | **P0** | 起因 / (A) | **「段階 1 に限る」と終了条件が両立しない**。未達 0 には全 13 対象の登録が必要で、それは段階 2 完了後・段階 4 の直前 | `tsk235-dsl` の回答(research §5-3「TSK-235〜239 では終わらない。段階 4 の直前まで続く」)と一致 | **採用**。段階限定を撤回し**状態ベース**(未達 0 かつ債務 0)へ。段階 2 の各 PR の扱いは `tsk235-dsl` へ照会中 |
| 3 | **P0** | 起因 / (-) | **6 アンカーは全数ではない**。research の列挙とも一致せず和集合で 7 箇所。さらに NFR-018(b)②「1 層でも欠ければ満たさない」/ NFR-019 柱書 / D-7 の他 4 行 / D-11 の変異運用規則・表外既定の表示対応も強制点 | 指摘のとおり。私が「6」と固定数を先に置いたのが誤り | **採用**。固定数をやめ、**全強制点の exact-set 表**(直接改訂 / 明示参照 / 非波及)を作る |
| 4 | **P0** | 起因 / (A) | **設計書 10.1 に実際に波及する**。10.1 は `consistency` を「**NFR-019a: 同一入力列 → クライアント/サーバー同一状況**」と定義しており、ジョブ名・個数が不変でも**ジョブ状態の意味が変わる**ため「確認のみ」では閉じない | `dev-harness-design-2026-08-07.md` の 10.1 表で逐語確認。**指摘は正しい** | **採用**(方向)。ただし**射程の拡大にあたるため PO 裁定が必要** — §4-9 へ記録し**人間へ提示** |
| 5 | **P0** | 起因 / (-) | **`contracts/authz/**` の 3 段手順では完了できない**。実際は **7 段**で、oracle の `oracle_commit` 更新と**人間査読**が必須。`source_id` が位置依存なので「足した行だけ」では足りない。**1 ステップ = 1 コミットのステップ 7 では既存コミットを参照する seal を作れない** | **H-85 が実在**(`harness-evaluation.md`)。候補①に「**実際に必要なのは 7 段階・うち 3 つが手作業のフィールド更新**」、TSK-278 実測に「**入力確定コミット → oracle_commit 差し替え〔7 ファイル〕→ 人間確認 → reseal の 2 段コミットが機構上必須**」と明記 | **採用**。§3 を全数列挙へ、ステップ 7 を**2 ステップに分割** |
| 6 | **P0** | 起因 / (-) | **「封印」だけでは追加禁止を迂回できる**。比較元が同じ PR の HEAD にあると台帳・digest・検査の同時更新で通る。**比較基準を変更中の PR から独立させる規範が計画書に無い** | research §5-5(`tsk235-dsl` からの制約)に同内容があるが**計画書へ落とせていなかった**。指摘は正しい | **採用**。正本へ 4 点(比較元の固定・当該 PR から変更不能・復活は fail・**対象は D-11 が要求する宣言のうち開始時点で未宣言のものすべて**)を規律として書く |
| 7 | **P1** | 起因 / (-) | **green の書き分けに発効条件が無い**。`manifest.json`・契約・`consistency` の実装が無いまま文書の approved 化だけで移行状態が発効すると、**機構が無いのにマージ許可だけ先に生じる** | research §6-8 の実測(NFR-018/019 の機械検査は CI に 0 本)と整合。指摘は正しい | **採用**。**発効原子性**を正本で拘束する(移行判定器・未達レポート・負例が CI の必須経路へ接続されるまでは「定義済みだが未発効」) |
| 8 | **P1** | 起因 / (A) | **単調性 4 条件は終了を保証しない**。「追加されない」と「必ず減る」は別。**要件書 7.2 が「期限なし・品質優先」**と明記しているため、**リリースゲートはリリースを止めるだけで移行状態の時間的上限にならない** | 要件書 7.2 を逐語確認 — 「スケジュール: **期限なし・品質優先**」。**私の §6-5 の材料は反論された** | **採用**(方向)。ただし是正案(厳密減少義務 / 日付 / 停滞失効)の選択は**PO 裁定が必要** — §4-9 へ記録し人間へ提示。`tsk235-dsl` へ実装可能性を照会中 |
| 9 | **P1** | 起因 / (-) | **「充足宣言」を使うかが二重決定**。§4-2 で契約として確定しながら §4-8 論点 4 で未決に戻している | 指摘のとおり | **採用**。論点 4 を人間判断表から**削除**し、既存の否定形に固定 |
| 10 | **P2** | 起因 / (B) | **D-13 の配置判断を人間へ投げる必要はない**。編集構造の選択であり作成者が決めれば足りる | 指摘のとおり | **採用**。論点 2 を**削除**し、ステップ 2 で作成者が決めて design.md に選定理由を記録 |
| 11 | **P2** | 起因 / (B) | **v2.8 の説明に過去版の「飛ばし」が残っている**。現行 v2.7 の次は通常の繰り上げ | 指摘のとおり | **採用**。「v2.7 → v2.8」のみにする |

**不採用: 0 件。全 11 件を採用**(うち 2 件は方向を採用し、選択に PO 裁定を要する)。

### レビューが明示的に認めた点(次周で再度争わない)

> 対象集合 13 件を維持し、未達として全件出力し、`NFR-018(b)②` を満たしたと判定しないなら、
> **§4-4 論点 1 は D-4 の「対象範囲の縮小」禁止と両立できます。**

**「債務台帳に載せても対象からは外さない。未達として計上し続け、マージ可否の判定だけを分ける」**という
書き分けは通る。また「ステップ 3 を正本間の単一コミットにする原則」も妥当と認められた。
**改善台帳・同期プロトコル・データモデル・設計書 6.3 には直接改訂が不要**とも確認された。

### PO 裁定が必要になった論点 2 件(**勝手に裁定しない** — 人間へ提示して停止)

1. **設計書 10.1 の扱い**(指摘 4)— 射程に加えるか / 別タスクにして移行状態の発効を後続の approved まで禁止するか
2. **「無期限の緩和にしない」の実現方法**(指摘 8)— 進捗義務(`未達数＋債務数` の厳密減少)/ 日付 / 停滞失効

## 発生源セッション(TSK-235)への照会と回答(2026-09-10)

1 周目の指摘 2・6・8 は TSK-235 の設計に直接効くため照会した。**回答で 2 件の重要な訂正があった。**

### 訂正 1: **レビューの是正案 B-1 をそのまま条文化するとリポジトリが凍結する**

> **移行状態はリポジトリ全体の状態であって、PR ごとの許可ではありません。** (b)③ は「宣言集合と実在入口集合の差」で
> fail するので、**その PR が何を触ったかと無関係に fail します**。未宣言の入口が 1 つでも残っている限り、
> **文書 PR でも ORM の PR でも赤くなる** — これが 2 周目 P0-1 の「三すくみ」の実体です。

→ 「移行状態を利用する PR」= **事実上すべての PR**。全 PR に `未達数＋債務数` の厳密減少を課すと
**TSK-343 の ORM 作業も本タスクの文書 PR も TSK-317 の認可検証も止まる**。**paths filter でも切れない**
(失敗が PR の触った paths と無関係に起きるため)。

**代案(採用 — §4-9 論点 B の B-1')**: ①厳密減少は**段階 2 の PR にのみ**課す(段階 2 の PR は定義上
ちょうど 1 対象を登録し未達が 1 減るため空振りしない)②停滞は「時間」ではなく**「マージ本数」**で測る
(移行状態が有効なあいだのマージ累計が N 本に達し、そのあいだに `未達数＋債務数` が 1 件も減っていなければ失効)。
**日付を書かず・機械判定可能・凍結しない。** `N` の値は**人間の裁定事項**(TSK-235 側は段階 2 のタスクが
未起票のため決められないと回答)。

### 訂正 2: **封印の比較元は固定 SHA。merge-base は誤り**(TSK-235 が前便の自案を撤回)

> 私が merge-base と言ったのは **core-guard の「他領域 byte-for-byte 不変」の文脈**からの類推でした。
> …**`bootstrapDebt` は「開始時点の集合と比べて増えていないこと」を見る**ので、**比較元は動いてはいけません**。
> merge-base は base が進むと動くので、**この用途には不適**です。

### 確認できた点

- **移行状態は段階 1 と段階 2 にまたがる**(状態ベースの定義が正しい)。
  段階 2 の PR について新しく書くのは「**全体の未達集合が厳密減少すること**」だけ
  (「当該対象は全緑」は `ADR-003` 段階 3 が既に定めている)
- **条文の根拠は段階 3 の括弧書きに置ける** — 逐語「**未完成のあいだ適合と判定しないであって、
  実装を禁じるものではない**」。**移行状態の授権はこの一文の延長線上にある**
- **失効と自動昇格は衝突しない**(どちらも「厳しくなる」向き)。**緩くなるのは再承認だけ**なので、
  そこにのみ人間の明示承認を要求する(自動延長にしない)
- **「対象からは外さない。未達として計上し続ける」**が TSK-235 の設計意図そのものであることを確認

## PO 裁定(2026-09-11・山田正輝)

計画レビュー 1 周目で発生した論点 2 件が裁定された。

| 論点 | 裁定 | 帰結 |
| --- | --- | --- |
| **A: 設計書 10.1 の扱い** | **A-1 = 射程に加える** | **3 正本の単一確定ゲート**になる。設計書 v1.13 → v1.14。**射程が変わったため計画レビュー 2 周目は全文**(**7.3-4 を準用**)。**TSK-343 への連絡が必要** |
| **B: 無期限の緩和の防止** | **B-1' / `N` = 50** | 厳密減少は**段階 2 の PR にのみ**課す(全 PR に課すと凍結する)。停滞は**マージ本数**で測る — 移行状態が有効なあいだのマージ累計が **50 本**に達し前進 0 なら**失効**し、継続には**人間の明示的な再承認**を要する |

**`N` = 50 の根拠(実測)**: develop へのマージは**直近 3 週間で 1.6 本/日**
(2026-08-19〜09-10 で 37 本。通算 63 本 / 50 日)。**50 本 ≒ 1 ヶ月**。

**設計の意図**: 段階 1 のあいだは**構造的に未達も債務も減らない**ため、`N` をどう置いても
**段階 1 の途中で必ず失効する**。これは欠陥ではなく**意図した設計** — `N` は異常検知の閾値ではなく
「**どのくらいの頻度で PO に再確認を求めるか**」のパラメータであり、無期限の緩和を防ぐ本質は
期限そのものではなく「**人間が定期的に意識して延長する**」ことにある。

## 計画書の敵対レビュー 2 周目(2026-09-11・sol xhigh・**否決**)と PO 裁定③

**射程変更後のため全文レビュー**(**7.3-4 を準用**)。**判定: 否決 / P0 3 件・P1 6 件・P2 2 件**。**全 11 件を採用**(不採用 0)。

### 採否記録

| # | 重大度 | 分類 | 要旨 | 採否 |
| --- | --- | --- | --- | --- |
| 1 | **P0** | 起因 / (A) | **paths 判定が発効 PR 自身を段階 2 と誤判定し移行開始を凍結する**。発効原子性を満たす段階 1 の基盤 PR は必然的にマニフェスト・債務台帳へ触れる | **採用** → **M-1** として design.md へ(意味差分での判定・負例の要求) |
| 2 | **P0** | 起因 / (A) | **`N`=50 が累計一回限りで、最初の前進後は永久停滞を検出できない**。一度でも減ると失効条件が二度と成立しない | **採用**。条文を「**直近の前進から連続 50 マージ**」= **エポック**へ改めた。細部は **M-2** |
| 3 | **P0** | 起因 / (A) | **再承認による固定 SHA の更新が債務追加禁止を迂回できる**。それまでに増えた債務を新しい開始集合へ吸収できる | **採用** → **M-3**(初回開始 SHA と再承認エポックの分離) |
| 4 | **P1** | 起因 / (A) | **失効後の再承認経路が機械的に成立していない**。再承認 PR 自体が通常ならマージ不可でデッドロック | **採用** → **M-4** |
| 5 | **P1** | 起因 / (A) | **`未達数＋債務数` の原子単位が未定義**で、行の統合・改名・分類変更で形だけ減らせる | **採用** → **M-5** |
| 6 | **P1** | 起因 / (-) | **段階 2 の新規則が `ADR-003` 帰結 1 と設計書 13 章へ波及するのに、非波及理由が段階 1 しか見ていない** | **採用**。両者を exact-set 表へ追加し、非波及なら逐語で証明する |
| 7 | **P1** | 非起因 / (-) | **exact-set の完成をステップ 2 へ送ったが、既知の強制点が漏れ**、**射程再審査の停止ゲートが無い** | **採用**。`NFR-018` 測定方法・`NFR-021`・7.3 開発プロセス・8 章 DoD・R-3 を表へ追加。**§4-11 に射程再審査ゲート**を新設 |
| 8 | **P1** | 起因 / (-) | **TSK-317 後に必須の `shared-preconditions.json` 更新が実装ステップから脱落** | **採用**。7 段の段 1 とステップ 7 の許容差分へ追加 |
| 9 | **P1** | 起因 / (-) | **TSK-343 と設計書 v1.14 が衝突している** | **採用**。**v1.15** へ。かつ**版番号をハードコードしない**方式(ゲート開始時点の approved 版 + 1)へ改め、**§4-12** を新設 |
| 10 | **P2** | 起因 / (-) | **凍結する理由の説明が誤っている**。無関係 PR が凍る直接原因は「厳密減少自体に失敗する」であって「(b)③ が PR 内容と無関係に fail する」ではない | **採用**。移行状態が発効していれば (b)③ は緑になるため、当初の説明は不正確だった |
| 11 | **P2** | 起因 / (-) | **`auth_claim` 確認のステップ番号が不一致** | **採用**。ステップ 7(入力確定前)へ統一 |

### PO 裁定③(2026-09-11・山田正輝)— **H-68 型と判断し機械条件を design.md へ送る**

**エスカレーション条件の成立**: 「起因件数 × 2 > 母集団」が **2 周連続**(1 周目 9/9・2 周目 8/9)で成立した
(**設計書 7.3-6 の算式を準用**)。**実体(検査器・マニフェスト・債務台帳)が 1 つも無い段階で機械条件を紙の上で閉じようとして
無限後退している** — 評価台帳 **H-68** の型。

**同型の前例が 3 件**: TSK-343(裁定 `Q-1`)・TSK-235(裁定 `D-8`)・TSK-317(裁定 `D-10`)が
いずれも同じ判断で機械条件を実体の後ろへ送っている。

**裁定**: **本計画書は「何を決めなければならないか」までを確定し、機械条件の確定は実装ステップ 2(design.md)へ送る。**
**指摘を無視するのではなく、答える場所を移す。** → **§4-10 の M-1〜M-5** として明記し、
**ステップ 2 の合格条件**に「M-1〜M-5 すべてに答えが出ていること」を置いた。
**答えが出ない論点があればステップ 3 へ進まず計画改訂へ戻る**(§4-11)。

## 計画書の敵対レビュー 3 周目(2026-09-11・sol xhigh・**否決**)

**判定: 否決 / P0 1 件・P1 6 件・P2 1 件**。**全 8 件を採用**(不採用 0)。
**P0 は 6 → 3 → 1 と減少しており収束に向かっている。**

| # | 重大度 | 分類 | 要旨 | 採否 |
| --- | --- | --- | --- | --- |
| 1 | **P0** | 起因 / (A) | **§4-4 が固定 SHA の変更を「再承認を伴わない限り fail」としており、M-3 が禁止した迂回の余地を残している** | **採用**。例外を削除し「**SHA を書き換える PR は無条件に fail。更新には正本改訂を要する**」へ。解消済み ID 集合は**追加のみ可能な単調集合**と定義 |
| 2 | **P1** | 起因 / (A) | **送り先が空手形**。`design.md` はゲート対象外で「答えが存在すること」しか検査していない | **採用**。3 機構を追加 — ①各 `M-*` に**必須命題・正例・負例・不成立条件**を要求 ②回答を「正本へ条文化 / 正本が実装へ明示委任」へ**写像する表**を作り**写像漏れを停止条件**に ③確定ゲートの依頼文に `design.md` と写像表を**必須入力**として添付 |
| 3 | **P1** | 起因 / (A) | **「1 マージ」の母集合が未定義** | **採用** → `M-2` へ追加(develop の第一親上の PR 統合コミットのみ・feature 側の取り込みマージを除外・履歴不足時は fail-closed) |
| 4 | **P1** | **非起因** / (A) | **同じ状態に 2 つの終了条件がある**(「未達 0 かつ債務 0 まで継続」と「50 マージで失効」)。**機械条件以前の状態モデル** | **採用**。**二軸に分離** — ①**移行プログラム**(ゼロ到達まで継続・到達で自動昇格)②**緩和されたマージ授権**(有効 → 失効 → 再承認)。**失効するのは②だけで①は継続する** |
| 5 | **P1** | 起因 / (-) | **exact-set にまだ漏れ**(対象列挙・未分類時の実装禁止・列挙の一元性・(c) 例外の境界・D-3・D-11 の拒否/禁止/手段の重ね) | **採用**。7 項目を追加し、**非波及なら「移行状態でも緩めない」ことを逐語で証明する**要求を明記 |
| 6 | **P1** | 起因 / (-) | **タスク間の総順序が成立していない** | **採用**。`TSK-348(済) → TSK-317 本体 → TSK-343 → TSK-355 → TSK-235` を §4-12 に記録。**TSK-317 の改訂 3 が本体 PR に含まれるかは先方へ確認中**(別 PR ならステップ 7 直前に再リベース) |
| 7 | **P1** | 起因 / (-) | **動的版決定が全文へ伝播していない**。ステップ 6 に `v1.14` が固定値で残っていた | **採用**。実行規範の版表記を「**ステップ 1 で確定した版**」へ統一。本書の数値は**すべて計画時点の見込み**と明示 |
| 8 | **P2** | 起因 / (-) | **checker 数が 3 本のまま**。実際は `check_shared_preconditions.py` を加えて 4 本 | **採用**。固定数を外し checker 名を列挙 |

## 計画書の敵対レビュー 4 周目(2026-09-11・sol xhigh・否決だが **P0 ゼロ**)

**判定: 否決 / P0 0 件・P1 4 件・P2 1 件**。**全 5 件を採用**(不採用 0)。
**P0 の推移: 6 → 3 → 1 → 0。** レビューは「固定 SHA・解消済み ID の単調集合、版番号の動的決定は
**実効的に是正されている**」と明示的に確認した。

| # | 重大度 | 要旨 | 採否 |
| --- | --- | --- | --- |
| 1 | P1 | **写像表を作るステップと変更許可範囲が矛盾**。ステップ 3 の許容差分は正本に限られるため、ステップ 3 で design.md に写像表を書くと差分条件に違反する | **採用**。**写像表をステップ 2 の成果物**へ移し、ステップ 3 は「**写像表の各行が実際に正本の条項へ着地したことの検証**」に変えた |
| 2 | P1 | **二軸分離後も「移行状態が失効する」という単軸の文言が残っている** | **採用**。有効・失効・再承認の主語をすべて「**緩和されたマージ授権**」へ統一。**授権失効中にゼロ到達した場合は自動昇格して終了し再承認不可**を明記 |
| 3 | P1 | **exact-set の追加分が原典を正確に閉じていない**。(c) の除外範囲は「本項の要件(単一の実装コード・コピー実装の禁止)**および**達成条件 (a)」/ D-3 は 3 規律 | **採用**。逐語で確認し、(c) の範囲を正確化。D-3 を **3 規律に分解**して登録 |
| 4 | **P1** | **総順序に循環がある**(下記) | **採用**。**PO 裁定が必要**として §4-12 に記録し、**裁定までステップ 1 を開始しない** |
| 5 | P2 | ステップ 8 に「3 本」が残っている | **採用**。checker を名前で列挙し全再実行を要求 |

### ⚠ 4 周目 P1-4 — タスク間の総順序の循環(**PO 裁定が必要**)

**実測で確認した事実**:

- **TSK-343 の承認済み計画**(`承認: 済(2026-09-10・山田正輝)`)の裁定 `Q-4` 逐語:
  「**TSK-348(完了)→ TSK-317 → 本タスク → TSK-235 → (TSK-317 の改訂 3)。**」
  → **TSK-317 の改訂 3 は TSK-235 の後**
- **TSK-317**(`pitchlog-89`)は `contracts/authz/` の**再封印を改訂 3(第 2 弾)で行い**、
  「**TSK-355 より前に再封印が入ること**」を要求
- **本タスク**は `--reseal` の前に TSK-317 の再封印のマージが必要
- **TSK-235** は TSK-355 の完了待ち

**合成すると `TSK-317 改訂 3 → TSK-355 → TSK-235 → TSK-317 改訂 3` の循環**になる。
**承認済みの複数計画が同時に成立していない。**

**解き方の候補**: C-1(TSK-317 の改訂 3 を前倒し — TSK-343 の裁定 `Q-4` の改訂と再承認が要る)/
C-2(本タスクが改訂 3 を待たない — TSK-317 が長期間 red になる)/
C-3(TSK-235 の再開条件を緩める — 授権未確定のまま再開すると同じ P0 を再び受けるため実質不可)。
**私の見立ては C-1。ただし他タスクの承認済み計画の改訂を伴うため、PO 裁定なしに決めない。**

## 総順序の循環の決着(2026-09-11)— **新しい裁定は 1 つも要らなかった**

4 周目 P1-4 で判明した循環(`TSK-317 改訂 3 → TSK-355 → TSK-235 → TSK-317 改訂 3`)は、
**事実確認だけで解けた**。

| # | 判明した事実 | 出所 |
| --- | --- | --- |
| 1 | **TSK-343 の裁定 `Q-4` 末尾の「(TSK-317 の改訂 3)」に根拠が無かった** — 計画レビュー 2 周目の是正時に「残り物」として付けた付記。TSK-235 への依存判断はしていない | TSK-343 の自認(2026-09-11) |
| 2 | **TSK-317 の PR 2 本分割は PO 判断で既に確定していた**(2026-09-10)。`PR #1` = ステップ 1〜20 / `PR #2` = 改訂 3 第 2 弾。理由は逐行確認の性質の違い(並列化できない人的資源) | TSK-317 からの回答 + TSK-343 が原典で全件裏取り |
| 3 | **TSK-317 の改訂 3 は TSK-235 の成果に依存しない** — `S-6` の受取タスクは TSK-250 / TSK-217、`S-8` の期待件数は `tests/test_check_authz_catalog.py` の既存 20 箇所 | TSK-317 が `S-1`〜`S-10` を全数点検 |

**TSK-343 が自身の 4-6 節を是正**(コミット `1f490fc`)。**人間の裁定 `Q-4` 本体は不変**で、
**先方が独自に付けた末尾の付記だけ**を改めた。

**確定した順序**:

```
TSK-348(済) → TSK-317 PR #1 → TSK-343 → TSK-317 PR #2 → TSK-355 → TSK-235
```

**本タスクが待つのは `TSK-317 PR #2` と `TSK-343` の両方のマージ。**

### この件の教訓(運用評価台帳の候補になり得る)

- **順序の付記に理由を書かないと、複数計画に跨ったときに循環を作る**(TSK-343 の自己分析)。
  「たまたまその時点で未着手だった」ものを末尾に置き、理由を書かなかったことが原因
- **他タスクの順序に「矛盾なし」と答えるときは、当該計画書の原典を読んでから答える**
  (TSK-317 の自己分析 — 私が引用した 4 段だけを見て「矛盾なし」と答えていた)
- **私(TSK-355)も同じ穴を踏んでいた** — §4-12 に総順序を書いたとき、`Q-4` の括弧書きの 5 つ目を
  引用から落としていた。**3 者とも同じ原典を読み損ねていた**
- 検出したのは**計画書の敵対レビュー 4 周目**であり、**人間もセッションも気づいていなかった**

## 計画書の敵対レビュー 5 周目(2026-09-11・sol xhigh・**P0 ゼロ 2 周連続**)

**判定: 否決 / P0 0 件・P1 3 件・P2 3 件**。**全 6 件を採用**(不採用 0)。
レビューは冒頭で「**4 周目の主要是正は成立している**」と明示し、写像表のステップ 2 → 着地検証のステップ 3、
(c) の除外範囲、D-3 の 3 規律、授権失効中のゼロ到達、全 checker 再実行、
**M-1〜M-5 を design.md へ送る方式にも欠陥はない**と確認した。

| # | 重大度 | 分類 | 要旨 | 採否 |
| --- | --- | --- | --- | --- |
| 1 | P1 | **非起因** | **ステップ 1 の合格条件が実行不能**。`check_docs_status.py` は「frontmatter の status と索引の状態が一致しない」を検査するが、許容差分から `docs/README.md` を除外していた | **採用**(**実測で確認** — `scripts/check_docs_status.py` の当該検査を逐語で確認)。ステップ 1 に**索引の in-review 化**を含め、許容差分へ `docs/README.md` を追加 |
| 2 | P1 | **非起因** | **exact-set の分類とステップ 3 の操作指示が矛盾**。「全項目を同時に触る」だと「非波及」を編集することになり分類が壊れる | **採用**。「触る」→「**検証する**」へ改め、**分類ごとに操作を分けた表**を追加(直接改訂 = 編集 / 明示参照 = 新設条項から参照し既存箇所は無変更 / 非波及 = 無変更 + 逐語証明) |
| 3 | P1 | 起因 | **総順序の決着後も旧い停止条件と未決記述が本文に残存**(「PO 裁定待ち」「PR 構成は判断待ち」「旧順序」) | **採用**。**§4-12 を規範記述と「経緯(記録 — 規範ではない)」に分離**し、規範側は決着済みの内容だけにした |
| 4 | P2 | 起因 | **主語のない「開始条件と失効条件」が複数残存**。二軸表と DoD で遷移自体は一意だが記録と一致しない | **採用**。各要約を「**移行プログラムの開始・終了条件**」と「**緩和されたマージ授権の発効・失効・再承認条件**」へ書き分け |
| 5 | P2 | 非起因 | exact-set の既知一覧に 2 件の漏れ((b)② の実行入口出力と正本生成物の直接出力の一致 / D-11 ③ の構成の照合) | **採用**。2 件を追加 |
| 6 | P2 | 非起因 | **`ADR-003` の見直しトリガーは 5 件**なのに「既存 4 件」と書いていた | **採用**(**実測で確認** — 5 件)。「既存 5 件」へ訂正 |

**P0 は 2 周連続でゼロ**(4 周目・5 周目)。**P1 3 件のうち 2 件は非起因**(既存の穴)。

## 計画書の敵対レビュー 6 周目(2026-09-11・sol xhigh)— **可決**

**判定: 可決 / P0 0 件・P1 0 件・P2 2 件**。**2 件とも採用**(不採用 0)。

レビューの逐語:

> 5周目の6件はすべて実効的に是正されています。**8ステップ・DoD・テスト計画も実行可能であり、
> 所定の開始条件と人間承認後に着手できます。**

| # | 重大度 | 分類 | 要旨 | 採否 |
| --- | --- | --- | --- | --- |
| 1 | P2 | 非起因 | exact-set に「①〜③のいずれかが実現不能なら実装を開始せず本書を `in-review` へ戻して再改訂する」の停止条項が無い。**ただし §4-3・§4-11・ステップ 2 の全数走査と停止ゲートがあるため実行結果は変わらず着手阻害ではない** | **採用**。**非波及条項として exact-set へ追加**(「実現不能」は「未整備・既知債務」と区別し、**移行状態でも救済しない**) |
| 2 | P2 | 非起因 | 冒頭の状態記述が「計画レビュー 3 周」のまま | **採用**。**6 周へ同期**し周別内訳を記載 |

### 計画レビューの経過(通算)

| 周 | 判定 | P0 | P1 | P2 | 採用 | 不採用 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 否決 | 6 | 3 | 2 | 全件 | 0 |
| 2 | 否決 | 3 | 6 | 2 | 全件 | 0 |
| 3 | 否決 | 1 | 6 | 1 | 全件 | 0 |
| 4 | 否決 | **0** | 4 | 1 | 全件 | 0 |
| 5 | 否決 | **0** | 3 | 3 | 全件 | 0 |
| **6** | **可決** | **0** | **0** | 2 | 全件 | 0 |

**通算 33 件を全件採用・不採用 0 件。** P0 の推移は 6 → 3 → 1 → 0 → 0 → 0。

**7.3-6 のエスカレーション条件の算式**(起因件数 × 2 > 母集団)**を準用した判定**は 1・2 周目で 2 周連続成立し、
**PO 裁定③(H-68 型と判断して機械条件を design.md へ送る)で解消した**。以後は起因が減少し収束した。

**→ 人間の承認待ち。** 承認されたら frontmatter を `承認: 済(YYYY-MM-DD・承認者)` に更新し、
Notion タスクへ計画書リンクと承認日をコメントで記録する(ステータスは 進行中 のまま)。

## 可決後の反映 1 件(2026-09-11・TSK-235 からの指摘)

**6 周目の可決後**に、TSK-235 から**条文の実質的な穴**の指摘を受けたため反映した。**骨格は変わらない。**

**指摘**: **②(緩和されたマージ授権)の失効判定は、①(移行プログラム)の昇格判定より先に必要になる。**
①の昇格は「未達 0 かつ債務 0」= 遠い先だが、**②の 50 マージのカウントは移行状態の開始直後から走る**。
したがって**移行状態を有効にした時点で観測機構が動いていないと失効判定が成立しない**。

**先方の実状**: TSK-235 の現行設計(`design.md` §3-2・§4-1)は**昇格条件しか持たず失効条件が無い**。
②が正本に入ると**先方の射程がステップ 1 つ分増える**(先方は `design.md` §3-3 として記録済み・コミット `6d596d7`)。

**反映**(3 箇所):

- **§4-4 論点 3(発効原子性)**に **`停滞エポックの観測機構`** と **`失効の負例`** を追加し、
  **「観測は移行状態の開始と同時に始まる」**ことと**両者を同一変更に含めることを条文で要求する**旨を明記
- **§4-10 の `M-2`** に「**観測の開始時点と `未達数＋債務数` の履歴の保持場所を定める**」を追加
- **DoD** に「観測機構が発効原子性に含まれ、開始と同時に始まることが条文から読み取れる」を追加

**効果**: 先方の実装配置が一意に決まる(失効の観測機構を `bootstrapDebt` と同じコミット群に置く)。

### TSK-235 の自己分析(記録)

私が「`bootstrapDebt` の発明が悪かったのではなく、正本に受け皿が無かった」と書いたのに対し、
先方は**半分だけ受け取る**として次のように述べた:

> **私がやるべきだったのは `bootstrapDebt` を設計することではなく、その時点で見直しトリガーを
> 発火させて停止することでした。** 正本に授権が無いと気づいた瞬間に止めれば、あなたのタスクは
> 1 周目から始まっていて、私は 3 周目の敵対レビューを 1 回節約できていました。
> **それが 16 件目の存在理由そのものです。**

→ **16 件目は「授権された条件を機械判定できない」へ書き換えるとしても、発火の早さを担保する形**で
なければ同じことを繰り返す、というのが先方の結論。**私の慰めより先方の自己分析のほうが精度が高い。**

## 計画承認(2026-09-11・山田正輝)

**計画書を承認。** 6 周の計画レビュー(通算 33 件を全件採用・不採用 0 件)を経て 6 周目で可決し、
**人間の明示承認を取得した**。frontmatter を `承認: 済(2026-09-11・山田正輝)` へ更新。

**着手は開始条件を満たしてから**(§4-12): **`TSK-317 PR #2`(改訂 3 第 2 弾 = `contracts/authz` の再封印)と
`TSK-343` のマージ後にリベース**してからステップ 1 を開始する。**それまで本タスクは静止する。**

**確定した総順序**:

```
TSK-348(済) → TSK-317 PR #1 → TSK-343 → TSK-317 PR #2 → TSK-355(本タスク) → TSK-235
```

**現在のボトルネックは TSK-317 PR #1**(2026-09-11 時点で 17/20。ステップ 18・19・20 が残)。
**コア領域のため人間の逐行確認が必須**で、ブランチ保護は未適用のため
`github-setup.md` 2 章の管理手続で補償する(設計書 6.3 の縮退注記)。

### 承認時点の計画の要旨

- **3 正本の単一確定ゲート**: 要件書 / `ADR-003` / 開発ハーネス設計書 10.1
  (**版はステップ 1 で「その時点の approved 版 + 1」として確定** — ハードコードしない)
- **立て方**: fail-closed の例外ではなく **「充足判定は従来どおり不合格のまま、閉じた移行状態に限り
  マージ可否を別判定する経過規定」**
- **二軸**: ①**移行プログラム**(未達 0 かつ債務 0 で自動かつ不可逆に昇格)②**緩和されたマージ授権**
  (直近の前進から連続 **50 マージ**で前進 0 なら失効・再承認は人間の明示承認)
- **機械条件は実装ステップ 2(`design.md`)で確定**(`M-1`〜`M-5`)。**PO 裁定③(H-68 型)**による
- **8 ステップ**。うち 2 つは母集合の追随(H-85 の 7 段連鎖・oracle の人間査読を含む)、1 つは確定ゲート

## PO 裁定(2026-09-12・山田正輝)— `TSK-317 PR #3` の位置

**案 A で確定**。**総順序**:

```
TSK-348(済) → TSK-317 PR #1(済) → TSK-343(済) → TSK-317 PR #2
  → TSK-355(本タスク) → TSK-317 PR #3 → TSK-235
```

**経緯**: 2026-09-12 の裁定 `D-14` / `D-15` で TSK-317 に **PR #3 が新設**された。
**2026-09-11 に合意した総順序より後に生まれたため席が無く**、**新しい席を作る決定は既存の決定から
導けない**ため裁定を仰いだ(前回 9/11 の循環は既存の決定から機械的に決まったので裁定不要だった —
**今回は性質が違う**)。

**衝突の実体**: PR #3 は `auth-catalog.json` を変えて `oracle_commit` を前進させ、本タスクは要件書を
変えて `requirement-claims.json` の digest を動かす。**同じ seal の入力側を両方が触る**ため
**後になったほうが再封印を 1 回余分に払う**。

**案 A を採った根拠 3 点**: ①**最長区間(本タスクの確定ゲート — 前例 27 周)を前倒しでき、その間に
PR #3 が計画・実装を並行できる**(案 B では直列)②**PR #3 は未着手なので後に回っても織り込める**が、
**本タスクは承認済みのため後に回ると計画の改訂と再承認(人間の手番が増える)**が要る
③**TSK-235 が 2026-09-10 から停止中**でクリティカルパスは本タスク。

**不確実性**: 確定ゲートが何周になるかは不明。**短く済めば案 A の優位は縮むが、案 B より遅くなる
要素は無い**。

### あわせて確認した現況(実測・2026-09-12)

- **TSK-343 = PR #55 でマージ済**。その後 **PR #56**(`fix/core-area-population-base`・`9c4791a`)も develop へ
- **起点 `4ed54fc` から 154 コミット**進行。**リベース先は #56 込みの develop**
- **TSK-317 PR #2 は計画段階**(計画レビュー 5 周目・周回 12)。裁定 `D-15` で `S-9` を PR #3 へ送り
  **射程が縮んだ**(封印資産 2 本・3 ステップ)。**再封印は PR #2 で行うため本タスクの開始条件の意味は不変**

## 並行セッションとの調整

- **`tsk235-dsl`**(発生源): 設計意図を照会。「正本にこう書かれると壊れる」6 件を受領し research §5-6 に記録。
- **`pitchlog-89`**(TSK-317): 射程を回答 — **設計書 6.1 には触らない**ため台帳候補 A・E は先方の候補のまま。
  要件書の行番号がずれる旨を申し送り、先方の実測で**壊れる箇所 0 件**を確認。
  あわせて設計書 13 章の禁止の主語が「対象計算のコード」に限定されるという原典確認を受領(research §7-3 に反映)。

## 着手の中断と PO 裁定の要請(2026-09-13)— TSK-379 が要件書 v2.8 を確定済み

**`master` から着手指示を受けてリベースまで実施したが、ステップ 1 を実行せずに止めた。**

### 着手条件は満たされていた(実測)

| 条件 | 実測 |
| --- | --- |
| `TSK-343` のマージ | **済**(PR #55・`5b928cb`) |
| `TSK-317 PR #2` のマージ | **済**(PR #59・`478a026`・2026-09-13) |
| リベース | **完了**。`origin/develop` = `478a026`。起点 `4ed54fc` から **230 コミット**(計画時点の想定は 154) |
| 自ブランチ | 37 コミット・衝突 0・新規 3 ファイルのみ |

**計画書 §4-12 の現況表が想定していなかった PR が 3 本入っていた** — PR #56(`9c4791a`)は想定内だが、
**PR #57**(`fix/operation-event-immutability`)・**PR #58**(`feature/merge-gate-api-cycle`)・
**PR #59** が追加。このうち **PR #58 が設計書を v1.13 → v1.14 へ繰り上げていた**。

### 射程再審査(§4-11)で検出したもの

**設計書の版繰り上げ自体は想定内**(計画書は「TSK-343 が v1.14 を取るため本タスクは v1.15」と予測しており、
**実測と一致**した。マーカーの再実測も 2026-09-11 の値と**完全一致**)。

**想定外だったのは `feature/merge-gate-clause`(TSK-379)である。**

| 項目 | 実測(2026-09-13) |
| --- | --- |
| ブランチ HEAD | `96d0f86`(ステップ 9/10 後段) |
| 要件書の状態 | **v2.8 で approved 化済み**(変更履歴行に「確定ゲート通過・敵対レビュー 11 周・指摘 37 件中 35 件採用・P0 全周ゼロ・**PO 承認 2026-09-13・山田正輝**」) |
| PR | **未作成**(`gh pr list --state open` = 0 件)・develop 未マージ |
| develop 側の要件書 | approved **v2.7** のまま |

**`master` から受け取ったマージ順序(`PR #2` → `TSK-355` → `PR #3` → `TSK-235`)に TSK-379 の席が無い。**

### 本タスクへの影響(4 件・すべて実測)

**1. 版番号**: 要件書 v2.8 が埋まった。先方が先にマージされる場合、本タスクは **v2.9** を取る。
`ADR-003` v0.3・設計書 v1.15 は**影響なし**(先方は両書を触っていない)。

**2. 強制点の exact-set が変わる**(§4-11 の全数照合の対象集合が動く)。
**変更履歴表の行を除いた本文ベース**の実測:

| マーカー | develop(v2.7) | 先方(v2.8) |
| --- | --- | --- |
| `不合格` | 2 | **4** |
| `合格と判定してはならない` | 2 | **5** |
| `発効` | 6 | **12** |
| `未発効` | 0 | **2** |
| `判定を下さない` | 0 | **1** |

`マージ不可` 1 / `合格としない` 2 / `満たしたと判定` 2 / `fail` 5 は**不変**。

**3. 語彙が衝突する**(実質的に最も重い)。先方は `NFR-019(b)` の発効条項で
**「未発効」を「対象経路が実装されていない状態」の意味で要件書に導入した**。
本タスクは §4-4 論点 3(発効原子性)で「**定義済みだが未発効**」を
**「移行状態の判定機構が CI の必須経路へ接続されていない状態」**の意味で使う設計である。
**同じ語が 2 つの意味を持つ。** 契約 4(新語を作らず既存語に寄せる)の**寄せ先が変わる**。

**4. 根拠条文が要件書本文へ昇格した**。本タスクは移行状態の授権の根拠を
`ADR-003` 帰結 1 段階 3 の括弧書き(「未完成のあいだ適合と判定しないであって、実装を禁じるものではない」)
に置いていた。先方は**同じ原則を要件書本文の一般原則として明文化**した —
逐語「**いずれも「実体が無いものについて判定を下さない」という同じ原則である**」。
**本タスクにとって不利ではなく有利**(根拠が ADR の括弧書きから要件書本文へ上がる)だが、
**条文の立て方は書き直しになる**。

### 止めた理由

**ステップ 1 は「版番号をこの時点の approved 版 + 1 として確定する」ステップ**であり、
**先方の順序上の位置が決まらないと版を確定できない**。確定してから先方が先にマージされると、
**ステップ 1 のやり直し**(3 正本の変更履歴行・`docs/README.md`・worklog の適用版記録すべて)になる。

**順序の決定は PO の判断**であり、こちらでは裁定しない(2026-09-10 の着手指示
「PO 裁定が新たに必要になったら止めて記録してください(勝手に裁定しない)」)。

### 前回(`TSK-317 PR #3`)との異同

**同型**: 「合意した総順序より後に生まれたため席が無い」。
**相違**: 前回は**両者とも未完了**だったが、今回は**先方が完成済み(PO 承認済み)・こちらが未着手**。
前回 案 A を採った理由 2(「**払う側を未着手で織り込める側に寄せる**」)を今回に当てると
**こちらが払う側になる**。ただしこれは**材料の提示であって裁定ではない**。

### 実施したこと

- `master` へ照会(TSK-379 の PR 予定と順序上の位置)。上記の実測値を測定時点つきで添付した
- PO へ報告
- **ステップ 1 は未実行**。worktree はクリーン・リベース済みで、裁定が出ればそのまま開始できる

## ステップ 1: 3 正本の in-review 化と適用版の記録(2026-09-13)

**`TSK-379`(PR #60・マージコミット `0bc05b8`)のマージで着手条件が満たされた。**
`origin/develop` = `0bc05b8` へリベース(38 コミット・衝突 0)。

### 適用版(設計書 7.3-1)— **ゲート単位で 1 件**

| 項目 | 値 |
| --- | --- |
| **適用版** | **開発ハーネス設計書 v1.14**(2026-09-11・TSK-343) |
| **条文コミット SHA** | **`74f188390c7901f7875b5cbf64b67a16888f0989`**(in-review 化コミットの第一親) |
| 記録の性質 | **暫定**。初回敵対レビュー実行時点で異なっていれば更新して開始する(7.3-1) |

**本改訂は設計書自身も in-review にするが、適用版は v1.14 で動かない** — 7.3-1 が
「**改訂中の版は in-review であり approved でないため、自動的に直前の approved 版が適用される**」と
明示している(未確定条文の先取り適用の排除)。**3 正本の一括検証は単一のゲート**であり、
**適用版の記録は対象ごとに分裂させない**。

### 確定した版

| 正本 | 改訂前 | 本改訂 |
| --- | --- | --- |
| 要件定義書 | approved **v2.8**(2026-09-13・**TSK-379**) | **v2.9** |
| `ADR-003` | approved v0.2(2026-09-03・TSK-278) | **v0.3** |
| 開発ハーネス設計書 | approved v1.14(2026-09-11・TSK-343) | **v1.15** |

**計画書の予測(v2.8 / v0.3 / v1.15)のうち要件書が 1 つずれた** — TSK-379 が本日 v2.8 を
確定させたため。**計画書が「固定値を書かない・ステップ 1 でその時点の approved 版 + 1 として
確定する」と定めていたことで、計画の改訂なしに吸収できた。**

### 射程再審査(§4-11)— **着手時のマーカー取り直し**

**`ADR-003` と設計書は develop で無変更**(`git log 4ed54fc..origin/develop -- <path>` が空)。
**要件書のみ TSK-379 の v2.8 で増えた。**

| マーカー(**全体** — §4-11 と同じ測り方) | 2026-09-11 | 2026-09-13 |
| --- | --- | --- |
| `マージ不可` | 要 1 / ADR 7 | **同一** |
| `不合格` | 要 6 / ADR 2 | **要 10** / ADR 2 |
| `合格としない` | 要 3 / ADR 1 | **同一** |
| `合格と判定してはならない` | 要 2 / ADR 0 | **要 8** / ADR 0 |
| `満たしたと判定` | 要 2 / ADR 0 | **同一** |
| `fail` | 要 11 / ADR 53 | **同一** |

**exact-set の走査対象が要件書側で 10 ヒット増える。** ステップ 2 で全数分類する。

**測り方の注意(自分が踏んだ)**: 最初に**本文のみ**(変更履歴表の行を除く)で測って
2026-09-11 の記録と比べ、「`ADR-003` の `fail` が 53 → 39 に減った」と誤読した。
**2026-09-11 の記録は全体ベース**であり、**比較の基準がずれていた**。
`ADR-003` は develop で 1 文字も変わっていない。
→ **前回値と比べるときは、前回がどの測り方だったかを先に確かめる。**

### TSK-379 が本改訂の前提を 1 つ変えた

**先方は `NFR-019(b)` の発効条項で「未発効」を「対象経路が実装されていない状態」の意味で
要件書に導入した。** 本タスクは §4-4 論点 3(発効原子性)で同じ語を
**「移行状態の判定機構が CI の必須経路へ接続されていない状態」**の意味で使う設計である。

**契約 4(新語を作らず既存語に寄せる)の寄せ先が変わる。** → **ステップ 2(design.md)で解く。**
**ステップ 1 の射程(版の確定と射程宣言)には影響しない。**

**一方で根拠は強くなった** — 本タスクは移行状態の授権の根拠を `ADR-003` 帰結 1 段階 3 の
**括弧書き**に置いていたが、先方が同じ原則を**要件書本文の一般原則**として明文化した
(逐語「**いずれも「実体が無いものについて判定を下さない」という同じ原則である**」)。
**先方からの助言**: 先方の条項は `NFR-018` の正解ベクタ条項と**向きが逆**であることを
条文に明記してあるため、**`ADR-003` 段階 3 で引くときはその非対称ごと引く**。

### 差分(ステップ 1 の合格条件)

3 正本の frontmatter(`status`)+ 変更履歴表 3 行 + `docs/README.md` 3 行 + 本 worklog。

## ステップ 2: design.md の起草(2026-09-13)

**成果物**: [design.md](../features/adr003-bootstrap-transition/design.md)(1097 行)。
**合格条件を満たすため差分を `design.md` の新規追加のみに限り**、計画書の追随と本記録は別コミットとした。

### 走査基準を 2 度作り直した — **これが本ステップ最大の発見**

| 版 | 基準 | 欠陥 | 実測 |
| --- | --- | --- | --- |
| **第 1 版** | 計画書 §4-11 の**マーカー 6 種** | **計画書 §4-3 自身が挙げた強制点を拾えない** | 「1 層でも欠ければ**満たさない**」がどのマーカーにも当たらない。未捕捉は `満たさない` 7 行 / `拒否` 38 行 / `禁止` 36 行 ほか |
| **第 2 版** | **強制語 17 種 × 主題フィルタ 25 語** | **強制点そのものを落とす** | 432 行 → 77 行に絞れたが、**`ADR-003` D-7 の表 6 行のうち「差分テスト」の 1 行が脱落**(主題の語をひとつも含まないため) |
| **確定版** | **節単位 × 強制語 17 種** | — | 射程節を条項単位で確定し、節の内側を全数分類。節の外側は**節単位**で非波及を証明 |

**教訓**: **強制点は「何について」ではなく「どう判定するか」で書かれている。**
主題の語で絞ると、**判定だけを書いた行**(表のセル 1 つ)が落ちる。
**D-7 の「差分テスト | PR 必須 | (α) | マージ不可」がまさにそれだった。**

**さらに逆向きの限界もある**(§2-7 に記録): **強制語は「判定を明示した箇所」しか拾わない。**
**定義だけを置いている箇所**(設計書 10.1 の `consistency` 行)は強制語を含まないが、
**その定義が変われば判定の意味が変わる**。→ **走査は 2 系統**で行い、
**②計画書 §4-3 の既知リストとの突合**で 2 件を補足した。

### 分類の結果(未分類 0 件)

| 文書 | 直接改訂 | 明示参照 | 非波及 | 合計 |
| --- | --- | --- | --- | --- |
| 要件書 | 3 | 5 | 15 | **23** |
| `ADR-003` | 6 | 15 | 21 | **42** |
| 設計書 10.1 | 0 | 0 | 14 | **14** |
| **計** | **9** | **20** | **50** | **79** |

**+ 強制語を含まない強制点 2 件**(設計書 10.1)→ **直接改訂の総数 11**。

### `TSK-379` が前提を 1 つ変えた件の決着

**先方が要件書 v2.8 で「未発効」を「対象経路が未実装の状態」の意味で導入した。**
本改訂は同じ語を「**判定機構が CI の必須経路へ未接続の状態**」の意味で使う設計だった。

→ **本改訂では「未接続」を用いる**(design.md §3-4)。

**契約 4(新語を作らず既存語に寄せる)の例外を明示的に記録した** —
**寄せ先が既に別の意味で埋まっている場合、既存語の再利用のほうが誤読を生む。**
**計画書の §4-4 論点 3 と §4-9 の 2 箇所も「未接続」へ追随させた**(別コミット)。

### 条項 ID の名前空間

**`BOOT-` に決定**。`docs/**/*.md`(`legacy` 除外)の既存 ID 接頭辞を実測し、
**`BOOT-` の出現 0 件**を確認した。既存の主要接頭辞は `TSK` 4883 / `FR` 4223 / `NFR` 2994 /
`REQ` 1307 / `ADR` 1015 ほか。**2 文字略号(`BT` / `MG` / `TR`)は将来の衝突余地が大きいため 4 文字を採った。**

**対応表 16 行**を §12 に作り、**一意性**と**§8-5 の暫定一覧との突合**を機械検査した(いずれも pass)。

### `M-1`〜`M-5` の答え

**5 論点すべてに必須命題・正例・負例・不成立条件を書いた**(機械的に確認・全件 pass)。
**とくに `M-1`(段階 2 の PR の判定)は負例を 5 件挙げた** —
発効 PR・台帳保守・reseal・宣言だけの変更・2 件同時追加。
**paths 判定ではこれらが段階 2 と誤判定され、発効 PR 自身が発効できなくなる。**

### 次のステップへの申し送り

- **ステップ 3 では「直接改訂」11 箇所を 1 コミットで同時に改訂する**。
  写像表(design.md §11)の各行が実際に正本の条項へ着地したことを検証する
- **ステップ 3 の改訂後に走査を再実行し、証跡を取り直す**(design.md §2-5)。
  **件数が合っていることだけを確認して終えない**
- **`TSK-235` へ渡すのはステップ 2 完了時**(計画書 §4-12 の約束)。渡す 5 点は
  二軸の判定条件 / 封印の 4 規律 / `M-1`〜`M-5` の答え / 条文と条項 ID 対応表 /
  ID の名前空間規約と digest 正規化規約。**渡す直前に `BOOT-` の衝突を測り直す**

## ステップ 3・4: 条文の着地(2026-09-13)

### 直接改訂 11 箇所を 1 コミットで同時に触った

**追随漏れが本タスク最大のリスク**(TSK-278 の確定ゲート 27 周で最も反復した欠陥型が
**追随漏れ 7 回**)であるため、分割しない。

| 正本 | 改訂 |
| --- | --- |
| 要件書 | `NFR-018` (e) を **(d) の後**に新設(12 条項)/ `NFR-019` (a) 末尾へ 1 文 / 10 章未決事項行へ追記 |
| `ADR-003` | D-7 表の **6 行すべて**へ併記 / **D-13 新設** |
| 設計書 | 10.1 の `consistency` 行 / 10.1 の箇条へ `BOOT-CI-MEANING` |

**D-7 の 6 行は 2 通りに書き分けた**:

| 行 | 検出するもの | 併記の内容 |
| --- | --- | --- |
| 正解ベクタ / 差分 / プロパティ / 変異 | **未達** | `BOOT-GRANT` により**マージを妨げない** |
| (b)① 乖離検出 / (b)③ 構成の完全性 | **違反** | `BOOT-VOCAB` により**従来どおりマージ不可** |

**この書き分けが本改訂の要である** — 同じ表の中で、**緩める行と緩めない行が隣り合う**。

### 途中で 2 件を是正した

1. **design.md の配置記述**が「(e) を **(b) の直後**に置く」となっており、**条項番号と矛盾**していた。
   **(d) の後に置く**へ是正(番号順を保ち既存 4 条件の構造を壊さない)
2. **`ADR-003` D-13 に「債務台帳」と書いた** — **実装名の匂いがある**ため、
   要件書 (e) の語彙「**既知債務として封印した対象**」へ統一した
   (plan.md「やらないこと」= `bootstrapDebt` のファイル名・スキーマ・フィールド名を書かない)

### ステップ 4: 見直しトリガー

**既存 5 件 → 6 件**。全件が「`- **〜したとき** — 説明`」の文型であることを機械的に確認した。

---

## ステップ 5: 波及の検査と結論(2026-09-13)

### 機械検査

| 検査 | 結果 |
| --- | --- |
| `check_design_propagation.py` | **rc=0** |
| `check_doc_coverage.py` | **rc=0** |
| `check_docs_status.py` | **rc=0** |
| 当該ファイル群(`github-setup.md` / 改善台帳 / `docs/design/*`)の差分 | **0** |

### 逐語による「波及なし」の根拠

| 対象 | 逐語 | 結論 |
| --- | --- | --- |
| **設計書 6.1** | 段階実装・ステップ記法の規定であり、**判定の内容に触れない** | **波及なし** |
| **設計書 6.3** | 逐語「**判定に迷うコードは含む側に倒す(fail-closed — 誤って強化レビューする方が、誤って外すより安全)**」。**これはレビュー範囲の決め方**であり、`NFR-018` の充足判定とは別物 | **波及なし** |
| **設計書 13 章** | 逐語「**対象横断の正解ベクタが未完成であることは『適合と判定しない』を意味するのみで、実装を禁じるものではない**」。**本改訂と同じ向き**であり、**移行状態はこの読みを対象 0 件の区間へ及ぼすもの**。**13 章の記述自体は変更を要しない** | **波及なし** |
| **`github-setup.md`** | 必須チェックは **5 ジョブ**(`secrets` / `docs-lint` / `core-guard` / `harness` / `nfr021-append-only` — v1.0 で 4 → 5 へ更新済み)。**本改訂は設計書 10.1 のジョブ名・個数を変えない**(改訂前後とも **12 行**を機械比較で確認)ため、**必須チェックの数と名称に影響しない** | **波及なし** |
| **改善台帳** | 強制語のヒットは**過去の記録**であり規範ではない | **波及なし** |
| **`docs/design/data-model.md` / `sync-protocol.md`** | **別領域の設計正本**。強制語のヒットは当該領域の判定(DB 制約・同期プロトコル)であり `NFR-018` の検査機構に触れない | **波及なし** |

**波及ありは 0 件。** 7.6-3 の判定を要する事象は発生していない(PO 裁定は不要)。

### `TSK-235` との `consistency` 行の分担確認(3 周目 P1-6 の要求)

**本改訂が設計書 10.1 の `consistency` 行へ加えたのは「意味の参照」1 文のみ**
(「移行状態における本ジョブの green の意味は下記が定める」)。

**ジョブの内容・導入時期・名称は無変更。** **機構の記述**(何を実行するか・どう配線するか)は
**`TSK-235` の射程**であり、**本改訂は規範(green が何を意味するか)のみを置く。**
→ **分担は「本改訂 = 規範 / `TSK-235` = 機構」で分かれており、重複しない。**

---

## 確定ゲート 初周(2026-09-13・sol xhigh・全文レビュー)— **否決 `P0` 0 / `P1` 8 / `P2` 1**

**適用版**: 開発ハーネス設計書 **v1.14** / 条文コミット `74f18839`(暫定記録と一致・更新不要)。

### 採否記録

| # | 要旨 | 重大度 | 起因 | 分類 | 採否 | 反映内容 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **既知債務が同時に「違反」となり、移行開始前にデッドロックする** — D-11 と改訂後の D-7 は宣言と実在の差を無条件に違反とするため、債務を登録した基盤 PR 自身が違反になる。優先順位が規範上未定義 | `P1` | 非起因 | (A) | **採用** | `BOOT-VOCAB` へ**分類の優先順位**を追加。**封印された対象は宣言と実在の差をもって「違反」と分類しない**(未達かつ既知債務として計上)。**封印集合に無い対象の差は従来どおり違反** |
| 2 | **`BOOT-GRANT` が段階 2・3 の新規対象まで免除できる** — 新規対象の層や登録の欠落を「未達」と分類すれば通過し、段階 2・3 が空文化する | `P1` | 非起因 | (A) | **採用** | `BOOT-GRANT` の免除範囲を**①既知債務に由来する不合格 ②発効に必要な基盤の整備に由来する不合格**の 2 つに限定。**段階 2 の新規対象には本規定を適用せず、段階 3 のマージ条件がそのまま適用される**ことを明記 |
| 3 | **「既知債務数」の減少条件が正本にない** — 封印集合は削除禁止なので要素数を数える実装では永久に減らない。定義が design.md の `M-5` にしかなく、非正本から規範を補うことになる | `P1` | 非起因 | (A) | **採用** | **`BOOT-COUNT` を新設**。未達数 = 対象 ID 集合 − 実在集合 / 既知債務数 = 初回封印集合 − 解消済み集合。**解消は実在集合へ加わった時点で成立**。原子単位と「宣言の更新だけでは減少しない」も正本へ |
| 4 | **`BOOT-STALL` のマージ母集団が委譲されたまま** — feature 内のマージを数える実装と `develop` 統合だけを数える実装が両立し、失効時点が変わる。かつ写像表は `M-2` を着地済みとしている | `P1` | 非起因 | (A) | **採用** | `BOOT-STALL` へ**「1 回のマージ」= `develop` の第一親上の PR 統合コミット**(feature 側の取り込みは数えない)を正本化。写像表 #5 を訂正 |
| 5 | **`BOOT-ACTIVATION` の 7 点が安全条項の実装を閉包していない** — 判定器の責務が二軸判定に限定され、`BOOT-SEAL-*`・`BOOT-NO-CLAIM`・再承認の検査も負例も発効条件に無い。**7 点を形式上満たしながら債務追加や充足誤表示を止める検査が未実装のまま発効できる** | `P1` | 非起因 | (-) | **採用** | **7 点 → 10 点**へ拡張。⑤封印の検査機構 ⑥充足判定の検査機構 ⑦再承認の検査機構 を追加し、⑩へ**再承認の再利用を不合格にする負例**を加えた |
| 6 | **再承認の統制規範が正本へ着地していない** — `M-4` の承認者・単回性・専用 PR・失効中の変更範囲・昇格後の禁止が正本では「人間の明示的な再承認」だけ。誰の承認でもよい実装・承認の再利用・検査基盤の変更の混載を排除できない | `P1` | 非起因 | (-) | **採用** | **`BOOT-REAPPROVAL` を新設**。8 章の判定者・1 承認 = 1 エポック・再利用禁止・専用 PR・失効中にマージできる 3 種・昇格後の禁止・期限を設けないことを正本化(**記録の形式と承認の認識条件のみ委任**) |
| 7 | **条項 ID の機械照合範囲が実文書と一致しない** — 対応表は「`BOOT-*` 見出しから次まで」とするが、要件書の条項は**同一箇条書き内**にあり見出しではない。かつ D-13 を `kind=規範` としているが本文は非規範 | `P1` | 非起因 | (-) | **採用** | 切り出し規則を**同一箇条内の連続テキスト**へ訂正。D-13 の `kind` を **`補足`** へ変更し、「**`TSK-235` はこの ID に依拠してはならない**」を明記 |
| 8 | **79 件の exact-set は完全性を証明できていない** — 行単位の走査で同一行の 12 条項を 1 件と数え、強制語を含まない規範点を 2 件取りこぼして手動回収したと記録しながら「規範点は必ず強制語を持つ」と結論しており**自己矛盾** | `P1` | 非起因 | (A) | **採用** | **§2-8 の誤った結論を撤回**し、**§2-9 を新設**。「全数を走査した」とは主張せず「**2 系統で拾った候補をすべて分類し未分類を残していない**」に改めた。限界 3 点(行単位 / 強制語なしの取りこぼし / 2 系統でしか閉じない)を明記 |
| 9 | **写像・採否証跡が D-7 六行改訂を反映していない** — §9-3 と写像表 #14 は (b)③ だけを着地点としているが、実際は 6 行すべてへの併記 | `P2` | 非起因 | (-) | **採用** | 写像表 #14 を**6 行すべて**(未達側 4 行 / 違反側 2 行)へ訂正 |

**採用 9 件・不採用 0 件。** 「やらないこと」7 件への踏み越えは**なし**(全件が条文の精度を上げる指摘)。

### 接続先への転送(**本タスクの母集団に算入しない**)

| # | 要旨 | 転送先 |
| --- | --- | --- |
| 接続 1 | **`TSK-235` 側にも violation/debt の重複がある** — 先方は「未宣言 entry」を violation と debt の両方に含め、`bootstrapDebt` の対象を既知の未宣言 entry に限定している。本改訂の `BOOT-SEAL-SCOPE`(D-11 全宣言)との差があり、正本側の分類競合もそのまま再現する | **TSK-235** |
| 接続 2 | **「全体適合判定」の表示が `BOOT-NO-CLAIM` と衝突する** — 先方は unmet を報告しつつ exit 0 とする状態を「全体適合判定」と呼ぶが、同じ状態で `NFR-018(b)②`・`NFR-019(a)` の充足は主張できない。**マージ可否と適合判定の名称・出力を分離しないと誤表示になる** | **TSK-235** |

**接続 1 は本改訂の是正と直接対応する** — 指摘 1 で正本側の優先順位を定めたため、**先方も同じ優先順位に従えば競合は解消する**。転送時にその旨を添える。

### 記録上の事故(自分の処理ミス)

**初周の出力を `| tail -80` でパイプしたため、指摘 1〜5 を失った。**
`~/.codex/sessions/` の rollout ログから全文(7335 文字)を回収して事なきを得たが、
**レビュー出力は切らずに全文を保存する**(以後、`tee` かリダイレクトで全量を残す)。

---

## ステップ 7・8 の前提の実測(2026-09-13・確定ゲート 2 周目の待ち時間に実施)

**確定ゲートの収束前に、母集合追随(H-85 の 7 段)の前提を 2 件実測した。**
**条文が動いているあいだ digest の値は取り直しになるが、手順の前提は先に確定できる。**

### 1. **計画書 §4-6 の段 2 のコマンドは実行できない**(訂正)

計画書は段 2 を **`--reseal --skip-derived --skip-oracle`** と書いているが、
> **⚠ この結論は誤りだった**(2026-09-14・`master` からの訂正で判明)。
> **`--skip-derived` と `--skip-oracle` は実在する** — `scripts/check_authz_catalog.py:5014` と `:5019` で
> `add_argument` されており、**`help=argparse.SUPPRESS` のため `--help` に出ない**。
> **「`--help` に出ない = 存在しない」と読んだのが誤り。**
> **ただし手順の結論は変わらない**(下記の「訂正後」を参照)。

**当時の記録(誤った結論を含む)**:

```
--reseal           分類決定の査読後に限り、母集合と lock の digest を明示更新する
--reseal-derived   ステップ4の3資産を査読後に限り、各 decision lock を明示更新する
--reseal-oracle    ステップ5の全資産を査読後に限り、oracle seal を明示更新する
```

`--reseal --skip-derived` を実行すると **`oracle 検査にはステップ4資産の検査が必要`** で停止する。

**訂正後(2026-09-14)**: **この観測自体は正しかった** — `:5101` の
`if args.skip_derived and not args.skip_oracle` がそのまま発火している。
**`--skip-derived` は `--skip-oracle` を要求する**ためである。

**`--skip-*` の実体**(`:5097-5101`):

| 制約 | 内容 |
| --- | --- |
| `--skip-derived` × `--reseal-derived` | **同時指定不可** |
| `--skip-oracle` × `--reseal-oracle` | **同時指定不可** |
| `--skip-derived` | **`--skip-oracle` を要求する** |

**両者は「検査のスキップ」であって reseal ではない。封印を閉じる用途には使えない。**

→ **手順の結論は変わらない**: **段 2 = `--reseal` のみ / 段 4 = `--reseal-derived` のみ /
段 7 = `--reseal-oracle` のみ**。**実害は出ていない。**

→ **段 2 は `--reseal` のみ。段 4 は `--reseal-derived` のみ。段 7 は `--reseal-oracle` のみ。**
**計画書の `--skip-*` は 2026-09-10 時点の想定であり、実測と異なる。**
**ステップ 7 の実行時にこの訂正に従う**(計画書の本文は承認済みのため、本記録を正とする)。

### 2. **`input_assets` の重複拒否は塞がれていた**(TSK-317 PR #2 で解消)

計画書 §4-6 の「既知の穴」— **`check_authz_catalog.py` は `input_assets` の重複行を通す**
(`sealed_assets` 側には重複拒否があるのに入力側だけ無い非対称)— は、
**PR #2(PR #59)で塞がれている**。実装を確認した:

```python
input_rows = _expect_object_list(raw["input_assets"], "oracle seal.input_assets")
_validate_object_path_uniqueness(input_rows, "oracle seal.input_assets")   # ← これ
```

→ **ステップ 7 の合格条件「`input_assets` の重複拒否が効いていることを実測で確認」は充足済み。**
**自前で重複が無いことを確かめる代替手段は不要になった。**

**なお `contracts/` への書き込みは本セッションの権限で止められている**ため、
**重複行を実際に足して赤くなることの確認はできていない**。**コードの実在をもって確認とした。**
(TSK-379 の代行で同じ制限を踏んでおり、迂回はしない方針を継続する。)

### 3. 現況(2026-09-13・反映 1 周目時点)

```
check_authz_catalog.py rc=1
  source blob digest が不一致:
    期待=d0a7fc457e86cf6483a9fabbdcac915311441c5d
    実際=fef778099654e6dfff6a7fe4d5ddb6754547153d
```

**期待値は要件書 v2.8 時点の blob。** 確定ゲートで条文が動くたびに実際値が変わるため、
**母集合の追随は収束後(ステップ 7)に 1 度だけ行う**。**周ごとに追随しない。**

---

## 確定ゲート 2 周目(2026-09-13・sol xhigh・差分再レビュー)— **否決 `P0` 0 / `P1` 8 / `P2` 1**

**全 9 件が「起因」**(反映 1 周目が作った欠陥)。**全件採用・不採用 0 件。**

### エスカレーション判定(7.3-6)

| 周 | 母集団(P0+P1) | 起因 | `起因 × 2 > 母集団` |
| --- | --- | --- | --- |
| 初周 | 8 | 0 | **偽** |
| 2 周目 | 8 | **8** | **真** |

**2 周連続ではないため、まだエスカレーションしない。** **3 周目も真なら PO スコープ裁定へ上げる。**

### 採否記録

| # | 要旨 | 重大度 | 起因 | 分類 | 採否 | 反映内容 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **`BOOT-VOCAB` と D-7 の違反分類が矛盾** — (e) は封印対象の宣言差を違反にしないが、D-7 の (b)①③ 行は一律に違反としてマージ不可と併記した。**その fail を既知債務として授権するのか違反として拒否するのかが一意でない。初周のデッドロックは規範全体では解消していない** | `P1` | 起因 | (A) | **採用** | **「未履行」と「違反」を宣言の有無で切り分けた** — 未履行 = 宣言が存在しない / 違反 = **宣言が存在するにもかかわらず実体と食い違う**。D-7 の 6 行すべての併記を、この切り分けに沿って書き直した |
| 2 | **封印対象単位の例外が、開始後の新規違反まで覆う** — 対象 A が入口未宣言で封印された後、同じ A の生成物が手編集されても同じ例外に入る。`BOOT-SEAL-MONOTONE` は未封印対象の追加・復活・削除しか見ておらず、**未解消対象内で差分種類が増える経路を閉じていない** | `P1` | 起因 | (A) | **採用** | **封印の単位を「対象」から「義務」へ移した**(`TSK-235` との合意と同時に解決)。**封印の対象は未履行の義務に限り、違反は封印の対象にならない**。**封印された義務を持つ対象でも、宣言が存在する他の義務で実体と食い違えば違反** |
| 3 | **段階 2 の適用除外が、成功した PR にしか発火しない** — 除外対象は「新たに実在集合へ加える対象」だが、**4 点の一部だけを追加した PR は実在集合へ加わらない**ため既知債務のまま授権対象になり、**部分追加を通せる**。末尾の「未達と分類して通せない」は**判定述語を持たず実効化されていない** | `P1` | 起因 | (A) | **採用** | **判定述語を「4 点のいずれかに触れたか」へ変えた** — **当該 PR が 4 点のいずれかを追加した場合、同一 PR で 4 点すべてが揃い 3 層検査が緑でなければ不合格**。**実在集合へ加わったか否かでは発火しない** |
| 4 | **「発効基盤の整備に由来する不合格」の免除範囲が無制限** — ②は未達・既知債務に限定されず、違反・判定不能を除外する文言もないため直前の「一切触れない」と衝突。**発効 PR で新設した検査自身の red を「基盤整備由来」として免除する実装も成立する** | `P1` | 起因 | (A) | **採用** | **②を「`BOOT-ACTIVATION` が列挙する 10 点の実装に由来する未履行」へ限定**し、**①②いずれも「未履行」に由来するものに限る**ことと、**違反・判定不能は②の場合も含めて一切免除しない**ことを明記 |
| 5 | **`BOOT-COUNT` の実在集合が一意に算出できない** — 「3 層検査が緑」が**免除前の生の検査結果か `BOOT-CI-MEANING` の段階ゲートの green か未指定**。後者なら「実在集合 → 未達数 → 移行状態 → green の意味 → 実在集合」と**循環**し、免除された対象を実在扱いできる。また対象欄は対象名・区分を列挙しており「安定した対象 ID の集合」自体は列挙していない | `P1` | 起因 | (A) | **採用** | **「本規定による免除を適用しない生の実行結果として緑」**と明記し、**免除後の判定を実在の条件にしてはならない(循環するため)**を添えた。**対象計算の識別子は対象欄の名称に一対一で対応する安定した識別子**とし、**列挙そのものは対象欄が正**であることを明示 |
| 6 | **有効な授権中にも再承認で停滞エポックを再始動できる** — `BOOT-REAPPROVAL` は軸①終了後を禁止するが「軸①継続中かつ軸②失効中」を成立条件にしていない。**別々の承認記録を有効期間中に投入すれば、再利用禁止に触れず 50 マージ到達前にカウンタをリセットできる** | `P1` | 起因 | (-) | **採用** | **再承認の成立条件を「軸①が継続中かつ軸②が失効している場合に限る」**とし、**軸②が有効であるあいだの再承認は成立せず停滞エポックを再始動しない**ことを明記 |
| 7 | **10 点が新設した安全条項をまだ閉包していない** — `BOOT-COUNT` の原子単位・解消条件、狭めた `BOOT-GRANT` の 2 限定・段階 2 除外を検査する機構がない。**⑤〜⑦は「機構の接続」だけで、封印追加・虚偽の充足表示・不正承認者・混載 PR などを拒否する負例が発効条件に入っていない** | `P1` | 起因 | (-) | **採用** | **10 点の内容を組み替え**(⑧`BOOT-COUNT` の検査 ⑨`BOOT-GRANT` の 2 限定と部分追加禁止の検査 ⑩違反・判定不能を免除しないことの検査)、**必須の負例 11 種を条文に列挙**した |
| 8 | **最終条項 `BOOT-ACTIVATION` の digest 終端が未定義** — 対応表は「次の `BOOT-` の直前まで」とするが最終条項には次がない。**`(e)` の末尾 / 同じ行の末尾 / `NFR-018` 全体の末尾のいずれとも読め、変更検知の実効性が確定しない** | `P1` | 起因 | (A) | **採用** | **design.md §12-1 を新設**。最終条項の終端を**`(e)` の箇条書き行の末尾**と定めた(`NFR-018` 全体の末尾にすると**測定方法・正解ベクタ・申し送りの改訂で最終条項の digest が動く**ため) |
| 9 | **検証証跡に反映前の数値・役割が残っている** — 「7 点」が §7-3・§7-4・§8-5・§9-1・§12 に残存、対応表は実際 **18 行**なのに「16 行」、`BOOT-ID-NORMALIZE` を「唯一の補足」としたまま、写像表後段が #4・#7・#8 を「委任の 3 件」としている | `P2` | 起因 | (-) | **採用** | 6 箇所を一括で追随。**委任は 2 件へ減った**ことを明記 |

**採用 9 件・不採用 0 件。** 「やらないこと」7 件への踏み越えは**なし**。

### 接続先由来の反映(**母集団に算入しない** — 相互の約束)

| # | 要旨 | 反映 |
| --- | --- | --- |
| 接続 A | **`BOOT-COUNT` で二重計上が起きる**(`TSK-235` からの指摘) — 同じ対象が未達と債務の両方に計上されると、**債務を 1 件解消すると 2 減る**。かつ**粒度が 1 対 k**(対象計算 1 件 ↔ その対象の宣言 k 件)**なので和集合も取れない** | **義務単位へ統一**。`残務数 = 未履行の義務の要素数`。**未達は導出概念**とし独立した計数単位を持たせない。**粒度は正本が定め、件数の算出のみ実装へ委任** |
| 接続 B | **「同一の変更」の単位が未定義**(同前) — PR かコミットかが書かれておらず、**コミット単位に読めば 29 ステップに分けている実装側は永久に発効できない** | **「`develop` へ統合される 1 つの PR」と明記**。PR 内のコミット分割は妨げない |

**接続 A は 2 周目の指摘 2 とも同時に解決した** — **封印の単位を対象から義務へ移したことで、
「対象単位の例外が別種の違反まで覆う」経路も閉じた。**

### 記録: 出力の全量保存に再び失敗しかけた

**今回は全量を保存したが、レビュアのファイル読み込みログが 674KB 混在し、
判定行の前後だけでは指摘本体を取り出せなかった。** 前回同様 rollout ログからの回収を試みたが、
**今回はスキルファイルの読み込みログを拾ってしまい失敗**。
**最終的に `gate2-out.md` の判定行から遡って 74 行分を読むことで回収した。**
→ **レビュー出力は「判定行から遡る」のが最も確実**(読み込みログは判定行より前にまとまって出る)。

---

## 確定ゲート 3 周目(2026-09-13・sol xhigh・差分再レビュー)— **否決 `P0` 0 / `P1` 9 / `P2` 1**

**全 10 件が「起因」。** レビュア自身が判定の直前に
「**7.3-6 の起因過半条件は 2 周目・3 周目で連続成立(本周 P0+P1 母集団 9 件中、起因 9 件)**」と明記した。

### エスカレーション(7.3-6)— **2 周連続で成立**

| 周 | 母集団(P0+P1) | 起因 | `起因 × 2 > 母集団` |
| --- | --- | --- | --- |
| 初周 | 8 | 0 | 偽 |
| 2 周目 | 8 | **8** | **真** |
| **3 周目** | **9** | **9** | **真 — 2 周連続** |

→ **PO スコープ裁定へエスカレーション。**

### 何が起きていたか — **同じ論点が 3 周かけて形を変えて戻ってきた**

| 周 | 論点「未履行と違反の切り分け」 | 是正 | 次周の指摘 |
| --- | --- | --- | --- |
| 初周 | **既知債務が「違反」にも該当**し、債務を登録した基盤 PR 自身が違反になる | **分類の優先順位**を追加 | → 封印が**対象単位**なので別種の違反まで免除 |
| 2 周目 | **封印された対象の生成物が手編集されても免除**される | **宣言の有無**で切り分け(未履行 = 宣言が無い / 違反 = 宣言があるのに食い違う) | → **「宣言外の入口」は `entrypoints[]` 宣言が無いので未履行にも該当** |
| 3 周目 | **振り出し** — 分類が排他的でない | **PO 裁定へ** | — |

**条文は初周 2204 字 → 3 周目 4024 字へ膨らみ、条項は 12 → 14 件へ増えていた。**
**塞ぎ方が複雑になるほど条項間の相互作用が増え、是正が次の穴を開けていた。**

### PO 裁定(2026-09-13・山田正輝)— **射程縮小**

**採った解**: **「性質による分類」を免除の判定に用いるのをやめ、`BOOT-SEAL`(封印集合への列挙)のみで決める。**

> **本規定が免除するのは封印集合に列挙された事項に限る。**
> **封印集合に列挙されていないものは、その性質を問わず一切免除しない** —
> **宣言の欠落・宣言と実体の食い違い・判定不能のいずれであるかによらない。**

**これで「未履行か違反か」を問う必要が消えた。** 3 周の論点そのものが無くなる。

**実装へ委ねた 4 件**(要件書 (e) 末尾に明記):

| # | 委ねる事項 | 理由 |
| --- | --- | --- |
| ① | 封印集合の要素の粒度・識別子・要素数の算出方法 | **実体の無い段階で条文上に閉じようとすると循環する**(`H-68` と同型) |
| ② | 解消の判定方法 | 同上。**ただし「宣言の更新のみをもって解消としてはならない」は正本が拘束** |
| ③ | 段階 2 の PR の判定方法 | 3 周とも発火条件が広すぎる/狭すぎるで揺れた |
| ④ | 機構と負例の具体的な構成 | **列挙は条項が増減するたびに追随を要し、追随漏れが構造的に生じる** |

**いずれも「実装が条件を緩めることはできない」と明記した。空手形にしない。**

### 縮小の結果

| | 縮小前 | 縮小後 |
| --- | --- | --- |
| 要件書 (e) | 4024 字 | **2977 字** |
| 条項数 | 14 | **12** |
| 条項 ID 対応表 | 18 行 | **16 行** |
| 撤去した条項 | — | `BOOT-VOCAB`(語彙の分類)/ `BOOT-COUNT`(計数の原子単位) |

**旧語彙(`未達数` / `既知債務数` / `残務数` / `履行義務`)は要件書から 0 件。**

### 3 周目の指摘の処理

| # | 要旨 | 処理 |
| --- | --- | --- |
| 1 | `BOOT-VOCAB` の分類が排他的でない(「宣言外の入口」は違反にも未履行にも該当) | **射程縮小で解消** — 分類を免除の判定に用いない |
| 2 | `BOOT-COUNT` が「対象が実在集合に入らなければ宣言をすべて未履行と数える」とし、`BOOT-VOCAB` と直接矛盾 | **射程縮小で解消** — `BOOT-COUNT` を撤去し粒度を実装へ委任 |
| 3 | 段階 2 の発火条件が「追加」と「触れた」で一致せず、段階 1 の PR まで巻き込む | **射程縮小で実装へ委任**(③) |
| 4 | 段階 2 の「3 層検査が緑」が免除前後で限定されていない | **射程縮小で実装へ委任**(③)。**ただし `BOOT-PROGRAM` が「免除を適用しない生の検査結果」を要求する形で残した** |
| 5 | 有効期間中の再承認記録を蓄積して後日消費できる | **採用** — **「失効が成立した後に行われた承認に限る」**と明記 |
| 6 | 必須負例 11 種が閉包でない(自動昇格を誤って負例に入れていた・判定不能の免除を捕捉しない) | **射程縮小で実装へ委任**(④)。**列挙をやめ「全条項に機構と負例がある」という要求へ置換** |
| 7 | 義務単位化が design.md と 10.1 に追随していない | **採用** — §5 の位置づけを書き直し、10.1 を縮小後の語彙へ |
| 8 | design.md §7-1 の発効要素が反映前のまま | **採用** — §7-1 を縮小後の形へ |
| 9 | §12-1 の digest 開始境界が対応表と矛盾 | **採用** — **マーカーの直後(マーカー自身を含めない)に統一** |
| 10 | 証跡表の追随漏れ(「7 点」・「唯一の補足」) | **採用** — 対応表を 16 行で作り直し |

**採用 5 件 / 射程縮小により解消・委任 5 件 / 不採用 0 件。**

### 記録: レビュアが自分でエスカレーション条件を計算していた

**判定行の直前に `7.3-6 の起因過半条件は2周目・3周目で連続成立` と書いてきた。**
**依頼文に 7.3-6 の算式は入れていない**(重大度定義と応答様式のみ)。
**前周までの件数は依頼文に書いていたため、そこから計算したと思われる。**
→ **周回の推移を依頼文に書くと、レビュアがゲートの状態そのものを評価する。**

---

## 確定ゲート 4 周目(2026-09-13・sol xhigh・**射程変更直後の全文レビュー**)— **否決 `P0` 0 / `P1` 6 / `P2` 1**

**件数が 9 → 7 に減った。** **P0 は 4 周連続ゼロ。**

### エスカレーション判定(射程縮小でリセット後)

| 周 | 母集団 | 起因 | `起因 × 2 > 母集団` |
| --- | --- | --- | --- |
| 3 周目 | 9 | 9 | 真 → **PO 裁定・リセット** |
| **4 周目** | **6** | **6** | **真(リセット後 1 回目)** |

**2 周連続ではないため続行。5 周目も真なら再び PO 裁定へ。**

### 指摘の内訳 — **実体の欠陥 3 件 / 追随漏れ 4 件**

| # | 要旨 | 重大度 | 種別 | 採否 |
| --- | --- | --- | --- | --- |
| 1 | **封印要素の内容・適用範囲の変更を禁止していない** — 既存 ID の範囲を後から広げれば、要素の追加・削除なしに新しい不合格を「列挙済み」へ取り込める。**粗い包括要素を最初から置く方法も排除されない** | `P1` | **実体** | **採用** |
| 2 | **段階 2 の判定後に適用する規範が残っていない** — 判定方法を実装へ委ねたが、**部分追加を段階 2 と判定できても、それを免除対象外にする条件がない**。「実装が条件を緩められない」は**拘束対象を持たない** | `P1` | **実体** | **採用** |
| 3 | **D-7 の (b)①③ 行に「未解消」「授権が有効」の限定がない** — **50 マージ後に失効しても、一度解消した事項が再発しても免除が継続**し、`BOOT-GRANT` の無条件復帰と `BOOT-SEAL-MONOTONE` に矛盾する | `P1` | **実体** | **採用** |
| 4 | **射程宣言が縮小前のまま** — 「未達 / 違反 / 債務」の語彙確定・「未達 0 かつ債務 0」・旧負例構成を「本改訂で確定する範囲」に列挙した後に縮小内容を追記しており、**相反する 2 つの射程が同じ行に残っている** | `P1` | 追随 | **採用** |
| 5 | **承認済み plan.md が射程縮小へ追随していない** — DoD が撤去済み語彙・段階 2 の厳密減少・旧計数方式を必須としており、**現行の正本案を満たすと承認済み計画に違反し、計画を満たすと PO 裁定に違反する** | `P1` | 追随 | **採用** |
| 6 | **design.md が部分的にしか追随していない** — 撤去済みの 4 型分類・`未達数＋債務数`・`BOOT-VOCAB`・`BOOT-COUNT`・`BOOT-SEAL-SCOPE`・10 点列挙が残り、**対応表だけが縮小後の 16 行**。**証跡内部が矛盾し追随証明として使用できない** | `P1` | 追随 | **採用** |
| 7 | **見直しトリガーが撤去済み語彙を使用** | `P2` | 追随 | **採用** |

**採用 7 件・不採用 0 件。**

### 実体の 3 件をどう塞いだか

**指摘 1** — `BOOT-SEAL` の粒度の拘束を **1 つから 3 つへ**:

> **①識別子は安定であり、改名によって同一要素が別要素として数えられてはならない**
> **②要素が指す事項の内容および適用範囲は、封印後に変更してはならない**(**範囲の変更は要素の追加と同じく `BOOT-SEAL-MONOTONE` により不合格**)
> **③要素は D-11 が要求する宣言の 1 件、または (b) が要求する事項の 1 件に対応しなければならない**(**包括要素を置いてはならない** — 束ねると**解消が部分的にしか進まない状態を「未解消 1 件」と数え続けられ、`BOOT-STALL` の測定が意味を失う**)

**指摘 2** — `BOOT-GRANT` へ**段階 2 の適用除外**を条文で置いた:

> **`ADR-003` 帰結 1 の段階 2 に該当する変更については、本規定を適用しない** — **同段階 3 が定めるマージ条件がそのまま適用される。**
> **したがって段階 2 と判定された変更において、当該対象に係る不合格は封印集合に列挙されていても免除されない。**
> **判定方法は実装が定める**(**判定できない場合は段階 2 として扱う** — fail-closed)。

**判定方法を委ねても、判定後に適用する規範は正本が持つ**。これが 3 周目の委任で抜けていた。

**指摘 3** — D-7 の 6 行すべてに**授権の限定**を入れた(「`BOOT-GRANT` が有効であるあいだに限り」「軸②の失効中および段階 2 の変更では免除されない」)。

### ◎ 3 周連続で「証跡が正本に追随していない」と指摘された

**2 周目 指摘 9 / 3 周目 指摘 7・8 / 4 周目 指摘 6。**

**原因は条文案の二重管理**である。design.md §9-1 が**条文の全文を持っていた**ため、
**正本を直すたびに §9-1 も直す必要があり、3 周とも漏れた。**

→ **§9-1 から条文の全文を撤去し、「どこに何を置いたか」のみを持つ形へ改めた**(設計書 7.1-1 の複製禁止)。
**条文そのものは正本を参照する。**

**これは `H-85` と同型の構造**(1 つの変更が複数の資産の更新へ拡大する)だが、
**こちらは「複製をやめる」だけで解消できる**点が違う。

### 接続先への転送(**母集団に算入しない**)

| 要旨 | 転送先 |
| --- | --- |
| **`TSK-235` の design.md が縮小後の正本へ未接続** — 宣言有無による分類・`BOOT-VOCAB`・旧計数方式・`未達数＋債務数`・旧 10 点構成を現行規範として参照している。**委任された 4 件の受け手が縮小後の正本へ未接続** | **TSK-235** |

### 6 周警告(7.3-6)に達したときの申し送り(2026-09-14・master からの依頼)

**7.3-6 は「`確定ゲート周回` が 6 に達したら、周回・重大度推移・起因比の一覧を PO へ提示する」と定める。**
**master(TSK-317 PR #3)から、それに加えて伝えてほしい事項を受領した:**

> **TSK-355 が 6 周警告に達して PO が打ち切りや射程再縮小を裁定すると、要件書の改訂範囲が変わり、
> PR #3 が乗る digest も変わる。そのときは中身の変化(要件書の blob が動くか否か)を一言もらえると助かる。**
> **「6 周に達した」という事実だけより、そちらのほうが効く。**

→ **6 周警告の提示時には、次を併記する**:

| 伝える事項 | 理由 |
| --- | --- |
| **要件書の blob が動くか否か** | **`requirement-claims.json` の digest が動くかが決まる**。PR #3 の封印手順の前提 |
| **`ADR-003` / 設計書の改訂範囲が変わるか** | 同上(ただし両書は入力資産ではないため影響は小さい) |
| **射程再縮小の場合、何を委任へ移すか** | **委任先(TSK-235)の作業量が変わる** |

**「事実だけでなく、他タスクの前提が変わるかを伝える」** — **接続先への転送と同じ考え方**である
(2026-09-12 に TSK-235 と相互に約束した「接続先の文書に出た指摘は転送する」の拡張)。

**あわせて master から確認結果を受領**(2026-09-14・すべて develop で再現):

- **reseal 系オプションは 3 つだけ**(`--reseal` :5000 / `--reseal-derived` :5005 / `--reseal-oracle` :5010)。
  **`--skip-derived` も `--skip-oracle` も存在しない**(**⚠ 2026-09-14 に訂正 — 実在する。
  `help=argparse.SUPPRESS` で隠れていた**)。`:4851` の `dedicated_flag` も `--reseal-oracle`
  → **PR #3 の封印手順は「段 2 = `--reseal` のみ / 段 4 = `--reseal-derived` のみ / 段 7 = `--reseal-oracle` のみ」で書く**
  (**先方いわく「指摘が無ければ計画書に誤ったコマンドを書いていた」**)
- **`_validate_object_path_uniqueness` は `:556` に実在**し、**`:4775` で `oracle seal.input_assets` に対して呼ばれている**
  → **PR #2 で塞がれているという整理も一致**
- **セッション ID の突合も再現** — TSK-379(`f8d5489`)と TSK-386(`d2379d1`)がともに
  `session_01VuxQorz5FMmci7J6mB3UnF`。**`ListAgents` の名前では特定できないため、
  `git log -1 --format=%b <branch>` の `Claude-Session` で判別する方法を採る**(先方も同じ方法へ移行)

---

## 確定ゲート 5 周目(2026-09-14・sol xhigh・差分再レビュー)— **否決 `P0` 0 / `P1` 7 / `P2` 1**

**`P0` は 5 周連続ゼロ。** **全件採用・不採用 0 件。**

### エスカレーション判定

| 周 | 母集団 | 起因 | `起因 × 2 > 母集団` |
| --- | --- | --- | --- |
| 4 周目 | 6 | 6 | 真(リセット後 1 回目) |
| **5 周目** | **7** | **6** | **真 — 2 周連続** |

**指摘 5 のみ非起因**(`BOOT-ACTIVATION` の正常系は初周から欠けていた)。
→ **7.3-6 により再び PO スコープ裁定へ上げる。**

### ◎ 最も重い指摘 — **deny-all 実装が条文を全部通る**(指摘 5・非起因)

> **`BOOT-ACTIVATION` が要求する正例は自動昇格と失効だけ。**
> **正当な封印要素について実際にマージを許す `BOOT-GRANT`、失効後の正当な再承認で授権が回復する
> `BOOT-REAPPROVAL`、全要素が揃ったとき実際に発効する `BOOT-ACTIVATION` の正常系がない。**
> **したがって、すべての PR・再承認を拒否する deny-all 実装でも負例と 2 つの正例を満たせる。**
> **fail-closed を維持したまま、経過規定が実際に使用可能であることを担保できていない。**

**これは 4 周目に私が「自動昇格を誤って負例に入れていた」と気づいた件の裏返しである** —
**負例を直し、正例を 2 つ足したが、「本規定が実際に機能すること」の正例を足していなかった。**

→ **要求④を新設**: **封印要素に由来する不合格を含む変更が実際にマージ可能となること /
失効後の正当な再承認で授権が実際に回復すること / 要求が揃ったとき実際に発効すること。**

### ◎ 指摘 4 — **自己点検の道具に、点検対象と同じ欠陥があった**

**2026-09-13 に「`BOOT-STATE` は不合格とする事象を持たない」と判定し、`kind` を `補足` へ落とした。**
**誤りだった** — **`BOOT-STATE` は「同じ状態に 2 つの終了条件を持たせない」という禁止事象を持つ。**
**私の検出語リストに「持たせない」が入っていなかった。**

**これは §2-1 で走査基準を 2 度作り直したのと同じ型である**(**マーカー 6 種では強制点を覆えなかった**)。
**同じ誤りを、同じタスクの中で 2 度踏んだ。**

→ **`BOOT-STATE` を `規範` へ戻した**(`kind = 補足` は 2 件へ)。**検出語を 12 語へ拡張**したが、
**この一覧も全数である保証はない**(§2-7 と同じ限界)。

**`TSK-235` へ「`BOOT-STATE` に依拠しないでください」と伝えた件を撤回する必要がある。**

### 採否記録

| # | 要旨 | 重大度 | 起因 | 分類 | 採否 | 反映 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **段階 2 の適用除外の量化範囲が矛盾** — 「段階 2 の**変更**には適用しない」と「**当該対象に係る**不合格だけ免除外」が同居。**前者だと最初の対象を追加する PR が他の未解消対象の不合格で止まり、段階 2 を進められない** | `P1` | 起因 | (A) | **採用** | **除外を対象単位に限定**。「**同一の変更に含まれる他の対象に係る不合格は従来どおり免除される**」を明記 |
| 2 | **判定不能 → 段階 2 の fail-closed が発効 PR を止める** — 基盤を初めて導入する PR の base には安定 ID 体系もマニフェストも無く、**判定不能に該当しうる**。**発効 PR 自身から授権が外れ発効できない** | `P1` | 起因 | (A) | **採用** | **「発効前に存在しなかった資産を基準として判定できないことは『判定できない場合』に当たらない。比較元に当該資産が存在しない場合は空集合として扱い、判定可能とする」**を追加 |
| 3 | **拘束③でも包括要素を排除できない** — D-11 の宣言は多層構造で、「宣言の 1 件」が**対象計算の宣言全体とも個々の配列要素とも解釈できる**。「対象 A の `entrypoints[]` 完全性」を 1 事項として封印すれば包括要素が形式上通る。**一意性・単射条件も無い** | `P1` | 起因 | (A) | **採用** | **③を「独立に解消の成否を判定できる最小の事項」へ書き換え**、**配列は要素 1 件**と明記。**④一意性(要素と事項は一対一)**を新設 |
| 4 | **`BOOT-STATE` の自己点検と `BOOT-ACTIVATION` が矛盾** — 「持たせない」は禁止事象であり、規範と読むなら補足へ落としてはならない | `P1` | 起因 | (A) | **採用** | **`規範` へ戻した**(上記) |
| 5 | **`BOOT-ACTIVATION` は正常系の実効性を閉じていない** — **deny-all 実装が通る** | `P1` | **非起因** | (-) | **採用** | **要求④を新設**(上記) |
| 6 | **design.md の全面追随が未完了** — §6 に旧定義「債務 = 未宣言対象」、§7-3 に「10 点 + 負例」、§9-3 の要旨に「債務台帳」「未達」「既知債務」 | `P1` | 起因 | (-) | **採用** | **9 箇所を追随**(うち 7 件は経緯の記録として残す) |
| 7 | **plan.md は DoD だけ変わり、スコープと実装ステップが旧計画のまま** — 「やること」が「未達・違反・債務」の語彙化を要求し、ステップ 2 の合格条件が「4 型の語彙」「M-1〜M-5 の全回答」。**承認済み計画の内部で相反する 2 つの実装契約** | `P1` | 起因 | (-) | **採用** | **§2 やること 3 を撤回・4 を封印集合の語へ / ステップ 2 の合格条件に射程縮小の注記 / §2 末尾に「射程宣言が優先する」注記** |
| 8 | **件数の記録が追随していない** — 「補足は 2 件」「(e) は 14 条項」「封印の 4 規律」(`BOOT-SEAL-BASE` と「比較元の固定」の重複計上) | `P2` | 起因 | (-) | **採用** | **封印は 3 規律へ是正**。対応表は **16 行 / 規範 14・補足 2** で整合を機械確認 |

### PO 裁定(2026-09-14・山田正輝)— **続行の明示指示**(7.3-6)

**7.3-6 の発火条件が 2 つ同時に成立したため PO へ提示した**:

| 条件 | 状態 |
| --- | --- |
| **起因過半が 2 周連続**(`起因 × 2 > 母集団`) | **4 周目 6/6・5 周目 6/7 で成立** |
| **`確定ゲート周回` が 6 に達する** | **現在 5。次の反映で 6** |

**提示した材料**:

| 周 | 判定 | P0 | P1 | P2 | 起因 | 内訳 |
| --- | --- | --- | --- | --- | --- | --- |
| 初周(全文) | 否決 | 0 | 8 | 1 | 0 | — |
| 2 周目(差分) | 否決 | 0 | 8 | 1 | 8 | — |
| 3 周目(差分) | 否決 | 0 | 9 | 1 | 9 | → **PO 裁定(射程縮小)** |
| 4 周目(全文) | 否決 | 0 | 6 | 1 | 6 | **実体 3 / 追随 4** |
| 5 周目(差分) | 否決 | 0 | 7 | 1 | 6 | **実体 4 / 追随 3** |

**裁定**: **続行の明示指示。** 7.3-6 の表により **カウンタ・コミットは発生せず、連続カウントを 0 にリセットする**
(**編集なし・指示を worklog に記録**)。**次周は差分再レビューを続行する。**

**判断の材料となった事実**:

- **`P0` は 5 周連続ゼロ**
- **起因の内訳が変わっている** — 4 周目は**追随漏れ 4 件**が過半だったが、
  **その原因(条文の二重管理)を構造ごと潰した**結果、5 周目は**実体 4 / 追随 3** へ反転した
- **追随 3 件はいずれも「射程縮小の波及」**であり、**新しい欠陥ではない**
  (design.md の旧規範・plan.md のスコープ・件数の記録)

**7.3-6 の「6 周警告」は次周の反映で発火する。** **そのときは改めて提示する**
(**本裁定は 6 周警告そのものへの裁定ではない** — 発火前に起因過半で上げたため)。

**master への申し送り**(2026-09-14 の依頼): **本裁定では要件書の改訂範囲は変わらない。**
**`requirement-claims.json` の digest が動く前提はそのまま**であり、**PR #3 の順序判断に影響しない。**

---

## 確定ゲート 6 周目(2026-09-14・sol xhigh・差分再レビュー)— **否決 `P0` 0 / `P1` 6 / `P2` 0**

**`P0` は 6 周連続ゼロ。`P2` が初めて 0 件。** **全件採用・不採用 0 件。**

### エスカレーション判定

| 周 | 母集団 | 起因 | `起因 × 2 > 母集団` |
| --- | --- | --- | --- |
| 5 周目 | 7 | 6 | 真 → **PO 裁定(続行指示)でリセット** |
| **6 周目** | **6** | **6** | **真(リセット後 1 回目)** |

**2 周連続ではないため続行可能。** ただし **`確定ゲート周回` が 6 に達する**ため **6 周警告が発火する**。

### ◎ 最も重い指摘 — **正例を作ったが、接続していなかった**(指摘 4)

**5 周目で要求④(本規定が実際に機能することを示す正例)を新設したが、
`BOOT-ACTIVATION` の発効条件は「機構と負例」の接続だけを列挙していた。**

> **③④の正例は存在要求に留まる。未実行・非必須経路の正例を置き、必須経路を deny-all にする読みが残る。**

**deny-all を塞ぐために作った要求④が、deny-all を塞げていなかった。**
**「存在すること」と「必須経路で実行されること」の差**である。

→ **発効条件を「①〜④のすべてが同一の変更で CI の必須経路へ接続され、実行された時点」へ改めた。**
**あわせて自己点検で見つけたツリー基準の明示も同時に入れた**(下記)。

### 自己点検の結論を条文へ入れた(6 周目の依頼文には含めていなかった事項)

**`TSK-235` との往復で「接続された時点がツリーかマージか未定義」と判明し、実測で決着した:**

- **`required_status_checks` は存在しない**(Free + private で rulesets API 403 — 設計書 10.2)
- **運用は「PR の最新 HEAD SHA に対して CI の全ジョブが green」**(`github-setup.md` 2 章)

→ **条文へ明示**: 「**当該変更のツリーにおいて、設計書 10.1 が定める CI の全ジョブに当該機構・負例・正例が
含まれ実行されることをいう。リポジトリ側の必須チェック設定の有無を問わない。**」

**最後の一文は、ブランチ保護が将来導入されたときに読みが変わるのを防ぐ**
(**現時点で利用不可であることに依存した条文にしない**)。

### 採否記録

| # | 要旨 | 重大度 | 起因 | 分類 | 反映 |
| --- | --- | --- | --- | --- | --- |
| 1 | **対象単位化が D-7 に追随していない** — 前半 4 行は**変更全体**を免除外とし、後半 2 行には**段階 2 の除外自体が無い**。前半では他対象の封印済み不合格までマージを止め、後半では追加対象自身の不合格を免除できる | `P1` | 起因 | (-) | **6 行すべてを対象単位へ統一** |
| 2 | **拘束③④でも原子単位が一意に決まらない** — D-11 は `directTargets[]` の 1 要素がさらに `components[]` を持つ**多層構造**。「配列なら要素 1 件」を外側へ適用すれば composite 全体、内側へ適用すれば component ごとになる | `P1` | 起因 | (A) | **「最も内側の配列の要素 1 件」へ特定**。例示(`directTargets[]` が `components[]` を持つ場合は後者が単位)を添えた |
| 3 | **空集合例外の境界が閉じていない** — 「比較元」が候補 PR の base か `BOOT-SEAL-BASE` の固定 OID か未特定。かつ**履歴的条件を発効 PR だけに限定していない**ため、**発効後に比較資産が欠落した場合の扱いが分岐する** | `P1` | 起因 | (A) | **比較元を `BOOT-SEAL-BASE` と特定**し、**例外を発効前に限定**。「**発効後に欠落した場合は判定不能として段階 2 に倒す**」を明記(**資産を消して判定を回避する経路を閉じる**) |
| 4 | **要求④の正例が発効条件へ入っていない** | `P1` | 起因 | (-) | **上記** |
| 5 | **design.md の追随が未完了** — §7-1 が要求を 3 件と列挙 / §6 と対応表が粒度 3 拘束 / **`M-1` が比較資産不存在を無条件 fail** とし新しい空集合境界と矛盾 / **`M-4` が失効中の第二の授権**を置き `BOOT-GRANT` と両立しない | `P1` | 起因 | (-) | **4 要求へ / 4 拘束へ / `M-1` に空集合境界を追記 / `M-4` に「正本の規範ではない」注記** |
| 6 | **plan.md に縮小前の現行契約が残る** — §4-4 が 4 型分類・旧発効要素・`未達数＋債務数` を「条文が内蔵すべき答え」とし、ステップ 2 が「封印の 4 規律」を合格条件に残す | `P1` | 起因 | (-) | **§4-4 へ失効注記**(論点そのものは現行条文に生きていることを対応表で示した)/ **ステップ 2 を 3 規律 + 4 拘束へ** |

**採用 6 件・不採用 0 件。**

### 記録: **塞いだつもりのものが塞げていない**型が 2 周続いた

| 周 | 作ったもの | 塞げていなかった理由 |
| --- | --- | --- |
| 5 周目 | **要求④**(deny-all を塞ぐ) | **存在要求のままで、接続要求になっていなかった** |
| 4 周目 | **拘束③**(包括要素を塞ぐ) | **「宣言の 1 件」が多層構造で一意に決まらなかった** |

**どちらも「条文を足したこと」で満足し、「その条文が実際に効くか」を点検していない。**
**`TSK-235` の 4 類型でいう「性質表明」を、条文の側でやっていた** —
**要求を置いたが、要求が機能することを確かめていない。**

### 6 周警告(7.3-6)の提示と PO の判断(2026-09-14・山田正輝)— **続行**

**`確定ゲート周回` が 6 に達したため、7.3-6 の規定により周回・重大度推移・起因比を PO へ提示した。**

| 周 | 種別 | 判定 | P0 | P1 | P2 | 起因 | 起因比 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 全文 | 否決 | 0 | 8 | 1 | 0 | 0% |
| 2 | 差分 | 否決 | 0 | 8 | 1 | 8 | 100% |
| 3 | 差分 | 否決 | 0 | 9 | 1 | 9 | 100% → **射程縮小** |
| 4 | 全文 | 否決 | 0 | 6 | 1 | 6 | 100% |
| 5 | 差分 | 否決 | 0 | 7 | 1 | 6 | 86% → **続行指示** |
| 6 | 差分 | 否決 | 0 | **6** | **0** | 6 | 100% |

**不採用は全周を通じて 0 件。** **PO 裁定は 3 回**(射程縮小 / 続行指示 / 本判断)。

#### 提示に添えた限界(`TSK-235` との往復で確認した点)

> **「P0 ゼロ」が示すのは「P0 と判定されたものが無い」までであり、「構造の穴が残っていない」ではない。**

**実例**: **5 周目の deny-all は `P1` でありながら構造的な穴**であった(初周から欠けていた)。
→ **重大度と構造性は別の軸。** **収束の判断材料に重大度だけを使わない。**

#### 収束していない理由 — **同じ型が 2 周続いた**

| 周 | 作ったもの | 塞げていなかった理由 |
| --- | --- | --- |
| 4 周目 | **拘束③**(包括要素を塞ぐ) | 「宣言の 1 件」が**多層構造で一意に決まらなかった** |
| 5 周目 | **要求④**(deny-all を塞ぐ) | **存在要求のままで、接続要求になっていなかった** |

**どちらも「条文を足したこと」で満足し、「その条文が実際に効くか」を点検していない。**
**`TSK-235` の 4 類型でいう「性質表明」を、条文の側でやっていた。**

#### 判断

**続行。** **7.3-6 は「判断を求めるが強制打ち切りはしない」と定めており、本判断はカウンタに影響しない。**

**続行の根拠**: **`P2` が初めて 0 件**になり、**6 周目の 6 件中 3 件は追随**(design.md / plan.md)で
**実体は 3 件**。**指摘が条文の核心部分へ集中している。**

**懸念として記録**: **起因比がほぼ 100% で 5 周続いている。** **同型の前例 `TSK-278` は 27 周。**

#### master への申し送り(2026-09-14 の依頼に対する回答)

**本判断で要件書の改訂範囲は変わらない。** **`requirement-claims.json` の digest が動く前提はそのまま**であり、
**`TSK-317 PR #3` の順序判断に影響しない。**

---

## 確定ゲート 7 周目(2026-09-14・sol xhigh・差分再レビュー)— **否決 `P0` 0 / `P1` 5 / `P2` 0**

**件数が 6 → 5 へ。** **`P0` 7 周連続ゼロ・`P2` 2 周連続ゼロ。** **全件採用・不採用 0 件。**

### ◎ 実験の答え — **「条文を足したが効くか点検していない」型は 3 周連続で成立した**

**依頼文で今回足した 4 つの条文を名指しし、「書いてあるが動かせない・目的を達しない箇所」を問うた。**

| 足した条文 | 結果 |
| --- | --- |
| **発効条件①〜④の接続・実行要求** | **★指摘 2 — 量化先が誤り** |
| **「最も内側の配列の要素 1 件」** | **★指摘 1 — composite と両立しない** |
| 空集合例外の発効前限定 | **指摘なし**(レビュアが明示的に「独立した指摘はない」と記載) |
| D-7 の 6 行の対象単位除外 | **指摘なし**(本文自体は是正済み。ただし証跡が未追随 = 指摘 3) |

→ **4 件中 2 件。** **3 周連続(4 周目の拘束③・5 周目の要求④・7 周目の 2 件)で同じ型が出た。**

**観点を与えたことで「どの条文が該当するか」が名指しで返ってきた** — **観点なしの周では
「この条文は効かない」という形でしか出ず、どれを点検すべきかの手がかりが無かった。**

### 指摘の内訳 — **実体 2 件 / 追随 3 件**

| # | 要旨 | 重大度 | 起因 | 分類 | 反映 |
| --- | --- | --- | --- | --- | --- |
| 1 | **「最も内側の配列要素」が D-11 の composite target と両立しない** — D-11 は `directTargets[]` の composite 全体を**共通の呼び出しと段間の値連鎖を持つ 1 つの直接呼び出し対象**として扱う。**外側の `invocation` や段間連鎖の不合格には「最も内側の要素」が無く、component 単体は composite を通す生の検査結果なしに独立解消できない** | `P1` | 起因 | (A) | **「D-11 が独立した検査の対象として扱う最小の単位」へ改めた**。**composite として束ねているものは束ね全体を 1 要素、束ねの外側の関係は別の 1 要素**。「最も内側」という表現を撤回 |
| 2 | **`BOOT-ACTIVATION` の量化先が誤り** — 「**全ジョブに**機構・負例・正例が含まれ実行される」を逐語で読むと、`backend` / `frontend` / `secrets` / `docs-lint` **すべてで同じ機構を実行する**ことになる。**未導入ジョブや不採用の `win-setup` まで含む** | `P1` | 起因 | (A) | **「ジョブのうち少なくとも 1 つに配線され、当該ジョブが PR ごとに実行され、その失敗がマージを妨げる扱いであること」**へ。**「全ジョブへ重複して配線することを求めるものではない」**を明記 |
| 3 | **D-7 は 6 行とも対象単位へ是正されたが、design.md と plan.md が変更全体の除外を維持** — **前周の停止問題を再導入する** | `P1` | 起因 | (-) | **両方を対象単位へ** |
| 4 | **発効条件の追随が実装委任・設計要約・DoD で未完了** — 要件書末尾の委任は「機構と負例」だけ、design.md の要旨と対応表も旧条件、plan.md の DoD は要求④と「実行済み」を欠く。**旧委任・旧 DoD に従えば deny-all を塞がない実装でも完了扱いにできる** | `P1` | 起因 | (-) | **4 箇所を①〜④へ**。委任事項④に「**それらをどの CI ジョブへ配線するか**」を追加 |
| 5 | **粒度拘束の追随も未完了** — 条項 ID 表と写像表が「粒度の 3 拘束」のまま | `P1` | 起因 | (-) | **4 拘束へ** |

**採用 5 件・不採用 0 件。**

### 追随漏れを機械検出で潰した

**3 周連続で追随漏れが出ていた**ため、**旧記述のパターン 6 種を全文検索して全数を潰した**:

```
段階 2 の変更では免除されない / 段階 2 の変更には適用されない / 粒度の 3 拘束 /
機構と負例の具体的な構成 / 機構と負例の同一 / 全条項の判定機構と負例
→ 反映前 8 件 → 反映後 0 件(機械確認)
```

**個別に直すのではなく、パターンで全数を取るほうが確実である** — **3 周とも「直したつもりで残っていた」。**

### 封印集合の規模 — `TSK-235` の実測(2026-09-14)

**先方が開始時点の未履行要素数を実測した。約 136 件。**

```
対象計算 13 件 × D-11 の宣言 10 種              = 130
トップレベル(propertyCatalog / 表示対応宣言)   =   2
(b) が要求する事項(既存の未宣言入口ほか・概算) =   4
                                         合計  ≈ 136
```

**決め手は「不在の配列は展開できない」こと**(先方の分析):

> **不在の `entrypoints[]` は「1 宣言 = 1 要素」**であって、**将来入るはずの n 要素へは展開されない**。
> **存在しないものの要素は数えられない。**

→ **「配列要素単位への変更で 3 桁がさらに増える」は成立しない。**
**移行開始時点では全対象が未登録でほぼすべての配列が不在**のため、
**7 周目で変えた粒度(composite の束ね方)も件数にほとんど効かない。**

### 条文の側からの確認

**先方から「封印集合は開始時点で固定で、段階 2 で配列が実在しても増えない」という
理解の確認を受け、条文で裏づけた。**

| 条項 | 逐語 | 帰結 |
| --- | --- | --- |
| `BOOT-SEAL-MONOTONE` ① | **封印集合へ要素を追加することは不合格** | **136 件のまま最後まで残る** |
| `BOOT-PROGRAM` | **宣言の更新のみをもって解消としてはならない。生の検査結果で充足を示す** | **「不在」の要素は、実在かつ充足が示された時点で解消** |

→ **配列の各要素まで封印集合が展開されることはない。**

#### ◎ 含意 — **中身の不備は封印の外に落ちる**

**開始時点**: 「対象 A の `entrypoints[]` が不在」= **1 要素**
**段階 2 の後**: A の `entrypoints[]` は実在するが**中身が不完全**(1 件だけ宣言・実際は 3 入口)

- **封印集合には「不在」の要素しかない** → **中身の不備は封印集合に無い**
- `BOOT-SEAL`: **列挙されていないものは一切免除しない**
- → **D-11 ③ の「宣言と実在の差が 1 件でもあれば fail」が免除なしでそのまま効く**

**「宣言を置けば解消、中身は後回し」ができない。** **そもそも不完全な宣言では解消が成立しない**
(`BOOT-PROGRAM` が生の検査結果を要求するため)。

→ **`TSK-235` の実装では「`entrypoints[]` を空でとりあえず置く」ことができない。**
**置くなら完全に置く。** **先方へ申し送り済み。**

---

## 確定ゲート 8 周目(2026-09-14・sol xhigh・差分再レビュー)— **否決 `P0` 0 / `P1` 5 / `P2` 0**

**`P0` 8 周連続ゼロ・`P2` 3 周連続ゼロ。** **全件採用・不採用 0 件。**
**非起因が 2 件**(指摘 4・5 — 以前の周から残っていた論点)。

### ◎ 前周のパターン検索が捕捉できていなかった(指摘 2)

**7 周目で「旧記述のパターン 6 種を全文検索し、反映前 8 件 → 反映後 0 件」と報告した。**
**しかし 8 周目で「旧粒度が残っている」と指摘された。**

| 検索語(7 周目に使った 6 種) | 捕捉できなかった表現 |
| --- | --- |
| 段階 2 の変更では免除されない / 段階 2 の変更には適用されない / 粒度の 3 拘束 / 機構と負例の具体的な構成 / 機構と負例の同一 / 全条項の判定機構と負例 | **「最も内側の配列の要素 1 件」**(7 周目に自分で撤回した表現)/ **「D-11 が要求する宣言の 1 件」** |

**検索語を自分で選ぶ以上、選ばなかった表現は残る。** **`BOOT-STATE` の検出語漏れ(5 周目)と同じ型。**
**同じ誤りを 3 度目に踏んだ**(走査基準 §2-1 / 検出語 §7-5 / パターン検索 7 周目)。

### 検出方法を変えた — **「旧記述を探す」から「正本の現行表現が証跡にあるか」へ**

**旧記述の列挙は網羅できない**(自分が何を書いたかを思い出す必要がある)。
**正本の現行表現は一意に取れる**(条文から機械抽出できる)。

```
正本の現行キーフレーズ      要件書  design  plan
粒度(D-11 が独立した…)        1      3      0 → 3
量化(少なくとも 1 つに配線)     0*     0 → 1  0
要求数(①〜④)                3      4      2
段階 2(対象単位)             1      4      2

* 全角スペースの差で 0 と出た。実際は在る(表記ゆれの検出も課題)
→ 「正本に在って design.md に無い表現」= 0 件を機械確認
```

**この方法なら「自分が何を書いたか」に依存しない。** **ただし表記ゆれは捕捉できない**(上記 `*`)。

### 採否記録

| # | 要旨 | 重大度 | 起因 | 分類 | 反映 |
| --- | --- | --- | --- | --- | --- |
| 1 | **粒度規則が単位を一意に定められない** — D-11 では `invocation`・段間連鎖が**同じ `directTargets[]` 要素を構成**し証跡直積も `directTargetId` 単位だが、**component ごとの hash と各段間の値連鎖も個別検査**する。「composite 1 件」「composite + invocation + 連鎖」「連鎖を辺ごと」が**すべて成立**し、拘束④の一対一とも衝突 | `P1` | 起因 | (A) | **本周は条文を変えず、design.md の追随のみ**。**単位の一意化は実装へ委ねた事項**(委任①)であり、**正本は「D-11 が独立した検査の対象として扱う最小の単位」という導出規則を与える**。**D-11 側の改訂を要するなら射程外**(plan.md「やらないこと」) |
| 2 | **粒度規則の design.md 追随が未完了** — §6 が「最も内側の配列の要素 1 件」を現行として掲げ、§6-4 は拘束 3 件、`M-5` も旧表現。**「旧記述 0 件」の検索がこの表現を捕捉していない** | `P1` | 起因 | (-) | **3 箇所を追随**。**検出方法を変更**(上記) |
| 3 | **発効条件①〜④の追随が未完了** — §7-1 が「機構と負例の接続だけで発効」と定義し**直後の①〜④表と矛盾**。§7-3・条項一覧・写像表・plan.md §4-9 も旧条件 | `P1` | 起因 | (-) | **5 箇所を①〜④へ**。§7-1 へ量化先の説明も追加 |
| 4 | **`M-4` 是正が依存箇所まで閉じていない** — `M-4-d` を非規範としたが、**`M-4-e` と正例が撤回済み規定によって再承認 PR・未解消削減 PR・無関係 PR をマージ可能とする記述を維持** | `P1` | **非起因** | (-) | **`M-4-e` にも非規範の注記**。**正本 `BOOT-GRANT` は失効中に第二の授権を持たない**ことを明記 |
| 5 | **封印比較元のコミットが未確定** — plan.md §4-9 が固定 SHA を**「発効コミット」**とするが、**発効は PR の `develop` 統合時点**であり、**merge commit の OID を当該 PR のツリーへ事前収録できない** | `P1` | **非起因** | (A) | **「発効コミット」という読みを撤回**。**どのコミットを比較元とするかは実装へ委任**(`BOOT-SEAL-IMMUTABLE` により**当該 PR から変更不能・更新には正本の改訂**) |

**採用 5 件・不採用 0 件。**

### 指摘 1 の扱い(記録)

**レビュアは「実装が分岐する」と指摘したが、単位の一意化は射程縮小で実装へ委ねた事項である**
(委任①: 封印集合の要素の粒度・識別子・要素数の算出方法)。

**正本が与えるのは導出規則**(「D-11 が独立した検査の対象として扱う最小の単位」)**であり、
その規則を D-11 へ適用した結果が一意になるかは D-11 の構造の問題**である。
**D-11 を改訂して単位を明示するのは本改訂の射程外**(plan.md「やらないこと」= `ADR-003` の
D-11 の 4 不変条件そのものを変更しない)。

→ **本周は条文を変えず、`TSK-235` へ「D-11 から導出する際に単位が分岐しうる」ことを申し送る。**
**先方が実装で確定し、必要なら D-11 の改訂を別タスクとして起票する。**

### 封印要素の粒度の決着(2026-09-14)— **`TSK-235` が A で確定。私の見立て B は誤りだった**

**8 周目 指摘 1**(粒度規則が単位を一意に定められない)を委任①として `TSK-235` へ申し送り、
**先方が D-11 の原典を読んで A(composite 全体で 1 要素)を確定した。**

#### 原典の逐語(こちらでも確認)

> 最終段の出力(3 段 = 最終表示文字列 / 2 段 = 写像適用後の表示名)**までを 1 つの直接呼び出し対象とする**。
> **証跡直積の軸は従来どおり `directTargetId` 単位**(**要素を 3 つに分けない** — 5 判定の証跡・経路一致検査の要求集合は不変)。

**D-11 が「要素を 3 つに分けない」と明示している。** **私が申し送りで推した B(composite + `invocation` + 連鎖 = 3 要素)は、まさにこれに反する。**

#### 私の誤りの中身

**条文の「束ねの外側にある関係は別の 1 要素」という文言から B を導いたが、
この文言が想定しているのは「D-11 が別の検査対象として扱っているもの」であり、
D-11 が明示的に「分けない」と書いているものを分けてよいという意味ではない。**

**「条文が与えるのは導出規則で、適用結果は D-11 が決める」と自分で申し送りに書きながら、
D-11 を読まずに見立てを述べた。**

**先方の理由 (2) も正確**: **値の連鎖の検証には両端の実行結果が要り、
それは `invocation` による composite の実行からしか得られない**
→ **拘束③(独立に解消の成否を判定できる最小の単位)に照らして辺も `invocation` も独立解消できない。**

#### ◎ レビュアの指摘が本タスクの文脈では空振りだった(先方の (4))

> **移行開始時点では `directTargets[]` 自体が不在。**
> **封印される要素は「対象 A の `directTargets[]` が不在」の 1 件で、composite の内部構造は存在しない。**
> **A/B/C のどれでも同じ。**

→ **8 周目 指摘 1 の「未解消件数と `BOOT-STALL` の失効時点が分岐する」は、本タスクでは成立しない。**

**ただし条文は変えない** — **条文は一般形で書かれており、「開始時点に composite が部分的に存在する」
場合には効く**。**本タスクでその状況が生じないことは、条文を弱める理由にならない。**
**指摘は条文の一般形に対して正しいので不採用にはしない。**

#### 先方が 2 度示した構造

| 場面 | 結論 |
| --- | --- |
| 封印集合 136 件の実測 | **不在の配列は展開できない** → 配列要素単位でも件数は増えない |
| A/B/C の選択 | **不在の composite に内部構造は無い** → どれでも同じ |

**共通するのは「粒度の議論は、実在する構造に対してのみ意味を持つ」**という点。
**ただし一般化には限界がある** — **本タスクは「移行開始時点で全対象が未登録」という特殊な状況**であり、
**「開始時点が空である移行では、粒度の設計が観測不能になる」**という限定つきでしか書けない。

**D-11 の改訂は不要**と先方が判断した。**別タスクの起票なし。**

---

## 確定ゲート 9 周目(2026-09-14・sol xhigh・差分再レビュー)— **否決 `P0` 0 / `P1` 4 / `P2` 0**

**件数が 5 → 4 へ。** **`P0` 9 周連続ゼロ・`P2` 4 周連続ゼロ。** **全件採用・不採用 0 件。**

### ◎ 様式不備で 2 回中断した — **依頼文が原因と切り分けた**

**同じ依頼文で 2 回とも「監査を完了しました」と述べた直後に判定行を返さず終了した。**

| 回 | レビュアの発話回数 | 出力行数 | 判定行 |
| --- | --- | --- | --- |
| 1 | 5 回 | 9,526 | **無し** |
| 2 | 7 回 | 10,402 | **無し** |
| **3(観点を絞る)** | — | **3,259** | **有り** |

**7.3-5 により 1・2 回目は周に算入していない**(`確定ゲート周回` は 8 のまま)。

**3 回目で変えたこと**:

| | 1・2 回目 | 3 回目 |
| --- | --- | --- |
| 依頼文 | 6.0KB | **2.9KB** |
| 観点 | 5 つ(全条項の追跡・初期の周の掘り起こしを含む) | **3 つ**(反映 5 件に限定) |
| 対象 | 3 文書 + worklog | **design.md と plan.md のみ** |
| 判定行 | 様式として要求 | **冒頭で「指摘 0 件でも必ず判定行を返せ」と明示** |

**分かったこと**: **観点の「数」ではなく「1 つあたりの追跡範囲」が上限を決める。**
**8 周目は観点 5 つで完走したが、9 周目は同じ 5 つでも「条項 12 件 × 参照箇所の全数追跡」を
含めたため発散した。** **レビュア自身が「条項 ID ごとに追跡します」と宣言してから止まっており、
追跡を宣言した時点で出力予算を使い切る構造だった。**

→ **`TSK-235` の実験(観点を与えると名指しで返る)に、限界が 1 つ加わった** —
**観点は有効だが、1 つあたりの追跡範囲に上限がある。**

### 採否記録

| # | 要旨 | 重大度 | 起因 | 分類 | 反映 |
| --- | --- | --- | --- | --- | --- |
| 1 | **非規範化の範囲が曖昧** — `M-4-e` に「以下は縮小前の設計の記録」と書いたが**対応する旧内容が無く、後続の `M-4-f` / `M-4-g` まで非規範とする読みが成立**する。両項は現行 `BOOT-REAPPROVAL` の規範 | `P1` | 起因 | (A) | **「ここまでが非規範(`M-4-d` と `M-4-e` の 2 項のみ)」**の区切りを挿入 |
| 2 | **正例が撤回済み規定を根拠にしている** — `M-4` の正-2・正-3 が**非規範化した `M-4-d` / `M-4-e` を根拠に失効中の変更を「マージ可」**としたまま。**`BOOT-GRANT` の「失効中は達成条件が無条件復帰」と矛盾** | `P1` | **非起因** | (-) | **正例表を全面差し替え**。**失効中に何がマージできるかは正本が定めない**ことを明記 |
| 3 | **発効条件の追随が残る** — plan.md §4-4 論点 3 が**④の正例と実行要件を欠き、昇格・失効を誤って「負例」としている**。design.md §8-5 の要旨も旧条件 | `P1` | **非起因** | (-) | **両方を①〜④へ**(昇格・失効を**正例**へ是正) |
| 4 | **私が委任を新設していた** — plan.md §4-9 が「どのコミットを比較元とするかは実装が定める」としたが、**正本 `BOOT-SEAL-BASE` は選択規則を固定**しており、**委任 4 項目にも比較元の選択は含まれない** | `P1` | 起因 | (A) | **委任を撤回**。**正本が固定していることを明記** |

**採用 4 件・不採用 0 件。**

### ◎ 指摘 4 — **8 周目の是正が行き過ぎていた**

**8 周目 指摘 5**(封印比較元が「発効コミット」と読まれる)への反映で、
**「発効コミット」という読みを撤回する際に「実装が定める」まで書いた。**

**しかし正本は `BOOT-SEAL-BASE` で選択規則を固定している。**
**撤回すべきは「発効コミット」という読みだけで、選択規則そのものは正本にある。**

→ **是正が行き過ぎて、正本に無い委任を証跡に新設した。**

**逆引きの限界がもう 1 つ判明した** — **逆引きでは「正本に在って証跡に無い」しか取れず、
「証跡に在って正本に無い」は取れない。** **双方向で測る必要がある。**

---

## 確定ゲート 10 周目(2026-09-14・sol xhigh・差分再レビュー)— **否決 `P0` 0 / `P1` 1 / `P2` 1**

**指摘が 2 件まで減った**(4 → 2)。**`P0` 10 周連続ゼロ。** **全件採用・不採用 0 件。**

### 採否記録

| # | 要旨 | 重大度 | 起因 | 分類 | 反映 |
| --- | --- | --- | --- | --- | --- |
| 1 | **`M-4-b` が正本に無い保存方式を固定していた** — 9 周目に「非規範は `M-4-d` と `M-4-e` のみ」と区切ったことで、**`M-4-b`(再承認は移行状態の宣言ファイルに記録する)が現行規範として残った**。しかし**正本 `BOOT-REAPPROVAL` は記録先を定めておらず、委任④の範囲**である。**証跡だけが保存方式を限定していた** | `P1` | 起因 | (A) | **`M-4-b` も非規範へ**。**非規範は `M-4-b` / `M-4-d` / `M-4-e` の 3 項**へ更新 |
| 2 | **plan.md §4-4 論点 3 の文が破綻** — 「接続され、実行された**が**同一変更で……接続された**ときにのみ**」と**条件を重複**させていた | `P2` | 起因 | (-) | **重複を解消** |

**採用 2 件・不採用 0 件。**

### ◎ 観点 3 が機能した — **「証跡に在って正本に無い」を 1 件捕らえた**

**9 周目 指摘 4**(私が委任を新設していた)を受けて、**10 周目の依頼文へ観点 3 を新設した**:

> **証跡に、正本に無い規範が書かれていないか。**
> **正本より緩い・厳しい・正本に無い条件を証跡だけが課しているもの。**

→ **`M-4-b` を捕らえた。** **逆引き(正本の現行表現が証跡にあるか)では取れない方向**であり、
**双方向で測る必要があることが実証された。**

**なお指摘 1 は 9 周目の是正が生んだもの**である — **非規範の範囲を明示したことで、
明示しなかった項が「規範として残る」ことが顕在化した。** **是正が別の箇所を照らし出した形。**

### 推移

| 周 | P0 | P1 | P2 | 合計 |
| --- | --- | --- | --- | --- |
| 6 | 0 | 6 | 0 | 6 |
| 7 | 0 | 5 | 0 | 5 |
| 8 | 0 | 5 | 0 | 5 |
| 9 | 0 | 4 | 0 | 4 |
| **10** | **0** | **1** | **1** | **2** |

---

## 確定ゲート 11 周目(2026-09-14・sol xhigh・差分再レビュー)— **否決 `P0` 0 / `P1` 3 / `P2` 0**

**`P0` 11 周連続ゼロ・`P2` 5 周連続ゼロ。** **全件採用・不採用 0 件。**
**3 件とも「証跡が正本に無い規範を課している」型**(10 周目に新設した観点 3 の横展開)。

### ◎ 観点 3 が 3 件を捕らえた — **10 周目の 1 件は氷山の一角だった**

**10 周目に `M-4-b` を捕らえたため、11 周目の依頼文で「同種のものが `M-1`・`M-2`・`M-3`・`M-5` に無いか」
と横展開を頼んだ。** **結果 2 件が追加で出た**(`M-2-e` と、plan.md 側の 3 箇所)。

| # | 要旨 | 重大度 | 起因 | 分類 | 反映 |
| --- | --- | --- | --- | --- | --- |
| 1 | **`M-4-b` の是正が plan.md へ波及していない** — design.md で非規範にしたが、**plan.md §4-10 が「記録場所を定める」を `M-4` の回答として要求したまま**。**ステップ 2 が `M-1`〜`M-5` を「`TSK-235` が満たすべき要求」として扱うため、保存方式を再び拘束できる** | `P1` | 起因 | (A) | **§4-10 の `M-*` 表へ失効注記** |
| 2 | **`M-2-e` が履歴の保存先を固定** — 正本 `BOOT-REPORT` が要求するのは**未解消要素一覧の機械可読な出力まで**で、**履歴の保存先は定めていない**。**Git 履歴からの再構成も別の状態資産も可能**であり、**委任②の範囲**。**`M-4-b` と同型** | `P1` | **非起因** | (A) | **`M-2-e` を非規範へ** |
| 3 | **plan.md §4-9 が旧指標を現在形で定めている** — 段階 2 の PR に **`未達数＋債務数` の厳密減少**を課し、同じ旧指標を停滞判定にも使うとしている。**同書の DoD 自身がこの語彙・厳密減少・原子単位を撤回済みとしている**。**歴史記録としての非規範表示が無いため `TSK-235` を旧方式で拘束し得る** | `P1` | **非起因** | (-) | **§4-9 論点 B へ失効注記**(`N` = 50 とマージ本数で測ることは現行も同じ旨を併記) |

**採用 3 件・不採用 0 件。**

### レビュアが「無い」と明示した範囲(記録)

> **`M-4-a` / `M-4-c` / `M-4-f` / `M-4-g` の規範性は今回の区切りで維持されている。**
> **plan.md §4-4 論点 3 の重複解消にも新たな欠陥はない。**
> **`M-3` に同種の独自規範は認められない。**

**「見た範囲で無い」を明示してもらうと、次周で同じ箇所を再確認せずに済む。**

### 非規範と明示した `M-*` 項(現在 4 件)

```
M-2-e  履歴の保持場所      → 委任②
M-4-b  承認記録の記録場所   → 委任④
M-4-d  失効中に許される範囲 → 正本は「無条件復帰」のみ
M-4-e  再承認 PR の扱い     → M-4-d に依存していたため
```

**いずれも「正本が定めていない保存方式・手続を証跡が固定していた」もの。**
**射程縮小で正本から撤去した事項が、証跡の `M-*` に残り続けていた。**

### 推移

| 周 | P0 | P1 | P2 | 合計 |
| --- | --- | --- | --- | --- |
| 8 | 0 | 5 | 0 | 5 |
| 9 | 0 | 4 | 0 | 4 |
| 10 | 0 | 1 | 1 | 2 |
| **11** | **0** | **3** | **0** | **3** |

**10 周目に減ったのは、観点 3 が新設された直後で 1 件しか出なかったため。**
**11 周目で横展開して 3 件に戻った** — **収束の判断に単調減少を使えない例。**

---

## 確定ゲート 12 周目(2026-09-14・sol xhigh・差分再レビュー)— **否決 `P0` 0 / `P1` 4 / `P2` 1**

**`P0` 12 周連続ゼロ。全件採用・不採用 0 件。** **自己走査で 2 件を追加で発見**(合計 7 件反映)。

### ◎ 観点 3 が §5 の位置づけそのものへ届いた

**11 周目は個々の `M-*` 項を非規範化したが、12 周目の指摘 1・2 は「`M-1` と `M-5` は
まるごと委任事項を再確定している」と述べた。** **正本は委任 4 項目について「判定方法を実装へ
委ねる」と明言している**ため、**証跡が必須命題として書けば `TSK-235` は別の方法を選べない。**

→ **項目単位の非規範化では足りない**と判断し、**§5 の柱書へ「規範性の区別」を新設**した:

```
〔正本〕 正本の逐語から導ける。実装は満たさなければならない(典拠を併記)
〔一案〕 正本が委任した範囲の設計案。満たし方の 1 つであり、別の方法を選べる
```

**plan.md §4-10 の「`TSK-235` が満たすべき要求は §5 が正」は〔正本〕の命題に限る**と限定した。

| # | 要旨 | 重大度 | 起因 | 分類 | 反映 |
| --- | --- | --- | --- | --- | --- |
| 1 | **`M-1` が委任③(段階 2 の PR の判定方法)を再確定** — base/head の実在集合差分・paths 判定の禁止・2 対象 PR の一律 fail まで必須命題にしていた。**plan.md §4-10 が「委任へ移った」と書きながら直後に「§5 が正」と述べ、証跡内で矛盾** | `P1` | 非起因 | (A) | **§5 柱書へ規範性の区別を新設**。`M-1-a`/`M-1-c`=〔一案〕・`M-1-b`=〔正本〕・`M-1-d` は分割。**plan.md §4-10 の注記も限定** |
| 2 | **`M-5` が委任①②を固定し、封印要素と対象計算を混同** — 「封印集合 − 実在集合」で差し引き、対象が 4 成果物と 3 層検査を満たすまで実在しない方式だった。**正本の要素は対象計算より細かく**(1 対象に複数要素)、**要素単位の独立解消が成立しない**ため、**解消時点・`BOOT-STALL` のリセット・自動昇格時点が正本と分岐する** | `P1` | 非起因 | (A) | **`M-5-b`/`M-5-c` を全面書き換え**(要素単位・独立解消)。**対象単位の実在集合方式は〔一案〕ですらなく正本と矛盾するため撤回**。正例・負例・不成立条件も要素単位へ |
| 3 | **`M-2` に撤回済みの二計数が残存** — `M-2-d` と正例が「未達 0 かつ債務 0」「未達が 1 減った」。**`M-2-e` の非規範注記の外側**にあり §5 の位置づけがそのまま適用される | `P1` | 非起因 | (-) | **`M-2-d`・正例・§4-1・§4-2・§7-2 を「未解消件数」へ統一**。**§3 へ語彙の現行対応を追記**(残る「未達」「債務」は封印集合の未解消要素を指す・二計数は撤回済み) |
| 4 | **plan.md §4-3 が免除対象を旧方式へ再限定** — 「移行状態が緩めるのは宣言と実在の一致部分だけ」を現在形で保持し、**この切り分けを条文へ写すよう要求**していた。現行は性質を問わず列挙のみで決まる | `P1` | 非起因 | (-) | **§4-3 へ失効注記**。**緩めないのは列挙されていない事項**であって条文の語形ではない旨を明記 |
| 5 | **§4-9 の新注記の条項帰属が不正確** — 「50 回で失効」を `BOOT-STALL` に帰属させたが、**失効条件そのものは `BOOT-GRANT`**、`BOOT-STALL` は**エポック開始点・母集合・優先順位**を定める | `P2` | **起因** | (-) | **帰属を 2 条項へ分けて書き直し**(正本の逐語で確認) |

**採用 5 件・不採用 0 件。**

### 自己走査で追加発見した 2 件(レビュー指摘とは別枠 — 7.6-1 黙殺禁止により記録)

**検出方法**: 8 周目に反転した「正本の現行表現が証跡にあるか」の**逆向き** —
**「射程縮小で撤去した語彙・条項 ID が証跡に現在形で残っていないか」。**

| # | 要旨 | 反映 |
| --- | --- | --- |
| 自-1 | **撤去済み条項 `BOOT-VOCAB` を分類の根拠として現在形で参照**(§2-6-2)。**同書 §12 が自ら「撤去した条項」に挙げている** | 根拠を `BOOT-SEAL` へ置換 |
| 自-2 | **撤去済みの 4 型語彙(型番号)を 9 箇所で参照**。うち **`§3-1 の型 4` は参照先に型 4 が存在しない**(§3 は射程縮小で全面改稿) | 型番号を記述語へ置換し、**§3 へ「型番号は撤去した」と明記** |

**`§3-3` が「報告の語彙としては残る」と書いているのは記述語のことであり、型番号ではなかった。**
**自分の書いた注記を、自分で広く読みすぎていた。**

### 推移

| 周 | P0 | P1 | P2 | 合計 | 起因 / 母集団 |
| --- | --- | --- | --- | --- | --- |
| 9 | 0 | 4 | 0 | 4 | 2 / 4 |
| 10 | 0 | 1 | 1 | 2 | 0 / 1 |
| 11 | 0 | 3 | 0 | 3 | 1 / 3 |
| **12** | **0** | **4** | **1** | **5** | **0 / 4** |

**7.3-6 のエスカレーションは不発火**(`起因 0 × 2 > 母集団 4` は偽。11 周目も `1 × 2 > 3` は偽)。
**起因が 0 件になったのは 12 周で初めて** — **今周の 5 件はすべて、射程縮小(3 周目後)の波及漏れが
より深い層から出てきたもの**であり、直前の反映が生んだ欠陥ではない。
