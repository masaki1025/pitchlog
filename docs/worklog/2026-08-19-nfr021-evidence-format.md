---
date: 2026-08-19
topic: Phase 4-4 失効対象パスの正本 + docs/ops/nfr021-acceptance/ の README・テンプレート
branch: feature/nfr021-evidence-format
---

# 作業ログ: 2026-08-19 Phase 4-4 失効対象パスの正本 + docs/ops/nfr021-acceptance/ の README・テンプレート

## やったこと

- /task-start: 引数「4-4」を設計書 13 章の論理スロット 4-4 と同定。Notion を検索して**既存タスクが無いことを確認**したうえで新規起票 → `feature/nfr021-evidence-format` ブランチ + worktree 作成(origin/develop = `f38d518` 起点)→ 計画書雛形・本 worklog 作成 → Notion を 進行中 へ

- /investigate: 調査サブエージェント 3 本(spec-checker / legacy-analyst / decision-tracer)を並列実行 + Web 技術調査を /research(Codex `research`)へ委任 → 4 本を原典と自分の実測で裁定して [research.md](../features/nfr021-evidence-format/research.md) へ統合

## 決定

- slug は `nfr021-evidence-format`(4-5 の検証器〔`nfr021-*` 系〕と区別できるよう「証跡の書式を決める」側であることを名前に出した)

## 調査で判明した重要事項

- **4-4 は設計を発明する仕事ではなく「10.1 を実務書へ落とす」仕事**。証跡の必須項目(要件 11 + 機械検証キー 7)・失効対象 15 件・allowlist 3 件・命名・文書分類はすべて正本が確定済み。ただし **10.1 に規定が無い穴が 10 件**あり、うち **6 件は 4-5 の検証器が分岐するため 4-4 で決めるしかない**
- **【実証】2 点差分では失効判定が取りこぼす** — `D:640` は「`tested_commit_sha` から候補 SHA までの**差分**」としか書かない。本リポジトリの実履歴 `HEAD~25..HEAD` で、2 点差分 193 パスに対しコミットごとの和集合は 194 パス。差の 1 件は **`frontend/src/lib/display_geometry_263_v1.json`**(4-1 で追加 `2152e78` → 4-3 で削除 `d21620b`。**両方が範囲内なので 2 点差分に一切現れない**)。座標契約の逐語移植ファイルであり `contracts/` は失効対象そのもの。**机上の懸念ではなく既に起きている**
- **【実測】ツールチェーン定義の実在位置が `D:640` の列挙と噛み合わない** — `.python-version`・`pyproject.toml`・`uv.lock` はルートと `backend/` の 2 本、`package.json`・`pnpm-lock.yaml` は **`frontend/` にのみ**存在。ファイル名だけの列挙を「直下のみ」と読むと `frontend/` 側を丸ごと取りこぼす。**`mise.toml`(Node 版の正本)は列挙に無い**
- **【実測】`check_docs_status.py` が証跡ディレクトリで落ちる** — `scripts/check_docs_status.py:24-27` が「当該ディレクトリが生まれた時点で除外規則を追加すること」と予告しているが未実装で、**担当スロットが正本に割り当てられていない**。遅くとも予約レコード監査 PR までに手当てが要る
- **予約レコードの書式・命名・`attempt_id` の形式は 10.1 に無い**(`D:641` は結果証跡の命名のみ)。改変禁止のレコードなので **4-4 の確定ゲートで固めないと監査 PR で救済不能**(`D:812`)
- **README は規範を引き取ってはならない** — 「詳細を README へ委譲する」案は **2026-08-12 の PO 裁定で否決済み**(`docs/worklog/2026-08-11-req-v1-9-nfr021-wsl2.md:107`)。要旨 + 参照 + 10.1 に無い実務手順に留める
- **否決済み 7 案の輪郭を反映周コミットの件名から復元した**(`7f52c79`〜`0d4719e`)。「マーカー」「スロットキー」「親台帳」系は 3 度否決されており、**同型の案を 4-4 で再提案しない**

## 調査エージェントの誤りを実測で訂正

- **spec-checker と legacy-analyst が独立に「`.env.example` は存在しない」と報告したが、実際には追跡済みで実在**(`git ls-files` で確認)。原因は `.claude/settings.json` の `Read(./.env.*)` 拒否で、エージェントからは存在しないように見えること。`secret_guard.py:35` は exact `.env.example` を明示許可しており、**フックの設計意図と permissions の実効が食い違っている**(4-2 からの申し送りと同一事象)
- **従来の評価より重い**: 台帳候補 (1) の昇格条件は「読めないことで作業が止まる事例」だが、実際に起きたのは**誤った事実が調査結果として流通する**という別種の害。昇格判断は PO・別タスク

## 未決・次の一歩

- 次は /plan。**人間裁定を仰ぐ論点は research.md 6-1 節に 6 件**(優先順): ①「差分」の定義 ② パターン構文とパス解決範囲 ③ 予約レコードの書式・命名・`attempt_id` ④ テンプレートの枚数とキーの置き場 ⑤ 索引カバレッジ除外の担当 ⑥ 送り出し 2 件(`phase4_base_sha` のキー化 / Notion 機械化)を 4-4 で閉じないことの明示
- **H-68 の教訓を持ち込んだ**: (A) 4-4 で決めないと 4-5 が分岐する 6 件と、(B) 4-5 / 4-6 の実装時に決めれば足りる 4 件を分類済み。**(B) を計画書で閉じようとしない**
- **台帳候補(本調査で新規)**: `git_guard` が空リポジトリ(未コミット)での git 操作をすべて遮断するため、**サンドボックス内で git 挙動の検証実験ができない**。今回は本リポジトリの実履歴で代替できたが、代替不能な検証もあり得る(H-69 と同型)
