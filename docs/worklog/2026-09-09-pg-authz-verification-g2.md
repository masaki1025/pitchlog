---
date: 2026-09-09
topic: PostgreSQL 認可構成の実機検証 第 2 群・第 3 群(TSK-317 — TSK-270 計画の改訂 2)
branch: feature/pg-authz-verification-g2
---

# 作業ログ: 2026-09-09 PostgreSQL 認可構成の実機検証 第 2 群(TSK-317)

## やったこと

- `/task-start 317` — worktree `../pitchlog-worktrees/feature-pg-authz-verification-g2` を
  `origin/develop`(`1424d67`)から作成。Notion TSK-317 を `進行中` へ。
- `/investigate` — 調査サブエージェント 3 本(要件突合 / 決定経緯 / 資産実測)+ 原典の直接確認。
  `research.md`(401 行)に統合。
- `/plan` — 計画書と詳細設計を起草し、**コア領域の敵対レビューを 5 周**通した。
- **PO 裁定 8 件**(`D-1`〜`D-8`)を取得。

## 決定

- **feature ディレクトリは新設した**(`pg-authz-verification-g2`)。
  当初は `docs/features/pg-authz-verification/` の再利用を試みたが、**現在地導出が縮退した**ため撤回。
  `scripts/feature_status.py:1491-1503` の `expected_feature_slug` は **plan の期待パスをブランチ名から導出する**
  ため、期待パス以外に `branch:` が一致する plan があると `plan 重複` になる。旧ブランチが残っていて
  同名を切れないので、TSK-335・TSK-342 と同じ **1 タスク = 1 ディレクトリ**へ揃えた。
- **前計画書の内容を複製しない**(設計書 7.1-1)。`R-1`〜`R-8` と第 1 群の実測は
  `../pg-authz-verification/plan.md` が正で、1 節から相対リンクで参照する。

### PO 裁定(2026-09-09・山田正輝)

| # | 論点 | 裁定 |
| --- | --- | --- |
| `D-1` | 第 2 群の射程 | 凍結資産の全量をやる(cut set だけに絞らない) |
| `D-2` | `search_path` の P0 の直し方 | 本タスクの先頭ステップで直す → **`D-5` が置き換えた** |
| `D-3` | DDL 適用器の実装手段 | **psycopg 直書き**(TSK-343 を待たない) |
| `D-4` | oracle の裁定待ち 2 件 | **frozen 値を確定として承認**(8 と 29)+ 要件の「7 操作」との 1:N 写像を明示 |
| `D-5` | 正本側の未処理 3 件 | **TSK-348 として v0.2 の改訂ゲートを 1 本立てる**(本タスクの射程外) |
| `D-6` | 187 件の status 変更と oracle の封印の衝突 | **統合ステップへ集約**(コミット構造は `S-1` が置き換えた) |
| `D-7` | 正本の同一トランザクション要求と資産の 03/04 分割 | **適用器側で同一 TX に収める**(資産も正本も変えない) |
| `D-8` | 計画レビューが収束しない(7.3-6 の発火) | **第 2 群をさらに分割** — 承認範囲をステップ 1〜21 とし、引き渡しと封印は改訂 3 へ |

## 計画レビューの採否記録(敵対レビュー 5 周・全 53 件採用・不採用 0 件)

| 周 | 判定 | 前周修正起因 | 採否 | 主な反映 |
| --- | --- | --- | --- | --- |
| 1 | 要修正 P0×7 / P1×8 | 0 / 15 | **全 15 件採用** | 正本改訂のゲート区分を確定ゲート事項と認めて射程外へ / body の自己 oracle 化 / 返却契約の三者不一致 / 移行バッチロールの無所有 / **1 トランザクション契約の撤回** / `R-4` の弱体化を戻す / reseal の順序 |
| 2 | 要修正 P0×2 / P1×11 / P2×1 | **10 / 14** | **全 14 件採用** | **`auth-catalog` は `input_assets` で `oracle_commit` に固定** / 正本の同一 TX 要求 vs 資産の分割 / **`AUTH-*` は実在しない** / `contract_only` の受取先は本タスク自身 / 失敗注入点と 6 前提の資産を新設 / DSN を増やさない |
| 3 | 要修正 P0×2 / P1×8 / P2×1 | **8 / 11** | **全 11 件採用** | **`source_commit` の自己参照が構築不能** / `enforcement_test_owner.id` は pytest node ID でない / `req-universe.json` に `list_item` が 0 件 / 失敗注入点は `step_id` だけでは重複可 / MC/DC の判定母集合が自己申告 → **7.3-6 が発火し `D-8` で分割** |
| 4 | 要修正 **P0×0** / P1×10 | **9 / 10** | **全 10 件採用** | 凍結資産不変の判定基準を `origin/develop...HEAD` の 15 パス列挙へ / **MC/DC の主張を撤回**(注記の網羅性は機械では閉じない)/ 失敗注入点を exact-set から実在 + 重複禁止へ / `R-3` の封鎖方式を戻す / claim 158 行 vs 一意 ID 152 個 / `S-2` の射程拡大 / `S-9` 追加 |
| 5 | 要修正 P0×0 / P1×4 /(B) 0 件 | 3 / 4 | **全 4 件採用** | `P1-2` の反映漏れ(`[機械]` に注記網羅性が残っていた)/ **`S-9` の件数は 178**(158 + probe_executable 20)/ ステップ番号の残存 5 箇所 / 検証コマンドの `cd` 二重パス |

**打ち切りと承認**: 5 周目は P0×0 かつ **(B) 分類 0 件**で、4 件はすべて**反映漏れと誤記**(方式の分岐なし)。
是正して**人間の承認を取得**(2026-09-09・山田正輝)。**周回は 5**(5 周目も反映を伴ったので計上)。

### 実測が 4 周連続で記述を覆した

**凍結資産を実測しないと分からない事実**が毎周出た。字面と設計の突合だけでは足りなかった。

| 周 | 実測 |
| --- | --- |
| 2 | `TX:PROVISIONING.atomic` は **`false`** / `AUTH-*` は **0 件**(全 187 件が `CATALOG:*`)/ `auth-catalog` は `input_assets` で `oracle_commit` 上の blob との一致まで要求される |
| 3 | `enforcement_test_owner.id` は **`::` を 0 件**(pytest node ID でない)/ `req-universe.json` に `list_item` は **0 件** |
| 4 | `contract_only` の claim 158 行に対し一意な `runtime_test_owner.id` は **152 個**(6 組が共有) |
| 5 | `receiving_task_id: TSK-270-GROUP-2` は **178 件**(`contract_only` 158 + `probe_executable` 20) |

### 正本側に見つかった欠陥(TSK-348 へ)

1. **【P0】`data-model.md` 3-2 節の `search_path` 契約が、実測で「誤り」と判定された記述のまま**
   — `REJ-003` の `candidate_statement_status: incorrect` / `observed_result:
   temporary_relation_hijack_returned_attacker_rows`。反証は 2026-08-31(PR #33)にあり、正本の approved は
   2026-09-09 で、**9 日古い実測が反映されていなかった**。TSK-342 の確定ゲート 13 周が捕まえなかった理由は
   反証が `contracts/authz/` にあってレビュー射程外だったことと、**正本側が `pg_temp` という語を 1 度も
   使っていない**こと(実測 0 件)。**台帳 `H-79` の型**。
2. **越境関数の返却契約が三者不一致** — 正本 3-6 節は「返す列を集計値に限定」と「集計は関数に書かない」を
   併記し、凍結資産は `aggregation_contract: none`(業務行を返す)。
3. TSK-342 の打ち切りで残した、定義の言い換えによる重複。

## 起票した Notion タスク

| ID | 内容 |
| --- | --- |
| **TSK-348**(高) | `data-model.md` v0.2 の改訂ゲート — 上記 3 件。**本タスクのマージの前提**(DoD 項目) |
| **TSK-349**(中) | 移行バッチ用ロールの実機検証と退役検査 — `data-model.md:289` の検査の所有者を実 ID で閉じた |

## 未決・次の一歩

- **`/implement` でステップ 1 から委任する。** 承認範囲は**ステップ 1〜21(実体を作る)**。
- **ステップ 4 以降は実 PostgreSQL が必要** — `PITCHLOG_TEST_ADMIN_DSN` と `PITCHLOG_TEST_ROLE_DSN` の
  実値が要る(未設定は skip ではなく `pytest.fail`)。実測で `docker compose config` が `POSTGRES_USER`
  欠落で失敗し、`psql` は未導入。**実値の用意は人間の作業**(`NFR-014`・設計書 12.1)。
  ステップ 1〜3 は SQL とコードだけなので DB 無しで進められる。
- **改訂 3 が満たすべき要件 `S-1`〜`S-9`** を計画書 4 節に記録済み(3 周分の指摘を失わないため)。
  ステップ 21 が終わった時点で `status` を `active` のまま改訂し、再レビューを通す。
- **申し送り(/pr で起票)**: ADR-003 `D-12` と `contracts/authz/` の位置づけ / アプリ用ロールの `DELETE` /
  `H-57` のジョブ行 / **`AUTH-*` と `CATALOG:*` の ID 体系の食い違い**(TSK-250 の計画書が `AUTH-*` を期待) /
  `closure-handoff-data-model.json` の「作成」と正本 `:2750` の「作成・実行」の差。
- **ハーネス運用評価台帳への追記の判断は `/pr` のクローズ処理で行う**(本タスクの経過は候補が複数ある —
  とくに「**凍結資産を実測しない計画レビューは 4 周連続で誤った前提を通す**」型と
  「**自作の母集合は自己申告になり、機械で縛ろうとすると無限後退する**」型)。


## TSK-235 のタブからの申し送り(2026-09-10)— 受領と実測

**申し送り B(TSK-317 宛)の 3 件を受領し、すべて実測で確認した。**

### 【1】`guard_paths` への未登録 — **指摘は 4 本だが実測は 6 本**

申し送りはステップ 9 までの 4 本を挙げていたが、**ステップ 14 で 2 本増えて 6 本**である。

| 未登録のパス | 新設したステップ |
| --- | --- |
| `scripts/check_authz_function_bodies.py` | 2 |
| `tests/test_check_authz_function_bodies.py` | 2 |
| `scripts/check_shared_preconditions.py` | 9 |
| `tests/test_check_shared_preconditions.py` | 9 |
| `scripts/check_failure_injection_points.py` | 14 |
| `tests/test_check_failure_injection_points.py` | 14 |

**現行の `guard_paths` は 30 件**で、**`check_design_propagation` / `check_doc_coverage` /
`check_processing_stages` をスクリプトとテストの対で個別列挙**している。
**命名規約上この 6 本も入る系列**であり、**未登録のままでは凍結資産を検査するスクリプトを
弱めても逐行確認が発火しない**(台帳 `H-12` の型)。

→ **計画書の `S-8`(改訂 3 の要件)へ実測 6 本を明記した。**
**`guard_paths` 追加は 6.3 規則⑤(敵対レビュー + 人間承認)**なので改訂 3 の射程である。

### 【2】harness の予算基線 — 測定条件を記録する

申し送りが「run id 付きで残してほしい」と要求しているので、**測定条件を明記して記録する**。

| 項目 | 値 |
| --- | --- |
| **測定値** | **1171 passed**(ステップ 14 時点) |
| **コミット** | `a9f28fa` |
| **測定日** | 2026-09-10 |
| **実行環境** | **ローカル**(WSL2 / Linux 6.6.114.1-microsoft-standard-WSL2)。**CI runner ではない** |
| **コマンド** | `uv run pytest tests/`(worktree のルートで) |
| **run id** | **なし**(ローカル実行のため)。**CI の run id は本タスクの PR 作成後に付く** |
| root tests への増分 | **+691 行**(`test_check_authz_function_bodies.py` 256 / `test_check_shared_preconditions.py` 204 / `test_check_failure_injection_points.py` 231) |

**申し送りが引いていた基線は 1144 passed / 285.07s @ `75b6cd3`**(TSK-235 のブランチ)。
**本タスクのマージ後に失効する**という見立てのとおりである。
**CI の run id 付きの数値は PR 作成時に記録する。**

### 【3】`auth-catalog.json` の owner は 2 種だった — **私の TSK-250 宛の申し送りを訂正する**

**実測**:

| owner | status |
| --- | --- |
| **`catalog_test_owner`** | **187 件すべて `implemented`** |
| **`enforcement_test_owner`** | **187 件すべて `planned`**(`TSK-270` 169 / `TSK-312` 18) |

**私が TSK-250 へ送った申し送り【3】は後者だけを見ていた。**
申し送り B の指摘どおり、**TSK-250 の計画書ステップ 19 の合格条件
「全 `AUTH-*` に DDL 要素とテスト ID がある」は、文字面では前者(既に `implemented`)で
通ってしまう**。

ただし同計画書 5 節(B の信頼境界・3 周目 `P0-3` の採用)が exact-set を
**「要件安定 ID → AUTH 主張 → 関数の全シグネチャ・ロール・期待結果 → テスト ID」**と定義しており、
**「期待結果」まで結ぶ以上これは enforcement 側**である。

→ **私が「開始条件 B が改訂 3 まで完了を意味する可能性がある」と書いた点は、
計画書の文言から支持される読みへ格上げできる。**
→ **計画書の `S-2` へ owner 2 種の実測を追記し、受取側が enforcement 側を見ることを
`S-6` の受取契約で固定する**ことにした。


---

## 台帳候補(`/pr` のクローズ処理で提出する)— 2026-09-10 時点の確定形

**セッション間で分担を確定した**: **TSK-317 の `/pr` で私が出す**。
理由(`tsk235-dsl` の判断に同意)= ① TSK-355 は 7.3 の確定ゲートを通す正本改訂で、
**射程宣言(7.3-7)に無関係な項目を混ぜるとゲートの周回が増える**(同型の TSK-278 は 27 周)。
しかも TSK-355 は TSK-235 のクリティカルパスなので遅らせる要素を足せない。
② `tsk235-dsl` の `/pr` は TSK-355 の approved → 計画の再改訂 → 敵対レビュー再実施 →
人間承認 → 28 ステップの後で、**数週間先**。③ **台帳は気づいた時点で出すのが機能する形**。

### 候補 A: `feature_status.py` の allowlist が `merge_allowed` を落とす(分類漏れ)

- **分類**: 機構の穴
- **検出日**: 2026-09-10(TSK-343 が発見・TSK-235 と TSK-317 が独立に原典追認)
- **事象**: `scripts/feature_status.py` は 2 親マージを **`merge_allowed`**(develop 取り込み)と
  **`merge_unknown`** に分類する(`:878-881`)。**`merge_unknown` を名指しで弾く専用分岐がある**
  (`:987-991`・`note="許可されないマージコミット混在"`)。ところが **`:999` の allowlist は
  `{"planning", "documentation"}` の 2 種のみ**で、**`merge_allowed` がそこを通れず `unknown` に落ちる**
- **設計意図の根拠**: **「許可されないマージ」を名指しで弾く分岐が別にある以上、
  許可マージは通す設計意図だった**と読める。意図的な仕様ではなく**分類漏れ**
- **非対称の追加根拠**: **3 つの `unknown` 経路のうち `merge_allowed` 経由だけ `note` が付かない** —
  `:986-991` は `note="許可されないマージコミット混在"`、`:993-997` は `note="無記法の実装コミット"`、
  **`:999-1000` の `Progress(kind="unknown", total=table.total)` だけ note 引数が無い**。
  利用者には「実装状況: 不明」しか出ず**原因にたどり着けない**
- **実害**: `feature/data-model-canonical`(TSK-250)が**ステップコミット 0 件・develop マージ 1 件**で
  「実装状況: 不明」を表示している。本来は **`completed == 0` の分岐(`:1446`)で
  「実装前(全 52 ステップ)」**と出るはず
- **観測の限界**: **単発だが 3 セッションが独立に追認**。隔離実験はしていない
- **昇格条件 / 対応案**: allowlist へ `merge_allowed` を追加、または `unknown` の条件を絞る。
  **`note` の付与も併せて行う**

### 候補 B: DB テストの強制終了が teardown を飛ばし、残骸が fail-closed で全 DB テストを落とす

- **分類**: 機構の穴 / 運用手順の欠落
- **検出日**: 2026-09-10(TSK-317 のステップ 15 で **4 回**)
- **事象**: DB テストがタイムアウトや強制終了で止まると **fixture の teardown が走らない**。
  残るのは **① 使い捨てクラスタの Docker コンテナ ② 開発 DB の `pitchlog_test_role`**。
  **② が致命的** — `conftest.py:205-211` は「**被検査ロールがテスト開始前から存在する**」を
  `pytest.fail` にする(fail-closed)ので、**残骸 1 本で以後の全 DB テストが error になる**
- **復旧手順(どの正本にも無い)**:
  `docker ps -aq --filter 'name=pitchlog-authz' | xargs -r docker rm -f` /
  `DROP OWNED BY "<role>"` → `DROP ROLE "<role>"`(**順序が逆だと依存で失敗**)
- **観測の限界**: **1 タスク内で 4 回**。ただし DB テストを持つのは現時点で本タスクだけ
- **昇格条件 / 対応案**: (a) `conftest.py` に**開始時の残骸自動除去**を入れる
  (fail-closed を弱めずに「開始前に存在したら消す」にするかは要検討)
  (b) **復旧手順を `docs/development/` へ文書化**する (c) 現状維持

### 候補 C: ジョブ横断の不変条件を特定ジョブ起点の検査で表現すると、ジョブが増えたときに静かに漏れる

- **分類**: 検査設計
- **検出日**: 2026-09-10(TSK-235 が 2 件・TSK-317 が 1 件)
- **共通点**: **赤くならずに検査対象から外れる**(「静かに」が肝)
- **実例**:

| # | 出典 | 実例 |
| --- | --- | --- |
| 1 | TSK-235 | **全葉変異の母集団**が `tests/test_ci_wiring.py:286` の `_backend_ci_contract_tree` で **`backend` ジョブの `services`〜`defaults` 手前に限定**。新設ジョブの契約木を覆わない |
| 2 | TSK-235 | **image の 3 者一致検査**が同 **`:426`**(および `:611`)の `_mapping_at(backend, ("services","postgres"))` で **`backend` ジョブに限定**。新設ジョブが `services: postgres` を書き忘れると**静かに `--pull=never` で落ちる**(`conftest.py` の使い捨てクラスタは image cache に依存) |
| 3 | TSK-317 | **是正した契約に対応する検査要求の対象集合がずれる**(確定ゲート 1〜3 周目。母集団が固定されているため) |

- **1 と 2 が同じ関数群の同じ限定の仕方から来ている**のが証拠として強い
- **観測の限界**: 2 タスクで 3 件。**`test_ci_wiring.py` に限れば 2 件が同一ファイル**
- **昇格条件 / 対応案**: **「DB を使う全ジョブ」を母集団にする**(`backend` に限定しない)/
  **新設ジョブで検査が漏れると red になる負例を必須にする**。
  **一般化の実装は TSK-235 が持つ**(`ci.yml` を変更するのは同タスクで、TSK-317 は言及 0 件 — 実測)

### 候補 D: guard 群がコマンド文字列の部分一致で誤検知する

- **分類**: 機構の穴(ガードの誤検知)
- **検出日**: 2026-09-10(TSK-235 が 3 件・TSK-317 が 3 件)
- **共通原因**: **コマンド文字列の部分一致で判定し、実際に実行される内容を見ていない**
- **実例(誤検知 5 件)**:

| # | 出典 | ガード | 事象 | 実行内容 |
| --- | --- | --- | --- | --- |
| 1 | TSK-235 | `git_guard` | heredoc 本文に `git ls-files backend/.coverage` という**文字列**を含めた → 「git のグローバルオプションまたはサブコマンドを解析できませんでした」 | `cat > design.md <<'EOF'`(クォート付き = 展開なし) |
| 2 | TSK-317 | `git_guard` | **コミットメッセージ本文に `git diff --check`** と書いた → 同じメッセージ | `cat > file` と `git commit -F file` |
| 3 | TSK-235 / TSK-317 | `git_guard` | **`-C` の値がシェル変数だと書き込み操作が落ちる** → 「現在のブランチを解決できませんでした」。**同じパスをリテラルで書くと通る** | 記載どおり |
| 4 | TSK-317 | `secret_guard` | **`docker inspect --format` の Go テンプレートで環境変数配列を参照**(`{{range .Config` + `.Env}}`)→ 「コマンドが `.env` を参照しています」。**`.` + `Env` の 4 文字が一致** | 記載どおり |
| 5 | TSK-317 | `secret_guard` | **本台帳候補の本文そのもの**を worklog へ書こうとしてブロックされた(候補 D の実例 4 を説明する文字列が同じ判定に当たる)。**台帳へ誤検知を記録することが同じ誤検知で妨げられる** | `python3 -` のヒアドキュメント |
- **3 の切り分け(TSK-317 で実測 — 2026-09-10)**: `W=<path>` を置いた同一コマンド内で

| コマンド | 結果 |
| --- | --- |
| `git -C "$W" log` / `status` / `add`(1〜3 個の複合を含む) | **通る** |
| `git -C "$W" commit -F <file>` | **ブロック** |
| `git -C <リテラル絶対パス> commit -F <file>`(同じ内容) | **通る** |

  → **複合コマンドが原因ではない**
- **3 の根本原因(原典で確定)**: `git_guard.py:26` の
  **`CURRENT_BRANCH_VERBS = {"commit", "merge", "rebase"}`** — この 3 語だけがブランチ解決を要求する。
  解決に使う cwd は **`apply_dash_c()`(`:207-212`)が `-C` のトークンから作る**が、
  同関数は**トークンを展開せずパスとして扱い、絶対パスでなければ cwd へ連結する**。
  `git -C "$W"` の字句解析結果は **`$W` という相対パス**なので、
  **`<cwd>/$W` という実在しないディレクトリ**になり `current_branch()` が `UNKNOWN` を返す →
  安全側で遮断。**変数を使うと 3 語のときだけ必ず落ちる**のはこの経路
- TSK-235 の `fetch && rebase` も **`rebase` が 3 語のひとつ**である点で同型
- **3 が厄介な理由**: **ハーネスの規約自身が `git -C <worktree>` を指示しており**、
  worktree パスは長いので**変数に入れるのが自然な書き方**である。
  つまり**規約どおりに書くと落ちる確率が高い**

- **導線の欠陥 1 件(誤検知ではない)**:

| # | 出典 | 事象 |
| --- | --- | --- |
| 6 | TSK-235 | `cd <path> && git ...` の遮断は**設計どおり**。ただし案内は「`git -C <path>` を使うか分けて実行」で、**案内どおり `git -C` に直しても複合だと 3 の理由で落ちる**。**案内どおりに直しても通らない導線**になっている |

- **回避はいずれも「正しい書き方に直す」ではなく「ガードを避ける」**(Write ツールへ切替 / 分割実行 /
  別の取得経路)。**1・2・5 は heredoc 本文**、**3 は `-C` を展開前の文字列から読むこと**、**4 は 4 文字の部分一致**
- **5 が示すこと**: **誤検知の記録が同じ誤検知で妨げられる**ため、**この型は台帳へ上がりにくい**。
  優先度の判定に効く事実である
- **観測の限界**: 2 タスクで 6 件(1 日)。**既存の `.env.example` 系の誤検知(`## 候補` (1))とは
  トリガが別**(4・5 は設定ファイルへの言及ではなく**別の語のなかの部分一致**)
- **昇格条件 / 対応案**: (a) **heredoc 本文を走査対象から外す**
  (b) **`-C` の値を展開後に解決する**(`git -C <値> rev-parse` を実際に評価する。現状はコマンド文字列から読むため変数で必ず失敗する)
  (c) **導線の是正** — 6 の案内文を実際に通る形へ (d) **語境界を見る**(部分一致をやめる)

### 候補 E: 射程変更後に旧射程の記憶で作業する

- **分類**: プロセス設計
- **検出日**: 2026-09-10(TSK-317 が 1 件・TSK-235 が 1 件)
- **実例**:

| # | 出典 | 事象 |
| --- | --- | --- |
| 1 | TSK-317 | **PO 裁定 D-8 で射程が変わる前の草案の記憶**で、`tsk343-orm` へ「設計書 10.1 に 1 行足す」と提案した。**承認済み計画書は 4 箇所で「10.1 は反映なし・TSK-343 の所有」と明記**し、しかも**自分の DoD `:383` に「設計書 10.1 の差分が 0」**があった。**自分の DoD を破る提案を他タスクへ持ち込んだ**。相手が原典 4 箇所を示して発覚 |
| 2 | TSK-235 | 計画書を **22 → 28 ステップへ再構成する過程で旧ステップ 21(CI 配線)が消滅**し、**`ci.yml` を変更資産に挙げているのに 28 コミットのどれもそれを実装しない**計画書をレビューへ出した(3 周目 `P0-7`)。**DoD とテスト計画には CI 配線が書いてあるので読むと気づきにくい** |

- **共通形**: **「記憶ではなく現物を読む」の別の面**。1 は**他タスクへ提案する前に自分の承認済み計画書を
  読み直す**、2 は**構造を作り直したら旧構造との集合差を機械的に取る**
- **観測の限界**: 2 タスクで 2 件。ただし**どちらも PO 裁定・再構成の直後**という共通の契機を持つ
- **昇格条件 / 対応案**: (a) **計画書の再構成時に旧ステップ表との集合差を出すことを 6.1 へ足す**
  (b) **他タスクへ所有・射程の提案をする際に該当箇所の引用を必須にする**(引用すれば読み直す)
  (c) 現状維持

---

## PO 裁定 `D-9`(2026-09-10・山田正輝)— ステップ 17 を撤去し改訂 3 へ送る

### 発火

ステップ 17(`contract_only` の runtime テスト)の**委任前の実測**で、
**合格条件が凍結資産の定義と矛盾している**ことが判明した。
実測を先にやる方針(ステップ 6・13・15 の差し戻し 3 回の反省)が効いた形である。

### 実測(すべて原典・2026-09-10)

| # | 事実 | 典拠 |
| --- | --- | --- |
| 1 | `receiving_task_id == TSK-270-GROUP-2` かつ `execution_class == contract_only` は **158 行**、一意な `runtime_test_owner.id` は **152 個**(共有 6 組)。**計画書の数字は正しい** | `contracts/authz/claim-mutant-map.json` |
| 2 | 内訳は `CONTRACT_ONLY_RUNTIME_TARGET_PENDING` **157** / `CONTRACT_ONLY_NO_DB_DECISION_POINT` **1**。理由コードは `route_universe_pending` **157** / `no_db_decision_point` **1** | 同上 |
| 3 | **`contract_only` の 165 行には `runtime_target` キーが存在しない**。`probe_executable` の 33 行は**持つ**。**資産が runtime のターゲットを定義していない** | 同上(キー集合の比較) |
| 4 | `runtime_kill_required: False` / `runtime_evidence_kind: handoff_runtime_test` / mutant の `expected_runtime_outcome: handoff` / `waiver_reason: contract_only_handoff` は、**検査器が `contract_only` 全行に機械的に強制している値**。**独立情報を持たない** | `scripts/check_authz_catalog.py:3803-3808`・`:3881-3888`・`:3923-3929` |
| 5 | `schema_drift_test_owner` は **198 行すべてが同一の 1 本**を指す(`tests/test_check_authz_catalog.py::test_repository_oracle_assets_are_valid`)。「既に覆われている」の実質は**資産の自己整合検査のみ** | `claim-mutant-map.json` |
| 6 | `kill_contract.contract_only_runtime_rule` = **`runtime_kill_forbidden_handoff_test_required`**。**runtime kill を禁じ、handoff テストを要求する** | 同上 |
| 7 | **`R-7` の原文**: 「HTTP/cache と実装しない 6 操作には**本タスク内の実越境 kill 経路が無い**ため、『全 claim に runtime kill』は不可能か、**契約 lint を実副作用 kill として数える抜け道**になる。前者だけ runtime kill を必須にし、**後者は schema-drift kill + 受取タスクの runtime テスト ID** を必須にする」 | `docs/features/pg-authz-verification/plan.md:414` |
| 8 | `backend/src/pitchlog/` は `__init__.py` / `main.py` / 本タスクの `authz/catalog.py` のみ。**HTTP 経路も製品スキーマも存在しない**(`route_universe_pending` の由来)。`scope.product_schema: false` は `S-7` の確定値 | 実測 |

### 矛盾の内容

ステップ 17 の合格条件は「**152 の一意な論理 ID それぞれに対応する実テストが `collect_pytest_node_ids` に存在する**」
「**負例: ある claim のテストを常時成功にすると red**(**claim の述語が assertion に現れる**ことを検査する)」を要求する。
しかし **事実 3 により資産は runtime のターゲットを定義しておらず**、**事実 8 により対象の製品コードが存在しない**。
assert できるのは「要件条文が存在する」「分類が期待どおり」程度で、**実質は資産の自己検査**になる。

これは **`R-7` 自身が名指しで警戒している「契約 lint を実副作用 kill として数える抜け道」**(事実 7)であり、
**設計書 7.3-3 が P1 と定める「文言は直したが実効がない」型**、
かつ**本計画書 4 節の実装方針 規律 2**(「合格条件を『検査が green』に置かず『割り当てが正しい』に置く
— 検査は literal 一致で通るため、literal を置くだけで green にできる」)に正面から当たる。

### 相談の経過

**fable(上位モデルのセッション)へ相談した**(2026-09-10)。回答の根拠 5 件を**すべて原典で追認**した(上表 4〜8)。
**私が当初「② を支持する材料」と考えた 3 属性(事実 4)は循環していた** — 検査器が強制する値なので独立情報がない。
これは fable の指摘で気づいた。

**fable の指摘のうち 1 件は誤りだったので採らなかった**: 「ステップ 17 を飛ばすと
`derive_progress` が max を取って `18/21` と表示され 17 未了が見えなくなる」。
**逆で、`scripts/feature_status.py:948-958` が `completed != set(range(1, maximum + 1))` を検査し、
欠番があれば `kind="inconsistent"` + `note="完了ステップに欠番がある"` を返す**(`:33`)。
**機構は捕まえる。** ただしその帰結として、**17 を飛ばして 18 をコミットすると現在地導出が不整合になる**ため、
**18〜21 を先に進める経路は存在しない**。これが「改訂 3 を今やる」ことの機構上の根拠になった。

### 裁定

**A: ステップ 17 を表から撤去し、`S-10` として改訂 3 へ送る。**

1. **ステップ 17 を撤去**し、**旧 18〜21 を 17〜20 へ連番で振り直す**(枝番を使わない — `feature_status.py` は認識しない)。**総数 21 → 20**
2. **`S-10` を新設**: `contract_only` 158 行の runtime テストの所有先と、TSK-317 側で機械固定する範囲を確定する
3. **`S-9` の置換先を 3 分割する** — 現行の `S-9` は `receiving_task_id: TSK-270-GROUP-2` の **178 件を一律 `TSK-317` へ置換**する要件だが、**157 件の受取先は TSK-317 ではない**(製品経路を作らないと自ら宣言している)。一律置換すると**所有先が空白の claim を封印で固定する**
4. **改訂 3 を今すぐ実施する**(旧予定は「ステップ 21 の完了後」)。理由は上記の欠番不整合により 18〜21 を先行できないこと

### 裁定の帰結(手続き)

- **`承認: 済(2026-09-09・山田正輝)` → `承認: 未` へ戻す**(実質的な改訂なので再承認が必要。
  `codex_run.py implement` は承認済みを要求するので、改訂中は実装を止める)
- **`status` は `active` のまま**(差し戻しではない)
- **コア領域なので改訂は敵対レビュー必須**、収束後に人間の承認
- **ステップ 1〜16 は影響を受けない**(番号が動かないため履歴の書き換えも不要)

---

## ステップ 16 の残余リスク記録(`[手動・外部]` 条件・申し送り先 = TSK-217)

**実機で確認した残余リスク**(2026-09-10・`tests/db/test_authz_trust_boundary.py` 1 passed)。
**隠さず、期待値として試験に持たせた。**

### 事象

**アプリ用ロール(`app_role`)自身で認証した実接続から、
`pg_catalog.set_config('app.tenant_id', <任意の値>, true)` が成功する。**
設定値は `current_setting` で読み戻せ、**その文脈で共有関数を呼ぶと当該テナントの行が返る。**

### 射程(3 文脈を実測して境界を確定した)

| # | 文脈 | 要求元テナントの付与 | 返却 |
| --- | --- | --- | --- |
| 1 | **本来の文脈**(`requester_tenant_id`) | なし | **空集合** |
| 2 | **偽装文脈**(付与あり) | あり | **対象行を exact-set で取得** |
| 3 | **偽装文脈**(付与なし・参加のみ) | なし | **空集合** |

→ **危険なのは「GUC を設定できること」ではなく「1 では届かない行が 2 では届くこと」**である。
文脈 1 を測っていなければ、**対象行がどの文脈からも返る壊れ方でも試験は green のまま**だった
(一次レビューの差し戻しで追加した — 当初は `marker` と `expected_invocation_ids` による
**構成での宣言**でしかなく、測定ではなかった)。

→ 文脈 3 により、**リスクの射程は「GUC だけで何でも取得できる」ではなく
「参加と付与を持つ別テナントへの成りすまし」**に絞られる。
**DB 層の認可(参加・付与)は迂回されていない。**迂回されているのは
**「接続主体とテナント文脈の対応」だけ**である。

### DB 層で塞げない理由

`app.tenant_id` は **GUC(実行時パラメータ)であり、接続主体との対応を DB が検証しない。**
`set_config` の実行権限はロールに紐づかず、**アプリ用ロールが自分の接続で自由に設定できる。**
これを DB 側で塞ぐには「テナント文脈を GUC ではなくロール自身から導く」設計が必要で、
**凍結資産の構成(1 アプリロール + GUC)を変えることになる**ため本タスクの射程外。

### 申し送り先: **TSK-217**

**理由**: 本計画 2 節「やらないこと」が **HTTP 経路の判定を TSK-217 の所有**と定めており、
`NFR-010` の測定方法が「**API 直叩き含む**」と定めるため。
**この残余リスクが実害になるのは「アプリの接続に任意のテナント ID を注入できる経路が
HTTP 層に存在する場合」**であり、**その判定は HTTP 層でしかできない。**

**TSK-217 へ渡す要求**(受取契約は改訂 3 の `S-6` で固定する):

1. **アプリの実接続へ渡す `app.tenant_id` が、認証済みの主体から一意に導出されること**を
   HTTP 層で検査する(リクエストのパラメータ・ヘッダ・ボディから来ない)
2. **`set_config` を呼ぶ経路がアプリコード内で 1 箇所に限られること**を機械で検査する
   (複数箇所あると 1 の保証が箇所ごとの人手保証になる)
3. **本試験(`test_app_role_can_spoof_tenant_context_only_with_matching_grants`)を
   安定テスト ID として TSK-217 の DoD へ登録し、相互リンクする** —
   HTTP 層の対策が入ったあとも **DB 層では依然として設定できる**ため、
   **本試験の期待値は変わらない**(「設定できてしまう」が正しいまま)。
   TSK-217 側が足すのは「**HTTP 層からは注入できない**」という別の試験である

**本タスクの成果を `NFR-010` 適合と主張しない**(計画 2 節の宣言どおり)。

### 実機測定(DB 全件)

| 項目 | 値 |
| --- | --- |
| コマンド | `uv run pytest tests/db -q`(backend/ で) |
| 結果 | **110 passed in 319.66s (0:05:19)** |
| 内訳 | ステップ 15 時点の 109 件 + ステップ 16 の 1 件。**回帰なし** |
| 実行環境 | **ローカル**(WSL2)。**CI runner ではない**・**run id なし** |
| 測定日 | 2026-09-10 |
| DSN | 人間が `.env` から供給(`NFR-014` — 実値は本ログに残さない) |

**CI の run id 付きの数値は PR 作成時に記録する**(TSK-235 への申し送りどおり)。

---

## 計画改訂 3 の敵対レビュー 1 周目(2026-09-10)— **否決・P0×3 / P1×2 / P2×1・全 6 件採用**

**判定行**: 否決。**全件 `(A)`(本改訂の射程内)・全件起因**(本改訂が作った・または本改訂が拾い残した)。

| # | 重大度 | 指摘 | 採否 | 反映 |
| --- | --- | --- | --- | --- |
| 1 | **P0** | **旧ステップ 17 の要求が 4 箇所に残っている** — `:125`(やること 5 が新 17 を越境テストとして数える)/ `:379`(DoD が 152 実テストを必須化)/ `:404`(テスト計画の `NFR-019`(b) が新 17 を計上)/ `:389`。**撤去した実装を再要求している** | **採用** | 4 箇所を是正 |
| 2 | **P0** | **`S-9` の ② と ③ は安定テスト ID 単位で分割不能** — ③ の `FR-041/list_item-014#request-validation` と ② に属する `#participant-authorization` が**同一の `runtime_test_owner.id` を共有**する。別受取先にすると **1 ID を複数タスクが所有する** | **採用** | **3 分割 → 2 分割**へ再是正 + **不変条件**を新設 |
| 3 | **P0** | **`S-10` が自己申告を封じていない** — ① は比較元・基準コミットが未指定、②③ は**受取先リテラルを置くだけで成立**(現行検査器 `check_authz_catalog.py:3803` は**非空文字列しか見ない**)。旧条件の「158 claim との対応」「常時成功にすると red」も落ちている | **採用** | **4 点 → 6 点**へ全面書き換え |
| 4 | **P1** | **`S-10` ④ の「凍結 15 パスの差分 0」が実行不能** — `S-9` は封印資産 `claim-mutant-map.json` を変更するので、**旧基準 `origin/develop...HEAD` では ② と ④ が同時に成立しない** | **採用** | 基準区間を 2 分して明示 |
| 5 | **P1** | **`承認: 未` の間、現在地導出が 16/20 を完全に隠す** — `feature_status.py:1390` の `if not approved(frontmatter)` が**履歴走査より前に return** する。さらに `:460` が「**ステップ 1 から委任**」のままで、**再承認後に 1〜16 を再実装する誤誘導** | **採用** | `:460` を「未了ステップから」へ + 表示挙動の注記を追加 |
| 6 | **P2** | DoD の記録条件が `S-1`〜`S-9` のままで `S-10` が脱落 | **採用** | `S-1`〜`S-10` へ |

**不採用: 0 件。**

### レビューが確認した肯定的事実(私の裁定根拠の追認)

- `contract_only` 165 行に `runtime_target` が無いこと
- 検査器が runtime kill を禁じていること
- `contract_only_runtime_rule` が提示どおりであること
- 検査器が現資産に対し `probe=33 / contract=165 / mutants=231` で green
- **「旧ステップ 17 の撤去自体は妥当」**
- **「改訂 3 を今開始することは、後半設計を確定せず新 17〜20 完了後に続ける限り `D-8` の趣旨と矛盾しない」**

### `P0-2` の実測(私が原典で追認した)

**共有 6 組のうち、理由コードが混在するのは 1 組だけ**だった。

| 共有 `runtime_test_owner.id` | claim | 理由コード |
| --- | --- | --- |
| **`TSK-270.group2.runtime.FR-041/list_item-014`** | `#request-validation` | **`no_db_decision_point`** |
| 同上 | `#participant-authorization` | **`route_universe_pending`** |
| 残り 5 組 | — | いずれも `route_universe_pending` のみ |

→ **受取先の分割単位は claim 行ではなく `runtime_test_owner.id` である。**
これを **`S-10` ③ の機械検査(同一 ID の行は同一受取先)**として固定した。
**リテラルでは通せず、分割を誤れば必ず red になる**ので、`P0-3` の自己申告問題への答えにもなっている。

### 自分の失敗として記録する — **台帳候補 E がそのまま再発した**

**`P0-1` は私が今日 worklog に書いた台帳候補 E**(「構造を作り直したら旧構造との集合差を機械的に取る」)
**の対象そのものだった。** しかも**集合差は実際に取っていた**(ステップ表の 21 行 → 20 行を検算し、
「真に消えたのはステップ 17 だけ」を確認済み)。**それでも漏れた。**

**理由**: 私が取った集合差は**ステップ表の内部だけ**を対象にしていた。
`:125` は「越境テスト(ステップ 7〜13・16〜17 —」、`:379` は「**152 個**」と番号を書かない形、
`:404` は「ステップ **7・8・10・11・12・16・17**」という列挙形で、
**表の外にある参照は集合差の母集団に入っていなかった**。
私の `grep` も「`ステップ 1[6-9]`」「`1〜21`」の形しか探せず、
**`:379` の「152 個の実テスト」は番号を含まないため原理的に引っかからなかった**。

→ **台帳候補 E の対応案を強める**: 「旧ステップ表との集合差を取る」だけでは足りない。
**撤去したステップの合格条件に現れる固有語(本件では「152」「158」「`contract_only` の runtime テスト」)
を全文検索の鍵にする**必要がある。番号だけを鍵にすると、**番号を書かない参照が残る**。

### reseal フラグの実測(TSK-355 のタブとの相互検証・2026-09-10)

**私の見立て「`--reseal` が `input_manifest` を再計算するはず」は誤りだった。** 相手の実測と、
私が原典で追認した内容:

| 事実 | 典拠 |
| --- | --- |
| **`reseal_catalog()` が更新するのは `claims[].decision_digest` と decision lock だけ** | `check_authz_catalog.py:1489-1503`(逐語で確認) |
| **`input_manifest`(`source_blob_digest` / `item_counts_by_kind` / `heading_ids` / `commit`)は一切触らない** | 同上 |
| **`--reseal` は `_validate_manifest` が reseal 分岐より先に走るため、digest 不一致だと `--reseal` を付けても exit 1 で到達しない** | 相手の実測(`exit=1`) |
| **`--reseal-oracle` は `verify_seal=not args.reseal_oracle` を渡して自分の seal 検査を明示的に飛ばす** | `:4958`(私が追認) |

→ **3 つの reseal フラグは fail-early の挙動が非対称である。**
**`S-1` へ追記した。** 帰結として **本タスクの `S-9` は `--reseal-oracle` で足りる**
(`claim-mutant-map.json` は oracle 層で、要件書を変更しないので `_validate_manifest` に掛からない)。

**私の側の訂正**: 「`shared-preconditions.json` も同じ digest を持つので同時に落ちる」と伝えたが、
**同資産は develop に存在しない**(相手の実測 — develop の `contracts/authz/` は 15 ファイルで、
`shared-preconditions.json` と `scripts/check_shared_preconditions.py` はいずれも**私の未マージ資産**)。
**develop 時点で `8d8335...` を持つのは `requirement-claims.json` だけ**である。
私の資産が同じ digest を持つのは同じ要件書 blob を指しているためで、
**衝突が顕在化するのは TSK-317 のマージ後**である。

**マージ順序は TSK-317 が先で合意した**(相手の同意 — 逆順だと私の `shared-preconditions.json` の
digest 検査が設計どおり red になり、残り 4 ステップをずっと red で作業することになる)。

---

## 計画改訂 3 第 1 弾の敵対レビュー 2 周目(2026-09-10)— **否決・P0×3 / P1×3・全 6 件採用**

| # | 重大度 | 起因 | 指摘 | 反映 |
| --- | --- | --- | --- | --- |
| 1 | **P0** | 起因 | **裁定 `D-9` 行が「3 分割」のままで `S-9` は 2 分割** — 承認済み裁定と実装指示が正面から矛盾 | `D-9` 行へ撤回の経緯を追記(分割単位は `S-9` を正とする) |
| 2 | **P0** | 非起因 | **番号を含まない旧ステップ 17 の残滓が尽きていない** — `:126`(越境テストに `contract_only` 158 件)/ `:100`(「158 件は本タスクが持つ」と断定)/ `:349`(`S-2` の 468 参照に移管済みの 158 が含まれ、`pytest --collect-only` 要求が残る) | 3 箇所を是正。`S-2` は「本タスクが解決するのは残り 310 参照」と明示 |
| 3 | **P0** | 起因 | **`S-10` ⑥ が実効を持たない** — `S-1` が凍結 15 パスのほぼ全体を変更対象にしているので「`S-1`・`S-9` が宣言した資産のみ」では**任意の変更を `S-1` 名目で通せる** | 論点 (6) として第 2 弾へ(許可パス・許可フィールドの閉じた集合が要る) |
| 4 | **P1** | 起因 | **`S-10` ① が成立しない** — `S-1` ② が同じファイルの `oracle_context.oracle_commit` も必ず変えるので「ファイル差分が `receiving_task_id` だけ」にならない | 論点 (1) として第 2 弾へ |
| 5 | **P1** | 起因 | **DoD が実行不能または無検証** —「機械で固定した」と「実装しない」が同じ項に同居。`S-6` の read-back 突合を含むかも不明 | 論点 (8) として第 2 弾へ + DoD を是正 |
| 6 | **P1** | 起因 | **履歴を要する検査が現行 CI で実行できない** — **root の `harness` ジョブは `fetch-depth` 未指定で浅い履歴**(`ci.yml:75`)。スキップすれば無効、fail-closed なら常時失敗 | 論点 (7) として第 2 弾へ |

**不採用: 0 件。**

### レビューが追認した肯定的事実

- **2 分割のデータ構造自体は整合**(158 行 / 152 ID / 共有 6 組 / 理由混在 1 組)
- **`no_db_decision_point` の性質は分類・理由コードを凍結すれば失われない**
- **`TSK-217` の 7 件との差は ID 共有グラフから説明できる**
- **`S-9` に `--reseal-oracle` だけを用いる帰結は正しい**(`claim-mutant-map.json` は `_validate_manifest` の対象外)

## PO スコープ裁定 `D-10`(2026-09-10・山田正輝)— **射程縮小**

### 発火(7.3-6)

| 周 | 母集団(P0+P1) | 前周修正起因 | 判定 |
| --- | --- | --- | --- |
| 1 | 5 | 5(**依頼文で `起因|非起因` を求めておらず、これは私の分類**) | `5×2=10 > 5` 成立 |
| 2 | 6 | **5(レビュアが明示ラベル)** | `5×2=10 > 6` 成立 |

**2 周連続で成立。** さらに実質的な理由として、**1〜2 周目の指摘が
「実体が無い段階で `S-*` の機械条件を紙の上で閉じようとすると無限後退する」に収束**しており、
**裁定 `D-8` が既に診断した `H-68` の型**である。
2 周目 `P1-6`(CI の `fetch-depth` が浅く履歴検査を実行できない)は**実装しないと分からない層**の典型。

### 裁定

**改訂 3 を 2 弾に分ける。**

- **第 1 弾(本改訂)= 機械的な作業のみ** — ステップ 17 の撤去・連番の振り直し・旧参照の除去・裁定と実測の記録
- **第 2 弾 = `S-1`〜`S-10` の機械条件の確定** — **ステップ 17〜20 の完了後**に行う

**洗い出した論点は 1 件も落とさない**: `S-9` に**確定済みの実測 4 件**、
`S-10` に**確定すべき論点 9 件**として記録した。

**射程が変わったので次周は全文レビュー**(7.3-4)。**連続カウントは 0 にリセット**(7.3-6 の表)。

### 自分の失敗として記録する — **台帳候補 E の 3 回目の再発**

`P0-2` は**同じ型の 3 回目**である(TSK-235 の `P0-7` → 私の 1 周目 `P0-1` → 本件)。
1 周目の反省で「**番号だけを鍵にすると番号を書かない参照が残る**」と書き、
固有語(`152`・`158`)での走査を対応案に足した。**それでも `:126` の
「`contract_only` 158 件」を取り逃した** — 私は `152 個` を鍵にして走査し、
**`158 件` のほうを鍵に入れ忘れた**。

→ **台帳候補 E の対応案をさらに強める**: 走査の鍵を人が列挙する方式は
**列挙の漏れがそのまま検出漏れになる**。**撤去対象の行に現れる全数値・全識別子を
機械的に抽出して鍵にする**(人が選ばない)形でないと閉じない。
**これは台帳候補 A(`feature_status.py` の allowlist)と同じ構造の欠陥である** —
**「人が列挙した集合」が母集団になっている。**

---

## 計画改訂 3 第 1 弾の敵対レビュー 3 周目(全文・2026-09-10)— **否決・P0×3 / P1 0 / P2 0・全件採用**

**射程縮小(`D-10`)直後なので 7.3-4 により全文レビュー。連続カウントはリセット済み。**

| # | 重大度 | 起因 | 指摘 | 反映 |
| --- | --- | --- | --- | --- |
| 1 | **P0** | 起因 | **`:101` は訂正を挿入した同じセルの末尾に「158 件は本タスクが runtime テストを持つ(外部への受け渡しではない)」を残していた** | 打ち消し線 + 撤回理由へ |
| 2 | **P0** | 起因 | **`D-10` の第 2 弾への送り出しが本文へ伝播していない** — `:185-191` / `:294-295` / `:332` / `:345` / `:365` / `:411` / `:438` / `:451-453` が `S-*` を現行の承認条件として読める | 「改訂 3 の射程」→「改訂 3 **第 2 弾**の射程」を全 5 箇所 + 見出し・DoD・検証節を是正。**`S` 要件節の冒頭に規範文を 1 つ置いた** |
| 3 | **P0** | **非起因** | **存在しないステップ 22 への現在形の指示が残っている**(`:206` / `:260` / `:264`) | 3 箇所を過去形の記録 + 第 2 弾への送り先へ |

**不採用: 0 件。** **P1・P2 はゼロ**(1 周目 P1×2・P2×1 → 2 周目 P1×3 → 3 周目 0)。

**レビューが追認**: ステップ表は `valid=True, total=20` / 再承認後の現在地は `16/20` /
`S-9` の実測 4 件と `S-10` の論点 9 件は記録されている /
DoD の `contract_only` 項は「158 行すべて `planned`・新設 runtime テスト 0 件」という**否定条件**になっている。

### 同型 4 回目 — **今回から鍵の列挙をやめた**

| 回 | 取り逃した箇所 |
| --- | --- |
| 1 | TSK-235 `P0-7`(旧ステップ 21 が消滅) |
| 2 | 1 周目 `P0-1`(番号を書かない「152 個」) |
| 3 | 2 周目 `P0-2`(`152` を鍵にして `158` を入れ忘れた) |
| **4** | **3 周目 `P0-1`(訂正を挿入した同じセルの末尾)+ `P0-3`(「歴史的記録」と自分で分類して除外した 3 箇所が実は現在形の指示だった)** |

**4 回目の 2 つの原因は別物である。**

- **`P0-1`**: **セル単位ではなく文単位で見ていなかった。** 同じセルの前半に訂正を入れて、
  後半の断定を残した。**「直した」という感覚が残りの走査を止めた。**
- **`P0-3`**: **私が「歴史的記録だから対象外」と分類した 3 箇所が、実は現在形の実装指示だった。**
  裁定表と訂正表は記録だが、**`:206`・`:260`・`:264` は実装方針の本文**であり、
  同じ「旧番号を含む」という表面的特徴で一括りにしていた。
  しかも**依頼文でレビュアに「歴史的記録は指摘対象外」と伝えていた**ので、
  **自分の誤分類をレビュアに追認させかける形になっていた**(レビュアは正しく見抜いた)。

→ **今回から方式を変えた**: **鍵を人が列挙するのをやめ、機械で全列挙してから 1 行ずつ分類する。**
実行結果 = **39 行が該当**し、そのうち **7 行が歴史的記録・3 行が現在形の指示・13 行が第 2 弾の区別が必要・
16 行が是正済みまたは問題なし**だった。**`P0-3` の 3 行はこの列挙で初めて可視化された。**

**依頼文への教訓**: **「これは対象外です」とレビュアへ伝える範囲を、自分の分類で決めない。**
分類の誤りがそのまま検出漏れになる。今後は**列挙結果を渡して分類自体をレビュー対象にする**。

---

## 計画改訂 3 第 1 弾の敵対レビュー 4 周目(2026-09-10)— **否決・P0×2 / P1 0 / P2×1・全件採用**

**2 件の P0 はいずれも非起因**(既存だが今周初めて可視化された)。

| # | 重大度 | 起因 | 指摘 | 反映 |
| --- | --- | --- | --- | --- |
| 1 | **P0** | 非起因 | **影響範囲表が第 2 弾の作業を現在形で指示** — `:170` が `core-areas.json` への登録を本計画の変更内容として指定。`:190`・`:191` も「改訂 3」のままで第 1/第 2 弾の区別がない | 3 行を「**本計画では変更しない** — 第 2 弾の `S-8` の射程」へ |
| 2 | **P0** | 非起因 | **`design.md` へ `D-9`・`D-10` が伝播していない** — 承認範囲「1〜21」・要件「`S-1`〜`S-9`」・旧ステップ番号(`:29`・`:104`・`:234`)・「ステップ 20 で `scope` を消化」(`:293`)。**撤去済みステップ 21 を復活させ `S-10` を脱落させる実装経路** | **10 箇所を是正** |
| 3 | **P2** | 起因 | `:261-264` が「同一コミットで行う」のまま(`S-1` の 2 段方式と矛盾) | 2 段方式である旨を明記 |

**不採用: 0 件。**

**レビューが追認**: ステップ表 1〜20 の連番 / **ステップ 17〜20 の合格条件は `S-1`〜`S-10` の確定を前提としていない** /
DoD は第 1 弾の射程と整合 / `承認: 未`・`status: active`・`計画レビュー周回: 8` は整合。

### 白紙分類の方式が機能した

**4 周目から「私の分類を渡さず、機械列挙 41 行をそのまま渡して白紙で判定してもらう」方式に変えた。**
結果、**レビュアが `:190`・`:191` を「私が (う) 送り出しと見なしたが実際は (い) 現在形の指示」**と判定した。
**3 周目まで私が自分の分類を渡していたら、この 2 行は指摘されなかった可能性が高い。**

### 同型 5 回目 — **今度は母集団のファイル選定が人手だった**

`P0-2`(`design.md` 未更新)が**同じ型の 5 回目**である。

| 回 | 母集団の欠陥 |
| --- | --- |
| 1〜3 | **走査の鍵**を人が列挙していた |
| 4 | **セル内の後半**を見ていなかった / **「歴史的記録」の分類**を人がやっていた |
| **5** | **列挙を機械化したが、対象ファイルが `plan.md` 1 本だけだった** |

**`design.md` は計画書 `:299` が「詳細設計の正本」と明示的に指定しているファイル**である。
**機械列挙の対象を人が選んだ時点で、機械化した意味が半分失われていた。**

→ **是正**: 検算の母集団を **`plan.md` + `design.md`** にした。
**より一般には「計画書が正と指定した全ファイル」を母集団にする**必要がある
(`research.md` は日付つきの調査記録なので対象外 — 冒頭に読み方の注記を置いてある)。

→ **台帳候補 E の対応案をさらに強める**: 「機械的に抽出する」だけでは足りない。
**抽出対象のファイル集合も、計画書 3 節の宣言と「正本と指定した文書」から機械的に導く**必要がある。
**5 回連続で「母集団を人が決めた層」が漏れている** — 鍵 → セル → 分類 → ファイル、と層が変わるだけで
**同じ構造の欠陥が再生産されている。**

---

## 計画改訂 3 第 1 弾の敵対レビュー 5 周目(2026-09-10)— **否決・P0×2 / P1×1 / P2×1**

**採用 3 件 / (B) で第 2 弾へ 1 件 / 不採用 0 件。**

| # | 重大度 | 起因 | 指摘 | 採否 |
| --- | --- | --- | --- | --- |
| 1 | **P0** | 起因 | **`:190` の送り出しが行の後半にしか掛かっていない** — 行頭が今も「中央 2 ファイル(`check_authz_catalog.py` + そのテスト)へ新設資産の検査を追加する」という現在形。**実装済みのステップ 2・9・14 は独立した検査器 3 本を新設しており中央 2 ファイルは無変更**。ステップ 18 で誤ったファイルへの重複実装を導く | **採用** |
| 2 | **P0** | 非起因 | **DoD の無限定な禁止が 231 変異と衝突** — 「158 行について何も実装しない・対象とする新設テスト 0 件」と書いたが、**231 変異のうち 173 件が `contract_only` のみを参照**し、ステップ 19 の全量実行が schema-drift チャネルで kill する。**DoD を守ると 231 変異を満たせず、231 変異を実装すると DoD に違反する** | **採用** |
| 3 | **P1** | 起因 | `design.md:23` の主語が「DDL 生成器」のまま「第 2 弾の射程」— 生成器自体を送る読みと登録だけを送る読みに分岐 | **採用** |
| 4 | **P2** | 非起因 | **抽出外**の `design.md:317` が 7 単位の安定 ID を `req-universe.json` から取るよう指示(同ファイルに `list_item` は 0 件で `S-3` と矛盾) | **`(B)` で第 2 弾へ**(送り先を `design.md` へ記録。第 1 弾の委任は阻害しない) |

### `P0-2` は残滓ではなく、私が 3 周目に作った実質的な矛盾だった

**`contract_only` の意味を私が取り違えていた。** 資産の
`kill_contract.contract_only_runtime_rule` = `runtime_kill_forbidden_handoff_test_required` は
**「runtime kill を禁じる」**であって、**「schema-drift kill もしない」ではない**。
`verdict_channels` も `schema_drift` を `independent_required_result` としている。

**実測**: **231 変異のうち 173 件が `contract_only` のみを参照する**。
**これらの kill は本タスク(ステップ 19)の仕事である。**

3 周目に DoD へ「158 行について何も実装しない」と**無限定に**書いたのが誤りで、
禁止対象を**「受取タスク所有の runtime テスト」と「論理 ID → node ID の解決」**に限定した。

**教訓**: **撤去した要求を DoD へ「否定形」で書くとき、否定の範囲を広く取りすぎると
残っている要求と衝突する。** 「何もしない」ではなく「何をしないか」を列挙する。

### 白紙分類の結果(レビュアの判定)

**抽出 70 行**: `(あ) 歴史的記録 10 / (い) 現在形の指示 24 / (う) 第 2 弾への送り出し 36`。
**うち問題は 3 行**で、**残りの旧ステップ番号はすべて正しく区別できていた**とレビュアが明記。
**さらに抽出外から 1 件**(`design.md:317`)を検出 — **私の抽出キーが `req-universe` を含んでいなかった**。

**機構面はすべて整合**: ステップ表 `valid=True, total=20` / 履歴からの現在地 `16/20` /
反映周コミット突合は一致 / `status: active`・`承認: 未`・`計画レビュー周回: 9` と表示が一致。

---

## 計画改訂 3 第 1 弾の敵対レビュー 6 周目(2026-09-10)— **可決・P0 0 / P1 0 / P2×1((B)×1)**

**収束した。** 残る 1 件は `(B)`(第 2 弾で扱えば足りる)なので、**送り先を記録した時点で当該ゲートでは解決扱い**。
**反映周としては数えない**(7.3-2 の「P0/P1 ゼロ・P2 あり」行 — 採用反映がないため
`計画レビュー周回` は **10 のまま**)。

| # | 重大度 | 起因 | 指摘 | 処理 |
| --- | --- | --- | --- | --- |
| 1 | **P2** | 起因 | **`:190` でステップ 18 が独立検査器 + テストを新設すると書いた一方、`S-8` の登録対象は「6 本」のまま** — 第 2 弾ではステップ 18 の 2 パスも登録母集団に含める必要がある | **`(B)` で第 2 弾へ。送り先を `S-8` へ記録** |

### レビュアの判定(重要)

- **「ステップ 17〜20 は、計画としてこのまま委任可能です」**(4・5 周目は「委任不能」だった)
- **抽出 87 行の白紙分類**: `(あ) 12 / (い) 40 / (う) 35`。**キー外を含む全文走査でも第 1 弾を阻害する残滓はなかった**
- **収束の見立て**: **「母集団外の P0/P1 型は尽きた」**
- **機構面**: ステップ表 `valid=True, total=20` / 現在地 `16/20`(承認後)/ 反映周コミット突合は一致 /
  **凍結 15 パスと指定正本の差分 0** / カタログ検査 `probe=33 / contract=165 / mutants=231 / cut_sets=24` で通過

### 6 周目の指摘が示した構造 — **同型 7 回目**

`S-8` の「**実測 6 本**」は **2026-09-10 時点の人手列挙**であり、
**ステップ 18 で 2 本増えるので陳腐化する**。

**本改訂のレビュー 6 周で、「人が列挙した集合が母集団になっている」型を 7 回踏んだ。**

| 回 | 人が決めていた母集団 |
| --- | --- |
| 1 | 走査の鍵(番号だけを鍵にした) |
| 2 | 走査の鍵(`152` を入れて `158` を忘れた) |
| 3 | セル内の位置(前半を直して後半を残した)/ 「歴史的記録」の分類 |
| 4 | 対象ファイルの選定(`plan.md` 1 本だけ・`design.md` を忘れた) |
| 5 | 抽出キー(`req-universe` が無かった)/ DoD の否定範囲(広く取りすぎた) |
| **6** | **`guard_paths` の登録対象の列挙(6 本 → 実際は 8 本)** |

**層は毎回変わるが構造は同一である。** 台帳候補 A(`feature_status.py` の allowlist に
`merge_allowed` が無い)・台帳候補 E(構造再構成時の集合差)と**同型**であり、
**台帳へ出す価値がこの 6 周で決定的に上がった**。

→ **`/pr` のクローズ処理で、台帳候補 A と E を統合するか、別の候補として立てるかを判断する。**
現時点の見立ては「**A と E は同じ根(人手列挙の母集団)の別の現れ**なので、
**候補として 1 本立て、A・E を実例として束ねる**」。
**実例が 7 + 2(A・E の元の実例)= 9 件あるので、傾向として確定している。**

---

## ステップ 18 の `[手動・外部]` 逐行確認 — **注記漏れ 7 件を検出した**(2026-09-10)

**計画書の合格条件**: 「**注記の網羅性はここで担保する** — 機械では『最初から注記を漏らす』ことを
捕まえられない(4 周目 `P1-2`)。**body の全認可判定に注記が付いていることを逐行で確認し、
判定の数と位置を worklog へ記録する**」

### 結果: **7 件の認可判定に注記が無かった**

| ファイル | 行(是正前) | 条件 | 影響 |
| --- | --- | --- | --- |
| `apply_representative_grant_change.sql` | 30 | `target_grant.group_id = caller_membership.group_id` | **外すと別グループの grant を操作できる** |
| **`authorized_shared_rows.sql`** | **182** | **`business_row.tenant_id = authorized_target.target_tenant_id`** | **外すと全テナントの行が返る — 読み取り経路のテナント分離の中核** |
| `read_control_resources.sql` | 94 | `controlled_group.group_id = request_access.group_id` | 外すと別グループの情報が返る |
| 同上 | 109 | `controlled_membership.group_id = ...`(member list) | 同 |
| 同上 | 127 | `controlled_membership.group_id = ...`(admin details) | 同 |
| 同上 | 144 | `controlled_grant.group_id = ...` | 同 |
| 同上 | 168 | `controlled_invitation.group_id = ...` | 同 |

**いずれも JOIN の `ON` 条件**であり、**`WHERE` 句の述語だけを注記していた**のが漏れの型である。

### 機構は設計どおり働き、人手が漏れた

**ステップ 1 の `[手動・外部]` 条件**が同じ確認を要求していた
(「body の全認可判定に注記が付いていることを逐行で確認した」)。**私はそこで漏らした。**
今回捕まえられたのは、**ステップ 18 が同じ人手確認をもう一度要求していた**ためである。
**同じ人手確認を 2 つのステップに置いた計画の設計が効いた。**

### 走査の 1 回目は 1 件しか見つけられなかった — **同型 8 回目**

最初に書いた補助スクリプトは「**演算子だけの行の次**」を見る形で、**1 件**しか検出しなかった。
盲点(**同一行に条件を伴う `AND`** と **`ON` 句**)に気づいて
「**注記の外にある比較・真偽演算を全掃き**」に変えたら **7 件**出た。

**1 回目の走査結果を信じていたら 6 件を取り逃していた。**
これは本タスクで **8 回目**の「**人が決めた母集団**」の失敗である
(走査の鍵 ×2 / セル内の位置 / 歴史的記録の分類 / 対象ファイルの選定 / 抽出キー /
DoD の否定範囲 / `guard_paths` の列挙 / **本件 = 走査位置の想定**)。

### PO 裁定(2026-09-10・山田正輝)— **注記を追加して再凍結する**

選択肢は 3 つ提示した: **A** 注記を追加して再凍結 / **B** 注記の射程を「`WHERE` 句の認可述語」に
限定すると明文化して 7 件を残余リスクにする / **C** 231 変異の被覆を先に測って判断する。
**A を採用。**

### 是正の 3 コミット(`H-85` の 2 段構造を踏んだ)

| コミット | 内容 |
| --- | --- |
| `14973f6` | body 3 ファイルへ注記 7 件を追加(ステップ 1 追随)。**この時点で検査は意図的に red** |
| `8a72a93` | `manifest.json` を `source_commit: 14973f6` へ再導出(ステップ 2 追随)。差分は **4 行のみ** |
| (次) | `mcdc-map.json` を **40 → 47** へ再生成(ステップ 18) |

**`manifest.json` の `source_commit` にはその body を含むコミットの SHA が必要**で、
**自コミットの SHA を内容へ埋め込めない**ため 1 コミットでは構築できない
(`check_authz_function_bodies.py` の `_verify_manifest_commit`)。
**台帳 `H-85` が記録している 2 段コミット構造そのもの**であり、
本日 TSK-355 と実測で突き合わせた構造を自分の側でも踏んだ。

### 追加した負例 — **今後は機械で漏らせなくする**

**`SHARED_BUSINESS_ROW_TENANT_MATCH` を写像から外すと red** になる負例を足した。
**今回人手で漏らした判定を、今後は機械が捕まえる**形にするためである。
これがないと同じ漏れが人手確認だけに依存し続ける。

### 判定の数と位置(47 件・合格条件の記録要求)

- **総数 47・一意 47**(是正前 40)
- **判定形の分布**: `ATOMIC` 34 / `AND` 9 / `OR` 1 / `NOT` 2 / `CASE` 1
- **条件総数 64**(是正前 57)

```text
functions/apply_representative_grant_change.sql (8)
    30  MANAGEMENT_TARGET_GRANT_GROUP_MATCH
    34  MANAGEMENT_REQUESTER_CONTEXT_PRESENT
    37  MANAGEMENT_GROUP_SCOPE
    40  MANAGEMENT_REQUESTER_MEMBERSHIP_EFFECTIVE
    45  MANAGEMENT_ADMIN_ROLE_REQUIRED
    48  MANAGEMENT_TARGET_GRANT_SCOPE
    51  MANAGEMENT_EFFECT_KIND_MATCH
    54  MANAGEMENT_TARGET_GRANT_ENABLED
functions/authorized_shared_rows.sql (14)
    76  SHARED_RESOURCE_GRANULARITY_ALLOWED
    79  SHARED_REQUESTER_CONTEXT_PRESENT
    82  SHARED_TARGET_SET_NONEMPTY
    85  SHARED_COMPARISON_LIMIT
    95  SHARED_TARGET_IDENTIFIER_PRESENT
   105  SHARED_GROUP_EFFECTIVE
   113  SHARED_REQUESTER_MEMBERSHIP_EFFECTIVE
   123  SHARED_TARGET_MEMBERSHIP_EFFECTIVE
   133  SHARED_SELF_TENANT_EXEMPTION
   137  SHARED_TARGET_GRANTS_COMPLETE
   155  SHARED_REQUESTER_GRANTS_COMPLETE
   182  SHARED_BUSINESS_ROW_TENANT_MATCH
   185  SHARED_RESOURCE_KIND_ALLOWLIST
   188  SHARED_SELF_OWNED_RESOURCE
functions/read_control_resources.sql (13)
    46  CONTROL_RESOURCE_KIND_ALLOWED
    49  CONTROL_REQUESTER_CONTEXT_PRESENT
    52  CONTROL_GROUP_EFFECTIVE
    60  CONTROL_REQUESTER_MEMBERSHIP_EFFECTIVE
    70  CONTROL_ADMIN_ONLY_ACCESS
    94  CONTROL_GROUP_ROW_GROUP_MATCH
   110  CONTROL_MEMBER_ROW_GROUP_MATCH
   114  CONTROL_MEMBER_LIST_ACTIVE_ROW
   129  CONTROL_ADMIN_MEMBER_ROW_GROUP_MATCH
   133  CONTROL_ADMIN_MEMBER_ACTIVE_ROW
   147  CONTROL_GRANT_ROW_GROUP_MATCH
   151  CONTROL_GRANT_TARGET_MEMBERSHIP_EFFECTIVE
   172  CONTROL_INVITATION_ROW_GROUP_MATCH
policies/POLICY:probe_business_rows:tenant_boundary.sql (2)
    10  RLS_PROBE_BUSINESS_ROWS_USING
    21  RLS_PROBE_BUSINESS_ROWS_WITH_CHECK
policies/POLICY:probe_grants:tenant_boundary.sql (2)
    10  RLS_PROBE_GRANTS_USING
    21  RLS_PROBE_GRANTS_WITH_CHECK
policies/POLICY:probe_groups:tenant_boundary.sql (2)
    10  RLS_PROBE_GROUPS_USING
    24  RLS_PROBE_GROUPS_WITH_CHECK
policies/POLICY:probe_invitations:tenant_boundary.sql (2)
    10  RLS_PROBE_INVITATIONS_USING
    21  RLS_PROBE_INVITATIONS_WITH_CHECK
policies/POLICY:probe_management_effects:tenant_boundary.sql (2)
    10  RLS_PROBE_MANAGEMENT_EFFECTS_USING
    21  RLS_PROBE_MANAGEMENT_EFFECTS_WITH_CHECK
policies/POLICY:probe_memberships:tenant_boundary.sql (2)
    10  RLS_PROBE_MEMBERSHIPS_USING
    21  RLS_PROBE_MEMBERSHIPS_WITH_CHECK
```

---

## ステップ 19 の実機実行 — **231 変異すべて kill・生存 0 件**(2026-09-10)

### 実測

| 軸 | 件数 | 所要 | 結果 |
| --- | --- | --- | --- |
| `r8_provisioning` | 2 | 9.31s | **1 passed** |
| `configuration` | 24 | 18.56s | **1 passed** |
| `authorization_predicate` | 205 | 38.24s | **1 passed** |
| **合計** | **231** | **約 66 秒** | **生存 0** |

**実行環境**: ローカル(WSL2)。**CI runner ではない・run id なし**。
**DSN は人間が設定ファイルから供給**(`NFR-014` — 実値は本ログに残さない)。

### 合格条件の充足

| 条件 | 結果 |
| --- | --- |
| **非等価変異の生存 0** | **達成**。`mutation-survivors-jsonl:` の出力なし = 全 231 件 kill |
| 変異集合が資産由来で**件数を定数で持たない** | 達成。`mutation_execution.py` に `231` / `205` / `173` / `52` の件数リテラルは **0 件**(実測) |
| **等価変異は人手判定で分母から除外し、除外の記録がある** | **除外 0 件** — **生存が 1 件も無かったため、等価性の人手判定を要する変異が存在しなかった**。**これを記録として残す**(「判定していない」のではなく「判定対象が 0 件だった」) |

### 見積もりの誤り(記録)

**私の当初見積もりは 12〜18 分で、実測は約 66 秒だった**(**11〜16 倍の過大見積もり**)。
原因は **`expected_runtime_outcome: handoff` の 173 件が DB を使わない**ことを
見積もりに織り込んでいなかったこと。実機で DB を触るのは **57 件**
(`kill` 52 + `survives_layered_defense` 4 + `availability_failure` 1)だけである。

### 実機でしか見つからなかった欠陥 — **観測層が期待どおりの失敗を捨てていた**

**`configuration` 軸の 1 件目で落ちた**(15.36s)。**原因は性能でも変異の適用でもなく、観測層**だった。

```python
def _observe_assertion(call):
    try:
        call()
    except AssertionError:          # ← ここを通り抜けていた
        return ChannelObservation.expected_assertion_failure()
    return ChannelObservation.passed()
```

**`pytest.Failed` の継承は `Failed` → `OutcomeException` → `BaseException`** であり、
**`AssertionError` でも `Exception` でもない**(実測)。
したがって **`pytest.raises(...)` を使う probe の失敗は観測されず、pytest の失敗として素通り**していた。

**皮肉なのは、落ちた内容自体が正しかったこと**である。
`DID NOT RAISE InsufficientPrivilege` は
**「本来ないはずの表権限を付与する変異が効いた」= `kill` の信号**だった。
**正しい信号が観測層で捨てられていた。**

**影響範囲(実測)**: `backend/tests/db/` 配下で `pytest.raises` / `pytest.fail` を使う箇所は **26 件**。
落ちたのは 1 件だが、`grant_management_caller_table_privilege` 8 件 +
`grant_app_role_control_table_dml` 4 件が同じ経路を通る。

**是正**: `except (AssertionError, Failed, pytest.fail.Exception)` の 1 行 + **回帰テスト 4 件**。
**`KeyboardInterrupt` / `SystemExit` は捕まえずに再送出する**
(飲み込むと強制終了時に teardown が飛び、開発 DB に `pitchlog_test_role` が残って
`conftest.py` の fail-closed で以後の全 DB テストが error になる — 本タスクで 4 回踏んだ型)。
**その他の `Exception` は再送出して `execution_error` にする** —
`KILL-03`(`expected_assertion_or_sqlstate_only`)の「期待外の例外を kill と数えない」契約を満たす。

**回帰テストで機械に固定した**: `DID NOT RAISE` の観測 / `pytest.fail` の観測 /
**`KeyboardInterrupt` と `SystemExit` を捕捉しないこと** / `RuntimeError` を kill と数えないこと。
**実機で回すまで分からなかった欠陥を、DB 不要の機械検査へ落とした。**

### 軸ごとに分けた判断が効いた

**231 件を一度に回していたら、24 件の軸の 1 件目で落ちた事実が埋もれ、
切り分けにもう一度全量を回すことになっていた。**
`r8_provisioning`(2 件)が先に通っていたため、
**適用経路は生きていて観測層だけの問題**と即断できた。
**件数の少ない軸から回す**という指示が実務で機能した。

### 台帳候補 D の 6 件目 — **記録が記録を妨げた 2 回目**

本節を worklog へ書き込もうとして **`secret_guard` にブロックされた**。
本文に設定ファイル名(`.` + `env`)を書いたためである。
**候補 D の実例 5(台帳へ誤検知を記録することが同じ誤検知で妨げられる)と同型で、通算 6 件目。**
回避は Write ツール経由での書き込み。

### 残る確認事項(PR 時)

**軸ごとに 3 プロセスで回したので、`MutationRunner._used_database_ids` の
プロセス横断の重複検出は未検証**である(「変異ごとに新 DB」の検査はプロセス内の集合で行う)。
**PR 前に絞り込みなしの全量 1 プロセス実行を 1 回行う**。

---

## 結果サマリ(PR #1・`/pr` クローズ処理・2026-09-10)

### 何を実装したか

**承認範囲はステップ 1〜20(第 2 群前半 = 実体を作る)。20/20 完了。**

| 群 | 成果 |
| --- | --- |
| **契約と実体**(1〜5) | 関数 body と DDL の SQL 実体を新設(**判定注記つき**)/ body manifest と 2 段照合 / DDL 生成器 / DDL 適用器(`ordered_steps` 5 件の順序を資産から読む)/ **4 ロール実接続 fixture**(`SET ROLE` を使わない) |
| **構成と越境**(6〜13) | カタログ検査(`R-1`・`R-2`・`R-8`)/ 越境の正例 6 セル・拒否例 / 共有関数の 6 前提の母集合を新設 / **6 前提 × 認可行列各行** / 表権限 **8 種** / 管理経路の代表 probe / **TOCTOU の 2 接続試験** |
| **原子性と残余リスク**(14〜16) | 失敗注入点 5 種の資産を新設 / **注入 → 再適用 → 一致**の検証 / **信頼境界の残余リスク試験**(GUC の成りすましを 3 文脈で実測) |
| **mutation**(17〜20) | kill 判定 5 条件を資産から導出 / **231 変異の全量実行(生存 0)** / MC/DC の写像 **47 判定** / **276 相互作用 + cut set 24 + MC/DC 128 入力** |

### 実機の測定値(ローカル・WSL2・run id なし)

| 対象 | 結果 |
| --- | --- |
| **DB テスト全件** | **110 passed / 5m19s** |
| **231 変異(軸別)** | `r8_provisioning` 2 / 9.31s ・ `configuration` 24 / 18.56s ・ `authorization_predicate` 205 / 38.24s |
| **231 変異(全量 1 プロセス)** | **65.10s ・生存 0 件** |
| 276 相互作用 + cut set 24 + MC/DC | **9 passed / 0.65s**(DB 不要) |
| ルート `pytest tests/` | **1183 passed** |
| backend `format` / `ruff` / `ty` | green |
| **凍結 15 パスの差分** | **0**(`origin/develop...HEAD` 基準) |

### 正本へ何を反映したか

| 正本 | 反映 |
| --- | --- |
| `docs/development/harness-evaluation.md` | **`## 候補` へ 2 件 + 既存候補 1 件へ実測補記**(`H-*` の新規採番なし・版は上げない) |
| `docs/README.md` | 台帳行の最終更新日と要約を現行化 |
| `docs/design/data-model.md` | **反映なし**(3 件の是正は TSK-348 で完了・PR #51 マージ済み) |
| 設計書 10.1 | **反映なし**(TSK-343 の所有。DoD の「差分 0」を満たす) |
| `.claude/core-areas.json` | **反映なし**(改訂 3 第 2 弾の `S-8`) |
| `docs/requirements/**` / `docs/adr/**` / `docs/ops/**` / `frontend/**` | **反映なし** |

### PO 裁定(2026-09-10 に 4 件)

| # | 論点 | 裁定 |
| --- | --- | --- |
| **`D-9`** | ステップ 17 の合格条件が凍結資産の定義と矛盾 | **ステップ 17 を撤去し `S-10` へ**(総数 21 → 20) |
| **`D-10`** | 改訂 3 第 1 弾のレビューが 2 周連続否決・起因過半(7.3-6 発火) | **射程縮小 — 改訂 3 を 2 弾に分割**(機械条件の確定はステップ 17〜20 完了後) |
| — | PR の本数 | **2 本**(PR #1 = ステップ 1〜20 / PR #2 = 改訂 3 第 2 弾 + reseal) |
| — | 認可判定 7 件の注記漏れ | **注記を追加して body を再凍結**(判定 40 → 47) |

### 本タスクで最も重要な発見

**テナント分離の中核が検証対象から漏れていた。**
`authorized_shared_rows` の `ON business_row.tenant_id = authorized_target.target_tenant_id` に
`-- DECISION:` 注記が無く、**MC/DC の被覆から外れていた**。外せば全テナントの行が返る条件である。
**捕まえられたのは、計画が同じ人手確認をステップ 1 とステップ 18 の 2 箇所に置いていたため**
(4 周目 `P1-2` が「機械は最初からの注記漏れを捕まえられない」と明記して二重に要求していた)。
**ステップ 1 でその確認をしたのは私で、そこで漏らした。計画の設計が正しく、実行が漏れた。**

### 申し送り(Notion へ起票する)

| 送り先 | 内容 |
| --- | --- |
| **改訂 3 第 2 弾**(PR #2) | `S-1`〜`S-10`。**論点 9 件を `S-10` に記録済み**。**写像表をどのステップの許容差分に入れるかを先に決める**(TSK-355 の助言) |
| **新規起票** | **`contract_only` 158 行の runtime テストの受取先タスク**(製品経路の実装)。`S-9` の 2 分割の ② |
| TSK-217 | **信頼境界の残余リスク**(GUC の成りすまし)。要求 3 件を worklog に記録済み |
| 申し送り(既報) | ADR-003 `D-12` と `contracts/authz/` の位置づけ / アプリ用ロールの `DELETE` / `H-57` のジョブ行 / `AUTH-*` と `CATALOG:*` の ID 体系の食い違い / `closure-handoff-data-model.json` の「作成」と正本の「作成・実行」の差 |

### マージ順序(4 タスクで合意・循環を解消済み)

```
TSK-348(済) → TSK-317 PR #1 → TSK-343 → TSK-317 PR #2 → TSK-355 → TSK-235
```

**TSK-355 は PR #2(再封印)を待つ** — 要件書を 1 文字でも変えると
`requirement-claims.json` の `source_blob_digest` と、私の `shared-preconditions.json` の
`git_blob_digest` が外れるため。**TSK-343 は PR #2 より前**(`test_ci_wiring.py` と
`core-areas.json` を双方が触るので、私が先方の更新後の版に乗るほうが rebase が小さい)。

---

## PR #2(改訂 3 第 2 弾)への申し送り — マージ後に判明した 2 件(2026-09-11)

### 【1】`S-8` の射程は 8 本では足りない可能性がある

**TSK-355 の実測による訂正を受けて再測定した結果**、3 つの数え方で値が違った。

| 数え方 | 件数 |
| --- | --- |
| **PR #1 で新設した検査器とテスト**(`git log --diff-filter=A` で実測) | **8 本・全件 `guard_paths` 未登録** |
| **私が TSK-355 へ伝えた数** | **6 本**(誤り) |
| **develop 上の未登録 `check_*` / `test_check_*` 全体** | **17 本** |

**私の 6 が誤りだった理由**: `S-8` に書いた「実測 6 本」を口頭で引用した。
**その 6 はステップ 14 時点の実測**で、**ステップ 18 で 2 本増えて 8 本になっていた**。
**`S-8` 自身に「ステップ 18 の 2 本を含めて 8 本になる」と書いてあるのに、古い数を引用した。**
→ **新規候補「母集団を人が列挙する検査は射程が動くと黙って古くなる」の 9 件目**であり、
**TSK-355 が観測した発火条件(記憶が「間違い」ではなく「一世代前」になる)がそのまま当てはまる。**

**より重い発見**: `guard_paths` に登録されている `check` 系は **6 本だけ**
(`check_design_propagation` / `check_doc_coverage` / `check_processing_stages` とその対)。
**`scripts/check_authz_catalog.py` — 凍結オラクル資産を検査する中央の検査器 — が未登録である。**

TSK-235 の申し送りが「**未登録のままでは、凍結資産を検査するスクリプトを弱めても
逐行確認が発火しない**」(台帳 `H-12` の型)と警告していたが、
**それが中央検査器そのものに当てはまっている。**

**未登録 17 本の内訳**:

| 分類 | ファイル |
| --- | --- |
| **PR #1 で新設(8)** | `check_authz_function_bodies` / `check_shared_preconditions` / `check_failure_injection_points` / `check_mcdc_map` と、それぞれの対 |
| **既存で未登録(9)** | `check_authz_catalog` / `check_docs_status` / `check_doc_profiles` / `check_nfr021_append_only` / `check_plan_docs_sync` と、対のうち 4 本 |

→ **`S-8` で判断すべき論点**:
**(a) 登録対象を「PR #1 の新設 8 本」に限るか、「未登録 17 本すべて」にするか。**
**(b) 既存 9 本の未登録は本タスクの射程か、別タスクか**
(**`check_authz_catalog.py` は本タスクが依存する中央検査器なので、
本タスクの射程だと主張できる**。一方 `check_docs_status` / `check_doc_profiles` /
`check_plan_docs_sync` は本タスクと無関係)。
**(c) 6.3 規則⑤(敵対レビュー + 人間承認)の負担が対象数に比例する**ので、
**(a)(b) の判断は PO 裁定へ上げるのが妥当と考える。**
**(d) 列挙をやめて `scripts/check_*.py` と対の `tests/test_check_*.py` の実在から
機械的に導く形にできないか**(候補の対応案 (b) と同型 — **ただし
`guard_paths` は `frozenset` の完全一致なので、パターン登録には
`core_guard.py` の変更が必要**であり、それ自体が別の射程になる)。

### 【2】台帳の候補 1 に交絡があった — 「3 者が独立」は言えない

**TSK-355 の指摘を受け入れる。** 候補 1 に「**3 者とも同じ原典を読み損ねた**」と書いたが、
**私の誤りは TSK-355 の誤りの下流である。**

| 者 | 誤り | 独立性 |
| --- | --- | --- |
| TSK-343 | 付記に理由を書かなかった | **独立** |
| TSK-355 | `Q-4` の括弧書きの 5 つ目を引用から落とした | **独立** |
| **TSK-317(私)** | **TSK-355 が引用した 4 段だけを見て「矛盾なし」と答えた** | **TSK-355 の欠落に依存(下流)** |

**正確には「2 者が独立に読み損ね、1 者がその欠落を継承した」。**

**示せること**: **読み損ねが連鎖する**(1 人の引用の欠落が下流の判断を汚染する)。
**示せないこと**: **3 者が独立に同じ誤りをする**。

**連鎖のほうがむしろ怖い性質である** — 構造原因(順序を書く場所が無く口伝で流通する)と
**直結する**ためで、**口伝の各ホップで欠落が起き、下流はそれを検証しない**。

→ **PR #2 のクローズ処理で台帳の候補 1 を是正する**(「示せること / 示せないこと」の書き分けを入れる)。
**TSK-355 と TSK-235 は同じ交絡を指摘し合って両方の文書で書き分けた**と報告しており、
**同じ形に揃える。**

---

## PR #2 の着手準備 — `S-1`〜`S-10` の実測(2026-09-11)

**PR #1 のマージ後、TSK-343 のマージを待つ間に実測した。**
**改訂 3 第 2 弾の起草に入る前の入力であり、この時点では計画書を変更しない**
(`status` は `in-review` のまま)。

### 実測の一覧

| # | 実測で確定した事実 |
| --- | --- |
| **`S-1`** | 入力資産 **8** + 封印資産 **6** = **14 パス**(凍結 15 パスは lock を含む数え方)。`reseal_policy` = `{normal_validation_reseals: false, dedicated_flag: "--reseal-oracle", human_review_required: true}`。**`oracle-seal.lock.json` 自身は `oracle_context.oracle_commit` を持たない**(`None`) |
| **`S-2`** | 解決対象は **468** = 本タスク所有 claims **178** + `positive_cases.cases[].test_owner` **6** + `two_factor_interactions` **276** + `ddl-elements` の表権限 **8**。**`::` を含む論理 ID は 0/468**。対して `catalog_test_owner` **187/187** と `schema_drift_test_owner` **198/198** は**実 node ID**(`::` あり) |
| **`S-3`** | 小項 ID は **`requirement-claims.json` の `list_item` 603 件**にある。**`req-universe.json` は `contracts/` ではなく `scripts/design_relations/` にあり別系統**(`tests/fixtures/` にも同名がある) |
| **`S-4`** | `auth-catalog.json` の `catalog_entry_id` は **187 件・一意 187・全件 `CATALOG:` 接頭辞**(`AUTH-*` は 0 件) |
| **`S-5`** | `proposal_status: pending_tsk_235_confirmation` / `pending_human_reviews` **2 件**(`PENDING-MANAGEMENT-COMMAND-COUNT` = `frozen_value: 8` / `alternative_value: 7` / `status: pending_human_decision`、`PENDING-ALL-LOGICAL-SCOPE` = `frozen_value: 29` / `alternative_value: null` / `status: pending_human_review`)。**status の綴りが 2 種ある** |
| **`S-6`** | `boundary-proposal.json` の owner は **TSK-235 が 3 件・TSK-250 が 1 件・TSK-217 が 1 件** |
| **`S-7`** | `scope` の現在値 = `{"status": "candidate_probe_only", "product_schema": false, "contains_sql_body": false, "second_group_approval_required": true}`。**変更するのは `status` と `second_group_approval_required` の 2 箇所** |
| **`S-8`** | PR #1 で新設した検査器とテストは **8 本・全件 `guard_paths` 未登録**。**develop 上の未登録 `check_*` / `test_check_*` は全体で 17 本** |
| **`S-9`** | `TSK-270-GROUP-2` は **`claim-mutant-map.json` の 178 件のみ**(他資産に 0 件) |
| **`S-10`** | `contract_only` 158 行 / 一意 152 ID(既測)。**`claims[].runtime_test_owner` 全体では 198 参照 / 187 一意で、8 組が ID を共有(重複 11 件)** |

### 計画書の記述との突合 — **食い違い 0 件だが、私の初回の数え方が誤っていた**

**`S-2` の「468 参照」を検算したところ、私の最初の測定は 474 と出て食い違った。**
原因は**私が「本タスク所有」で絞らず全 198 claims を数えた**こと。
**正しい内訳は 178 + 6 + 276 + 8 = 468 で、計画書の記述は正しい。**

**教訓**: **計画書の数字を疑う前に、計画書が何を数えているかを読む。**
本件は「計画書が間違っている」と誤認しかけた 1 例で、
**新規候補の型(母集団の定義の食い違い)の裏返し**である。

### 計画書に無い新事実 — **第 2 弾で扱う必要があるもの 4 件**

**① `S-6` に TSK-235 が抜けている(最重要)**

`boundary-proposal.json` の owner は **TSK-235 が最多の 3 件**である:

| 箇所 | owner |
| --- | --- |
| `boundaries[0]` `BOUNDARY:SHARED-AUTHORIZED-ROWS` の `aggregation_owner_task_id` | **TSK-235** |
| `boundaries[1]` `BOUNDARY:CONTROL-READS` の `aggregation_owner_task_id` | **TSK-235** |
| `deferred_equivalence_contract.owner_task_id` | **TSK-235** |
| `boundaries[2]` `BOUNDARY:REPRESENTATIVE-MANAGEMENT` の `aggregation_owner_task_id` | TSK-250 |
| `trust_boundary.verification_owner_task_id` | TSK-217 |

**`S-6` は「受取タスク(TSK-250 / TSK-217)」しか挙げていない。**
**集計の等価性契約が TSK-235 の所有**なのに受取契約に入っていない。
しかも **`proposal_status: pending_tsk_235_confirmation`** で
**TSK-235 の確認を待っている状態**である。
→ **`S-6` の射程に TSK-235 を加えるか、別要件を立てるかを第 2 弾で決める。**

**② `S-8` の射程が 8 本では足りない可能性**

`guard_paths` に登録されている `check` 系は **6 本だけ**
(`check_design_propagation` / `check_doc_coverage` / `check_processing_stages` とその対)。
**`scripts/check_authz_catalog.py` — 凍結オラクル資産を検査する中央の検査器 — が未登録。**
未登録 17 本の内訳 = **PR #1 の新設 8** + **既存 9**
(`check_authz_catalog` / `check_docs_status` / `check_doc_profiles` /
`check_nfr021_append_only` / `check_plan_docs_sync` と対のうち 4 本)。
→ **登録対象を 8 本に限るか 17 本にするかは PO 裁定へ上げる**
(6.3 規則⑤の負担が対象数に比例するため)。

**③ `S-10` の共有 ID は 152/158 だけの話ではない**

`contract_only` の 158 行で 6 組が共有していることは既測だが、
**`claims[].runtime_test_owner` 全体では 198 参照 / 187 一意で 8 組が共有**している。
**`S-2` の解決方式も共有 ID を扱えないと落ちる。**

**④ `S-1` の「7 ファイル × 1 行」の内訳**

> **【撤回 2026-09-12】本項の「`oracle_commit` は seal に存在しない」は誤りである。**
> **原典**: `contracts/authz/oracle-seal.lock.json:4` に**トップレベルの `oracle_commit`**(`dd2cb92...`)があり、
> **`scripts/check_authz_catalog.py:4521-4529` が 6 資産の `oracle_context.oracle_commit` との一致を要求する**。
> **誤った理由**: **存在しない入れ子 `seal["oracle_context"]["oracle_commit"]` を見て `None` を得た** — **探す場所を人が選んで外した実測である。**
> **計画書の `S-1` は最初から正しい。**(計画レビュー 3 周目 `P2`)

**`oracle-seal.lock.json` 自身は `oracle_commit` を持たない**(実測で `None`)。
`H-85` の実測が「6 資産と seal の `oracle_commit` 差し替え(7 ファイル × 1 行)」と記録しているが、
**seal 側は別のフィールド**である可能性がある。
→ **第 2 弾で実際に reseal を打つ前に、seal のどのフィールドが変わるかを実測する**
(`H-85` の候補①が記録する「委任プロンプトには誤った手順を書いていた」を踏まないため)。

### 第 2 弾の起草時にやること(順序つき)

1. **TSK-343 のマージを待ってリベース**(`test_ci_wiring.py` と `core-areas.json` が動く)
2. **リベース後に本節の実測を取り直す**(TSK-343 が触る 2 ファイルと、
   `check_authz_catalog` の出力〔`total=1078` 等〕が変わり得る)
3. **上記 ① ② を PO 裁定へ上げる**(`S-6` の射程 / `S-8` の対象数)
4. **写像表をどのステップの許容差分に入れるかを先に決める**(TSK-355 の助言)
5. 計画書を `status: in-review → active`・`承認: 未` へ戻して改訂
6. **敵対レビュー(コア領域なので必須)→ 人間承認 → 実装**

---

## 新事実 4 件の解決導線と、③ ④ の解決(2026-09-11)

| # | 論点 | 決めるのは | 状態 |
| --- | --- | --- | --- |
| **①** | `S-6` に TSK-235 が抜けている | **TSK-235 の事実確認 → PO 裁定** | **申し送り済み** |
| **②** | `S-8` の対象は 8 / 10 / 17 本 | **PO 裁定** | **選択肢を作成済み・裁定待ち** |
| **③** | 共有 ID(8 組)への解決方式 | **私**(第 2 弾の設計判断) | **解決** |
| **④** | seal のどのフィールドが変わるか | **実測で解決** | **解決** |

### ④ の解決 — `oracle-seal.lock.json` に `oracle_commit` は無い

> **【撤回 2026-09-12】本項の「`oracle_commit` は seal に存在しない」は誤りである。**
> **原典**: `contracts/authz/oracle-seal.lock.json:4` に**トップレベルの `oracle_commit`**(`dd2cb92...`)があり、
> **`scripts/check_authz_catalog.py:4521-4529` が 6 資産の `oracle_context.oracle_commit` との一致を要求する**。
> **誤った理由**: **存在しない入れ子 `seal["oracle_context"]["oracle_commit"]` を見て `None` を得た** — **探す場所を人が選んで外した実測である。**
> **計画書の `S-1` は最初から正しい。**(計画レビュー 3 周目 `P2`)


`scripts/check_authz_catalog.py:4464-4493` の `_build_oracle_seal()` が書き換えるのは **2 種類だけ**:

- **`input_assets[].git_blob_digest`**(**8 件** — 各入力資産の blob digest を再計算)
- **`sealed_assets[]` を丸ごと再構築**(**6 件** — `path` / `asset_role` / `asset_kind` / `canonical_sha256`)

> **【撤回 2026-09-12】本項の「`oracle_commit` は seal に存在しない」は誤りである。**
> **原典**: `contracts/authz/oracle-seal.lock.json:4` に**トップレベルの `oracle_commit`**(`dd2cb92...`)があり、
> **`scripts/check_authz_catalog.py:4521-4529` が 6 資産の `oracle_context.oracle_commit` との一致を要求する**。
> **誤った理由**: **存在しない入れ子 `seal["oracle_context"]["oracle_commit"]` を見て `None` を得た** — **探す場所を人が選んで外した実測である。**
> **計画書の `S-1` は最初から正しい。**(計画レビュー 3 周目 `P2`)

**`oracle_commit` は seal に存在しない**(実測でも `seal["oracle_context"]["oracle_commit"]` は `None`)。

→ **`H-85` の実測が言う「6 資産と seal の `oracle_commit` 差し替え(7 ファイル × 1 行)」の
7 ファイル目は seal ではない**か、**当時の seal の構造が違った**ことになる。

**本タスクの 2 段コミットの正しい形**:

| 段 | 内容 |
| --- | --- |
| ① 基準コミット | 入力資産・封印資産・検査器・追随テスト・derived lock を変更する |
| ② reseal コミット | **6 資産の `oracle_context.oracle_commit` を ① の SHA へ手で書き換える** + **`--reseal-oracle` で seal を再生成**(seal 側に手で書く行は無い)+ **人間査読**(`reseal_policy.human_review_required: true`) |

**第 2 弾で実際に打つ前に、この形が成立することを空実行で確認する**
(`H-85` の候補①が記録する「委任プロンプトには誤った手順を書いていた」を踏まないため)。

### ③ の解決 — 共有 ID は「解消するのではなく表現する」

**実測: 共有 ID は 8 組・重複参照 11 件で、構造が 2 種類に分かれる。**

| 組 | 行数 | 受取先 | 共有の意味 |
| --- | --- | --- | --- |
| `TSK-250.runtime.FR-041/list_item-006` | 3 | TSK-250 | 招待発行の「権限 / 状態 / 定員」を 1 テストで覆う |
| `TSK-250.runtime.FR-041/list_item-007` | 4 | TSK-250 | 招待受諾の「権限 / 状態 / 定員 / 直列化」を 1 テストで覆う |
| `TSK-270.group2.runtime.FR-034/heading-002/list_item-005` | 2 | GROUP-2 | 「読み取り / 操作権限」 |
| 同 `list_item-006` | 2 | GROUP-2 | 同 |
| `TSK-270.group2.runtime.FR-035/list_item-005` | 2 | GROUP-2 | 「主体状態 / 失効」 |
| 同 `list_item-010` | 2 | GROUP-2 | 「パスワード状態 / 失効」 |
| **`TSK-270.group2.runtime.FR-041/list_item-014`** | 2 | GROUP-2 | **理由コードが混在する唯一の組**(`no_db_decision_point` + `route_universe_pending`)— 裁定 `D-9` で 2 分割へ再是正した組 |
| `TSK-270.group2.runtime.NFR-011/list_item-002` | 2 | GROUP-2 | 「資格情報状態 / 失効」 |

**混在は 1 組だけで、残り 7 組は `execution_class` も受取先も理由コードも均一。**
**共有は意味のある設計である**(1 テストが複数の claim を覆う)。

→ **`S-2` の解決方式**: 「論理 ID → node ID の 1 対 1」ではなく、
**「claim 行 → 論理 ID が多対 1」+「論理 ID → node ID が 1 対 1」の 2 段構造**にする。
**共有を解消しようとしてはいけない。**

→ **負例の素材になる**: `S-10` の不変条件
「**同一 `runtime_test_owner.id` を共有する行の `receiving_task_id` は一致する**」は
**8 組すべてで実際に成立している**(混在 1 組も受取先は同一)。
**負例は「1 組の受取先を意図的に食い違わせると red」で書ける** — **架空のデータを作る必要がない**。

### ② の選択肢(PO 裁定へ)

| 案 | 対象 | 評価 |
| --- | --- | --- |
| **A** | PR #1 の新設 **8 本**のみ | 最小。ただし **`check_authz_catalog.py` が未登録のまま残る** |
| **B(推奨)** | **10 本**(A + `scripts/check_authz_catalog.py` と `tests/test_check_authz_catalog.py`) | **本タスクが依存する中央検査器を含む**。無関係な分は別タスクへ |
| **C** | **17 本**(未登録すべて) | 網羅的だが `check_docs_status` / `check_doc_profiles` / `check_plan_docs_sync` は本タスクと無関係で**射程が汚れる(`H-68` 型)** |

**裁定のタイミング**: **TSK-343 が `core-areas.json` を改訂する**ので、
**リベース後に対象集合を測り直してから裁定を仰ぐほうが手戻りがない。**

---

## `S-6` の受取先の食い違い — TSK-235 との突き合わせ結果(2026-09-11)

### 経緯

**私の照会**: `boundary-proposal.json` の owner が TSK-235 が最多の 3 件なのに
`S-6` の受取契約から漏れている。所有の事実確認と `pending_tsk_235_confirmation` の解消主体を問うた。

**TSK-235 の回答**: 受け取れない。本タスクの所有ではない。
**核心の主張** = 「**機構を持つことは、その対象を所有することではない**」
(`generated_sql_expression` を生成する**機構**は本タスクの射程だが、
**対象別の生成物は作らない** — `ADR-003` 段階 2)。

### 原典で確認したこと

| 先方の申告 | 確認結果 |
| --- | --- |
| 計画書は **`承認: 未`** | **正**(`domain-calc-dsl/plan.md:4`)。**私の問い「あなたの承認済み計画書に射程として入っているか」は前提が成立していなかった** |
| `BOUNDARY` 等の語が **0 件** | **正**。`design.md` の 5 件は**すべて私の照会と先方の分析を記録した行**(`:1056-1065`)で、照会前は 0 件 |
| `aggregation_location: generated_sql_expression` | **正** |
| **「(β)①〜⑧ に認可境界の集計は含まれない」** | **誤り**(下記) |

### 先方の 1 点が事実に反していた — **(β)④ は「共有集計 FR-041」**

**要件書 v2.1 の変更履歴に逐語**:

> **(β) は判定規則＋閉じた列挙①〜⑧**(分析集計 FR-025〜027 / カルテ掲載指標 /
> シーズン通算 / **共有集計 FR-041** / チーム集計 FR-042 / 移行時の座標変換 FR-038・付録B-5 /
> 打球方向の導出 / 移行時の責任投手導出 FR-038)

**FR-041 本文(`:490` 以降)が「共有指標の定義の正」として集計を定めている**:

> **共有される指標は次の 3 つに限られる**(**本項が共有指標の定義の正**)—
> ① 付録A-5 のチーム概要 7 項目 ② **チーム合算の率指標**
> (**通算の分子分母から算出し個人の率を平均しない**)③ 選手個別の指標
> **補足(範囲)**: **突合は集計値の比較まで**とする

`BOUNDARY:SHARED-AUTHORIZED-ROWS` の `product_entry_contract` は
**`single_query_combines_authorized_rows_and_generated_aggregation`** であり、
**この `generated_aggregation` が共有集計 FR-041 に当たる**。

→ **したがって要件書の改訂は不要である**(既に列挙済み)。
**先方の二者択一「Yes なら要件書の改訂が要る / No なら認可側へ移す」はどちらも当たらない。**

### 結論は同じ・帰結が違う

| | 先方の案 | 実測に基づく案 |
| --- | --- | --- |
| TSK-235 の所有か | **否** | **否**(同じ) |
| 要件書の改訂 | 必要(Yes の場合) | **不要**(FR-041 は既に (β)④) |
| owner の移し先 | 認可側(TSK-317 / TSK-250) | **FR-041 の対象別生成タスク**(ADR-003 段階 2・**未起票**) |

**認可側へ移すのは誤り** — **集計は認可ではない**ので TSK-317 も TSK-250 も所有できない。
**今日 `D-9` で `contract_only` 158 行に対してやったのと同じ形**(受取先タスクを新規起票する)になる。

### `BOUNDARY:CONTROL-READS` は別問題 — **単純な資産の欠陥**

```json
{
  "boundary_id": "BOUNDARY:CONTROL-READS",
  "aggregation_location": "none",          ← 集計が無い
  "aggregation_owner_task_id": "TSK-235",  ← なのに owner がある
  "product_entry_contract": "paged_rows_only"
}
```

**集計が存在しないのに owner を持っている。** NFR-018 の議論とは無関係で、
**owner を外すだけで片付く。裁定を要しない。**

### `guard_paths` — **先方の「数ではなく規則で」を採る**

先方の指摘(逐語):

> **「10 本」と数で書くと、次に検査器が増えたとき同じ裁定をやり直すことになります**
> (あなたが台帳へ出した「母集団を人が列挙する検査は射程が動くたびに黙って古くなる」の実例が
> **この裁定自身**になってしまいます)

**これは決定的である。** 私は**自分が台帳へ出した候補の型を、その候補を出した直後の裁定で踏むところだった。**

→ **採用する基準**: **「凍結資産(オラクル・封印台帳)またはコア領域の成果物を検査する検査器と、その対」**
この規則なら **10 本が導出され、18 本目が増えても自動で判定できる**。

**ただし機構上の制約がある** — **`guard_paths` は `core_guard.py` で `frozenset` の完全一致**であり
(`areas[].paths` は `fnmatch` だが `guard_paths` は違う)、
**規則をそのまま機械化するには `core_guard.py` の変更が必要**。
→ **規則を裁定で確定し、当面は規則から導いた 10 本を列挙で登録、機械化は別タスク**とする。

### PO 裁定へ上げる論点(4 件)

| # | 論点 | 備考 |
| --- | --- | --- |
| **1** | **`SHARED-AUTHORIZED-ROWS` の `aggregation_owner_task_id`** を「未起票の対象別生成タスク」へ移し、そのタスクを起票するか | **要件書の改訂は不要** |
| **2** | **`CONTROL-READS` の `aggregation_owner_task_id`** を外す | **裁定不要・事実の是正**として実施 |
| **3** | **`deferred_equivalence_contract.owner_task_id`** の移し先 | **等価性の検査機構は基盤側かもしれない**ので TSK-235 の確認待ち |
| **4** | **`guard_paths` の登録基準**を「凍結資産またはコア領域の成果物を検査する検査器とその対」とする | 当面は列挙で 10 本・機械化は別タスク |

**1〜3 が片付くまで `boundary-proposal.json` を再封印しない**(先方の要求は正当)。
**封印資産なので、誤った owner を固定すると `--reseal-oracle` + 人間査読をやり直すことになる。**

---

## `S-6` の論点 — TSK-235 と 4 件すべてで見解が一致(2026-09-11)

**先方が (β)④ の件を原典で確認して訂正した**(先方コミット `04baf07`)。
**先方の自己訂正の要点**: 「二者択一の『Yes の枝』の帰結を誤っていた。
『対象計算である → 要件書の改訂が要る』ではなく
『対象計算である → **既に列挙済みなので改訂は不要・受取先は段階 2 のタスク**』が正しい」。

**核心の主張(機構と対象の区別)は双方で維持**され、**結論も変わらない**。

### 一致した見解

| # | 論点 | 一致した結論 |
| --- | --- | --- |
| **1** | `SHARED-AUTHORIZED-ROWS` の `aggregation_owner_task_id` | **段階 2 の未起票タスクへ移す。要件書の改訂は不要** |
| **2** | `CONTROL-READS` の `aggregation_owner_task_id` | **外す。裁定不要**(`aggregation_location: none` なのに owner がある単純な資産の欠陥) |
| **3** | `deferred_equivalence_contract.owner_task_id` | **段階 2 側へ**。**機構は TSK-235 だが契約の中身は対象別** |
| **4** | `guard_paths` の登録基準 | **規則で確定 + 当面 10 本を列挙 + 機械化は別タスク** |

### 先方が追加した実測 2 件

**① `deferred_equivalence_contract` は載せ先の機構すら未確定**

先方の実測: 本タスクの plan / design に **FR-041 も本契約も記述は 0 件**。
**判定は 2 段** — (1) 等価性の**検査機構**は先方の射程(§8-3 プロパティ runner・§7 経路一致検査)で、
**私の見立て「等価性の検査機構は基盤側」は正しい**。
(2) **しかし `comparison` の中身は対象別の契約 = 段階 2**。

**さらに形の食い違いがある**:
`product_entry_equals_direct_authorized_rows_plus_generated_aggregation` という形は、
**§8-3 の (β)①〜⑤ の等価性(複合 target の最終表示文字列 ⇄ 表示まで生成した参照実装)とは形が違い**、
むしろ **§7 の経路一致検査(製品経路の出力が宣言された生成物の出力と一致する)に近い**。

→ **どちらの機構へ載るかは、対象別契約が書かれるまで決まらない。**
**owner を先方にしても載せ先が決まらないので前へ進まない。**

**② 起票は 1 本にまとめるべき(先方の提案)**

**論点 1 と 3 は同じ未起票タスクの受取になる**ので、**起票するなら 1 本**にできる
(**FR-041 の対象別生成 + その等価性契約**)。
**別々に起票すると「契約だけ先に決まって生成物が無い / 逆」という状態が生じる。**

→ **この提案を採る。** 今日 `D-9` で `contract_only` 158 行の受取先を起票する話も出ているので、
**起票は 2 本になる**(① FR-041 の対象別生成 + 等価性契約 / ② `contract_only` 158 行の runtime テスト)。

### `guard_paths` の機械化タスクは TSK-235 の §11 と隣接する(先方の警告)

**先方は §11 で `core_guard.py` の基線機構を触る予定**である。
**機械化を別タスクにするなら同じファイルを触る**ので、
**起票時に順序か所有を決める必要がある**。**いまは決めなくてよいが、起票時に先方へ声をかける。**

### 私の側の誤りの記録

**照会の前提が誤っていた** — 「あなたの**承認済み**計画書に射程として入っているか」と問うたが、
**先方の計画書は `承認: 未`** であり、**前提が成立していなかった**。
**他タスクへ照会するときに相手の計画書の承認状態を確認していなかった。**

**これは今日 3 回目の「相手の原典を読まずに問う / 答える」である**
(① TSK-343 へ設計書 10.1 の誤った提案 ② TSK-355 の総順序に「矛盾なし」と答えて `Q-4` の原典を読まなかった
③ 本件)。**台帳候補②(順序の付記)の「読み損ねが連鎖する」の実例がさらに 1 件増えた。**

---

## PR #2 の PO 裁定(2026-09-11・山田正輝)— **TSK-343 の完了を待たず先に取得**

**取得の判断**: **裁定は人間の手番で TSK-343 と内容の依存が無い**ため先に取り、
**計画書の改訂は TSK-343 のマージ後**に行う(**改訂すると `承認: 未` に戻り、
TSK-343 の残り 20 ステップ分ずっと未承認状態が現在地導出に出るため**)。
**PR #1 も `D-1`〜`D-4` を先に取ってから計画を書いた**ので同じ手順である。

**依存の確認(実測)**: TSK-343 は `.claude/core-areas.json` を触るが **`areas[].paths` のみ**で、
**本タスクの裁定対象 `guard_paths` とは別キー**(先方の詳細設計 9 節 4 項が
「**本タスクは `guard_paths` を増やさない**」と明文で決めている)。**内容の依存は無い。**

### 裁定 3 件 + 事実是正 1 件

| # | 論点 | 裁定 | TSK-235 との一致 |
| --- | --- | --- | --- |
| **D-11** | `SHARED-AUTHORIZED-ROWS` の `aggregation_owner_task_id` | **新タスクを起票して移す** — 「**FR-041 の対象別生成 + 等価性契約**」を **1 タスク**として起票し owner をそこへ。**要件書の改訂は不要**((β)④ として既に列挙済み) | **一致** |
| **D-12** | `deferred_equivalence_contract.owner_task_id` | **D-11 と同じ新タスクへ移す**。**載せ先の機構が未確定でも、契約が書かれればどちらか決まる** — **owner を TSK-235 にしても載せ先が決まらないので前へ進まない** | **一致** |
| **D-13** | `guard_paths` の登録基準 | **規則で決める** — 「**凍結資産またはコア領域の成果物を検査する検査器とその対**」。**当面は規則から導いた 10 本を列挙で登録し、機械化は別タスク** | **一致** |
| — | **`CONTROL-READS` の `aggregation_owner_task_id`** | **裁定不要・事実の是正として実施** — **`aggregation_location: none` なのに owner がある単純な資産の欠陥** | **一致** |

**`D-11` と `D-12` を 1 タスクにまとめる理由**(PR #1 のクローズ処理で決めた形を維持):
**別々に起票すると「契約だけ先に決まって生成物が無い」状態が生じる。**

### `D-13` の射程 — **件数ではなく規則で決めた理由**

**S-8 の実測は「PR #1 の新設 8 本が未登録・develop 全体で 17 本未登録」だったが、
件数で裁定すると着手時に古くなる**:

- **`coverage-artifact-scan`(#54)で私が `tests/test_repository_artifacts.py` を 1 本増やした**
- **TSK-343 がステップ 28 で新設資産を登録する**(ただし `areas[].paths` 側)

**これは本日 13 件積んだ「母集団を人が列挙する検査は射程が動くと黙って古くなる」型そのもの**なので、
**母集団のスナップショット(件数)ではなく母集団の定義(規則)で決めた。**

### TSK-343 の追加材料 — **規則を導出型にすれば古くなる経路が消える**(2026-09-11)

**先方の指摘**: **`guard_paths` と `areas[].paths` は効く仕組みが違うので、
規則も別に書かないと同じ言葉で両方を縛ってしまう。**

| 配列 | 照合 | 過小のリスク | 過剰のリスク |
| --- | --- | --- | --- |
| `areas[].paths` | **`fnmatch.fnmatchcase`**(`*` が `/` を跨ぐ) | **glob で覆えば構造的に低い** | 設計書 `:368` 規則④ が「**含む側に倒す**」と明示 |
| **`guard_paths`** | **`path in frozenset`**(完全一致) | **列挙漏れが黙って通る**(`H-12` の再発型) | 実在しないパスを書いても検出されない可能性 |

**`guard_paths` 側だけが「列挙漏れが黙って通る」性質**を持つ。

**したがって機械化タスク(`D-13` が別タスクへ送る分)の中身は
「登録対象の条件に合致するのに `guard_paths` に無いファイルがあれば red」という導出型の検査**とする。
**件数でも人手の列挙でもなく、条件から導出する形。**
**先方がステップ 4・6 で `_missing_implementation_tables` と FK の保留集合を manifest から導出したのと同じ形。**

**この形なら規則が古くなる経路自体が消える** — **`D-13` の「当面は 10 本を列挙」は
機械化までの暫定であり、暫定であることを明示して記録する。**

### 次の手順

| # | 段 | 担い手 |
| --- | --- | --- |
| 1 | **TSK-343 のマージを待つ**(現在 **9/28**) | — |
| 2 | **リベース → `S-1`〜`S-10` の実測を取り直す**(TSK-343 が触る 2 ファイルと `check_authz_catalog` の出力が変わり得る) | Claude |
| 3 | **新規 2 件の起票** — ① `D-11`/`D-12` の受取タスク(FR-041 対象別生成 + 等価性契約)② `contract_only` 158 行の runtime テスト受取先 | Claude |
| 4 | **写像表をどのステップの許容差分に入れるかを決める**(TSK-355 の助言) | Claude |
| 5 | 計画書を `status: in-review → active`・`承認: 未` へ戻して改訂 | Claude |
| 6 | **敵対レビュー(コア領域なので必須)→ 人間承認 → 実装** | Claude + 人間 |

**`D-11`〜`D-13` はこの改訂で計画書の「PO 裁定」節へ転記する。**

---

## 計画レビュー 3 周目の採否(2026-09-12)

**判定: 否決(P0 5 / P1 3 / P2 1)。** ただしレビューは
**「入力資産不変なら `oracle_commit` を動かさず `--reseal-oracle` 1 回で最終 seal を再構築できる」分析**と
**`S-5` の 3 status 値・`alternative_value` / `review_id` 維持の判断**に
**「単独の指摘はありません」と明記**した — **本改訂の構造の核は 3 周目で通った。**

| # | 重大度 | 要旨 | 採否 | 反映内容 |
| --- | --- | --- | --- | --- |
| 1 | P0 起因 | 旧 1〜20 表と新 1〜7 表が**どちらも機構の読取対象**。実測 `StepTable(valid=False, total=20)` | **採用** | **旧表を「実装ステップ」見出しの配下から外し**、`### 第 1 弾の実施記録` として**機構の射程外**へ置いた。**`feature_status.parse_step_table` と `codex_run.step_table_status` の双方で確認**(`valid=True, total=3` / `OK`) |
| 2 | P0 起因 | 第 1 弾の記録化宣言が全文へ伝播していない(7 箇所 + design.md)。特に検証節が**変更する 3 資産に差分 0 を要求**し、正しく実装しても必ず失敗する | **採用** | 2 節・3 節・4 節(oracle 節・規模)・4 節末・5 節・6 節(回帰)・7 節(検証・人間確認)・8 節・design.md 冒頭を**第 1 弾の記録**として書き分けた。**検証節は「11 パス不変」+「4 パスに差分があること」+「入力側の封印が動いていないこと」の 3 本へ作り直した** |
| 3 | P0 起因 | `S-9` が正しい受取先を保証しないまま再封印する(**178 行を任意の同一文字列にしても通る**) | **採用** | **分類 → 受取先の閉じた写像**(`runtime_target` 有 → `TSK-317` / 無 → 起票済みタスク)を**exact に検査する**合格条件へ。**負例 4 件**(全行同一文字列 / `contract_only` を `TSK-317` / 任意の非空文字列 / 同一 owner ID を別受取先)。あわせて**「封印するのは所有者であって契約ではない」**を明記し、**所有者が資産から決まる**ことを示した |
| 4 | P0 起因 | 7 ステップの封印順序が段階実装と両立しない(**ステップ 1〜4 が封印資産を変えるのに reseal は 7 だけ** → 各完了点が red) | **採用** | **封印は原子単位**という実測を計画へ書き、**封印資産を変える作業をすべて 1 コミットへ収めた**。**3 ステップ**へ再構成(1: 期待件数撤去 / 2: `core-areas.json` 登録 — **どちらも封印資産に触れない** / 3: 封印資産の確定と reseal) |
| 5 | P0 起因 | owner 削除条件の母集団が広すぎる(`BOUNDARY:REPRESENTATIVE-MANAGEMENT` も `aggregation_location: none` で正当な `TSK-250` owner を持つ) | **採用** | **条件での一般化をやめ、3 境界の閉じた表**へ。`REPRESENTATIVE-MANAGEMENT` は**`TSK-250` のまま不変**と明記し、**owner を消すと red** を負例に置いた。**実測で確認**(`boundary-proposal.json` の 3 境界) |
| 6 | P1 起因 | `D-13` の当面対応と合格条件が同じ機能を二重配置。固定 10 本の比較では 11 本目を検出できず、それを検出する処理こそ別タスクへ送った導出型 | **採用** | 本改訂の合格条件を**「列挙した 10 本 + 9 パターンが登録されており 1 件外すと red」まで**に限定。**導出型の一般化は別タスク**と書き分け、**PR #56 の `schema_contract_asset_paths()` が同型の先例**であることを記録した。`areas[].paths` 側も**9 パターンを実値で確定**(実測で既登録分を除外) |
| 7 | P1 非起因 | `S-7` の実行証跡参照が検証不能(`scope` は 4 キー exact で参照フィールドを足せない) | **採用** | **機械条件から外し `[手動・外部]` へ**。**参照は PR 本文と worklog に置く**(第 1 弾 PR #52 / `67e06a2` の当該 CI run)。**資産へ置けない理由を実測(`_expect_keys`)つきで明記** |
| 8 | P1 非起因 | 「期待件数ハードコード 20 箇所」の母集団が閉じていない | **採用** | **「20 箇所」を撤回**(**根拠のない数だった**)。**AST の規則で母集団を定義**し(`Eq` 1 個・右辺 int リテラル・左辺 `len(...)` か `count`・`returncode` と `== 0` を除外)、**走査器 + allowlist(理由必須・規則で再導出可能)** の形へ。**計画書に件数を書かない** |
| 9 | P2 起因 | PR #3 への射程表が 1:1 でない(`D-14` の列に `S-8` が無い / `S-1` 残部の扱いが送り先表と食い違う) | **採用** | `D-14` の PR #3 列へ **`S-8` の残部**を追加。**`S-1` は本改訂で完結**し、**PR #3 の 2 段コミットは `S-1` の残部ではなく PR #3 自身の実装制約**であると書き分けた |

**あわせて 2 周目の P2(worklog の誤記)を反映した** — `oracle_commit` は seal に存在しないとする
**3 箇所(`:1197` `:1224` `:1231`)へ撤回注記**を置いた。

**射程の変更はない**(`D-14` の封印系のまま)。**ステップ数が 7 → 3 になったのは
封印の原子性による構成変更であり、やることの増減ではない。**

---

## 計画レビュー 4 周目の採否(2026-09-12)

**判定: 否決(P0 2 / P1 2 / P2 1)** — 3 周目の P0 5 / P1 3 / P2 1 から減った。
レビューは **3 ステップ化・ステップ 1/2 が封印資産に触れない点・11 + 4 = 15 の分割・
2-c の対象フィールド**を**「妥当です」と明記**した。

**残る P0 2 件はどちらも `S-9`(受取先 178 件)に集中していた** → **PO 裁定 `D-15` で PR #3 へ送った。**

| # | 重大度 | 要旨 | 採否 | 反映内容 |
| --- | --- | --- | --- | --- |
| 1 | P0 起因 | `S-9` の写像は**置換後に対象集合を再導出できない**。実測は全 **198 行**で、`runtime_target` あり 33 行 = 対象 20 + 既存 `TSK-250` 13 / なし 165 行 = 対象 158 + 既存 `TSK-217` 7。分類だけで当てると**正しい既存 20 行を誤移管する** | **採用**(裁定 `D-15`) | **`S-9` を丸ごと PR #3 へ**。**実測で指摘が正しいことを確認した**(`claim-mutant-map.json` の 198 行を集計) |
| 2 | P0 起因 | `contract_only` の受取先 UUID が**所有者だと保証されていない**。受取タスクの名は「受取先を**決める**」で、受取側 DoD と read-back は PR #3 へ延期されている | **採用**(裁定 `D-15`) | 同上。**158 行は `contract_only_reason_code: route_universe_pending` = 経路の母集合が未確定だから受取先が決まっていない行**であり、**決まる前に確定所有者として封印することになる**。`D-14` の「受取タスクが未着手なら PR #3」と同じ理由で一貫させた |
| 3 | P1 起因 | 期待件数走査の母集団に**実際の取りこぼし**。`attempts == 894` と `Counter({"allow": 6, "deny": 6})` が規則から漏れる | **採用** | **規則を「`assert` 文中のすべての `int` リテラル」へ広げ、位置の限定をやめた**。**除外は `returncode` 比較の 1 種類だけ**にし、**それ以外は allowlist へ理由つきで載せる**形へ。**allowlist のキーを `(テスト関数名, 整数値)`** にして行移動に耐えさせた。**実測: 40 組 47 出現**(取りこぼしていた 2 件を含む) |
| 4 | P1 起因 | 検証 2-b は**4 パス中 1 本に差分があれば通る**。`ddl-elements.json` を空白だけ変えても 2-b・資産検査・2-c のすべてを抜ける | **採用** | **2-b をパスごとのループへ**(3 パスそれぞれに差分を要求)。**2-c を新設**し、**seal の `canonical_sha256` が実際に動いた資産の集合が期待と完全一致すること**を検査する形にした(**空白だけの変更では canonical が動かないので落ちる**)。あわせて `oracle_commit` / `semantics` / `input_assets[].git_blob_digest` の不変も同じスクリプトで見る |
| 5 | P2 起因 | 第 1 弾の記録化が**影響節へ伝播し切っていない**(3 節の `core-areas.json` 行・正本体系外の表・「現在のステップ表は 1〜20」・`(ステップ k)` と `(ステップ k/3)` の競合) | **採用** | 3 節の 4 行と core-guard の段落を本改訂の実態へ。**`(ステップ k/3)` を撤回し `(ステップ k)` へ戻した**(4 節「ステップ番号の規律」が総数接尾辞を付けないと定めている) |

### 射程の変更(**7.3-6 の記録**)

**裁定 `D-15` により `S-9` を PR #3 へ移した。** これは**縮小**である。
**理由**: **受取先が未確定の行を確定所有者として封印することになる**ため。
**影響**: 本改訂が触る封印資産が **3 本 → 2 本**(`boundary-proposal` / `ddl-elements`)。
**凍結 15 パスの不変側が 11 → 12 パス**。**ステップ 3 の内訳が 3-a〜3-e → 3-a〜3-d**。
**ステップ数は 3 のまま変わらない。**
**射程が変わったので、次周(5 周目)は全文レビューとする**(7.3-4)。

---

## 計画レビュー 5 周目の採否(2026-09-12)

**判定: 否決(P0 1 / P1 2 / P2 1)。** P0 は **5 → 2 → 1**。
レビューは **`S-9` の PR #3 送りが空手形でないこと**・**3 ステップの分割**・
**ステップ 3 への封印変更の集約**・**`S-5`・`S-7` の確定値と資産・検査器の同時変更**を
**「妥当」と明記**し、**機構が読むステップ表が `valid=True, total=3` であること**も確認した。

| # | 重大度 | 要旨 | 採否 | 反映内容 |
| --- | --- | --- | --- | --- |
| 1 | P0 起因 | **seal の不変検査が重複行を見逃す**。`check_authz_catalog.py:4531-4566` は `input_assets` の path を `set` へ積むだけなので、**同じ path の行を複製しても green**。前周で私が足した検証 2-c も辞書化していて同じ穴を持つ | **採用** | **自分でも実測して確認した**(`validate_oracle_seal` を直接呼び、**正常 8 行 = green / 重複行を足した 9 行 = green**)。**ステップ 3 へ 3-d を新設**し、**`sealed_assets` 側に既にある `if path_text in sealed_by_path: raise`(`:4582`)と同じ形へ入力側を揃える**。**検証 2-c は辞書ではなく「行の列」で比べる形へ** |
| 2 | P1 起因 | **ステップ 1 の母集団が閉じていない**。`"total=1078 auth_claim=184 out_of_scope=894"`(`:350`)のような**文字列のハードコード**が走査外で、**整数を変数や文字列へ移すだけで抜けられる** | **採用** | **規則を 3 種へ広げた** — ① `int` リテラル ② **2 桁以上の数字列を含む `str` リテラル** ③ **`int` リテラルに束縛された参照名**。**負例も「文字列へ移すと red」「定数へ退避すると red」を追加** |
| 3 | P1 起因 | **allowlist の `(関数名, 整数値)` が出現を一意に識別しない**。現物では **47 出現が 40 キーへ潰れ**、同じ関数へ資産件数の `== 1` を足しても同じキーで許可される | **採用** | **キーを `(テスト関数名, 種別, 値, 出現回数)` へ**。**出現回数を持たせたのが要点**で、**同じ関数の同じ値でも回数が増えれば red** になる |
| 4 | P2 起因 | **`D-15` の射程変更が全文へ伝播していない**(送り先「6 件」/ sealed asset「3 本」が 2 箇所 / 旧記録節の `S-1`〜`S-10` 全件が第 2 弾射程という現在形) | **採用** | 7 件・2 本へ是正。**旧記録節の冒頭へ「本表は要件の記録であり射程の割り当てではない。割り当ての正は 2 節の送り先表」**と明記 |

### 射程の追加(**記録**)

**`input_assets` の重複行検査(3-d)を本改訂へ入れた。** **これは射程の拡大ではない** —
**本改訂の DoD が「`input_assets` 8 件が不変」を要求している**のに、
**検査器が 9 行を通すならその DoD は検証できない**。**自分の受け入れ基準を検査可能にする是正**である。
**`sealed_assets` 側には既に同じ検査があり、入力側だけが持っていないという非対称**だった。

### マージ順序の確定(**PO 裁定 2026-09-12**)

```
TSK-348(済)→ TSK-317 PR #1(済)→ TSK-343(済)→ TSK-317 PR #2
  → TSK-355 → TSK-317 PR #3 → TSK-235
```

**`D-14`・`D-15` で新設した PR #3 が 2026-09-11 の総順序に席を持っていなかった**ため、
**TSK-355 のタブへ申し送り → 先方が PO へ提示 → 案 A(こちらの推奨)で裁定**。
**根拠**: **TSK-355 の確定ゲートが最長区間**(前例 TSK-278 で 27 周)で、
**この順序なら PR #3 はその間に計画・実装を進められる**(待つのはマージだけ)。
**逆順は 2 つの長い作業が直列になり、さらに TSK-355 の承認済み計画の改訂と再承認で人間の手番が 1 回増える。**
**分担どおり、本計画書 2 節へ順序を書いた。**

### 台帳候補の素材(**/pr のクローズ処理で判断する** — 2026-09-12)

**「同じ性質の検査が片側にしかない」型**。**`sealed_assets` には重複拒否(`:4582`)があるのに
`input_assets` には無い**(5 周目 `P0-1`・自分で実測)。

**TSK-355 のタブが指摘した観点**: **人が「どちら側に検査を置くか」を選んだ結果、
選ばれなかった側が空く。** **本 PR が積んでいる列挙型の候補(母集団を人が列挙する検査)と隣接する** —
**どちらも「人が選んだ範囲が、選ばなかった範囲を黙って無検査にする」**。

**まだ傾向として確定していないので `H-*` は与えない。**
**同型の 2 例目**(`sealed`/`input` の非対称)**が出た時点で候補として立てるかを /pr で判断する。**

### 起因比で見る規律(**TSK-355 との相互確認** — 2026-09-12)

**件数の推移だけでは収束を判定できない。** 本 PR の P0 は **5 → 2 → 1** と減っているが、
**5 周目の P0 は前周で私が足した検証スクリプトの欠陥**(起因)だった。
**起因が前周の修正である限り、件数が減っていても往復は止まっていない。**
**7.3-6 のエスカレーション条件が起因比で書かれているのはこのため。**

---

## 計画レビュー 6 周目の採否(2026-09-12)

**判定: 否決(P0 0 / P1 3 / P2 0)。P0 がゼロになった。**
レビューは **3-d(`input_assets` の重複行検査)を「既存検査器・既存 seal・実測済み反例の
同一境界内の補修であり `H-68` には当たらない」と明示的に通した**。
**allowlist のキーが関数の改名・分割で黙って古くならないこと**も確認された。

### ただし P1 3 件はすべて起因 (A) で、すべて同じ箇所

| # | 要旨 | 採否 |
| --- | --- | --- |
| 1 | 文字列規則が「2 桁以上」限定で、**1 桁の文字列化**(`str(len(x)) == "6"`)が抜ける | **採用**(別タスクへ) |
| 2 | 「同一モジュール内で束縛」が**字句スコープと到達する代入を定義していない**。`attempts` は複数関数で `0` に束縛される一方、`:671` では `Counter()` に束縛されている | **採用**(別タスクへ) |
| 3 | 出現回数は**多重度を固定するだけで出現を一意に識別しない**。許可済みを 1 個削って同値を 1 個足せば総数不変で通る(**置換経路**) | **採用**(別タスクへ) |

### エスカレーション(**7.3-6 の型**)— **起因比で判定した**

**5 周目・6 周目とも、前周修正起因が過半**(6 周目は 3/3 = 100%)。
**件数は 5 → 2 → 1 → 0 と減っていたが、収束していなかった。**

**根本原因**: **構文的な走査で「ハードコードが無い」を閉じることはできない。**
**性質が意味的だからである。** 実際に 3 周連続で型の違う抜け道が出た:

| 周 | 抜け道 |
| --- | --- |
| 4 | コンテナリテラルの中の値 / 名前との比較 |
| 5 | 文字列への退避 / 変数への退避 |
| 6 | **1 桁の文字列化** / **出現の置換** |

**私は毎周「次はこれで閉じた」と書いていた。** **閉じていないことを示す側にしか
新しい情報が出ない構造**で、**規則を足すほど抜け道の形が細かくなるだけだった。**

### PO 裁定 `D-16`(2026-09-12)

**期待件数のハードコード撤去(旧ステップ 1)を別タスクへ送る。**
**起票**: 「authz テストの期待件数ハードコードを資産由来の導出へ置き換える」
(`3d993b75-e687-8157-8e53-d7a2268aded7`)。

**新タスクの方針**: **「ハードコードが無いこと」を走査で示すのをやめ、
「テストが資産から導出していること」を変異で確かめる** —
**資産を 1 要素増やした fixture でテストの期待値が追随するか**を見る。
**追随しないテストが撤去対象である**(**構文を見ずに実体で判定できる**)。

**本改訂とは独立であることを実測で確認した** — **ステップ 2 が変えるのは
`S-5` の status 文字列と `S-7` の scope 値で、件数ではない。**

→ **本改訂は 2 ステップになった**(`core-areas.json` 登録 / 封印資産の確定と reseal)。
**どちらもレビューが既に「妥当」と認めた部分である。**

---

## 計画レビュー 7 周目の採否(2026-09-12)

**判定: 否決(P0 1 / P1 1 / P2 1)。**
**P0 が「非起因」になった** — **前周の修正が原因ではない新しい発見**で、**起因の連鎖は切れた**。
レビューは **12 パス不変 / 3 パス個別差分 / canonical 変更集合 / `input_assets` の行列比較 /
2 コミットへの分割**を「妥当」と認め、**期待件数について `29` が不変の `auth-catalog` 由来であること・
owner キー削除に影響する leaf 数が動的導出であること**も確認した(**ステップ 2 が件数に触れない**という
私の実測の裏づけ)。

| # | 重大度 | 要旨 | 採否 | 反映内容 |
| --- | --- | --- | --- | --- |
| 1 | **P0 非起因** | `boundary-proposal.json` の**多重度が閉じていない**。検査器が `boundaries` を `set`(`:4341`)、`pending_human_reviews` を `dict`(`:4371`)へ畳むため**重複行が潰れる**。**本改訂はこの資産を再封印するので不正形を固定できる** | **採用** | **自分でも実測して確認した**(先頭行を複製 → 両方 green)。**さらに母集団を列挙せず変異で測ったところ、レビューが挙げた 2 件に加えて 2 件見つかった** — `ddl-elements.json` の `transaction_boundaries` と `provisioning_claim.completion_catalog_expectations`。**2-d を「5 配列」へ作り直した** |
| 2 | P1 起因 | **`D-16` の送り先が全文で一意でない**。`S-*` 割当説明は `S-8` の残部をすべて PR #3 とし、`S-8` 本文に「既存 20 箇所すべて」が残り、第 1 弾当時の宣言が資産・封印へ触れることを禁じたまま | **採用** | **`S-8` が 3 つの送り先(本改訂 / PR #3 / 別タスク)へ分かれる**ことを 3 箇所で明記。**第 1 弾当時の禁止宣言へ「本改訂では失効している」**と注記 |
| 3 | P2 起因 | **2 ステップ化の番号伝播が未完了**(「3 ステップ」「1〜3」「0/3」/ design.md 冒頭) | **採用** | plan 3 箇所・design.md 3 箇所を是正 |

### 母集団を変異で測った(**裁定 `D-16` の教訓を適用した**)

**列挙は 3 周連続で取りこぼした**(走査規則の抜け道)。**同じ失敗を繰り返さないため、
今回は「再封印する 3 資産の `dict` を要素に持つ全配列について、先頭行を 1 件複製して
検査器を通す」実験で母集団を導いた。**

| 結果 | 配列 |
| --- | --- |
| **通る(要是正・5 件)** | `input_assets` / `boundaries` / `pending_human_reviews` / `transaction_boundaries` / `provisioning_claim.completion_catalog_expectations` |
| red(対象外) | `provenance` / `roles` / `schemas` / `tables` / `predicates` / `policies` / `functions`(+ `owner_dependency_acl`)/ `acl_expectations` / `ordered_steps` / `column_acl_expectations` / `table_privilege_probe_matrix` |

**レビューが挙げたのは 2 件だったが、実測すると 4 件だった。**
**「指摘された分だけ直す」と、また取りこぼしていた。**

---

## 計画レビュー 8 周目の採否(2026-09-12)

**判定: 否決(P0 2 / P1 3 / P2 2)。前周の 1/1/1 から増え、7 件すべてが起因だった。**

### 7 周目の「変異で測ったので閉じた」は誤りだった(**撤回**)

**走査の入口を「`dict` を要素に持つ配列」と私が選んだ**ため、**文字列の配列が漏れていた**。

| 漏れていた配列 | 件数 |
| --- | --- |
| `pending_human_reviews[0].affected_ids_if_changed` | 1 |
| `tables[0..5].row_shape_ids` | 6 |
| `transaction_boundaries[0..2].step_ids` | 3 |

**5 配列ではなく 10 配列だった。** さらに **JSON オブジェクトの重複キー**は、
**解析後のオブジェクトを走っても観測できない**(`_read_json` が `json.loads` の
last-wins で潰す — `:491`)。**多重度の軸がもう 1 本あった。**

**同じ型の失敗が本 PR で 4 度目である**:

| 周 | 「閉じた」根拠 | 次周の抜け道 |
| --- | --- | --- |
| 5 | `input_assets` を塞いだ | `boundaries` / `pending_human_reviews` |
| 7 | **変異で測った**(5 配列) | **走査の入口**が `dict` 限定だった(10 配列) |
| 8 | — | **重複キー**は解析後に観測できない |

**変異で測っても、走査の入口を選ぶ時点で同じ失敗をしていた。**

### 実測 — **現資産に重複は 1 件も無い**(2026-09-12)

**凍結 15 資産の全配列ノード 2341 本**(うち非空 2290 本・空 51 本)を走査して、**重複要素 0 件・重複 JSON キー 0 件**。
(**「2290 配列」は非空だけの数だった** — 9 周目 `P2-2` で訂正)
→ **本改訂が封印する形は正しい。封印が壊れる危険はない。**
→ **2-d は「将来の混入を防ぐ」硬化であって、本改訂の封印の前提条件ではない。**

### 反映(**人間の指示: まず最終修正をして結果を見る**)

| # | 重大度 | 要旨 | 採否 | 反映内容 |
| --- | --- | --- | --- | --- |
| 1 | P0 起因 | 文字列配列 5 本が走査から漏れ、計 10 配列 | **採用** | **2-d を「列挙した 5 配列の部分的な硬化」と明示**し、**「多重度を閉じる主張ではない」**と書いた。**残る母集団は別タスクへ**([起票済み](https://app.notion.com/p/3d993b75e68781b59bd3c55024f9ffea)) |
| 2 | P0 起因 | JSON の重複キーが未検査(解析後には観測できない) | **採用** | 同上。**多重度の軸を 2 本(配列要素 / オブジェクトキー)として新タスクの DoD へ書いた** |
| 3 | P1 起因 | 負例が目的の検査で落ちたことを保証しない(**digest 不一致だけで必ず red**) | **採用** | **合格条件を「多重度検査を外すと負例が red でなくなる」形へ**。**検査を書かないと DoD が満たせない**ようにした |
| 4 | P1 起因 | 5 配列への拡張が影響節へ伝播していない | **採用** | 中央 2 ファイルの行を是正 |
| 5 | P1 起因 | `D-16` の送り先が全文で一意でない(`S-8` の「残部」が 2 箇所) | **採用** | 2 箇所とも**具体の列挙**へ。DoD へ**「`S-8` の 3 つの送り先を書き分けた」**を追加 |
| 6 | P2 起因 | 2 ステップ化の番号伝播(6 箇所) | **採用** | 全 6 箇所を是正 |
| 7 | P2 起因 | 「2-e oracle の再封印」の二重記載 | **採用** | 解消 |

### 本改訂の封印を守るのは検査器ではなく実測にした

**DoD へ「再封印の直前に、変更後の 3 ファイルへ重複が 0 件であることを実測する」**を置いた。
**全配列の要素と全 JSON キー対を走る汎用走査**で、**列挙を含まないので取りこぼさない**。
**検査器の硬化(将来の混入防止)とは目的が別である。**

---

## 計画レビュー 9 周目(2026-09-12)

### 1 回目の応答は周として数えなかった(7.3-5)

**応答が途中で崩れ、判定行が無かった**。**7.3-5 の規約どおり同一周を再依頼した。**
**計画書は触らずに再依頼した**(反映を挟むと同一周でなくなるため)。

### 再依頼の結果: 否決(P0 0 / P1 1 / P2 4)

**P0 がゼロになり、前周の 2/3/2 から減った。**
**残る P1 は設計の穴ではなく、2 つの層を混ぜて書いていたこと**だった。

| # | 重大度 | 要旨 | 採否 | 反映内容 |
| --- | --- | --- | --- | --- |
| 1 | P1 起因 | **「実測」と「恒久検査」が分離できていない**。汎用走査を `tests/` へ置くと**毎回走る**ので、実態は**3 資産全体の恒久的な硬化**であり、「5 配列だけの部分的硬化」と矛盾する | **採用** | **2 層に書き分けた** — **層 ①(検査器)= 5 配列だけ** / **層 ②(資産の恒久検査)= 3 資産を閉じる**。**別タスクへ送るのも 2 件**(① の残り 10 配列 + 重複キー / ② の残る 12 資産)。**指摘は「実態のほうが主張より広い」型で、今までと逆向きだった** |
| 2 | P2 起因 | **実測件数が再現できない**。凍結 15 資産の配列ノードは **2341 本**(空 51・非空 2290)で、「2290 配列」は**非空だけの数**だった | **採用** | 計画書と本 worklog を訂正。**重複 0 件という結論はレビュー側でも再現された** |
| 3 | P2 起因 | 番号伝播が 3 箇所残る | **採用** | 是正 |
| 4 | P2 起因 | 「2-e oracle の再封印」が**依然として二重記載**(前周で解消したと記録したのに直っていない) | **採用** | 解消。**原因は反映スクリプトが見出しを再挿入したこと** — **前周の「解消した」という記録が誤りだった** |
| 5 | P2 起因 | `S-8` の送り先表が同期していない(`test_ci_wiring.py` の欠落) | **採用** | PR #3 の送り先表へ追記 |

---

## 計画レビュー 10 周目の採否(2026-09-12)

**判定: 否決(P0 0 / P1 1 / P2 2)。** 前周の 0/1/4 から減った。
レビューは**実測値を独立に再現**し(15 資産・全配列ノード 2341 本 = 非空 2290 + 空 51・
重複要素と重複キーとも 0 件)、**層 ② を 3 資産に限る判断**(残る 12 パスは差分禁止のため)・
**`S-8` の 3 送り先**・**`2-e` の単一記載**も確認した。

| # | 重大度 | 要旨 | 採否 | 反映内容 |
| --- | --- | --- | --- | --- |
| 1 | P1 起因 | **層 ② の恒久検査に負例が無い**。正常な 3 資産で重複 0 件を確かめるだけなので、**走査が再帰しない / `json.loads` の last-wins で重複キーを潰す / 検査が無効化されている**、いずれの壊し方でも green になり得る。**層 ① の負例は別の validator が対象**で、層 ② の実効性を保証しない | **採用** | **走査自体を壊す 3 種の負例**を合格条件と DoD へ置いた(深さ 2 以上の重複で red / 重複キーで red / 重複 1 件で必ず red・skip や空走査で通らない)。**負例の資産は一時ディレクトリの写しで作り、本物の凍結資産を変異させない** |
| 2 | P2 起因 | **影響節への層 ② の伝播漏れ**。中央 2 ファイルの変更を「literal 追随と 5 配列の多重度検査だけ」と限定していた | **採用** | **3 件**(literal 追随 / 層 ① の 5 配列 / 層 ② の恒久検査と負例 3 種)へ是正 |
| 3 | P2 非起因 | `guard_paths` 登録を「ステップ 2」とする記述が残存 | **採用** | ステップ 1 へ是正 |

**「検査に負例が無い」という指摘は、この PR で繰り返し出ている型**である
(5 周目の `input_assets`・8 周目の digest 不一致・今回の走査自体)。
**検査を足すたびに「その検査が本当に効いているか」を別に示す必要がある**、という規律が
毎回 1 段ぶん遅れて適用されている。

---

## 計画レビュー 11 周目の採否(2026-09-12)

**判定: 否決(P0 0 / P1 3 / P2 2)。P0 は 3 周連続でゼロ。**
**件数は増えたが、P1 3 件のうち 2 件が「非起因」**である。
**依頼文で「残る合格条件にも同じ型が無いか先に見てほしい」と頼んだ結果、
既存の穴が 2 つ前倒しで出た** — **狙いどおりの増え方**である。

| # | 重大度 | 要旨 | 採否 | 反映内容 |
| --- | --- | --- | --- | --- |
| 1 | P1 起因 | 層 ② の負例 3 種では足りない。**対象資産の集合を狭める / 深さ制限つきの走査 / 分岐単位の例外握り潰し**が残る(`ddl-elements.json` は**深さ 5 の配列**を持つ) | **採用** | **負例を 6 種へ**。**3 資産を同じ入口へ通し、対象パスの exact-set を DoD で固定**。**走査が実際に到達した配列パスの集合を assert** する条件を追加 |
| 2 | **P1 非起因** | **`S-5` の負例がフィールドごとに閉じていない**。「資産だけ / 検査器だけ」の 2 例しかなく、**一部フィールドの検査を削っても別フィールドの負例だけで green**。**`S-7` は 4 キーそれぞれの負例があるのに `S-5` には無い** | **採用** | **7 フィールドそれぞれの負例表**を置いた(`S-7` と同じ形へ揃えた) |
| 3 | **P1 非起因** | **`S-8` の `core-areas.json` 検査方式が正反対**。要件表の `S-8` は「**人手で列挙せず実在から機械的に導く**」と要求するが、裁定 `D-13` と現ステップは「**10 本を列挙し、導出型は作らない**」。**実装者は両方を満たせない** | **採用** | **裁定 `D-13`(2026-09-12)が上書きしたことを `S-8` 本文へ明記**。**「機械的に導く」は撤回ではなく延期**(別タスク)と書き分けた |
| 4 | P2 非起因 | `S-8` の PR #3 送りで**版・digest が 3 箇所で脱落** | **採用** | 5 箇所すべてを「パスと版・digest」へ揃えた |
| 5 | P2 起因 | 層 ② の負例が**検証節へ伝播していない** | **採用** | 検証節へ 2-e(負例)を追加し、既存の多重度検査を 2-f へ繰り下げ |

**`S-8` の矛盾(#3)は、私が裁定 `D-13` を「ステップの合格条件」にだけ反映し、
「要件表の本文」へ反映していなかったために生じた。**
**裁定を入れたとき、同じ要件を述べている全箇所を当たっていなかった。**

---

## 計画レビュー 12 周目の採否 + 裁定 `D-17`(2026-09-12)

**12 周目の判定: 否決(P0 0 / P1 4 / P2 1)。P0 は 4 周連続ゼロだが P1 が 1 → 3 → 4 と増えた。**

### fable への相談と、言語化できた原因

**12 周で同じ欠陥を 8 回、場所を変えて再生産していた**ので、相談役セッションへ観点を求めた。
**私が自分で掴めていなかったのはここだった**:

> **変異実験には人が選ぶ自由度が 2 つある** — **① 変異を置く場所の母集団**
> (走査の入口・配列の選択・AST のノード種)と **② 変異の操作**。
> **「全変異が検出された」は ①② を固定した条件付きの主張**で、**① の外については何も言わない**。
> **標本誤差ではなくカバレッジ誤差**である。
> **測定は検査器の性能を測るが、枠の妥当性は測らない** —
> **枠を選んだのが自分なら、測定結果は自分の選択を反射しているだけ。**

**7 周目に「変異で測ったので閉じた」と書けたのは、標本誤差をゼロにしたからで、
カバレッジ誤差(`dict` 限定の走査入口)には触れていなかった。**
**測ったという事実が、閉じたという確信をくれてしまう構造**がここにあった。

**12 周で出た抜け道 8 件は、すべて枠の縁に出ている** —
コンテナリテラルの中の値 / 1 桁の文字列化 / 別の配列 / `dict` 限定の走査入口 /
パース後に消える重複キー。

### PO 裁定 `D-17`(2026-09-12): 検査基盤を先行ステップへ切り出す

**根本は「属性を箇所ごとに書いている」こと**だった。
**だからレビューが合格条件を 1 つずつ順に当たり、周回が箇所数に比例していた**
(5 周目 `input_assets` → 10 周目 層② → 11 周目 `S-5` → 12 周目 `2-a`)。

**規則 1 つ + 機構 1 つへ移した**:

- **規律 4(型 A)**: **母集団は列挙せず「導出元 `S` + 導出器 `f` + `f` の全数性を `S` 自身で示す」**。
  **`S` は自分が作っていないもの**(言語の文法・スキーマ・資産の構造・正本の表)。
  **観測層で潰れる欠陥は観測層を下げる**(重複キーは `object_pairs_hook` か生テキスト)。
- **規律 5(型 B)**: **全 `[機械]` 条件を 3 点組で登録**((a) 正例 green (b) 負例 red
  **(c) 当該検査だけ無効化すると負例が通る**)。**(c) は条件ごとに文章で書かず、
  ステップ 1 の検査基盤が全ペアへ機械適用する**。**登録漏れは「条件の数 = 登録ペアの数」で red。**

→ **本改訂は 3 ステップになった**(検査基盤 / `core-areas.json` 登録 / 封印資産の確定と reseal)。
**`S-5` と `core-areas.json` の母集団も、列挙から導出元の形へ書き換えた。**

### 12 周目の個別指摘

| # | 重大度 | 要旨 | 採否 |
| --- | --- | --- | --- |
| 2 | P1 | `S-5` の `alternative_value` が `null → 任意値` の負例を持たない | **採用**(負例表へ追加) |
| 3 | P1 非起因 | `2-a` の負例が旧 owner 維持・owner 再追加・deferred 未更新を見逃す | **採用**(4 箇所の母集団を導出で定義) |
| 4 | P1 非起因 | `S-8` の「延期」の受取先が空手形 | **採用**(受取先を明記) |
| 5 | P2 | 6 種化の伝播が 2 箇所残る | **採用** |
