---
date: 2026-08-19
topic: Phase 4-2 backend 骨格(uv / ruff / ty / pytest)+ CI の backend ジョブ
branch: feature/backend-skeleton
---

# 作業ログ: 2026-08-19 Phase 4-2 backend 骨格(uv / ruff / ty / pytest)+ CI の backend ジョブ

## やったこと

- /task-start: 引数 `TSK-223` を既存 Notion タスクと同定し、新規起票はしていない → `feature/backend-skeleton` ブランチ + worktree 作成(origin/develop = c8bc258 起点)→ 計画書雛形・本 worklog 作成 → Notion を 進行中 へ

- /plan: 4 論点を人間裁定 → 計画書を記入 → `codex_run.py review normal` を **指摘反映 2 周 + 収束確認 1 周**(1 周目 P1×4・P2×3 / 2 周目 P1×2・P2×2 / 3 周目 指摘なし。**全件採用・不採用 0 件**)→ 人間承認(2026-08-19・山田正輝)

## 決定

- slug は `backend-skeleton`(Phase 4-1 の `frontend-skeleton` と対を成す命名。スロット番号は入れない — 4-1 の前例に揃える)
- **(人間裁定 2026-08-19)最小 FastAPI アプリ(`GET /health` + テスト)を 4-2 に含める** — 要件書 NFR-021 の「backend が起動して疎通確認できる」を**どのスロットが作るかの決定が正本に無く**、4-1・4-3 が閉じている以上 4-2 か 4-6 の二択だったため。**4-2 は受入判定も証跡化も行わない**(それは 4-6)
- **(人間裁定 2026-08-19)`ty check` が緑にならない場合は除外設定で回避せず作業を止めて裁定を仰ぐ** — mypy 切替は ADR 起票が要る(設計書 5.1・論点E)ため、4-2 の中で方式を変えない
- **(人間裁定 2026-08-19)非同期テストは AnyIO 方式**(FastAPI 公式準拠)。これに伴い**設計書 5.1 の列挙を pytest-asyncio → anyio へ実装追随で更新**する(版は上げない — 7.6-3 前段)
- **(人間裁定 2026-08-19)追加スコープなし** — `.claude/rules/backend.md` 新設 / 台帳 H-57 の CI ジョブ表追記 / `.env.example` ガード不整合の是正 はいずれも本タスクに含めない

## 計画レビューで動いた要所

- **1 周目 P1: 既存 CI ジョブ数を 5 と書いていた(正しくは 6)** — `frontend-changes` を落としていた。同時に「削除行が無いこと」では `needs` / `if` / filter の変更を検出できないと指摘され、合格条件を強化
- **1 周目 P1: `.env.example` ガード不整合の台帳起票を計画に入れていたが、昇格条件を満たさない** — 台帳は本件を既に**候補 (1)** として記録済みで、昇格条件は「読めないことで作業が止まる事例」。本タスクは同ファイルを読まないため条件外 → **3 節から台帳を外した**(research メモの「本調査で発見」という記述も訂正)
- **2 周目 P1: CI の合格条件が自己矛盾していた** — 「既存ジョブに影響がない」と「`ci.yml` 変更で frontend が起動する」が両立しない書き方だった。**定義の不変(base SHA との構造比較・許容差分は 2 ジョブ追加のみ)と実行挙動(frontend は起動して成功する)を別条件に分離**
- **2 周目 P1: 計画外変更の歯止めが弱かった** — 「差分は worklog へ記録」だけでは AGENTS.md 絶対規則 5 に反し得る。**再承認が要る変更(直接依存・ソースディレクトリ・CI 設定・正本の追加)と、worklog 記録で足りるもの(`uv.lock` の推移的依存差分)を分離**
- **2 周目 P2: 「未着手の Phase 4 スロットは本タスクのみ」は誤り** — 4-4〜4-6 も未着手(Notion に未起票なだけ)。「現時点で着手可能な次のスロットは 4-2 のみ」へ訂正

## 申し送り(本タスクでは扱わない)

- **台帳の候補 (1)「`.env.example` が Read deny の glob に巻き込まれる」の前提が実測と食い違う** — 同項は「**Bash 経由で実務は回る**」ため実害が小さいとするが、**2026-08-19 の実測では Bash の `cat .env.example` も拒否された**。`secret_guard.py` は exact `.env.example` を明示的に許可しており、**フックの設計意図と permissions の実効が食い違っている**。昇格判断は PO・別タスクへ

## 着手時点の前提

- **段階導入計画(設計書 13 章)の残スロット**: 4-1(TSK-219)・4-3(TSK-222)は完了済み。**未着手の Phase 4 スロットは本タスク 4-2 のみ**(4-4 以降は未起票)
- **後続の依存**: TSK-235(ドメイン計算の DSL・生成器・検査基盤 — ADR-003 D-1/D-11)は `backend/` の存在が前提(マニフェストと (β) 側の置き場)

- /investigate: 調査サブエージェント 3 本(spec-checker / legacy-analyst / decision-tracer)を並列実行 + Web 調査を /research(Codex `research`)へ委任 → 4 本を原典で裁定して [research.md](../features/backend-skeleton/research.md) へ統合

## 調査で判明した重要事項

- **4-2 は設計を発明する余地がほぼ無い**: `backend/` の形(設計書 `:159-161`)も CI ジョブの中身(同 `:606`)も既に正本が決めており、作業は「正本どおりに置く」ことに閉じる。マージ条件も通常 PR 要件のみ(確定ゲート不要・NFR-021 判定なし — 同 `:797`・`:804`)
- **骨格にドメイン計算コードを 1 行でも置くと 4-2 が TSK-235 に化ける**(要件書 `:872`、ADR-003 `:374`)。旧システム側の「先に用意しておくべき 12 項目」(センチネル正規化・所有権必須引数・冪等キー等)は**置き場と申し送りとしてのみ採用**する裁定を置いた
- **旧 FastAPI 版 API の構成は `docs/legacy/` に記述がなく不明** — 旧側の構成を骨格へ写す判断はできない
- **ty は beta・`0.0.x`・破壊的変更あり得ると公式明記**。**最新は 0.0.73(2026-08-19)で、Codex 報告の 0.0.65(07-29)は約 3 週間古かった**(Claude 側で Releases を確認して訂正)。リリース間隔が数日単位のため `uv.lock` の版固定が唯一の担保
- **`.python-version` は project 境界を越えて探索されない** → ルートの `3.12.3` は `backend/` から起動した uv には効かない(公式仕様)。`backend/.python-version` か `UV_PYTHON` の明示が要る
- **`uv sync --locked` は workspace 宣言が無ければ backend を対象にしない** → CI の `harness` ジョブへの影響は無い見込み(実測で確認する)
- **`UV_CACHE_DIR` でキャッシュを退避できる** — 4-1 で pnpm が踏んだサンドボックスの EROFS と同型の対処が uv でも取れる
- **設計書 `:192` の pytest-asyncio と FastAPI 公式の AnyIO 方式が食い違う**可能性を検出(計画で判断する)

## 未決・次の一歩

- **/plan で人間裁定を要する論点 2 件**: ① **最小 FastAPI アプリ(health + テスト)を 4-2 に含めるか**(要件書 `:921` の疎通確認をどのスロットが作るかの決定が存在しない — 残るのは 4-2 か 4-6 のみ)② **`ty check` が緑にならない場合の退避**(mypy 切替は ADR 起票が必要)
- **台帳への起票候補(調査中に発見)**: **`.env.example` が読めない** — `secret_guard.py:35` は exact `.env.example` を明示的に許可しているのに、`.claude/settings.json:42` の `Read(./.env.*)` が拒否する(Bash の `cat` 経由でも拒否・実測)。AGENTS.md 絶対規則 2 は同ファイルを「設定の正本」と定めており、**正本が読めない**状態
- 次は /plan(H-68 の (A) 方式が分岐する /(B) 実装時に決めれば足りる の分類を持ち込み、(B) を計画書で閉じようとしない)

