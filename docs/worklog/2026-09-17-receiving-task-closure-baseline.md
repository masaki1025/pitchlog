---
date: 2026-09-17
topic: 差分閉包の基準が自分自身になる欠陥の是正
branch: fix/receiving-task-closure-baseline
---

# 作業ログ: 2026-09-17 差分閉包の基準が自分自身になる欠陥の是正

## やったこと

## 決定

## 未決・次の一歩

## やったこと

- **develop の red を実測で特定**(`4b46c03` で `check_authz_catalog.py` が exit 1)。**原因は私の PR #66**
- **他セッション 2 つ(`U-T1` / `420`)へ警告**。`420` が独立に再現し、**私の見落としを 2 件指摘**
- `/task-start` → `/plan`(**敵対レビュー 7 周で収束** → 承認)

## 決定

**閉包の入口にゲートを 1 つ置き、置換が済んだ後は素通りさせる。**

```
① claims が 存在・配列・空でない・要素がオブジェクト(両側)
② 全行が receiving_task_id を持ち、非空文字列(両側)
     ①② が確かめられない → red
③ 基準版に対象行があるか
     ある → 4 段をそのまま
     無い → (a) 基準版の全行が最終文法に一致 かつ (b) HEAD にも 0 件
             どちらかが崩れていれば red
```

**固定 SHA は置かない**(基準の固定は `TSK-421` の射程)。**`contracts/` は 1 バイトも変えない。**

## 症状は 2 つあった(**起票時は 1 つだと思っていた**)

| | 症状 | 実測 |
| --- | --- | --- |
| **A** | develop とその全子ブランチで**段 2 が red** | `基準版に受取先置換の対象行がない` |
| **B** | **`contracts/authz/` へのあらゆる変更を段 1 が止める** | ファイルを 1 本足して `受取先置換で変更できない contracts パス: ['contracts/authz/__probe.json']` |

**B は `TSK-421` が足す `frozen-baselines.json` も止める。**
**PR 限定の仕掛けを永続的に武装させたまま**マージしていた。

## 敵対レビュー 7 周の推移と、私の失敗

| 周 | 判定 | 性質 |
| --- | --- | --- |
| 1 | 否決 `P0 0 / P1 3 / P2 2` | コア判定の誤り・ゲートの形状検査漏れ・テストの弱さ |
| 2 | 否決 `P0 2` | **`receiving_task_id` の欠落・非文字列で 4 段を迂回できる** / `7.7-3` を現行条文として引いた |
| 3 | 否決 `P0 1 / P1 1` | **`7.7-1` の追随漏れ** / 注入側の指定が閉じていない |
| 4 | 否決 `P0 1` | **`claims: []` / `""` / 文法外文字列で迂回できる**(**同型の 3 度目**) |
| 5 | 否決 `P0 0 / P1 1` | **スコープ表の追随漏れ** |
| 6 | 否決 `P0 0 / P1 1` | **`:120` の追随漏れ** |
| 7 | **可決** `P0 0 / P1 0 / P2 0` | — |

### 出続けた 2 つ

**1. ゲートの入力領域を列挙しなかった。** 2・4 周目の `P0` は**同じ形**(基準版を壊して HEAD を正常にすれば 4 段を素通り)で、**私は指摘されるたびに 1 つずつ塞いだ**。4 周目でようやく**迂回の手口を表にして、どこで落とすかを対応づけた**。

**2. 同じ条件を複数箇所に書いた。** 3・5・6 周目はいずれも追随漏れ。**5 周連続。**
**「条件の正は 4 節」という規律を本書に明記して畳んだ** — `U-01` で `research.md` から判断を外したのと同じ手当て。

### 根拠の射程を強く書く癖(**タスクをまたいで 3 回目**)

**`7.7-3` / `7.7-1` を現行条文として引いたが、develop のハーネス設計書は 7.6 までで 7.7 節は存在しない。**
**読んでいたのは `feature/frozen-baseline-clause` の草案**で、**本 fix はそれより先にマージする**と自分で書いていた。
`U-01` でも `NFR-014` / `NFR-005` / `FR-041` を強く引いて撤回している。

## `420` セッションからもらったもの

- **独立再現**(一時 worktree を `origin/develop` に立てて実測)
- **「この修正は緩和であって除去ではない」** — 基準がプレースホルダの存在しないコミットへずれた場合、
  **基準版も HEAD も 0 件で通る**。**除去には基準の固定が要り、それは `TSK-421` の射程**
- **`7.7-3` は本件に掛からない** — 「確かめられない」ではなく「**確かめた結果、前提が崩れていた**」
- **マージ順の確定** — `TSK-420` の PR は `pull_request` でマージ結果に CI を回すので、
  **本 fix が入るまで構造的に green にならない**

## 未決・次の一歩

`/implement` でステップ 1 を委任する。申し送りは `plan.md` 7 節が正。

---

## 結果サマリ(`/pr` クローズ処理・2026-09-18)

### 実装したもの — 1 ステップ・1 コミット(`8545fe9`)

`scripts/check_authz_catalog.py` の `_validate_receiving_task_changed_paths`(差分閉包 段 1)の手前に**入口ゲート**を置き、
**閉包 4 段を「置換が進行中のときだけ」効かせる**ようにした。**条件の正は `plan.md` 4 節**:

| | 確かめること | 満たされないとき |
| --- | --- | --- |
| **C1** | 基準版・HEAD の `claims` が**存在し・配列で・空でなく・要素がオブジェクト** | **red** |
| **C2** | 両側の**全行**が `receiving_task_id` を持ち**非空文字列** | **red** |
| **C3** | 基準版に `TSK-270-GROUP-2` の行が**ある** | → **4 段をそのまま走らせる** |
| **C4** | (C3 が 0 件のとき)基準版の**全行が最終文法に一致**(`_validate_receiving_task_id_syntax` を再利用) | **red** |
| **C5** | (C3 が 0 件のとき)**HEAD にも対象行が 0 件** | **red** |

**「0 件なら素通り」ではない** — **0 件は「置換が完了した状態」であることを C4・C5 で確かめてから**素通りする。

### 独立検証(worktree で実測)

| 見たこと | 結果 |
| --- | --- |
| **症状 A**(マージ後の develop で `check_authz_catalog.py` が exit 1) | **rc=0**(解消) |
| **症状 B**(`contracts/authz/` へのあらゆる変更が段 1 で拒否される) | **rc=0**(`__probe.json` を置いて再現していたものが通る) |
| **迂回 5 種**(`claims: []` / 全行 `""` / 全行 `"xyz"` / `receiving_task_id` 欠落 / 非文字列) | **すべて red** |
| **正常系**(置換進行中のスナップショット) | **4 段がそのまま走って green** |
| **メタ負例**(同じ「0 件」で、入口経由は成功・4 段を直接呼ぶと既存エラー) | **対照が成立** |
| `uv run pytest tests/` | **1390 passed**(10:07) |
| `uv run ruff check .` / `uv run ty check` | **green** |
| `git diff origin/develop...HEAD -- contracts/` | **0 バイト**(資産に触れていない) |

### 正本への反映

| 正本 | 反映 |
| --- | --- |
| `docs/development/harness-evaluation.md` | **既存候補「母集団を版管理の差分で導出すると、マージ後に基準が自分自身になって空洞化する」へ 2 例目** + **候補 1 件新設「計画書が根拠の射程を強く書き、条文がそこまで言っていない」** + 変更履歴表 1 行。**`H-*` は採番せず・版は上げない** |
| `docs/README.md` | 台帳行の要約と最終更新日を現行化(2026-09-17 → 2026-09-18) |

**`H-*` を採番しなかった理由**: 本候補自身の昇格条件(「同型が出たら」)は**満たされた**が、
**台帳の分類規則(`:145` の表)は「制御目的の典拠」を要求**しており、**設計書は依然として当該規律を定めていない**。
**`TSK-420` の `7.7` が入れば典拠になりうるので、その時点で再判定する。**
**候補の昇格条件が表の規則より緩く食い違っている点は PO 判断へ送る**(台帳に記録済み)。

### 申し送り

1. **本修正は緩和であって除去ではない** — 基準がプレースホルダの存在しないコミットへずれた場合、基準版も HEAD も 0 件で通る。**除去は `TSK-421` の射程**。
2. **マージ後に `420`(TSK-420)と `U-T1` へ通知する** — 両セッションが本 fix のマージを待っている。
3. **マージ順**: 本 fix → `TSK-420`(条文)→ `TSK-421`(実装)。
