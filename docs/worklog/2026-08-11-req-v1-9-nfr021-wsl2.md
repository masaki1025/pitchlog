---
date: 2026-08-11
topic: 要件書 v1.9 確定(NFR-021 の WSL2 追随)
branch: feature/req-v1-9-nfr021-wsl2
---

# 作業ログ: 2026-08-11 要件書 v1.9 確定(NFR-021 の WSL2 追随)

## やったこと

- **/task-start**: Notion タスク起票([TSK](https://app.notion.com/p/3b993b75e687814ea6bcde081bd4d611))→ ブランチ `feature/req-v1-9-nfr021-wsl2` + worktree(`../pitchlog-worktrees/feature-req-v1-9-nfr021-wsl2`・origin/develop 起点)→ 計画書雛形 → 本ログ作成
- **着手前の /research(2026-08-11)**: GitHub Actions で WSL2 が使えるかを一次情報で確認。結論は `research.md` に記録。**当初の前提「windows-latest で WSL2 は使えない」は誤り**で、実際は動くが入れ子仮想化が公式サポート外という結論だった
- **/plan の計画レビュー(review normal・terra max)**:
  - **1 周目 = 承認不可(P0×1 / P1×6 / P2×1)— 全件採用**。P0-1 設計書のゲート区分を「節更新」から **v1.4 + 確定ゲート**へ / P1-1 影響正本の漏れ 5 件(**AGENTS.md:5 の v1.7 記載**・要件書 8 章 DoD⑦・設計書 :65/:102/:708 の現在形・ADR-002 :9/:27・索引の設計書行)/ P1-2 「v1.7 を grep で 0 件」は過去事実を壊すため撤回し**更新対象を行単位で明示** / P1-3 v1.8 履歴は原文保存・裁定根拠を「7.6 の決定表 + in-review 状態」へ差し替え / P1-4 確定ゲートは反映周ごとに 1 コミット / P1-5 設計書の `windows-latest` 固定指定を残すのは矛盾 / P1-6 `check_docs_status.py` は **status 突合のみ**で版・日付・履歴は検査しない(記述の誤りを訂正)/ P2-1 NFR-021 の確定文を計画に固定
  - **2 周目 = 承認不可(P0 なし / 新規 P1×4 + 1 周目指摘の「不十分」2 件 = 実質 6 件 / P2×1)— 全件採用**。1 周目指摘は 8 件中 6 件解消、2 件(P1-1・P1-5)が「文言は直ったが付随文書が矛盾したまま」。設計書を in-review にする際は**索引の設計書行も in-review にしないと docs-lint が落ちる** / ADR-002 の変更履歴追記と索引行の最終更新日が未定義 / 波及追随を**設計書の確定ゲート前**に置く依存関係の是正 / **research.md と本 worklog の記述が改訂前の方針のまま**(本コミットで訂正)/ P2 「更新しない」分類に完了済み feature の調査記録と ADR-002 の歴史的文脈を追加
  - **3 周目 = 承認不可(P0 なし / P1×4 / P2×2)— 全件採用**。2 周目指摘は全 7 件解消。新規: 要件書を v1.9 起案した直後に**索引が v1.8 のまま残る**(`check_docs_status.py` は status しか見ないため CI で検出されない)→ ステップ 1 で索引を「in-review・v1.9」へ / **WSL2 方針が未裁定で現行指示に漏れ**(設計書 `:418`・`:517`・`setup-dev/SKILL.md:19`)→ 人間の裁定へ / 本 worklog の 2 周目件数の誤記 / **DoD に Notion 同期がない**。P2: `core_guard.py` の引用行・worklog の重複行
  - **4 周目 = 承認不可(P0 なし / P1×2 / P2×3)— 全件採用**。3 周目指摘は 4/6 解消、2 件が不十分。新規: **「WSL2 必須」と言いながら NFR-021・7.1・改善台帳が「Windows ネイティブ版 PostgreSQL」を残しており排他性が不十分**(→ 人間の裁定で DB も WSL2 内へ統一)/ `setup-dev/SKILL.md:19` の「削除」指示は**WSL2 確認自体を消し得る**(同行が WSL 判定・WSL1 是正・ネイティブ例外を併記しているため)→ 末尾文のみ置換へ。P2: `matched_paths()` の引用行(定義 `:210`・判定式 `:223-224`)/ 確定ゲートの停止境界とコミットトークン記法の明記 / `codex_run.py:185-190` の Windows 互換実装(`.cmd` を `cmd /c`)は方針とは別レイヤーとして残す旨の明記
  - **5 周目 = 承認不可(P0 なし / P1×1 / P2×1)— 全件採用**。4 周目指摘は全 5 件解消。NFR-021 の確定文が要件書内の他記述(7.1・NFR-019・8 章 DoD・10 章未決・付録)と矛盾しないことも確認された。新規: **改善台帳を「PR レビュー(事実の追随)」とした区分が不適切**(I-6 の変更対象は方針そのものだから確定ゲート)→ 人間の裁定で**「要件書への参照に置換」= 重複記述の解消**へ方針転換 / P2 DoD が「文字列が消えた」ことしか検査していない → WSL2 内の Docker 動作確認を追加。**スコープの歯止めも確認** — win-setup のランナー選定は「(B) 別タスク」で申し送りで十分、過去の決定記録・完了済み feature の旧表現は履歴として更新対象外でよい、と判定された
  - **6 周目 = 承認不可(P1×1 のみ)— 採用**。参照化という解決自体は**妥当と認められた**(I-6 全体は複製ではないが配置記述だけの委譲は「1テーマ1正本」の適用として妥当・可読性も許容範囲)。残った唯一の指摘は**相対リンクのパス誤り** — 改善台帳は `docs/` 直下なので `../requirements/...` ではなく `requirements/...` が正しい(`../` 付きはリポジトリ直下の存在しないディレクトリを指し CI の lychee が落ちる)。修正し、パス解決を実測で確認済み。あわせてレビューの限定「**『参照化すれば常に PR レビューで足りる』という論法は成立しない** — 今回足りるのは方針変更の本体を要件書 v1.9 の確定ゲートに載せたうえで台帳から非正本記述を除去するだけという限定条件による」を計画書 4 節に明記した

- **/finalize-doc(要件書)の確定ゲート — 敵対レビュー(`review adversarial`・sol xhigh)**:
  - **1 周目 = 否決(P0×3 / P1×5 / P2×2)— 全件採用**。**6 周の計画レビューが見つけられなかった矛盾を 3 件掘り出した**:
    - **P0-1**: approved になる要件書が `status: draft` の `onboarding.md` を「セットアップ手順の**正**」として参照していた。しかも同文書の現状はハーネス疎通確認までで pitchlog 本体の依存導入・DB 初期化・起動・確認を含まない。「**将来作成する文書の配置先として draft を参照するのは可能だが、draft の内容を現在の規範として扱うことはできない**」→ 規範内容は要件書側に置き、参照は**配置先**に弱め、DoD⑦ を「approved かつ現行化」要求へ
    - **P0-2**: Must 要件の合否が一意に決まらない(「クリーン」の定義・対象ディストリ・DB 経路・どこまでで合格か・実行者・証跡・再検証時期が未定義)→ 測定方法に**開始状態・合格条件・判定時期・判定者・証跡**を明記
    - **P0-3**: **approved な改善台帳 I-4(`:42`)が「FastAPI + React + TS」のまま**で、要件書 7.1・ADR-002 の Vue と**直接矛盾**していた。2026-08-07 の Vue 化(v1.8)の追随漏れが残存。計画は I-6 だけを対象にしていて見落としていた
    - **P1-1**: 配置方式の二択が設計・手順の実態(Docker 一経路)と乖離。`onboarding.md:32` の「WSL 内ネイティブ導入」は **Docker Engine の導入方法**で PostgreSQL ネイティブ経路は存在しない → **要件から配置方式を削除**(人間の裁定 2026-08-11)
    - **P1-2**: 「Windows ネイティブは対象外」の射程が曖昧 → 「**開発者ワークステーションの受入保証対象**」に限定し、CI・本番・移行実行環境・既存コードの移植性は本要件で制限しないと明記
    - **P1-3**: 変更履歴の「一括検証は実施されなかった」は**過剰**(v1.8 の変更自体はハーネス敵対レビューから生まれている)。また 2026-08-07 決定と 2026-08-11 裁定の**帰属を分離**すべき → 書き直し
    - **P1-4**: 9 章 R-1 の属人性対策が onboarding を含まない → 追加
    - **P1-5**: 追随先の追加漏れ 4 件(設計書 `:70` の「標準」止まり / onboarding 本文の「標準」「必須」混在 / **approved な `github-setup.md:14` が draft onboarding を正として参照** / **`spec-checker.md:10` が改善台帳を「I-1〜I-24」と記載(実際は I-27)**)
    - **P2**: 10 章に「NFR-021 の継続検証基盤」を未決事項として登録 / 版規則の明文化は別タスク。**版番号 v1.9 は妥当**と判定(v2.0 の根拠なし・v1.8 への混載は不適切)
  - **2 周目 = approved 不可(未収束。解消 7 / 不十分 2 / 未対応 1・新規 P0×2/P1×2/P2×2)— 全件採用**。指摘の核心は**進め方そのもの**だった: 「**確定ゲートは『レビュー対象の現状態を approved にできるか』を判定するもの**であり、既知の P0 を未来ステップへ送ったまま『全件反映』『approved』とすることはできない」。1 周目 P0-3(改善台帳 I-4 の React/Vue 矛盾)を「計画のステップ 4 に追加した」ことで解消扱いにしていたのは誤りで、**改善台帳は現に React のまま**だった。さらに要件書の変更履歴に「1 周目 10 件を全件反映」と書いたが I-4 未編集のため事実と不一致
    - **新規 P0-1**: Phase 4 時点の「NFR-019 の規定テスト」が一意でない(全部と読めば完了不能・存在するものだけと読めば漏れても合格)→ **Phase 4 完了時とリリース候補時で合格条件を分離**し、証跡の必須項目も定めた。あわせて **8 章 DoD ② の NFR-019 列挙に Vitest・Playwright・(d)同期故障系を補完**
    - **新規 P1-1**: 承認済み計画書に**削除した配置方式の二択が残っていた**(`:114`・`:130`・`:140` と `:110`・DoD が矛盾)→ 除去。反映漏れ
    - **新規 P2-1**: `windows-latest` は **Windows Server 2025 で Windows 11 ではない** → 10 章の未決事項に「必須ゲートは Windows 11 x64 上の WSL2、Windows Server ランナーは補助検査扱い」を追記
    - **P2-2(B)**: 改善台帳の「I-1〜I-24」表記が要件書 `:41`・設計書 `:444`・`decision-tracer:12` にも残る → 別タスクで全数走査
  - **3 周目(波及追随を済ませた最終ツリーに対して)= approved 不可(P0×2 / P1×2 / P2 新規なし)— 全件採用**。改善台帳 I-4/I-6 の実編集・`setup-dev` の WSL 判定保持・`spec-checker` の I-27 一致・相対リンクの解決は**すべて確認された**
    - **P0-1**: 受入環境と証跡がまだ一意でない(「新規 WSL2 ディストリビューション」に**版の選定規則がない**・onboarding も Ubuntu を「推奨」止まり。証跡も実行コマンドと終了コードだけでは後から検証できない)→ **受入プロファイル**(Ubuntu の最新 LTS 版のみ保証・ホスト側の事前導入は WSL2 有効化のみ)を明記し、証跡に主要ツール版・各合格項目の期待値/実測値・ログ成果物への参照を追加
    - **P0-2**: **波及文書が未完了のゲートを「2026-08-11 に通過・approved 化済み」と先取りしていた**(ADR-002 `:9`/`:27`・`README.md:14`)。要件書は今も in-review で、7.3 では人間承認まで終えて初めて approved → 「**v1.9 案へ反映済み・確定ゲート中**」へ是正し、**承認後に実際の承認日で更新する手順**をステップ 4 に明記。v1.9 起案行(2026-08-11)は保存し、確定行を承認日で別途追記する方式に
    - **P1-1**: 計画書 `:50`・`:108`・`:230` に削除済みの旧案が残存(2 周目で同種を指摘されたのに漏れていた)→ 是正。測定方法は多行化したため**確定文の全文転記をやめ、要件書の実文を正とする**方式へ変更
    - **P1-2**: **approved な設計書 `:150`(4 章の目標ツリー)が「Vue または React(論点A決着後)」のまま**。履歴説明ではなく目標ツリーで、v1.4 の更新範囲にも入っていなかった → ステップ 5 の対象・合格条件に追加
  - **4 周目 = approved 不可(P0×1 / P1×3 / P2 新規なし)— 全件採用**。うち 3 件は反映漏れ、1 件は順序
    - **P0-1**: 「**Ubuntu の最新 LTS 版**」は試験対象を事前に一意化できていない(「最新」の判定時点・判定元の公式情報・WSL 向け配布開始のずれ・試験中に切り替わった場合の固定単位・切替猶予と再検証規則が未定義。「**最新ではない**」と「**EOL**」は別概念)→ **「Ubuntu 26.04 LTS の番号付き x64 WSL イメージ」に固定**し、切替は「WSL 向け正式提供 + onboarding 更新 + 受入再実施」の 3 点を満たした時点で版を改訂して行う規則を明記(標準サポート終了前に必ず切替)
    - **P1-1**: 「実文を正とする」方式**自体は妥当と認められた**(「計画書が契約であることは確定文を全文複製することを意味しない」)。ただし**ステップ 1 の合格条件に旧表現「NFR-021 の 2 文が一字一致」が残っていた** → 一字一致は要件文のみ・測定方法は 4 箇条書きの項目単位確認へ統一
    - **P1-2**: ADR-002・README の実文書は解消済みだが、**計画書の 4 箇所に「通過済み(2026-08-11)」が残存**していた → ステップ 2 では「確定ゲート中」、ステップ 5 で実承認日を用いて「通過済み」の **2 段更新**に統一
    - **P1-3**: **設計書 `:150` をステップ 5 へ送ったまま要件書を approved にする順序は不適切**(2 周目 P0-2 で採用した原則と同じ)→ **設計書 v1.4 の起案をステップ 3 へ前倒し**し、要件書の承認に依存する部分(`:65`・`:102`・`:708` の通過済み表記)だけをステップ 6 に残す。**確定ゲートの順序変更はこれで 3 度目** — ハーネス評価の材料
  - **5 周目 = approved 可(収束)**。**P0・P1・P2 いずれも新規指摘なし**。4 周目指摘 4 件はすべて解消と判定された
    - **Ubuntu 26.04 LTS 固定の評価**: 「固定方法と切替規則は妥当。**残る曖昧さはない**」。Microsoft の WSL 配布定義では既定名 `Ubuntu` と番号付き `Ubuntu-26.04` は**別のディストリビューション識別子**で、前者だけが安定版 LTS を追随するため、除外規則により受入対象が一意になる。切替を「正式提供・onboarding 更新・再受入」の 3 条件に結び付け標準サポート終了前の切替を義務化した規定は「**未検証版への自動追随とサポート切れの双方を防ぐ**」と評価。Ubuntu 26.04 LTS の標準サポート終了は 2031 年 5 月で、具体日を固定せず「標準サポート終了前」としたのは日程変更への耐性がある
    - **approved 正本間の整合**: 設計書 v1.4 は主要点(WSL2 を唯一の受入保証対象・NFR-021 対応・Vue 3 + TypeScript・Phase 4・未決の Windows ランナー)で要件書 v1.9 と整合。設計書本体と索引がともに `in-review` で一致しているため **v1.4 は approved 正本ではなく、要件書承認後に別途確定ゲートを通す扱いが状態遷移規則と整合**すると確認された。改善台帳・ADR-002・AGENTS.md・README・setup-dev・onboarding・github-setup との間にも承認を妨げる矛盾なし
    - 全章・付録 A〜F の再走査でも矛盾なし。文書状態検査・差分の空白/競合マーカー検査ともに合格

- **/finalize-doc(ハーネス設計書 v1.4)の確定ゲート — 敵対レビュー(sol xhigh)**:
  - **1 周目 = 否決(P0×2 / P1×4 / P2×2)— 全件採用**。指摘の中心は「**要件書側で強化した内容が設計書に落ちていない**」
    - **P0-1**: **NFR-021 の受入契約が設計に落ちていない**。要件書は受入プロファイル・2 時点・時点ごとの合格項目・証跡・判定者を必須化したが、設計書は「onboarding を再現・ランナー未決」「クリーン環境で手順どおり成功」程度で、「**Ubuntu 26.04 LTS 以外でも Phase 4 を完了扱いにでき、証跡なしでも合格できる**」状態だった → **10.1 に「NFR-021 受入ゲート」を新設**(実施時点・実施者・判定者・環境・証跡の保存先 `docs/ops/nfr021-acceptance/`・保持期間・CI との関係)。13 章 Phase 4 の完了条件を具体項目へ、6.4・8.4 `/release` にリリース候補時の証跡確認を追加。**要件を複製せず参照する**形(7.1-1)に留めた
    - **P0-2**: **draft の `onboarding.md` を現行規範として扱っていた**(設計書 8.4・10.1・13 章・14 章論点C と `github-setup.md:14`)→ すべて「**手順の配置先。draft のため approved 化までは規範ではない**」へ是正し、Phase 4 の完了条件に **onboarding の完成と v1.0 approved 化**を明示
    - **P1-1**: `setup-dev/SKILL.md` の「**Windows ネイティブでの開発は認めない**」が要件書・設計書本文の「**保証対象外**」より強かった(要件書の確定ゲート 1 周目 P1-2 で射程が狭まった結果)→ 人間の裁定(2026-08-12)で**正本に合わせて弱める**:「公式ハーネスと受入証跡は WSL2 のみ。Windows ネイティブは未保証で受入判定に使用できない」
    - **P1-2**: 「**OS 非依存**」の主張と実装境界が不一致(根拠の WSL2 と CI はどちらも Linux・hooks 起動は `/usr/bin/python3` 固定なのに、`codex_run.py` は `.cmd` を `cmd /c` で起動)→ 射程を「**WSL2/CI Linux 共通**」に是正し、`.cmd` 経路を**未保証の互換フォールバック**と明記
    - **P1-3**: 「v1.9 から継承する制約」が実際には v1.9 と不一致 — **2.3 の NFR-019 が「pytest に一本化」のまま**(v1.9 の正は 3 ランナー + (a)〜(d))、`:39`「発見済みの矛盾1件」・7.1 行が解決済みなのに現在形 → すべて是正
    - **P1-4**: `decision-tracer` の典拠範囲が **D-1〜D-41・I-1〜I-24**(実際は **D-44・I-27**)→ レビューの推奨に従い**番号上限を撤去して「全 D-*／全 I-*」**へ(追記で腐るため)。`spec-checker` も同じ方式に揃えた
    - **P2-1**: 「NFR-021 が Docker を許容した」という帰属が v1.9 と不一致(v1.9 は配置方式を設計フェーズへ委ねた)→「**NFR-021 の WSL2 完結要件を満たす配置方式として 5.3 で採用**」へ
    - **P2-2**: v1.4 変更履歴の自己不整合(「2.2」と書いたが開発環境行は 2.1・行番号が変更履歴挿入で 1 行ずれた)→ **行番号ではなく安定した節名ベース**で記述し直した
  - **問題なしと確認された点**: Vue 3 + TypeScript の版表記(ADR-002 が明示的に「Vue.js(3系)+TypeScript」で 5.2・AGENTS.md と整合)/ **v1.4 + 確定ゲートの判定は妥当**(「CI 設計と Phase 4 受入契約を変更するため節更新ではなく版繰り上げ + 7.3 が妥当」)/ `windows-latest` を受入証跡から外す判断 / 7 章のゲート原則 / Windows 互換コードの保持自体
  - **2 周目 = approved 不可(P0×2 / P1×4 / P2×3)— 全件採用**。1 周目 8 件のうち **4 件が「不十分」**(骨格は入ったが実効が伴っていない)。解消は NFR-019 の 3 ランナー化・agent 典拠範囲・Docker 帰属・変更履歴の節名ベース化の 4 件
    - **P0-1**: **`/release` の実体に NFR-021 証跡確認がなく、設計上のブロックが発効していない**(設計書は「証跡確認を含む」と書いたが `release/SKILL.md:11` は従来の DoD 8 項目のみ)→ **人間の判断(2026-08-12)で B 案を採用**: 設計書を未来形に直し、**Phase 4 の成果物に「`/release` への証跡検証ステップ追加・証跡の機械検証器・そのテスト」を明記**し、**それまではプロダクトリリースを行わない**と定めた(リリースは Phase 4 のさらに先で、`docs/ops/` も証跡もまだ存在しないため今実装しても検証できない)
    - **P0-2**: 証跡がリリース候補そのものに対応することを判定できない(「該当ディレクトリに証跡があるか」しか規定していない)→ **`tested_commit_sha`・対象版・`result`・判定者・使用した approved onboarding 版を機械検証対象**とし、**コードまたは環境構築設定の変更で証跡は失効**(文書のみの変更では失効しない)という規則を明記
    - **P1-1**: approved onboarding を使って受入する順序が保証されず(「draft で実行し後で内容を変えて approved にしても合格できる」)、かつ 10.1 が「要件を複製しない」と宣言しながら 13 章で試験項目を再列挙していた → **実施順序①〜④を明記**(onboarding v1.0 approved 化 → その版で実行 → 証跡 → 判定)し、**13 章の試験項目列挙を削除して要件書 NFR-021 の参照へ戻した**
    - **P1-2**: 実施者と判定者の分離が **approved 要件書 10 章の「判定者が実施」と衝突**(かつ 1 名体制では別ロールを書いても独立性は成立しない)→ 「**実施責任者・判定者 = 8 章の判定者。開発担当・Claude は操作補助と記録の起草のみ**」へ是正し、分離を装わないと明記
    - **P1-3**: 「WSL2/CI Linux 共通」への置換が 3 箇所(2.3・4 章 hooks 行・14 章論点C)未完了 → 是正。論点C の移行コスト説明も「両方が Linux 実行系で既存実装を継続利用できた」へ
    - **P1-4**: **承認済み計画書 4 箇所と worklog の決定欄が旧裁定(「認めない」)のまま**、`github-setup.md` が「やらないこと」と「変更対象」に同居 → 計画書を再裁定へ更新し、`github-setup` を「やること」へ移動。worklog の旧決定は**削除せず superseded と明記**
    - **P2-1**: 5.1 バックエンド節の「NFR-019『pytest 一本化』」→「NFR-019 の**バックエンドランナー = pytest**(他へ分散させない)」へ
    - **P2-2**: 証跡の文書分類・命名・保持根拠が未整理 → **ディレクトリの `README.md` とテンプレートのみを正本**(索引掲載・確定ゲート対象)、**個々の証跡は変更不可の監査記録**(索引外)と分類。命名を `YYYY-MM-DD-phase4.md` / `YYYY-MM-DD-release-vX.Y.Z.md` に分離。**保持根拠を「ハーネスの監査方針」に改めた**(要件書 4.0-2 の論理削除は**アプリ利用者データ**の規定であり帰属先として不適切だった — 私の誤り)
    - **P2-3(B)**: 索引と要件書に残る D-41/I-24 の静的上限 → 既知の別タスク(再提起せず)
  - **3 周目 = approved 不可(P0×2 / P1×1)— 全件採用**。2 周目 8 件のうち 5 件解消・3 件不十分。6.4 の確定ベースライン例外を対象外とした判断は「**将来のリリースを迂回する経路になっていない**」と Git 履歴で確認された
    - **P0-1**: **リリース禁止が宣言だけで現行 `/release` を止められない**(設計書 `:48` 自身が「規約は破れないに寄せる」と謳っている)→ **人間の判断(2026-08-12)で B′ を採用**: `release/SKILL.md` の**手順 0 に fail-closed の暫定ゲート**を追加(検証器 `scripts/verify_nfr021_evidence.py` が存在しなければ即中断し、DoD チェック・人間判定・PR 作成・タグへ進まない)。検証器本体は Phase 4 の成果物のまま。あわせて **v1.4 変更履歴が「証跡確認を追加」と完了形**で本文の「未実装」と矛盾していた点も訂正
    - **P0-2**: 証跡の失効・対応規則がまだ機械判定可能な契約でない → **検証器の契約**(入力 = 候補 SHA・ゲート種別・要求版。合格条件 5 項目。**判定不能なら fail-closed**)と**失効対象パスの正本**(`.claude/nfr021-invalidating-paths.json` を Phase 4 で定義。**`onboarding.md` を失効対象に含める** — 受入で実行した手順そのものが変われば旧証跡は無効)を明記。**`onboarding_blob_sha`** を必須キーに追加(**版番号では内容を一意に識別できない** — 節更新は版繰り上げなしで可能)。実施順序の「v1.0」は Phase 4 初回に限定し、**リリース候補時は「その時点で現行の approved 版」**へ
    - **P1-1**: 同日に失敗 → 再実施すると**証跡のファイル名が衝突**し「上書き禁止」と両立しない → 命名を `YYYY-MM-DDTHHMMSSZ-<gate_kind>-<phase4|vX.Y.Z>-<short_sha>.md` へ。**append-only の CI 検査**を Phase 4 成果物に追加。「索引外なので docs-lint 対象外」は不正確だったため「**`check_docs_status.py` の status 検査の対象外。lychee は全 Markdown を走査する**」へ訂正
  - **4 周目 = approved 不可(P0×1 / P1×1 / P2 なし / (B) なし)— 全件採用**。指摘が 2 件まで絞られ収束が近い。1〜3 周目の反映は再指摘されず
    - **P0-1(旧 P0-2 の残り)**: `onboarding_blob_sha` を**必須キーに追加しただけで合格条件から一度も参照していなかった** — 文字どおり実装すると「候補 SHA に未収録のローカル証跡を対象にする」「`onboarding_blob_sha` に任意の SHA を書く」でも祖先関係と失効差分だけで合格してしまい、要件書 NFR-021 の「**approved な onboarding の完走**」を機械的に裏付けられない → 合格条件へ **④ `onboarding_blob_sha` == `tested_commit_sha` 時点の `docs/development/onboarding.md` の Git blob SHA、かつその blob の frontmatter が `status: approved`** を追加し、**検出対象を「候補 SHA の Git ツリーに収録された証跡のみ」**に限定。fail-closed の列挙に**必須キー欠落・Git オブジェクト解決不能・frontmatter 解析不能**を明記
    - **P1-1**: 入力を「候補 SHA・ゲート種別・**要求版**」と一律に書いたが `release_version` は `release` のときだけ必須 → `phase4` 呼び出しで要求版を省略するのか固定値を渡すのかが未定義だった → **`gate_kind` による判別共用体**として明記(`release` = 要求版必須・証跡と一致 / `phase4` = 要求版を渡さない・与えられたら入力エラー。証跡側でも `release_version` の記載を禁止)
  - **5 周目 = approved 不可(P0×2 / P1×2 / P2×1 / (B) なし)— 全件採用**。4 周目の 2 件は**解消と確認**され、あわせて「鶏と卵になっていないか」「過去コミット時点の frontmatter を見る要求が 7.4『現在状態の正は frontmatter』と衝突しないか」「`phase4` の `release_version` 禁止と命名の `<phase4|vX.Y.Z>` が矛盾しないか」「要件の複製(7.1-1 違反)がないか」の 4 点も**問題なしと明示的に検証**された。新規指摘は**受入済み SHA と実リリースの結び付き**に集中
    - **P0-1**: 暫定ゲートを「検証器ファイルが存在すれば実行」としたため、**Phase 4 作業中の未コミット・スタブ検証器でゲートが開く**。証跡だけを候補 SHA のツリーに限定しても**判定コード自身が候補 SHA の外**なら fail-closed にならない → **Phase 4 完了までは有無で分岐せず無条件中断**へ。置換は **Phase 4 の PR でのみ**行い、置換後は**候補 SHA 収録の検証器と実行する検証器の blob 一致**を要求
    - **P0-2**: 検証した SHA と実際にリリースされる SHA が固定されておらず、**検証後に `develop` が進めば失効対象パスの検査が実リリースから切り離される**(C0 で合格 → `frontend/` 変更で C1 → 再検証せず C1 をマージ → タグも対象 SHA 未指定)→ 10.1 に「**候補 SHA の固定**」行を新設(`candidate_sha` を完全 OID で固定 / PR 作成直後とマージ直前に再照合・動いていれば中断 / マージコミットの第 2 親とツリー一致を確認 / **タグは対象 SHA を明示**)。`release/SKILL.md` の手順 3 にも反映。※ 6.4 の 1 回限りの例外で既に同種の固定・再照合を用いていた
    - **P1-1**: 実施順序 ③ が「証跡を作成」としか書かず、**候補ツリーへコミットしてから候補 SHA を確定する工程が欠落**。`<short_sha>` が T(試験コミット)か C(候補 SHA)かも未定義で、C だと**ファイル名が C のハッシュ計算に入る自己参照**になる → ③ を「作成 → コミット → 証跡コミット後の HEAD を候補 SHA として固定」へ。`<short_sha>` = **`tested_commit_sha` の短縮形**と明記し、**証跡の本文・ファイル名に候補 SHA を書かない**とした
    - **P1-2**: `failed` 証跡も永久保持する一方で、**合格条件がどの証跡に適用されるか未定義**だった(「全件 passed」= 1 件の失敗記録で永久に合格不能 / 「1 件でも passed」= 後続の失敗を無視して古い合格で通る)→ 検証器入力に **`evidence_path`(証跡 1 ファイルの名指し)を必須追加**し、自動選択はしない方針を明記
    - **P2-1**: 13 章 Phase 4 行の `・****既存証跡` という Markdown 誤記 → 修正
  - **6 周目 = approved 不可(P0×2 / P1×1 / P2×1 / (B) なし)— 全件採用**。5 周目の実体反映は 5 件とも確認された(P0-1 は 8.4 要約に旧条件の取り残しあり = P2 へ / P0-2・P1-2 は「追加はされたが穴が残る」)。要件複製(7.1-1)違反なしも再確認
    - **P0-1**: `evidence_path` の名指しだけでは「**後発の `failed` を無視して古い `passed` を名指しする**」経路が閉じない(後発証跡も失効 allowlist なので判定 ⑥ をすり抜ける)→ 証跡に **`attempt_seq`**(ゲートキー = `phase4` / `release + release_version` 単位の単調増加。`failed` も番号を消費)を必須追加し、合格条件 ⑦ に「**同一ゲートキー内で最大 `attempt_seq` であること**」を追加。重複・欠番・比較不能は fail-closed。→ `failed` を永久保持したまま**再実施して最新 `passed` を得れば回復できる**
    - **P0-2**: **SHA 再照合が非原子的** — マージ直前の照合と人間のマージは別操作で、その間に `develop` が進めば GitHub は新しい PR head をマージでき、**相違はマージ後の第 2 親・tree 検査で初めて判明**する(main へのマージ自体がリリースなのでタグを止めても手遅れ)。6.4 の例外では固定していた base 側も通常リリースでは照合対象外だった → **`base_sha`(開始時の `origin/main`)も完全 OID で固定** / **GitHub API から PR の `.head.sha`・`.base.sha` を再取得**して照合(ローカル追跡参照に頼らない) / **`gh pr merge --merge --match-head-commit <candidate_sha>` で原子的に拘束**(導入済み gh 2.46.0 に実在することを確認済み) / マージ後は**第 1 親 = `base_sha`・第 2 親 = `candidate_sha`・ツリー一致**を確認。**base 側はサーバー側で拘束できず**ブランチ保護も未導入のため、10.1 に「**残余リスク(機械強制の限界)**」行を新設し、**人間管理の排他区間**であること・所有者や他メンバーの UI/API 操作は防げないこと・事後検出にとどまることを明記(ブランチ保護が使えるようになったら置き換える)
    - **P1-1**: 「証跡コミット後の HEAD = 候補 SHA」が **PR 必須・no-ff(6.2)と接続していない** — feature ブランチの HEAD は `origin/develop` と一致せず、develop へ直接コミットすれば 6.2 違反、PR を no-ff 統合すれば候補は証跡コミットではなく統合コミットになる。`phase4` の実施入口(誰が検証器を呼ぶか)も未定義だった → 10.1 に「**実施経路(ゲート種別ごと)**」行を新設。記号 **T**(試験コミット)/ **E**(証跡コミット)/ **M**(develop への no-ff 統合コミット)を定義し、**`phase4` = Phase 4 PR 上で `candidate_sha` = E として検証器を直接実行・`/release` を経由しない・合格時のみマージ**、**`release` = 専用 feature ブランチで E を作り PR で develop へ統合 → `candidate_sha` = M を固定して `/release` 開始**。失効判定は T → 最終候補に対して行う。13 章 Phase 4 の完了条件と 8.4 の `/release` 要約にも明記
    - **P2-1**: 8.4 の `/release` 要約に「検証器が無ければ即中断」という**5 周目 P0-1 で廃した旧条件**が残っていた → 「Phase 4 完了までは有無にかかわらず無条件中断」へ統一
  - **7 周目 = approved 不可(P0×3 / P1 なし / P2×1 / (B) なし)— 全件採用**。6 周目の反映は 4 件とも実体確認された。**指摘 3 件はいずれも「6 周目で自分が新設した規定の穴」**(attempt_seq・実施経路・排他区間)であり、新しい領域は開いていない
    - **P0-1**: `attempt_seq` は**候補ツリー内しか列挙しない**ため、**並行する 2 ブランチが同じ番号を採ると破れる** — `passed` 側だけ先に統合すると未統合の `failed` 側が見えず古い `passed` が最大として通る。両方統合すると番号重複で fail-closed になるが、**append-only なので重複を消せずゲートキーが恒久閉塞**する → 10.1 に「**試行の排他と採番**」行を新設(ゲートキー単位で同時 1 件・採番主体 = 8 章の判定者・**予約レコードを先に develop へ統合して番号を確定** → 受入 → `passed`/`failed` いずれでも結果証跡で予約を閉じてから排他解除)。検証器の合格条件を **⑦ 最大 `attempt_seq` かつ最大値が一意 / ⑧ 同一ゲートキーの予約がすべて閉じている**へ。**下位番号の重複・欠番それ自体は不合格理由にしない**(恒久閉塞を避けるため)とし、**回復規則**(既存を変更せず新しい予約を取り厳密に大きい番号で再実施)を明記
    - **P0-2**: **Phase 4 経路だけ統合結果 M が未検証**だった — `candidate_sha` = E で合格後に develop へマージするが、検証後に develop へ入った変更は no-ff 統合結果 M のツリーに含まれ、**追加された失効対象パスを検査しないまま Phase 4 を完了できる**(リリース経路は T→M なのでこの穴はない)→ Phase 4 経路にも同じ統制を適用(`phase4_base_sha` の固定・排他区間・API 再照合・`--match-head-commit E`・M の第 1 親/第 2 親/ツリー検査)。**base が動いた場合と事後検査不一致は Phase 4 未完了とし、M を新たな試験対象 T として再受入**。※ 人間の判定者が関与する経路は 10.1・13 章に残っており失われていないことも確認された
    - **P0-3**: 排他区間を `release/SKILL.md` で「**一切 push しない**」と書いたが、**GitHub UI/API による他 PR のマージは push ではない**ため、規則を守ったまま base を動かせた(6 周目 P0-2 の競合が再成立)→ **禁止対象を「`main`・`develop` を進めるあらゆる操作」**へ(直接 push・UI/API マージ・Web エディタ編集・bot/Actions。例外は当該リリース PR のマージのみ)。**排他管理者 = 8 章の判定者(= リポジトリ所有者)**、**開始と解除を当該 PR へコメントで記録**、**時間切れ・不一致は解除して受入からやり直す**を明記
    - **P2-1**: 承認済み計画書の「影響する正本」表に**旧暫定ゲート条件**(検証器が無ければ即中断)が残存 → 無条件中断+Phase 4 PR でのみ置換へ更新し、手順 3 の追加(候補 SHA 固定・排他区間)も追記
  - **8 周目 = approved 不可(P0×1 / P1×1 / P2 なし / (B) なし)— 全件採用**。**P0 が 3 → 1 へ減少**。7 周目の反映は 4 件とも実体確認され、「M を T として再受入しても無限後退にならない」ことも検証された(新しい予約・証跡は失効 allowlist 内で、再試行 E を最新 `phase4_base_sha` へ載せて排他すれば収束する)。指摘 2 件はいずれも 7 周目で新設した予約機構の穴
    - **P0-1**: 予約レコードと結果証跡を結び付ける**一意キーが無い**ため、**予約 0 件の候補ツリーに大きい `attempt_seq` の `passed` を手書きするだけで ⑧ が空集合として成立**し、採番統制を機械的に迂回できた。加えて**私の記述自体にバグ**があり、「予約レコードと結果証跡を列挙して最大 `attempt_seq` が一意」は**予約と結果が同じ番号を持つため最大値が必ず 2 回現れ、恒久 fail-closed になる**。回復規則(新しい予約を取る)も ⑧(未閉塞予約があれば開始不可)と矛盾していた → `attempt_id`(予約時発行・不変・一意)を必須キーへ追加し、結果証跡に**閉じる予約の `attempt_id` を必須記録**。**`attempt_id` で 1 対 1 に畳み込んだ「試行集合」で最大値の一意性を判定**すると明記し、**⑨ 名指し結果証跡に対応する予約がちょうど 1 件・予約を持たない結果証跡が存在しない**を追加。回復規則を「**まず未閉塞予約を同じ `attempt_id` の `failed` 閉塞記録で閉じて統合 → そのうえで既存全予約より厳密に大きい番号を新規予約**」の順序へ是正
    - **P1-1**: **Phase 4 のブートストラップが実行不能**だった — 「各 Phase = 1 PR」(13 章)・「Phase 4 PR は受入合格時のみマージ可」・「予約は受入前に統合済み」の 3 条件が両立せず、`docs/ops/` 自体が Phase 4 成果物。さらに `failed` 時は Phase 4 PR をマージできないので結果証跡を統合できず**予約が永久に閉じない**。排他区間の例外も「当該**リリース** PR のマージのみ」だったため文言どおりなら **Phase 4 PR 自身のマージも禁止**されていた → 10.1 に「**Phase 4 のブートストラップ**」行を新設(① 予約だけの監査 PR を先に統合〔`docs/ops/` 新設もここ〕→ ② Phase 4 PR に統合点を取り込む → ③ 受入・結果証跡追加・検証 → ④ 合格時のみマージ。`failed` 時は**実装を含まない結果証跡だけの監査 PR** で予約を閉じる)。**13 章冒頭に「監査 PR は 1 PR に数えない」例外**を明記し、排他区間の例外を「**当該ゲート対象 PR のマージのみ**(`release` = リリース PR / `phase4` = Phase 4 PR)」へ是正

  - **9 周目 = approved 可(P0 なし / P1 なし / P2 なし / (B) なし)— 収束**。8 周目の 2 件は解消確認(`attempt_id` による畳み込み・⑧⑨・対応不能/多重対応の fail-closed が揃い、恒久 fail-closed が解消。回復規則も ⑧ と排他規則の双方と両立)。**監査 PR の例外は Phase 本数の計上だけで、通常 PR・直接コミット禁止・no-ff・CI・レビュー規律を緩める記述はない**ことも確認された。7.1-1 の要件複製違反なし、6.2・6.4・8.4・10.2・13 章との矛盾なし
    - **Phase 4 への申し送り(承認阻害ではない)**: ① `attempt_id` の形式・発行手順・一意性の範囲を定義し**発行主体を判定者と明記**する。予約/結果のレコード種別と、**同一 `attempt_id` 間でゲートキー・`attempt_seq` も一致必須**であることをスキーマ化する ② **予約レコード専用の命名規則**を追加し、結果証跡名にも `attempt_id` の短縮形を含めることを検討する ③ **append-only CI では変更・削除だけでなく、新規レコードの孤児・多重対応・不正スキーマも統合前に拒否**する(修正不能な監査記録が残る事故を防ぐ) ④ `win-setup` ランナー選定は既に Phase 4 の未決事項として管理済み

- **ハーネス設計書 v1.4 を approved 化(2026-08-12・山田正輝)**。確定ゲート = 敵対レビュー **9 周**(sol xhigh)で **P0×13 / P1×15 / P2×8 = 計 36 件を全件採用・不採用 0 件**、9 周目は指摘なしで収束。要件書 v1.9(5 周)とあわせ、本タスクの確定ゲートは合計 14 周
  - **周回が伸びた構造**: 5 周目以降の指摘はすべて「**受入証跡と実リリースを機械的に結び付ける**」という一点への深掘りで、各周が**前周で自分が書いた文言の穴**を突く形だった(発散ではない)。運用プロトコルを設計書内で完全に規定しようとすると、書くほど検証面が増える。→ **ハーネス評価台帳の材料**
  - **自分の記述バグを敵対レビューが捕捉した例**: 8 周目 P0-1 — 「予約レコードと結果証跡を列挙して最大 `attempt_seq` が一意」は、**予約と結果が同じ番号を持つため最大値が必ず 2 回現れ、書いたとおり実装すると受入ゲートが一度も通らない**。`attempt_id` での畳み込みを明記して是正

- **10.1 の粒度について PO 裁定(2026-08-12)**: 設計書 10.1 の運用プロトコルは**「改訂前提を明記して確定」**とする。10.1 冒頭に「本節は検証器の実装に先立つ設計であり、**Phase 4 の実装で実機検証し必要なら改訂する**(改訂は 7.3 の確定ゲート)。**Phase 4 完了までは `/release` 手順 0 の無条件中断が唯一の実効ゲート**であり、本節の各規定はその置換時に満たすべき要件として機能する」を追記した。選択肢は (A) 改訂前提を明記して確定 / (B) 不変条件だけ残し詳細を `docs/ops/nfr021-acceptance/README.md`(Phase 4)へ委譲 / (C) 収束するまで回し切る の 3 案で、**(A) を採用**。理由: 7 周ぶんの敵対レビュー成果を捨てず周回を止められ、残余リスクは手順 0 の無条件中断で押さえられているため

- **`/check`(2026-08-12)**: harness `uv run pytest tests/` = **213 passed** / `check_docs_status.py` exit 0 / 変更 md 15 件の相対リンク = **1 件のリンク切れを検出して修正**(計画書が改善台帳の一行を引用するときに `[NFR-021](requirements/...)` を**リンク記法ごとコピー**していた。台帳〔`docs/` 直下〕からは解決するが計画書〔`docs/features/<slug>/`〕からは解決せず、CI の lychee で落ちる箇所)/ `core_guard.py` は PR イベント外でスキップ / backend・frontend は**未導入のためスキップ**(Phase 4 の成果物)
- **`/pr` の突合で宣言漏れを 1 件補記**: `.claude/agents/decision-tracer.md` を変更しているのに計画書 3 節の宣言に無かった(`spec-checker.md` は宣言済み)。内容自体は設計書 8.5 の番号上限撤去に対応する追随で、確定ゲート 1 周目 P1 の指摘由来。→ **ハーネス評価台帳の材料**(「影響する正本」の宣言と実差分の突合を機構が持たず、/pr の人手突合に依存している)

## 結果サマリ(PR 提出時点)

- **要件書 v1.9 approved**(2026-08-12)— NFR-021 の受入保証対象を **WSL2 に限定**。受入プロファイルを **Ubuntu 26.04 LTS の番号付き x64 WSL イメージ**に固定し切替規則を明記、合格条件を **Phase 4 完了時 / リリース候補時**に分離、**開発 DB の配置方式は設計フェーズへ委譲**。敵対レビュー **5 周・24 件全件採用**
- **ハーネス設計書 v1.4 approved**(2026-08-12)— 要件書 v1.9 への追随(2.1・2.3・2.4・4 章・5.1・5.3・6.4・8.3・8.4・8.5・9.3・13 章・14 章)+ **10.1 に NFR-021 受入ゲートを新設**。敵対レビュー **9 周・36 件全件採用**
- **10.1 で確定した統制**: `/release` 手順 0 の**無条件中断**(Phase 4 完了まで)/ 証跡が **approved な onboarding での完走**であることの機械的裏付け / **検証 SHA と実リリース SHA の一致**(完全 OID 固定・API 再照合・`--match-head-commit`・マージ後の親とツリー検査)/ **古い `passed` で通せない**(予約 + `attempt_id` + 畳み込んだ試行集合)/ **判定不能はすべて fail-closed** / 機構で強制できない範囲は**残余リスク**として明記
- **波及追随**: ADR-002・改善台帳(I-4/I-6 を参照化)・`AGENTS.md`・`README.md`・索引・`spec-checker`・`decision-tracer`・`setup-dev`・`github-setup`
- **コミット 22 件**(ステップ 1〜7 + レビュー反映 + `/check` 修正)。**コア領域・`guard_paths` ともに非該当**

## 決定

- **要件書 NFR-021 を WSL2 前提へ追随させる**(ハーネス設計者の了承 2026-08-11)。文言は設計書側の既定解釈(変更履歴 v0.11「WSL2 を含む Windows 11 上で完結」/ 論点C「セットアップ再現手順は onboarding.md が正」)に合わせる。Must 要件の文言変更にあたるため **v1.9 として確定ゲートを通す**
- **ゲート経路は要件書へ単独で /finalize-doc を回す**。要件書 v1.8 行(:18)は「ハーネス設計書の確定ゲートで一括検証する」と書くが、要件書の frontmatter は現在も `status: in-review` であり一括検証は行われていない。**根拠は「設計書のほうが後発だから」ではなく(両記録は同日 2026-08-07)、正本更新ゲートの唯一の決定表である設計書 7.6 と現在の in-review 状態**(計画レビュー 1 周目 P1-3 で訂正)。なお **v1.8 行は原文のまま保存**し、訂正内容は v1.9 行に記録する(変更履歴は遷移履歴であり過去行を書き換えない — 設計書 7.1-4)
- **win-setup のランナー選定は Phase 4 へ申し送る**。/research の結果、`windows-latest`(Server 2025)では WSL2 は動くが GitHub は入れ子仮想化を「experimental / at your own risk」= 公式サポート外と明記しており、必須の継続検証基盤には不適切。本タスクでは設計書 10.1 の win-setup 行を「**ランナー未決・Phase 4 で選定**」へ訂正する(`windows-latest` の固定指定を残さない — 計画レビュー 1 周目 P1-5 で訂正)。あわせて参照先を README → onboarding.md に直す。ランナー候補(ubuntu-latest 主 / windows-2025 を非必須併用 / self-hosted / 手動)と根拠は research.md に記録
- **要件書の測定方法は CI 方針に依存させない**: 他 NFR と同様「何を測るか」の粒度で書く(= 「WSL2 クリーン環境でのセットアップ再現」)。ランナー選定は設計書 10.1 の役割
- **ステップ順序を変更 — 波及追随を両方の確定ゲートより前へ**(人間承認 2026-08-11・確定ゲート 2 周目 P0-2)。当初計画は「要件書起案 → ゲート → approved → 波及追随 → 設計書…」だったが、**確定ゲートは現状態を判定するものなので、approved にする時点で approved 正本間の矛盾(改善台帳 I-4 の React)が残っていてはならない**。新順序は「要件書起案 → **波及追随** → 要件書ゲート → approved → 設計書起案 → 設計書ゲート → approved」。計画レビュー 4 周目では「波及追随を**設計書**ゲートより前に」と言われ、今回は「**要件書**ゲートより前に」と言われたため、**両方を満たすには最前に置くしかない**
- **開発 DB の配置方式は要件化しない**(確定ゲート 1 周目 P1-1・人間の裁定 2026-08-11)。計画レビュー 4 周目では「DB の設置場所も WSL2 内へ統一」と定めたが、**確定ゲートで「要件が実装手段を規定しすぎている」と否決**され、要件書は「**WSL2 から完結して利用できる**(配置方式は設計フェーズで決定)」までに縮小した。決定的だったのは `onboarding.md:32` の「WSL 内ネイティブ導入」が **Docker Engine** の導入方法であり **PostgreSQL のネイティブ導入経路はどこにも存在しない**という指摘。Windows ホスト側の排除という意図は「WSL2 から完結」で保たれる。以下は当該裁定の当初の記録:
- ~~開発 DB(PostgreSQL)の設置場所を WSL2 内へ統一~~(計画レビュー 4 周目 P1-1 の裁定 — 上記により**確定ゲートで縮小**)。「WSL2 必須」と裁定しながら要件書 NFR-021・7.1(`:739`)・改善台帳(`:58`)が「**Windows ネイティブ版** or Docker」を残しており、排他性が不十分だった。実装側の文書はすべて Docker 前提(設計書 `:209` 5.3「開発環境は docker-compose.yml」/ `:153` / `:676` / `AGENTS.md:33` / `onboarding.md:32`)で Windows ホスト上の PostgreSQL はどこも採っていない。**要件レベルでは「WSL2 内のネイティブ導入 or Docker」と許容幅を残し、Windows ホスト側だけを排除**する。設計書 5.3 の Docker 選択は変更しない。**改善台帳が 4 件目の影響正本**になる
- **改善台帳の該当箇所は「文言の書き換え」ではなく「要件書への参照に置換」する**(人間の裁定 2026-08-11・計画レビュー 5 周目 P1)。レビューは「I-6 の変更対象は pitchlog が許容する開発 DB 配置という方針そのものだから確定ゲートが必要」と指摘したが、構造を確認すると `:58` の「開発は Windows ネイティブ版 or Docker」は**要件書 NFR-021・7.1 の内容の複製**であり、docs 規約第 1 項「**1テーマ1正本・内容を複製しない**」に反している。I-6 の改善の核心は「SQLite/PostgreSQL 二重運用をやめ PostgreSQL に一本化」であって設置場所は付随記述。**参照化により方針を持たない形に変える**ことで、(a) 規約違反の重複が解消し (b) 今後 NFR-021 が変わっても台帳の追随が不要になり (c) 節更新(PR レビュー)として筋が通る。なお改善台帳は**変更履歴表を持たず版管理もしていない**(索引の版も「—」)ため、確定ゲートを回しても版繰り上げの概念が存在しないという事情もある
- **Windows ネイティブは「未保証・受入判定に使用不可」— 禁止ではない**(人間の再裁定 2026-08-12・設計書の確定ゲート 2 周目 P1-1)。要件書の確定ゲート 1 周目 P1-2 で射程が「開発者ワークステーションの**受入保証対象外**(CI・本番・移行・移植性は制限しない)」に狭まった結果、`setup-dev/SKILL.md` の「認めない」が **approved 正本より強くなっていた**ため、正本に合わせて弱めた。設計書本文・要件書・skill の語義が一致した
- ~~**Windows ネイティブ開発は認めない — WSL2 必須に統一**~~(人間の裁定 2026-08-11・計画レビュー 3 周目 P1-2)→ **2026-08-12 の再裁定で superseded**(上記)。以下は当初裁定の記録として保存する。設計書 `:668`「WSL1 非対応のため**必ず WSL2**」・論点C の PO 決定・`onboarding.md` 0 章が WSL2 を必須項目化・Codex の bubblewrap が WSL1 非対応・実環境も WSL2 — の 5 点で整合。**論点C 改訂(ネイティブ継続 → WSL2 移行)前の前提が 3 箇所に残っていた**ため除去する: 設計書 `:418`・`:517` の「Windows sandbox 設定」言及と `setup-dev/SKILL.md:19` の「ネイティブ例外時のみ `[windows] sandbox = "elevated"` を提案」。NFR-021 の確定文「WSL2 を含む Windows 11 上で完結」はこの裁定と両立する

## 未決・次の一歩

- 次: **人間承認**(計画書の「承認: 済」化)。承認後にステップ 1 から着手する
- **設計書は v1.4 として確定ゲートを通す**(当初は「7.6-3 の実装追随の節更新・版繰り上げなし」で起案したが、**計画レビュー 1 周目 P0-1 で否決**)。理由: `:699`(Phase 4 の完了条件)は未実装フェーズの受入契約そのものの変更、`:561` は将来 CI 設計の変更であり、いずれも構造的変更に当たる。前例 — v1.1 の Phase 3 完了条件改訂も確定ゲート対象、v1.2・v1.3 も当初の「節更新」判定が覆っている。よって `/finalize-doc` を **要件書 → 設計書の順で 2 回**回す
- win-setup のランナー選定について、ハーネス設計者からの回答が未着(a: ubuntu-latest 必須 / b: windows-2025 を非必須 / c: 併用 / d: self-hosted か手動)。回答が来なければ設計書を「ランナー未決」に留めて Phase 4 へ申し送る
- ~~Windows ネイティブ例外の可否が未裁定~~ → **裁定済み(上記「決定」参照)**
- **次タスクとして「ハーネス運用評価台帳」を新設する**(2026-08-11 合意)。本プロジェクトでハーネスを回すのが初回であり、**タスク単位の worklog では横断的な傾向が残らない**ため。`docs/development/harness-evaluation.md` に、改善台帳(`improvements-from-baseball-scoring.md`)と同じ「蓄積台帳」型で新設する想定。**新設 = 7.6-3 後段(`/finalize-doc`)** に該当する見込み。本タスク完了後に `/task-start` で立てる
  - 本タスクで既に出ている材料(台帳へ移す候補):
    - **ゲート区分(7.6-3)の判定が実務で機能していない** — v1.2・v1.3・今回の設計書・改善台帳と、当初「節更新」で起案した判定が**連続して確定ゲートへ覆っている**。判定基準の具体化(例「Phase の完了条件・受入契約に触れたら確定ゲート」)を検討する価値がある
    - **計画レビューの収束コストが高い** — 文書のみのタスクで 5 周。前例も feature-status で 11 周・~~harness-baseline-merge で 5 周~~
      - **訂正(2026-08-12・harness-evaluation の /investigate で発見)**: harness-baseline-merge の**計画レビューは反対側レビュー 1 周**(`docs/worklog/2026-08-10-harness-baseline-merge.md:13`)で、**5 周は確定ゲートの周回数**(`:18`)。計画レビュー数として引用したのが誤り。正しい横断値は本 feature の `docs/features/harness-evaluation/research.md` 4.1 の表を正とする
    - **`check_docs_status.py` の検査範囲が狭い** — status のみで版・最終更新日・変更履歴を検査しないため、索引の版ズレが CI をすり抜ける(3 周目 P1-1 で実際に露呈)
    - **版参照の散在** — 要件書を 1 版上げるだけで 14 箇所の追随が発生する(`AGENTS.md`・設計書 4 箇所・spec-checker・README・索引・ADR-002 ほか)。単一の参照点に集約できないか
    - **方針転換時の波及チェックの仕組みがない** — 論点C(ネイティブ → WSL2)の追随漏れが 4 周にわたって発見され続けた。決定を正本へ反映するときの波及チェックリストが機構化されていない
    - **worklog の SessionStart 注入は諸刃** — 文脈が引き継げる利点の裏で、古い記述がそのまま次セッションへ伝播する(2 周目 P1-6 で実際に指摘された)
    - **plan-template のサンプル行**が埋まっているため「空テンプレ拒否」が実質機能していない
    - **機能した点も記録する** — 計画レビュー 1 周目で P0 を検出(自分では通していた)/ `AGENTS.md:5` の版参照漏れ・`check_docs_status.py` の能力誤認・引用行の不正確を機構が捕捉 / `feature_status.py` の現在地導出が正しく機能
