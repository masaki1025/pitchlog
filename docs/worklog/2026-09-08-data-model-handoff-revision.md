---
date: 2026-09-08
topic: TSK-250 の受け渡し契約の改訂とスキーマ + ORM の切り出し
branch: feature/data-model-handoff-revision
---

# 作業ログ: 2026-09-08 TSK-250 の受け渡し契約の改訂とスキーマ + ORM の切り出し

## やったこと

### /task-start

- Notion タスク **TSK-335**(2026-09-07 に TSK-271 の送り先として起票済み)を取得。**重複起票はしていない**
- worktree `../pitchlog-worktrees/feature-data-model-handoff-revision`
  (ブランチ `feature/data-model-handoff-revision`・起点 `origin/develop` = `196627e`)を作成
- **重さ分類 = コア領域**(データ移行 + テナント分離に触れる射程判断を含むため)
- **着手時点の Notion 側 DoD は 4 ブロック**(内部矛盾の解消 / 短縮判断① / スキーマ + ORM の切り出し /
  authz DDL の判定)。**うち 3 ブロックは 2026-09-08 に本タスクへ追記したもの**

### /investigate

**3 観点に分解して調査サブエージェントへ並列で委任**した(設計書 8.5 の既定 3 並列):
**spec-checker** = 要件がデータモデルに直接課す条項の全数と authz DDL の要求の有無 /
**decision-tracer** = 受け渡し契約と開始条件の決定経緯・5.1 の ORM 留保のゲート水準・
13 章「開始条件を定めない」の履歴 / **legacy-analyst** = 移行が新スキーマへ要求する構造の最小集合。

**エージェントが触らない領域は自分で実測した**(重複回避): `T250P` のステップ 1〜10 の実体 /
候補案の節構成と 3 節の RLS 記述 / 候補案の `REQ:<行>` 引用の腐り率 /
`tests/test_ci_wiring.py` の ORM 非導入検査の実在確認 / 候補案 3-2 節の内部矛盾の照合。

**統合先**: [research.md](../features/data-model-handoff-revision/research.md)(702 行)。
**引用規約の自己検査**: 可変正本への行番号引用 **0 件** / `docs/legacy/` への `記号:行` 参照 **94 件**
(残すのが正)/ `check_docs_status.py` rc=0。

**本調査で新規に検出した切り出しの阻害要因 3 件**(いずれも承認済み計画・正本・機械検査との衝突):

| # | 内容 | 検証 |
| --- | --- | --- |
| (a) | **設計書 10.1 の実装追随箇条(approved)が「ORM は入れない(`sqlalchemy` / `alembic` が lock に無いことを検査)」と定め、`tests/test_ci_wiring.py` の `test_psycopg_is_exact_product_dependency_without_orm_packages` が実際に検査している**。一方 `T250P` ステップ 25 は「ORM = SQLAlchemy 2.x・Alembic を確定」。**調停した決定は無い** | 正本の逐語とテストコードを実見。`backend/` に両者の文字列は 1 件も無いことも確認 |
| (b) | **候補案 1〜12 節の `REQ:<行>` 引用 286 件が腐っている**。`T250P` ステップ 2 は「変更は相対リンクの解決のみ」で移送する設計だが、照合可能な 54 件のうち**現行要件書の同一行に逐語があるものは 0 件**(行ずれ 6 / 逐語が実在しない 48)。貼り替えはステップ 22(20 ステップ後) | 逐語つき引用を抽出し、現行要件書の ±5 行の窓・全文・空白正規化の 3 段で照合 |
| (c) | **候補案 3-2 節が移行バッチの書き込み経路について内部矛盾している** — 「マイグレーション用(テーブル所有者)= バイパスする」と書く一方、同節が公式のバイパス条件を「③ テーブル所有者(**かつ `FORCE ROW LEVEL SECURITY` 無し**)」と明記し、全テーブルへ `FORCE ROW LEVEL SECURITY` を推奨している。**越境関数側は 3 周目 `P1-1` で `BYPASSRLS` を与えて是正されたが、マイグレーション用ロールの行は是正されていない** | 候補案 3-2 節を実見。legacy-analyst の申告を原典で確認 |

**(A)/(B) の枠組みを是正した**: 着手時は「authz DDL を含める / 外す」の二択で立てていたが、
**要件は authz DDL を要求も禁止もしていない**(走査でヒット 0 件)一方、**候補案はテーブル制約の形を
RLS の挙動から導出している**(一意制約と FK は RLS を貫通するため、制約違反エラーが他テナントの行の
存在を漏らす)。→ **論点は 3 分割**: ① RLS を防御層とする設計判断(残さないとテーブル制約の根拠が消える)
/ ② DDL の実機確定(TSK-317)/ ③ 移行バッチの書き込み経路(決定が存在しない)。

**あわせて是正した自分の記述 2 件**:
- **「(B) は要件違反」ではない** — 「RLS 未適用のスキーマが develop に入ること」を禁じる要件条項は
  存在しない。8 章柱書は「**リリース可と判定する**」時点を定めており、`NFR-010`「初日から全機能に適用」と
  改善台帳 `I-2`「最初から実装する」は適用範囲と後付け禁止を述べたもの
- **`TSK-330` の「開始条件 = TSK-250 の完了」は人間の裁定ではない** — TSK-321 の計画レビュー
  1 周目 `P1-4` の反映として起票時に付けた条件で、**同タスクの裁定 1 は逆向き**
  (「backend 不変と TSK-250 非依存は維持」)。差し替えは裁定の変更ではなく根拠のない条件の是正

## 計画レビュー(敵対レビュー)3 周の記録

**周ごとの件数**: 1 周目 **P0 4 / P1 4**(8 件)→ 2 周目 **P0 5 / P1 6**(11 件)→
3 周目 **P0 3 / P1 5 / P2 1**(9 件)。**減らなかった。**

**3 周目 9 件の性質別内訳**:

| 性質 | 件数 | 中身 |
| --- | --- | --- |
| **中身の欠陥** | 2 | `#1` `R-6` と旧ステップ 14/16 の移管が矛盾(P0)/ `#8` 改訂中の `T250P` を `承認: 未` へ戻していない(P1) |
| **自分で書いた検査層の欠陥** | 6 | `#2` 正規表現が表の指定形式と不一致 + 旧番号 18 固定(P0)/ `#3` import が例外で検査 ⑥⑨ が一度も動いていない(P0)/ `#4` 射影順のせいで `未起票` が必ず 0(P1)/ `#5` 宣言した exact-set になっていない(P1)/ `#6` `[機械]` と書いた条件にコマンドが無い(P1)/ `#7` 期待値を判定していない(P1) |
| 文言 | 1 | `#9` 「`T250P` ほか 4 文書」が 5 文書に読める(P2) |

**レビューを待つ間の自己走査で同じ層から 4 件を自分で発見した**(いずれも実測で確認):

| 発見 | 実測 |
| --- | --- |
| `test -s <(allowed "$T")` は**必ず rc=1** | プロセス置換は FIFO でサイズ 0。`f() { echo hello; }; test -s <(f)` → rc=1 |
| 旧検査 ④ の右辺が**原理的に rc≠0** | 右辺 = 本書中の `TSK-3[0-9]{2}` 全部 = `250 / 269 / 271 / 317 / 330 / 334 / 335 / 339`(言及だけの ID を含む) |
| 旧検査 ⑤ が**空表で green** | `grep -cv` は入力 0 行で 0 を返す |
| 旧検査 ⑧ が `未取得` を**除きすぎ** | `format_progress()` は `未取得(git 失敗)` / `未判定(...)` も**ステップ進捗行に**出す |

**P0 `#3` の実測**(検査 ⑥⑨ が一度も動いていなかった):

```
AttributeError: 'NoneType' object has no attribute '__dict__'
```

`importlib.util.module_from_spec()` の戻りを `sys.modules` へ登録せずに `exec_module()` すると、
dataclass の処理が `sys.modules.get(cls.__module__)` で `None` を引く。`sys.modules[spec.name] = m`
を置くと通る(実測)。

## 決定

### 打ち切りの裁定(2026-09-08・山田正輝)

**選択肢 3 案を提示し、「検査層を最小化して打ち切り」を採った。**

| 案 | 内容 | 採否 |
| --- | --- | --- |
| (a) 検査層を最小化 | 機械検査を 3 本(旧記述の不在 / `parse_step_table` が valid / 文書 lint)に絞り、残りは `[手動・外部]` へ | **採用** |
| (b) `scripts/` + `tests/` へ出す | 検査スクリプトとテストを新設。false green は原理的に潰れるが「コード変更 0 行」の宣言と 3 節「反映なし」を破る | 不採用 |
| (c) 9 件直してもう 1 周 | 過去 3 周が 8→11→9 件なので同じ層から新しい false green が出る見込み | 不採用 |

**採った代価(明記して人間の承認を仰ぐ)**: `[手動・外部]` へ落とした 7 項目
(許可入力集合 / 送り先タスクの ID 台帳 / ステップの資産所有表 / 旧→新対応表 / ステップ 7 の禁止・必須集合 /
行番号引用 / `feature_status.py` の表示)は**言い換えで通せる**。担保は**代替統制 2(人間の逐行確認)と
3(PR の必須チェック行 + 実施記録行)だけ**になる。**逐行確認は PR 作成者以外**が行う。

**成果物は全部作る** — 落としたのは機械判定だけである。

### 3 周目の反映内容(コミット `39eebcd`)

- `#1`: 4 節の資産表でステップ **14・16 を資産単位の 4 行に分割**。`D4`(`C13` 非再利用・世代)と
  復旧世代・`RG1`・`I6` の**永続化写像だけを TSK-250 に残す**。**`R-6` の除外**(生成アルゴリズム・
  物理形式は論点 17 の受け取り先が持つ)を明記し、対応表・資産所有表・DoD へも書く指示を入れた
- `#2`: 見出しを `#### ステップ 18 の許可入力集合` → **`#### 写像ステップの許可入力集合`**
  (旧番号を見出しに入れない)。対応する写像ステップは旧→新対応表経由で特定する
- `#3`: `sys.modules[spec.name] = m` を検査 ② へ入れた
- `#8`: **ステップ 1 で `T250P` の `承認` を `未` へ戻す**。最終の人間承認時にだけ新しい日付・承認者で `済`
- `#9`: 「`T250P` **を含む** 4 文書」へ統一
- `#4`〜`#7`: 該当する機械検査を撤去し、成果物と人間の確認項目として 4 節の `[手動・外部]` 表へ移した

**全機械検査に是正前の実測値を併記し、番人型と是正型を書き分けた**:

| 検査 | 是正前の実測 | 型 |
| --- | --- | --- |
| ① 旧記述の不在 | `行き先表` **1** / 4 項目 **1**(負例は同階層・上位とも **1**) | 是正型 |
| ② `parse_step_table` | **両方 True**(`total=28`)— 値は是正前から green | **番人型**(再採番・分割で壊さない) |
| ③ 文書 lint | `check_docs_status.py` rc=0 / `check_plan_docs_sync.py` rc=0 | 番人型 |
| 節の存在(手動表の対象) | 線引き **0** / 台帳 **0** / 許可入力 **0** / 資産所有 **0** / 対応表 **0** | 是正型 |
| 行番号引用 | `T250P` **0** / 本書 **0** / research.md **0** | **番人型**(是正の証拠ではない) |

## ステップ 1: `T250P` を含む 4 文書の内容移送(2026-09-08)

**移送した 4 文書**(**マージしていない** — 通常のステップコミットで内容を持ってきた):

| 文書 | 出所 |
| --- | --- |
| `docs/features/data-model-canonical/plan.md`(`T250P`) | `feature/data-model-canonical` worktree |
| `docs/features/data-model-canonical/research.md` | 同 |
| `docs/worklog/2026-08-30-data-model-canonical.md` | 同 |
| `docs/worklog/2026-09-04-data-model-canonical.md` | 同 |

**なぜマージしないか**(1 周目 `P0-2`): `feature/data-model-canonical` は develop から **147 コミット
遅れており develop に無いコミットを 6 件持つ**。マージすると第 2 親が `origin/develop` 系統でない
ためステップ進捗の導出が `merge_unknown` になる(設計書 6.1)。

### 取り込みの帰結(**明記して申し送る**)

1. **TSK-250 のブランチ側に旧版が残る** — `feature/data-model-canonical` の `plan.md` は
   2026-09-04 時点の版のままである。**同ブランチを再開すると本ブランチの改訂版と全文コンフリクトになる。**
2. **`T250P` は develop 側を正とし、ブランチ側を捨てる** — 再開時は `git checkout origin/develop --
   docs/features/data-model-canonical/` 相当でブランチ側を破棄する。**両方を手で混ぜない。**
3. **`feature_status.py` は本ブランチの `T250P` を読まない**(実測 2026-09-08)— 同スクリプトは
   worktree ごとに `expected_feature_slug(branch)` の 1 本だけを選ぶため
   (`collect_features()` 内 `expected_plan_path` の決定)、移送した `T250P` は
   **永久に読まれない**(frontmatter の `branch` が別なので「plan 重複」にもならない)。
   `feature: data-model-canonical` のブロックは**他 worktree 由来の旧版**から出続ける。
   → **本ブランチでの `T250P` の検証は 4 節の機械検査 ② でパーサへ直接渡す。**

### `T250P` の承認を「未」へ戻した(3 周目 `#8`)

| 対象 | 変更前 | 変更後 |
| --- | --- | --- |
| frontmatter `承認` | `済(2026-09-04・山田正輝)` | **`未`**(改訂完了後に新しい日付・承認者で `済` へ) |
| DoD「本改訂に対する人間の再承認」 | `- [x]` (2026-09-04) | **`- [ ]`**(TSK-335 の射程改訂後の再承認を待つ。過去の承認 2 件は注記として保持) |

**理由**: ステップ 3〜7 で `T250P` を大幅改訂する間、旧承認日の `済` を残すと**再承認前の新版が
承認済みに見える期間**が生じる(確定ゲート手順 8 は射程変更に再レビューと人間承認を要求する)。

### 合格条件の判定

| 種別 | 条件 | 結果 |
| --- | --- | --- |
| `[機械]` | 4 文書が worktree に存在 | **4/4 OK** |
| `[機械]` | `git log -1 --format=%P` の親が 1 個 | **1 個**(マージコミットでない) |
| `[機械]` | `T250P` frontmatter が `承認: 未` | **1 件**(literal) |
| `[機械]` | 検査 ③(`check_docs_status.py`) | **rc=0** |
| `[手動・外部]` | `feature_status.py` が `ステップ進捗: 1/8` | **一致**(下記の実測) |
| `[手動・外部]` | 取り込みの帰結が worklog に記録されている | **本節**(帰結 3 件) |

**`feature_status.py` の実測**(ステップ 1 のコミット直後):

```
feature: data-model-handoff-revision (feature/data-model-handoff-revision)
  段階: 実装中(ステップ 1/8 完了)
  ステップ進捗: 1/8
  PR 状態: 未取得
  計画レビュー周回: 3
  反映周コミット突合: 一致
```

**`PR 状態: 未取得` は PR 未作成時の正常値**(2 周目 `P0-1` の是正どおり、段階・ステップ進捗の 2 行だけを見る)。

## ステップ 2: 先行タスク 1 本の起票と TSK-330 の開始条件の差し替え(2026-09-08)

### 起票した先行タスク

**[TSK-342](https://app.notion.com/p/3d593b75e68781958074ed04dff7dab7)** —
「データモデルのスキーマ本体と ORM 導入(`docs/design/data-model.md` 新設 + 設計書 5.1・10.1 改訂 +
`models`・`migration`)— TSK-250 の先行」/ ステータス `未着手` / 優先度 `高` / 担当者・プロジェクト設定済み

**射程 3 群を DoD の必須要素として書いた**(裁定 `R-2` により 1 本へ集約):

| 群 | DoD 項目 |
| --- | --- |
| ① スキーマ本体 | `docs/design/data-model.md` 新設 + **RLS を防御層とする設計判断とそこから導出されるテーブル制約**(一意制約・複合 FK)。**7.3 の確定ゲート** |
| ② 設計書 5.1・10.1 の改訂 + 機械検査の更新 | 5.1 の ORM 留保解除 / 10.1 の「ORM は入れない」改訂 / `tests/test_ci_wiring.py::test_psycopg_is_exact_product_dependency_without_orm_packages` の更新。**版は上げず差分を確定ゲートの敵対レビュー対象へ**(`R-5`) |
| ③ 実装 | SQLAlchemy `models` / Alembic `migration` / 依存追加 / バックエンドテスト(`NFR-019`) |

**カードに載せた制約**: コア領域(`sol xhigh`・敵対レビュー・逐行確認は PR 作成者以外)/ 要件の地雷 5 件
(`FR-015`・2.2 Won't・`NFR-014`・`4.0-2`)/ 候補案の `REQ:<行>` 引用 **286 件が腐っている**こと /
**候補案 3-2 節の内部矛盾**(移行バッチの書き込み経路 — 決定が存在しない)/ **開始条件 = TSK-335 の完了**

### 重複起票の確認(`[手動・外部]`)

「✅ タスク」DB を **`スキーマ ORM SQLAlchemy data-model`** で検索(`page_size=25`・データソース限定)。
**同一射程のタスクは無し。** 近いものは次の 3 種で、いずれも別物である:

| ヒット | 判定 |
| --- | --- |
| TSK-250(データモデル設計の正本化)/ TSK-335(本タスク)/ TSK-248(候補案の起草) | **本件の親・兄弟**。射程が重複しないよう本ステップで切り分けた |
| 「Implement: SQLAlchemy モデルと Pydantic スキーマを作る」ほか 2 件(2026-06) | **別プロジェクトの SQLite 期のタスク**(pitchlog 以前) |
| TSK-270 / TSK-317(認可構成の実機検証) | **DDL の実機確定**が射程。スキーマ本体は持たない |

### TSK-330 の開始条件の更新前後(`[手動・外部]`)

**対象**: [TSK-330](https://app.notion.com/p/3d493b75e68781ce8417d9ed882ece5b)
(同期プロトコル — 8 章の原子性の実行検証と backend 適用の実装〔後続 γ'〕)

| | 記述 |
| --- | --- |
| **変更前** | `- **開始条件: TSK-250(データモデル設計の正本化)の完了**` |
| **変更後** | `- **開始条件: [TSK-342](...)(データモデルのスキーマ本体と ORM 導入)の完了**` + 差し替えの理由と旧条件の由来 |

**理由**: TSK-330 が待っているのは **DDL・マイグレーション・ORM の存在**であり、TSK-250 の文書正本化の
全体ではない。それを持つのは TSK-342。**旧条件は TSK-321 の計画レビュー 1 周目 `P1-4` の反映として
起票時に付けたもので、同タスクの裁定 1 は逆向き**(「backend 不変と TSK-250 非依存は維持」)だった。
→ **裁定の変更ではなく、根拠のない条件の是正。**

**相互リンク**: TSK-342 ↔ TSK-335 / TSK-342 → TSK-250・TSK-330(コメントで記録。`URL` プロパティは
`/pr` 専用欄なので使っていない)

### 合格条件の判定

| 種別 | 条件 | 結果 |
| --- | --- | --- |
| `[機械]` | 本書と `T250P` に先行タスクの実 ID(URL 付き)がある | **本書 1 件 / `T250P` 2 件** |
| `[手動・外部]` | 重複起票していないこと(検索結果を worklog へ) | **上記のとおり** |
| `[手動・外部]` | TSK-330 の更新前後の記述と URL を worklog へ記録 | **上記のとおり** |
| `[手動・外部]` | 先行タスクの DoD に ① ② ③ の 3 つがすべて含まれている | **3/3**(判定者: 山田正輝) |

## ステップ 3: 受け渡し契約の改訂(裁定 `R-4`)(2026-09-08)

### 改訂した 2 箇所

| 場所 | 改訂前 | 改訂後 |
| --- | --- | --- |
| `T250P` 1 節 受け渡し契約表 **C 行** | 88 列すべての 保存元 / 変換 / 欠損時規則 / 逆変換可能性 の確定 | 88 列すべての**意味欄 4 項目**(**実際の中身 / 旧コードの参照箇所 / 新スキーマの行き先 / 変換規則**)の確定 + **`逆変換可能性` を任意の判断材料へ降格**(理由つき) |
| `T250P` 2 節 やること 5 | 先行タスク C の成果(88 列の**行き先表**)を本文へ写像する | 先行タスク C の成果(**88 列写像 JSON**)を本文へ写像する |

### 付録 D-1 の意味欄との 1:1 照合(`[手動・外部]`)

**要件書 付録 D-1 の逐語**(`docs/requirements/requirements-pitchlog-2026-07-22.md` 付録 D 箇条 1・v1.7):

> スキーマ定義ファイルに意味欄(**実際の中身・旧コードの参照箇所・新スキーマの行き先・変換規則**)を含める

| # | D-1 の語 | 改訂後の C 行 | 判定 |
| --- | --- | --- | --- |
| 1 | 実際の中身 | 実際の中身 | **逐語一致** |
| 2 | 旧コードの参照箇所 | 旧コードの参照箇所 | **逐語一致** |
| 3 | 新スキーマの行き先 | 新スキーマの行き先 | **逐語一致** |
| 4 | 変換規則 | 変換規則 | **逐語一致** |

区切り記号だけが `・` → ` / ` に変わっている(表セル内の可読性のため)。**4 項目の集合は完全一致・過不足 0。**

**降格の理由**(C 行に明記): **`逆変換可能性` は要件のどの条項にも紐づかない**。旧 4 項目のうち
`保存元` と `欠損時規則` も D-1 の語彙に無い(TSK-335 の `research.md` §7)。

### 改訂前の記述の隔離

`T250P` の末尾に **`## 履歴` / `#### 履歴(改訂前の記述)`** を新設し、改訂前の逐語を
「改訂日 / 場所 / 改訂前の逐語 / 置き換えた理由」の 4 列表で残した。

**運用上の罠(自己走査で発見・計画へ追記した)**: `operative()` の除外は「次の `^#{1,4} ` 見出しまで」
なので、**末尾の履歴節より後ろはすべて除外される**。→ **ステップ 5・6・7 で `T250P` へ節を足すときは
必ず履歴節より前に挿入する**(末尾へ append すると operative 文が検査対象から外れ、検査 ① が黙って
green になる)。

### 合格条件の判定

| 種別 | 条件 | 結果 |
| --- | --- | --- |
| `[機械]` | 検査 ① が 0 件・0 件(是正前の実測は各 1) | **0 / 0** |
| `[機械]` | 負例 2 種(同階層 `####` / 上位 `###`)で 1 になること | **1 / 1** |
| `[機械]` | 逐語が履歴節に残っていること(全文では各 1 件) | **1 / 1** |
| `[手動・外部]` | 4 項目が付録 D-1 の意味欄と 1:1 で対応 | **4/4 逐語一致** |
| `[手動・外部]` | 降格の理由が書かれている | **C 行に明記** |

## 追加の人間の裁定(2026-09-08・ステップ 4 の直前)

**ステップ 4 に入った時点で `T250P` の送り先・入力に事実誤認 2 件を発見し、人間の裁定を得た。**

| # | 論点 | 裁定 |
| --- | --- | --- |
| **R-8** | `T250P` ステップ 24・26・27 の資産を誰が持つか | **`T250P` に残す**(TSK-339 へ送るのは 26 のうち `contracts/legacy-columns/` 資産の paths 登録だけ) |
| **R-9** | 88 列写像の値を誰が決めるか | **値は TSK-250 が決め、TSK-339 は器だけを作る**(ステップ 18 の合格条件を書き換える) |

### 発見の中身(実測)

**(1) 送り先の誤り** — 私の 4 節は `T250P` ステップ 24・26・27 を TSK-339 へ送っていたが、
TSK-339 のカードを実見すると射程が別物だった:

| | `T250P` が触る資産 | TSK-339 の射程(カード実見) |
| --- | --- | --- |
| ステップ 24 | `scripts/design_relations/**` の staging→本番移設 / `profiles/registry.json` の `data-model` entry / `tests/test_ci_wiring.py` の docs-lint 系 / 設計書 10.1 の `docs-lint` 行 | `contracts/legacy-columns/` の JSON・schema・lock / `scripts/check_column_map.py` / **その** CI 3 段 |
| ステップ 26 | `data-migration.paths` 充填 **+ 既存 4 領域へ本正本を追加 + `guard_paths` へ新資産の全パス完全列挙 + 実設定を読む回帰テスト** | **`contracts/legacy-columns/` 資産の** `data-migration.paths` 登録 |
| ステップ 27 | 台帳 H-12・H-78・H-79 の更新 | (射程外) |

根拠として引いた research.md C-6 も「`contracts/` への契約化・CI 配線・`core-areas.paths` の充填」で
あって、文書検査機構の本番移設ではない。

**(1-b) 同じ行にもう 1 件の誤り**(裁定の適用中に発見): 旧版は「**24 のうち『設計書 10.1・テスト追随』
→ 先行タスク**」という行を持っていたが、**ステップ 24 が触る 10.1 は `docs-lint` 行**であり ORM 条項では
ない。`tests/test_ci_wiring.py` も **`test_docs_lint_runs_all_document_checks_without_selectors`(:978)** と
**`test_psycopg_is_exact_product_dependency_without_orm_packages`(:1250)** は**別テスト**(実見)。
→ **ステップ 24 は分割せず全体を TSK-250 に残す**。ORM は 25(→ TSK-342)の射程。

**(1-c) 2 周目 `P0-4` の是正を撤回した**: 「ステップ 24 を分割して TSK-339 へ送るから、`T250P` へ
『ステップ 28 の実行前に TSK-339 の完了を要する』開始条件を置く」としていたが、**送り先が成立しない**。
→ ステップ 24 を送らないので**外部依存が消え、この開始条件は不要**になった。

**(2) ステップ 18 の入力が存在せず循環していた** — `T250P` ステップ 18 の合格条件は
「**C の JSON** と 4 項目 + 共通正規化 ID で exact-set 一致(**C の blob digest** を照合)」だが、
TSK-271 `research.md`「履歴・非現行」節にこうある:

> 2026-09-07 の PO 裁定で本タスクの射程が「旧資料矛盾の判定 22 件 + 本調査メモ」へ縮小され、
> **88 列写像 JSON(行き先・変換・逆変換可能性・共通正規化 ID)は TSK-250 が所有**することになった

**C は JSON を作らない。** かつ器を作る TSK-339 の開始条件は「**TSK-250 のマージ**(物理実体名の
crosswalk を受け取らないと契約を作れない)」。→ `T250P` 18 → 88 列写像 JSON → TSK-339 → TSK-250 で
**循環**していた。

## ステップ 4: 写像ステップの許可入力集合の固定と合格条件の書き換え(2026-09-08)

### 新設した節

`T250P` へ **`#### 写像ステップの許可入力集合`** を新設(**確定ゲート節の直前** — 末尾の履歴節より前。
ステップ 3 で記録した罠のとおり)。**見出しに旧番号「ステップ 18」を入れていない。**

| 入力 ID | 出所 | digest / 版の参照先 |
| --- | --- | --- |
| `IN-01` | TSK-271 `research.md` 1 節 `DM-LG-*` 16 件の判定 | `research_blob_sha` = `59cb50fca6fdbcc045c5604f935c415e6d16b90a` |
| `IN-02` | 同 3 節 `DM-LX-*` 6 件の判定(`LX-01`・`LX-02` は射程外で「不明」) | 同上 |
| `IN-03` | 同 2 節 センチネル全 88 列表 | 同上 |
| `IN-04` | 同 4 節 座標の確定式(付録 B-5・x 恒等 / y 反転) | 同上 |
| `IN-05` | TSK-334 の値の採否(列 44 / 列 86-87 / 列 63-71) | TSK-334 の裁定記録(採否 ID + 決定日) |
| `IN-06` | 要件書 付録 D-1(列名・順序・型 + 意味欄 4 項目) | 要件書の版 |
| `IN-07` | `docs/legacy/research/data-layer.md`(**`evidence` のみ**・`記号:行`) | 版固定アーカイブ |
| `IN-08` | TSK-342 の `docs/design/data-model.md`(**新スキーマの物理実体名** = 行き先の値域) | 同書の安定 ID |

### ステップ 18 の合格条件の書き換え

| | 内容 |
| --- | --- |
| **改訂前** | 「**C の JSON** と 4 項目 + 共通正規化 ID で exact-set 一致(**C の blob digest** を照合し、一致しなければ fail)/ 本文の記述のうち **C の JSON に無い値が 0 件**(再解釈の機械検出)」 |
| **改訂後** | 「`column_index` 0〜87 の全行が本文にある(欠番 0)/ **各行の根拠が `IN-01`〜`IN-08` のいずれかを明示**し、**集合外の資料を根拠に引いた行が 0 件** / **`IN-01`〜`IN-04` は `research_blob_sha` を照合** / `docs/legacy/` が `normative` として参照されていない / **`LX-01`・`LX-02` に値を書いていない**」 |

**性格の変化**: 「**写像するだけ・再解釈しない**」→「**許可入力集合だけを根拠に決める**」。
再解釈の禁止は「C の JSON に無い値が 0 件」ではなく「**集合外の資料を根拠に引いた行が 0 件**」で担保する。
改訂前の逐語は履歴節へ隔離した。

### 循環の解消(`[手動・外部]`)

**`T250P` に TSK-339 を待つ記述は 0 件**(実測 — 残っているのは「本タスクは TSK-339 の完了を待たない」
という宣言文と履歴節の 2 箇所のみ)。依存の向きは **TSK-250 → TSK-339** の一方向。
**`IN-08` だけが TSK-342 待ち**で、それは本タスクの開始条件そのもの(裁定 `R-3`)。

### 合格条件の判定

| 種別 | 条件 | 結果 |
| --- | --- | --- |
| `[機械]` | `#### 写像ステップの許可入力集合` が 1 件(是正前 0) | **1** |
| `[機械]` | 検査 ① が 0 件のまま(履歴節への隔離が効いている) | **0 / 0** |
| `[機械]` | 検査 ②(`parse_step_table`) | **frontmatter True / valid True / total 28** |
| `[手動・外部]` | 許可入力 ID が集合として立っている | **`IN-01`〜`IN-08` の 8 件** |
| `[手動・外部]` | 循環が消えている | **`T250P` の TSK-339 待ちは 0 件**(判定者: 山田正輝) |

### 自己走査で見つけた自傷(ステップ 4 のコミット直後に是正)

**本書(TSK-335 の計画書)の実装ステップ表が壊れた。** `R-8` の反映でステップ 7 の行へ差し込んだ
文章に**改行が入り、1 行の表セルが 2 行に割れた**。結果 `feature_status.py` が
`実装状況: 不整合(実装ステップ表の番号が連番でない)` を返した(実測)。

→ 行を結合して是正。**`ステップ進捗: 4/8`** へ復帰(検査 ⑧ 相当の `[手動・外部]` 確認)。
**教訓**: 表セルへ複数行の文章を差し込むときは、置換文字列に改行を含めない。

## 未決・次の一歩

- **人間の承認を待っている**(`承認: 未`)。承認後、ステップ 1 から Claude が直接編集で進める
  (コード変更 0 行のため `/implement` は使わない)
