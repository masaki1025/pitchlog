---
date: 2026-08-21
topic: 製品データモデル設計の起草(論理データモデル + マイグレーション方針)
branch: feature/product-data-model-design
---

# 作業ログ: 2026-08-21 製品データモデル設計の起草(論理データモデル + マイグレーション方針)

## やったこと

- **/task-start**: Notion タスク起票(優先度 高・見積 8)→ worktree → 計画書・worklog の雛形 → `進行中` へ遷移 + ブランチ名をコメント。**Phase 4-5(TSK 別タスク)と並行**で進める(設計書 `:786`「Phase 4 は設計フェーズの進行と同期させる」)

- **/investigate**: 調査サブエージェント **4 並列**(既定 3 に対して +1 — 対象が要件書 6 章 + ブロック 1〜8 + 付録 D の 88 列と広いため)。**本タスクでは legacy-analyst が主役**(4-5 で外した理由がそのまま逆になる — 旧システムの 88 列データ構造と移行制約が設計に直接効く)。結果を [research.md](../features/product-data-model-design/research.md) へ統合

- **/research(Web 調査)**: `codex_run.py research`(read-only + live search + terra high)。**`--deep` は付けていない** — 論点が外部の一般技術(PostgreSQL・SQLAlchemy・Alembic・ドライバ)で、コア領域の深い技術検証には当たらないため(ADR-001 `:42`)。**秘密レスの作業ディレクトリから実行**(ラッパーが `.env*` のある場所での live search を機構で拒否 — 設計書 12.1。リポジトリには `.env.example` が追跡されているためリポジトリ内からは実行できない)。**サンドボックスのネットワーク例外は開いていない**(`network_access=false` を明示固定・live search は Codex 自身の経路)

- **Codex の出力の一部が `tail` で切れたため、セッション記録(`~/.codex/sessions/`)から復元した**(再実行していない)

- **/plan**: 計画書を起票し、`review adversarial`(sol xhigh)で **2 周**回して条件付き可決。**人間承認 2026-08-21**

## 決定

### 調査の裁定 2 件(原典で確認)

1. **旧資料のテーブル数の矛盾は矛盾ではなかった** — `data-layer.md:3` は 12 テーブル・`db-structure.md:17` は 13 個で `game_lineup_snapshot` の有無が食い違うが、**`db-structure.md:8` 自身が「88 列の実際の意味・値例・移行の罠は `research/data-layer.md` が正本。本書は構造の俯瞰図であり、移行変換層の実装は data-layer.md を正として行う」と優先関係を明記**していた。テーブル数の差は**対象リポジトリと時点が違う**ため(`C:/develop/baseball_system/` vs `Baseball_Scoring` develop HEAD `ed6a20f`)。`game_lineup_snapshot` は FastAPI 新 UI 期に追加されたテーブルで、**要件書 REQ:960 が既に「アーカイブ後に追加された領域」として未確定登録済み**
2. **「当時の値で表示」の対立も対象違い** — 遡及しないのは**背番号と適用規則**(4.0-2 が名指し)、遡及するのは**語彙の表示名**(4.0-3 が名指し)。**「試合ごとに語彙スナップショットを永続化する」という要求は要件書に存在しない**(全走査して 1 箇所のみ = NFR-019 のテスト入力面)。ADR-003 `:96` も「写像テーブルに焼き込むことは禁止」と明記

### Web 調査で設計を左右した事実(原典で確認)

**PostgreSQL 公式が「参照整合性検査(一意制約・主キー・外部キー参照)は常に RLS をバイパスする」と明記している**(`https://www.postgresql.org/docs/17/ddl-rowsecurity.html`)。テナント跨ぎの一意制約を置くと、**RLS を有効にしても制約違反エラーから他テナントの行の存在が推測できる**。要件書 REQ:823 は「**内容・存在を含め参照できない**」を要求しているため、**一意制約をテナント内に閉じる**必要がある。

あわせて確認: **RLS は PostgreSQL 9.5 からのコア機能**(Supabase 固有ではない)→ 要件の「標準 PostgreSQL の範囲」に適合 / バイパス経路は superuser・`BYPASSRLS`・**テーブル所有者(既定でバイパス)** / **transaction pooling では `set_config(..., true)`(transaction-local)をトランザクション内で発行する**(第 3 引数の意味も原典で確認)。

### 計画レビュー(敵対レビュー)2 周

| 周 | 指摘 | 主な反映 |
| --- | --- | --- |
| 1 | **否決**(P0×1 / P1×9 / P2×3) | 下記の「自分の判断ミス 4 件」+ 事前集計を (A) → (B)・無効化契約を無条件の (A) へ・ステップ 8 → 12 分割・重さ分類を `通常` → `コア領域`・禁止事項に安定 ID・典拠検査を全件機械検査へ |
| 2 | **条件付き可決**(P0×2 / P1×2 / P2×1 — **すべて (B)**。レビュア自身が「前周修正への指摘」と自己申告) | 共有集計の実行経路(共有専用ロール / `SECURITY DEFINER` の制限関数 / 追加コンテキスト)をステップ 3 の合格条件へ / FK エラーの存在推測経路をステップ 2 へ / **88 列の「互換出力では列名維持・移行変換では意味に従う」の分離** / `research.md` の未伝播箇所の同期をステップ 1 へ / 典拠検査を「直近の FR/NFR 見出しまで遡る」形へ |

### 自分の判断ミス 4 件(1 周目で是正)

1. **テナント跨ぎの FK を一律禁止していた** — 一意制約の漏洩と FK 一般を混同。**FR-041 の制御資源は複数テナントに跨る関係を持つのが要件**(REQ:824「記録・集計とは別のクラス」)で、禁止すると要件が表現できない。→ 論点 2 を一意制約に限定し、**制御資源の表現を論点 12 として新設**
2. **ADR-003 が既に固定した切り分けを未決論点に戻していた** — 「プレイ行と操作イベントのどちらが正か」は ADR-003 `:93-104` が「確定イベントを持ち現在状態・成績は導く」と固定済み。決めてよいのは**永続化上の表現**だけ
3. **88 列対応表を移行仕様タスクへ送る根拠が誤っていた** — REQ:960 が送っているのは**カットオーバー日と追加領域の線引きだけ**。付録D-1(REQ:1180)は各列の意味欄に「新スキーマの行き先・変換規則」を要求している(原典で確認)。**受け皿の欠落は論理モデルの作り直しになる**ので (A) に格上げして本タスクに含めた
4. **成果物が宙に浮く構成だった** — 設計書 7.6-4 が `docs/features/<slug>/` を「活動中のみ意味を持つ一時ディレクトリ」と定めているのに、12 件の決定をそこに置いて閉じる計画だった。→ **成果物を「候補案」に降格**し、**正本化タスクの起票と相互リンクを DoD の必須項目**に(設計書 `:825` の 4-6 → `P4-後` と同じ形)

### 逆方向の是正 1 件

**事前集計の採否を (A) → (B)**。私は「後付けは NFR-003 の共有画面条件と衝突する」と考えていたが、**NFR-005(REQ:795)は事前集計の採用可を「毎球参照される画面に限り」と限定**しており、共有画面性能を理由に先に採ると**逆に許容範囲を踏み外す**。今決めるのは再計算可能性と 14 トリガーの無効化契約まで。

### 教訓

- **「機構を足す前に、正本がその穴を既にどう扱っているかを確認する」**(4-5 で `push` 事後検査を撤回したのと同じ型)。1 周目の指摘は「穴がある」という事実として正しかったが、**私の対処が要件の別の要求を潰していた**
- **要件の 2 つの要求が衝突して見えるときは、対象の違いを疑う**(裁定 2 件はいずれもこの型)
- **調査エージェントが矛盾を報告したら、両文書の「対象と時点」を先に読む**(裁定 1 は `db-structure.md:3`・`:8` と `data-layer.md:11` を読むだけで解けた)

## 未決・次の一歩

- **次は起草ステップ 1**: `research.md` 3-2 節の禁止事項へ安定 ID(`P-01`〜)を付与し、**同メモの要約・3-3 節・5 節・6-5 節を計画書の 22 論点へ同期**する(2 周目 P1 — 前周修正の未伝播)。`design.md` は ID を参照するだけで本文を複製しない
- **(A) 12 件は候補案まで**。**「確定」と書かない**。ORM・ドライバ・Alembic の最終確定は**正本化タスクで設計書 5.1 の追随と同一 PR**
- **/task-done で正本化タスクを Notion へ起票し相互リンクする**(DoD の必須項目)
- **典拠は全件機械検査**(`REQ:<行>` を全件抽出し、行の実在と**直近の FR/NFR 見出しまで遡った**要求 ID の対応を検査するスクリプトを scratchpad で書く)
- **Phase 4-5 との並行は抵触しない**(起草のみで開発 DB へスキーマを適用しないため — 設計書 `:758`)

## 結果サマリ(クローズ処理)

**作ったもの**: `docs/features/product-data-model-design/design.md`(**1,271 行の候補案**)。
論理データモデル(エンティティ・関係・主要属性・一意性制約・テナント境界・削除の 4 系統)+
テナント分離の方式 + キャッシュ無効化契約(14 トリガー × 5 対象範囲)+ 全 88 列の行き先 +
サイドカー境界 + マイグレーション方針(推奨案)+ 索引設計の原則 + FR-001〜042 の全数対応表 +
未解決と申し送り。**「確定」とは書かない**(成果物は候補案であり、正本化は別タスク)。

**機械検査で DoD を確認した**:

| 検査 | 結果 |
| --- | --- |
| 禁止事項 `P-01`〜`P-60` の照合 | **60 / 60 件**(未記入 0) |
| FR-001〜042 の区分と該当節 | **42 件・欠落なし** |
| 88 列の行き先 | **列 0〜87 すべて・欠落なし** |
| 典拠の全件機械検査 | **199 引用すべて実在** |
| 「本設計で確定」等の表現 | **0 件** |
| 却下案 / 残余リスク | 9 箇所 / 5 箇所 |

**/check**: harness 396 passed / backend は ruff format・ruff check・ty・pytest すべて通過 /
**frontend はスキップ**(この worktree に `node_modules` が無く、差分が `frontend/` に触れないため
CI の paths filter も起動しない — **実行していないことを記録する**)/ 変更した markdown 4 件の
相対リンク不正 0 件 / `check_docs_status.py`・`check_plan_docs_sync.py` ともに exit 0。

**正本への反映**: **ハーネス運用評価台帳のみ**(版は上げない — 7.6-3 前段)+ `docs/README.md` の
台帳行の最終更新日を現行化。要件書・設計書・ADR・`contracts/` は**反映なし**(成果物が推奨案のため
設計書 5.1 の「設計フェーズで最終確定」は動かさない — 追随は正本化タスクで同一 PR に含める)。

### 台帳への記録

| 対象 | 内容 |
| --- | --- |
| **H-74**(新設・追跡優先度 通常) | **非正本の起草だけでタスクを閉じると未承認の設計判断が一時ディレクトリに残る**。設計書 7.6-4 が `docs/features/<slug>/` を活動中のみ意味を持つ一時領域と定めているのに、(A) 12 論点の結論をそこへ置いて閉じる計画だった。前例は 13 章 `:825` だが**一般規約として参照できる場所が無い** |
| **H-68**(更新) | **再発を 3 件目として記録** — 対応案 (c)((A)/(B) 分類の必須化)を**先回りで適用した実例**。敵対レビューが **2 周で条件付き可決**。限界も併記(1 周目の指摘は分類の妥当性そのものへのものだったため、分類しただけで指摘は減らない) |
| **`## 候補` (1)** | レビュー指摘に反応して規則を足す前に、正本がその穴の扱いを定めていないか確認する(**別ブランチの同型事象を典拠に張れないため候補**) |
| **`## 候補` (1)** | 典拠の全件機械検査では「正当な文脈クラス」を段階的に足す必要がある |

**挿入位置を見出し行で取った**(H-42・H-46 の教訓 — `## 候補` は冒頭の運用規則の**文中**にも現れるため、
文字列検索で挿入すると偽の見出しが生まれる)。挿入後に位置を確認済み。

### `/pr` の突合が宣言漏れを捕捉した(機構が働いた実例)

**突合 2-2 が exit 1 でブロックした**。原因は**私の宣言漏れ** — 計画書 3 節で `docs/README.md` を
「**反映なし**」(`docs/features/` 配下は索引カバレッジの除外集合)と宣言していたが、**クローズ処理で
台帳へ `H-*` を追記すると `/pr` 手順 1-3 ④ が索引の台帳行の現行化を要求する**ため、差分に現れた。

- **起草そのものは索引に載らない**という判断は正しかったが、**クローズ処理に伴う索引更新を見落として
  いた**。「反映なし」の宣言は**タスクの成果物だけを見て書いていた**
- 3 節の当該行を「台帳行の最終更新日を現行化(クローズ処理)」へ是正して再突合し exit 0
- **これは H-4(宣言と実差分の突合が人手依存)の機構側が実際に効いた実例**であり、
  `check_plan_docs_sync.py` の双方向突合(H-64/H-67 の是正で入ったもの)が捕捉した

### セッション中の自分の発言の訂正

作業中に「コミット時の遮断は **H-51 の再発 7 件目**」と述べたが、**これは誤り**。遮断されたのは
「`cd` と git 操作の複合はブランチ判定ができません」で、これは `git_guard` が**設計どおりに動いた**もの
(設計書 8.3 が `git -C <path>` を使うよう案内している)。H-51 はヒアドキュメント**本文**の行分割による
誤検知で、別事象である。**台帳項目ではない**ため記録しない。

### 未解決・次の一歩

- **次は `/pr`** の残り(push → PR 作成 → Notion `確認待ち`)。反対側レビューは **Claude 直起草 →
  `codex_run.py review adversarial`** の経路(内容がコア領域 5 領域の意味範囲に触れるため通常レビューでは足りない)
- **`/task-done` で正本化タスクを Notion へ起票し相互リンクする**(DoD の必須項目)。起票内容 =
  `docs/design/` へ新設 + **7.3 の確定ゲート** + **設計書 5.1 の追随を同一 PR** + ORM・ドライバ・
  Alembic の**最終確定** + 「要件に明示がなく導出で決めた 5 件」の裁定
- **移行仕様タスクへの申し送り**: カットオーバー日・移行対象の線引き / `game_lineup_snapshot` の帰属 /
  サイドカーの列定義 / 88 列対応の変換規則の詳細
- **`core-areas.json` の paths 充填**は別作業(敵対レビュー + 人間承認が必要)。論理モデルが確定すれば材料になる

## 敵対レビュー 4 周と「閉じる」判断(2026-08-22〜23)

**PR #23 は敵対レビューで 4 周にわたり否決され、指摘 37 件をすべて反映した。** P0 は 2 周目以降 0 件。

| 周 | 判定 | 件数 | うち直近の是正由来 | 主な内容 |
| --- | --- | --- | --- | --- |
| 1 | 否決 | P0×2 / P1×7 / P2×4 | — | **自チームと対戦相手を `tenant_id` で判別しようとしていた**(両方同じ値なので判別不能)/ **共有関数の認可条件から対象テナント側の付与が脱落**(6 前提だけを写し認可行列の層を落とした) |
| 2 | 否決 | P1×5 / P2×2 | **4** | 部分一意索引は「高々 1 件」で 0 件を排除しない / 旧チーム行の一対一振り分けでは移行できない / **FR 名 17 件が要件書と不一致・うち 5 件は別の要件を指していた** |
| 3 | 否決 | P1×9 / P2×1 | **4** | **`SECURITY DEFINER` は RLS をバイパスしない**(関数所有者とテーブル所有者の混同)/ 実効 `admin` の述語が兄弟節へ伝播していない / 移行プレイ行を再生成する正本イベントが無い |
| 4 | 否決 | P1×5 / P2×2 | **5** | **`NOLOGIN` + 無 `GRANT` では呼び出し経路を限定できない**(新規関数の `EXECUTE` は既定で `PUBLIC` へ)/ **同期連番を論理順序に使うと挿入が末尾に再生される** |

### 3 周目に「直し方」を変えた

2 周連続で「直近の是正の伝播漏れ」が最大の発生源だったため、**局所編集をやめて述語を 1 箇所へ集約**した
(`design.md` **3-0 節「認可の共通述語」** — テナントが有効 / 実効参加 / 実効 `admin` / 実効グループ /
実効な共有文脈を定義し、適用先 8 節を表で明示)。あわせて**「緩い述語表現の残存」を機械検査**したところ
**6 箇所を検出**した(3 件は是正の説明文で正当・**3 件は本当の残存**)。

**教訓**: **定義を集約しただけでは伝播漏れは直らない**。**残存を検出する検査が別に必要**である。

### 4 周目で閉じた(人間の裁定 2026-08-22)

**是正のたびに 4〜5 件の新しい欠陥が生まれる状態**が 3 周続いたため、**周回を止めて申し送る**判断をした。
**`design.md` 12-7 節**に 3 領域それぞれの「決めたこと / 決まっていないこと / 受け取り先 / なぜ文書だけでは
閉じないか」を書いた。

| 領域 | 受け取り先 | なぜ文書だけでは閉じないか |
| --- | --- | --- |
| **認可機構の物理構成** | **正本化タスク(実機検証を伴う)** | **公式ドキュメントを引用しながら 2 回続けて誤った**(関数所有者とテーブル所有者の混同 / ロール継承と関数 ACL の混同)。**どちらも実機で 1 度試せば分かる誤り**。要求する越境テスト 4 件を明記した |
| **イベントの順序** | **同期プロトコル設計(別文書)→ 正本化** | **計画書 2 節が既に「同期プロトコルの詳細設計は別文書の領域」と宣言していた**。3 周目に「イベント連番へ一元化」と決めて 4 周目に差し戻されたのは、**宣言した境界を越えて決めようとしたから** |
| **移行のファンアウト** | **移行仕様タスク** | **旧 DB の実データを見ないと複製倍率も名寄せの困難さも分からない**。REQ:960 が既に関連事項を同タスクへ送っている |

### 私の誤りのうち、記録しておくべきもの

- **要件書の構造を読み違えた** — 共有関数の認可を「6 前提」だけで書いた。**その直後に資源粒度ごとの認可行列があり、そちらが「相手が付与」を要求**していた。前提だけを写して行列の層を落としていた
- **FR 名を独自の短縮名で書いていた** — 要件書の見出しと機械照合したら **17 件不一致・5 件は別の要件**を指していた。以後は `#### FR-NNN:` 見出しを正として再生成し、**照合を機械検査に加えた**
- **「取りこぼし 0 件」を行数の話で報告していた** — 42 行あることを確認して「全数対応」と書いたが、**名称がずれていれば対応先の妥当性も検証できない**
- **自分の是正が新しい穴を開けた** — 1 周目に「専用ポリシーは作らない」と書いたとき**バイパス手段を与えないままポリシーだけ取り上げ**、3 周目の P1-1 の原因になった。3 周目の `BYPASSRLS` 導入は 4 周目の P1-4(関数 ACL)の原因になった

### クローズ処理

- 計画書 `status: active → in-review`
- 台帳へ **H-78 を新設**(局所編集を重ねる是正は収束しない)+ **`## 候補` の 1 件を `H-79` へ昇格**
  (同種の欠陥を全経路へ適用せずに是正する — **昇格条件「同型の適用漏れが別タスクでも発生した場合」を満たした**:
  Phase 4-5 の `-z` 適用漏れ + 本タスクの 3 周連続)+ 変更履歴 1 行(**版は上げない**)
- `docs/README.md` の台帳行を 2026-08-23 へ
- **`docs/README.md` の開発ハーネス設計書の行は反映なし** — 当初は現行化すると宣言していたが、
  **並行タスク(Phase 4-5・PR #24)が先にマージされ同じ行を同じ日付へ更新した**ため差分から消えた。
  **H-77(並行ブランチの台帳追記で衝突する)の別の現れ方**であり、宣言を是正した

### develop 取り込みで競合 2 件を解消した

PR #24 のマージで develop が進み **CONFLICTING** になったため取り込んだ(`25e248a`)。競合は
`docs/README.md` の設計書の行(**develop 側を採用** — 本ブランチ側は日付が古い)と
台帳の変更履歴表(**両方を保持** — 異なる追記)。**マージが内容を落としていないことを件数で確認**した
(H-* 76 → 77 件・`## 候補` 14 → 16 件)。
