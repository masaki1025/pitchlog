---
date: 2026-09-01
topic: ADR-003 D-1 の改訂 — 数値表示 primitive の追加
branch: feature/adr003-display-primitives
---

# 作業ログ: 2026-09-01 ADR-003 D-1 の改訂 — 数値表示 primitive の追加

## やったこと

## 決定

## 未決・次の一歩

## 停止(2026-09-01)— 人間の判断で実装優先へ切り替え

### 経緯

**計画レビュー(敵対)2 周。1 周目 P0 2 件 / P1 14 件 → 2 周目 P0 2 件 / P1 5 件 / P2 2 件。**
**指摘は 16 → 9 件へ減り収束傾向にあったが、射程が周ごとに広がった。**

| 時点 | 射程 |
| --- | --- |
| 起票時 | **`ADR-003` D-1 の射程に数値表示 primitive を足す(5pt)** |
| 1 周目後(人間の裁定 = 甲) | + **要件書の改訂**(付録A へ書式 binding)+ **母集合と派生資産の追随**(`H-85` の連鎖) |
| 2 周目後 | + **ADR の証跡モデルの変更**(`directTargets[]` を単数参照から順序付き複数参照へ)+ 確定ゲート 2 本 |

### 2 周目の P0 2 件(未反映 — 再開時に対応する)

| # | 内容 |
| --- | --- |
| 1 | **ステップ順序が機構的に成立しない** — ステップ 2 で母集合を追随しても、**確定ゲートで要件書の版・変更履歴・status が変わるため blob digest が必ず古くなる**。かつ**正本を編集した後で `approved → in-review` とする順序は 7.3 の状態遷移と逆**。**是正案**: ①両正本を先に `in-review` へ ②改訂と敵対レビューを完了 ③人間承認後に版・変更履歴・索引を確定したコミットを作る ④**その最終コミットの要件書 blob に対して母集合を追随** ⑤全 checker green。**あわせて甲-3 は「非推奨」ではなく選択不能**(P0 7 件を残した母集合を再封印するため) |
| 2 | **`(β)` の証跡モデルに収まらない** — SQL target は構造化数値まで / Python target は DB 集計経路を通らない / **製品経路の `SQL → Python表示` 全体と一致する直接 target がない**。**是正案**: `(β)①〜⑤` の direct target を「**生成 SQL → 型付き受け口 → 生成 formatter までを実行する 1 つの複合 target**」として D-11 で再定義し、**`directTargets[]` を単数参照から順序付き複数参照へ変更**する |

**1 周目 P0 の判定**: P0-1(完成表示値が確定しない)= **解消** / P0-2(`(β)` の生成先)= **部分的**。

### 人間の判断(2026-09-01・山田正輝)

**「実装を止めているものを止める」** — 設計への投入が過大で、**製品コードが未着手のまま**であることを異常と判断。
**TSK-278 を停止**し、**実装を解除する最小の 1 箇所**へ切り替える。

### 停止時の状態(計画書 §4-(7) 相当の手順)

| # | | 状態 |
| --- | --- | --- |
| 1 | 実装 | **1 行も着手していない**(計画未承認) |
| 2 | 証跡 | `research.md`(調査 3 本 + 原典確認)・`plan.md`(2 周目未反映)・本 worklog。**`ADR-003` と要件書に差分ゼロ** |
| 3 | レビュー全文 | `rev278-full.txt` / `rev278b-full.txt`(スクラッチパッド。再開時に参照) |
| 4 | Notion | **`保留中`**(外部要因待ちではなく**人間の優先度判断による停止**のため `ブロック中` ではない — `.claude/notion-map.json` の `status_options`) |

### 再開の条件

**TSK-235 の再開が必要になった時点。**本タスクは依然 TSK-235 のブロッカである。
再開時は **2 周目の P0 2 件を反映し、敵対レビューを最初から再実施する**。

### 測定して分かったこと(本タスクの副産物 — 実装解除の経路)

**「実装できる設計が実在する」ことを実測した。**

| 事実 | 典拠 |
| --- | --- |
| **approved 設計が決めている FR は 10 件**(`FR-006〜013・015・040` = 記録系) | `docs/design/sync-protocol.md:1820-2049`「11-3. 要件の全数割り当て」 |
| 決めているのは **`docs/design/sync-protocol.md`**(**approved・2,434 行**) | 同書 frontmatter |
| **クライアント側は完全に決まっている** — 4 章(イベント契約の骨格)/ 7-2(キューの状態遷移)/ 7-4(クライアント側の規律)/ 7-6(undo の同期上の表現)/ 7-7(ローカル書き出しと復旧後取り込み) | 同書の節見出し |
| **サーバー側スキーマは TSK-250 へ送られている**が、**クライアント側は送られていない** | 同書 11-2「データモデル影響差分(**TSK-250 へ渡すもの**)」 |
| **設計書に `CREATE TABLE` も DDL も 0 件** | 実測 |
| **`NFR-018` の対象計算の列挙にキューの状態遷移は入っていない** | 要件書 `:883`(状況判定・座標変換・捕球選手推定・成績集計の前処理・終了判定) |
| → **ベクタ(TSK-236/237)にも検査基盤(TSK-235)にも依存しない** | 上記より |

**唯一の構造的ブロッカ**: **設計書 13 章の Phase 表に FR を実装するフェーズが存在しない**
(Phase 4 = 骨格 / Phase 5 = 拡張 で終わり。柱書は「原則 各 Phase = 1 PR、この順に develop へ」)。
→ **製品機能を実装する規範上の置き場が無い。**

**Claude の誤りの訂正**: 「製品コードのタスクが 0 件だから作れない」と説明したが、
正確には**「置き場が無いから起票されていない」**であり、**設計は既にある**。

## 再開(2026-09-02・PO 判断)

- **再開条件が成立**: 「契約先行 + フロント表示系並行」の路線決定(2026-09-02)により TSK-235 の再開が必要になった。Notion を `保留中 → 進行中` へ戻しコメントで記録
- なお停止時の構造ブロッカ「13 章に製品実装の置き場が無い」は、**設計書 v1.13(TSK-279・2026-09-02 approved)で解消済み**(製品実装は Phase の対象外・着手可否は 6.1 の計画書ゲート)
- **2 周目 P0×2 を反映(採否記録 — 全件採用)**:
  1. **P0-1(ステップ順序が機構的に成立しない)** → 採用: §1 に「ステップ順序の機構的制約」を新設(in-review 化が先 → 改訂・敵対レビュー → 人間承認・確定コミット → **その最終 blob に対して母集合追随** → 全 checker green)。ステップ表を 10 段へ再構成(母集合追随を最終ステップへ移動・確定ゲートを表内ステップ 9 へ)。**甲-3 は「非推奨」から「選択不能」へ**(裁定は甲-1/甲-2 の 2 択)
  2. **P0-2(`(β)` が証跡モデルに収まらない)** → 採用: §4-(3) に複合 target の規定を追記(`(β)`①〜⑤ の direct target = 「生成 SQL → 型付き受け口 → 生成 formatter」の 1 複合 target・D-11 で再定義・**`directTargets[]` を順序付き複数参照へ変更**)。ステップ 2/5/6・DoD へ反映
- スクラッチパッドのレビュー全文(`rev278-full.txt`/`rev278b-full.txt`)は消失していたが、**worklog 本記録の P0 要旨と是正案で反映は完結**(1 周目 P0 は反映済みだったため影響なし)
- 停止時の取り決めどおり、この反映を 2 周目の反映として計画レビュー周回を 2 へ更新し、**敵対レビューを最初から(全文)再実施**する

### 再開後 1 周目(全文)— 否決 P0×6・P1×5(採否記録: 全件採用・不採用 0)

1. P0 **要件書 v2.5 が TSK-267 と競合** → 採用: 3 節から「v2.4 → v2.5」を除去し「番号は承認時に PO 裁定(先行関係 or 別版・同一版番号を別内容で確定しない)」へ。手動確認 #7 も競合裁定込みへ
2. P0 **ステップ 2 の受入が未実施のステップ 3 に依存** → 採用: witness 確認をステップ 3 の合格条件へ移動(ステップ 2 は骨子の整合で単独完結)
3. P0 **ステップ 5 の実施内容と機械受入の不一致 + D-11 重複** → 採用: 受入の差分範囲を実施内容(generated[]/directTargets[]/変異対象)へ拡張・D-11 の定義はステップ 6 に一本化(5 は参照のみ)
4. P0 **生成 formatter が D-7 の property layer から脱落** → 採用: スコープ 6・3 節・ステップ 6・DoD へ D-7 を追加((β) 比較を最終表示結果まで拡張)
5. P0 **複合 target の証跡スキーマ未定義** → 採用: §4-(3) に指針(1 つの directTargets[] 要素・順序付き構成生成物リスト・各生成物 hash・要素を 3 つに分けない — 証跡直積を崩さない)を明記
6. P0 **甲-2 で未完了のままステップ 10 を完了できる** → 採用: 甲-2 時は TSK-270 側の追随完了確認を /task-done 前に必須化(DoD の H-85 解消は当該完了で充足 — 引継ぎ記録だけで完了にしない)
7. P1 **符号規則の欠落** → 採用: binding 必須列に符号(負値の文字と適用対象)を追加(3 節・§4-(5)#9・ステップ 3・DoD)
8. P1 **甲の選択の確定記録なし + 甲-3 残存** → 採用: 裁定を §1 と worklog へ記録してから承認とする要件を明記・影響正本表と手動確認表から甲-3 を除去
9. P1 **設計書への影響あり分岐にゲートなし** → 採用: 設計書は「確認のみ・変更は本タスクで行わない」へ(波及ありは 7.6-3 判定 + 別タスク起票 + 計画改訂 — TSK-308 A-1 と同型)
10. P1 **意味検査の機械誤分類** → 採用: DoD に判定対応の注記(意味条件はすべて判定者 山田正輝・機械条件の正は 6 節コマンド)・ステップ 5 の一意性判定を [手動] 化
11. P1 **参照 47 の固定値が完全性条件** → 採用: ステップ 8 開始時の再計測値を母集合に(計測コマンドと件数を worklog 記録・固定値を使わない)

### 再開後 2 周目(差分)— 否決 P0×2・P1×4(採否記録: 全件採用・不採用 0)

1. P0 **ステップ 10 が 1 ステップ = 1 コミットとして成立しない** → 採用: 甲-1 = `codex_run.py implement` 経由で本タスク内実施(文書タスクの /implement 不使用の例外として明記)/ 甲-2 = 本ステップのコミットを「引き渡し + 完了確認の記録コミット」と定義(ステップ 9 の SHA・blob digest・コメント URL を worklog へ・両 PR は本タスク先行)
2. P0 **D-7 拡張対象が閉じていない** → 採用: 複合 target・D-7 拡張を「表示 primitive を持つ `(β)①〜⑤`」に全箇所で限定(⑥〜⑧ は現行のまま)。比較相手 = 同一宣言モデルから表示まで生成した参照実装の最終表示文字列(design.md で確定)
3. P1 **ステップ 7 の受入が「変更しない」宣言と矛盾** → 採用: 受入を「波及ありなら別タスク起票の記録があり設計書に差分が無い」へ
4. P1 **primitive 4 種の確定順序矛盾** → 採用: ステップ 2 は「候補 4 種(上限)」・最終決定はステップ 3 の binding 後(design.md の追随更新は feature 文書として差分制限外)
5. P1 **期間ラベル二択とステップ 3 差分境界の不両立** → 採用: 語彙側選択時は REQ:411 本文改訂もステップ 3 の差分に含める
6. P1 **判定対応の不完全(手動 #3 の D-7 脱落・機械条件の判定器未接続)** → 採用: 手動 #3 へ D-7 追加・6 節に「機械条件の判定方法」表を新設(節限定差分・同一コミット・行番号参照 0 件・母集合計測・文言存在の 5 型)

### 再開後 3 周目(差分)— 否決 P0×1・P1×3(採否記録: 全件採用・不採用 0)

1. P0 **甲-2 のステップ 10 が依然実行不能**(完了確認は本 PR マージ後にしか成立しない)→ 採用: 甲-2 のコミットは引き渡し記録のみで閉じ、**完了確認は /task-done の前提条件として Notion 相互コメントへ記録**(worklog はマージ後に編集しない)
2. P1 **ステップ 4 が無条件に「4 種」を反映する契約** → 採用: 「ステップ 3 で最終決定した集合(上限 4・null 代替が既存拡張で足りれば 3)」へ
3. P1 **ステップ 7 が計画改訂へ戻る条件を落としている** → 採用: 規範変更の波及時は逸脱発動の記録と以降ステップ停止を受入条件へ
4. P1 **判定器接続の残り**(frontmatter 値・存在参照・索引一致・digest 整合・`--stat` の証明力不足)→ 採用: 判定方法表へ 4 型を追加し「同一コミット」型を差分本体の確認へ強化

### 再開後 4 周目(差分)— 否決 P0×1(採否記録: 採用)

1. P0 **ステップ 10 作業欄の「引き渡しと完了確認の記録コミット」文言が合格条件と矛盾**(前周反映の残存)→ 採用: 作業欄を「引き渡しの記録コミットのみ(完了確認はコミットに含めない)」へ是正

### 再開後 5 周目(収束確認)— 承認可・指摘なし → 収束

- 総括: 再開後レビュー 5 周(全文 1 + 差分 4)・指摘 22 件(P0×9・P1×13)**全件採用・不採用 0**。停止前と合わせ計画レビュー周回 = 6

### 計画承認(2026-09-03・山田正輝)

- **承認: 済** — frontmatter 更新・承認コミット
- **裁定① 甲-1 を採用**: TSK-270 の母集合修正(P0 7 件)が先。完了がステップ 10 の実施前提(未完ならステップ 9 まで進めて待つ)。§1 へ記録
- **裁定② 見積 13pt へ再設定**(起票時 5pt は甲への拡大で過小)。Notion の見積ポイントを更新
