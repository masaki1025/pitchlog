---
date: 2026-08-19
topic: Phase 4 監査 PR — 予約レコード seq001 + 索引カバレッジ除外規則
branch: feature/nfr021-reservation-audit
---

# 作業ログ: 2026-08-19 Phase 4 監査 PR — 予約レコード seq001 + 索引カバレッジ除外規則

## やったこと

- **/investigate**: 調査サブエージェント 3 本を並列実行(要件突合 = spec-checker / 決定経緯 = decision-tracer / 実装面の実査 = Explore)。legacy-analyst は題材(索引検査の実装・受入証跡の採番)に旧システムの事実が関与しないため外した。Web 調査(/research)は**不要と判断** — 外部技術ではなく自リポジトリの正本と自作スクリプトの契約に閉じるため。結果を [research.md](../features/nfr021-reservation-audit/research.md) へ統合

- **自分の実測 2 件**(scratchpad の複製リポジトリで実行・本リポジトリは無変更):
  1. 除外規則なしで正規形の予約 1 枚を置くと `check_docs_status.py` が `docs/README.md の正本一覧に載っていない` で **exit 1**。予約レコードと除外規則を同一 PR で運ぶ必然性(設計書 10.1 の循環回避)が実測で裏付いた
  2. 接頭辞を 1 行足す実装では exit 0 になるが、これは**正本 4 件も索引カバレッジから外れる**という禁止された副作用込みの exit 0(README の「接頭辞単位にしてはならない」に違反)

- **/task-start**: Notion を検索して**既存タスク TSK-243 を発見**(4-4 のクローズ処理で起票済み・担当者/プロジェクト/優先度 高/見積 3 が設定済み)。新規起票はせず既存タスクを使用 → `feature/nfr021-reservation-audit` ブランチ + worktree 作成(`origin/develop` = `de9670f` 起点)→ 計画書雛形・research.md・本 worklog 作成

## 決定

- **fail-closed の対象は `.md` に限る** — README:117(規範)が「未知の `.md`・サブディレクトリ配下の `.md`」と限定しており、同 :123 の受入条件が「ツリー項目」と拡張子の限定を落としているのは言い換えの緩さ。拡張子を問わない fail-closed は README:42・150 が **4-5 の検証器・統合時検査**に帰属させており、docs-lint の走査は `docs/**/*.md` に限られる(`scripts/check_docs_status.py:391`)。docs-lint に非 `.md` の検査を課す規定は正本に存在しない
- **統合時の受入検査 4 項目(完全性 / 命名文法 / スキーマ適合 / 孤児・多重対応の拒否)はこの PR の責務ではない** — 全部 4-5 の受入契約(README:132-134・162)
- **既存の接頭辞機構は流用しない** — `is_excluded` はパス構成要素の前方一致しか表現できず、`EXCLUDED_PREFIXES` 側への追加は `docs/features/*/plan.md` の検査まで無効化する。直下判定 + 正規形の述語を新設する
- **順序逸脱(4-4 が監査 PR より先にマージ)は規範改訂せず記録に留める**(PO 判断)。台帳の分類は**計画レビュー 1 周目 P2-2 で `## 候補` (1) へ是正**した — 候補の受け皿は 2 種類あり、(1)「まだ傾向として確定していない単発事象」/ (2)「制御目的の典拠が確認できず優先度を判定できないもの」。本件は (2) には当たらないが (1) に当たる(逸脱は 1 件で、恒常的なプロセス欠陥も再発も示せない)。**調査時に `H-*` と結論したのは (1) の受け皿を見落としたため**。昇格条件を併記する
- **`operator` は「山田正輝」**(要件書 8 章の判定者 = PO。設計書 10.1 が採番・発行主体を判定者に固定しており、Claude は起草止まり)
- **台帳へ追記する項目は 2 件**(PO 判断): ① 順序逸脱 ② `git_guard` の誤検知
- **H-70 は閉じない** — 同じ `check_docs_status.py` を触るが、台帳が対応案 (c) で「本タスクの計画スコープ外のため別タスク」と明示している

## 未決・次の一歩

- 次は **/plan**(計画書 6 節記入 → `codex_run.py review normal` → 人間承認)。実装ステップは 2 本(① 除外規則 + 正例・負例テスト ② 予約レコード seq001)。**順序は固定** — 逆にすると各ステップ末尾の `/check` が落ちる
- **予約レコードの実値(`attempt_id` の発行時刻・`started_at`)は書き込み時の UTC 実測値**を使う。`started_at` は**排他区間の開始**であり、予約は改変禁止なので取り違えると訂正不能。ファイル名と `started_at` は `YYYY-MM-DDTHHMMSSZ`(日付部にハイフン)・`attempt_id` 内は `YYYYMMDDTHHMMSSZ`(ハイフンなし)で**書式が逆**
- **`git_guard` の誤検知を本作業中に踏んだ** — 調査メモをヒアドキュメントで書き出す `cat > ... <<EOF` が、**本文に Git のサブコマンド名を含むというだけで**「git のグローバルオプションまたはサブコマンドを解析できませんでした。安全側で遮断します」として遮断された。git コマンドの実行ではない。4-4 で `secret_guard` に起きた事象(コマンド文字列に台帳のファイル名を含むだけで遮断)と同型。**既存 `H-51`(ヒアドキュメント本文が行分割され、本文中の文字列がコマンドとして遮断される)と同根**であり、**新規 `H-*` は起こさず `再発` を 5 → 6 件へ更新して事象 ⑥ を追記する**(計画レビュー 1 周目 P1-2 で是正 — 別項目にすると再発数と対応先が分裂する)。H-51 の追跡優先度は `通常`(遮断が外れる方向の実害は無いため)
- **この PR ではディレクトリ新設を行わない** — 設計書 10.1・13 章・README はいずれも `docs/ops/nfr021-acceptance/` の新設を監査 PR に割り当てているが、4-4 のマージで既に存在する(帰属の消滅)。計画書に明示する

## 計画レビュー 1 周目(Codex `review normal` — 重さ分類「通常」)

**P0 なし・P1×3・P2×2 を全件採用**(不採用 0 件)。**うち 2 件は自分の判断の誤りを正すもの**。

| 指摘 | 内容 | 反映 |
| --- | --- | --- |
| P1-1 | 正規形の仕様とテストが「直下限定」と「閉じた命名文法」を十分に保証していない。サブディレクトリ内の**正規形の名前**を拒否することが固定されておらず、ファイル名だけで除外する実装でも通る。ゲート組(`phase4-v1.2.3`・`release-phase4`)・実在しない時刻(`2026-99-99T246099Z`)の負例と、`seq1000` の正例が無い(`\d{3}` 固定長だと誤拒否する) | 4 節へ `fullmatch`・実在 UTC 時刻としての検証・桁数を固定しない旨を追記。5 節 DoD と 6 節へ負例 5 種・正例 1 種を追加 |
| P1-2 | `git_guard` の誤検知は**既存 H-51 と重複**。新規 `H-*` にすると再発数と対応先が分裂する | 「H-51 の `再発` を 5 → 6 件へ更新」に変更 |
| P1-3 | `docs/README.md` の索引更新を無条件に宣言すると、**同日クローズでは差分が出ず** `status: in-review` で `check_plan_docs_sync.py` が「反映すると宣言したが差分にない」で exit 1 になる(台帳行の最終更新日は既に 2026-08-19) | 3 節から索引行を外し、「クローズ日が変わる場合のみ宣言する」条件を明記 |
| P2-1 | 非 `.md` の扱いの説明が正本と矛盾。README の**閉じた命名文法は拡張子を問わず**未知項目を不正とし、`.md` 限定なのは**索引カバレッジ検査に限る** | 2 節の文言を書き換え(結論は変えない) |
| P2-2 | 順序逸脱を `H-*` と断定する根拠が不足。台帳の `## 候補` には「傾向として確定していない単発事象」も含まれる | `## 候補` (1) へ変更し昇格条件を併記 |

**自分の側の誤り 2 件**: ① 台帳の `## 候補` の受け皿を (2) だけだと読み、(1)「単発事象」を見落としていた(調査で `H-*` と結論した根拠が不十分だった)② `git_guard` の誤検知を新規事象として扱い、既存 H-51 を突合していなかった(調査エージェントの決定経緯レビューも H-51 を挙げていない)。**いずれも台帳の全数確認をしていれば防げた** — 4-4 の教訓「是正は指摘の字面ではなく全数確認で閉じる」と同型の見落ち。

## 計画レビュー 2 周目(収束確認 → 指摘があったため周回として計上)

1 周目 5 件のうち **P1-3 だけが部分反映**と判定された(他 4 件は反映済み・新規指摘なし)。

- **事象**: 3 節の表から `docs/README.md` の行は外したが、**説明文中のコード span が反映宣言として抽出される**。`scripts/check_plan_docs_sync.py:439-481` の `extract_declarations` は、3 節の各行からコード span と Markdown リンクのリポジトリ相対 `.md` パスを**全件**抽出し、**同じ行に「反映なし」の語が無ければ「反映あり」**として扱う。差分が無いまま `status: in-review` になると exit 1
- **是正**: 当該行を「`docs/README.md` は **反映なし**」の形に変え、条件付き現行化の説明は**コード span を含まない別行**へ分離した
- **機械で裏取りした**(目視で終わらせない): `extract_declarations` を直接呼んで抽出結果を確認 → 反映あり = `docs/development/harness-evaluation.md` / 反映なし = `docs/README.md`。意図と一致
- **これは台帳 H-64・H-67 と同系の摩擦**(計画書 3 節の入力解釈の緩さ)。research.md 5 節で「踏む可能性が高い」と予告していた箇所を実際に踏んだ

## 計画レビュー 3 周目(新規 P1×1 — 周回として計上)

**新規 P1: 予約レコードが `/pr` の機械突合で未宣言となり PR 作成が停止する。**

- **根因(自分で機構を確認)**: `scripts/check_plan_docs_sync.py` の `filesystem_paths` は docs 配下の非除外 Markdown を**全件**突合対象に入れ、`EXCLUDED_PREFIXES` に docs/ops 配下は含まれない。一方 `.claude/skills/pr/SKILL.md` は突合対象を「索引に掲載されている正本 / 新設する正本の配置先 / 索引自身」と定めている。**実装がスキルの規定より広い**
- **是正**: スキルが定める経路(3 節の別枠「正本体系外だが同一 PR で更新するもの」)で宣言する。`affected_section_lines` の終端は `^##` のみなので、`###` の別枠は 3 節の内側に留まり抽出される(確認済み)。宣言は**実在確認を伴わない**ため、ステップ 2 でファイルと宣言を同一コミットに入れれば足りる(確認済み)
- **是正の最中に自分でも同じ穴を踏んだ**: 追記した説明文が「反映」+「なし」の語と `.md` のコード span を同一行に含み、**`.claude/skills/pr/SKILL.md` と `docs/**/*.md` が非該当宣言として抽出されていた**。`extract_declarations` を直接呼んで検出し、コード span を平文へ落として解消(再確認: 反映あり = 台帳のみ / 非該当 = 索引のみ)
- **台帳へ 3 件目を起票する**: 突合器の対象集合とスキルの規定の齟齬。docs/ops 配下に正本でない文書が入る設計になったのに除外集合が更新されていない。**① 典拠あり + ② 誤検知の実証あり**で `高` 判定になり得るため、**分類・優先度と follow-up 起票の是非は `/pr` のクローズ処理で PO の判断を仰ぐ**

**教訓**: 正本の規範だけでなく、**機構の実装を読んで実際に呼んで確かめる**まで「反映済み」と言えない。3 周とも「文書としては正しいが機構が別の判定をする」型の指摘だった。

## 計画レビュー 4 周目(新規 P1×1 — 周回として計上)

3 周目 P1 の機械的な是正は**完全**と判定された(3 節の抽出対象 19 行を全行確認・`###` 別枠が 3 節内に含まれること・ステップ 2 完了時点で `check_sync` が違反なしになることをレビュー側でも実測)。

**新規 P1: 台帳の記録件数が節間で矛盾**。2 節・3 節を 3 件へ更新したのに 5 節 DoD が 2 件のままで、**3 件目を起票せずに DoD 達成と判断できてしまう**状態だった。DoD を 3 件へ同期し、3 件目について PO が分類・追跡優先度・follow-up 起票または見送りを決定して記録することを明記した。

**自分の反映漏れ**。指摘を受けて増やした項目が、同じ計画書の別の節に伝播していなかった — 4-4 の「是正は指摘の字面ではなく全数確認で閉じる」を今回も守れていない。**節をまたぐ数値・件数は、変更したら必ず全節を `grep` して同期する**。

## 計画レビュー 5 周目(新規 P1×1 — 周回として計上)

4 周目 P1(件数の節間矛盾)の是正は**完全**と判定。節間照合(除外対象・対象ファイル・予約 1 件・正負例・スコープ境界)も整合。

**新規 P1: ステップ 1 の合格条件が実行環境と噛み合っていない**。ルート `pyproject.toml` の dev 依存は **pytest のみ**で、**ruff / ty は backend 専用**(`.claude/skills/check/SKILL.md` が層別に定義)。ルートで `uv run ruff check` / `uv run ty check` は満たせず、backend で実行しても**変更対象であるルート側スクリプトを検証しない**。加えて `ruff format` は検証ではなく変更操作。

- **原因**: AGENTS.md「コマンド(実装フェーズで有効)」の backend 用コマンドをそのまま流用した。**層(harness / backend / frontend)の区別を確認していなかった**
- **是正**: ステップ 1 の合格条件を `uv run pytest tests/`・`scripts/check_docs_status.py` exit 0・実リポジトリ統合テストの 3 点に揃え、6 節に「ルート側の品質ゲートは pytest だけ / 静的検査の導入はスコープ外」と明記
- **副産物の観測**: ルート側の `scripts/` には**リンタも型検査も未導入**。ハーネス自身のコードが静的検査の外にあるという構造的な観測だが、本 PR では扱わない(スコープ外)

## 計画レビュー 6 周目(収束確認 — 指摘なしのため周回に数えない)

**新規指摘なし。是正判定「完全」。着手可**(規約上の着手は人間承認後)。レビュー側で層別定義(`.claude/skills/check/SKILL.md`)・ルート依存(`pyproject.toml`)・CI 設定まで確認し、DoD の `/check` 全グリーンは**各層の既存ゲートを指す集約条件**であってルートで ruff / ty を実行する指定ではないことも確認された。

### 計画レビューの総括(5 周 + 収束確認 1 周)

指摘数の推移: **6(P0 なし・P1×3・P2×2)→ 1 → 1 → 1 → 1 → 0**。**不採用 0 件**。

| 周 | 指摘の型 | 原因の所在 |
| --- | --- | --- |
| 1 | 正規形の仕様不足 / 台帳の重複起票 / 索引宣言の空振り / 説明の正本との不一致 / 台帳分類の根拠不足 | **自分**(台帳の全数確認をせず、調査エージェントの報告と推論で結論した) |
| 2 | 索引宣言が**説明文のコード span から**抽出される(部分反映) | **自分**(文書として直しただけで機構の抽出規則を確かめていなかった) |
| 3 | 予約レコードが `/pr` の突合で未宣言となり中断する | **機構とスキルの齟齬**(実装がスキルの規定より広い)+ 是正中に自分も同じ穴を踏んだ |
| 4 | 台帳の記録件数が節間で矛盾 | **自分**(増やした項目を別の節へ伝播していない) |
| 5 | ステップ 1 の合格条件にルート未導入の ruff / ty を要求 | **自分**(AGENTS.md の backend 用コマンドを層の区別を確認せず流用) |

**この 5 周から取り出せる規律**:

1. **正本の規範を読むだけでは足りない** — 機構(スクリプト)を実際に呼んで、意図した判定になることを確かめる。3 周分がこの型だった
2. **件数・対象・スコープ境界を変えたら、計画書の全節を `grep` して同期する**(4-4 の教訓の再発)
3. **層(harness / backend / frontend)の区別を、コマンドを書く前に確認する**
4. **台帳へ起票する前に既存項目の全数確認**をする(H-51 との重複・`## 候補` の受け皿 2 種)
