---
date: 2026-09-07
topic: 88 列の行き先と旧資料矛盾の裁定(移行変換の確定 — TSK-250 の先行 C)
branch: feature/legacy-column-mapping
---

# 作業ログ: 2026-09-07 88 列の行き先と旧資料矛盾の裁定(移行変換の確定 — TSK-250 の先行 C)

## やったこと

### /task-start

- Notion タスク **TSK-271**(タスクID 271・2026-08-31 の人間裁定で TSK-250 の計画レビューから切り出された先行タスク C)を取得。
  担当者・プロジェクトリレーション・DoD は起票時に設定済みのため**新規起票はしていない**
- worktree `../pitchlog-worktrees/feature-legacy-column-mapping`(ブランチ `feature/legacy-column-mapping`・
  起点 `origin/develop` = `fdbe18c`)を作成
- 計画書雛形 `docs/features/legacy-column-mapping/plan.md` を作成(重さ分類 = **コア領域(データ移行)**)
- Notion ステータスを `未着手` → `進行中` へ遷移し、ブランチ名をタスクへコメント

### /investigate

- 調査サブエージェント **3 本を並列**(legacy-analyst / spec-checker / decision-tracer)で投げ、
  結果を `docs/features/legacy-column-mapping/research.md`(802 行)へ統合した
- **エージェント間の食い違いを原典で裁定した** — 座標の視点について legacy-analyst は
  「`docs/legacy/` に記述 0 件」、spec-checker は「改善台帳 I-17 が事実認定済み」と報告。
  参照範囲の違いで**両方とも正しい**と確認し、規範は要件書 付録 B-2/B-3/B-5 が既決と裁定
- 自分の初稿の誤り 1 件を是正 — 「88 列の正は data-layer.md」の典拠を AGENTS.md と書いていたが、
  **AGENTS.md には無い**(正は `docs/README.md:35`・要件書 付録 D-1・`legacy/research/README.md:36`)

### /plan

- Explore 2 本(コア領域 plan の前例 / JSON 契約の機械検査基盤)を並列で投げ、前例の型を抽出した
- `plan.md`(固定フォーマット・192 行)と **`design.md`(224 行 — JSON スキーマ・ID 体系・検査 ID `C01`〜`C15`/`E01`)** を作成。
  plan は契約(機構が読む状態とステップ表)だけを持たせ、詳細は design.md へ分離(設計書 6.1 の 3 ファイル役割分担)
- **人間の裁定 4 件を取得**(`R-1`〜`R-4` — plan.md 冒頭の裁定表)
- 敵対レビュー(`codex_run.py review adversarial` = sol xhigh)を **3 周**実施

#### 敵対レビューの採否記録

| 周 | 指摘 | 採否 | 主な反映 |
| --- | --- | --- | --- |
| 1 | **P0×8・P1×15** | **全件採用・不採用 0 件** | 独立 oracle の新設(`P0-16` = 全検査を通る破損 JSON を構成できた)/ `semanticTarget`・`rawAuditTarget`・`compatOutput` の 3 構造(`P0-1`・`P0-3`)/ ADR-003 D-12 を確定ゲートで改訂(`P0-6`)/ 独立レビュー台帳(`P1-7`)/ 列群縦割り(`P1-10`・`P1-13`)/ 自由文を判定に使わない(`P1-15`)/ digest 3 種 + リビジョン照合 + float 禁止(`P1-18`)/ CI 3 段固定(`P1-19`)/ `schemaVersion` へ(`P0-14a`)/ キー名の一意化(`P0-14b`) |
| 2 | **P0×7・P1×10** | **全件採用・不採用 0 件** | **`P1-10` を受けて PO 裁定で 4 単位へ分割**(下記「決定」)/ 論理概念 ID 化(`P0-3`)/ 要件矛盾台帳を独立母集合にし解消状態を検査器が導出(`P0-4`)/ 変換規則を整数の有理アフィン式で実行可能に(`P0-5`)/ fragment + compose(`P1-2`)/ 検査器を列群作成前に全実装(`P1-3`)/ レビュー台帳のセル母集合を schema から機械生成(`P1-4`)/ `[機械]` の再分類(`P1-5`)/ decision-bearing leaf に限定した変異(`P1-6`)/ `88` と `0..87` は正本由来の不変条件(`P1-7`)/ 最終レビューをステップ外ゲートへ(`P1-8`) |
| 3 | **P0×8・P1×11** | **全件採用** | **契約への縮小**(1〜2 周目の是正が要件先取りを生んでいた)。行き先・互換出力・論理概念 ID・関係母集合・`scale`/丸め・fragment 機構を落とし、キー名を snake_case へ戻す |
| 4 | **P0×5・P1×7** | **全件採用** | **写像 JSON を TSK-250 へ移す**(受け渡し契約が内部矛盾)。`head_tree_hash` を廃止・要件矛盾の解消判定を廃止・`C13` を手動確認へ |
| 5 | **P0×6・P1×5・P2×1** | **全件採用** | **周回を打ち切り調査タスクへ**(PO 裁定)。**判定材料が承認済み precedence と反するため裁定は権限外**と判明 |
| 6 | **P0×7・P1×4・P2×1** | **全件採用** | 局所欠陥の是正。**私の grep が `\|` をリテラル解釈して false green(0 件)** だったことが実測で判明。**台帳の既存候補の昇格条件が本タスクで成立**していることも判明 |
| 7 | **承認阻害 6 件**(受理範囲を限定) | **全件採用** | blob SHA へ変更 / 直接パス形式の検出追加 / ID の exact-set / **`docs/legacy/` の規範性を限定射程へ**(私の DoD が無限定に「証拠のみ」と書いて precedence と矛盾していた)/ ⑤ を承認前起票へ / Notion DoD 同期の手続きを新設 |
| 8 | **承認阻害 7 件** | **全件採用** | SHA の型を `research_blob_sha` に統一(1 箇所だけ直して代替統制と DoD に残していた)/ ④ に直接パス形式を追加 / ① に uniq 前の行数・③ を判定表の先頭列のみへ / **`DL` 採用時まで改訂を必須化していた矛盾**を条件付きへ(Notion 側も)/ **逐項照合表を実際に作成**(作ったことで Notion 側の未修正が判明)/ ステップ 3 の機械条件を実行可能に / DoD へ 3 項目 |
| 9 | **承認阻害 6 件**(3 件は解消判定) | **全件採用** | **正規表現方言のミスを 2 度**(`HIST` の `\(` が BRE で group・ERE でリテラル → **grep=0 / awk=1** に割れて条件が原理的に満たせなかった。`[(]` へ統一)/ **④ を `.md` 限定から可変拡張子全体へ**(`.claude/core-areas.json:2` 等 **非 .md が 9 件実在**し false green だった。是正前の実測は **222**)/ 1 節に残っていた無条件記述 / **周回数が 7 のまま**(8 周分反映済みだった)/ `RQ-C / RQ-E / RQ-F / RQ-G` の範囲記法が `RQ-D` を 2 タスクへ二重割当 / 照合表が旧文引用のまま「要修正」 |

**1 周目で最も重かった指摘(`P0-16`)**: 私が設計した検査 `C01`〜`C15` は
「全型 `TEXT`・全 target を同一値・全 transform を『そのまま』・任意の 20 列を `documented=false`・
全 evidence を同一の無関係な legacy 行」にして reseal すれば**全部通る**。
座標変換・列名乖離 5 列の意味・型・ラインアップがすべて壊れていても green になる。

**2 周目で判明した自己矛盾**: 1 周目の是正として入れた「独立 oracle」は、
**後段のステップが oracle を同時更新する構造**だったため循環しており、かつ
oracle が比較するのは全項目の**部分集合**にすぎなかった(`C17`「全フィールド一致」は事実でない)。
→ **射程分割によって構造的に解消**(本タスクの成果物が唯一の権威ある内容になる)。

## 決定

- slug は `legacy-column-mapping`。TSK-250 の `data-model-canonical` とは別 feature として扱う
  (本タスクのマージが TSK-250 の開始条件 — 成果は 88 列写像 JSON として受け渡す)

- **`DM-LG-01`〜`LG-16` / `DM-LX-01`〜`LX-06` の ID 対応表はリポジトリに存在しない**(件数だけが台帳にある)。
  → **ID の割り当て自体が本タスクの最初の作業**。件数 16 / 6 は TSK-250 が exact-set で機械検査するため不変
- **座標の視点と左右反転は既決**(要件書 付録 B-2/B-3/B-5)。Notion の DoD が「旧資料だけでは確定できない」と
  する項目は、**答えが旧資料から出るものではない**ため人間裁定に上げない
- 参照経路: `T250R` は off-branch のまま**照合対象として引くだけ**にし、典拠は原典に取った

### /plan で確定した設計判断

- **確定ゲート(/finalize-doc)は通さない** — 設計書 7.2 の置き場表に `contracts/` の行が無く、7.6 の決定表に
  `contracts` の語が 0 件。`contracts/*` 系 3 feature(`pg-authz-verification`・`authz-claims-corpus`・
  `core-area-paths`)はいずれも `確定ゲート周回: 0`。担保は敵対レビュー + PR の人間逐行確認
- **`data-migration.paths` に `contracts/` が 1 件も無いため core-guard が構造的に非発火**(台帳 H-12 の 4 件目)。
  前例に倣い**代替統制 4 点**を 4 節に明文化し、ステップ 13 で paths 登録(6.3 規則⑤)まで行う
- **`check_authz_catalog.py` は改造しない** — `_derived_lock_entries` の `table_specs` に `asset_kind` を足すと
  `tenant-isolation` のコア領域を触る。digest 関数 2 本だけ import して lock は自前実装
- **`ci.yml` は変更しない** — 本物の資産に対する pytest で `harness` ジョブ経由で必須化する
- **ステップコミット件名は `/N` を付けず `(ステップ k)` のみ** — 裁定の分割で総数が変わり得るため(枝番は
  `feature_status.py` が認識しない)
- 人間の裁定: `R-1` 列 63-71 は原本事実として保持 / `R-2` 列 44・86-87 は裁定 + 原本値保持の併用 /
  **`R-3` は撤回**(下記)/ `R-4` 裁定の根拠は JSON 内に典拠ごと保持

### 裁定 `R-3` の撤回(1 周目 `P0-2` / `P0-21a`)

当初「負値座標に `x<=0` を採る」と裁定したが、敵対レビューが 2 つの理由で P0 を返した:

1. 付録 B-5 の規範文は `0`・空・`"0"` 限定 + 範囲外はクランプであり、**`x<=0` を写像に書くのは
   承認済み正本を要件改訂ゲートなしで上書きする**(AGENTS.md 絶対規則 4 違反)
2. **私が裁定を求めたときに提示できていなかった事実** — TSK-250 が写像の digest を凍結した後に
   要件が B-5 側へ確定すると、「再解釈禁止 + digest 照合」により**修正不能**になる

→ **PO 裁定(2026-09-07)で `R-3` を撤回し、要件改訂(分割単位 ①)を先行ブロッカーにした。**

### 射程の分割(**PO 裁定 2026-09-07** — 計画レビュー 2 周目)

2 周目 `P1-10`「独立確定単位が再び一タスクへ混在しており、**TSK-250 を分割した理由を再導入している**」
を受けて 4 単位へ分割。**TSK-271 の射程は ③ に絞る**:

| 単位 | 内容 | 受け皿 |
| --- | --- | --- |
| ① | 座標センチネル条件の要件改訂(`RQ-A`) | **新規起票**・本タスクのステップ 1 の開始条件 |
| ② | ADR-003 D-6 / D-12 の改訂(`legacy-columns/` 領域の追加) | **新規起票**・④ の開始条件 |
| ③ | **88 列の裁定と期待値の確定(seal)** | **TSK-271(本タスク)** |
| ④ | 88 列写像契約の実装(`contracts/`・検査器・CI・paths) | **新規起票**・本タスクのマージ + ② が開始条件 |

**分割が解消した構造的欠陥**: 本タスクの成果物が「唯一の権威ある内容」になるため、
**循環 oracle(2 周目 `P0-1`)と「偽の独立性の主張」(同 `P0-2`)が構造的に消える**。
後続 ④ にとっては本タスクの seal 済み成果が**真に独立した oracle** になる。

### 既存タスクへの合流(重複起票の回避)

Notion を照会し、受け皿になり得る未着手タスクを 4 本特定した:

- **`RQ-B`(責任投手の移行導出)→ TSK-239** 「移行ベクタの作成(付録B-5 座標変換 + FR-038 責任投手・成績帰属の導出)」
- **`RQ-C` / `RQ-E` / `RQ-F` / `RQ-G` → TSK-306** 「要件書 10 章への穴 25 件の反映(**H/E/K/B 定義不在**を含む)」— `LX-04`(列 86-87 末尾 4 集計)に直結
- **`RQ-SIDECAR-KEY` → TSK-214** 「要件書 v2.3 改訂 — 移行契約の更新(カットオーバーと移行対象)」
- **② は TSK-275 に合流させない** — TSK-275 も ADR-003 D-12 を改訂するが、その 4 操作は
  `display_geometry_263_v1.json` の grandfather 例外の卒業であり**別内容**。かつ TSK-275 は
  「画面受入タスクが未起票でありその起票が着手前の前提条件」で止まっており、
  **合流させると ② と ④ が無期限にブロックされる**

### 要件矛盾の `kind` 区別(2 周目 `P0-4` の反映時に自分で見つけた穴)

要件矛盾 8 件をすべて完了ブロッカーにする設計にしていたが、それでは**本タスクが 8 本の要件改訂を待つ**
ことになる。→ **`CONTRADICTION`(承認済み条文どうしが衝突 → どちらを書いても上書き)**と
**`GAP`(条文が沈黙 → 典拠を明示して埋められる)**を分け、`C16` のブロッカー判定を
`CONTRADICTION` に限定した。**ブロッカーは `RQ-A`(= 分割単位 ①)のみ。**
`GAP` にも `fillEvidence[]` の非空 + 逐語照合を課し、「条文がないから何を書いてもよい」を封じた。

### 承認前の外部手続(2026-09-08 実施)

**送り先 7 本を新規起票**(すべて担当者・プロジェクトリレーション・DoD 付き):

| ID | タスク |
| --- | --- |
| **TSK-334** | 88 列と旧資料矛盾の裁定セッション(判定 22 件の採否)— **DoD に二分岐を固定**(`DL` 採用 / 非 `DL` 採用 → 要件改訂の 7.3 確定ゲートとマージを TSK-250 の開始条件へ) |
| **TSK-335** | TSK-250 の計画改訂(受け渡し契約の内部矛盾の解消)— **TSK-250 の開始条件** |
| **TSK-336** | 旧 DB 実データ確認(センチネル記述なし 20 列 + 列 33 の「両」+ FastAPI 期の書き込み) |
| **TSK-337** | 要件書改訂 — 負値座標のセンチネル条件(付録 B-5 のクランプ vs FR-038 の `x<=0`) |
| **TSK-338** | ADR-003 改訂 — D-6 の参照データ契約の列挙と D-12 の領域列挙(`legacy-columns/`) |
| **TSK-339** | 88 列写像契約の `contracts/` 化(schema・lock・検査器・CI 配線・core-areas paths) |
| **TSK-340** | **ハーネス設計書 6.1 改訂 — 計画レビューの終端条件**(台帳の**成立済み昇格条件の実行**) |

**既存 3 本へ引き取り事項を追記**(重複起票を避けた):

- **TSK-239** ← `RQ-B`(責任投手の移行導出と付録 A-2 の非同一。代表集計値に失点系が入った時点で検証が必ず失敗する)
- **TSK-306** ← `RQ-C`・`RQ-E`・`RQ-F`・`RQ-G` の 4 件。**同タスクの既存射程「H/E/K/B 定義不在」は判定 `DM-LX-04`(列 86-87 末尾 4 集計)に直結**することも記録
- **TSK-214** ← `RQ-D`(Won't 廃止列の互換出力値・本体 NULL 表現)・`RQ-SIDECAR-KEY`(移行試合の結合キー)

**TSK-271 の DoD を現行化** — 更新後 URL: <https://app.notion.com/p/3cc93b75e68781dba1f3ec87cf24711e>

#### 計画書 5 節 × Notion DoD の逐項照合(8 周目 `P1-1`)

| 本書 5 節の項目 | Notion 側の対応 | 判定 |
| --- | --- | --- |
| research.md の可変正本参照が安定 ID | 「**可変正本への行番号引用を安定 ID へ貼り替えた**」 | 一致 |
| 判定 22 件の ID が全文形で確定割り当て | 「**判定 22 件の ID 割り当てと原典照合**が全件記録(件数 16 / 6 不変・**全文形**で統一)」 | 一致 |
| 2 節の欠陥 a・b・c の是正 | 「**全 88 列のセンチネルが列単位で記録**され、**確定/未確定が列ごとに明示**」+「センチネルの記述が無い列は 20 列 + **列 33 は「両」登録投手の場合のみ記述なし**」 | 一致 |
| 全 88 列のセンチネルが列単位・確定/未確定の明示 | 同上 | 一致 |
| 旧所有契約の残滓の是正・履歴節への隔離 | (Notion 側は個別項目を持たない — **計画書側のみ**の作業項目) | **計画書のみ**(Notion DoD は成果の性質を記述し、文書内の作業手順は持たない) |
| 「判定であって裁定ではない」の明記 | 「**「本メモは事実の判定であって採否の裁定ではない」**と明記」 | 一致 |
| `DM-LX-03`・`DM-LX-04` の precedence 制約 | 「**`DM-LX-03`(列 44 球種)・`DM-LX-04`(列 86-87 末尾 4 集計)で `data-layer.md` 以外を採用する場合に限り要件書 precedence の改訂が必要**であり、**`data-layer.md` を採用するなら現行正本のままでよい**と明記されている」(**2026-09-08 に Notion 側を修正済み** — 8 周目 `P0-4`) | 一致 |
| `docs/legacy/` の限定射程 | 「**`docs/legacy/` の規範性を限定射程で扱っている**」 | 一致 |
| 移行に効く申し送り(列 56 の二重変換 / JSON 列の固定長禁止) | 「**移行に効く申し送りが記録されている**」(2 件を逐語で列挙) | 一致 |
| 送り先 7 本の起票と実 ID | 「**送り先 7 本を起票**した(TSK-334 / 335 / 336 / 337 / 338 / 339 / 340)」 | 一致 |
| `RQ-*` を TSK-239 / 306 / 214 へ合流 | 「**`RQ-B` を TSK-239 へ・`RQ-C` / `RQ-E` / `RQ-F` / `RQ-G` を TSK-306 へ・`RQ-D`・`RQ-SIDECAR-KEY` を TSK-214 へ合流**させた」 | 一致 |
| 台帳の既存候補へ実測追記 + ⑤ の起票 | 「**運用評価台帳の既存候補へ実測を追記**した。**昇格条件は本タスク(7 周)で成立**したため **TSK-340 を起票済み**」 | 一致 |
| 88 列写像 JSON・裁定台帳・検査器・seal を作っていない | 「**88 列写像 JSON・機械可読な裁定台帳・検査器・seal を作っていない**(射程の縮小)」 | 一致 |
| worklog に全周の採否記録と未解決一覧 | (Notion 側は持たない — **計画書のみ**) | **計画書のみ** |
| `docs/README.md` の現行化 | (同) | **計画書のみ** |
| Notion へ PR URL を記録 | (PR 後手続。Notion 側は `URL` プロパティで受ける) | 一致 |
| 敵対レビュー + 人間の逐行確認 | 「**コア領域として敵対レビュー + 人間の逐行確認**(2 節のセンチネル表・判定 22 件と根拠段落・申し送り全件)を通っている」 | 一致 |
| `research_blob_sha` の記録と最終 HEAD 一致 | (Notion 側は持たない — **計画書のみ**の機械条件) | **計画書のみ** |

**判定の要約**: **一致 14 / 計画書のみ 4 / 要修正 0**(文書内の作業手順・機械条件は Notion DoD に載せる性質ではない)。
`DM-LX-03`・`DM-LX-04` の precedence は **2026-09-08 に Notion 側も条件付きへ修正済み**(9 周目 `P1-4`)。

- **`research_blob_sha`**(4 節 ⑥ の固定書式。**ステップ 4 の最終反映コミット `467d810` 時点の
  `research.md` の blob**。**ステップ 5 以降は `research.md` を変更しない**ため最終 HEAD と一致する):
- research_blob_sha: 59cb50fca6fdbcc045c5604f935c415e6d16b90a

### 計画承認(2026-09-08)

- **承認者**: 山田正輝(PO)/ **承認日**: 2026-09-08
- **計画レビュー周回**: **9**(P0 の推移 8→7→8→5→6→7。**7〜9 周目は受理範囲を 3 種に限定**)
- **計画書**: `docs/features/legacy-column-mapping/plan.md`(ステップ 5 本・コード変更 0 行)
- **9 周目の残余 6 件はすべて是正済み**で、実測で確認した:
  - `P0-1` ④ を可変拡張子全体へ(**非 `.md` の可変正本参照が 9 件実在**し false green だった。負例
    `.claude/core-areas.json:2` で旧版 0 件 → 是正版 1 件を確認。是正前の総数 **222**)
  - `P0-2` 1 節の無条件記述を条件付きへ(残存 0 件)
  - `P1-1` **正規表現方言のミス**(`\(` が BRE で group・ERE でリテラル → **grep=0 / awk=1**)を `[(]` へ統一
  - `P1-2` 周回数を 9 へ・DoD から周数の数値固定を除去
  - `P1-3` `RQ-C〜RQ-G` の範囲記法が `RQ-D` を 2 タスクへ二重割当(plan 1 件 + worklog 4 件を明示列挙へ)
  - `P1-4` 逐項照合表を Notion 修正後の内容へ更新(**一致 14 / 計画書のみ 4 / 要修正 0**)

**9 周を要した原因の総括**(TSK-340 へ渡す材料):

1. **是正が構造を追加し続ける輪** — 1 周目「保存先の構造が薄い」に応えて構造を足すと、
   足した構造が要件先取りとして次の P0 を生んだ。**縮小方向の是正**でしか抜けられなかった
2. **受理範囲の限定が効いた** — 7 周目に 3 種へ絞って初めて指摘が局所化した(台帳の既存候補が
   記録する TSK-269 の 9 周目と同じ収束の仕方)
3. **同型のミスを繰り返した** — 正規表現方言(`\|` → `\(`)・「1 箇所直して他を直し忘れる」が反復。
   **是正後に横断 grep で残存を数える**手順を入れてから収まった

## 実装ステップの記録

### ステップ 1 — 可変正本への行番号引用を安定 ID へ貼り替え(2026-09-08)

**ドキュメントのみの変更なので Claude が直接編集した**(CLAUDE ハーネス「Git 操作・PR・
ドキュメントは Claude の役割」/ 本計画は 0 行のコード変更 — `/implement` は使わない)。

**機械検査の実測**:

| 検査(計画書 4 節) | 是正前 | 是正後 |
| --- | --- | --- |
| ④ 可変正本への行番号引用 | **222** | **0** |
| ├ a) 非 `docs/legacy/` のパス形(出現数) | 41 | 0 |
| ├ b) 記号形 `REQ:`/`T250P:`/`T250R:`/`CAND:`/`improvements:`(出現数) | 129 | 0 |
| └ c) 継続形 `` `:<行>` ``(行数) | 52 | 0 |
| `docs/legacy/` への `記号:行` 参照(**残すべきもの**) | 342 | **342** |
| ① 88 列の一意網羅 | 88 / `0..87` 一致 | 88 / `0..87` 一致 |
| ② `【なし】` 行 | 21 | 21 |
| `scripts/check_docs_status.py` | rc=0 | **rc=0** |
| `uv run ruff check .` | green | green |

**貼り替えの規律**(記法の節を research.md 冒頭で明文化した):

- **版固定アーカイブ `docs/legacy/` への典拠だけ `記号:行`** で書く(版固定で行番号が腐らない —
  設計書 8.5 は「`ファイルパス:行` **または節番号**」を許容、同期プロトコル 2-6 節が
  「版固定アーカイブは行番号で引き、可変な正本本文の引用と扱いを分ける」と定める)
- **可変正本・一時作業域は安定 ID**: 要件書 = FR/NFR・付録・節番号 / 設計書・ADR = 節・決定 ID /
  改善台帳 = `I-<n>` / 候補案・他タスクの計画 = 節 / スクリプト・テスト = 関数名・ケース名 /
  JSON = キー名
- **略記表に `BA` = `docs/legacy/research/baseball-apps.md` を追加**(裸の `baseball-apps.md:46` は
  検査上「非 legacy のパス形」に見えるため、legacy 記号へ寄せた)
- **`T250R` の読み替え表(旧行番号 → 新行番号)を除去** — 参照が安定 ID になり読み替えが不要になった。
  代わりに「`T250R` は要件書 v2.4 時点の行番号で引いており、本メモは現行の安定 ID へ再照合した」旨を残した

**手動確認の支援資料**: `docs/features/legacy-column-mapping/citation-sheet.md` を機械生成した
(1 表 = 記号形 89 対の「原典の当該行 → 安定 ID」/ 3 表 = パス形 34 対 / 2 表 = 差分 69 ブロックの前後比較)。
**`[手動・外部]` の合格条件「貼り替え後の参照先が同じ条項を指している」は未実施**(判定者: 山田正輝)。

**計画書の実測値の是正(射程不変)**: ステップ表のセルに旧測定値 **215** が残っていた
(計画書 4 節の検査コメントは既に 222 で内訳も一致していた)。**222 へ揃えた**。

### ステップ 2 — ID の全文形統一 + 表構造の是正(2026-09-08)

**機械検査(計画書 4 節)は全項目期待値どおり**:

| 検査 | 期待 | 実測 |
| --- | --- | --- |
| ① 88 列表の行数 / `0..87` の `diff` | 88 / rc=0 | **88 / rc=0** |
| ② `【なし】` 行 | 21 | **21** |
| ③ `DM-LG-*` 行数 / `diff` | 16 / rc=0 | **16 / rc=0** |
| ③ `DM-LX-*` 行数 / `diff` | 6 / rc=0 | **6 / rc=0** |
| ③ 裸形 `LG-nn` / `LX-nn` | 0 | **0**(是正前 **78**) |
| 表見出しから「案」を外した | 1 | **1** |
| `センチネルの記述が無い列は 20 列` | 1 | **1** |
| `列 33 は「"両" 登録投手の場合のみ記述なし」` | 1 | **1** |
| ④ 可変正本への行番号引用(ステップ 1 の維持確認) | 0 | **0** |
| `check_docs_status.py` / `ruff check .` | green | **green** |

**やったこと**:

- **ID を全文形へ統一**(裸形 78 → 0)。`LG-01`〜`LG-16` / `LX-01`〜`LX-06` を
  `DM-LG-*` / `DM-LX-*` へ。**接頭辞の有無での取り違えを構造的に潰した**
- **表見出しを確定形へ**: `| ID 案 |` → `| ID |`(2 表)。1 節前文の「割り当て**案**で、確定は
  計画書の承認ゲートで行う」を「**下表が確定割り当てである**(計画承認 2026-09-08・山田正輝)」へ
- **同期プロトコル正本化タスク側の別体系 ID を裸形から外した** — 同タスクにも `LG-01`〜`LG-08` が
  存在するため、「**接頭辞のない別体系の `LG-*` 系 8 件**」と書き換えた(裸形 0 件の検査と両立させるため)

**是正した欠陥 3 件**:

| 欠陥 | 内容 | 是正 |
| --- | --- | --- |
| **a** | 結論 4 が「センチネルを **88 列全数について確定した**」と書く一方、本文は 20 列を「記述なし」としていた | 「**列単位で 88 列全数について記録した。ただし 20 列は記述なしで確定していない**」へ |
| **b** | 表の `【なし】` 行は **21 行**なのに本文は「記述なしの列は **20 列**」と書き、列番号の列挙にも列 33 が無かった | 「**センチネルの記述が無い列は 20 列**。表の `【なし】` 行は **21 行**あり、差の 1 行は **列 33 は「"両" 登録投手の場合のみ記述なし」**という条件付きの欠落」へ |
| **c** | **FastAPI 期(Tsukuba PSS 内製の SPA)が調査範囲外**という限界が 0 節にしか無く、結論に出ていなかった | **結論の冒頭に「すべての結論に共通する射程の限界」として引き上げた**(センチネル定義 2 節と先発オーダー判定 5 節は Streamlit 期に限って成立する) |

**あわせて是正した述語の混同(欠陥 b と同じ段落)**: 「`T250R` が未言及とした 10 列のうち
**44・52・57・72・73・74 は `DL` に記述がある**」と「記述なし 20 列」に**列 44 が両方に出て矛盾**して
見えていた。前者は**列の意味**の記述、後者は**センチネル規則**の記述で述語が違うため、明示的に書き分けた。

**`[手動・外部]`**: 欠陥 a・c の是正が妥当かの確認は**未実施**(判定者: 山田正輝 — 7 周目 `P0-3`)。

### ステップ 3 — 旧所有契約の残滓の是正と判定/裁定の区別(2026-09-08)

**機械検査(計画書 4 節 ⑤)は 3 行すべて期待値どおり**:

| 検査 | 期待 | 実測 |
| --- | --- | --- |
| 固定見出し `## 履歴・非現行(参照しない)` | 1 | **1** |
| 履歴節を除外した本文の禁止パターン | 0 | **0**(是正前 **2**) |
| `移行仕様の事実の正本` | 1 以上 | **2** |
| ①〜④ の維持(88 / 21 / LG 16 / LX 6 / 裸形 0 / 行番号引用 0) | 不変 | **不変** |
| `check_docs_status.py` / `ruff check .` | green | **green** |

**やったこと**:

- **`## 履歴・非現行(参照しない)` を新設し、非現行の記述を隔離した** — 旧 8 節(成果物の置き場と
  形式・37 行)と旧 E 節(当初の次の一歩・22 行)。見出しも `履歴 1.` / `履歴 2.` へ振り直して
  現行の節番号と混ざらないようにした
- **`A. 人間の裁定が要るもの` を `A. 人間の裁定 — 現況` へ差し替え**。A-1〜A-7 をすべて
  **現在の所有者・状態**で書いた(裁定済み `R-1`/`R-2` / TSK-250 の射程 / 分割単位 ④ の射程 /
  TSK-250 ステップ 26 の所有)。**禁止パターン「和集合 9 項目を提案」は A-5 に残っていた**
- **結論 7 を現行の所有者へ書き換え** — 「成果物の形は 3 つの要求集合の和で満たす必要がある」
  (旧所有の operative 文)を「**要求集合が調停されていない事実の記録のみ。本メモは項目集合を
  提案しない**」へ
- **`RQ-F` の「本タスク内で是正」を「所有は TSK-250 / 契約の改訂は TSK-335」へ**。7-6 の論点 12 の
  `(8-2)` 参照も所有者表記へ
- **判定/裁定の区別を結論の冒頭に明記**(「**本メモの身分 — 事実の判定であって採否の裁定ではない**」)。
  あわせて **2 点の前提**を書いた:
  - `DM-LX-03`・`DM-LX-04` は判定材料が `data-layer.md` の反対側に傾くため、
    **`data-layer.md` 以外を採る場合に限り要件書の改訂ゲートが必要**(採るなら不要)という**条件付き**の制約
  - **`docs/legacy/` の規範性は限定射程** — 要件書が `data-layer.md` を正とする射程は
    88 列の意味・センチネル値・列名の罠に限られ、それ以外では証拠であって規範根拠ではない。
    射程外(`DM-LX-01`・`DM-LX-02`)は設計書 8.5 のとおり「優先順位を定めず「不明」と報告」

**あわせて是正: ステップ 1 の貼り替えが残した重複表記 19 件**。`` `付録D-1`(付録 D-1) `` /
`` `FR-002`(FR-002) `` / `改善台帳 改善台帳 `I-5`` / `` `FR-038`・`FR-038` ``(同一条項の並置)等。
**別条項の並置(`NFR-018`・`NFR-019` と `FR-038`・`FR-030`)は残すのが正**なので残した。

**`[手動・外部]`**: 旧所有文の書き換え漏れの有無と、precedence 制約・限定射程の記述の正確さの
確認は**未実施**(判定者: 山田正輝)。

### ステップ 4 — 成果物の敵対レビュー 1 周目の採否記録(2026-09-08)

**依頼方式**: `codex_run.py review adversarial`(sol xhigh)。**受理する指摘を 5 種に限定して依頼した**
(① 承認済み正本との矛盾 ② 原典と照合して誤っている判定 ③ 文書内部の非整合 ④ 射程・所有者の誤り
⑤ 典拠のない断定)。射程外(写像 JSON の設計・採否の裁定・要件改訂案・構造/様式の改善提案・
`docs/legacy/` の行番号引用・履歴節の内容)を明示的に除外した。

**結果: 11 件(P0 0 / P1 9 / P2 2)。全件を原典で検証して全件採用。**
9 周を要した計画レビューと違い、**指摘は最初から局所化した**(受理範囲の限定が効いた)。

| # | 重大度 | 種別 | 指摘 | 検証 | 反映 |
| --- | --- | --- | --- | --- | --- |
| `P1-1` | P1 | ④ | 表題が「行き先」「裁定」「移行変換の確定」を含む / 7-6 #13 に「本タスクが裁定しても」 | **成立** | 表題を「88 列の意味と旧資料矛盾の**事実判定**(採否の裁定と写像は後続タスクの所有)」へ。裁定者を TSK-334 へ |
| `P1-2` | P1 | ③ | `DM-LG-15` の判定が「2 節で全 88 列を確定」(欠陥 a と同型が判定表に残存) | **成立** | 「列単位に確認し、原典に記述のない列は「不明」として記録」へ |
| `P1-3` | P1 | ② | **列 85 `bottom_lrs` を `【D】` としているが誤り** — `DL:229` は後攻 82-85 を「同上」とするだけで、参照先の**列 81 自身が `DL:228` にセンチネル記述を持たない** | **成立**(`DL:228`・`DL:229` を実見) | 列 85 を `【なし】` へ。**`【なし】` 22 行 / 記述なし 21 列**へ連動。**計画書の ② の期待値も 21 → 22 へ追随** |
| `P1-4` | P1 | ③ | `DM-LX-05` の見出しが「食い違いでは**なく**」だが本文は `DL:208` を不完全と判定 | **成立** | 見出しを「食い違いは成立。`DL` は単純化された不完全記述」へ |
| `P1-5` | P1 | ① | 400 / 421 を「**正規化の分母**」と書いているが、これは旧 UI が 263 値を作るときの換算元。**移行の正規化分母は両列群で共通して 263**(`付録B-5`) | **成立**(`IS:201`・`IS:210` は保存形式を「0〜263 の float」と明記) | 用語を是正し「**旧 UI 内部の換算元であって移行の分母ではない**(混同すると 400 / 421 で割る誤った移行式になる)」と明記 |
| `P1-6` | P1 | ① | **`RQ-E`(列 50-51 の移行変換式が要件に無い)は誤り** — 付録 B の前文が打球位置も射程に入れ、`付録B-5` は列群を限定しない | **成立**(付録 B 前文「コース(ストライクゾーン)・**打球位置(フィールド)**の…座標の意味を固定する」を実見) | **`RQ-E` を撤回**し、申し送り `B-3` を取り下げ。**要件改訂の送り先(TSK-306)からも外した** |
| `P1-7` | P1 | ④ | 「本タスクが写像 JSON に落とす」「本タスクの成果物に課す条件」が残存 | **成立** | 「本調査結果を入力として TSK-250 が写像 JSON へ反映する」へ。7-8 の表は**本メモを直接拘束するのは「件数の固定」だけ**と明記 |
| `P1-8` | P1 | ③ | サイドカー結合キーを 7-7 では「本タスクの裁定対象」、A-4 では TSK-250 の射程としていた | **成立** | 「採否は TSK-250 側の移行仕様で扱う未決事項。本メモは矛盾の存在を記録するに留める」へ統一 |
| `P1-9` | P1 | ③ | **`plan.md` 1 節に `R-1`〜`R-4` の裁定表は存在しない**(ステップ 3 で私が張った参照が dangling)/ A-2 が「裁定済み」と「採否は TSK-334」を同時に書いていた | **成立**(計画書を grep して 0 件を確認) | 参照先を worklog の「決定」節へ。A-2 を「**事実判定は完了・値の採否は TSK-334 で未確定**。`R-2` が裁定したのは扱い方」へ分離 |
| `P2-10` | P2 | ③ | 「典拠が 7 本」だが直後の列挙は 8 か所 | **成立**(`DOM:14`・`DOM:59`・`DOM:111`・`AR:82`・`IS:180`・`IS:224`・`DT:84`・`AR:129`) | 「8 か所」へ(2 箇所) |
| `P2-11` | P2 | ② | 旧資料 9 本すべてを「Streamlit 実装の解剖」と一括しているが、`RM` は技術スタックの将来案・入力方式の一般調査・他製品調査も含む | **成立**(`RM:26`〜`:31` を実見) | 「**データ列と書き込み経路を記述する資料は** Streamlit 版を対象としている」へ限定 |

**あわせて自分で見つけた 2 件**(レビューは指摘していない):

| # | 内容 | 反映 |
| --- | --- | --- |
| 自 1 | 結論 4 の「`T250R` が挙げていない **12 列**が記述なし・不完全」が**どう数えても実測と合わない**(列挙は 11 エントリ / 展開すると 19 列) | **本文で件数を二重に持たない**方針にし、「内訳は 2 節の全 88 列表が正」へ |
| 自 2 | 「センチネルの記述が無い **20 列**」の参照が 2 節本文の別箇所にも残っていた | 21 列へ連動 |

**計画書の追随(射程不変・ステップ数不変)**: 成果物レビューが**承認済み計画書の期待値そのものの誤り**を
2 件見つけたため、計画書側も更新した。**PO の確認事項**として残す。

| 箇所 | 変更前 | 変更後 |
| --- | --- | --- |
| 4 節 ② の期待値 | 21 | **22**(列 85 の追加) |
| ステップ 2 の固定文言 | `センチネルの記述が無い列は 20 列` | **21 列** |
| 3 節の要件改訂の送り先 | `RQ-C` `RQ-E` `RQ-F` `RQ-G` → TSK-306 | **`RQ-C` `RQ-F` `RQ-G` → TSK-306**(`RQ-E` 撤回) |
| 実データ確認タスク(TSK-336) | `undocumented` 20 列 | **21 列** |

**機械検査(反映後)**: ① 88 / `0..87` diff rc=0 ・② **22** ・③ LG 16 / LX 6 / 裸形 0 ・④ 0 ・
⑤ 見出し 1 / 禁止 0 / 正本 2 ・固定文言 2 種とも 1 ・`check_docs_status.py` rc=0。

**次**: 収束確認周(2 周目)を回す。P0/P1 = 0 で収束したらその最終反映に `(ステップ 4)` を付ける。

### ステップ 4 — 成果物の敵対レビュー 2 周目の採否記録(2026-09-08)

**結果: 12 件(P0 0 / P1 8 / P2 4)・判定 否決。全件を原典で検証して全件採用。**

**うち 3 件は 1 周目の反映漏れだった**(`P1-1` A-2 だけ直して A-1・問い 2 を残した / `P1-2` 所有者の
残滓が他節に残っていた / `P1-4` 申し送りの「20 列」)。**「1 箇所だけ直して他に残す」の 3 回目**なので、
今周は**是正のたびに類型全件を走査**した(下記)。

| # | 重大度 | 種別 | 指摘 | 検証 | 反映 |
| --- | --- | --- | --- | --- | --- |
| `P1-1` | P1 | ④ | 問い 2 が「どちらを採れるか」を本メモの問いに残し、A-1 が `DM-LG-03` を「裁定済み」としている(採否は TSK-334 の所有) | **成立**(1 周目に A-2 だけ直していた) | 問い 2 を「判定材料がどちらにどれだけあるか」へ。A-1 を「事実判定は完了・採否は TSK-334 で未確定。`R-1` が裁定したのは扱い方」へ |
| `P1-2` | P1 | ④ | 「本タスクの成果物」「本タスクの写像 JSON」「本タスクが埋める」が 5 箇所に残存 | **成立** | 全件を「本調査の事実を入力として TSK-250 が写像 JSON へ反映する」へ統一 |
| `P1-3` | P1 | ④ | `data-migration.paths` の充填を TSK-250 ステップ 26 に割り当てているが、PO 分割後は**分割単位 ④(TSK-339)の所有** | **成立** | 所有者を TSK-339 へ。「TSK-250 のステップ 26 は旧割り当てで TSK-335 の改訂対象」と明記(2 箇所) |
| `P1-4` | P1 | ③ | 1 周目で本文・結論を 21 列へ更新したのに**申し送りだけ「20 列」**。TSK-336 の確認対象を 1 列不足させる | **成立** | 21 列へ |
| `P1-5` | P1 | ③ | 「列 63-71 を捨てると approved 3 本に違反」の断定が強すぎる。**固定されているのは互換出力**で、内部で生値を物理保持する方式は要求されていない(6-1 自身が列単位一致・往復一致の条文が無いと書いている) | **成立** | 判定を「**候補案に再構成の方法が無く、互換出力の可能性が未証明**」へ狭めた(判定表・1-2 見出し・結論 3・7-5 の**4 箇所を全件走査して是正**)。**違反と判定するには生値保持を要求する安定 ID と再導出不能の根拠が要る**と明記 |
| `P1-6` | P1 | ② | 列 27 の「初回行はスタメン確定後も DEFAULT 値が残り得る」が原典を超えている。`SS:205` は「実害は `update_list` 側の再導出で吸収されている模様」と続く | **成立**(原典を実見) | 「初期値が DEFAULT であることは確認済みだが、**DB 保存行に残るかは不明**」へ |
| `P1-7` | P1 | ② | poses の `'H'`/`'R'` を E-1/E-2 の痕跡と断定して**機械判定の十分条件**にしているが、`SS:194`〜`:199` により**交代確定時の stamem は保存され次の試合で再ロードされる**ため当該試合の遡及上書きとは限らない | **成立**(原典を実見) | 「**参考兆候(十分条件ではない)**」へ降格し、理由を典拠つきで明記 |
| `P1-8` | P1 | ⑤ | `RQ-A` の負値実在性の根拠が誤り。`IS:201` は保存形式を「**0〜263 の float**」と記述しており、パディング除去から負値を導くのは**推論であって典拠ではない** | **成立** | 「**負値の実在性は不明**」へ。設計書 8.5「典拠が見つからないことは「不明」と報告する — 推測で埋めない」を明示。**規則上の食い違いは残る**ことは維持 |
| `P2-9` | P2 | ⑤ | `E-5` の導出が飛んでいる。`MAX(id)` 削除と読み出しがプレイの番号順であることだけでは両順序のずれを導けない(順不同 INSERT・重複・遅延同期などの追加条件が要る) | **成立** | **`E-5` を撤回**(重複は `E-4` が別途扱う) |
| `P2-10` | P2 | ③ | 結論 5 が `DM-LX-03/04/06` と `DM-LX-01/02` の 5 件しか扱わず **`DM-LX-05` が脱落** | **成立** | 「**`DM-LX-05` は採否の論点ではない** — 列 56 は保存済みの完成値なので移行は恒等」を追加 |
| `P2-11` | P2 | ③ | ID 割り当て先を「4 節・5 節」としているが対応表は 1 節と 3 節 | **成立** | 「対応表は 1 節と 3 節」へ |
| `P2-12` | P2 | ① | `RQ-G` が「座標変換を定めるのは B-1/B-2」とするが、**旧 263 スケールからの変換式とベクタを定めるのは `付録B-5`** | **成立** | 「正規化と視点は B-1/B-2、変換式とテストベクタは `付録B-5`。10 章の「座標変換ベクタ」の正しい行き先は `付録B-5`」へ |

**あわせて自分で見つけたステップ 1 の残滓 4 件**: 範囲畳み込みで生じた
`候補案 12-1 節-候補案 12-1 節`(3 件)と `改善台帳 I-5 改善台帳 I-5`(1 件)。**類型全件を走査して 0 件を確認**。

**類型ごとの全件走査(反映漏れの再発防止)**:

| 類型 | 走査 | 是正後 |
| --- | --- | --- |
| A: 「本タスク」を主語にした所有・作成の記述(履歴節を除く) | `grep -nE '本タスク\|本メモが(作\|埋\|裁定)'` | 残るのは**射程外の宣言のみ**(「本タスクの射程外」「本タスクで裁定してはならない」) |
| B: 「20 列」 | `grep -c '20 列'` | **0** |
| C: 「裁定済み」(履歴節を除く) | `grep -c '裁定済み'` | **1**(他文書の節名「B. 裁定済み指摘 — P2」の引用のみ) |
| D: paths の所有者 | `grep -n 'ステップ 26\|paths 充填'` | **2 箇所とも TSK-339 へ** |
| E: 列 63-71 の「違反」の断定 | `grep -nE 'approved 3 本\|衝突する\|満たせない'` | **4 箇所とも射程を狭めた** |
| 残滓: 範囲畳み込みの重複表記 | `grep -noE '(節\|表)-(候補案\|要件書\|同期\|改善台帳\|TSK)'` | **0** |

**機械検査(反映後)**: ① 88 / diff rc=0 ・② **22** ・③ LG 16 / LX 6 / 裸形 0 ・④ 0 ・
⑤ 見出し 1 / 禁止 0 / 正本 2 ・固定文言 2 種とも 1 ・`check_docs_status.py` rc=0 ・`ruff check .` green。

### ステップ 4 — 成果物の敵対レビュー 3 周目の採否記録(2026-09-08)

**結果: 6 件(P0 0 / P1 4 / P2 2)・判定 否決。全件を原典・承認済み計画で検証して全件採用。**
**推移: 11 → 12 → 6**(P0 は 3 周すべて 0)。

**4 件が再び前周の是正の連動漏れだった。** 2 周目で類型走査を導入したが、**走査した類型の外に
兄弟箇所があった**(列 27 の是正が 1-5 と E-6 に届いていない / 射程縮小が 2 箇所に届いていない /
所有者置換が 1 箇所残っていた)。今周は**指摘ごとに兄弟箇所を先に全件列挙してから編集**した。

| # | 重大度 | 種別 | 指摘 | 検証 | 反映(兄弟箇所を含む) |
| --- | --- | --- | --- | --- | --- |
| `P1-1` | P1 | ② | 列 27 の永続化の断定を 2 周目に 88 列表だけ直し、**1-5 と E-6 に連動していない**。`DL:411` は列 69 の整数 `17` の根拠であって氏名列の根拠ではない | **成立**(`SS:205`・`DL:411` を実見) | **4 箇所を同時に是正** — 1-5 本文 / `E-6`(「不明」へ)/ `DM-LG-13` の判定 / 1-5 の見出し。**残存を確認できるのは `DL:411` の列 69 のみ**と限定 |
| `P1-2` | P1 | ③ | 「写像に『捨てる』行き先を作れない」「捨てるなら要件改訂が必要」が残存し、**2 周目に狭めた「再導出できれば違反ではない」と矛盾**。`FR-038` の原本保全は**行単位の黙殺禁止**で全旧列の生値保持を要求していない | **成立** | 6-4 を**行単位の黙殺禁止**へ限定 / 7-5 末尾を「**保持も再導出もできず互換出力を満たせない場合に限り**要件改訂が必要」へ条件付き化 |
| `P1-3` | P1 | ① | **列 56 の変換を「恒等 + `0` を NULL」に限定しているが `FR-038` の条件は `球速<=0`**。負値が存在した場合に恒等移行され、正本の NULL 件数条件に反する | **成立**(`FR-038` の V2 条件を実見) | 「**正値は恒等・`球速<=0` は NULL**」へ(3-5 と結論 5 の 2 箇所)。65〜99 km/h を保持する結論は不変 |
| `P2-4` | P2 | ④ | FastAPI 期と 21 列の実データ確認は **TSK-336**、`RQ-A` の要件改訂は **TSK-337** の所有だが、本文は「移行仕様タスク」と書いている。**結論の冒頭は「送り先の正は plan」と書きながら plan と一致していない** | **成立**(plan 3 節の送り先表を実見) | 3 箇所を実 ID へ(結論の射程の限界 / 2 節の末尾 / `RQ-A`)。**`RQ-A` は要件改訂 = TSK-337・負値の実データ確認 = TSK-336 に分けて明記** |
| `P1-5` | P1 | ④ | 移行試合のサイドカー結合キーを **TSK-250 の射程**としているが、承認済み計画 3 節は `RQ-SIDECAR-KEY` を **TSK-214** へ合流させている | **成立**(plan 3 節を実見) | A-4 と 7-7 末尾の **2 箇所とも TSK-214 へ** |
| `P2-6` | P2 | ④ | 「本タスクの JSON」が 1 箇所残存(2 周目の所有者置換の漏れ) | **成立** | 「**TSK-250 が作る写像 JSON**」へ |

**類型ごとの全件列挙(今周は編集前に実施)**: `プレースホルダ`(10 箇所)/ `捨てる`(9 箇所)/
`列 56`(6 箇所)/ `移行仕様タスク`(6 箇所)/ `サイドカー結合キー`(5 箇所)/ `本タスク`(9 箇所)。
**うち是正対象を判別して 12 箇所を編集**し、残りは正当な用法(射程外の宣言・他文書の引用・
要件書自身の文言)であることを確認した。

**さらに自己走査で見つけた連動漏れ 4 件**(4 周目を投げる前に自分で潰した — 3 周続いた失敗型なので
**反映後に必ず同型を全件走査する**運用にした): ① 88 列表の列 56 行に移行条件(`球速<=0` は NULL)を
明示 ② 氏名列の残存確認の送り先を TSK-336 へ ③ 結論 4 の「実データ確認」の送り先を TSK-336 へ
④ C 節の「センチネル記述なし 21 列」の送り先を TSK-336 へ。

**機械検査(反映後)**: ① 88 / diff rc=0 ・② 22 ・③ LG 16 / LX 6 / 裸形 0 ・④ 0 ・
⑤ 見出し 1 / 禁止 0 / 正本 2 ・固定文言 3 種とも 1 ・`check_docs_status.py` rc=0 ・`ruff check .` green。

### ステップ 4 — 成果物の敵対レビュー 4 周目の採否記録(2026-09-08)

**結果: 6 件(P0 0 / P1 4 / P2 2)・判定 否決。全件を原典・承認済み計画で検証して全件採用。**
**推移: 11 → 12 → 6 → 6。P0 は 4 周すべて 0。**

**この周の重要な観測**: レビュアが冒頭で「**3 周目で修正された列 27・列 56・`DM-LG-03`・
`RQ-SIDECAR-KEY` の旧記述は再発していません**」と明記した。**3 周続いた「1 箇所だけ直して兄弟箇所に
残す」型は止まった**(編集前に類型全件を列挙する運用 + 反映後の自己走査が効いた)。
今周の 6 件は**再提示ではなく新規に見つかった欠陥**である。

| # | 重大度 | 種別 | 指摘 | 検証 | 反映 |
| --- | --- | --- | --- | --- | --- |
| `P1-1` | P1 | ① | **`RQ-A` を「負値座標」と x/y 一括で書いている**。B-5 の式では**負の x はクランプ後 `0.0`・負の y は `1.0`** になり、`FR-038` の V2 が算入する条件は **`x<=0`** だけ。構造的不一致が成立するのは**負の x** に限る | **成立**(B-5 の 2 式と V2 の条件を実見) | **3 箇所を「負の x」へ限定**(結論 8・`RQ-A`・`B-1`)。**y が射程外である理由を式で明記** |
| `P1-2` | P1 | ④ | **`RQ-F` を TSK-250 / TSK-335 の所有としているが、承認済み計画 3 節は `RQ-F` → TSK-306**。TSK-250 は写像 JSON、TSK-335 は受け渡し契約の所有者で、**`RQ-F` そのものの所有者ではない**。B 表に各実 ID が欠落 | **成立**(plan 3 節を実見) | **要件側の不足 / 成果物 / 契約改訂の 3 者を書き分け**(`RQ-F`・A-5・7-6 #12・7-6 #4 の 4 箇所)。**B 表の全行へ送り先の実 ID を追加**(B-2 → TSK-306 / B-4 → TSK-214 / B-5 → TSK-239 / B-6 → TSK-306) |
| `P1-3` | P1 | ④ | `DM-LG-01` で `DL:170` と `SS:131` の矛盾を示した直後に「移行変換では列 18 も読む必要がある」と**一方を無条件に採用**している(採否は TSK-334 の所有) | **成立** | 「**現行の precedence に従い `DL` を採る場合は**列 18 も読む。**最終的な採否は TSK-334 の所有**」へ条件化 |
| `P1-4` | P1 | ⑤ | 「**`P-02` は既に機械強制済み**」の断定が過大。引用テストが固定するのは**サイドカー結合キーの 3 要素だけ**で、88 列本体への列追加の禁止と互換出力側の実装は検査していない | **成立**(テスト名を実見) | 「**同期キーの 3 要素は機械強制済み**。88 列本体への列追加の検査は **TSK-339** の所有」へ(7-3 と 7-6 #5 の 2 箇所) |
| `P2-5` | P2 | ③ | 本メモ自身が 22 ID の対応表を持つのに、現行節が「**リポジトリのどこにも存在しない**」「まだどこにも存在しない」と断定している | **成立** | **すべて過去時点へ限定**(結論 1・1 節見出し・1 節本文・7-1 表の 4 箇所) |
| `P2-6` | P2 | ③ | 6-2 の安定 ID が `` `NFR-019`-`NFR-019` `` と重複し範囲表記のように壊れている(ステップ 1 の範囲畳み込みの残滓) | **成立** | 単一の `` `NFR-019` `` へ |

**反映後の自己走査**: 負値の x/y 一括表記の残存 0 / 「存在しない」の断定 0 / 「機械強制済み」の
無限定表記 0 / 範囲畳み込みの残滓 0。

**機械検査(反映後)**: ① 88 / diff rc=0 ・② 22 ・③ LG 16 / LX 6 / 裸形 0 ・④ 0 ・
⑤ 見出し 1 / 禁止 0 / 正本 2 ・固定文言 3 種とも 1 ・`check_docs_status.py` rc=0 ・`ruff check .` green。

### ステップ 4 — 成果物の敵対レビュー 5 周目(最終周)の採否記録(2026-09-08)

**結果: 3 件(P0 0 / P1 3 / P2 0)。P1 全件を原典で検証して全件採用。**
**推移: 11 → 12 → 6 → 6 → 3。P0 は 5 周すべて 0。P2 は 0 件だったため申し送りは生じなかった。**

**PO 裁定(2026-09-08)により本周で打ち切る**(計画書 4 節「成果物レビューの打ち切り」)。

| # | 重大度 | 種別 | 指摘 | 検証 | 反映(兄弟箇所を含む) |
| --- | --- | --- | --- | --- | --- |
| `P1-1` | P1 | ① | 「**未知長・不正 JSON は原本保全・隔離へ回す**」と一括している。`FR-038` は不整合行を「**無検閲で取り込み保全し警告レポートで可視化する(弾かない)**」と定め、**隔離は「新スキーマの制約を満たせず取り込めない行」に限る**。未知長だけでは制約違反とは限らない | **成立**(`FR-038` の 2 つの受入基準を実見) | 「**未知長は原本を保持して警告付きで移行し、解析不能または新スキーマ制約を満たせない行だけを隔離**」へ。**一括して隔離と書くと TSK-250 が有効な可変長データまで隔離するおそれがある**ことを明記 |
| `P1-2` | P1 | ③ | 実データ確認の対象を「記述なし 21 列 + 列 33」に限定しているが、**本文自身が「`0` がセンチネルか実値か記述なし」と判定した列 73・74 を漏らしている**(`DL:220`・`:221`) | **成立**(原典と本文の両方を実見。**兄弟の列 72 も同型**だったので自分で追加) | **未確定を 3 種に区別する表を 2 節へ新設** — **(a) 記述なし 21 列 / (b) `0` の意味が未確定の列 72・73・74 / (c) 条件付きの列 33**。**3 種すべてが TSK-336 の確認対象**であることを結論 4・2 節・C 節の 3 箇所へ反映 |
| `P1-3` | P1 | ② | `DM-LX-06` を「**`DL` が 3 資料と衝突。かつ `DB` 内部でも不一致**」としているが誤り。**`DL` の「ピクセル座標」は尺度を指定していない**ので 0〜263 保存と論理的に衝突しない。**`DB:305`(列 42-43)と `DB:306`(列 50-51)は別の列群**なので内部矛盾でもない。**明示的な食い違いは列 50-51 の `DB:306` vs `IS:210`** | **成立**(`DL:194`/`:195`/`:202`/`:203`・`DB:305`/`:306`・`IS:210` を実見) | 3-6 の見出しと本文・LX 表の衝突相手・88 列表の列 42 の注記を是正(4 箇所) |

**`P1-3` の帰結(レビュアは指摘していないが自分で追った)**: **`DM-LX-06` は `DL` が対立側にいないため
`DL` precedence では解けない。** approved の **`付録B-5`** が旧 263 スケールからの変換式を「(確定)」と
定めていることで決着する。→ **結論 5 を「解決経路は 3 通り」へ書き換えた**
(① `DL` precedence の射程内 = `DM-LX-03`・`DM-LX-04` / ② approved 要件が直接解く = `DM-LX-06` /
③ 射程外で未決 = `DM-LX-01`・`DM-LX-02`)。7-2 の帰結も同様に是正。

**機械検査(反映後・最終)**: ① 88 / diff rc=0 ・② 22 ・③ LG 16 / LX 6 / 裸形 0 ・④ 0 ・
⑤ 見出し 1 / 禁止 0 / 正本 2 ・固定文言 3 種とも 1 ・`check_docs_status.py` rc=0 ・`ruff check .` green。

### 成果物の敵対レビュー 5 周のまとめ

| 周 | 指摘 | P0 | P1 | P2 | 性質 |
| --- | --- | --- | --- | --- | --- |
| 1 | 11 | 0 | 9 | 2 | 文書の欠陥(所有者の残滓・原典との食い違い) |
| 2 | 12 | 0 | 8 | 4 | うち **3 件が 1 周目の連動漏れ** |
| 3 | 6 | 0 | 4 | 2 | うち **4 件が 2 周目の連動漏れ** |
| 4 | 6 | 0 | 4 | 2 | **連動漏れ 0**・新規のみ |
| 5 | 3 | 0 | 3 | 0 | 新規のみ・**P2 0 件** |
| **計** | **38** | **0** | **28** | **10** | **全件採用・不採用 0 件** |

**観測 1: 受理範囲の限定が最初から効いた。** 計画レビューが 9 周を要したのに対し、成果物レビューは
1 周目から指摘が局所化した(受理する指摘を 5 種に限定し、射程外を明示的に列挙して依頼した)。

**観測 2: 周回を伸ばしたのは文書の欠陥ではなく私の反映漏れだった。** 2・3 周目の指摘の過半が
「1 箇所だけ直して兄弟箇所に残す」型。**編集前に類型全件を列挙し、反映後に自己走査する運用**へ
切り替えた 4 周目で止まり、レビュアも「3 周目の修正は再発していない」と明記した。
**これは TSK-340(設計書 6.1 の終端条件)の条文材料**である。

**観測 3: 自分の判定が典拠を超えていた件が 7 件あった**(列 63-71 の「approved 違反」/ 列 27 の
永続化 / poses の `H`/`R` の十分条件化 / `RQ-A` の負値実在性 / `E-5` の順序ずれ / 「`P-02` は機械
強制済み」/ `DM-LX-06` の対立構造)。**いずれも「推論を典拠として書いた」型**であり、設計書 8.5 の
「典拠が見つからないことは「不明」と報告する — 推測で埋めない」に反していた。

### 短縮判断(2026-09-08 — ステップ 1 と並行して実施)

**backend へ至る経路を縮めるための 3 判断。** 詳細と根拠は
[2026-09-08-handover.md](2026-09-08-handover.md) 2 節「短縮判断」。

| # | 判断 | 反映先 | 状態 |
| --- | --- | --- | --- |
| ② | TSK-317(先行 B)を本タスクと**並行で着手可**とする | TSK-317 のカードへ追記 | 記録済み |
| ⑤ | TSK-340(設計書 6.1 の終端条件)を **TSK-250 の前**に置く | TSK-340 のカードへ追記 | 記録済み |
| ① | TSK-250 の開始条件を**タスク単位 → ステップ単位**へ緩める | TSK-335 へ裁定材料 + 追加 DoD 3 件 | **人間の裁定待ち** |

**① で判明した是正(当初の読みが誤りだった)**: 「TSK-250 の 28 ステップのうち C に依存するのは
ステップ 6・18 の 2 件だけ」と読んでいたが、**ステップ 6 は B と C の両方を要求する母集合固定であり、
28 ステップ中もっとも早い依存点**だった。後段はステップ 6 の母集合を exact-set で照合する構造なので、
**開始条件をステップ単位へ緩めてもステップ 1〜5 しか前倒しできない**。短縮の実際の要は
**ステップ 6 の母集合固定を出所ごとに分割すること**で、これは TSK-335 の既存 DoD の射程内。

**TSK-340 の実測の是正**: 同カードは起票時点の「計画レビュー 7 周」で書かれていた。**最終は 9 周**
なので 8・9 周目の実測(承認阻害 7 件 / 6 件)と根本原因 3 点を追記した。

## 未解決・申し送り(ステップ 5 — 2026-09-08 時点の現行)

**未解決の一覧の正は [research.md](../features/legacy-column-mapping/research.md) の「未解決・申し送り」節**
(A. 人間の裁定の現況 / B. 要件改訂タスクへ送るもの / C. 実測・別タスク待ち / D. 参照経路の課題)。
本節はその**行き先とブロック関係**を記録する。

### 送り先(全件が実 ID を持つ — 2026-09-08 起票済み)

| 送り先 | 内容 | 状態 |
| --- | --- | --- |
| [TSK-334](https://app.notion.com/p/3d493b75e6878176bf75dcbdd70d35b9) | **判定 22 件の採否の裁定**。本メモは事実の判定までで、採否は行わない | 未着手 |
| [TSK-335](https://app.notion.com/p/3d493b75e687817eb966e0b5511ebd31) | TSK-250 の計画改訂(受け渡し契約の内部矛盾の解消)+ **スキーマ + ORM の切り出しの設計と起票**(PO 裁定 2026-09-08) | 未着手 |
| [TSK-336](https://app.notion.com/p/3d493b75e68781418434d215aabbbb29) | 実データ確認 — **(a) センチネル記述なし 21 列 / (b) `0` の意味が未確定の列 72・73・74 / (c) 列 33 の「両」ケース / FastAPI 期の書き込み / 列 44 と列 86-87 の実値 / 負の x の実在性** | 未着手 |
| [TSK-337](https://app.notion.com/p/3d493b75e6878138a7e2d1dd23ab1054) | `RQ-A`(**負の x 座標**のセンチネル / クランプ衝突)の要件改訂。**P0 相当** | 未着手 |
| [TSK-338](https://app.notion.com/p/3d493b75e68781d4b449c8bb844d0eac) | ADR-003 D-6 / D-12 の改訂 | 未着手 |
| [TSK-339](https://app.notion.com/p/3d493b75e6878160abe1c3564bfcca8c) | `contracts/` への契約化・検査器・CI 配線・**`core-areas.paths` の充填** | 未着手(② + TSK-250 が開始条件) |
| [TSK-340](https://app.notion.com/p/3d493b75e687811c8cf0df9ff82b3700) | **ハーネス設計書 6.1 の改訂** — 台帳の成立済み昇格条件の実行(計画レビューの終端条件) | 未着手 |
| TSK-239 | `RQ-B`(責任投手の移行導出と付録 A-2 の非同一) | 合流済み・未着手 |
| [TSK-306](https://app.notion.com/p/3ce93b75e687816692a2c07da345a09e) | `RQ-C`(NULL 由来の区別定義)/ `RQ-F`(意味欄の項目集合)/ `RQ-G`(付録 B の項番参照)。**`RQ-E` は撤回して外した** | 合流済み・未着手 |
| [TSK-214](https://app.notion.com/p/3bb93b75e6878103a735e5585b47c5b3) | `RQ-D`(Won't 廃止列の互換出力値・本体 88 列の NULL 表現)/ `RQ-SIDECAR-KEY`(移行試合のサイドカー結合キー) | 合流済み・未着手 |

**撤回した申し送り 1 件**: `RQ-E`(列 50-51 の移行変換式の列単位明記)は**不成立**だった —
付録 B の前文が打球位置も射程に入れ、`付録B-5` の変換式は列群を限定していない。列 42-43 と 50-51 は
どちらも 0〜263 の float で保存される(`IS:201`・`IS:210`)ため **`付録B-5` が列 50-51 の変換も確定済み**。
**TSK-306 からも外した**(成果物レビュー 1 周目 `P1-6`)。

### backend への最短路との関係(PO 裁定 2026-09-08)

**本タスクは backend のクリティカルパス上にない。** ハーネス設計書 13 章「製品機能の実装(v1.13・
approved)」が「**本章は製品実装の開始条件を定めない**」と明記しており、実装のゲートは 6.1 の計画承認
のみである。backend を止めているのは **スキーマ + ORM の確定**(TSK-250 から切り出す — TSK-335 が設計)
と **ADR-003 の検査基盤**(TSK-235・TSK-250 と独立)である。詳細は
[2026-09-08-handover.md](2026-09-08-handover.md) 2 節。

**本タスクの成果物が入力になる先**: TSK-334(採否の裁定)→ TSK-250(88 列写像 JSON)。
**スキーマ本体は 88 列の意味を必要とするが、採否の裁定は必要としない**可能性があり、
**先行スキーマタスクの入力として research.md で足りるかは TSK-335 の切り出し設計で判定する**論点。

### 次の一歩

1. **PR を作成**(`/pr`)— 必須チェック行の手動付与 + 設計書 6.3 の実施記録行
2. **人間の逐行確認**(代替統制②)— 2 節の全 88 列表 / 1 節と 3 節の判定 22 件と根拠段落 / 申し送り全件。
   **`citation-sheet.md` で支援**。確認時の `research_blob_sha` を PR コメントへ記録
3. **`/task-done`** で worktree を回収し Notion を完了へ
4. **次のタスクは TSK-335 と TSK-235**(PO 裁定 2026-09-08)
