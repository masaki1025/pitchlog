---
date: 2026-09-06
topic: 同期プロトコル正本の不整合の改訂ゲート(TSK-280 / 後続 α / 後続 β の申し送り)
branch: feature/sync-canon-revision
---

# 作業ログ: 2026-09-06 同期プロトコル正本の不整合の改訂ゲート

## やったこと

### /task-start

- Notion タスク **TSK-322**(TSK-280 の /task-done で起票済み)を取得。**重複起票はしていない**
- worktree `../pitchlog-worktrees/feature-sync-canon-revision`(ブランチ `feature/sync-canon-revision`・
  起点 `origin/develop` = `f9c0c04`)を作成
- 計画書雛形と本 worklog を作成

### /investigate

- 調査サブエージェント **3 並列**(要件書側の受け皿 / 11-3 食い違いの全数と決定経緯 / H-85 連鎖の実測)
- 結果を `docs/features/sync-canon-revision/research.md` に統合
- **報告を鵜呑みにせず主要な数値・主張を自分で再検証した**(下記「調査で覆った前提」)

### /plan

- `docs/features/sync-canon-revision/plan.md` を作成(**ステップ 15 本**)
- 探索 2 本(非 FR ID の引用記法の実在 / 54 件の直し方)+ 設計 1 本(ステップ表と順序)
- `feature_status.py` が「計画段階(計画レビュー周回 0 周・承認待ち)」と正しく導出することを確認
- 敵対レビュー(`codex_run.py review adversarial`)へ

### 実装ステップ 1 — 両正本の in-review 化と改訂枠の設置

- **適用版(7.3-1 暫定記録)**: ハーネス設計書 `docs/development/dev-harness-design-2026-08-07.md` の **v1.13**(approved)。**条文コミット SHA = `9768edf`**(本 in-review 化コミットの第一親であり、適用版が approved 状態で収録されている最新のコミット)
- **初回敵対レビュー実行時点で暫定記録と異なっていれば、更新してから開始する**
- 本件は同期プロトコル設計 v0.3 と要件書 v2.7 の**複数正本を一括検証する単一の確定ゲート**であり、**適用版の記録はゲート単位で 1 件**とする

## 決定

### TSK-267(v0.2)へ合流させない

**カード TSK-322 の注意書き**「TSK-267(v0.2)との関係を最初に確認する。TSK-267 の射程は 5・6・7 章であり
4 章を含まないが、同一の確定ゲートでまとめられるならそちらへ合流させる方が安い」に対する裁定。

**合流させない。** 根拠:

| 観点 | TSK-267(v0.2) | TSK-322(本タスク) |
| --- | --- | --- |
| 性質 | **新規規則の設計** — v0.1 が射程外と宣言した経路(端末喪失 gap・遅着原イベント・誤墓標訂正・退避取り込み)を閉じる | **既存記述の不整合・欠落の是正** |
| 要件書 | **v2.5 への版繰り上げが必須**(カード「要件に規則が無いため、設計だけでは決められない」) | **2 件は要件書の改訂が要る**(時刻属性 / NFR-020 前提不成立時 — /investigate で判明。**当初の見込みは誤り**) |
| 収束実績 | ④ 退避取り込みは先行タスクの確定ゲート **4 周で収束せず切った**(累計 **P0 8 件・P1 5 件**) | 未知。ただし論点は局所 |
| 優先度 | 高・未着手 | 中 |

- 合流の唯一の論拠は **H-85 の連鎖コスト**(正本 1 文字の変更で `tests/fixtures/sync-protocol-source.txt` と
  11 関係マニフェストが連鎖)を 2 回払わずに済むこと。しかしこれは**同じ作業を 2 回踏むコスト**であり、
  **収束しない側に収束する側が人質を取られるリスク**より小さい
- **同型の判断が先行タスクに存在する** — 「3 件を同時に決めると要件書 v2.4 の確定が遅れる」ため
  **v2.4 は U-2 だけを載せて確定させ、残りを TSK-267 へ送った**(TSK-267 カード)。本件も同じ形

**判断は維持する(根拠が 1 つ崩れた後も)**: 「要件書の改訂を要さない」は /investigate で崩れたが、
**TSK-267 ④ の非収束リスクは変わらず**、かつ **要件書 v2.6 が TSK-267 の v2.5 を飛ばして
単独ゲートで先に確定した前例**(要件書 `:39`・`:40`)があるため、別ゲートで進める運用は確立している。

**版番号(/investigate で判明 — 当初の見立てを訂正)**:
- **版番号の予約制度は実在する**。要件書 `:39`「**v2.5 は TSK-267 の先約のまま維持・同一版番号を
  別内容で確定しない**」/ `:40`「本版が先に確定するため**確定日順と版番号順は一致しない**」
- 7.3 節の条文には予約の規定が無いが、**運用前例が正**(当初「予約制度を持たない」と見立てたのは誤り)
- → 設計正本は **v0.3**・要件書を改訂するなら **v2.7 以降**が妥当
- **追随が要る**: **11-4 の 4 箇所**(`:2056`・`:2057`・`:2069`・`:2078`)が「要件書 v2.5 + 本正本 **v0.2** を
  同一の確定ゲートで一括検証」と明記している。変更履歴表と索引 `docs/README.md:12` には記載なし

### 調査で覆った前提 4 件(いずれも自分で再検証済み)

| # | 当初の前提 | 実際 | 典拠 |
| --- | --- | --- | --- |
| 1 | 「正本を 1 文字変えると fixture と 11 関係マニフェストが連鎖する(**H-85**)」 | **誤り。** 正本 sha256 `7ea01f09…`(2434 行)/ fixture `2e38de6b…`(**1178 行**)で別物。突合 assert が存在せず 8 通りの変異で 1 度も red にならない。**`H-85` 本体は要件書 + `contracts/authz/*` の事象**で `sync-protocol.md` は登場しない | `harness-evaluation.md:730`・実測 |
| 2 | 不整合は 3 件 | **11-3 の食い違いだけで 38 ID / 120 ペア**。しかも **`U-9` が既に記録し受け取り先を本タスクと名指していた** | `sync-protocol.md:2063` |
| 3 | 有効化で即 fail は 18 行 | **実測 54 件**(帰属表は 212 エントリ・8 カテゴリ。18 は FR/NFR だけの数) | 自分で有効化して実測 |
| 4 | 「`FR-006` の G・J に規則行が無い」 | **G・J は要件書の記号ではない**(β のローカルなラベル)。実体は FR-006 `:239` の補足 2 条項 | `sync-ack-contract/research.md:188` |

### 最大の発見 — 参照規約が帰属母集合を覆っていない

- 正本の参照規約 `:135`(2-6)が扱うのは **4 カテゴリ**(条・章節・付録項番・条内小項)
- 帰属母集合 `req-universe.json` は **8 カテゴリ 212 件**
- 差の合成 ID(`6.1/未同期キュー`・`ブロック1`・`8章DoD/1`)は**要件書のアンカーとして存在せず、
  規約どおりに本文へ引く手段が無い**
- **二文書目(データモデル正本)は存在せず**、テストの合成サンプルも `FR-001`/`NFR-006` のみ
  → **この照合パスはリポジトリで一度も実行された実績が無い**
- **これは `U-9` が言っていたことそのもの**

### 逃げ道を使わない判断

`対象外` の行は検査がスキップされる(`check_attribution_destinations`:
`if assignment.kind == "対象外": continue`)。28 件を対象外へ移せば有効化できるが、
**`6.1/未同期キュー` は現在「同期側で決める」**であり、同期正本がまさに決めている対象を
「決定にも入力にも用いない」と宣言することになる。**7.3-3 が P1 と名指す
「文言は直したが実効がない」型そのもの**のため、**参照規約 2-6 の拡張で正面から閉じる**(人間の裁定)。

### 実装ステップ 1〜17

**全 17 ステップ完了。** 11-3 の帰属先検査の指摘を **54 → 0 件**にし、`attribution-destination` を有効化した。

| # | ステップ | コミット | 経路 |
| --- | --- | --- | --- |
| 1 | 両正本の in-review 化と改訂枠 | `6407b31` | Codex(強制終了後に成果残存) |
| 2 | 参照規約 2-6 を 8 カテゴリへ拡張 | `b2382f3` | Codex |
| 3 | 要件書 v2.7 — `NFR-020` の条文化 | `b30e7df` | Codex |
| 4 | `Q2`/`Q2-a` の矛盾解消 | `c9a12e3` | **直接 + review(可決 0 件)** |
| 5 | 4-5 r4 と 6-3 の非対称 | `cef0f1e` | Codex |
| 6 | `FR-013` の典拠射程 + テナント粒度 | `b5927f8` | Codex |
| 7 | `U1`〜`U6` へ undo 2 条項 | `66804cf` | Codex |
| 8 | 時刻記述を禁止の範囲へ | `f4b8461` | Codex(強制終了後に成果残存) |
| 9 | 11-4 の整備・版番号の除去 | `3b79ba0` | Codex |
| 10 | 11-3 是正 ① FR/NFR 12 件 | `bb00416` | Codex |
| 11 | 11-3 是正 ② 6 件 | `962da2d` | Codex |
| 12 | 11-3 是正 ③ 章節 6 + 付録 2 | `2cd78d5` | **直接 + review(否決 P1 3)** |
| 13 | 11-3 是正 ④ ブロック 5 + 指標/リスク 5 | `2bff445` | Codex |
| 14 | 11-3 是正 ⑤ 表行 12 | `21a8d49` | **直接 + review(否決 P1 1)** |
| 15 | 11-3 是正 ⑥ DoD 5 + 10章 1 | `bcedb7c` | **直接 + review(否決 P1 2)** |
| 16 | **`attribution-destination` の有効化** | `cf2be73` | 直接 |
| 17 | 申し送りの記録 | 本コミット | 直接 |

### 復元不能として残すもの — `U2-a` / `R5` の反転

- 候補案では「**`R5` は gap より先のイベントを拒まず保存する**」「`U2-a` は **drain する・要求の境界で止めない**」
  (`sync-protocol-design/design.md:516`・`:731`・`:735-737`)
- approved v0.1 では「**要求を越えて保存もしない**」「**要求間の drain は行わない**」(`sync-protocol.md:649`・`:1118`)
- **確定ゲート 10 周の反映記録**(`sync-protocol-canonical/plan.md` の実装ステップ 25〜42・当該 worklog)に
  **`U2-a`・`R5`・drain・相殺・undo を主題とするステップが 1 つも無い**
- → **記録から特定できない。本タスクでは埋められないため、復元不能として残す**


## 確定ゲート(設計書 7.3)

**適用版(7.3-1)**: ハーネス設計書 **v1.13**(approved)・条文コミット **`9768edf`**。
初回敵対レビュー実行時点で暫定記録と一致することを確認済み(v1.13 のまま・第一親が記録どおり)。
**複数正本の一括検証だが記録はゲート単位で 1 件。**

### 1 周目(全文レビュー・`sol xhigh`)— 否決 P0 0 / P1 8 / P2 2 → **全 10 件を採用**

| # | 重大度 | 起因 | 分類 | 指摘の要旨 | 採否と反映 |
| --- | --- | --- | --- | --- | --- |
| 1 | P1 | 起因 | (A) | `NFR-020` の前提 2〜4 は**何との同一かと判定主体が未定義**。Web Locks は同一 storage bucket 内しか調停せず、別プロファイル・別 private session は**ページから観測できない**。「観測不能なら常に禁止」と「現在の bucket 内だけ確認して許可」で挙動が分岐する | **採用**。**人間の裁定(2026-09-06)= 観測可能性で分ける**。要件書へ「前提の観測可能性による区別」を新設し、**1・5・6 は観測できるので不成立時に記録を拒む**、**2〜4 は選出が及ぶ調停範囲を定める前提であり、満たさない環境は本要件の保証対象外**とした |
| 2 | P1 | 起因 | (-) | 「満たされていない前提を**すべて**表示する」Must に対し、測定方法は 1 つずつ不成立にする試験しか要求しておらず、**最初の 1 件だけ表示する実装も通過する** | **採用**。測定方法へ「**2 つ以上を同時に不成立にし、不足している前提がすべて列挙されること**」を追加 |
| 3 | P1 | 起因 | (-) | `FR-038` の帰属先 `2-3` は、用語「隔離」を D8 と区別する**裸の `(FR-038)`** だけで同期規則の入力ではなく、**2-6 のリンク規約にも従っていない**。`10-3` 側だけで `any()` 検査が通る抜け道 | **採用**。**`2-3` を落として `10-3` のみ**に |
| 4 | P1 | 起因 | (-) | `6.1/毎球データ` を `4-3・8-1` の境界入力にした記述が**対象を過大化**。4-3 は交代・その場登録・墓標・P3 を含む全イベント契約、8-1 は T5〜T9 まで含む。**別データ行との境界が崩れている** | **採用**。4-3 は「**V7 のペイロードが保存される先**」、8-1 は「**T1〜T4 が原子的に確定する確定プレイの行**」に限定し、**他のデータ行に対応するものは本行の対象外**と明記 |
| 5 | P1 | 起因 | (-) | `6.1/管理者操作ログ（Should）` の帰属先 `6-5` が**データ行を紹介するだけ**で、B6 の非開示判定に日時・操作種別・対象を使っていない | **採用**。「**B6 で拒否した管理コンソール経由の操作も 3 項目で記録され得る**。本書は記録の要否を決めず、**拒否の事実が記録対象になり得ることだけを境界として受ける**」へ |
| 6 | P1 | 起因 | (-) | `6.1/認証情報` の**内容を取り違えている**。要件行は**チーム/管理者のパスワードハッシュ**だが、7-2 は端末側の「認証の保持物」としてキュー状態節に置いた。**パスワードハッシュとクライアント認証トークンの混同**。かつ併記された `6-3` に当該 ID の参照が無い | **採用**。**7-2 の記述を撤回**し、帰属先を **`6-3` のみ**へ。6-3 の B5(認証失効)へ「**パスワードハッシュの変更による即時失効**」として根拠を置いた |
| 7 | P1 | 非起因 | (-) | `U-9` の受け取り先が「**後続の同期プロトコル設計改訂**」という相対表現で、タスク ID・パス・確立済み手続のいずれもなく追跡できない | **採用**。**人間の裁定 = 先に起票して ID で名指す**。**`TSK-326`**(11-3 の全帰属先ペアの意味判断と `STABLE_ID_RE` の拡張)を起票し名指した |
| 8 | P1 | 起因 | (-) | `U-11` も受け取り先が「要件改訂タスク」だけで実在先を特定できない | **採用**。**`TSK-327`**(要件書改訂 — イベントが時刻属性を持つことの条文化)を起票し名指した |
| 9 | P2 | 起因 | (-) | 10-2 に壊れた強調 `成立する。****(a)` | **採用**。`**(a)` へ是正 |
| 10 | P2 | 非起因 | (-) | 11-3 が「**要件書 v2.4 の安定 ID**」を全数割り当てると記録しており、v2.7 で一括確定する本改訂と来歴が不整合 | **採用**。「**要件書の安定 ID**(母集合の正は `req-universe.json`。本改訂 v0.3 の時点で要件書 v2.7 と ID 集合は一致)」へ。**`req-universe.json` は「抽出器より先に固定した期待値」であり書き換えない** |

**不採用: 0 件。** **(B) 分類: 0 件。**

**副次の是正**: 受け取り先の表に行を 1 つ足したため 11-4 の `r5` → `r6` にずれ、台帳キー 10 行を追随させた。

### 2 周目(差分再レビュー・`sol xhigh`)— 否決 P0 0 / P1 3 / P2 0 → **全 3 件を採用**

**添付した採否記録の抜粋**(7.3-5): 終結済み一覧 = なし / 未終結の不採用一覧 = なし(1 周目は全件採用)。
**レビューは `4c041f0` の blob に固定して判定**し、作業中の未コミット変更を除外した。

| # | 重大度 | 起因 | 分類 | 指摘の要旨 | 採否と反映 |
| --- | --- | --- | --- | --- | --- |
| 1 | P1 | 起因 | (-) | **`Q2-a`・`Q2-d` が依然「6 前提のどれかが不成立なら記録拒否」**とし、`Q2-r3` も**観測不能な前提 4 を `Q2-d` で拒否できる**としている。1 周目で要件書を 1・5・6 に限定したことと**正本間で矛盾**する | **採用**。`Q2-a`・`Q2-d` は**レビュー実行前に自分で気づいて是正済み**(観測できる 3 前提へ限定・調停範囲の前提を区別)。**`Q2-r3` は未対応だったため本周で是正**し、「同一 storage bucket は調停範囲を定める前提であり範囲外は保証対象外。本契約は範囲内の待機側についてのみ成立する」へ |
| 2 | P1 | 起因 | (-) | **`6.1/毎球データ` の限定が成立していない**。4-1 は墓標・改訂・P3 を「6.1 の別のデータ行」としたが**6.1 にその受け皿は無く**、P3 には**毎球データを対象とするプレイ修正・論理削除**が含まれる。8-1 の「T5〜T9 が扱う退避・変更イベント」も、**T5 は D2 処理・T6 は全受理経路の確定結果・T9 は内容拒否**であり**イベント種別の区分ではない** | **採用**。**他種別への割り当てを主張する形をやめた**。4-3 は「**墓標・改訂・変更イベントはこの行を無効化または書き換える操作であって別のデータ種別ではない**」+ 交代とその場登録の保存先だけを名指す形へ。8-1 は「**本節の経路が確定プレイとして書く行**」とし、**T5〜T9 についての主張を落とした** |
| 3 | P1 | 起因 | (-) | **管理者操作ログの義務が内部矛盾**。同じ段落で「3 項目で記録される」と「記録の要否を決めず記録対象になり得るだけ」を併記し、**監査表はさらに「サーバー側のログには記録する」と断定**している。Should 採用時に B6 拒否も必ず対象に含めるのか候補なのかを実装者が決められない | **採用**。**監査規則の断定に揃えた**。「**本節の監査規則が定める『サーバー側のログには記録する』対象に B6 拒否を含める**。記録の項目は 6.1 の 3 項目に従う。**当該データ種別の保持そのものは Should であり、採用しない場合は本規則の記録先が存在しない**」へ |

**不採用: 0 件。** **(B) 分類: 0 件。**

**自己是正(レビュー前に発見)**: 1 周目で要件書だけを 1・5・6 へ限定し、**設計 `Q2-a`・`Q2-d` を 6 前提のまま残していた**。
片方の正本だけを直して正本間に矛盾を残す型であり、**本タスクが是正している欠陥そのものを自分で作りかけた**。

### PO スコープ裁定(7.3-6)— 2026-09-06

**エスカレーション条件が 2 周連続で成立**したため PO へ上げた(母集団 = 当該周の P0+P1 件数):

| 周 | 母集団 | 起因 | `起因 × 2 > 母集団` |
| --- | ---: | ---: | --- |
| 1 周目 | 8 | 7 | 14 > 8 ✅ |
| 2 周目 | 3 | 3 | 6 > 3 ✅ |

**PO 裁定 = 続行の明示指示。** 7.3-6 の表により **編集なし・カウンタなし**で、
**連続カウントを 0 にリセット**して差分再レビューを続行する。

**判断の材料**(提示した内容): P0 は全周ゼロ / P1 は 8 → 3 と半減以下 /
指摘はいずれも本改訂が入れた記述の精度に関するもので**新しい領域へ広がっていない** /
取り下げた方がよい項目は見当たらない。

### 3 周目(差分再レビュー・`sol xhigh`)— 否決 P0 0 / P1 3 / P2 0 → **全 3 件を採用**

**PO 裁定後の最初の周。** 添付した採否記録の抜粋: 終結済み = なし / 未終結の不採用 = なし(全周とも全件採用)。

**3 周連続で同じ 3 箇所**(`6.1/毎球データ`・7-4 の前提・管理者操作ログ)に指摘が出た。
台帳 **`H-68`**(修正が指摘を生む螺旋)の型であり、**個別にパッチを当てるのをやめ「書きすぎを削る」方向**で閉じた。

| # | 重大度 | 起因 | 分類 | 指摘の要旨 | 採否と反映 |
| --- | --- | --- | --- | --- | --- |
| 1 | P1 | 起因 | (-) | **墓標・改訂・変更イベントの扱いが既存契約と矛盾**。三者を一律に「確定プレイ行を無効化または書き換える操作」としたが、**P3 は新しい変更イベントを追記し既存を書き換えない**契約であり、**墓標・改訂は内容拒否された既存キュースロットを置換するもので確定済みプレイの変更には使わない**。各イベント自身の履歴保存先も確定せず、**上書き実装と追記実装に分岐する** | **採用**。**主張そのものを撤回**した。「**本書が定めるのはイベントが持つ必須値までであり、どの表へどう保存するかは決めない**(4-1)。墓標・改訂・変更イベントおよびそれらと対象行の関係は、**11-2 の影響差分として TSK-250 が扱う**」へ |
| 2 | P1 | 起因 | (-) | **`Q2` の導入文が観測可能性の限定に追随していない**。「前提を満たさない環境では `Q2-d` により記録自体を開始させない」が**全 6 前提に読める断定**のまま残っている | **採用**。「**観測できる前提**を満たさない環境では」+「**選出が及ぶ調停範囲の中で**単一書き手が崩れたまま記録が続く状態は生じない(**範囲外は `Q2-a` のとおり保証対象外**)」へ |
| 3 | P1 | 起因 | (A) | **管理者操作ログの Should 未採用時に監査契約が成立しない**。B6 拒否を記録対象に含めると断定しつつ「未採用時は記録先が存在しない」とし、一方で監査表は**無条件に**「サーバー側のログには記録する」を要求している。**未採用を許すなら監査を条件化、監査を必須とするなら記録先を Must にする方式判断**が要る | **採用**。**方式判断は本書では決めない**。記述を「**監査規則が参照する記録項目は 6.1 の 3 項目である**。**本書は記録の対象範囲を決めない**」へ最小化し、**`U-12` を新設して要件改訂タスクへ送った**。**本改訂が作った矛盾ではなく、顕在化した既存の緊張**である旨も記録 |

**不採用: 0 件。** **(B) 分類: 0 件。**

**方針の転換**: 1〜2 周目は「不足を補う」方向で反映していたが、**それが次の周の指摘を生んでいた**。
3 周目は **(a) 過剰な主張を撤回する (b) 決められないものは受け取り先つきで送る**の 2 つに限った。

### 4 周目(差分再レビュー・`sol xhigh`)— 否決 P0 0 / P1 1 / P2 0 → **採用**

**これまで争点だった箇所はすべてクリアされた**(レビュアの明示): 4-3 と 8-1 による毎球データの帰属 /
7-4 と `NFR-020` の観測可能性の区別 / `U-1`〜`U-12`(`U-6` 欠番)の行集合 / 11-5 の追加行。

| # | 重大度 | 起因 | 分類 | 指摘の要旨 | 採否と反映 |
| --- | --- | --- | --- | --- | --- |
| 1 | P1 | 起因 | (-) | **`U-12` の送り先が実効化されていない**。11-4 は受け取り先の名指しを要求するが「**要件改訂タスク**」という追跡不能な相対表現のままで、**受け取り先ごとの整理にも `U-12` が欠落**している。**同じ欠陥は 1 周目の `U-11` で P1 と判定され `TSK-327` の名指しに直した実績がある** | **採用**。**`TSK-328`**(要件書改訂 — 監査規則の無条件要求と管理者操作ログ Should の衝突)を起票し、**未解決表と受け取り先ごとの整理の両方**で名指した |

**不採用: 0 件。** **(B) 分類: 0 件。**

**自己の一貫性の欠如**: 3 周目に `U-12` を新設した際、**同じ周に `TSK-326`・`TSK-327` を名指していた**にもかかわらず
相対表現で書いた。**1 周目に指摘され是正した型を、同じゲートの中で繰り返した**。

**射程外として触らなかったもの**: `U-3` の受け取り先「要件改訂タスク」は**改訂前から存在する既存項目**であり、
本改訂が触っていない。**本改訂で新設・変更した行(`U-9`・`U-10`・`U-11`・`U-12`)のみを名指しに揃えた**。

**副次の是正**: 受け取り先の表に行を 1 つ足したため `r6` → `r7` にずれ、台帳キー 10 行を追随させた。

### 5 周目(差分再レビュー・`sol xhigh`)— **可決 P0 0 / P1 0 / P2 0**

**指摘なし。** 7.3-2 の遷移表により**カウントしない**(指摘ゼロの収束確認周)。**最終全文確認周へ進む。**

レビュアが確認した内容:

- **`TSK-328` は未解決表・受け取り先整理表で名称まで一致**
- **台帳は該当 10 行のみ `r6` → `r7` に追随し、漏れや余計な変更なし**
- **`U` 項目は `U-6` 欠番を含め、意図した複数送り先も含めて過不足なし**
- **`U-3` は改訂前から不変で、本差分レビューの射程外とする判断は妥当**

**確定ゲート周回は 4 のまま**(採用反映を伴わないため)。

### 最終全文確認周(1 回目)— 否決 P0 0 / P1 4 / P2 0 → **3 件採用・1 件不採用**

**4 件すべて `非起因`**。**本改訂の反映が作った欠陥ではなく、全文確認が拾った既存の記述**である。

| # | 重大度 | 起因 | 分類 | 指摘の要旨 | 採否と反映 |
| --- | --- | --- | --- | --- | --- |
| 1 | P1 | 非起因 | (-) | **記録権照合を「段階④」としているが、処理段階表では ④ が D5 照合・⑤ が記録権照合** | **不採用(誤検知)**。**「記録権照合を段階④」とする記述は本文に存在しない**。処理段階表は `④ D5 の照合`・`⑤ 記録権(D4・V12)` で正しく、`:694`「③ 認可は ④ D5 照合より必ず先」とも整合する。指摘が引いた行は **6-3 の 2 軸の例示表**(D3 の前進と境界結果)であり**段階番号を扱っていない**。`grep` で「記録権」と「段階/④」が同一行に現れる箇所を全数確認し、該当なしを確かめた |
| 2 | P1 | 非起因 | (-) | **7-2 と 11-2 が端末内保持物を一律に「未同期キュー」としている**。要件書は未同期キューと P3 受理結果を**別データ種別**とし、**P3 はキューにも通常再送にも載せない**。設計自身の分離規則(`:1032`)とも矛盾する | **採用**。**私の追記が過大だった**。両節に **`6.1/P3受理結果の端末保持`** を併記し、「**両者は別のデータ種別であり、P3 の受理結果はキューにも通常の再送にも載せない**」と明記 |
| 3 | P1 | 非起因 | (-) | **10-2 の改訂適用シナリオが「D3 は 5 → 7」**としているが、**B3 規則では `D1=5` を拒否した時点で D3 は直前の 4 で停止**する。**故障系テストのオラクルが本文規則と矛盾** | **採用(射程拡大)**。**PO スコープ裁定 2026-09-07**(7.3-6)で射程へ追加。`4 → 7` へ是正し、**射程宣言に⑨として追記**。**次周は全文レビュー**(7.3-6 の表) |
| 4 | P1 | 非起因 | (-) | **`8章DoD/3` が「同期側で決める」だが、対応先 10-2 は実戦リハーサルの内容を要件書から参照するだけ**。区分定義に従えば「境界として参照」で、**集計の追随も必要** | **採用**。区分を**境界として参照**へ変更し、集計を **37 / 83 / 92** へ。**ハードコードするテスト 2 本も追随** |

**不採用: 1 件**(#1 — 誤検知。根拠は上記)。**(B) 分類: 0 件。**

**PO スコープ裁定(7.3-6 — 2026-09-07)= 射程拡大。** 指摘 #3 は**本改訂が触っていない既存欠陥**だが、
**本文規則とテストオラクルが矛盾したまま approved になることを避けるため取り込んだ**。
**変更範囲と理由は変更履歴の射程宣言⑨と本記録に残す。次周は全文レビュー。**

### 射程変更直後の全文レビュー周 — 否決 P0 0 / P1 1 / P2 1 → **全 2 件を採用**

| # | 重大度 | 起因 | 分類 | 指摘の要旨 | 採否と反映 |
| --- | --- | --- | --- | --- | --- |
| 1 | P1 | 非起因 | (-) | **6-3 の例示が「記録権の照合は段階 ④」と書くが、処理段階表では ④ が D5 照合・⑤ が記録権照合**。段階番号が食い違い**実装者が照合順を一意に読めない** | **採用**。`④` → `⑤` へ。「**D1 の処理より前**」という結論は ⑤ < ⑥ なので維持される |
| 2 | P2 | 非起因 | (-) | **2 軸の説明表が第 1 軸の行で終わり、空行と本文を挟んで第 2 軸の行が孤立**している。**Markdown で表としてレンダリングされない** | **採用**。第 2 軸の行を第 1 軸の直後へ移して表を閉じた。**11-5 の連番は動いていない**(`ledger` green・既存行の削除 0) |

**不採用: 0 件。** **(B) 分類: 0 件。**

#### 前周の不採用 1 件について — **維持(再指摘)を受け、判断を撤回した**

**前周で「誤検知」として不採用にしたのは誤りだった。** レビュアの「維持(再指摘)」は正しい。

- **当該文言は実在する**: `:841`「**B4 は含めない** — **記録権の照合は段階 ④** で **D1 の処理より前**に行われるため、**B4 では D3 が前進しない**(6-3)」
- **不採用の根拠にした検証が誤っていた**。`grep` の出力を **`head -6` で打ち切った**まま
  「該当なし」と断定した。`:841` はそれより後ろのマッチだった
- **「全数確認して該当なし」と worklog に書いたが、実際には全数を見ていない**

**7.3-5 の「維持は当該指摘の不採用 2 周目として扱う」規定には至らず、本周で採用して解消した。**

### 差分再レビュー(6 周到達の PO 判断後)— **可決 P0 0 / P1 0 / P2 0**

**指摘なし。** 7.3-2 によりカウントしない。**最終全文確認周を再実施する**
(前回の最終全文確認周で採用反映があったため — 7.3-2「採用反映があった場合は最終全文確認周を再実施する」)。

レビュアが確認した内容: 段階⑤が記録権照合・段階⑥が D1 連続性検査であり **B4 では D3 が前進しない**結論と整合 /
**2 軸表は正常に閉じられ**単一 `(試合, D4)` の説明も表直後で意味を維持 /
**11-5 は親コミットと見出し位置が同一で連番への影響なし** / 6-1・6-3 の既存規則との新たな矛盾なし。

**確定ゲート周回は 6 のまま**(採用反映を伴わないため)。

### 最終全文確認周(2 回目)— 否決 P0 0 / P1 2 / P2 0 → **全 2 件を受け取り先つきで送付**

**2 件とも `非起因`** で **v0.1 から存在する既存欠陥**。**私の射程内の項目はすべてクリア**された
(D3 の `4 → 7` / 段階⑤への是正 / P3 の別データ種別化 / 11-3 の集計 `37 / 83 / 92 = 212` /
`U-9`〜`U-12` の受け取り先 / frontmatter・索引の in-review 状態)。

| # | 重大度 | 起因 | 分類 | 指摘の要旨 | 採否と処理 |
| --- | --- | --- | --- | --- | --- |
| 1 | P1 | 非起因 | (-) | **部分成功したイベントを同期済みへ遷移できない**。7-2 は「未送信 → 同期済み」を **`B1` に限定**するが、同じ 7-2 が「**D1 付きキュー遷移の根拠は A5 だけ**」と定める。**A5 はイベント単位**で `B2`/`B3` より前は確定できるため、**拒否より前のイベントが未送信のまま残る** | **採用(送付)**。**`U-13` を新設し `TSK-329` へ** |
| 2 | P1 | 非起因 | **(A)** | **保存済みの退避結果の再掲が `B1` と `B4` に分岐**。6-3・7-2 は `B1` の経路を明記、7-1・再送手順・故障系は一貫して `B4`。**選択規則がない** | **採用(送付)**。**`U-14` を新設し `TSK-329` へ** |

**不採用: 0 件。**

#### PO スコープ裁定(7.3-6 — 2026-09-07)= **範囲確定**

**最終全文確認周が 2 周連続で `v0.1` 由来の既存欠陥だけを拾う**状態になったため、
**「`v0.1` 由来の既存欠陥は本改訂では直さない。発見したものは 11-4 へ受け取り先を立てて送る」**と
**射程を確定**した。**射程宣言(変更履歴行)に記録済み。**

**判断の理由**: `U-14` は **(A) 方式が分岐する指摘**で、**同期プロトコルの中核**(`B1`/`B4` の使い分け)に触れる。
変更すると `R-QUEUE-LIFE`・`R-ACK-STATE`・`R-BOUNDARY` と 2-5 宣言表、
`frontend/src/lib/sync/queueTransition.ts`(727 行)まで波及する。
**十分な分析なしに 9 周目の終わりで決めるべきではない。**

### 射程確定後の全文レビュー周 — 否決 P0 0 / **P1 0** / P2 1 → **採用**

**P0 と P1 がともにゼロになった。** 射程確定が効き、`v0.1` 由来の既存欠陥は
**射程外・送付対象**として扱われ指摘件数に算入されていない。

| # | 重大度 | 起因 | 分類 | 指摘の要旨 | 採否と反映 |
| --- | --- | --- | --- | --- | --- |
| 1 | P2 | 非起因 | (-) | **`10章/NFR-018 の正解ベクタの整備` のリンク先に未エスケープの空白**があり、**Markdown リンクとして解釈されない**(`markdown-it-py 3.0.0` で実測)。2-6 の参照規約に対する表記上の不整合。導入は `bcedb7c`(ステップ 15) | **採用**。**リンク先を `<...>` で囲んだ**(Markdown の標準的な形)。**同型を全数走査し他に無いことを確認**(`H-79`)。**実測でリンクとして解釈されることと `href` が正しく URL エンコードされることを確かめた** |

**不採用: 0 件。**

**レビュアが確認した事項**: 射程拡大⑨の `D3: 4 → 7` は **B3 で 4 に停止した後、置換された `D1=5` と
未処理の 6・7 を再送する規則と整合** / `NFR-020` と `Q2`/`Q2-a`・11-3 の 54 件・11-4 の受け取り先に **P0/P1 なし** /
**`U-13`・`U-14` は射程外・送付対象として `TSK-329` に記録済みで指摘件数に算入していない** /
3 検査すべて rc=0 / 未終結の不採用一覧なし。

### 最終全文確認周(3 回目)— **可決 P0 0 / P1 0 / P2 0** → **収束**

**前周に採用反映(P2 1 件)があったため 7.3-2 により再実施した**(approved になる版そのものを全文確認する)。
対象は **HEAD `19176d1`** の両正本全文。**指摘ゼロ・未終結の不採用ゼロ**で収束した。
**採用反映がないため `確定ゲート周回` は数えない**(8 のまま)。

**レビュアが確認したと明記した事項**: 射程⑨の `D3: 4 → 7` は **B3 が拒否直前の水位 4 で停止し、
同一 D1 の墓標・改訂と後続再送で 7 まで進む規則と整合** / `NFR-020` と設計 `Q2`・`Q2-a`〜`Q2-e` が
**観測可能性・不成立時の停止・キュー保持・persist 拒否時の継続まで一致** / **11-3 の 212 件全数帰属**・
11-5 台帳・11-4 の受け取り先・前周のリンク修正がいずれも有効 /
`check_doc_profiles`・伝播検査・帰属検査・正本状態検査・`git diff --check` すべて成功 /
**射程外・送付対象の `U-13`・`U-14` は `TSK-329` への記録を確認**・新たな射程外事項なし。

### ゲート全周の集計(7.3 手順 5 で人間へ提示したもの)

| 周 | 種別 | 判定 | P0 | P1 | P2 | 採用 | 不採用 |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: |
| 1 | 全文(初周) | 否決 | 0 | 8 | 2 | 10 | 0 |
| 2 | 差分 | 否決 | 0 | 3 | 0 | 3 | 0 |
| 3 | 差分 | 否決 | 0 | 3 | 0 | 3 | 0 |
| 4 | 差分 | 否決 | 0 | 1 | 0 | 1 | 0 |
| 5 | 差分 | **可決** | 0 | 0 | 0 | — | — |
| 6 | 最終全文(1 回目) | 否決 | 0 | 4 | 0 | 3 | 1 → **撤回して採用** |
| 7 | 全文(射程拡大直後) | 否決 | 0 | 1 | 1 | 2 | 0 |
| 8 | 差分 | **可決** | 0 | 0 | 0 | — | — |
| 9 | 最終全文(2 回目) | 否決 | 0 | 2 | 0 | 2(送付) | 0 |
| 10 | 全文(射程確定直後) | 否決 | 0 | 0 | 1 | 1 | 0 |
| 11 | 最終全文(3 回目) | **可決・収束** | 0 | 0 | 0 | — | — |
| **累計** | | | **0** | **22** | **4** | **25** | **0**(最終) |

- **全 11 周を通して P0 はゼロ。**
- **不採用は最終的に 0 件**。6 周目に 1 件を不採用としたが、**維持(再指摘)を受けて判断を撤回し採用**した
  (自分の全数確認が `head -6` による切り詰めで誤っていた — 同節に記録済み)
- **PO 裁定 3 回**: 続行の明示指示(2026-09-06)/ **射程拡大**(2026-09-07・`D3: 4 → 7`)/
  **範囲確定**(2026-09-07・`v0.1` 由来の既存欠陥 2 件を送付対象とする)
- **`確定ゲート周回` = 8** — 採用反映を伴った周(**1・2・3・4・6・7・9・10**)のみを計上した。
  指摘ゼロで可決した 5・8・11 周は数えない(/finalize-doc 手順 4)。**反映周コミット 8 本と一致する**
- **射程外へ送った項目の受け取り先**: `TSK-326`(`U-9` 残余 102 ペア + `STABLE_ID_RE` 拡張)/
  `TSK-327`(イベントの時刻属性)/ `TSK-328`(監査規則と管理者操作ログ Should の衝突)/
  `TSK-329`(`v0.1` 由来のキュー状態遷移の矛盾 `U-13`・`U-14`)

## 結果サマリ(/pr クローズ処理)

### 何を正本へ反映したか

**同期プロトコル設計 `v0.1 → v0.3`(approved)**

- **参照規約 2-6 を 4 → 8 カテゴリへ拡張**(ブロック・識別付き表行・キー付き表行・8 章 DoD)。
  あわせて **11-5 の列定義を同一コミットで整合**させた(両者が同じカテゴリ列挙を持つため)
- **`attribution-destination` 検査を有効化**した。無効化理由「同期正本には対応する資産が無く」は
  **成り立っていなかった**(当該検査は文書と帰属表だけで動く)。有効化は 4 点同時
  (profile の `required_checks` 追加 + `not_applicable` 削除 / `registry.json` の `must_require` /
  `pins.profile_gating_digest` 再計算 / テストの書き換え)
- **11-3 の要件帰属を 54 件是正し、212 件を全数確定**した(37 / 83 / 92)。
  是正は行ごとに (a) 割り当てを直す (b) 本文に根拠行を足す (c) 対象外へ移す を裁定した
- `Q2`・`Q2-a`〜`Q2-e` を新しい `NFR-020` 条文へ整合 / 4-5 r3・r4 と 6-3 の非対称を是正 /
  `FR-013` の典拠表記を分離 / `U1`〜`U6` へ undo 2 条項 / `U7`・`U8` 追加 /
  11-4 に `U-10`〜`U-14` を追加し**版番号を 5 箇所から除去** / 6-1 の 2 軸表を閉じる /
  6-3「記録権の照合は段階 ④ → **⑤**」/ 10-2 の `D3 期待値 5 → 7` を **`4 → 7`** へ

**要件定義書 `v2.6 → v2.7`(approved)**

- `NFR-020` へ 3 条項: **前提の観測可能性による区別**(1・5・6 は観測可能 / 2〜4 は調停範囲を定める前提で
  保証対象外)/ **選出前提の不成立時**(fail-closed。記録を開始させず、不足前提をすべて表示し、
  キューは破棄せず、閲覧は許す)/ **前提 5 と永続化保証の切り分け**
- 測定方法を拡張(2 つ以上を同時に不成立にして全列挙を確認する)/ 主語を
  「記録画面は」→「**記録を行うブラウザ環境が**」へ是正

### 正本体系外

- `scripts/design_relations/profiles/`(検査の有効化)/ テスト 4 ファイル 12 箇所 /
  **`contracts/authz/` 15 ファイル**(要件書 v2.7 への追随・再 seal)

### 実装の経緯で特筆すること

- **`H-85` は要件書に正面から効いた**。ステップ 1 で要件書を触った時点で
  `test_repository_catalog_covers_the_entire_requirements_file` が red になり、
  **approved 化の後に再 seal する**クローズ処理を計画へ組み込んだ(全ステップの合格条件を
  「**既知 red 1 件を除いて green**」に書き換えた)
- **再 seal の手順は前例コミット 2 本(`dfd523a`・`027d0ac`)を読んで復元した**。
  `--reseal` 系フラグは `input_manifest` も `oracle_commit` も更新しない(→ 台帳の候補へ)
- **`source_text_digest` は一意でない**(1073 行中 76 件重複)ため、追随は
  **旧版原文 `dc9d114` との位置合わせ**で行った。既存 1073 行が位置で 1:1 一致し、
  `source_id` の導出規則(見出し+kind ごとの文書順連番)も全行で一致することを先に検証した。
  **既存行の `source_id` は 1 件も動いていない**
- **Codex の強制終了が通算 23 回**。うち再 seal の委任は 2 回とも出力ゼロで即死したため、
  **再実行上限 2 回**の運用に従い Claude 直接実装へ切り替え、**ラッパー経由の通常レビューで可決 0 件**

### 台帳への追記

**該当あり** — `## 候補` へ **2 件**(`H-85` の連鎖の実行手順が無い / `source_text_digest` が一意でない)、
既存候補(長時間実行の強制終了)へ**実測 1 件**、変更履歴へ 1 行。
**`H-*` の新規採番はしていない・版は上げない**(7.6-3 前段)。

### 残っているもの(すべて人間の作業)

1. **人間の逐行確認**(コア領域 4 領域該当・**PR 作成者以外**)。CI が `- [x]` を機械検査する
2. **実施記録行の記入**(`対象= 範囲= 方法=`)
3. **マージ**

**逐行確認の優先度順**: ① **機械が担保しない 6 箇所**(`verification-sheet.md` の A 節に先頭配置)
② 11-3 の変更 18 行 ③ `NFR-020` の 4 条項 ④ 全数版 212 行(`verification-sheet-full.md`)。
**検査は素のテキストの literal 一致で通る**ため、帰属の実質はここでしか担保されない。

## 未決・次の一歩

- **射程が 3 件から増えている**。TSK-280 の申し送り 3 件に加え、後続 α・後続 β で見つかった項目がある。
  **/investigate で全数を確定してから**カード本文と DoD を計画書と同期する(TSK-280 の前例)
- 版を繰り上げるのか、`7.6` 決定表の「実装追随の節更新・変更履歴追記 = PR レビューで可」に収まるのかの
  切り分けが必要。**項目ごとに判定が割れる可能性がある**
- 次の導線: /investigate → /plan

### 次タスクへの申し送り: Codex の astra を使えるようにする(人間の指示 2026-09-06)

**人間の指示**: 「今後なにか難しいタスクに当たった際には codex の astra を使用してもいい。
使用する際には人間に明示して」→ **本タスクには持ち込まず次タスクで対応**(同 2026-09-06)。

**現状では起動経路が無い**(調査済み):

- `ADR-001`(モデル対応表の**正本**)は GPT-5.6 世代の 3 tier(`sol` / `terra` / `luna`)+ 旧 spark のみ。
  **`astra` の記載なし**
- `.claude/scripts/codex_run.py:31-40` の `MODEL_MAP` / `REVIEW_*` が**モデル ID をハードコード**し、
  計画書の重さ分類から自動選択する。**`astra` は選べない**
- 生の `codex` は `codex_guard` がブロックする(実際に `codex --version` も止まった)。
  **迂回はしない**

**必要な変更 2 件**:

1. **`ADR-001` の改訂** — 正本なので 7.6 のゲートに従う(版繰り上げなら 7.3 の確定ゲート)
2. **`codex_run.py` の `MODEL_MAP`** — どの作業区分に astra を割り当てるかを ADR に従って反映

**使用時の規律**: 人間の指示により、**astra を使うときは毎回その旨を明示する**。
