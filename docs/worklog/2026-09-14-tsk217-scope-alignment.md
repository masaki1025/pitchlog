---
date: 2026-09-14
topic: TSK-217 の射程を裁定 C へ追従させる(対象横断の判定とハーネスの所有)
branch: feature/tsk217-scope-alignment
---

# 作業ログ: 2026-09-14 TSK-217 の射程を裁定 C へ追従させる

## やったこと

## 決定

## 未決・次の一歩

## ステップ 1 — `TSK-380` からの分担の受諾(2026-09-16)

**受諾済み。判定者 = 人間(PO・山田正輝)。**

**`TSK-380` に担当セッションが存在しない**ため(`ListAgents` 実測 — 稼働中の 4 セッションはいずれも
`TSK-235` / `TSK-355` / `TSK-379` / `TSK-363` を持つ)、**カード上の担当者である人間が判定した**。
計画書のステップ 1 が定めた「受諾者 = `TSK-380` の担当セッション。**判定者 = 人間(PO)**」の後段による。

**受諾の証跡**: `TSK-380` のカードへコメント(`discussion://…3dc93b75-e687-8160-9379-001c65bb132f`)。
必須文面 3 項目をすべて含む。

| 担当 | 範囲 |
| --- | --- |
| **`TSK-380`** | 「前提・依存」節の訂正(**役割の宣言を含む**)/ 37 経路の `test_owner` の再割り当て方式 |
| **本タスク** | 「スコープ」節へ `contract_only` **10 owner** を ID で列挙 |
| **どちらにも入っていない** | **`cells` 12 / `claim-mutant-map` 7 行 / `boundary-proposal` 1** の再割り当て方式(`ADR-004:46` は 37 経路しか送っていない)→ **申し送り `#1`** |

### 受諾の過程で判断根拠が 1 つ増えた

**`TSK-380` のカードには既に「訂正後(案)」が書かれており、そこに「ハーネス」が入っていた。**

> 本タスクが持つのは**対象横断の判定**(認可行列の網羅・存在秘匿の同値性・既定拒否の検証)と**ハーネス**である。

**本計画の判断 ①(裁定 `C` 本文を正とし「判定 + ハーネス」を採る)を独立に裏づける。**
当初の根拠 3 点に**4 点目として加えた**。

**逆に、TSK-367 のカードへ入った裁定 `C` のコメントだけが「判定に限られます」で
「ハーネス」を落としていた**ことになる。**一次記録(worklog)と起案(TSK-380 の案文)が一致し、
要約(カードのコメント)だけがずれていた**という構図である。

### 射程を狭めた

**役割の宣言は `TSK-380` が書く**ため、本タスクは**書かない**。
**同じことが複数箇所に書かれると片方だけ古くなる** — 本計画が 5 周のレビューで繰り返し踏んだ型であり、
**ここで自ら再生産しない**。スコープ節からは**裁定 `C` を直接引く**(`TSK-380` の記述に依存しない)。
