---
date: 2026-09-14
topic: TSK-217 の射程を裁定 C へ追従させる(対象横断の判定とハーネスの所有)
branch: feature/tsk217-scope-alignment
---

# 作業ログ: 2026-09-14 TSK-217 の射程を裁定 C へ追従させる

## やったこと

## 決定

## 未決・次の一歩

## ステップ 1 — `TSK-380` からの分担の受諾(2026-09-16)

**受諾済み。判定者 = 人間(PO・山田正輝)。**

**`TSK-380` に担当セッションが存在しない**ため(`ListAgents` 実測 — 稼働中の 4 セッションはいずれも
`TSK-235` / `TSK-355` / `TSK-379` / `TSK-363` を持つ)、**カード上の担当者である人間が判定した**。
計画書のステップ 1 が定めた「受諾者 = `TSK-380` の担当セッション。**判定者 = 人間(PO)**」の後段による。

**受諾の証跡**: `TSK-380` のカードへコメント(`discussion://…3dc93b75-e687-8160-9379-001c65bb132f`)。
必須文面 3 項目をすべて含む。

| 担当 | 範囲 |
| --- | --- |
| **`TSK-380`** | 「前提・依存」節の訂正(**役割の宣言を含む**)/ 37 経路の `test_owner` の再割り当て方式 |
| **本タスク** | 「スコープ」節へ `contract_only` **10 owner** を ID で列挙 |
| **どちらにも入っていない** | **`cells` 12 / `claim-mutant-map` 7 行 / `boundary-proposal` 1** の再割り当て方式(`ADR-004:46` は 37 経路しか送っていない)→ **申し送り `#1`** |

### 受諾の過程で判断根拠が 1 つ増えた

**`TSK-380` のカードには既に「訂正後(案)」が書かれており、そこに「ハーネス」が入っていた。**

> 本タスクが持つのは**対象横断の判定**(認可行列の網羅・存在秘匿の同値性・既定拒否の検証)と**ハーネス**である。

**本計画の判断 ①(裁定 `C` 本文を正とし「判定 + ハーネス」を採る)を独立に裏づける。**
当初の根拠 3 点に**4 点目として加えた**。

**逆に、TSK-367 のカードへ入った裁定 `C` のコメントだけが「判定に限られます」で
「ハーネス」を落としていた**ことになる。**一次記録(worklog)と起案(TSK-380 の案文)が一致し、
要約(カードのコメント)だけがずれていた**という構図である。

### 射程を狭めた

**役割の宣言は `TSK-380` が書く**ため、本タスクは**書かない**。
**同じことが複数箇所に書かれると片方だけ古くなる** — 本計画が 5 周のレビューで繰り返し踏んだ型であり、
**ここで自ら再生産しない**。スコープ節からは**裁定 `C` を直接引く**(`TSK-380` の記述に依存しない)。

## ステップ 2 — カードの更新と引き渡し(2026-09-16)

### やったこと

**TSK-217 のカードの「スコープ」節へ `contract_only` 10 owner を ID で列挙した。**
**役割の宣言は書いていない**(`TSK-380` の射程 — ステップ 1 の受諾どおり)。
**スコープ節からは裁定 `C` を直接引いた**(`TSK-380` の記述に依存しない形)。

書いた内容は 4 点。

- **10 owner の ID と内容、および届くか否かの根拠**(**上の 9 項目は「概要」であり射程の定義ではない**ことを明記)
- **「ハーネス」は定義文が存在せず、運用例 2 件で暫定運用する**(定義の確定は PO 裁定へ)
- **既存 7 行との関係** — **7 + 10 = 17 で互いに素**
- **既存 7 行の割り当て根拠の訂正** — 「`404`/`400`/存在秘匿の同値性」ではなく
  `_has_db_decision()` による機械分類。**存在秘匿に当たるのは 1 行だけ**

### 終着区分つき裁定結果表

**終着区分**: **(a)** 既存正本アンカー / **(b)** 本 PR で正本へ反映 / **(c)** 正本パス・節・ゲートを DoD に持つ後続タスク。

| 群 | 内容 | 終着区分 | 送り先の実 ID |
| --- | --- | --- | --- |
| カードの射程記載 | 10 owner の列挙 | **(b) 相当**(正本ではなく Notion のタスク定義)| [TSK-217](https://app.notion.com/p/3bc93b75e68781f2a613d791db2231ab) |
| 役割の宣言・前提の訂正・37 経路 | | **(c)** | [TSK-380](https://app.notion.com/p/3d993b75e68781a2afa3e2c637cf4f50) |
| TSK-367 の文書是正 | 件数・内訳は未確定 | **(c)** | [調査タスク](https://app.notion.com/p/3dc93b75e68781b28214fe6438591c21)(**本タスクで起票**)|
| 「ハーネス」の定義 | | **PO 裁定** | — |

### 引き渡しの実値(**射程確認の結果を含む**)

| # | 送り先 | 追記 | 射程確認の結果 | 確認者 |
| --- | --- | --- | --- | --- |
| 1 | **TSK-380** | `discussion://…3dc93b75-e687-8160-9379-001c65bb132f` | **受諾済み**(2026-09-16・人間が判定)。**ただし `cells` 12 / `claim-mutant-map` 7 / `boundary-proposal` 1 はどちらの射程にも入っていない** — `ADR-004:46` は 37 経路しか送っていない。**同コメントで追記を依頼** | 山田正輝 |
| 2 | **調査タスク**(新規)| カード本文 | **起票時に受領範囲・開始条件・設計方針を本文へ記載**。射程行はカード本文「開始条件・受取先」節 | Claude |
| 3 | **TSK-217** | 本文の「スコープ」節 | 更新対象そのもの | — |

### 未了

- **`TSK-317`(PR #3)への申し送り `#4` `#5` `#8`** — **ステップ 2 では送っていない**。
  PR #3 の計画が起こされる時点で受領範囲を明記させる必要があり、**先方が未着手**のため。
  **`/pr` のクローズ処理で送る**

### 台帳への追記の判断(**`/pr` の手順 1-3 で確定する**)

**候補が 1 件ある** — **「是正の完全性を証明する機構を同一射程に入れると輪になる」の 3 例目**。
本タスクは**同じ構造で 2 度の射程縮小**を強いられた(3 周目に検証器ごと撤去・5 周目に内訳ごと撤去)。
**2 度目は「申し送りの内訳を列挙する義務」という形で残っていた**もので、
**既存の 2 例(TSK-348・TSK-363)には無い現れ方**である。
**`/pr` のクローズ処理で既存候補への実測追記として書くかを判断する。**

---

## 結果サマリ(`/pr` クローズ処理・2026-09-16)

### 何をしたか

**TSK-217 のカードの「スコープ」節へ `contract_only` 10 owner を ID で列挙した。**
**役割の宣言は書いていない**(`TSK-380` の射程 — 2026-09-16 に分担を受諾)。

**当初は TSK-367 の計画書の文書是正(射程 B)を同梱していたが、計画レビュー 5 周で全件撤去した。**

### 正本へ反映したもの

| 正本 | 内容 |
| --- | --- |
| `../development/harness-evaluation.md` | 既存候補「是正の完全性を証明する機構を同一タスクの射程へ入れると輪になる」へ**3 例目**。**対応案 (e) を追加**。変更履歴表へ 1 行(**版は上げない**)|
| `../README.md` | 台帳行へ本タスクの寄与を追記 |

**`H-*` は与えなかった** — 昇格判断は PO の事項。

### 台帳への追記を判断した記録(`/pr` 手順 1-3)

**該当する**と判断し、**既存候補へ 3 例目を追記**した。**新設はしていない。**

**3 例目が既存 2 例と違う点**: 1 例目は**機構**(検査コード)、2 例目は**散文**(計画書の中の証明)が
輪を作った。**3 例目は「射程から外したものの内訳を書く義務」が輪を作った。**
**対象を射程から外しても、その対象を列挙する義務が残れば同じ走査が続く。**
**「外す」と「数えない」は別の操作である。**

**既存の対応案 (a)(d) では拾えない**ため **(e) を追加**した —
(a) は「証明する機構」、(d) は「完全性の主張」を自己点検項目にするが、
**撤去後に残ったのは単なる列挙**でどちらにも当たらない。

### レビューの経過

**計画レビュー 5 周**(`codex_run.py review normal`)。**すべて否決 → 反映。不採用 0 件。**

| 周 | `P0` | `P1` | 起因 | 主な帰結 |
| --- | --- | --- | --- | --- |
| 1 | 2 | 2 | 0/4 | 是正 6 → 8 件 |
| 2 | 5 | 4 | 4/9 | **射程縮小**(13 → 5 件)|
| 3 | 5 | 3 | 7/8 | **射程 B を全件撤去**(検証器ごと)|
| 4 | 1 | 4 | 4/5 | 防御根拠の是正・`#7` の内訳化・停止条件の強化 |
| 5 | 2 | 3 | 5/5 | **申し送りの内訳ごと撤去** |

**4・5 周目が 2 周連続で起因過半**となり、**`7.3-6` のエスカレーション条件が成立**した。
**PO 裁定は 2 度とも「射程縮小」**(2026-09-14 に 2 回)。

**指摘は全件、原典で検証してから反映した。** とくに 1 周目の「`FR-041` ラベルは 17 → 15 が正しい」は
**行自身の列挙と `(+1)` 行の独立ラベルを混同していた自分の誤り**で、検算して確認した。

### 未了

- **`TSK-317`(PR #3)への申し送り `#4` `#5` `#8`** — **先方が未着手**で受領範囲を明記させられない。
  **本 PR のレビュー中に送る**
- **`TSK-380`** — `cells` 12 / `claim-mutant-map` 7 / `boundary-proposal` 1 の再割り当て方式が
  **どちらの射程にも入っていない**(`ADR-004:46` は 37 経路のみ)。**追記を依頼済み・対応待ち**
- **「ハーネス」の定義** — **PO 裁定待ち**(本タスクは運用例 2 件の列挙で暫定運用)
