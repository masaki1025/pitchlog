---
date: 2026-09-16
topic: TSK-355 の approved を受けた TSK-235 の再開(逐条突合・ステップ表 45 への再構成)
branch: feature/domain-calc-dsl
---

# 作業ログ: 2026-09-16 TSK-235 の再開

## やったこと

| # | 作業 | 結果 |
| --- | --- | --- |
| 1 | **`develop` へのリベース** | **395 コミット分・衝突 0**。本ブランチが触るのは `docs/features/domain-calc-dsl/**` と worklog 2 本のみで、いずれも `develop` に不在だったため |
| 2 | **〔正本〕20 命題の逐条突合** | **是正 5 件**(`A`〜`E`)。実質が崩れた命題は 0 件 — design.md §17 |
| 3 | **`P0-1` / `P0-2` の反映** | design.md §4 を封印集合の実装へ全面改稿 |
| 4 | **`P1-5` + 見直しトリガー 16 件目** | design.md §12-1(4 列追加)・§12-3(停止ゲート) |
| 5 | **`P1-1` / `P1-7` / `P0-2` のステップ再構成** | **29 → 45 ステップ** |
| 6 | **`P1-6` の DoD 再構成** | **`stepId` 対応表 45 行**。1 対 1 を機械検査する節を付けた |
| 7 | **行番号参照の一掃** | **69 件 → 0 件**。うち **23 件は表の区切り行・空行**を指していた |
| 8 | **`§16-12` 自己適用監査表の母集合是正** | **10 → 13 項目**。`16-8` と `16-11` が落ちていた |
| 9 | **`§16-8` の実施** | **「未解消件数」の 3 用途のうち報告は同値でない**(正本は**一覧**を要求)|
| 10 | **計画レビュー 4 周目の敵対レビュー** | **承認不可** — `P0` 1・`P1` 4・`P2` 2 / **起因 7・非起因 0** |
| 11 | **4 周目の全件反映** | **45 → 53 ステップ**。両表を [`steps.py`](../features/domain-calc-dsl/steps.py) から生成する形へ |
| 12 | **計画レビュー 5 周目の敵対レビュー** | **承認不可** — `P0` 1・`P1` 4・`P2` 1 / **起因 6・非起因 0** |
| 13 | **5 周目の全件反映** | **53 → 55 ステップ**。`check()` を負例 4 種で実証する形へ |
| 14 | **計画レビュー 6 周目の敵対レビュー** | **承認不可** — `P0` **0**・`P1` 7・`P2` 0 / 起因 7・非起因 0 / **新種 0・再発 7** |
| 15 | **PO 裁定(2026-09-16)= 計画レビューを 6 周で打ち切る** | **甲 = 7 件を是正して確認周なしで人間承認へ**。実効性は合成 fixture と CI 実測へ委ねる |
| 16 | **6 周目の全件反映** | **55 → 56 ステップ**。`PB_FALSE` を封印へ・解消述語を代数的型へ接地 |
| 17 | **人間承認** | **`承認: 済(2026-09-16・山田正輝)`**。残余 3 件を了承のうえ |
| 18 | **承認直後にステップ表の打ち切りを実測** | `#### 第 N 群` が表を 11 個に分断。**敵対レビュー 3 周が見逃していた**。群見出しを太字行へ |
| 19 | **`TSK-355` の指摘で §16-14 を訂正** | **新規候補ではなく台帳の既存候補への 2 件目の実発生**。原典で確認し二重計上を回避 |
| 20 | **2026-09-18: 着手前提の再測** | **`TSK-317` PR #3 は PR #66 でマージ済み**(2026-09-17)。**マージ順序の全 6 件が充足** |
| 21 | **2026-09-18: `ruff` 169 件を是正** | **`steps.py` の `E501`**。**ルートの `ruff` は `docs/` 配下の `.py` も検査する** — docstring の主張が誤りだった。**データを `steps.json` へ分離** |
| 22 | **2026-09-18: 予算基線を専有で確定** | **1390 passed / 599.82s**。**DB 依存 194 件はローカルで測れない**(環境変数の実値を読まない規約) |

## 決定

### 逐条突合の是正 5 件(`TSK-355` へ提出する)

| 是正 | 内容 | 本タスクの実装 |
| --- | --- | --- |
| **`A`** | **`M-1-d` 前半が正本より緩い** — 「2 件以上が同時に加わった PR は段階 2 ではない」を〔正本〕としているが、`BOOT-GRANT` の除外は**対象単位**。段階 2 から外すと**両対象の不合格がいずれも免除される** | **対象単位で除外を適用**し、**2 件以上は `ADR-003` 段階 2 違反として fail** |
| **`B`** | **`M-1` 負例-4 の「かつ fail」が正本から導けない** — 宣言だけの変更も封印要素由来なら `BOOT-GRANT` で免除される | **fail としない。解消が成立しないだけ**(`M-5-f` が既に塞いでいる) |
| **`C`** | **`M-2-a` の発効の言い換えから「実行された」が落ちている** | **発効条件は「接続され、かつ実行された」**。停滞計数の起点も実行の証跡を見る |
| **`D`** | **`M-3-b` の典拠が `BOOT-SEAL-IMMUTABLE` のみで不足**。かつ**「債務」は撤回済みの旧語彙** | **本タスクの記述から「債務」を撤去**(design.md §4 を全面改稿) |
| **`E`** | **`M-5-a` の典拠が拘束③ではなく `BOOT-PROGRAM`**。かつ**「または (b) が要求する事項の 1 件」は正本の単位規定に無い** | **委任①で (b) 由来事項の単位を本タスクが定義**し、封印集合の資産へ明記する |

### 委任 4 項目への決定(design.md §4-3〜§4-6 に集約)

| 委任 | 決定 |
| --- | --- |
| **①粒度・識別子・要素数** | **`A`(composite は束ね全体で 1 要素)**。識別子は `<対象 ID>/<D-11 の宣言名>`。**約 136 件で、段階 2 が進んでも増えない** |
| **②解消の判定方法** | **二値**。「部分的に充足」を作らない |
| **③段階 2 の PR の判定** | **対象単位**。**2 件以上は fail**。**paths で判定しない**。**発効前に限り資産不在を空集合として扱う** |
| **④機構・負例・正例と配線先** | **負例 12(条項ごと)+ 正例 A 2 件 + 正例 B 3 件**。配線先は **`consistency` ジョブ** |

## 計画レビュー 4 周目の指摘(7 件・全件 起因)

| # | 重大度 | 指摘 | 反映 |
| --- | --- | --- | --- |
| 1 | **`P0`** | **`BOOT-*` 12 条項の母集合が入れ替わっていた** — 正本は `BOOT-STATE` を含むのに `BOOT-CI-MEANING`(設計書 10.1 の条項)を代わりに数えていた。**件数 12 だけが合っており、`BOOT-STATE` の違背検出と負例が無くても `BOOT-ACTIVATION` ①②を満たせた** | design.md §4-1 を正本と**完全一致**に是正(機械確認済み)。**ステップ 19 で母集合を正本から逆引きし `assert len(ids) == 12`**。**ステップ 22 に `BOOT-STATE` の負例** |
| 2 | `P1` | **`(b)` 由来要素が「約 4」のままで全件性・一対一を証明できない** | **136 = D-11 由来 133 + (b) 由来 3** の確定値へ。**2 集合が互いに素**であることをステップ 14 が検査 |
| 3 | `P1` | **正例 B の是正が未完了で、逆に不要な正例も数えていた** | **`requiresPositiveB` を台帳へ**(true 41 / false 12)。**生成時に 12 件のずれを機械検出** |
| 4 | `P1` | **第 4 群に後続成果物への前方依存** | **個別機構 → 対応表 → 統合 → 負例 → 正例** へ並べ替え。**発効の実確認はステップ 51 へ集約** |
| 5 | `P1` | **分割後も逐行レビュー不能なコミットが残る**(3 周目 `P1-7` の再発) | 6 ステップをさらに分割(45 → 53) |
| 6 | `P2` | **§17-2 の判定集計が自己矛盾**(①は 13 件で 12 件ではない) | 是正。**表を作ったあと手で数えた**ための誤り |
| 7 | `P2` | トリガーの評価ステップ番号が旧番号のまま | **ステップ表から機械導出**する形へ |

**観点 5(宣言と成果物の被覆)は指摘なし** — `stepId` 1〜45 が各 1 回・`artifact` 53 本がすべて §3 に被覆。

## 計画レビュー 5 周目の指摘(6 件・全件 起因)

**観点 1(4 周目 `P0` の是正)は機械照合で閉じたと確認された** — 12 条項・6+6 分割・`BOOT-STATE` 負例が整合し、
`plan.md` と `steps.py` の出力も完全一致。

| # | 重大度 | 指摘 | 反映 |
| --- | --- | --- | --- |
| 1 | **`P0`** | **生成結果との一致検査に実装ステップが無い** — §5-3 の 7 検査のうち **5 件に実装者がいなかった**。`check()` も `plan.md` を読まないため将来の乖離を検出できない。**3 周目 `P0-7` の 3 回目** | **ステップ 50 を新設**。§5-3 に**実装ステップ列**を設け空欄を作れない形へ |
| 2 | `P1` | **`(b)` 由来 3 件が過不足** — `(b)②` の判定機構が無く、代わりの「走査対象の**宣言**が不在」は D-11 の `entrypoints[]` と意味上重なる。**互いに素の根拠が崩れていた** | **`(b)`①②③ の判定機構へ再定義**。**解消述語の突合**を追加(ID の積集合だけでは意味上の重複を検出できない) |
| 3 | `P1` | **ステップ 7 を `pb=false` にしたのは誤り** — マニフェスト schema は受理・拒否する機構 | **`pb=true` へ**。**`PB_FALSE` を閉じた集合として宣言**し exact-set で突合 |
| 4 | `P1` | **ステップ 4 が後続成果物へ前方依存** — 証拠資産はステップ 27 以降・最遅 48 で作られ、**コミット時点で合格不能**だった | **ステップ 4 を定義に限定**。**評価完了の検査はステップ 49 へ**。停止ゲートは**期限を過ぎた未評価のみ**を対象 |
| 5 | `P1` | **`check()` がステップ欠落・群不整合を検出しない** — **末尾を 1 件落としてもエラー 0 件** | **総数・群の連続性と見出し範囲・`PB_FALSE`** を追加。**負例 4 種で実証** |
| 6 | `P2` | §3 の新設資産表のステップ番号が旧番号のまま | **生成元から導出**する形へ |

## 計画レビュー 6 周目の指摘(7 件・全件 起因・全件 再発・新種 0)

**レビュアー自身の見立て(逐語)**: 「今回の 7 件がすべて前回反映起因かつ再発であり、とくに `PB_FALSE`・
解消述語・`check()` が**自己申告を別の自己申告で検査する**形になっているため、**`H-68` 型の循環には
入っている**。実装前には構造上の抜け道だけを閉じ、実効性は合成 fixture と CI 実測へ委ねるのが**打ち切り線**」。

| # | 重大度 | 指摘 | 反映 |
| --- | --- | --- | --- |
| 1 | `P1` | **ステップ 3 が「全 53 ステップ」のまま**で 54・55 の授権行を欠いても通った | **数値リテラルを廃し `{N}` を `EXPECTED_TOTAL` から展開** |
| 2 | `P1` | **`check()` の群検査が閉じていない**(群 ID の重複・順序崩し・`S[].g` 不一致・解析不能な見出し表記) | **5 検査を追加**。**負例 10 種のうち 9 種の検出を実測** |
| 3 | `P1` | **解消述語の文字列一致では意味上の重複を検出できない** | **代数的型の正規キーへ** — 互いに素な 2 構成子・136 件の一意性 |
| 4 | `P1` | **`PB_FALSE` は書き換え可能**・**ステップ 1 も誤分類** | **ステップ 1 を `pb=true`**。**`PB_FALSE` を固定 SHA に対して封印** |
| 5 | `P1` | **ステップ 4・5・49 の分離が design.md §12-3 へ未反映**。期限が未定義 | **§12-3 を同期**。**`evaluationDeadline`** を導入 |
| 6 | `P1` | **ステップ 50 が自己再帰を含み粒度が過大** | **50(構造検査)と 51(履歴と `command` の監査)へ分割** |
| 7 | `P1` | **ステップ 50 の検査が実 CI へ配線される保証がない** | **ステップ 52・54 へ配線検査と実行証跡確認を追加** |

**打ち切りの根拠**: `P0` が 0 になり**新種が 0**。4〜6 周目の 20 件がすべて起因・非起因 0。
**是正のたびに「自己申告を検査する層」が 1 つ増えるだけ**になっていたため、
**封印(固定 SHA)と代数的型という「自己申告でないもの」へ接地させて打ち切る。**

**残る限界(承認時に了承いただきたいもの)**:

- **`check()` は `pb` と `PB_FALSE` の同時書き換えを原理的に検出できない**(どちらも自己申告)。
  **ステップ 50 の封印検査が固定 SHA に接地して閉じる。**
- **44 → 46 件の正例 B が実装された deny-all を実際に落とすか**は **実装しないと確定できない**。
- **実 CI での所要時間と証跡**も同様。**ステップ 48・52・54 の実測で確定する。**

## 未決・次の一歩

1. ~~**人間承認**~~ → **済(2026-09-16・山田正輝)**(`codex_run.py review adversarial`)。**依頼文は追跡範囲を絞る**
   (design.md §16-11)— **「本タスクが依拠を宣言した条項についてのみ、依拠が成立するかを見よ」**
   「**正例 B を持たないステップを挙げよ**」の形。
2. **§16-2 の全数突合**(45 ステップの成果物と許容差分のずれ)と **§16-6-a の目視 4 類型分類**は
   **敵対レビューの依頼文と同時**に行う(**依頼文の観点に載せると追跡範囲が爆発する**ため分ける)。
3. **`TSK-317` PR #3 のマージ**が実装着手の前提。
4. **再ポイント(13pt → 34pt 提案)**と Notion の状態は**人間判断**。

## 実測

| 項目 | 値 |
| --- | --- |
| 行番号参照(`ADR-003:<数字>`) | **69 件 → 0 件**。うち区切り行・空行を指していたもの **23 件** |
| `ADR-003:99` の被参照数 | **12 箇所**。現在の 99 行目は `\| --- \| --- \|` |
| 封印集合の要素数 | **約 136**(対象 13 × 宣言 10 + トップレベル 2 + (b) 由来 約 4) |
| `.claude/core-areas.json` | areas 5・`guard_paths` 42。**`game-state` は 27 → 53 paths**(うち backend 21 本) |
| リベース | **395 コミット分・衝突 0** |
| 品質ゲート | `ruff` ✅ / `ty` ✅ / **`pytest tests/` 1346 passed / 597.23s** |
| `harness` 予算基線 | **1144 / 285.07s(2026-09-09)→ 1346 / 597.23s(2026-09-16)**。**後者は敵対レビューと並行実行した上限値**で、**専有での測り直しが要る** |

---

## 実装完了(2026-09-19〜21)— `/pr` クローズ処理

### 結果

**57 ステップすべて完了。** 実装は全ステップを Codex へ 1 ステップ = 1 委任 = 1 コミットで委任した。

| 検査 | 結果 |
| --- | --- |
| `tests/domain/` | **608 passed** |
| `tests/`(ルート全体) | **2026 passed** |
| `backend`(DB 除く) | **254 passed** |
| `frontend`(Vitest) | **680 passed** |
| **実 CI**(PR #74・run `35529110007`) | **`core-guard` 以外すべて success** |

**新設した CI ジョブ 2 本が実 CI で実際に実行され、いずれも success**
(`consistency` 20 分枠 / `mutation` 40 分枠)。これが `BOOT-ACTIVATION` 要求④ (iii) の証跡。

`core-guard` の failure は **PR 本文のコア領域チェック 2 項目を意図的に未チェック**にしているため。
**逐行確認は人間が行うもの**なので Claude はチェックを入れない。**正しい挙動。**

### 正本への反映

- `docs/development/github-setup.md` — 2 章の常時実行 5 → **7 ジョブ** / 3 章 Ruleset JSON **9 → 11 context** / 変更履歴
- `docs/development/dev-harness-design-2026-08-07.md` — **10.1 のジョブ表**へ `consistency` / `mutation`
- `docs/development/harness-evaluation.md` — **既存候補 3 件へ実測**(下記)
- `docs/README.md` — 上記 3 本の最終更新日

**いずれも版は上げない**(実装追随 = 設計書 7.6-3 前段)。
要件書・`ADR-003`・`contracts/**`・`docs/design/**` は**反映なし**(宣言どおり)。

### 見直しトリガー 16 件 — すべて非発火で確定

PO 裁定は 1・8(2026-09-19)/ 15(2026-09-20)/ 5・12(2026-09-21)、機械判定が残り。
**限界はすべて資産へ併記した。** とくに:

- **トリガー 1**: 合成 DSL での確認であり製品データではない。製品の「スコアボード全欄」は **TSK-236〜239 まで確定しない**
- **トリガー 8**: 保守手順が 2 ファイルで閉じることが根拠。**1 名体制での実運用はまだ無い**
- **トリガー 5・12**: **「上限超過は観測されていないが、超過しないことの証明ではない」**
  (合成 fixture の対象計算は 1 件・製品は 13 件 / **CI ランナー上の値ではない**)

### 台帳への追記(該当する)

**新規候補は立てず、既存候補 3 件へ実測を追記した。** 二重計上は昇格条件を満たす機会を捨てるため。

1. **「検証コマンドを人が選ぶと CI が走らせるコマンドとの差分が黙って残る」へ 3〜5 例目**
   — **昇格条件は 2026-09-16 に成立済み**で、本 3 件はそれを補強する(採番は PO 判断)
2. **「母集団を人が列挙する検査は、射程が動くたびに黙って古くなる」へ 21 例目**
3. **「clone して検査するテストはブランチの ref を見る」へ 2 例目**

### 申し送り

- **マージ順序**: **TSK-429(`feature/post-public-doc-sync`)を先に**。
  同タスクが `github-setup.md` 3 章 JSON を API 実測の全現値へ同期済みで、
  **同期前の JSON には `require_extra_approval_for_unattributed_changes` が欠落**していた。
  **古い JSON を再適用すると実設定が黙って失われる。**
  本タスクは develop 取り込み後に **context を 2 件足すだけ**にする
- **ruleset を 9 → 11 へ更新**するのは**人間の作業**(CI 緑を確認してから — PO 判断 2026-09-20)
- **敵対レビュー + PR 作成者以外による逐行確認**が未実施(コア領域のため必須)
- **Notion の見積 13pt → 40pt** が未処理

### クローズ処理中の差し戻し 1 往復(2026-09-21)

**`/pr` の突合 2-4 で `uv run pytest tests/` が 2 failed になり、差し戻し手順を 1 往復した。**
**どちらもステップ 51・52 で自分が置いた検査の欠陥**で、**検査は正しく働いていた**。

| 失敗 | 原因 | 是正 |
| --- | --- | --- |
| 履歴監査の昇順検査 | **ステップ 55 を意図的に最後へ回した**(`…54, 56, 57, 55`)。55 の合格条件が**実 CI の証跡**を要求し、実 CI を回すには 56・57 を含むブランチの push と PR が要ったため | **昇順は緩めない。** 順序の例外を `steps.json` へ**理由つきで宣言**し、**宣言に無い違反**と**宣言と実履歴の不一致**を個別に fail させる |
| 現在地導出の検査 | **実計画書のいまの段階に依存**して `/57` が出力に現れることを assert していた。`status` を `in-review` にした途端に落ちた | **`status: active` の写しを組み立てて実行**する形へ。段階で検査が消える形にしない |

**2 件目は、本タスクが 57 ステップを通じて繰り返し潰してきた
「実資産の可変フィールドの現在値を assert する」型そのもの**である。
**恒常規則として委任のたびに渡していたのに、自分が書いた検査で踏んだ。**
**判定法「この assert は 10 ステップ後も成り立つか」は、
`/pr` のクローズ処理という「10 ステップ後」で初めて反証された。**

**台帳への追記はしない** — 既存候補「母集団を人が列挙する検査」「検証コマンドを人が選ぶと…」の
いずれとも面が違い、**単発事象で傾向として確定していない**ため(`H-*` も候補も与えない)。
**本 worklog に記録を残すことで、忘れたのか判断したのかを区別できるようにする。**
