---
date: 2026-09-16
topic: TSK-355 の approved を受けた TSK-235 の再開(逐条突合・ステップ表 45 への再構成)
branch: feature/domain-calc-dsl
---

# 作業ログ: 2026-09-16 TSK-235 の再開

## やったこと

| # | 作業 | 結果 |
| --- | --- | --- |
| 1 | **`develop` へのリベース** | **395 コミット分・衝突 0**。本ブランチが触るのは `docs/features/domain-calc-dsl/**` と worklog 2 本のみで、いずれも `develop` に不在だったため |
| 2 | **〔正本〕20 命題の逐条突合** | **是正 5 件**(`A`〜`E`)。実質が崩れた命題は 0 件 — design.md §17 |
| 3 | **`P0-1` / `P0-2` の反映** | design.md §4 を封印集合の実装へ全面改稿 |
| 4 | **`P1-5` + 見直しトリガー 16 件目** | design.md §12-1(4 列追加)・§12-3(停止ゲート) |
| 5 | **`P1-1` / `P1-7` / `P0-2` のステップ再構成** | **29 → 45 ステップ** |
| 6 | **`P1-6` の DoD 再構成** | **`stepId` 対応表 45 行**。1 対 1 を機械検査する節を付けた |
| 7 | **行番号参照の一掃** | **69 件 → 0 件**。うち **23 件は表の区切り行・空行**を指していた |
| 8 | **`§16-12` 自己適用監査表の母集合是正** | **10 → 13 項目**。`16-8` と `16-11` が落ちていた |
| 9 | **`§16-8` の実施** | **「未解消件数」の 3 用途のうち報告は同値でない**(正本は**一覧**を要求)|
| 10 | **計画レビュー 4 周目の敵対レビュー** | **承認不可** — `P0` 1・`P1` 4・`P2` 2 / **起因 7・非起因 0** |
| 11 | **4 周目の全件反映** | **45 → 53 ステップ**。両表を [`steps.py`](../features/domain-calc-dsl/steps.py) から生成する形へ |

## 決定

### 逐条突合の是正 5 件(`TSK-355` へ提出する)

| 是正 | 内容 | 本タスクの実装 |
| --- | --- | --- |
| **`A`** | **`M-1-d` 前半が正本より緩い** — 「2 件以上が同時に加わった PR は段階 2 ではない」を〔正本〕としているが、`BOOT-GRANT` の除外は**対象単位**。段階 2 から外すと**両対象の不合格がいずれも免除される** | **対象単位で除外を適用**し、**2 件以上は `ADR-003` 段階 2 違反として fail** |
| **`B`** | **`M-1` 負例-4 の「かつ fail」が正本から導けない** — 宣言だけの変更も封印要素由来なら `BOOT-GRANT` で免除される | **fail としない。解消が成立しないだけ**(`M-5-f` が既に塞いでいる) |
| **`C`** | **`M-2-a` の発効の言い換えから「実行された」が落ちている** | **発効条件は「接続され、かつ実行された」**。停滞計数の起点も実行の証跡を見る |
| **`D`** | **`M-3-b` の典拠が `BOOT-SEAL-IMMUTABLE` のみで不足**。かつ**「債務」は撤回済みの旧語彙** | **本タスクの記述から「債務」を撤去**(design.md §4 を全面改稿) |
| **`E`** | **`M-5-a` の典拠が拘束③ではなく `BOOT-PROGRAM`**。かつ**「または (b) が要求する事項の 1 件」は正本の単位規定に無い** | **委任①で (b) 由来事項の単位を本タスクが定義**し、封印集合の資産へ明記する |

### 委任 4 項目への決定(design.md §4-3〜§4-6 に集約)

| 委任 | 決定 |
| --- | --- |
| **①粒度・識別子・要素数** | **`A`(composite は束ね全体で 1 要素)**。識別子は `<対象 ID>/<D-11 の宣言名>`。**約 136 件で、段階 2 が進んでも増えない** |
| **②解消の判定方法** | **二値**。「部分的に充足」を作らない |
| **③段階 2 の PR の判定** | **対象単位**。**2 件以上は fail**。**paths で判定しない**。**発効前に限り資産不在を空集合として扱う** |
| **④機構・負例・正例と配線先** | **負例 12(条項ごと)+ 正例 A 2 件 + 正例 B 3 件**。配線先は **`consistency` ジョブ** |

## 計画レビュー 4 周目の指摘(7 件・全件 起因)

| # | 重大度 | 指摘 | 反映 |
| --- | --- | --- | --- |
| 1 | **`P0`** | **`BOOT-*` 12 条項の母集合が入れ替わっていた** — 正本は `BOOT-STATE` を含むのに `BOOT-CI-MEANING`(設計書 10.1 の条項)を代わりに数えていた。**件数 12 だけが合っており、`BOOT-STATE` の違背検出と負例が無くても `BOOT-ACTIVATION` ①②を満たせた** | design.md §4-1 を正本と**完全一致**に是正(機械確認済み)。**ステップ 19 で母集合を正本から逆引きし `assert len(ids) == 12`**。**ステップ 22 に `BOOT-STATE` の負例** |
| 2 | `P1` | **`(b)` 由来要素が「約 4」のままで全件性・一対一を証明できない** | **136 = D-11 由来 133 + (b) 由来 3** の確定値へ。**2 集合が互いに素**であることをステップ 14 が検査 |
| 3 | `P1` | **正例 B の是正が未完了で、逆に不要な正例も数えていた** | **`requiresPositiveB` を台帳へ**(true 41 / false 12)。**生成時に 12 件のずれを機械検出** |
| 4 | `P1` | **第 4 群に後続成果物への前方依存** | **個別機構 → 対応表 → 統合 → 負例 → 正例** へ並べ替え。**発効の実確認はステップ 51 へ集約** |
| 5 | `P1` | **分割後も逐行レビュー不能なコミットが残る**(3 周目 `P1-7` の再発) | 6 ステップをさらに分割(45 → 53) |
| 6 | `P2` | **§17-2 の判定集計が自己矛盾**(①は 13 件で 12 件ではない) | 是正。**表を作ったあと手で数えた**ための誤り |
| 7 | `P2` | トリガーの評価ステップ番号が旧番号のまま | **ステップ表から機械導出**する形へ |

**観点 5(宣言と成果物の被覆)は指摘なし** — `stepId` 1〜45 が各 1 回・`artifact` 53 本がすべて §3 に被覆。

## 未決・次の一歩

1. **計画レビュー 5 周目の敵対レビュー**(`codex_run.py review adversarial`)。**依頼文は追跡範囲を絞る**
   (design.md §16-11)— **「本タスクが依拠を宣言した条項についてのみ、依拠が成立するかを見よ」**
   「**正例 B を持たないステップを挙げよ**」の形。
2. **§16-2 の全数突合**(45 ステップの成果物と許容差分のずれ)と **§16-6-a の目視 4 類型分類**は
   **敵対レビューの依頼文と同時**に行う(**依頼文の観点に載せると追跡範囲が爆発する**ため分ける)。
3. **`TSK-317` PR #3 のマージ**が実装着手の前提。
4. **再ポイント(13pt → 34pt 提案)**と Notion の状態は**人間判断**。

## 実測

| 項目 | 値 |
| --- | --- |
| 行番号参照(`ADR-003:<数字>`) | **69 件 → 0 件**。うち区切り行・空行を指していたもの **23 件** |
| `ADR-003:99` の被参照数 | **12 箇所**。現在の 99 行目は `\| --- \| --- \|` |
| 封印集合の要素数 | **約 136**(対象 13 × 宣言 10 + トップレベル 2 + (b) 由来 約 4) |
| `.claude/core-areas.json` | areas 5・`guard_paths` 42。**`game-state` は 27 → 53 paths**(うち backend 21 本) |
| リベース | **395 コミット分・衝突 0** |
| 品質ゲート | `ruff` ✅ / `ty` ✅ / **`pytest tests/` 1346 passed / 597.23s** |
| `harness` 予算基線 | **1144 / 285.07s(2026-09-09)→ 1346 / 597.23s(2026-09-16)**。**後者は敵対レビューと並行実行した上限値**で、**専有での測り直しが要る** |
