---
date: 2026-09-16
topic: ハーネス設計書へ 7.7「凍結基準の更新経路」を新設し、fail-open を塞ぐ
branch: feature/frozen-baseline-clause
---

# 作業ログ: 2026-09-16 7.7 凍結基準の更新経路

Notion: [本タスク](https://app.notion.com/p/3dd93b75e6878176afdbccfc6ca69788)。
計画書: `docs/features/frozen-baseline-clause/plan.md`。

## 経緯 — なぜ切り出したか

**送り元は TSK-386(PR #63)。** 同タスクは「凍結基準を検査器のソースから追い出す」もので、
**正本の条文と実装を同時に動かしていた。**

**実装の敵対レビューで 3 周連続 `P0`**:

| 周 | 判定 | 要旨 |
| --- | --- | --- |
| 1 | 否決 `P0 3 / P1 1` | 3 指定が宣言に無い / `7.7-4` の必須項目欠落 / `F-5` が到達可能性を見ない / 更新経路の閉塞 |
| 2 | 否決 `P0 3 / P1 2` | **宣言を足しただけで実装が使っていない**(`identity` をでたらめにしても通る)|
| 3 | 否決 `P0 2 / P1 2` | **比較関数を no-op にしても「実行済み」を出力する** |

**3 周とも同じ形だった** — **実装に「使った」と言わせる設計**で、**いずれも no-op で通った。**

**効いたのは `R-1` 型だけ** — **「宣言の要素を変えると挙動が変わることを変異で示す」**。
**自己申告は証拠にならない。** これが後継タスクへの最大の申し送りである。

**あわせて develop が 104 コミット進み、`oracle_commit` が `24ef4fc…` と `b64fdef…` で食い違った。**
**版番号も衝突した** — **`v1.15` は TSK-355 が取得して develop へマージ済み**。

## 判断(2026-09-16・山田正輝)

**仕切り直す。正本を先に出す。**

**ただし射程は 3 度広がった**(2026-09-16 更新):
**9 周目 `P0`** で `7.7-3` が現存検査に掛かると判明し fail-closed 化を射程へ /
**10 周目 `P0`** で fail-open が `:4798` 以外にもあると判明し 3 箇所へ /
**12 周目 `P1`** で `.git` 無しを前提にしたテストへの波及が判明。
**結果、コア領域を触るので人間の逐行確認が必要になった。**

**理由**:

| | |
| --- | --- |
| **条文は書き直した**(2026-09-16 更新)| 当初は「8 周 + PO 承認を通った本文をそのまま使う」前提だったが、**計画レビュー 4 周で、その条文のうち 3 項が `P0` を出し続けた**(受理条件の構成・委任の境界・保証する範囲 — **どれも存在しない機構を記述している**)。**PO 判断で削り、147 行 8 項 → 50 行 4 項になった。** **8 周の成果は `fix/oracle-input-baseline` の `design.md` に残り、実装タスクが引き継ぐ** |
| ~~**文書 3 本はコア領域に 0 件該当**~~ | ~~**人間の逐行確認が不要**~~ — **9・10 周目の `P0` で射程が広がり、`check_authz_catalog.py` の fail-open 3 箇所を塞ぐことになった。** **コア領域なので逐行確認が必要**(2026-09-16 更新)|
| **contracts に触らない** | **再封印も衝突も無い** |
| **穴が閉じる** | 「正本に凍結の規律を定めた条文が 1 つも無い」が解消する |

**PR #63 は close する。** ブランチは残す(`design.md` の失敗記録 43 件を後継タスクが引き継ぐ)。

## 確定ゲートの適用版(設計書 7.3-1 — 暫定記録)

**対象**: `docs/development/dev-harness-design-2026-08-07.md` の **`v1.15 → v1.16`**
(`7.7 凍結基準の更新経路` の新設 — **7.6-3 後段**)。

| 項目 | 値 |
| --- | --- |
| **適用版(7.3 の版数)** | **1.15** |
| **条文コミット SHA** | **`1dfb3da20ce21f9e08c848053838f0bc05e4604c`** |
| 根拠 | **in-review 化コミットの第一親**。同コミット時点で 7.3 は `v1.15` の approved 状態で収録されている |
| 記録日 | 2026-09-16 |

**本改訂は 7.3 自身を変更しない**ので、直前 approved 版がそのまま適用版になる。
**本記録は暫定であり、初回敵対レビュー実行時点で異なっていれば更新してから開始する。**
**当該ゲートはこの版で最後まで運用し、途中で切り替えない。**

**対象は 1 正本**なので一括検証には当たらない。適用版の記録は 1 件。

## 確定ゲートの採否記録(7.3-2)

### 1 周目(全文・`否決 P0 0 / P1 2 / P2 0`)

**`P0` なし。** **計画レビュー 14 周で条文を削ったことが効いている。** 2 件とも 起因・(A)・全件採用。

| # | 要旨 | 採否と反映 |
| --- | --- | --- |
| 1 | **削除時に宣言をどうするかが決まらない。** `7.7-1` は「動かすとき宣言へ置く」と要求し削除も「動かす」に含まれるが、**宣言も消すのか取り除いたことを表す宣言を残すのか**が分岐する。`7.7-2` の「取り除いたことを表す値」は**履歴レコードの話で宣言には及ばない** | **採用**。**「宣言から当該基準を取り除く。取り除いたことを表す宣言を残さない。記録は 7.7-2 の履歴が持つ」**と明記。**宣言は「現に凍結している基準」を表すので、凍結をやめたものを残さない** |
| 2 | **射程宣言が fail-open 3 箇所の「解消」を確定範囲に入れているが、解消はゲート後のステップ 3。** 「解消を要求することを確定する」のか「実際の解消まで確定ゲート対象」なのかが一致しない | **採用**。**射程宣言を「7.7 の 4 項のみ」へ。** **fail-open の解消は条文が要求する結果であり、同一 PR の後続ステップで実施して PR レビューで確認する**と明記 |

**不採用: 0 件。** **(B) の送り先: 無し。**

## やったこと

## 決定

## 未決・次の一歩
