---
date: 2026-09-24
topic: TSK-424 製品認可面の確定
branch: feature/product-authz-surface
---

# 作業ログ: 2026-09-24 TSK-424 製品認可面の確定

## やったこと

- /task-start: TSK-344 は製品 DDL 資産が未整備で DoD 1 を満たせないため、先行の TSK-424 に着手(人間の選択)
- /investigate: 4 並列(spec-checker / decision-tracer / Explore / legacy-analyst)→ research.md に統合。主要主張は原典で確認

## 決定

- 移行バッチ用ロールのライフサイクルの実行(退役・接続監査・孤児回収・同時実行)は TSK-349 へ切り出す(人間の判断 2026-09-24)。TSK-424 は書き込み先の資産・有効な間の形・定常の不変条件だけを持つ
- 表分類の自己充足の残余を受容する(人間の判断 2026-09-24・★10)。3 つの資産を同じ PR で整合的に弱める変更は、コア領域の人間の逐行確認が止める
- **PR A を A1(ステップ 1〜12・DB 呼び出しを増やさない)と A2(ステップ 13〜21・`allowed_symbols` を足すので TSK-431 の 7C の後)に分ける**(人間の判断 2026-09-24)。迂回検査は `backend/src` の新しい DB 呼び出しを TB005 で止め、許可リスト `base-allowlist.json` は凍結基準で、7.7-2 に適合する記録は 7C まで書けないため
- **Session 供給(`TenantRepositoryBase._session` を満たす手段)の所有は TSK-424**(PO 裁定 2026-09-24。本人に確認済み)。**TSK-424 の PR C** として、別の計画書・別ブランチで進める。PR A の射程は広げない。PR C は PR A と並行できる(ランタイム側で、DB 側の資産と独立)

- PR を 2 本に分ける(人間の承認 2026-09-24): ステップ 1〜8 = PR A(TSK-431 を待たない)/ ステップ 9 = PR B(TSK-431 マージ後)。理由: 1 本だと表分類を待つ U-M1・U-D1 のマージが TSK-431 待ちに巻き込まれる(帯 2 の進行役からの問い合わせで判明)

## 未決・次の一歩

- **計画承認(2026-09-24・山田正輝)**。計画レビュー 12 周の後、人間の判断で打ち切り(eb0690b)
- 起票: PR A2 = TSK-442 / PR B = TSK-443 / PR C = TSK-444 / 3-2 節の典拠訂正 = TSK-445。TSK-349 の射程を書き換えた。TSK-424 のカードの DoD を改訂版へ同期した
- FR-014 の到達経路の所有 = **U-X1(TSK-405・凍結中)のまま**(PO 決定 2026-09-24)。**当初「単位分割計画に載っていない」と報告したのは誤り**(表は裸の番号 `014` で列挙していた。`FR-014` の文字列だけで検索した)。master の指摘で訂正し、PO が U-G1 → U-X1 に決め直した。U-G1 のカードに書いた追加所有は撤回と明記した

- 人間の裁定が要る 4 件(research.md 未解決 ★): TSK-349 との二重所有 / SP-06(TSK-382)を踏まない線引き / 移行ロールの書き込み先 exact-set / 正本 3-2 の監査根拠
- 次: /plan

## 実装(PR A1・13 ステップ)

- /implement: ステップ 1〜13 を 1 委任 = 1 ステップ = 1 コミットで実施。是正 2 件(ステップ 5 の `.items()`・ステップ 13 の `enumerate` が迂回検査の TB007 に当たった)。**迂回検査は committed の差分しか見ない**ので、以後はコミットごとに走らせた
- 実装中の訂正: トリガ関数は暫定資産の 33 個でなく **37 個**(migration 0015・0016・0017・0024 の 4 個が暫定契約から漏れていた)。製品資産は 37 個とし、4 個を `provisional_contract_gap` の追加分として宣言。暫定契約の欠落は TSK-443 へ
- /sync-docs: data-model.md 12-8 節の行を probe(TSK-317・完了済み)と製品への写像(TSK-424 系)に分け、「確定・未発効」と残件の所有者を記録。**解消済みにはしない**。版は上げない
- origin/develop へ rebase(PR #76 の取り込み。README の索引行が競合 → 両方の行を採って解消)
- **正本の追随に派生資産 2 か所が連動する**: data-model.md を変えると `contracts/authz/shared-preconditions.json`(git blob digest)と `contracts/db/schema-manifest.json`(SHA-256)の取り直しが要る(前例 0cf994f)。ORM 受入シートも正本から生成しているので、変更履歴の行に誘発語(「変えない」等)を書くと出現が 1 件増えて red になる → 言い換えで回避

## 実装後の敵対レビュー

| 周 | P0 | P1 | P2 | 扱い |
| --- | --- | --- | --- | --- |
| 1 | 0 | 3 | 3 | 5 件是正(56b57d9・57dc947・ecc54a3・92f13a7)。1 件(P1-2)は人間の判断へ |
| 2 | 0 | 1 | 1 | P1(インスタンスへ差し込む `_compiler` 等の漏れ)は禁止の列挙を許可リストへ反転して是正(6a91a1b)。P2(残る秘密列 3 件の典拠)は人間の判断へ。1 周目の 4 件の閉鎖を確認 |
| 3 | 0 | 1 | 0 | P1(`Label.element` のキャッシュ差し替え)を是正(a833ee4)。**P1 が 3 → 1 → 1 と同じ検査に集中し、周ごとに SQLAlchemy の内部を 1 段ずつ掘る形**になったため、人間の判断で「直して脅威モデルを宣言」(design.md 10 節) |
| 4 | 0 | 2 | 2 | 最終確認周。P1-1(`Table.c` の内部参照)と P2-a は**私の宣言の書き過ぎが原因**(「インスタンスの状態も閉じる」と書き、承認範囲〔公開 API まで〕を越えた)→ 宣言を訂正して保証の外へ(92e81c0)。P1-2(DML の対象表の列)は保証範囲の中の本物の穴 → 是正(63fb563)。P2-b(ID の行)→ 表へ追加 |
| 5 | 0 | 0 | 0 | 4 周目の是正の差分だけを見る確認周。**収束** |

- **P1-2(人間の判断待ち)**: 計画書 4 節の不変条件「`contracts/authz/` 直下の probe 資産の差分 0 行」と、正本追随に伴う `shared-preconditions.json` の digest 1 行の変更が衝突する。**封印(`oracle-seal.lock.json`)の対象外**のファイルで、封印は動いていない。選択肢: (a) 正本追随に連動する digest の取り直しを宣言つきの例外として受容 / (b) data-model.md の追随を A1 から外し、別 PR で運ぶ
- 1 周目 P1-1 の是正で、capability 登録検査は **SQLAlchemy 2.0.52 に版を固定**した(版が変わると再監査まで拒否)。fail-closed だが、依存の更新のたびに red になる — PR 本文で人間の判断事項として挙げる
- 1 周目 P2 の是正で、`tenant_tokens.id`・`admin_sessions.id` を秘密の列から除いた(正本がトークンの提示形式を定めていない)。確定は U-A1

- **人間の判断(2026-09-24・山田正輝)**: P1-2 の digest 1 行 = **宣言つきの例外として受容** / 秘密列 3 件 = **残余として受容**(逐行確認の観点へ・正本の非露出の明記は別タスク)。計画書 4 節に訂正注記として記録

- **知見(台帳候補)**: 打ち切りの宣言を書くとき、人間が選んだ選択肢の文言より広い保証を書いてしまい、それがそのまま次周の指摘の入口になった。**宣言は承認した選択肢の文言から写す**(言い換えると範囲が動く)

## 結果サマリ(/pr のクローズ処理)

- **実装**: PR A1 の 13 ステップ(表分類・露出の事実・capability カタログと登録の検査・`AuthzAssetSpec` による spec 対応・製品 DDL の staged 資産と静的検査)。是正コミットはステップ 1・3・5・7・13 に付けた。`backend/src` の DB 呼び出しは増やしていない。最終パス `ddl-elements.json` は作っていない(未発効)
- **正本への反映**: data-model.md 12-8 節(「確定・未発効」と残件の所有者 — TSK-442/443/444/445・U-*・TSK-344/349)と変更履歴(版は上げない)/ docs/README.md。派生資産 2 か所の digest を取り直した(判断 1 の例外)
- **敵対レビュー**: 実装後 5 周で収束(P0 は全周 0)。人間の判断 3 件(digest 1 行の例外・ハッシュ列 3 件の残余・登録検査の保証範囲の宣言)
- **起票・申し送り**: TSK-453(ハッシュ列の非露出の明記)を起票。U-A1 へトークンの提示形式の確定を申し送り
- **台帳への追記あり**: 既存候補「『すべての抜け道』を条文の閉じた一覧で捉えようとすると…終わらない」へ 5 例目(打ち切りの宣言が承認範囲を越えた変種)。`H-*` は与えない(昇格条件は確定ゲートが対象で、本件は実装後の敵対レビュー)
