---
date: 2026-08-15
topic: feature_status.py の縮退の明示(F5)
branch: feature/feature-status-degradation
---

# 作業ログ: 2026-08-15 feature_status.py の縮退の明示(F5)

## やったこと

- /task-start: 既存タスク F5 に着手 → `feature/feature-status-degradation` ブランチ + worktree 作成(origin/develop 起点・`094a64e`)→ 計画書雛形・本 worklog 作成

## 本タスクで扱うもの(着手時点の把握)

台帳 **H-45**(追跡優先度 高)と **H-41**(同じく 高)の follow-up。**台帳側は既に本タスクを `F5` として参照している**(F2 のステップ 1 で参照を張った)。

### スコープ 3 件

1. **`feature_status.py` の縮退の明示**(H-45) — plan のステップ表に N 件あるのに `base..HEAD` のコミットで一致が **0 件**のとき、「**ステップ記法のコミットなし(書式未一致の可能性)**」と表示する。現状は**無言で `0/N`**。設計書 7.6-4 は「縮退は『未取得(理由)』で明示し無言で消さない」と定めており、これに反している
2. **`/implement` を経由しない経路への書式の伝達**(H-45) — ドキュメントのみのタスクでも書式が伝わる導線を作る。**置き場所は本タスクで決める**
3. **H-41: 確定ゲート周回と反映コミットの突合** — `feature_status.py` が `確定ゲート周回` の値とステップ 6 のトークン付きコミット数を突合する(2026-08-15 の PO 裁定で F2 から移管)

### なぜ起きたか(H-45 の訂正後の理解)

当初「書式がどこにも文書化されていない」と台帳に書いたが**事実誤認**で、2026-08-15 に訂正済み。書式 `(ステップ <k>[/<N>][ 付記])` は**規範的な 3 箇所に明記**されている。真の欠陥は:

1. **`/implement` を経由しないタスクでは書式を知る導線がない** — 要件書 v2.0 改訂は Claude が直接コミットしたため `implement/SKILL.md` を一度も読んでおらず、かつ 19 周の改訂で plan.md からテンプレートのコメントを削り落としていた
2. **書式未一致を「ステップ外のコミット」と区別せず無言で 0 件として扱う**

## 調査(2026-08-15)

`/investigate` を 2 本並列(`spec-checker` = 縮退規定と実装の全数突合 / `decision-tracer` = 3 スコープの決定経緯)。結果と典拠は [research.md](../features/feature-status-degradation/research.md)。**着手時点の前提が 3 つ変わった。**

### ① H-41 は現在の機構では実装できない

不足 5 点。最も重いのは **突合の期待値が正本に存在しない**こと — 「反映周ごとに 1 コミットし件名に `反映<r>周目` を付す」は **3 つの feature ローカル計画書にしかなく、直後の req-v2-legacy-parity は逆方針**(確定ゲートをステップ表に載せない)を採っている。正本(設計書 6.1 拡張キー表・`/finalize-doc`)は「**+1 する**」しか要求していない。

→ **機構化するには正本改訂(版繰り上げ + 確定ゲート)が先行する。** F2 の H-47(規約側に手を出す誘惑)そのものの構図。

### ② H-45 の縮退表示は現行の正本規定と競合する

`設計書:252`(**approved 正本の本文**)と `fs-design:84` が「計画系/文書系コミットのみ → known 0(実装前)」と**明示的に規定**している。

ただし **規定の目的は「承認直後の通常経路」に限定**されており、H-45 は**目的外の入力に規則が適用された**ケース。補完と読む材料が優勢(7.6-4 の無言縮退禁止 / 判別不能なら known を採らない方針が既に規定内にある)。**副作用は「承認直後の正常な表示にも注記が付く」こと。**

### ③ スコープ 2 の本質は「伝達経路」ではなく「厳密文法が正本にない」こと

書式の正は **`設計書:252`**(スキル・テンプレは派生)で、3 者の文言に食い違いはない。**しかし 3 者とも厳密文法を書いていない** — 同種括弧の一致・`k=0` 不正・`/<N>` の一致要求・非境界文字。これらは**一時ディレクトリの文書と実装にしかない**。

→ **伝達経路を作っても、伝える内容が不完全なら同じ事故が起きる。**

### 私の仮説の訂正

着手時に「`malformed` が使われていなければ検出材料を捨てている」と書いたが**誤り**。`malformed` は `fs.py:707-708` で使われている。**発火条件が `[（(]ステップ` に限られる**ため、要件書 v2.0 の件名(括弧なし)では**材料がそもそも生成されない**。

### 未報告の穴が 5 件

C-2/C-3 は **worktree を丸ごと無言で落とす**経路(glob 例外 / detached HEAD・2 階層ブランチ)。設計書 7.6-4 の「worktree の現存を正とする」に対する明確な穴で、hook で全 worktree が落ちると text 側は「進行中 feature: なし」と**誤断定**する。ほかに C-4(`inconsistent` の理由を捨てる)・C-5(ステップ表の総数が見出しで途中打ち切り)・C-6(`Notion 期待:` 行が消える)。

## 決定(調査時点)

- **重さ分類は計画段階で確定する** — コア領域 5 つには触れないため「通常」を見込むが、**スコープ 2 の置き場所によっては `guard_paths` に触れる**可能性がある(`.claude/skills/**` は guard_paths に `pr/SKILL.md` のみが載っている)。/plan で機械的に判定する

## 未決・次の一歩(調査時点 — いずれも計画段階で解決済み)

### PO 裁定が必要(2 件)

1. **H-41 を本タスクで扱うか** — (a) 別タスクへ送る (b) 正本改訂を含めて本タスクで通す (c) **突合対象を「正本に既にある観測可能量」へ置き換える**
2. **H-45 の縮退表示の副作用を許容するか** — 承認直後の正常な表示にも注記が付く。`設計書:252` と競合するため 7.6-3 のゲート判断が要る

### 計画段階で決める(2 件)

3. スコープ 2 を「伝達経路」から「**厳密文法の明文化**」へ広げるか
4. 未報告の C-2〜C-6 を本タスクで扱うか(**C-4 は H-45 と同じ無言縮退の系統で同時に直すのが自然**)

- 次の一歩: 上記 1・2 を PO 裁定に掛けて `/plan`

## 実装(ステップ 1〜4)— 設計書 v1.5 の改訂

`e1a919c`(in-review 化 + v1.5 行の器 + 索引)/ `f4a780a`(A-1 厳密文法 + A-3b 参照除去)/
`59b79ed`(A-2 反映周コミット規定)/ `a29f451`(A-3 known 0 の例外)。

F2 で入れた索引 ↔ 変更履歴の突合が効いており、版と最終更新を同時に更新しないと
ステップ 1 単独で `check_docs_status.py` が落ちることを実測した。

## 確定ゲート(敵対レビュー 11 周・sol xhigh)

**P0 は全 11 周でゼロ。P1/P2 合計 34 件を全件採用・不採用 0 件。** 11 周目に
「収束。approved 前に閉じる必要がある指摘なし」の判定を得て **PO 承認(2026-08-15・
山田正輝)**。

反映は 1 周につき 1 コミット(`反映1周目`〜`反映10周目`)。**本ゲート自身が v1.5 で
新設した規定に従って証跡を残した**(ドッグフーディング)。

### 方式を 2 度差し替えた

| 周 | 何が壊れていたか | どう直したか |
| --- | --- | --- |
| 1 | 反映周コミットに**ステップ番号を割り当てる案**が「1 ステップ = 1 コミット」と衝突(3 周なら同一番号のコミットが 3 本) | **番号を割り当てない 1 通りへ一本化**。副次的に `implement/SKILL.md`・`plan-template.md`・`CLAUDE.md` への例外追記が不要になりスコープが広がらずに済んだ |
| 1〜2 | 非遡及の境界を**日付(`created`)**で判定 → 精度が日単位で、**本タスク自身が確定日と同日**のため判定不能 | git の**祖先関係**へ。さらに「着手」は Git に痕跡を持たない(`/task-start` は未コミットの plan.md を作るだけ)ため境界を**「計画書の初回コミット」**へ再定義 |
| 4 | 祖先関係方式が周回ごとに穴を生み、**確定コミット定数はそれを埋める PR 自身では成立しない**(approved 化コミットはマージ前まで `origin/develop` の祖先にならない) | **PO 裁定で方式ごと差し替え** — plan frontmatter の**明示キー `反映周コミット`** へ。確定コミット定数・自己検証・初出コミット探索・rename 追跡がすべて不要になった |
| 5 | 明示キーの**欠落を「規約制定前」の既定**にしたため、テンプレを経由しない計画書の**記入漏れが正常系に紛れた** | **`適用` \| `規約制定前` の 2 値必須・既定値なし**へ。欠落は縮退。既存 8 件へ `規約制定前` を明示移行 |

### 自分が作った欠陥を指摘された(6 件)

- **`/pr` が `feature_status.py` を実行しない**(4 周目)— 確定ゲート反映からマージまで同一セッションで進めると**一度も突合されないままマージでき**、マージ後は `対象外` で永久に検査されない。守れていたのは「偶然 SessionStart が走ったとき」だけだった
- **「台帳へ起票する」が H-48 型**(6 周目)— 3 節・ステップ 10・DoD がいずれも候補 2 件のままで、**機械化候補を 1 件も追記せず完了できた**
- **候補送りが台帳の分類規則違反**(7 周目)— 制御目的の典拠があるものは `H-*`。**既知の構造的残余を追跡義務の弱い置き場へ下げていた** → `H-50`(通常)へ
- **識別子 `C-5`・`C-6` が調査書と計画書で逆のスコープ**(8 周目)— 調査書では候補送り(範囲外)、実装方針では実装対象。**当初から潜伏し 8 周かけて検出された**
- **実行不能な合格条件**(8 周目)— 「本タスクの PR 本文に実行出力が転記されている」をステップ 5 の合格条件にしたが、`/pr` は全実装後に走るため**ステップ 5 完了時点で PR 本文が存在しない**
- **H-41 と H-45 の解決方式を混同**(10 周目)— 現行方針を `A-1 + A-3 + B-4` と一括で書いたが、これは H-45 の方式。H-41 を誤って H-45 の方式へ書き換えても文面上合格した

### 台帳の対応案が「効かない解決策」のままだった(9 周目)

H-45 の旧対応案は「`/implement` のスキル本文と計画書テンプレのステップ表に書式を
明記する」だったが、**その 2 箇所は事象の発生時点で既に存在していた**。つまり旧案は
そもそも解決になっていない(だから H-45 が起きた)。状態だけを `対応済み` にすると、
**効かないと分かっている対応策を「済み」として残す**ことになる。H-41 と併せて対応案の
書き換えを DoD へ入れた。

### 参照の腐り(9・10 周目)

8 周目に自分で入れた `台帳:27` は、**ステップ 10 自身が台帳の変更履歴に 1 行足すことで
即座に腐る**。台帳自身が「参照は識別子が変化しないもので書く」と定めており、それに
反していた。計画書に残っていた `設計書:252`・`設計書:272` も 6 箇所すべて節名参照へ
置換した(行番号参照 0 件を機械検証)。

### 確定ゲート中に自分でステップ表を壊した(`2e6cd48`)

10 周目の反映でステップ 10 の**セルに改行入り箇条書き**を入れた結果、表が分断されて
総数が **10 → 9** に化けた。**敵対レビュー 11 周目はこれを「ステップ4/9・確定ゲート10周を
正常導出」と報告して見逃している。** レビューと機構の両方をすり抜け、人が総数の変化に
気づいて初めて検出された。

これは調査書の **C-5「ステップ表の総数が途中で打ち切られる」と同型**(原因が見出しでは
なくセル内改行という違いだけ)。C-5 は「実在 plan で発生しているかが不明」として
`## 候補` へ送る扱いだったが、**実在の発生例が 1 件確認された**ため起票内容へ実測値を
反映した(`b5c4988`)。**総数が黙って減っても導出は警告せず、9 も 10 ももっともらしい
ため表示だけでは判別できない**。

## 確定した v1.5 の到達点

保証する範囲と**保証しないもの**を正本に明記した(7 周を経て規約が複雑化したため)。

- **H-45(A-3)**: 判定は変えない。実装が完了していても表示は `known 0`(実装前)のまま
  であり、**実装完了を復元する保証ではない**。保証は「**無言で `実装前` と表示しない**」
  ことだけ。承認直後は注記を抑止するが、それ以外の正当な文書系コミットへの誤注記は残る
- **H-41(A-2)**: **先勝ち 1〜6 に該当せず第 7 経路(突合実行)へ到達した場合に**、周回集合の
  一致・1 件名 1 マーカー・各周 1 コミットを**警告表示する**。**強制・実行そのもの・
  追加 push 後の再検査・マージ時点の最新 HEAD での検査は保証しない**。残余は **`H-50`**
  (`/pr` 突合の機械化・追跡優先度 通常)で追跡する

## 決定(確定ゲート)

- 設計書 v1.5 を **approved**(2026-08-15・山田正輝)。索引を現行化(状態 approved・版 1.5・
  最終更新 2026-08-15)
- 非遡及の適用境界は **plan frontmatter の明示キー方式**(git 祖先関係方式は 4 周目に廃止)
- `/pr` 突合の機械化は**本版の範囲外**とし `H-50` で追跡する

## 実装(ステップ 5〜10)

| ステップ | 内容 | 委任 | 差し戻し |
| --- | --- | --- | --- |
| 5 | 伝達経路の追随(`AGENTS.md` / `finalize-doc` / `pr`) | Claude 直 | — |
| 6 | 縮退の明示(C-1 — H-45) | Codex | **2 回** |
| 7 | worktree の無言除外(C-2・C-3) | Codex | — |
| 8 | `inconsistent` の理由(C-4) | Codex | — |
| 9 | **H-41 の突合** | Codex | — |
| 10 | 台帳の更新 | Claude 直 | — |

テスト **257 → 285 passed**。

### ステップ 6 の差し戻し 2 回 — 対策コードに対策対象と同じ欠陥を作った

**1 回目**: 縮退の表示形式が正本と不一致だった。実装は `不明(承認履歴取得失敗)` を返して
いたが、正本は **`未取得(承認履歴取得失敗)`** を 9 箇所で規定している。本リポジトリでは
**`未取得(理由)` = 縮退**(取得・解決できなかった)、**`不明(理由)` = 取得はできたが確定できない**、
と使い分けられている。あわせて `derive_feature` の `unknown` 経路が `stage = "実装状況: 不明"`
として **`note` を捨てており**、SessionStart や text 表示に理由が一切出ていなかった。
**H-45(無言で誤った現在地を表示した事象)の対策コードの中に、H-45 と同じ型の欠陥を作っていた**。

**2 回目**: 変異テストで 2 件を検出した(→ 台帳 H-49 の 2 件目の実例として記録)。

- **`git 失敗` の縮退を潰していた** — `derive_feature` の `if rejection is None: return git_failure_result(plan)`
  を `approval_history_failure_result` へ置き換えていた。`has_unresolved_rejection` が `None` を
  返すのは **git コマンドの失敗**であって承認履歴の解析失敗ではない。**原因の誤報**であり、
  **計画のスコープ外の挙動変更**でもある
- **本来の縮退経路がテストされていなかった** — テストが上記の誤った経路に先に捕まっており、
  `derive_progress` 内の縮退を**丸ごと無効化しても 261 passed** だった(H-48 型)

### 変異テストは委任先の自己申告で代替しない

全ステップで **Codex の「変異でテスト失敗を確認済み」という報告を受けたうえで、同じ変異を
PR 作成者が再現**した。計 14 種を当て、**ステップ 6 の 1 件だけが見逃されていた**。
自己申告のみで受け取っていれば、無効なテストと誤ラベルの両方が通っていた。

| ステップ | 変異 | 結果 |
| --- | --- | --- |
| 6 | 縮退経路 / 縮退の理由 / 注記 / 承認直後の抑止 / git 失敗の誤ラベル(5 種) | 全種 1 failed |
| 7 | C-2・C-3 をそれぞれ無言 `continue` へ(2 種) | 全種 1 failed |
| 8 | `format_progress` 旧固定 / k>N の理由 / StepTable の理由(3 種) | 14・1・6 failed |
| 9 | 件数比較 / 部分集合判定 / 複数マーカー見逃し / 各周 1 コミット見逃し(4 種) | 1・2・1・1 failed |

### 最良の検証は本タスク自身だった

ステップ 9 で実装した突合を実リポジトリで走らせると **`反映周コミット突合: 一致`** が出る。
本 feature は `確定ゲート周回: 10` と `反映1周目`〜`反映10周目` の各 1 コミットを持つため、
**確定ゲートが規定どおりに証跡を残していたことを機構自身が裏付けた**形になる。

## 台帳への反映(ステップ 10 + `/pr` クローズ処理)

- **H-41 の `対応案` を全面改訂**(旧案のステップトークン内マーカー方式は v1.5 が禁止した形)+
  **`状態` を対応済みへ**。残余は **H-50** へリンク
- **H-45 の `対応案` を全面改訂** — 旧案の 2 箇所(`/implement`・plan テンプレ)は
  **事象の発生時点で既に書式を明記しており、そもそも解決になっていなかった**。実際に
  足りなかったのは ① 厳密文法が正本に無い ② 書式を外すと無言で `実装前` と表示される
  の 2 点。**`状態` を対応済みへ**(保証範囲の限界を併記)
- **`H-50` を新設**(必須 8 要素) — `/pr` の突合は**実行そのものを機構で保証していない**。
  追跡優先度 `通常`(制御目的の典拠はあるが、素通りでマージされた実例はまだ無い)
- **`H-49` の `再発` を 2 件へ更新** — F5 で**変異テストが初めて実効性のないテストを実検出**
  した。限界も追記(**変異テストの対象は実装であり、文書の破損は捕まらない**)
- **`## 候補` へ 2 件**(3 要素つき) — ステップ表の総数が途中で打ち切られる(**F5 で実発生**)/
  `未取得(` で `Notion 期待:` 行が消える

## 決定

- 設計書 v1.5 を **approved**(2026-08-15・山田正輝)。索引を現行化
- 非遡及の適用境界は **plan frontmatter の明示キー方式**(git 祖先関係方式は 4 周目に廃止)
- `/pr` 突合の機械化は**本版の範囲外**とし `H-50` で追跡する
- **委任実装の変異テストは PR 作成者が再現する**(自己申告で代替しない)

## 未決・次の一歩

- **H-50**: `/pr` 突合の機械化(最新 HEAD に結び付いた機械生成結果の自動挿入と再実行)
- **候補 2 件**の昇格条件を満たす事象が出たら `H-*` へ
- 本 PR は **`pr/SKILL.md` が `guard_paths` 該当**のため core-guard が発火する。
  PR 本文の逐行確認チェックが必須
