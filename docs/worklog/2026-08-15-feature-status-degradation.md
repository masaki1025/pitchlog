---
date: 2026-08-15
topic: feature_status.py の縮退の明示(F5)
branch: feature/feature-status-degradation
---

# 作業ログ: 2026-08-15 feature_status.py の縮退の明示(F5)

## やったこと

- /task-start: 既存タスク F5 に着手 → `feature/feature-status-degradation` ブランチ + worktree 作成(origin/develop 起点・`094a64e`)→ 計画書雛形・本 worklog 作成

## 本タスクで扱うもの(着手時点の把握)

台帳 **H-45**(追跡優先度 高)と **H-41**(同じく 高)の follow-up。**台帳側は既に本タスクを `F5` として参照している**(F2 のステップ 1 で参照を張った)。

### スコープ 3 件

1. **`feature_status.py` の縮退の明示**(H-45) — plan のステップ表に N 件あるのに `base..HEAD` のコミットで一致が **0 件**のとき、「**ステップ記法のコミットなし(書式未一致の可能性)**」と表示する。現状は**無言で `0/N`**。設計書 7.6-4 は「縮退は『未取得(理由)』で明示し無言で消さない」と定めており、これに反している
2. **`/implement` を経由しない経路への書式の伝達**(H-45) — ドキュメントのみのタスクでも書式が伝わる導線を作る。**置き場所は本タスクで決める**
3. **H-41: 確定ゲート周回と反映コミットの突合** — `feature_status.py` が `確定ゲート周回` の値とステップ 6 のトークン付きコミット数を突合する(2026-08-15 の PO 裁定で F2 から移管)

### なぜ起きたか(H-45 の訂正後の理解)

当初「書式がどこにも文書化されていない」と台帳に書いたが**事実誤認**で、2026-08-15 に訂正済み。書式 `(ステップ <k>[/<N>][ 付記])` は**規範的な 3 箇所に明記**されている。真の欠陥は:

1. **`/implement` を経由しないタスクでは書式を知る導線がない** — 要件書 v2.0 改訂は Claude が直接コミットしたため `implement/SKILL.md` を一度も読んでおらず、かつ 19 周の改訂で plan.md からテンプレートのコメントを削り落としていた
2. **書式未一致を「ステップ外のコミット」と区別せず無言で 0 件として扱う**

## 調査(2026-08-15)

`/investigate` を 2 本並列(`spec-checker` = 縮退規定と実装の全数突合 / `decision-tracer` = 3 スコープの決定経緯)。結果と典拠は [research.md](../features/feature-status-degradation/research.md)。**着手時点の前提が 3 つ変わった。**

### ① H-41 は現在の機構では実装できない

不足 5 点。最も重いのは **突合の期待値が正本に存在しない**こと — 「反映周ごとに 1 コミットし件名に `反映<r>周目` を付す」は **3 つの feature ローカル計画書にしかなく、直後の req-v2-legacy-parity は逆方針**(確定ゲートをステップ表に載せない)を採っている。正本(設計書 6.1 拡張キー表・`/finalize-doc`)は「**+1 する**」しか要求していない。

→ **機構化するには正本改訂(版繰り上げ + 確定ゲート)が先行する。** F2 の H-47(規約側に手を出す誘惑)そのものの構図。

### ② H-45 の縮退表示は現行の正本規定と競合する

`設計書:252`(**approved 正本の本文**)と `fs-design:84` が「計画系/文書系コミットのみ → known 0(実装前)」と**明示的に規定**している。

ただし **規定の目的は「承認直後の通常経路」に限定**されており、H-45 は**目的外の入力に規則が適用された**ケース。補完と読む材料が優勢(7.6-4 の無言縮退禁止 / 判別不能なら known を採らない方針が既に規定内にある)。**副作用は「承認直後の正常な表示にも注記が付く」こと。**

### ③ スコープ 2 の本質は「伝達経路」ではなく「厳密文法が正本にない」こと

書式の正は **`設計書:252`**(スキル・テンプレは派生)で、3 者の文言に食い違いはない。**しかし 3 者とも厳密文法を書いていない** — 同種括弧の一致・`k=0` 不正・`/<N>` の一致要求・非境界文字。これらは**一時ディレクトリの文書と実装にしかない**。

→ **伝達経路を作っても、伝える内容が不完全なら同じ事故が起きる。**

### 私の仮説の訂正

着手時に「`malformed` が使われていなければ検出材料を捨てている」と書いたが**誤り**。`malformed` は `fs.py:707-708` で使われている。**発火条件が `[（(]ステップ` に限られる**ため、要件書 v2.0 の件名(括弧なし)では**材料がそもそも生成されない**。

### 未報告の穴が 5 件

C-2/C-3 は **worktree を丸ごと無言で落とす**経路(glob 例外 / detached HEAD・2 階層ブランチ)。設計書 7.6-4 の「worktree の現存を正とする」に対する明確な穴で、hook で全 worktree が落ちると text 側は「進行中 feature: なし」と**誤断定**する。ほかに C-4(`inconsistent` の理由を捨てる)・C-5(ステップ表の総数が見出しで途中打ち切り)・C-6(`Notion 期待:` 行が消える)。

## 決定

- **重さ分類は計画段階で確定する** — コア領域 5 つには触れないため「通常」を見込むが、**スコープ 2 の置き場所によっては `guard_paths` に触れる**可能性がある(`.claude/skills/**` は guard_paths に `pr/SKILL.md` のみが載っている)。/plan で機械的に判定する

## 未決・次の一歩

### PO 裁定が必要(2 件)

1. **H-41 を本タスクで扱うか** — (a) 別タスクへ送る (b) 正本改訂を含めて本タスクで通す (c) **突合対象を「正本に既にある観測可能量」へ置き換える**
2. **H-45 の縮退表示の副作用を許容するか** — 承認直後の正常な表示にも注記が付く。`設計書:252` と競合するため 7.6-3 のゲート判断が要る

### 計画段階で決める(2 件)

3. スコープ 2 を「伝達経路」から「**厳密文法の明文化**」へ広げるか
4. 未報告の C-2〜C-6 を本タスクで扱うか(**C-4 は H-45 と同じ無言縮退の系統で同時に直すのが自然**)

- 次の一歩: 上記 1・2 を PO 裁定に掛けて `/plan`

## 決定

## 未決・次の一歩
