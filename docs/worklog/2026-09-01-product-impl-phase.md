---
date: 2026-09-01
topic: 設計書 13 章 — 製品機能の実装を Phase の対象外として明記する(Phase は追加しない)
branch: feature/product-impl-phase
---

# 作業ログ: 2026-09-01 設計書 13 章 — 製品機能の実装を Phase の対象外として明記する(Phase は追加しない)

## やったこと

### ステップ 1 — 13 章へ「製品機能の実装」の節を追加(`6ff3abe`)

**Phase を追加せず、「対象外である」ことを明記する形**(人間の裁定 = 丙)。**追加は 6 行・項目 4 個。**

歯止め 7 点を機械検証し**全 PASS**:

| # | 歯止め | 結果 |
| --- | --- | --- |
| 1 | Phase 表の行数が不変 | PASS(6 → 6) |
| 2 | **Phase 0〜5 の行が `origin/develop` とバイト一致** | PASS |
| 3 | **「Phase 4 の分割」節(42 行)がバイト一致** | PASS |
| 4 | 追加が 1 節・項目 5 個以内 | PASS(6 行・4 項目) |
| 5 | 禁止語が肯定文で現れない | PASS(3 件すべて「定めない」の否定文) |
| 6 | 柱書「Phase 4 のみ分割」が不変 | PASS |
| 7 | 10.1 の「実装期」が不変 | PASS(2 件) |
| — | 差分が設計書 1 本のみ | PASS |

**検査 5 は当初 FAIL した。**単純な文字列検索で「群」「段階」を禁止したため、
**「群・段階を定めない」という否定文まで違反と判定した**。歯止めの趣旨は**定めないこと**なので、
**検査を文脈判定(同一行に「定めない」があるか)へ直した**。内容は初めから適合していた。

### ステップ 2 — 後続タスクの起票

**TSK-280「同期プロトコル — イベント契約の骨格の実装(製品コードの 1 本目)」**(高 / 8pt)。
**採番を Notion で確認済み**(TSK-275 で踏んだ MCP の欠陥は再発せず)。

**射程を `sync-protocol.md` 4 章「イベント契約の骨格」のみに絞った。**
7-2(キューの状態遷移)・7-4(クライアント側の規律)・7-6(undo)・7-7(ローカル書き出し)は
**すべてイベントの型に依存する**ため、イベント契約が全部の前提。**かつ状態機構も永続化も含まないので最小。**

## 記録する副作用(3 点 — 計画書 §4-(4))

### ① 10.1 の「実装期」を触らない判断

10.1 の CI ジョブ表は `consistency` / `e2e` の導入時期を「**実装期**」としている。
**本改訂で「実装期 = 製品機能の実装」と同定することを検討したが、触らないことにした。**

**理由**: **「実装期」を定義する記述がリポジトリ内に存在しない**
(要件書 `:948` も「一致性・越境・E2E・同期故障系は**実装期のため対象外**」と書くだけ)。
**定義を与えると要件書との整合判断が必要になり、射程が広がる。**
→ **別タスクへ送る。**

### ② 台帳の行番号典拠が既に破損しており、本改訂で +1 ずれる

**`docs/development/harness-evaluation.md` の 4 箇所(`:49`・`:1097`・`:1099`・`:1102`)が
`13 章 :825` と書いているが、該当規定(4-6 の `/task-done` で `P4-後` を起票する形)は
現在 `:850` であり、約 25 行ずれて既に破損している。**

**本改訂で 13 章に 6 行足すため、さらに +6 ずれる。**
→ **本改訂では直さない**(台帳は射程外)。**別タスクへ送る**(台帳の実装追随・版は上げない)。

### ③ 設計書は NFR-021 の失効対象パスである

`.claude/nfr021-invalidating-paths.json` は `default: "invalidating"` で、
`allowlist` は `/docs/ops/nfr021-acceptance/**`・`/docs/worklog/**`・`/docs/features/**` の 3 つのみ。
→ **`docs/development/dev-harness-design-2026-08-07.md` は既定で失効側に落ちる。**

**帰結**: **13 章の編集は、次回リリース時の `gate_kind: release` の失効判定で差分として現れる。**
**ただし済んだ Phase 4 の受入(`result: passed`・`T = 3873ce85`)は遡って無効にならない** —
失効判定は **`T` → 当該証跡の候補 SHA の範囲**だけを見るため、本改訂は判定範囲より後の変更である。

## PR #35 との関係(人間の裁定 2026-09-01)

**他の担当者の PR #35(`feature/gate-convergence-rules` = TSK-273)が設計書 v1.12 を持っている。**
中身は **7.3 確定ゲートの収束規則の全面再設計**(`7.3-1`〜`7.3-8`)で、**approved 済み**
(敵対レビュー **反映 12 周**・指摘 54 件全採用・P0 全周ゼロ)。**あわせて台帳 `H-68` を「対応済み」へ**
変更している(対応案 (a)(b)(c) の 3 案すべてを条文化・TSK-240 解消)。

| やること | いつ | 理由 |
| --- | --- | --- |
| ステップ 1〜3 | **先行して行う** | #35 に依存しない |
| **確定ゲート(初回敵対レビュー)** | **#35 マージ後** | **`7.3-1`「適用版 = 初回敵対レビュー実行時点で approved の本節の版」**。先に走らせると適用版が **`v1.11` に固定**され、**`v1.12` の歯止めが永久に効かなくなる** |
| **PR のマージ** | **#35 の後**(rebase してから) | **#35 は本リポジトリの PR #34 が原因で既に CONFLICTING**。**先にマージすると相手のコンフリクトが増える** |

**版は最初から `1.13`**(`1.12` は #35 が使う)。

### コンフリクトの実体(測定済み)

| ファイル | develop 側(= PR #34 = Claude) | #35 側 |
| --- | --- | --- |
| `docs/README.md` | +2 -2(台帳行と ADR 行の最終更新日) | +3 -3 |
| `docs/development/harness-evaluation.md` | **+94 -1**(`H-85` 新設・`H-22`/`H-42` の実測・候補 1 件・変更履歴) | +13 -6(`H-68` を「対応済み」へ) |

**内容は競合していない**(こちらは `H-85` を足し、#35 は `H-68` を直している)。
**衝突は変更履歴表の同じ位置と `H-*` 節の並び = 追記位置の問題。**

## ◎ `H-68` の引用の訂正(本セッションの Claude の誤り)

**Claude は本セッション中、「H-68 が『Phase 4 の分割で 13 章の確定ゲートが 18 周の閉じた輪に
入った』と記録している」と繰り返し述べた。その逐語は H-68 に存在しない。**

| H-68 が実際に書いていること | 典拠 |
| --- | --- |
| **1 件目 = 設計書 v1.7 の確定ゲート 12〜15 周目**。「**検証器が 1 行も存在しない段階で運用機構を机上設計し、12〜15 周目の P1 11 件中 11 件が直前の周の修正に対する指摘になった**」 | `harness-evaluation.md:1022` |
| 「本件は 25 周、**1 例目は 18 周**」 | 同 `:1024` |
| **H-68 は「Phase 4 の分割」という語を使っていない** | — |
| **18 周全体が閉じた輪ではない**。閉じた輪だったのは **12〜15 周目** | 同 `:1022` |

**「Phase 4 の分割」と明示して結びつけているのは、取り下げ済みの `feature/implementation-roadmap` の
計画書だけ**である。

**H-68 の本質は「実体(検証器・生成物・ベクタ)が存在しない段階でフィールド設計まで文書で
閉じようとすると輪に入る」**であり、**本改訂は細部設計を一切行わないので該当しない。**

## Phase の役割について(本タスクの出発点)

**設計書 13 章の Phase は「開発ハーネスの導入単位」であって、製品機能の作業単位ではない。**

| 典拠 | 内容 |
| --- | --- |
| 設計書の位置づけ | 「三者で pitchlog を開発するための**開発ハーネス**(開発フロー・規約・権限・自動化・ドキュメント管理…)」 |
| 13 章の章題 | 「**段階導入計画**」— 段階導入するのは**本設計書の中身** |
| **Phase 0** | **本設計案の確定(approved 化)** ← Phase が何の単位かを決定づける |
| 13 章の箇条 | 「Phase 1〜3 はコードが無くても成立する。**Phase 4 は設計フェーズの進行と同期させる**」 |

→ **製品機能を Phase で刻む設計ではなかった。** Phase 4 で骨格を作り、そこから先は
**要件書 → 設計 → 6.1 の実装計画書ゲート → 実装**へ引き渡す想定である。

**Claude の誤りの訂正**: セッション中「製品コードのタスクが 0 件だから作れない」と説明したが、
正確には**「置き場が無いから起票されていない」**であり、**設計は既にあった**。

## 2026-09-02 ステップ 3(変更履歴・版・索引)

**PR #35(TSK-273)が `89d1ad6` でマージされた**ので、待ちを解いて `origin/develop` へ rebase した
(競合なし・`eb75b36` → ステップ 1 / `47b710b` → ステップ 2 へ付け替え)。

### 適用版の確定(`7.3-1`)

**本改訂の確定ゲートに適用される 7.3 の版は v1.12** である。
`7.3-1`(適用版の原則)が「**初回敵対レビュー実行時点で approved の本節の版**」と定めており、
v1.12 は 2026-09-01 に approved 化されている(`06f6155`)。
**PR #35 のマージを待ってから初回レビューを回すのは、この規則を満たすためである**
(先に回すと v1.11 が適用版として固定され、新収束規則の外でゲートを進めることになる)。

### やったこと

| 対象 | 内容 |
| --- | --- |
| 設計書 frontmatter | `approved` → **`in-review`**(v1.12 の起案コミット `2d6f152` と同じ手順) |
| 設計書 変更履歴 | **1.13 行(2026-09-02・状態 in-review)** を追記。**`7.3-7` の射程宣言を同行へ逐語で記載**(既存正本の改訂は文書内の恒久節ではなく変更履歴行に置く — `7.3-7` の配置規定) |
| `docs/README.md` | 設計書行を **版 1.13・最終更新 2026-09-02・状態 in-review** へ現行化。**v1.12 の要旨は括弧内に残した**(版が上がっても直前版の内容が索引から消えないようにする) |

### 歯止めの機械確認(全 7 点)

| # | 歯止め | 結果 |
| --- | --- | --- |
| 1 | Phase を追加しない | **13 章 Phase 表の行数 = 6(改訂前と同一)** |
| 2 | 既存 Phase 0〜5 を書き換えない | **`origin/develop` の同行とバイト一致**(4-1〜4-8 の 8 行も一致) |
| 3 | 「Phase 4 の分割」節に触れない | **md5 一致** |
| 4 | 追加は 1 節・5 項目以内 | **9 行追加・箇条 4 項目** |
| 5 | 群・順序・段階を定めない | **該当語の出現は「本章では順序・群・段階を定めない」の否定文 1 行のみ** |
| 6 | 新しい規則を作らず既存決定を参照する | 各項に典拠(6.1 / 10.1 / ADR-003) |
| 7 | 10.1 の「実装期」を定義しない | **10.1 に差分なし** |

### 検査

`check_docs_status.py` / `check_doc_coverage.py` / `check_design_propagation.py` = **いずれも exit 0**。
`uv run ruff check .` / `uv run ty check` = グリーン。

### この先

**確定ゲート(`/finalize-doc`)は未実施**。適用版 v1.12 の規律で回す:

- **`7.3-4`**: 初周は全文 → 2 周目以降は**差分と影響節のみ**(射程変更周は全文へ戻す)
- **`7.3-5`**: 依頼文に**判定行を必須**・**起因フラグ**・**(A) 方式が分岐する / (B) 実装時に決めれば足りる**の分類
  (**(B) は P2 のみ** — 台帳 H-68 の対応案 (c) が v1.12 で規範化された)
- **`7.3-2`**: 収束条件は **P0/P1 ゼロの周**。**全件不採用の周はカウントしない**
- **`7.3-6`**: 前周修正起因が 2 周連続で過半 → **PO 裁定**。**6 周で PO へ提示**
- 反映周コミットには**ステップ記法を付けず `反映<r>周目`** だけを含める(6.1)

## 確定ゲート 反映1周目(2026-09-02)— 採否記録

**適用版 = 7.3 v1.12**(`7.3-1` — 初回敵対レビュー実行時点で approved の版。PR #35 が `89d1ad6` でマージ済み)。
**レビュー範囲 = 全文**(`7.3-4` の初周)。モデル = sol xhigh。
**レビュー判定: 否決 / P0: 0 / P1: 4 / P2: 4**(全 8 件。**8 件すべて原典で裏を取った** — 誤指摘なし)。

### 採否

| # | 重大度 | 起因 | 分類 | 指摘の要旨 | 採否と内容 |
| --- | --- | --- | --- | --- | --- |
| 1 | P1 | 起因 | (A) | **開始条件が弱い** — 10.1 は「正式受入 = 4-6 マージ + マージ後検査の合格」と定義し「`passed` は正式受入ではない」と明記する。`passed` だけを条件にすると、その後に検証器やマージ後検査が失敗しても製品実装だけは開始でき、**未完了の Phase 4 を基盤にする経路**が開く | **採用(PO 裁定 甲・2026-09-02・山田正輝)**。開始条件を **3 条件**へ改めた — **① `passed` 判定の記録 ② 検証器の合格条件①〜⑩の合格 ③ 4-6 の `develop` へのマージ**(①②= 10.1「4-6 マージ前条件」)。あわせて **①〜③は「正式受入」ではない**ことを明記し、**逆行条件**(10.1 の再受入が必要になった場合は成立まで新規着手を止める)を追加した |
| 2 | P1 | 起因 | (A) | **ADR-003 の援用が過大** — ADR-003 は **① 検査基盤とベクタ schema・期待値の先行確定(この段階では対象計算のコードを追加しない)→ ② 対象計算ごとに同一変更で追加 → ③ 全検査緑をマージ条件**という順序を必須にしている。「実装を禁じない」は②③の話であり、**検査基盤の未整備を許容する規定ではない** | **採用**。「正解ベクタ・検査基盤の未整備は…」を「**対象横断の正解ベクタが未完成であること**は…」へ縮小し、**`NFR-018` の対象計算に触れる実装は ADR-003 の導入順序に従う**ことと、**検査基盤の未整備を理由に対象計算を先に実装できない**ことを明記した |
| 3 | P1 | 起因 | (A) | **射程宣言が自己矛盾** — 「進捗とタスク対応の機械化」を実装時確定範囲に置きながら、同じ文で「行わない」としている。実装タスクが機械化禁止を固定条件と見るのか再検討可能と見るのか一意に決まらない | **採用**。射程宣言を書き直し、**機械化は「射程外・実装時にも再検討しない」**(13 章「機械化しない」の箇条で既に確定済み)として**実装時確定範囲から外した**。確定範囲は ①〜⑤ に列挙し直した |
| 4 | P1 | **非起因** | (A) | **`.claude/rules/backend.md`・`frontend.md`・`testing.md` が未整備** — 8.6 は Phase 4 で導入と定めるが実在は `docs.md` のみ。製品実装がパス別規約なしで始まる | **採用(PO 裁定 甲・2026-09-02・山田正輝)**。追加節に **5 項目目「適用する規約」**を置いた — **規約の正は `AGENTS.md`**・3 本の整備は**別タスク**・**未整備は着手を止めない**。整備タスクを起票した(**TSK-303**) |
| 5 | P2 | 起因 | (-) | **`:847` の行番号典拠が誤り** — 現在の 847 行は空行で、該当規定は 908 行。行番号から追跡した監査者が無関係な位置へ到達する | **採用**(**レビュー前に Claude が自己発見していた**)。**行番号をやめ**、「13 章『Phase 4 の分割』節末尾の『機械化しない』の箇条」という**節名参照**へ置き換えた(設計書・計画書の 3 箇所) |
| 6 | P2 | 起因 | (-) | **失効判定の説明が過大** — 正本は失効判定を `T` → 候補 SHA の変更パス集合に限定しており、`T` が本改訂を含む状態なら**差分には現れない**。監査記録が機械検出の範囲を過大に説明している | **採用**。計画書・本ログの記述を「**`T` が本改訂のマージより前に置かれた場合に限り失効差分として現れる**」へ改めた |
| 7 | P2 | 起因 | (-) | **本ログの表題と `topic` が「製品実装フェーズの追加」** — 実際の決定は「Phase を追加しない」。題名検索で逆の内容として抽出される | **採用**。`topic` と見出しを「**製品機能の実装を Phase の対象外として明記する(Phase は追加しない)**」へ改題した |
| 8 | P2 | 起因 | (-) | **本ログに空の重複節** — 空の「やったこと」「決定」「未決・次の一歩」の直後に同名の「やったこと」が始まり、見出しリンクが空節へ到達する | **採用**。テンプレート由来の空 3 節を削除した |

**不採用 0 件。(B) 分類 0 件。**(全件が (A) または (-) のため、`7.3-2` の「全件不採用」「(B) の送り先記録」の経路には入らない。)

### 射程が変わった — 次周は全文レビュー

**指摘 1・2・4 の採用により射程が拡大した**(開始条件の 3 条件化・ADR-003 導入順序への従属・「適用する規約」項の新設)。
`7.3-6` の**範囲確定**の行に従い、**`7.3-4` の射程変更周として次周も全文レビュー**とする。
射程宣言(変更履歴 1.13 行)は確定範囲を **①〜⑤** に書き直した。

### 歯止めの改定(自己申告)

**歯止め 5 の機械検査を意味判定へ改める。** 追加節は指摘 2 の採用により
「**ADR-003 が定める導入順序に従う**」という語を含むため、「群・順序・段階の語を含まない」という
文字列検査は成立しなくなった。**歯止めの趣旨は「本章が製品実装の順序・群・段階計画を定めないこと」**であり、
**ADR-003 の導入順序への参照は既存決定の参照であって新規の順序規定ではない**。
機械検査は「**『本章では順序・群・段階を定めない』の否定文が存在すること**」+ 意味判定に置き換える。

### 副作用 3 の記述を訂正(指摘 6)

**当初「13 章の編集は次回リリース時の `gate_kind: release` の失効判定で差分として現れる」と書いたが、これは過大**であった。
失効判定は **`T`(試験対象コミット)→ 候補 SHA の変更パス集合**だけを見る(10.1)。
したがって**次回受入の `T` が本改訂のマージより後なら、本改訂は差分に現れず、その状態が試験対象になるだけ**である。
**差分として現れるのは `T` が本改訂より前に置かれた場合に限る。**
**済んだ Phase 4 の受入(`passed`・`T = 3873ce85`)が遡って無効にならない**点は変わらない。

## 確定ゲート 反映2周目(2026-09-02)— 射程縮小の PO 裁定と採否記録

**レビュー範囲 = 全文**(前周の射程拡大により `7.3-4` の射程変更周)。モデル = sol xhigh。
**レビュー判定: 否決 / P0: 0 / P1: 6 / P2: 1**(全 7 件。**7 件すべて原典で裏を取った** — 誤指摘なし)。

### `7.3-6` のエスカレーションが成立した

| 周 | 母集団(P0+P1) | 起因件数 | `起因 × 2 > 母集団` |
| --- | --- | --- | --- |
| 1 周目 | 4 | 3 | 6 > 4 — **成立** |
| 2 周目 | 6 | **6** | 12 > 6 — **成立** |

**2 周連続で成立したため PO スコープ裁定へエスカレーションした**(`7.3-6`)。

**PO 裁定: 射程縮小**(2026-09-02・山田正輝)。**連続カウントは 0 にリセットされる**(`7.3-6`)。

### なぜ縮小が必要だったか

**反映がそのつど隣接する規範を引き込んでいた。** 2 周目の 6 件中 6 件が起因(= 1 周目の反映が生んだ指摘)で、
その内容は **10.1 の正式受入 / 8.4 のスキル群 / `AGENTS.md` / 13 章柱書 / 6.1・7.6 の進捗導出**へ次々に及んでいた。

**これは台帳 H-68 の型である** — 実体のない段階で隣接する運用機構まで文書で閉じようとして輪に入る。
**計画書は「本改訂は H-68 の型に該当しない」と書いたが、開始条件を書いた時点で該当し始めていた。**
**この判断の誤りを記録する。**

### 縮小した範囲(2 項目を落とし 5 項目 → 3 項目)

| 落とした項目 | 落とす理由 | 受け取り先 |
| --- | --- | --- |
| **開始条件**(① `passed` 判定 ② 検証器合格 ③ 4-6 マージの 3 条件 + 逆行条件) | **実効を持たなかった** — ① `/task-start`・`/implement` のどこにも 3 条件の照合点がなく、通常の導線を通るだけで開始条件を確認せず実装できる(2 周目 P1-2)② **4-6 のマージからマージ後検査の完了までの区間**と**進行中の実装単位**に穴が残る(同 P1-1)。**13 章が 10.1 より弱い条件を新設しないことが正しい** | **10.1 と各実装計画書**。13 章は「**着手可否の判断は 10.1 の定めと各実装計画書による**」とだけ書く |
| **適用する規約**(規約の正は `AGENTS.md`・rules 3 本の未整備は着手を止めない) | **非阻害化の根拠が事実と一致しなかった** — `AGENTS.md` には 8.6 が rules に持たせる **FastAPI 構成規約・エラー処理・状態管理・コンポーネント規約・ゴールデンベクタの読み方・E2E 命名**が**存在しない**(2 周目 P1-3。実測で確認)。**「規約自体は `AGENTS.md` が持つ」は私の未検証の断定だった** | **TSK-303**(パス別規約の整備)へ全面的に送る |

### 採否

| # | 重大度 | 起因 | 分類 | 指摘の要旨 | 採否と内容 |
| --- | --- | --- | --- | --- | --- |
| 1 | P1 | 起因 | (A) | 4-6 のマージ時点で開始可能にすると、**マージ後検査の完了までの区間**と**進行中実装**に未検証の `M` を基盤にする経路が残る | **射程縮小により解消**(開始条件の項を削除)。13 章は弱い条件を作らず、判断を 10.1 と実装計画書へ戻す |
| 2 | P1 | 起因 | (A) | 開始条件に**実行上の照合点がない** — `/task-start` に製品実装の識別も 3 条件の照合もなく、`/implement` の検査は plan status と承認だけ | **射程縮小により解消**(同上)。機構のない条件を正本に置かない |
| 3 | P1 | 起因 | (A) | **`AGENTS.md` に 8.6 が rules に持たせる内容が存在しない** → 「規約自体は `AGENTS.md` が持つ」が事実と不一致 | **射程縮小により解消**(適用する規約の項を削除)。**TSK-303** へ全面送り |
| 4 | P1 | 起因 | (A) | 「**本章では**順序・群・段階を定めない」が同じ章の**柱書・Phase 表・順序規則を文言上否定**する。限定が「製品機能について本節では」になっていない | **採用**。「**本節は製品機能の実装について**順序・群・段階を定めない(**本章の Phase 間の順序規則は変更しない**)」へ限定した |
| 5 | P1 | 起因 | (A) | 「進捗とタスク対応の機械化は実装時にも再検討しない」が**広すぎる** — 既存の禁止は「論理スロットと Notion タスクの対応」限定であり、6.1・7.6 は `feature_status.py` で進捗を機械判定している | **採用**。射程宣言を「**Phase 4 の論理スロットと Notion タスクの対応**の機械化」に限定し、**`feature_status.py` による feature 進捗の機械導出は従来どおり適用され本改訂は触れない**ことを明記した |
| 6 | P1 | 起因 | (-) | **計画書が反映後の契約に同期されていない** — 「追加する文言(逐語)」「DoD」「やらないこと」が旧開始条件・旧 ADR 援用のまま | **採用**。逐語節・射程宣言・DoD・やらないこと表を**縮小後の内容へ全面同期**した |
| 7 | P2 | 非起因 | (-) | 6.3 のコードレビュー体制に「**※提案**」表記が残存(88 行では確定済み)。役割分担表だけを見ると未決と誤認する | **不採用**。**6.3 はコア領域の節・`guard_paths` の対象**であり本改訂の射程外(歯止め 2 の趣旨)。**別タスク(実装追随)へ送る**。計画書「やらないこと」に受け取り先を記録した |

**採用 3 件・射程縮小による解消 3 件・不採用 1 件((B) 0 件)。**

### 歯止め 5 の「緩和」を撤回する — 前周の自己申告は誤りだった

前周に「歯止め 5(群・順序・段階の語を含まない)の機械検査を意味判定へ改める」と自己申告したが、**これは不要だった**。
**計画書の歯止め 5 の逐語は「追加節に『群』『段階』『順序を定める』を含まない」**であり、
「ADR-003 が定める導入順序に従う」は **`順序を定める` を含まない**ので**元の検査に通る**。
**私が場当たりに走らせた検査(素の `順序` を拾う)が計画書の歯止めより厳しかっただけ**である。
**緩和は撤回し、計画書の逐語どおりの検査に戻す**(2 周目 P1-4 の後半で指摘された)。

### 次周

**射程縮小の裁定後は全文レビュー**(`7.3-6` の射程縮小の行 → `7.3-4`)。**連続カウントは 0 へリセット**。
