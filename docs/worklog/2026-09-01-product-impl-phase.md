---
date: 2026-09-01
topic: 設計書 13 章へ製品実装フェーズの追加
branch: feature/product-impl-phase
---

# 作業ログ: 2026-09-01 設計書 13 章へ製品実装フェーズの追加

## やったこと

## 決定

## 未決・次の一歩

## やったこと

### ステップ 1 — 13 章へ「製品機能の実装」の節を追加(`6ff3abe`)

**Phase を追加せず、「対象外である」ことを明記する形**(人間の裁定 = 丙)。**追加は 6 行・項目 4 個。**

歯止め 7 点を機械検証し**全 PASS**:

| # | 歯止め | 結果 |
| --- | --- | --- |
| 1 | Phase 表の行数が不変 | PASS(6 → 6) |
| 2 | **Phase 0〜5 の行が `origin/develop` とバイト一致** | PASS |
| 3 | **「Phase 4 の分割」節(42 行)がバイト一致** | PASS |
| 4 | 追加が 1 節・項目 5 個以内 | PASS(6 行・4 項目) |
| 5 | 禁止語が肯定文で現れない | PASS(3 件すべて「定めない」の否定文) |
| 6 | 柱書「Phase 4 のみ分割」が不変 | PASS |
| 7 | 10.1 の「実装期」が不変 | PASS(2 件) |
| — | 差分が設計書 1 本のみ | PASS |

**検査 5 は当初 FAIL した。**単純な文字列検索で「群」「段階」を禁止したため、
**「群・段階を定めない」という否定文まで違反と判定した**。歯止めの趣旨は**定めないこと**なので、
**検査を文脈判定(同一行に「定めない」があるか)へ直した**。内容は初めから適合していた。

### ステップ 2 — 後続タスクの起票

**TSK-280「同期プロトコル — イベント契約の骨格の実装(製品コードの 1 本目)」**(高 / 8pt)。
**採番を Notion で確認済み**(TSK-275 で踏んだ MCP の欠陥は再発せず)。

**射程を `sync-protocol.md` 4 章「イベント契約の骨格」のみに絞った。**
7-2(キューの状態遷移)・7-4(クライアント側の規律)・7-6(undo)・7-7(ローカル書き出し)は
**すべてイベントの型に依存する**ため、イベント契約が全部の前提。**かつ状態機構も永続化も含まないので最小。**

## 記録する副作用(3 点 — 計画書 §4-(4))

### ① 10.1 の「実装期」を触らない判断

10.1 の CI ジョブ表は `consistency` / `e2e` の導入時期を「**実装期**」としている。
**本改訂で「実装期 = 製品機能の実装」と同定することを検討したが、触らないことにした。**

**理由**: **「実装期」を定義する記述がリポジトリ内に存在しない**
(要件書 `:948` も「一致性・越境・E2E・同期故障系は**実装期のため対象外**」と書くだけ)。
**定義を与えると要件書との整合判断が必要になり、射程が広がる。**
→ **別タスクへ送る。**

### ② 台帳の行番号典拠が既に破損しており、本改訂で +1 ずれる

**`docs/development/harness-evaluation.md` の 4 箇所(`:49`・`:1097`・`:1099`・`:1102`)が
`13 章 :825` と書いているが、該当規定(4-6 の `/task-done` で `P4-後` を起票する形)は
現在 `:850` であり、約 25 行ずれて既に破損している。**

**本改訂で 13 章に 6 行足すため、さらに +6 ずれる。**
→ **本改訂では直さない**(台帳は射程外)。**別タスクへ送る**(台帳の実装追随・版は上げない)。

### ③ 設計書は NFR-021 の失効対象パスである

`.claude/nfr021-invalidating-paths.json` は `default: "invalidating"` で、
`allowlist` は `/docs/ops/nfr021-acceptance/**`・`/docs/worklog/**`・`/docs/features/**` の 3 つのみ。
→ **`docs/development/dev-harness-design-2026-08-07.md` は既定で失効側に落ちる。**

**帰結**: **13 章の編集は、次回リリース時の `gate_kind: release` の失効判定で差分として現れる。**
**ただし済んだ Phase 4 の受入(`result: passed`・`T = 3873ce85`)は遡って無効にならない** —
失効判定は **`T` → 当該証跡の候補 SHA の範囲**だけを見るため、本改訂は判定範囲より後の変更である。

## PR #35 との関係(人間の裁定 2026-09-01)

**他の担当者の PR #35(`feature/gate-convergence-rules` = TSK-273)が設計書 v1.12 を持っている。**
中身は **7.3 確定ゲートの収束規則の全面再設計**(`7.3-1`〜`7.3-8`)で、**approved 済み**
(敵対レビュー **反映 12 周**・指摘 54 件全採用・P0 全周ゼロ)。**あわせて台帳 `H-68` を「対応済み」へ**
変更している(対応案 (a)(b)(c) の 3 案すべてを条文化・TSK-240 解消)。

| やること | いつ | 理由 |
| --- | --- | --- |
| ステップ 1〜3 | **先行して行う** | #35 に依存しない |
| **確定ゲート(初回敵対レビュー)** | **#35 マージ後** | **`7.3-1`「適用版 = 初回敵対レビュー実行時点で approved の本節の版」**。先に走らせると適用版が **`v1.11` に固定**され、**`v1.12` の歯止めが永久に効かなくなる** |
| **PR のマージ** | **#35 の後**(rebase してから) | **#35 は本リポジトリの PR #34 が原因で既に CONFLICTING**。**先にマージすると相手のコンフリクトが増える** |

**版は最初から `1.13`**(`1.12` は #35 が使う)。

### コンフリクトの実体(測定済み)

| ファイル | develop 側(= PR #34 = Claude) | #35 側 |
| --- | --- | --- |
| `docs/README.md` | +2 -2(台帳行と ADR 行の最終更新日) | +3 -3 |
| `docs/development/harness-evaluation.md` | **+94 -1**(`H-85` 新設・`H-22`/`H-42` の実測・候補 1 件・変更履歴) | +13 -6(`H-68` を「対応済み」へ) |

**内容は競合していない**(こちらは `H-85` を足し、#35 は `H-68` を直している)。
**衝突は変更履歴表の同じ位置と `H-*` 節の並び = 追記位置の問題。**

## ◎ `H-68` の引用の訂正(本セッションの Claude の誤り)

**Claude は本セッション中、「H-68 が『Phase 4 の分割で 13 章の確定ゲートが 18 周の閉じた輪に
入った』と記録している」と繰り返し述べた。その逐語は H-68 に存在しない。**

| H-68 が実際に書いていること | 典拠 |
| --- | --- |
| **1 件目 = 設計書 v1.7 の確定ゲート 12〜15 周目**。「**検証器が 1 行も存在しない段階で運用機構を机上設計し、12〜15 周目の P1 11 件中 11 件が直前の周の修正に対する指摘になった**」 | `harness-evaluation.md:1022` |
| 「本件は 25 周、**1 例目は 18 周**」 | 同 `:1024` |
| **H-68 は「Phase 4 の分割」という語を使っていない** | — |
| **18 周全体が閉じた輪ではない**。閉じた輪だったのは **12〜15 周目** | 同 `:1022` |

**「Phase 4 の分割」と明示して結びつけているのは、取り下げ済みの `feature/implementation-roadmap` の
計画書だけ**である。

**H-68 の本質は「実体(検証器・生成物・ベクタ)が存在しない段階でフィールド設計まで文書で
閉じようとすると輪に入る」**であり、**本改訂は細部設計を一切行わないので該当しない。**

## Phase の役割について(本タスクの出発点)

**設計書 13 章の Phase は「開発ハーネスの導入単位」であって、製品機能の作業単位ではない。**

| 典拠 | 内容 |
| --- | --- |
| 設計書の位置づけ | 「三者で pitchlog を開発するための**開発ハーネス**(開発フロー・規約・権限・自動化・ドキュメント管理…)」 |
| 13 章の章題 | 「**段階導入計画**」— 段階導入するのは**本設計書の中身** |
| **Phase 0** | **本設計案の確定(approved 化)** ← Phase が何の単位かを決定づける |
| 13 章の箇条 | 「Phase 1〜3 はコードが無くても成立する。**Phase 4 は設計フェーズの進行と同期させる**」 |

→ **製品機能を Phase で刻む設計ではなかった。** Phase 4 で骨格を作り、そこから先は
**要件書 → 設計 → 6.1 の実装計画書ゲート → 実装**へ引き渡す想定である。

**Claude の誤りの訂正**: セッション中「製品コードのタスクが 0 件だから作れない」と説明したが、
正確には**「置き場が無いから起票されていない」**であり、**設計は既にあった**。
