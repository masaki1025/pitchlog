---
date: 2026-09-08
topic: データモデルのスキーマ本体と ORM 導入(TSK-342)
branch: feature/data-model-schema-orm
---

# 作業ログ: 2026-09-08 データモデルのスキーマ本体と ORM 導入(TSK-342)

## やったこと

### 着手(`/task-start`)

| 項目 | 値 |
| --- | --- |
| Notion | [TSK-342](https://app.notion.com/p/3d593b75e68781958074ed04dff7dab7)(`進行中`) |
| ブランチ / worktree | `feature/data-model-schema-orm` / `../pitchlog-worktrees/feature-data-model-schema-orm`(`f0eeae1` 起点) |
| 開始条件 | **満たしている** — 前タスク TSK-335 は PR #49 でマージ済み(`f0eeae1` / 2026-09-08) |

### 計画段階の調査(`/investigate`)

**3 観点を並列で委任**(spec-checker = 要件突合 / decision-tracer = 決定経緯 / legacy-analyst = 旧システムの事実)
+ **自分で 3 件を実測**。統合先は [research.md](../features/data-model-schema-orm/research.md)(488 行)。

**自分の実測 3 件**:

1. **候補案の規模と節構成** — 1,866 行。**1〜12 節が移送対象**。**13 節「禁止事項の照合」は対象外**
   (T250P の `DM-MT-01` が `absent-section: "13"` で**正本に 13 節が無いこと**を検査する)
2. **引用形式の分布** — `REQ:<行>` **286 件** / その他の `<file>:<行>` 5 件 / `docs/legacy/` への行引用 **0 件** /
   安定 ID 142 件。**参照行番号の範囲は 83〜1231 で要件書 1295 行を超えない**(= 行番号だけ見ると壊れていないように見える)
3. **`FR-015` の逐語の位置と射程** — 現行 357 行目の受け入れ基準の中。**禁止対象は「同一チーム内の背番号の重複」に限定**

**decision-tracer が Bash を持たず辿れなかった履歴を、自分で埋めた**(research.md 1-1)。これが**本調査で一番大きい発見**。

### 自分の誤りの是正 3 件(research.md 0 節に記録)

| # | 誤り | 是正 |
| --- | --- | --- |
| 1 | 調査依頼に「旧 DB は 13 → 33 テーブル」と**旧側の事実として**書いた | **33 テーブルは版固定アーカイブではなく旧リポの現在位置**(`dd03160`)。`docs/legacy/` は `ed6a20f` 時点の 12(13)テーブルのみ |
| 2 | TSK-271 が「列 9 のみ・UNIQUE なし」と**実測**したと伝えた | **旧 DDL の読解**であって本番データの実測ではない。**重複行の実測件数はどこにも無い** |
| 3 | 引用の行ずれを「**−636 〜 +36**」と測った | **抽出の癖による誤り**(`REQ:n` の直前の逐語を機械的に拾い、別の引用のものを当てた)。条項単位では **+14 〜 +36 で全部プラス** |

## 決定

**まだ決定していない。** 調査の結論は research.md の「結論(要約)」5 件。計画は次の `/plan` で書く。

**調査で確定した、計画に効く事実**:

1. **ORM の矛盾は「同じ日に別タスクが逆向きに決めた」もの** — `2026-08-31 01:00` に TSK-250 が 3 値を固定
   (`a320568`)、**同日 13:19 に検査が実装され**(`3b731ba`)、**23:19 に 10.1 の箇条が入った**(`50b38d0`)。
   **TSK-250 が 10.1 を無視したのではなく、裁定の時点で箇条が存在しなかった。**
   しかも 10.1 の箇条は **TSK-250 自身の調査が出した「ORM と独立にする」という着想**を TSK-270 が正本へ書いたもの。
   **同じ意図から出発して逆の条文になった。**
2. **5.1 の ORM 留保は設計書の初版(v0.9・2026-08-07 02:19)から存在**(decision-tracer の「特定不能」を確定)
3. **`FR-015` との衝突は成立しない** — 一意制約 18 件 + 複合 FK 6 件のうち重なるのは背番号 1 件で、候補案の結論も「張らない」
4. **RLS は要件から導出されない設計判断** — 要件書・改善台帳ともに 0 hits。**本タスクが決める**
5. **`WAIT-01`〜`WAIT-08` の全 8 件が本タスクの射程**。ただし `waiting-data-model.json` は**未作成**

## 未決・次の一歩

### 人間の裁定を計画承認前に取る(4 件・research.md 申し送り)

| # | 論点 |
| --- | --- |
| **A-1** | **`game_lineup_snapshot` の受け皿を作るか** — 旧資料が矛盾(`DL` は 12 種で存在を認めず / `DB` は 13 個目として挙げる。**両者とも `ed6a20f` 時点**)。一方**要件書 `FR-038` が名指しして「別に保全する」と要求**。precedence は 88 列の意味に限定で射程外 |
| **A-2** | **越境テスト再実行ゲートの所有者** — TSK-270 が「実スキーマ適用後に再実行するゲートを TSK-250 の完了条件に置く」と決めたが、**スキーマ本体を作るのは本タスク**になった |
| **A-3** | **移行バッチの書き込み経路** — 決定が存在しない。かつ **probe の実測が候補案 3-2 のロール表と形が違う** |
| **A-4** | **`NFR-018` (b)③「動的コード生成の禁止」が ORM の実行時 SQL 生成に及ぶか** — 要件書に適用範囲の明示が無い |

### 次の一歩

- **`/plan`** で計画書を書く(**コア領域なので敵対レビュー必須**)。上の裁定 4 件は**計画承認前**に取る
- 並行トラックの **TSK-235** は計画段階まで(実装は本タスクのマージ後 — 競合 7 ファイル)


---

## 計画レビュー(2〜5 周目)と承認

| 周 | 指摘 | 反映の要点 |
| --- | --- | --- |
| 2 | 8(P0 2 / P1 6) | fixture 固定位置 / `WAIT-*` closure の所有 / 件数是正 / 対応表 2 層化 / 7.3-7 射程宣言 / 7.4 順守 / 独自停止条件の削除 / 意味条件の `[手動・外部]` 化 |
| 3 | 5(P0 0) | 検査 ①④ の rc 化 / `game_lineup_snapshot` 条件を逐行確認へ / `TSK-` 0 件条件の撤回 / `WAIT-04` 送り先の実タスク化 |
| 4 | 1(P0 0) | fixture の固定を確定ゲート後へ |
| 5 | 1(P0 0) | その固定を**ステップ 13** として表内へ(`tests/fixtures/` と `scripts/` は文書パスでないため、記法なしでは現在地導出が `unknown` に落ちる) |

**人間の承認 2026-09-08・山田正輝**(コミット `48ec1a8`)。13 ステップ。
実行順は **ステップ 1〜12 → 確定ゲート(7.3)→ ステップ 13**。

### 3 周目 `P1-4` の実測(規約の解釈を是正した)

**承認済み正本は送り先の目印として裸の `TSK-<番号>` を使っている** — `docs/design/sync-protocol.md` に
**56 箇所**・要件書に **6 箇所**。**いずれも `app.notion.com` は 0 件**。
→ 設計書 7.4「正本は Notion を参照しない」が禁じるのは **URL 参照と内容の複製**であり、
**タスク ID の目印は禁じていない**。2 周目に自分が置いた「`TSK-` 0 件」条件は過剰だったので撤回した。

---

## ステップ 1: 正本の新設・素材移送・引用の貼り替え・対応表

### 引用の貼り替えをどう機械的に解決したか(**行番号のずれは一定でない**)

下調べの時点では「ずれは **+14 〜 +36** で一定でないため機械的な一括置換では移せない」としていた。
**解決の鍵は、候補案を書いた 16 コミットすべてで要件書が同一内容だったこと**である
(`git show <sha>:docs/requirements/...` の md5 が **1 種**・**1,249 行**)。
→ **行番号引用の解決基準は単一**なので、次の 3 段で全件を機械的に同定できた:

1. **旧行本文の取得** — 基準版(1,249 行)の当該行をそのまま読む
2. **現行版での同定** — その行本文を現行の要件書 v2.7(1,295 行)から完全一致で探す
   (**173/177 が一意に一致**。残り 4 件は個別に解決 — 下記)
3. **囲む安定 ID の算出** — 基準版側と現行版側の**両方**で最内の安定 ID を計算し、**一致することを確認**
   (母集合の正は `scripts/design_relations/req-universe.json` の 8 カテゴリ 212 件)

**下調べで個別照合していた 8 件のずれ(+14・+18・+18・+18・+18・+19・+21・+36)は、
この機械的解決の出力と全件一致した**(独立な再現)。

**個別に解決した 4 件**:

| 参照 | 状況 | 解決 |
| --- | --- | --- |
| 素材 303 行 | 行本文が改稿されていた | `FR-013`(現行 321 行)。「適用されず、破棄されず退避され」は不変 |
| 素材 355 行 | 行本文が `- **受け入れ基準**:` で 25 箇所に重複 | 囲む条で解決し `FR-017`(現行 373 行)。区分 3 固定・拡張ラベル Should・全区分可逆はいずれも現行にある |
| 素材 722 行 | **規範が変わった** | `FR-035`(現行 740 行)。**v2.4 で「閲覧・書き出し = Must」「挿入位置を指定して取り込む = Should」へ優先度分割**。素材が 1 つの Must として書いていた記述は**ステップ 9(`C16`)で分ける** |
| 素材 1055 行 | v2.6 で追補 | `付録A/A-1`(現行 1093 行)。「割合・率の表示には常に母数を併記する」は不変(併記値の定義が加わって強まった) |

### 対応表の構造(`scripts/design_relations/citation-map-data-model.json`)

| 層 | 件数 | キー |
| --- | --- | --- |
| **① `references`** | **145** | 旧参照の文字列(`REQ` 形式 141 + 未修飾パスへの行引用 4)。**候補案 1〜12 節のユニーク集合と exact-set 一致を機械確認済み** |
| **② `occurrences`** | **291** | `(source_line, ordinal)`。**291 組すべて一意**を機械確認済み |
| `companion_lines` | 36 | 「素材 597-606」「素材 722・724・744」型の**2 個目以降の行番号**。検査器の正規表現には掛からないが規範としては引用されている |
| `bare_line_citations` | 5 | `ADR-003 :93-104` 型の裸の行番号(`ADR-003 D-1` / `D-6・D-8` / `D-6` / `決定記録 D-43` ×2 へ) |

**未修飾パスへの行引用 4 件はすべて `docs/legacy/` だった** — 引用規約は版固定アーカイブへの行番号引用を
**許容する**ので、パスを修飾して残した(`docs/legacy/baseball-scoring-db-structure.md:168`・`:176`・`:286` /
`docs/legacy/research/data-layer.md:402`)。**未修飾だと検査器の legacy 判定に落ちて違反になる**のが原因。

### 移送で変えたこと / 変えていないこと

**変えたこと(文書同一性の言い換えのみ)**: 「本書は候補案であり決定ではない」→ 正本の位置づけ /
`**候補案**:` → `**本書の設計**:`(15 箇所)/ 見出しの `候補案: ` を落とす(12 箇所)/
`本候補案`・`本起草` → `本書` / `正本化タスク` → 内容に応じて `TSK-343`・`実機確定タスク`・`本書` /
`docs/features/` 配下(`plan.md`・`research.md`)への参照を**全廃**(6 箇所は事実を残して出所表記を落とした)。

**変えていないこと**: 技術内容は 1 文字も変えていない。**移管欠陥 ID 群の是正はステップ 2〜10** が行う。

**節の移送は全数確認済み** — 候補案 1〜12 節の見出し **84 件**がすべて正本にあり
(改稿した 19 件は 1 対 1 で対応)、正本側の追加は `## 変更履歴` と `### 1-1. 参照規約` の **2 件**だけ。

### 検査の結果(是正前の実測を含む)

| 検査 | 結果 |
| --- | --- |
| **①** 引用形式 | **正本 `()` / 候補案 3 形式** → **rc=0** |
| **②** 文書検査 3 本 | `check_docs_status` / `check_design_propagation` / `check_doc_coverage` すべて **rc=0**(**索引行を入れる前は `check_docs_status` が「docs/README.md の正本一覧に載っていない」で rc=1** — 番人が効いていることを確認した) |
| **③** core-guard | `tests/test_core_guard.py` **45 passed** |
| **④** ID の在/不在 | **rc=1(必須 54 件すべて不在)** — ステップ 1 直後の期待値そのもの。是正の出発点 |
| `## 13.` の不在 | **0 件**(T250P の `DM-MT-01` が検査する条件) |

### 気づいたこと(後続ステップの制約)

- **射程宣言(ステップ 12)は `## 13.` にしてはいけない** — T250P の `DM-MT-01` が
  `absent-section: "13"` を検査する。**`### 12-8.` の下に置く**
- **`P-01`〜`P-60` の台帳に恒久的な置き場が無い** — 正本は 49 件を 104 箇所で参照するが、
  **`docs/design/sync-protocol.md` にあるのは 44 行分**で、`P-04`・`P-16`・`P-40`・`P-45` 等 31 件は
  候補案の `research.md` 3-2 にしかない。**本書の 1-1 節に「台帳の恒久的な置き場は本書では定めず
  受け取り先へ送る」と書いた**。**ステップ 12 の射程宣言で送り先を明示する**
- **正本本文に `REQ:<数字>` と書くと検査 ① が落ちる**(`\bREQ:\d+\b` に当たる)。
  変更履歴で件数を説明する際も「`REQ` 形式の行番号引用」と書く

---

## ステップ 2: RLS の設計判断とテーブル制約

### 本文へ入れたもの

1. **3-1 節に「この採否は要件から導出されない — 本書の設計判断である」を新設**
   - **独立再実測(2026-09-08)**: `RLS` / `row level security` / `行レベルセキュリティ` は
     **要件書 0 件・要件対話の決定記録 0 件・改善台帳 0 件**
   - `NFR-010` が課すのは **① 結果 ② 適用範囲 ③ 検証**の 3 つだけで**実現層を指定していない**
   - 改善台帳 `I-2` が指名する手段も `team_id` 列
2. **採る 3 つの理由**(旧「RLS を採れる根拠」を理由として整理し、2 点を足した)
3. **この判断の帰結**を明文化 — 3-4 節の 3 規則は RLS 採用に**従属して決まる**。要件から独立には導けない
4. **3-4 節に「本書が置く業務的一意性の全数」表(8 行 + 置かない 3 件)**を新設
5. **規則 3「制約違反の詳細を一律に秘匿する」を新設**(旧・規則 2 の手段 (c) から昇格)。
   手段 (c) は**規則 3 への参照だけ**にした(同じ規則を 2 箇所で定義しない — 3-0 節と同じ規律)

### 理由 2 に足した典拠(**旧システムが実際にその状態だった**)

テナント境界は `game.owner_team_id` の 1 列とアプリ層のガードだけで、
**所有権チェックはオプショナル引数を省略すると全データへ到達できた**
(`docs/legacy/baseball-scoring-db-structure.md:176` / `docs/legacy/research/data-layer.md:402`)。
改善台帳 `I-2` が「分離の穴が後追い是正になった」ことを課題に挙げ「利用が 1 チームの段階から組み込む」と
定めた対象そのものである。

### 理由 3 に使った条項の位置を計画から是正した

計画 4 節は「**`NFR-019(b)` の越境テストが『API 直叩きを含む全経路』を要求する**」と書いていたが、
**「API 直叩き含む」は `NFR-019` ではなく `NFR-010` の測定方法にある**(要件書 846 行 —
`NFR-010` の節内。`NFR-019` は CI 常設先として参照されている側)。
→ **本文では `NFR-010` の測定方法として引いた。**

### `tenant_id` を先頭に含まない業務的一意制約が 0 件 — 逐項の証跡

**本書の業務的一意性の全数 8 件**(代理キーは対象外。節番号は本文で機械確認した。**ステップ 3 の裁定 A-1 で 9 件目を追加した** — 下記):

| # | 節 | 一意性 | キーの形 | 判定 |
| --- | --- | --- | --- | --- |
| 1 | 5-3 | べき等キー | `UNIQUE (tenant_id, べき等キー)` | ✅ |
| 2 | 5-3 | (試合, 世代, 連番) の有効な行が 1 件 | `UNIQUE (tenant_id, 試合, 世代, 連番) WHERE 置換されていない` | ✅ |
| 3 | 6-1 | 記録権の現世代が 1 件 | `UNIQUE (tenant_id, 試合) WHERE 失効していない` | ✅ |
| 4 | 7-1 | テナント自身のチームレコードは高々 1 件 | `UNIQUE (tenant_id) WHERE kind = 'self'` | ✅ |
| 5 | 9-1 | 参加は `active` が最大 1 件 | `UNIQUE (グループ, 参加テナント) WHERE 状態 = active` | ✅ **グループの内側**(制御資源 — `NFR-010` が「記録・集計とは別のクラス」と定める) |
| 6 | 10-3 | 語彙(チーム拡張可の層) | `UNIQUE (tenant_id, キー名)` | ✅ |
| 7 | 10-3 | 語彙(システム固定の層) | `UNIQUE (キー名)` | ➖ 対象外(テナントに属さない) |
| 8 | 10-4 | 付録C のシステム設定値 | `UNIQUE (キー名)` | ➖ 対象外(同前) |

**意図的に置かない 3 件**: 背番号(7-2 — `FR-015` / `P-15`)/ チーム名(7-1 — `FR-039`)/
試合の業務的一意性(5-1 — 要件に明示がない)。

→ **✅ が 6 件・➖(テナントに属さないもの)が 2 件で、`tenant_id`(またはテナントに閉じた親)を
先頭に含まない業務的一意制約は 0 件。**

**節番号の同定方法**: 見出しを走査して各行の所属節を計算し、`部分一意索引` / `WHERE 状態 = active` /
`一意制約はキー名でグローバル` 等の実文言の位置から節を機械的に取った
(当初 `kind = 'self'` を 3-6 節、設定値を 10-5 節と書いていたが、**実測で 7-1 と 10-4 に是正**した)。

### 移送の残滓 3 件を同時に直した

**`Web 調査 6-2 節`・`Web 調査 6-3・6-4 節`** という参照が 3 箇所残っていた(候補案 `research.md` の節番号で、
ステップ 1 の置換は `research.md` の文字列がある行しか拾えていなかった)。
→ **`PostgreSQL 公式ドキュメント` / `Alembic・SQLAlchemy の公式ドキュメント`** へ改めた。

### 検査

| 検査 | 結果 |
| --- | --- |
| ① 引用形式 | 正本 `()` / 候補案 3 形式 → **rc=0** |
| ② 文書検査 3 本 | **rc=0** |

---

## ステップ 3: 裁定 A-1・A-3 の反映

### A-3(移行バッチの書き込み経路)— 3-2 節

**候補案の内部矛盾を閉じた**: 3-2 節は「マイグレーション用ロール(テーブル所有者)は**バイパスする(所有者)**」と
書きながら、同じ節の末尾で「**全テーブルへ `FORCE ROW LEVEL SECURITY` を推奨**」していた。
**FORCE は所有者もポリシーに従わせる**ので、この 2 つは両立しない。

**反映内容**:

- 節見出しを **「ロールを 4 つに分ける(論点 3・裁定 A-3)」** へ
- ロール表の**マイグレーション用**の RLS 欄を「バイパスする(所有者)」→
  **「従う — 全テーブルへ `FORCE ROW LEVEL SECURITY` を掛けるため、所有者でもポリシーの適用を受ける」**
- **`#### 移行バッチの書き込み経路(裁定 A-3)`** を新設 — 専用ロール + `BYPASSRLS`。
  **`BYPASSRLS` は FORCE でも拘束されない**ので、**全テーブルに FORCE を掛けたまま一括投入できるのはこの経路だけ**
- **8-2-B 節の「常時バイパス経路を作らない」と矛盾しない理由を表で明示**
  (稼働: 常時 / 移行期間のみ・ログイン: `NOLOGIN` / `LOGIN` 必須・到達経路: 関数の `EXECUTE` / バッチの接続のみ)
- **このロールに課す条件 6 件**(期間限定・アプリから到達しない・DDL を持たない・書き込み先の限定・監査・検査対象)
- **却下案 4 件**(所有者で投入 / 移行中だけ FORCE を外す / ポリシーに従わせて `set_config` で切り替え / superuser)

### A-1(移行元の最終オーダーの保全先)— 12-3 節

**`#### 移行元の最終オーダーの保全先(裁定 A-1)`** を新設し、**物理実体として定義**した
(表・所属・キー・FK・列・由来・読み方の禁止・突合・削除の 9 項目)。

**要点**:

- **キーは `UNIQUE (tenant_id, 試合, チームレコード)`**・**FK は複合参照** — 3-4 節の規則 1・2 に従う
  (**ステップ 2 で作った「業務的一意性の全数」表への追加が必要**。下記「次のステップへの持ち越し」)
- **選手 ID へ解決しない** — 旧側は選手を名前・背番号の JSON 文字列で持つため、
  **原本のまま保持**し、解決結果は別の nullable 列(88 列写像で列 63-71 に採った原則と同型 — 裁定 `R-1`)
- **7-4 節のスタメンとは別の表**である。混ぜると先発として読まれる経路ができる(**P-45** に反する)
- **突合の相手がこの表**である — 「最初のプレイ行と食い違う試合は交代のあった試合として警告レポートへ列挙」
  (`FR-038`)の比較対象

**旧側の存在矛盾の扱い**: `docs/legacy/research/data-layer.md:17` は全 12 種として挙げず、
`docs/legacy/baseball-scoring-db-structure.md:176` は 13 個目として挙げる(**両者とも同一リビジョン**)。
→ **存在しなければ移行 0 件で終わるだけ**で、要件が名指しする保全先を持たない方が害が大きい、という
裁定の理由を本文に書いた。

### 機械検査の実測(**計画の「是正前 1 件」は誤りだった**)

計画 4 節のステップ 3 は「**「テーブル所有者」+「バイパス」が同一文に現れる箇所が 0 件**
(**是正前 = 候補案に 1 件**)」としていたが、**実測では候補案に 2 件**あった:

| # | 箇所 | 性質 | 処置 |
| --- | --- | --- | --- |
| 1 | 3-2 節冒頭「テーブル所有者は既定で RLS をバイパスするため…」 | **実効する設計記述**(矛盾の本体) | 書き換えた |
| 2 | 12-7 (1)「① 『`SECURITY DEFINER` は所有者権限で走るため RLS をバイパスする』」 | **過去の誤りの記録**(誤った記述の引用) | 「RLS の適用を受けない」へ語を改め、記録としての意味は保った |

**判定方法**: 本文を `。` `改行` `|`(表セル)で切って文に分け、
**両語が同一の文に現れる件数**を数えた。**正本 0 件 / 候補案 2 件**。

### 検査

| 検査 | 結果 |
| --- | --- |
| 所有者バイパスの同一文 | **正本 0 件 / 候補案 2 件** → rc=0 |
| ① 引用形式 | 正本 `()` → **rc=0** |
| ② 文書検査 3 本 | **rc=0** |

### 次のステップへの持ち越し

- **ステップ 2 で作った「本書が置く業務的一意性の全数」表へ、A-1 の
  `UNIQUE (tenant_id, 試合, チームレコード)` を足す**(表自身が「表にない一意性を後から足すときは
  この表へ行を足して同じ判定を通す」と定めている)。**本ステップで足した**

---

## ステップ 4: 順序系の写像(`C01`〜`C05`・`C08`)+ `WAIT-05`

### 同期プロトコル正本との逐行照合(**`U-7` の要求**)

**同 11-4 `U-7`**(sync-protocol.md 2077 行): 「**D1 と D2 の用途分離のデータモデルへの伝播**(5-1)。
**出場区間の境界・下流再計算の起点には D2 を使い、D1 を使わない**ことを製品データモデルへ反映する」/
本書の境界外 = 「列・索引・表分割」/ 受け取り先 = TSK-250(→ TSK-335 の裁定 `R-1` で本タスクへ移管)。

**同 5-1** の規範(逐語): 「**同期連番(D1)は同期の順序・欠落の判定と undo の逆順に用い、
論理順序(D2)はプレイの投影・出場区間の境界・下流再計算の起点に用いる**」/
「過去位置へ挿入するイベントは、**D1 では生成時点の末尾**に置かれ、**D2 では指定された過去位置**に置かれる」/
「**出場区間の境界または再計算の起点に D1 を使ってはならない**」。

**同 5-2** の確定(起草時は本書の (B) 送りだった): 順序ラベルの方式は **整数の間隔(gap)方式**。
**有理数(分母が指数的に伸びる)・LexoRank(単一書き手なので衝突耐性が不要)・相対参照(範囲集計ができない)
はいずれも却下**。

### 4 箇所 + 識別子表の照合結果

| # | 箇所 | 是正前 | 是正後 | ID |
| --- | --- | --- | --- | --- |
| 0 | **4-3 節 識別子表** | 「**イベント連番**」1 本だけ。役割は「順序と欠落の判定」 | 「**同期連番(`D1`)**」へ改称し**「出場区間の境界・再計算の起点には使わない」を明記** + **「論理順序(`D2`)」の行を新設**((試合, 記録権世代)内で全順序・プレイ投影/境界/起点が役割) | `C01` / `C02` |
| 1 | **5-2 節 用途対応表** の「論理順序」 | 「同期連番とは別の順序値を持つ。**具体の順序ラベル方式は (B)**」 | **`D2`**。**物理表現は整数の間隔(gap)方式で確定**(同 5-2 を取り込み。**(B) 送りを解除**) | `C03` |
| 2 | **5-2 節 用途対応表** の「出場区間の境界」 | 「**イベント連番**で持つ」 | **`D2`** で持つ。**`D1` を使ってはならない**(同 5-1) | `C04` |
| 3 | **5-2 節 用途対応表** の「再計算の起点」 | 「**イベント連番**。行識別子でもプレイ番号でもない」 | **`D2`**。**`D1` でも行識別子でもプレイ番号でもない**。**undo の逆順だけは `D1`** | `C05` |
| 4 | **6-2 節 出場区間(投影)** | 有効開始・有効終了 = 「**イベント連番またはプレイ番号**」 | **`D2`**。**`D1` でもプレイ番号でもない** | `C04` |
| 5 | **6-3 節 状態補正の位置** | 「以降の再計算の起点」だけ | 「**`D2`** で表す(**`D1` を使わない**)」 | `C05` |
| 6 | **12-3 節 移行** | 「**イベント連番** — 旧行の行識別子の順序で振る。**これが論理順序の正になる**」 | **`D1` と `D2` の両方**を旧行識別子の順序で振る。**移行では並びが一致するが、同じ値を 1 本の列で兼用しない**(移行後に `FR-007` の任意位置挿入が入ると分岐するため**列は最初から 2 本**) | `C08` |

**「到着順」の語は全廃した**(是正前 5 件 → **0 件**)。`D1` を説明する語は
**「キュー追記時に採番される順」**へ統一した(同 5-1 の「生成時点の末尾に置かれる」と整合)。

### 12-7 (2) を「解消済み」へ

起草時は **「イベントの順序」を収束しなかった 3 領域の (2)** として、
「順序ラベルの方式 / 挿入時に再採番するか / 同期連番の意味の正」を未確定のまま同期設計へ送っていた。
**同期プロトコル設計が `v0.3` で `approved` になり 3 件すべてが確定した**ので、
**(2) を「解消済み」と明記し、決まっていないことを「なし」にした**。
「なぜ文書だけでは閉じなかったか」は**記録として過去形で残した**。

### 検査

| 検査 | 結果 |
| --- | --- |
| ④ ID の在/不在 | **`C01` 3 / `C02` 1 / `C03` 1 / `C04` 2 / `C05` 4 / `C08` 3 / `WAIT-05` 3**(是正前は全件 0)。`C13` は **0 件**を維持(裁定 `R-6` — TSK-250 の所有) |
| 「到着順」 | **0 件**(是正前 5 件) |
| ① 引用形式 | 正本 `()` → rc=0 |
| ② 文書検査 3 本 | rc=0 |
| ④ 全体 | **rc=1** — 残り 47 件はステップ 5〜10 の担当分(期待どおり) |

### 気づいたこと(重要)

**欠陥 ID(`C01`〜`C16` 等)の定義そのものはリポジトリに存在しない。** T250P にあるのは
**群の件数と所有**だけで、**ID ごとの `source` / `location` / `invariant` は TSK-250 のステップ 4 が
これから作る**(`defects-data-model.json` のベースライン 78 件)。
→ **本タスクは計画書の各ステップ行が書いている「何を直すか」と、同期プロトコル正本の該当節を
典拠にして是正している。** ID と是正内容の対応は**本 worklog が唯一の記録**であり、
**ステップ 12 の `closure-handoff-data-model.json` でそれを機械可読にして TSK-250 へ渡す**。
ステップ 12 の設計時に、**`closure_evidence` に「反映節」だけでなく「本 worklog の該当行」も入れる**。

---

## ステップ 5: 削除経路の分離(`C06`・`M06`)

### 典拠(同期プロトコル設計 11-2 — **データモデル影響差分**)

**この節が本タスクのステップ 5〜8 の一次典拠である。** 逐語で確認したもの:

- **`W1`**: 確定済みイベントの修正・削除は**新しい変更イベントとして追記**し、**既存イベントを書き換えない**
- **参加区分表**(11-2 の直後): **#10 プレイの修正 / #11 プレイ行の論理削除 / #12 交代イベントの修正** —
  いずれも **従属**・**変更版順を持つ**・**自身の `D2` を持たず、対象参照と確定版が必須**
- **`W3`**: `P3`(= `D1` なし変更イベント)は **`D1` を持たない** / **`W3-c`**: `P3` は **prefix コミットの対象外**
- **`W3-a`**: `V11` は対象の期待版で、**一致するときだけ受理**して**対象の確定版を進める**
- **`W3-b`**: **変更版順は `D1`・`D2` とは別の順序**で、**論理再生順に使わない**
- **`W4`**: **進行中の変更操作では `V12` を照合**し、**終了後の変更操作では `V12` を前提にしない**
- **`V3`/`V6`/`V10`/`V11`**: `P3` は **`D4` を持たず**復旧世代だけ照合 / **`D2` を持たず**対象に従属 /
  **`V10` は必須** / **`V11` は必須**
- **影響差分 9**: 対象は **`(試合, 対象 D4, 対象 D1)`** で名指す。**対象 `D1` だけの列では不足**
- **影響差分 13-a**: **対象側に「確定版」の列**が要る。**`D1`・`D2` とは別**
- **影響差分 4**: **`D2` を持たない種別がある** → **`D2` を `NOT NULL` にできない、または別表に分ける判断**

### 是正内容

| 箇所 | 是正前 | 是正後 | ID |
| --- | --- | --- | --- |
| **4-2 節の箇条** | 「『プレイ行の論理削除』は投影側のフラグであり、**正は墓標・改訂イベント**」 | 「**正は変更イベント(参加区分 #11)**。投影側のフラグはその投影」+ **墓標(#8)・改訂版(#9)を流用しない**理由(同期の拒否と再送の仕組みであって訂正ではない) | `C06` |
| **5-3 節に新設** | 変更イベントの実体が無かった | **`#### 変更イベント(参加区分 #10〜#12)`** を新設 | `C06` / `M06` |

**「正は墓標・改訂イベント」は 0 件**(是正前 1 件)。

### #10〜#12 に対応する実体・属性・制約(**逐項の証跡**)

| # | 事項 | 本文での定義 | 典拠 |
| --- | --- | --- | --- |
| 1 | 置き場 | **操作イベント表に同居**。種別で識別。別表に分けない | `W1` / `4.0-2` |
| 2 | `D1` | **持たない**(採番しない)。**列は NULL 可**。prefix コミットの対象外 | `W3` / `W3-c` |
| 3 | `D2` | **持たない**(対象に従属)。**列は NULL 可** | `V6` |
| 4 | `D4` | **持たない**。要求時の**復旧世代だけを照合** | `V3` |
| 5 | 対象参照 | **`V10` = `(試合, 対象 D4, 対象 D1)` の複合参照**が必須 | `V10` / 影響差分 9 |
| 6 | 期待版 | **`V11`** が必須。**一致するときだけ受理**し対象の確定版を進める | `V11` / `W3-a` |
| 7 | 対象側の確定版 | **対象イベント側に確定版の列**。`D1`・`D2` とは別系列 | 影響差分 13-a |
| 8 | 変更版順 | **`K4`**。対象ごとの期待版・確定版の連鎖。**論理再生順に使わない** | `K4` / `W3-b` |
| 9 | 記録権証明 | **進行中は `V12` を照合・終了後は前提にしない** → 経路を分ける | `W4` |
| 10 | べき等キー | **`V1`** で無条件。**`(テナント, D5)` で一意**(3-4 節 規則 1) | `V1` |
| 11 | 原子境界 | **`P3`** の経路(`T1`・`T2`・`T4`・`T6`・`T7`) | 影響差分 10 |

**11 項目すべてを本文の表として置いた。**

### `M06`(削除機構の混同)の是正

**11-1 節の削除 4 系統**(試合・選手・テナント・制御資源の**エンティティのライフサイクル**)と、
**#11 の確定済みプレイ行の訂正**は**別の話である**ことを明記し、**同じ機構にまとめないと書いた**。

### `NOT NULL` にできないことの帰結を明記(影響差分 4 の判断)

**本書は変更イベントを操作イベント表に同居させる**ので、**`D1`・`D2`・`D4` は NULL 可**になる。
その帰結として:

- **`(試合, 世代, 連番)` の部分一意インデックスの述語に `D1 IS NOT NULL` を加える**
  (PostgreSQL の `UNIQUE` は NULL を重複と見なさないが、**述語で明示して意図を読めるようにする**)
- **`D1` が NULL のイベントの順序は変更版順で決まる**。**`D1` の欠落として扱ってはならない**(`W3-c`)

### 検査

| 検査 | 結果 |
| --- | --- |
| ④ | **`C06` 3 件 / `M06` 3 件**(是正前 0) |
| 「正は墓標・改訂イベント」 | **0 件**(是正前 1 件) |
| ① 引用形式 | `()` → rc=0 |
| ② 文書検査 3 本 | rc=0 |

---

## ステップ 6: 識別子と一意性(`M01`〜`M04`・`M07`・`M08`・`M17`)+ `WAIT-03`

### 前提の再確認

同期プロトコル設計 **4-1** は「**列名・型・NULL 性・FK・索引・表分割は決めない**」と明記している。
**それを決めるのが本節である**(同 11-2 が「物理構造の決定は各受け取り先が担う」と書いている側)。

### 5-3 節へ「識別子と一意性の物理写像」を新設(7 行の表)

| 値 | 型 / NULL 性 | 一意性・制約 | ID |
| --- | --- | --- | --- |
| `D1` | `bigint` / **NULL 可**(変更イベント #10〜#12 は持たない) | `UNIQUE (tenant_id, 試合, D4, D1) WHERE D1 IS NOT NULL AND 置換されていない` | `M01` |
| `D2` | `bigint` / **NULL 可**(変更イベント・その場登録 #6・墓標 #8 は持たない) | **一意制約を置かない**(`O2` の局所再採番で値が動く) | `M02` |
| `D4` | `bigint` / **NULL 可**(`V3`) | 記録権世代表側で `UNIQUE (tenant_id, 試合) WHERE 失効していない` | `M04` |
| `V10` | `(試合, 対象 D4, 対象 D1)` の 3 列 | **複合 FK**。**対象 `D1` だけの単一列にしない**(影響差分 9) | `M07` |
| `V11` + 確定版 | `V11` は変更イベント側 / **確定版は対象イベント側** | 受理時に「`V11` = 対象の確定版」を検査し確定版を進める。**`D1`・`D2` とは別系列** | `M08` |
| 一時 ID 写像 | 一時 ID は UUID | `UNIQUE (tenant_id, 一時 ID)`。**写像行の有無が状態の一部**(`C4`) | `M17` |

**業務的一意性の全数表(3-4 節)へ 2 行を反映**: 5-3 の `(試合, D4, D1)` を
**`D1 IS NOT NULL` 付きの述語へ精緻化**し、**一時 ID 写像の `UNIQUE (tenant_id, 一時 ID)` を追加**した
(表自身の規律「表にない一意性を後から足すときはこの表へ行を足す」に従った)。**全数は 10 行**になった。

### `WAIT-03`(`D2` の数値型)— **`bigint`** と根拠 4 点

1. **gap 方式は値の間に隙間を空ける**ので**行数よりはるかに広い値域が要る**。1 試合 250〜350 行
   (`6.1/毎球データ`)で、`integer`(約 21 億)だと**初期間隔 2^20 で 1 試合 2,000 行程度で枯渇域**
2. **`numeric` は採らない** — 有理数方式を却下した以上**任意精度が要らず**、索引サイズと比較コストが重い
3. **文字列ラベルは採らない**(同 5-2 が却下)
4. **`bigint` は標準型**で `7.1` に適合

**初期間隔・再採番の閾値は本書では決めない** — 同期プロトコル設計 5-2 が
「**実クエリが 1 本も無い段階で定数を確定させない**」として **(B) 論点 26** へ送っている。
**型だけを決める理由**: 型が決まらないと `models`(TSK-343)が書けない。

### `M03`(`D2` に不変前提の索引を張らない)— 11-3 節の索引原則 **8** として追加

**`D2` は `O2` の局所再採番で値が動く**(影響差分 2・3・26)。したがって:

- **一意キー・主キー・FK の参照先にしない**
- **他表へコピーしない**(投影行・サイドカー・キャッシュの結合キーに使わない。結合キーは
  `(試合, D4, D1)` または行識別子)
- **`D2` 値を埋め込んだ部分索引の述語や永続カーソルを作らない**
- **`D2` を含む索引は範囲・ソート用に限る**

### 手動条件の充足

| 条件 | 充足 |
| --- | --- |
| `(試合, D4, D1)` の部分一意がある | ✅ `M01` の行(`WHERE D1 IS NOT NULL AND 置換されていない`) |
| `D2` に不変前提の索引を張らない旨が索引原則にある | ✅ 11-3 節の原則 **8** |
| `WAIT-03` の数値型と根拠が本文にある | ✅ `bigint` + 根拠 4 点 |

### ID の割り当てについて(**申し送り**)

**ステップ 4 の worklog に書いたとおり、欠陥 ID の定義はリポジトリに存在しない。**
本ステップの 7 ID は**計画書ステップ 6 行の記述**(「D2 の列・部分一意・索引の禁止、V10 の複合参照列、
V11 と確定版列を物理写像として置く」)を満たす単位へ**本タスクが割り当てた**:

| ID | 割り当てた内容 |
| --- | --- |
| `M01` | `D1` 列と `(試合, D4, D1)` の部分一意 |
| `M02` | `D2` 列(型・NULL 性・一意制約を置かないこと) |
| `M03` | `D2` に不変前提の索引を張らない(索引原則 8) |
| `M04` | `D4` 列 |
| `M07` | `V10` の複合参照列 |
| `M08` | `V11` と対象側の確定版列 |
| `M17` | 一時 ID → 正式 ID の写像(`C1`〜`C4` の物理表現) |

**`M17` の割り当ては本タスクの判断である**(他ステップが請求していない「識別子」の残余として置いた)。
**ステップ 12 の `closure-handoff-data-model.json` に本割り当てを載せ、TSK-250 のステップ 4
(欠陥台帳のベースライン固定)で突合してもらう。**

### 検査

| 検査 | 結果 |
| --- | --- |
| ④ | `M01` 2 / `M02` 1 / `M03` 2 / `M04` 2 / `M07` 2 / `M08` 2 / `M17` 2 / `WAIT-03` 2(是正前は全件 0) |
| ① 引用形式 | `()` → rc=0 |
| ② 文書検査 3 本 | rc=0 |

---

## ステップ 7: 一意範囲・連続性・適用水位・履歴文脈・楽観ロック(`C09`〜`C12`・`C14`)+ `WAIT-01`

### 手動条件の充足(**逐項の証跡**)

| 条件 | 充足 | 典拠(同期プロトコル設計) |
| --- | --- | --- |
| **`D5` の一意範囲が `(テナント, D5)`** | ✅ **`UNIQUE (tenant_id, D5)`**。**試合・世代を跨ぐ**(`(tenant_id, 試合, D5)` にしない)。**DB 全体一意は置かない**(他テナントの同一 `D5` を重複として扱うと存在が漏れる)。**3-4 節の全数表にも反映** | 影響差分 **14** / 6-5 |
| **`D1` の連続性が新規割り当てに限られる** | ✅ 「**連続するのは未使用 `D1` の新規割り当てだけ**」と明記。**既存スロットの置換(`D6`・`D7`)と不可分な追記の失敗では番号を消費しない**。→ **DB 側で連番の欠落を制約として強制しない**(理由 2 点を本文に書いた) | 影響差分 **1** / **8** |
| **`WAIT-01` の物理表・列・後退禁止の表現が同期正本の (B) #23 と整合** | ✅ 下表 | 影響差分 **5** |

### `WAIT-01`(`D3` の物理表現)の逐行照合

**同期プロトコル設計 11-2 影響差分 5**(逐語): 意味規則 = 「**(試合, D4) ごと。単調非減少、墓標を越えて前進**」/
構造上の帰結 = 「**D3 の保持場所**((B) #23)。**後退しない制約**を機構で表せるか」。

**本書の写像**:

| 事項 | 本書の決定 | 同期正本の要求との対応 |
| --- | --- | --- |
| 粒度 | **`(試合, D4)` ごとに 1 値** | 「(試合, D4) ごと」に一致 |
| **保持場所** | **記録権世代の行に持つ**(6-1 節)。**操作イベント行にも端末側にも持たない** | 「**D3 の保持場所**」への回答。`(試合, D4)` の単位が世代行と一致するため |
| 初期値 | **新世代の生成時に `D3` = 0** | 影響差分 **1**「新世代は D3 = 0」 |
| 型 | **`bigint`**(`D1` と同じ値域を取り得る) | (B) #23 が求める物理型 |
| **後退禁止の機構** | **`CHECK` では表せない**(更新前後の比較が必要)。**条件付き `UPDATE` または `BEFORE UPDATE` トリガ**。**「後退させない責務を DB 側に置く」ことを規則として定め、機構の選択は実装へ**。いずれも標準 PostgreSQL の範囲(`7.1`) | 「**後退しない制約**を機構で表せるか」への回答 |
| 用途の限定 | **連続 prefix の整合確認と再送範囲の決定に限る**。**キュー遷移の判定に使わない**(判定は `A5`) | 影響差分 **7** |
| 混同の禁止 | **チェックポイント(再計算の粒度)を `D3` prefix 水位の意味で使わない** | 同 3 章の用語表 |

### `C12`(履歴文脈)で決めた NULL 性の規律

同期プロトコル設計 影響差分 **13** が「**V1・V4・V5・V7 を NULL にできない**」「**V2・V3 と種別条件付きの
V6・V8・V9・V10・V11 は種別ごとの制約または別表で必須性を表す**」と要求している。→ 本書:

- **`V1`・`V4`・`V5`・`V7` は `NOT NULL`**
- **`V2`(`D1`)・`V3`(`D4`)・`V6`(`D2`)・`V8`(状態差分)・`V9`・`V10`・`V11` は `NOT NULL` にできない**
  → **種別ごとの `CHECK` 制約で必須性を表す**(別表に分けない — 5-3 節で同居を選んだ)
- **`V12` をイベント行に保存しない** — **個人利用者 ID・入力者識別列を作らない**
  (8 節の入力者識別列を一切作らない規律と同じ。Won't に正面衝突する)

### `C14`(楽観ロック)

- **`V11` と対象側の確定版の一致で受理**し、**受理と同時に確定版を進める**(`W3-a`)
- **確定版の初期値は確定時点で 1**、受理のたびに 1 進める。**`D1`・`D2` とは別系列**で**論理再生順に使わない**
- **目的は「同じ対象への複数の変更を DB から再生したときに順序が一意に決まること」**(同 4-3 の `V11` 定義)
- **旧 FR-007 から継承した楽観ロック**がここに対応する。**幻の得点・アウトの禁止と差分プレビューは
  投影の再計算側**で本項の対象ではない、と切り分けた

### `C13` を書かないことの確認

**裁定 `R-6`**(TSK-335)により **`C13`(= `D4` の非再利用・世代)は TSK-250 の新ステップ 10 の所有**である。
**本書に `C13` は 0 件**。ただし **`D4` の列の物理写像自体はステップ 6 の `M04` で置いた**
(「ロールバック後も再利用しない」は影響差分 **1-a** の要求として言及するが、
**非再利用を保証する機構は書いていない** — そこが `C13` の範囲)。

### 検査

| 検査 | 結果 |
| --- | --- |
| ④ | `C09` 2 / `C10` 1 / `C11` 1 / `C12` 2 / `C14` 2 / `WAIT-01` 2(是正前は全件 0)。**`C13` は 0 件** |
| ① 引用形式 | `()` → rc=0 |
| ② 文書検査 3 本 | rc=0 |

---

## ステップ 8: 原子境界と結果保持(`M05`・`M09`・`M10`・`M15`・`M18`・`M20`)+ `WAIT-06`・`WAIT-08`

### 「17 行」の出所を機械的に確認した

計画は「**9-2 境界表 17 行の永続化写像**」とだけ書いていた。**実測**:

**同期プロトコル設計 9-2 の境界表は 22 行**あり、**「永続化の正本」列が `TSK-250` を含む行がちょうど 17 行**
だった(残り 5 行は `P3` の完全な応答集合 / 引き継ぎ照合の確定水位の値 / `B4` の応答 /
複数タブの単一書き手 / `RG1` で、いずれも「—」または規則本文)。
→ **計画の 17 という数はこの部分集合を指す**。**5-4 節に 17 行の写像先対応表を置いた**(`WAIT-08`)。

**内 2 行は「永続化の正本」が条件つき**である(`D4` の非再利用 = 「物理形式は実装計画、**永続化との接続は
TSK-250**」/ `V12` = 「物理形式・寿命・格納先は実装計画、**永続化との接続は TSK-250**」)。
→ **本書は「接続」だけを書き、物理形式は書いていない**。**`D4` の非再利用を保証する機構は `C13` = TSK-250**。

### 5-4 節「原子境界と結果保持」を新設(既存の 5-4 は 5-5 へ)

**ステップ 5 で「8 節の原子性の規律に従う」と書いた参照先が存在しなかった**ので、
**5-4 節として実体を作り、参照を `5-4 節` へ直した**。

### 手動条件の充足(**逐項の証跡**)

| 条件 | 充足 |
| --- | --- |
| **`P1`〜`P5` と `T1`〜`T9` の対応表がある** | ✅ 2 表を置いた — ① `P` 経路 → 原子的に確定する `T` 要素(`P1` = `T1・T2・T3・T4・T6` / `P2` = +`T5` / `P3` = `T1・T2・T4・T6・T7` / `P4` = `T8` / `P5` = `T9`)② `T` 要素 → **本書での書き込み先** |
| **拒否原本が受理済み行と混ざらない旨がある** | ✅ `M10`。**拒否原本は操作イベント表に入れず別表**。理由 2 点(① 操作イベント表は正史であり投影再生成の入力。混ざると全再生クエリに `WHERE 拒否されていない` が必要になり 1 箇所落とすと幻の記録が出る ② `D3` を進めない規則が「適用済みの範囲」の意味を壊す) |
| **9-2 境界表の 17 行すべてに物理写像がある** | ✅ 17 行の対応表(上記)。**exact-set** — 対象外 5 行の理由も明記 |
| **クラッシュ注入点が定義できる配置になっている** | ✅ `WAIT-06`(`U-8` 構造側)として **5 項目**を置いた(下記) |

### `WAIT-06`(クラッシュ注入点)で構造側が満たすこと 5 項目

1. **1 つの `P` 経路の `T` 要素が触る表を同一トランザクションで書ける配置**(別スキーマ・別 DB・別サービスに分けない)
2. **`T` 要素の境界は「表の境界」と一致しない**ことを明記(`P2` は 4 表を 1 トランザクションで書く)。
   **表ごとにトランザクションを分ける実装を許さない**
3. **`T` 要素の順序に依存する制約を置かない** — 即時 `UNIQUE` が `T` 要素の途中で発火すると境界を
   分割せざるを得なくなる。**`P` 経路内で相互参照する FK は `DEFERRABLE INITIALLY DEFERRED` を選べる形**にする
4. **どの `T` 要素まで確定したかを外から観測できる**ように、**`T` 要素と書き込み先を 1 対 1 で対応づけた**
5. **`T8` は「応答を返す前に退避が確定している」順序**を守れる配置(退避原本表への書き込みと `B4` の
   応答生成を同一トランザクション)。同 9-2 の注記が根拠

### `M09`(`T4` の所在)— サーバー側にキュー状態を作らない

同 影響差分 **7** は「**端末側に**未送信・要操作・同期済み・退避済みを表す状態が要る」と定める。
→ **キュー状態は端末側の構造で DB スキーマ外**。**サーバー側の `T4` は投影の更新と適用水位の前進**であり、
**キュー状態を鏡写しにする列をサーバー側に作らない**(2 つの正本ができる — `4.0-2`)。

### `M18`(保存済み結果の再掲)

- **既存 `D5` の同一内容には保存済み結果を再掲し再適用しない**(`DI2`・`I2`)→ **`D5` 台帳に確定結果を不変で持つ**
- **上書きしない**(先着の原本と確定結果を不変にする — 影響差分 14)
- **再掲では `T7` の無効化意図を重複させない**(`I5`)→ **安定した意図 ID で 1 件だけ永続化**
- **`P4` の退避結果も同じ規律**(保存済み退避結果は現在の記録権によらず再掲)

### 検査

| 検査 | 結果 |
| --- | --- |
| ④ | `M05` 2 / `M09` 2 / `M10` 3 / `M15` 7 / `M18` 4 / `M20` 2 / `WAIT-06` 2 / `WAIT-08` 3(是正前は全件 0) |
| ① 引用形式 | `()` → rc=0 |
| ② 文書検査 3 本 | rc=0 |

---

## ステップ 9: 退避と復元の構造側 + **旧ステップ 16 の ID 単位の内訳の確定**

### 内訳の確定(**T250P の宿題 1 への申し送り**)

**割り当ての基準**(裁定 `R-6` を機械的に適用できる形にしたもの):

> **主題が「復旧世代」「`RG1`」「`I6`」のいずれかなら TSK-250、それ以外は本タスク。**

**旧ステップ 16 が持つ 9 ID の内訳**(**二重所有 0 件・未割当 0 件**):

| ID | 主題(同期プロトコル正本の該当箇所) | 所有 | 判定理由 |
| --- | --- | --- | --- |
| **`C07`** | **退避原本の表**(9-4 `E1`・`E2` の永続化) | **本タスク** | 復旧世代・`RG1`・`I6` に触れない |
| **`C15`** | **端末キュー状態 4 種の破棄契機の器**(9-5 の表) | **本タスク** | 同前(`I6` 行は除く — 下記 `M16`) |
| **`C16`** | **管理関数クラスから「取り込み」を分離**(8-2-B) | **本タスク** | 計画書ステップ 9 行が明示 |
| **`M11`** | **退避原本の列・キー・FK・由来 2 系統** | **本タスク** | 同前 |
| **`M12`** | **閲覧・書き出しの経路**(管理コンソール・原本のまま・ページング) | **本タスク** | 同前 |
| **`M13`** | **保持期間の器**(`WAIT-04` の構造側) | **本タスク** | 同前 |
| **`M14`** | **`RG1` 中の「`D5` を消費せず何も保存しない」の永続化表現**(9-5 の変更経路群 12 行) | **TSK-250** | **主題が `RG1`** |
| **`M16`** | **`I6`(`P3` 受理結果の端末保持 5 要素)の回収・退避** | **TSK-250** | **主題が `I6`** |
| **`M19`** | **現復旧世代の照合の永続化接続**(`VF6`・`I1`) | **TSK-250** | **主題が復旧世代** |

**本タスク 6 件 / TSK-250 3 件。** 本文での在/不在:
**`C07` 2 / `C15` 2 / `C16` 2 / `M11` 3 / `M12` 2 / `M13` 2** — すべて 1 以上。
**`M14` 0 / `M16` 0 / `M19` 0** — すべて不在。

**T250P の宿題 1 へ申し送る内容**: 上表をそのまま。**ステップ 12 の
`closure-handoff-data-model.json` に機械可読で載せる**(`owner` フィールド)。

### 本文へ入れたもの(6-4 節の直後に新設)

| 項目 | 内容 | ID |
| --- | --- | --- |
| 退避原本の表 | **操作イベント表と分ける**(退避行は正史に適用されないので投影再生成の入力に混ぜない — 5-4 節 `M10` と同じ理由)/ `UNIQUE (tenant_id, 試合, 旧世代, 元の連番)` / **旧世代への FK は張らない**(全世代フェンスで世代行が失効しても退避原本は残す) | `C07` |
| 由来の 2 系統 | **① 記録権不一致による退避**(`T8`・`P4`)/ **② 復元時の端末回収**(9-5)。**同じ表に置き由来を列で区別**。回収元の端末・回収日時を持つ | `M11` |
| 閲覧・書き出し | 管理コンソールのクラス **(b)**。**原本のまま出せる形**・**ページングする** | `M12`・`WAIT-02` |
| 端末キュー状態 4 種 | **サーバー側の対応物だけを持つ**表(未送信 = 無し / 要操作 = 拒否原本表と `D5` 台帳 / 同期済み = `D5` 台帳の確定結果・**24 時間の期限はサーバーに持たない** / 退避済み = 退避原本表)。**鏡像列を作らない** | `C15` |
| 保持期間の器 | **保持期限列(NULL = 無期限・既定 NULL)** / **破棄も物理削除しない** / **`(tenant_id, 保持期限)` で絞れる形**(全件読み込みで期限判定しない)/ **`RG1` 中は破棄ジョブを止められる形** | `M13`・`WAIT-04` |

### `WAIT-04` は構造側のみ — 理由を本文に明記した

**要件書に行が無い。** 同期プロトコル設計 9-4 は「退避イベント(サーバー)の保持期間は**要件書に定めがない**」
「本書は『**破棄しない**』を既定とする」とし、**保持期間そのものを 11-4 の `U-3`** で要件改訂タスクへ送っている。
→ **本書も値を決めない。決めるのは器だけ**と本文に書いた。
**理由**: 要件書 6.1 のデータ保持方針は種別ごとに期間を定めており、**そこに行を足すかは要件の判断**である。
**設計が単独で期間を決めると要件の創出になる。**
**受け取り先はステップ 12 が起票する要件改訂タスク**(3 周目 `P1-5` で実タスク化した送り先)。

### `C16`(取り込みの分離)と機械条件の適用範囲

**是正内容**: 8-2-B 節のクラス **(c) 破壊的操作**の行にあった「退避イベントの取り込み(挿入位置指定)を含む」を
**削除**し、**表の直後の箇条に分離の理由を書いた**。

**要件の実際の形**(ステップ 1 で見つけた `FR-035` の優先度分割と同じもの):
**「閲覧・書き出し = `Must`」「挿入位置を指定して受け入れる = `Should`」**。
**優先度が違うものを 1 クラスに入れると、`Should` を落としたときに `Must` のクラスごと落ちる。**
→ **管理関数クラスは `Must` の経路だけを定める**。`Should` 側の関数契約は**採用時に加算的に足す**
(**構造は 6-4 節が既に持っている**)。

**機械条件の適用範囲を確定した**(ステップ 3 の「同一文」と同じく、計画の文言に判定範囲が無いため):

> **「管理関数の記述に『取り込み』が残っていない」= 管理関数クラス表の行に「取り込み」が 0 件**。

**表の外の散文(分離の理由・6-4 節への案内)は対象外**とする。**理由**: 分離したこと自体を説明する文に
語が現れるのは避けられず、それを禁じると**是正の記録が書けなくなる**(ステップ 3 の 12-7 (1) と同型)。
**実測: クラス表の行 8 行(3-4 節の手段 (a)〜(d) と 8-2-B のクラス (a)〜(d))に「取り込み」は 0 件。**

### 検査

| 検査 | 結果 |
| --- | --- |
| ④ | `WAIT-02` 4 / `WAIT-04` 4 / `C07` 2 / `C15` 2 / `C16` 2 / `M11` 3 / `M12` 2 / `M13` 2(是正前は全件 0)。**`M14`・`M16`・`M19` は 0 件**(TSK-250 の所有) |
| 管理関数クラス表の「取り込み」 | **0 件** |
| ① 引用形式 | `()` → rc=0 |
| ② 文書検査 3 本 | rc=0 |

---

## ステップ 10: キャッシュ無効化の物理化・要件由来の写像・`NFR-018` の境界

### 着手時に見つけた問題と、その処置(**人間の裁定 6 件を取得**)

**計画のステップ 10 の手動条件は「`RQ-*` 各件に裁定 ID・決定者・決定日・反映節がある」**だが:

1. **`RQ-01`〜`RQ-09` がどの 9 件を指すかがリポジトリから決まらない**(欠陥 ID の定義は無い)
2. **候補の 6 件は正本自身が「確定ゲートで確認を仰ぐ」と書いており、裁定が存在しなかった**
3. **T250P の `FB-06` は「要件由来の写像**と、そのための人間の裁定**」を本タスクの担当**とし、
   除去された旧ステップ 20 も「**計画承認前に得た裁定**を反映し」と書いていた
   → **裁定を取る工程が本タスクの計画から落ちていた**(`A-1`〜`A-4` にこの 6 件は含まれない)

**裁定の無い件に決定者・決定日を書くのは捏造になる**ので中断し、**人間へ 6 件の裁定を求めた**。
**6 件すべて取得(2026-09-08・山田正輝)** — いずれも正本の導出案どおり:

| 裁定 ID | 事項 | 裁定 |
| --- | --- | --- |
| `A-5` | 再参加時の付与の扱い | **既定(すべて非共有)から始める** |
| `A-6` | カルテ所見の世代上限超過分 | **残して保持対象外**(復元候補から外す) |
| `A-7` | 移行の取り込みバッチの識別子 | **持つ** |
| `A-8` | 非表示の復元可否 | **復元できる** |
| `A-9` | 移行由来で未検証のプレイ行の区別 | **表せる列を持つ** |
| `A-10` | 個人アカウント化の「余地」 | **認証情報を別表にする** |

**`RQ-01`〜`RQ-09` の同定**: 上の 6 件 + **裁定 `A-1`(`RQ-07`)・`A-3`(`RQ-08`)・`A-4`(`RQ-09`)** = **9 件**。
**`A-2`(越境テスト再実行ゲートの所有者)は入れない** — **写像ではなく工程のゲート**で、射程宣言が受け取る。
**計画書 1 節の裁定表へ `A-5`〜`A-10` を追記**し、**取得が計画から落ちていた事実も併記**した。

### 無効化の物理化(`B01`〜`B05`・`WAIT-07`)

**発火条件の正は同期プロトコル設計 8-5**(`D1` 付き経路と `I5` の 2 行)。**本書は参照だけを置き、
条件を規則として写さない**(`B01`)。同 8-5 が「同期側の責務はここまで」としてマトリクスの正本化を
本書へ送っている(`WAIT-07`)。

**本書が決めたもの**:

| ID | 内容 |
| --- | --- |
| `B02` | **物理的な無効化先**(5 区分 × 鍵の構成)。**鍵は必ず `tenant_id`(またはグループ)を先頭に含める** — 他テナントの鍵を消せる経路を作らない。**④ 共有集計の鍵には要求元テナントも含める**(付与は (グループ × 自テナント)単位なので、対象テナントだけを鍵にすると付与を撤回した要求元へ古い結果が返る) |
| `B03` | **無効化意図表**。**`UNIQUE (tenant_id, 意図 ID)`** で、**意図 ID は「対象イベントの `V10` + 対象の確定版」から決定的に導く** → **保存済み結果の再掲で同じ意図 ID になり、重複作成が構造的に起きない** |
| `B04` | **配信状態(未配信 / 配信完了)**。**`(tenant_id, 配信状態)` で未配信だけを引ける形**(全件走査しない) |
| `B05` | **事前集計を持たない場合の契約**。無効化先はアプリ層・HTTP キャッシュの鍵で、**上表の単位がそのまま鍵の構成**になる。**「キャッシュが無いから対応表が不要」にはならない** |

**14 トリガーの全数対応(手動条件)**: **表の行数 = 14**(機械確認)。1 復帰同期 / 2 プレイ修正 / 3 undo /
4 交代の記録・修正 / 5 試合の削除・復元・再開 / 6 終了後修正 / 7 選手統合・分割 / 8 設定値変更 /
9 付与フラグの変更 / 10 グループからの離脱 / 11 グループの終了 / 12 テナントの無効化 /
13 テナントの再有効化 / 14 選手の在籍区分の変更。**「招待の失効は含めない」の明記も 1 件ある**。

**「発火条件の再記述が 0 件」の適用範囲**(計画に判定範囲が無いため確定した):

> **`D3` の前進 / `D3` が前進 / 未使用 `D5` の変更受理 / 無効化意図を永続化 のいずれかを含む行が 0 件。**

**実測 0 件。** 当初の草稿には「(**`D3` の前進**を条件とする行)」という**参照のための言い換え**が
入っていたが、**条件そのものを書いている**ので**節参照だけに改めた**。

### `SP-01`〜`SP-06`(同期プロトコル設計が判定を送った禁止事項)

**同定できた**: 同 8-6 の受け取り先表に「**DB スキーマ・物理構成の領域(TSK-250)= `P-01`・`P-03`・`P-04`・
`P-10`・`P-11`・`P-22`**」とあり、**件数が 6 で計画の `SP-01`〜`SP-06` と一致**する。

| ID | 禁止事項 | 判定 | 対応する構造不変条件 |
| --- | --- | --- | --- |
| `SP-01` | `P-01` 88 列を物理スキーマ / 唯一のデータ契約に | **遵守** | 88 列は入出力の互換フォーマット。行き先表 + サイドカー |
| `SP-02` | `P-03` SQLite 併用・開発と本番で別 DB | **遵守** | PostgreSQL 単一・dialect は `postgresql+psycopg` |
| `SP-03` | `P-04` Supabase 固有機能に依存 | **遵守** | RLS・`BYPASSRLS`・`FORCE RLS`・`SECURITY DEFINER`・`set_config` はいずれも標準 |
| `SP-04` | `P-10` プレイ 0 件エンティティの物理削除 | **遵守** | 削除はすべて論理削除。`RQ-04` で復元可能 |
| `SP-05` | `P-11` TTL・アーカイブ削除 | **遵守** | 退避の保持期限は既定 NULL(無期限)・破棄も物理削除しない(`M13`) |
| `SP-06` | `P-22` `team_id` 分離の後付け前提設計 | **遵守** | `tenant_id` を全業務表に持ち一意制約の先頭に含める。RLS を初日から全テーブルに |

**6 件すべて「遵守」で、判定を保留したものは無い。**

### `NFR-018` の境界(裁定 `A-4` = `RQ-09`)

**判定: (b)③ は ORM の実行時 SQL 生成に及ばない。** 理由 3 点を本文に書いた
(① (b) は宣言と機械判定の条項 ② 達成条件 (d) が本項の列挙を正とし `models`・`migration` は該当しない
③ ORM が生成するのは永続化のクエリで、両言語間で一致させる対象ではない)。
**要件書に適用範囲の明示は無いことを「不明」として記録し、確定ゲートの敵対レビューに掛ける**と明記。

**(a) の発火点を 1 つ記録した**: **(β) の集計クエリを ORM で人手記述する段で
(a)「人手で保守する実装は 1 系統」が発火する**。ADR-003 D-1 が「(β) は宣言モデルから SQL 式を生成して
DB 側で実行する」と定めているため、同じ集計を ORM で人手記述すると 2 系統になる。
**本書と TSK-343 の射程外**であり、**発火点の所在だけを記録**した。

### `FR-030` と管理者操作ログを独立した行として閉じた

| 実体 | 扱い |
| --- | --- |
| `FR-030` の勝敗投手・セーブの手動指定 | **実体を置かない**。`Could` で「任意・後から変更可」なので**採用時に加算的に足せる形**にする(**P-58** と同じ規律) |
| `6.1/管理者操作ログ（Should）` | **6.1 に行がある**。日時・操作種別・対象 / テナントに属さない / 無期限 / **個人利用者 ID を持たない**(**P-20**)。同 11-4 の `U-12` が「監査規則が無条件に要求する記録先が `Should`」のずれを要件改訂へ送っているので、**本書は `Should` の器として置く** |

### 検査 ④ が退行を 1 件捕まえた

**ステップ 8 で書いた「非再利用を保証する機構は `C13` = TSK-250」が `C13` の言及**になっていた。
**検査 ④ が「存在する禁止 ID: C13」で rc=1 を返し、ステップ 10 の実行中に検出**した
(ステップ 7 では 0 件を確認済みだったので、**ステップ 8 で入った退行**)。
→ **「非再利用を保証する機構は TSK-250 が持つ(裁定 `R-6`)」へ書き換え**た。
**rc 化しておかないと表示を見落として通っていた**(3 周目 `P1-2` の反映が効いた実例)。

### 検査

| 検査 | 結果 |
| --- | --- |
| **④ ID の在/不在** | **rc=0** — **必須 54 件すべて在・禁止 1 件(`C13`)不在**。**ステップ 4〜10 で全 ID を覆い終えた** |
| 14 トリガーの全数 | **表の行数 14**(機械確認)+「招待の失効は含めない」の明記 1 件 |
| `RQ-*` の 4 項目 | **9 行すべてに裁定 ID・決定者・決定日・反映節がある**(機械確認) |
| 発火条件の再記述 | **0 件** |
| ① 引用形式 | `()` → rc=0 |
| ② 文書検査 3 本 | rc=0 |

---

## ステップ 11: `core-areas.json` へ新設資産を登録(**Codex 委任**)

### 委任と差し戻し

**1 回目の委任**では「`game-state` には追加しない」と指示した。**理由は「状況計算(要件書 4.0-4・付録E)は
本書が決めないと本書自身が明記している」**。→ **敵対レビュー(6.3 規則⑤)で P1 が 1 件成立し、差し戻した。**

**P1 の内容(成立)**: **`game-state` にも登録すべき**。根拠は設計書 6.3 の `paths` への落とし込み規則:

- **①** コアの不変条件・強制点・契約を**変え得る**ファイルを含める
- **②** 混在ファイルは**ファイル全体を含める** / **③** **領域間の `paths` 重複を許す**
- 「**判定に迷うコードは含む側に倒す**(fail-closed)」

**`game-state` の境界定義には「状態補正(FR-040)」と「下流再計算 / リプレイ」が含まれる**。
**本書はそれらの契約を変え得る** — **再計算の起点を `D2` に固定**(5-2・6-3 節)/
**下流再計算は投影を作り直し交代時点で選手を帰属**(4-2 節)/ **`FR-040` の補正位置と直接上書きの禁止**(6-3 節)。
→ **「算術そのものを決めない」は除外の理由にならない**(規則①は「変え得る」で判定する)。

**是正後: 5 領域すべてに登録**(`sync-protocol` / `game-state` / `recording-rights` /
`tenant-isolation` / `data-migration`)。

### 登録内容

| 対象 | 登録先 | 根拠 |
| --- | --- | --- |
| `docs/design/data-model.md` | **5 領域すべての `paths`** | 上記 |
| `scripts/design_relations/citation-map-data-model.json` | **`guard_paths`** | 既存の `defects.json`・`req-universe.json` と同種(文書検査の宣言資産) |
| `scripts/design_relations/closure-handoff-data-model.json` | **`guard_paths`** | 同前 |
| `tests/fixtures/data-model-source.txt` | **`guard_paths`** | 既存の `tests/fixtures/sync-protocol-source.txt` と同種(退行 fixture) |
| `scripts/design_relations/fixture-sha256-data-model.txt` | **`guard_paths`** | 既存の `fixture-sha256.txt` と同種 |

**`guard_paths` は 26 → 30 件。** **完全一致集合なので個別のパス文字列で書いた**
(ディレクトリ・グロブでは配下の変更で発火しない — `scripts/core_guard.py`)。

**未存在パスの先行登録**: `closure-handoff-data-model.json`(ステップ 12)と fixture・SHA(ステップ 13)は
**この時点で存在しない**が先に登録した。**敵対レビューで「機構を壊さない」ことを確認** —
`core_guard.py` は設定文字列を集合化して差分パスと比較するだけで、**登録先ファイルを読まない**。

### 負例の実効を独立に確認した(**9 通り**)

| 変異 | 述語 |
| --- | --- |
| 5 領域のいずれか 1 つから `data-model.md` を外す(5 通り) | **すべて False** |
| `guard_paths` の 4 件のいずれか 1 つを外す(4 通り) | **すべて False** |

**敵対レビュー側でも「領域 ID の改名」「`paths` の型崩れ」でも False になることを確認**したと報告があった。
**既存の exact-set テストは領域 ID の重複や余分な登録も検出する。**

### `/pr` の判定への影響(**重要**)

**ステップ 11 の完了後は本書が 5 領域の `paths` に入る**が、
**`/pr` はコア領域判定を base 側の `core-areas.json`**(`git show origin/develop:.claude/core-areas.json`)
**で行う**。→ **本 PR の時点では `guard_paths` 該当のみ**で、計画書 4 節の判定は変わらない
(PR テンプレは**逐行確認チェックのみ**を有効化する)。
**次の PR 以降は本書への変更がコア領域として発火する。** **計画書 4 節へこの旨を追記した。**

### 検査

| 検査 | 結果 |
| --- | --- |
| ③ core-guard | `tests/test_core_guard.py` **51 passed** |
| ruff / ty | green |
| 変更ファイル | `.claude/core-areas.json` と `tests/test_core_guard.py` の 2 件のみ |
| 敵対レビュー(6.3 規則⑤) | 1 周目 P1×1 → **反映済み**。**残る手動条件は PR での人間の逐行確認**(PR 作成者以外) |

---

## ステップ 12: 射程宣言・ゲートの定義・受け渡し契約

### 起票した送り先 2 件(**重複起票の確認つき**)

**先に Notion を検索して重複が無いことを確認した**:

| 検索語 | 既存で近いもの | 判定 |
| --- | --- | --- |
| 「越境テスト 再実行 実スキーマ」 | **TSK-317**(認可 DDL の実装・実機検証・**越境テスト 4 件**の作成)/ TSK-270(DB テスト基盤) | **別物**。あちらは**テストを作る側**で、**実スキーマ適用後の再実行ゲート**は無かった |
| 「退避 保持期間 要件改訂 U-3」 | TSK-322 の兄弟(`U-11`・`U-12`・`U-13`/`U-14` の要件改訂) | **`U-3` 専用のタスクは無かった**(同期正本化カードの本文に言及があるだけ) |

**起票した 2 件**:

| タスク ID | タスク名 | URL |
| --- | --- | --- |
| **TSK-344** | 越境テスト再実行ゲートの実行 — 実スキーマ適用後の再実行(TSK-342 の裁定 A-2) | https://app.notion.com/p/3d593b75e687811f8ad5f5da4a8af046 |
| **TSK-345** | 要件書改訂 — 退避イベントの保持期間(同期プロトコル正本 11-4 の U-3) | https://app.notion.com/p/3d593b75e687812bb76dc39b661aa287 |

**URL は worklog にだけ書いた** — **正本には裸の `TSK-344` / `TSK-345` だけを書く**(設計書 7.4
「正本は Notion を参照しない」。3 周目 `P1-4` の実測どおり、承認済み正本は**裸のタスク ID を目印として使う**)。
計画の合格条件「本書と worklog に起票した 2 件の実 ID(**URL 付き**)がある」は、
**正本 = 実 ID / worklog = 実 ID + URL** と解した。

### 12-4 節へ non-serving 宣言と越境テスト再実行ゲート

**non-serving 宣言 3 項目**: ① 初回 `migration` は利用経路を開かない ② **RLS のポリシー / ロール DDL の
適用と実スキーマに対する越境テストが green になるまで、DB を利用する製品機能をマージまたは有効化しない**
③ 暫定的にアプリ層分離で先行する場合も同じ越境テストの green を条件とする。

**なぜ宣言が必要か**を本文に書いた: `NFR-010`「初日から全機能に適用」は**製品の要件**であり、
**RLS 未適用のスキーマが `develop` に入ることを禁じる条項は存在しない**。
一方で**スキーマが入った直後から DB を使う実装が始められる**ため、**止める条件を書かないと穴になる**。

**ゲートの定義 6 項目**(対象・通過条件・越境テストの最低要求 4 件・測定経路・本書の責務・判定の記録)。
**Notion の URL は書いていない。**

### 12-8 節 射程宣言(設計書 7.3-7)

**7.3-7 の配置規則を確認した** — 「**新設の正本 = 文書内の節として置く**」
「**plan.md のスコープ節はタスクの契約であり本宣言の代替にならない**」。→ **節として置いた**。

**`## 13.` にしていない** — **T250P の `DM-MT-01` が `absent-section: "13"` を検査する**ため、
**`### 12-8.`** にした(ステップ 1 の worklog で持ち越した制約)。**実測 `## 13` は 0 件**。

- **本書で確定する範囲 = 14 項目**
- **実装時に確定する範囲 = 15 行**(すべてに受け取り先がある)
- **受け取り先の内訳**: TSK-317 / TSK-344 / TSK-343 / TSK-250 / TSK-345 / 移行仕様タスク /
  実装期の実測 / `Could`・`Should` 採用時 / 要件改訂の判断
- **計画の手動条件「射程宣言が TSK-343・TSK-250・TSK-317 への送り先を列挙している」を満たす**

**`C13` の扱いで検査 ④ が 2 度目の退行を捕まえた**: 射程宣言の「実装時に確定する範囲」に
**`C13`・`M14`・`M16`・`M19` を ID で書いた**ところ rc=1 になった。
→ **主題の記述に改め、ID 単位の所有は `closure-handoff-data-model.json` が機械可読で持つ**形にした。
**理由を本文にも書いた** — **所有していない是正の ID が本文にあると「是正済み」と読める**。

### `closure-handoff-data-model.json`(TSK-250 への受け渡し契約)

| 節 | 件数 | 内容 |
| --- | --- | --- |
| `defects` | **52** | 本タスク所有の欠陥 ID。各件に `resolved: true` と `closure_evidence`(**正本の反映節 + worklog のステップ + コミットのステップ番号**) |
| `waiting` | **8** | `WAIT-01`〜`WAIT-08`。同上 |
| `not_owned` | **4** | `C13`・`M14`・`M16`・`M19`(`resolved: false`・`absent_from_canon: true`・理由つき) |
| `rulings` | **10** | `A-1`〜`A-10`(裁定 ID・主題・裁定・決定者・決定日・反映節) |
| `old_step_16_split` | — | **T250P の宿題 1 への回答**(基準と 6 / 3 の内訳) |
| `handoff_tasks` | 5 | TSK-343 / TSK-344 / TSK-345 / TSK-317 / TSK-250 |

**機械確認した**: 検査 ④ の必須 54 件を**全件収載**/ ステップ 9 で本タスク所有になった 6 件も収載
(**この 6 件は検査 ④ の must 集合に入っていない** — must はステップ 3 時点で固定したため。
**closure-handoff が唯一の網羅記録**)/ **全件に `resolved` と `closure_evidence` がある** /
**反映節として書いた節番号がすべて正本に実在する**(節見出しの集合と照合)。

**`_note` に明記した**: 欠陥 ID の定義はリポジトリに存在せず(T250P にあるのは群の件数と所有だけ)、
TSK-250 のステップ 4 がベースライン 78 件を作る。**したがって `subject` は「TSK-342 が何を是正したか」の
記述であり、TSK-250 は自身のベースラインと突合したうえで取り込む。**

### 検査

| 検査 | 結果 |
| --- | --- |
| 節見出し 2 つ | **「本書で確定する範囲」1 / 「実装時に確定する範囲」1** |
| `## 13.` | **0 件** |
| `app.notion.com`(正本) | **0 件** |
| ゲートの定義・non-serving・マージ停止条件 | **本文にある**(`non-serving` 3 / `越境テスト再実行ゲート` 5) |
| `closure-handoff` | **60 件 + 4 件 + 裁定 10 件**。全件に `resolved` と `closure_evidence` |
| ④ ID の在/不在 | **rc=0** |
| ①② | rc=0 / ③ `test_core_guard` 51 passed |

---

## 確定ゲート(設計書 7.3)— 適用版の記録と開始

### 適用版(7.3-1)

| 項目 | 値 |
| --- | --- |
| **7.3 の版数** | **開発ハーネス設計書 `v1.13`**(`approved` — 索引で確認) |
| **条文コミット SHA** | **`1bf690c58c8de30a2bbe9d7b1dadc3b24d1683f0`**(2026-09-04 — `docs/development/dev-harness-design-2026-08-07.md` を最後に変更したコミット) |
| **記録時点** | 2026-09-08(in-review 化と同日。**初回敵対レビューの実行時点で異なっていれば更新する**) |

**当該ゲートはこの適用版で最後まで運用し、途中で切り替えない。**

### 開始条件(計画書 4 節の検査)

| 検査 | 結果 |
| --- | --- |
| ① 引用形式 | 正本 `()` / 候補案 3 形式 → **rc=0** |
| ② 文書検査 3 本 | **rc=0** |
| ③ core-guard | **51 passed** |
| ④ ID の在/不在 | **rc=0**(必須 54 件すべて在・`C13` 不在) |
| 射程宣言(7.3-7) | **12-8 節として存在**(`## 13.` は 0 件) |
| `check_plan_docs_sync` | **rc=0** |

### 状態遷移(手順 1)

- 計画書 `status`: `active` → **`in-review`**
- `docs/design/data-model.md` frontmatter: `draft` → **`in-review`**
- `docs/README.md` の索引行: `draft` → **`in-review`**

**ステップ 13(退行 fixture と SHA-256 の固定)はゲート通過後に実行する**(計画書 5 周目 `P1-1`)。

---

## 確定ゲート 1 周目(**全文レビュー** — 7.3-4 の初周)

**判定: 否決(P0: 4 / P1: 5 / P2: 3)** — 起因フラグは全件 `非起因`(1 周目)。

### 採否記録(7.3-2)

| # | 要旨 | 重大度 | 起因 | 分類 | 採否 | 反映内容 / 理由 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **`D2` の一意範囲と重複防止機構が同期正本に反する** — 一意制約を置かないと「同位置を作らない」を強制できず、世代を跨ぐ重複も検出できない。undo #2・改訂版 #9 の `D2` 写像も無い | **P0** | 非起因 | (A) | **採用** | **`UNIQUE (tenant_id, 試合, D2) WHERE D2 IS NOT NULL AND 置換されていない`** を置いた(同 5-2 の「**同時に有効で、かつ自身の `D2` を持つイベントの間で一意**」をそのまま述語にした。**`D4` を含めない** — 正史は世代の連結)/ **参加区分 #1〜#12 × `D2` の有無**の全数表を新設(#2 undo は従属で持たない・#9 改訂版は元の `D2` を継承し部分一意の述語で衝突しない・#7 は `Should` なので `NOT NULL` にしない)/ **`O4` の fail-closed(投影と再計算の停止・顕在化・自動再採番で隠さない)**を構造側の要求として明記 / **`O1` の直列化単位を `(tenant_id, 試合)`** へ(`D4` を含めない — 世代単位だと別世代と同位置を作れる)/ 4-3 節の一意性の範囲も「(試合)内で全順序・世代を跨ぐ」へ |
| 2 | **undo の対象導出が世代を無視している** — 「連番降順」では引き継ぎ後に旧世代の大きな `D1` が先に選ばれる | **P0** | 非起因 | (A) | **採用** | 履歴文脈の導出を **「世代の連結順 + 世代内 `D1`」の降順**へ改め、**`D1` の降順だけで並べてはならない**理由(`D1` は世代ごとに 1 から始まる)を明記。**記録権世代表に発行順を持つ**ことを要求に加えた |
| 3 | **`D5` の一意性と保存済み結果が経路ごとの別表に分断されている** — `T8` の書き込み先に `D5` と退避結果が無く、別表の一意制約では全経路横断の重複を排除できない | **P0** | 非起因 | (A) | **採用** | **`#### `D5` 台帳は全経路唯一の一意性の所在`** を新設。**受理・拒否・退避のどの経路も台帳へ書く**/ **他の表は台帳への FK を持ち自前で `D5` を一意にしない**/ **確定結果(受理 / 拒否と理由 / 退避)を台帳の同じ列集合で持つ**/ **不変性と照合位置**。`T8`・`T9` の書き込み先に `D5` 台帳を追加し、拒否原本表の記述も台帳参照へ改めた。**表を跨いだ一意性は PostgreSQL の制約で表せない**ことを理由に書いた |
| 4 | **共有集計キャッシュの物理キーから要求元テナントが欠落**(表と直後の説明が矛盾) | **P0** | 非起因 | (A) | **採用** | 鍵を **`(グループ, 要求元テナント, 対象テナント, 期間)`** に直した |
| 5 | **`V10` の複合 FK は提示された参照キーでは作成できない** — 参照先が部分一意索引で、4 列自体も改訂版・墓標があるので一意でない | **P1** | 非起因 | (A) | **採用** | **`#### イベントスロット`** を新設。**`(tenant_id, 試合, D4, D1)` を主キーとする表**を置き、**`V10` の FK をそこへ向ける**。**操作イベント行はスロットに属する版**で、**確定版はスロットの列**が持つ(改訂で版が変わっても対象参照が壊れない)。**変更イベントはスロットを持たない**(参照は NULL 可・`CHECK` で表す)。**却下案 3 件**(完全一意を置く / FK にしない / サロゲートを参照先にする)を記録 |
| 6 | **「業務的一意性の全数」表が本文の一意性を覆っていない** — 走者・出場区間・退避原本・スタメン記憶・カルテ所見・無効化意図が未掲載。除外基準も成立しない | **P1** | 非起因 | (A) | **採用** | **判定基準を明確化**(利用者・外部システム・移行元が与える値の一意性 + 業務上「高々 1 件」を強制する組。**サロゲートを含む組で業務的な制約を表しているものは含める**)。**9 行を追加**(イベントスロット・走者・出場区間・スタメン記憶・カルテ所見・退避原本・拒否原本・無効化意図)。**全数は 18 行** |
| 7 | **「4 ロール」の構成と権限が自己矛盾** — 実際は 5 ロール。アプリ用の権限モデルも確定していない | **P1** | 非起因 | (A) | **採用** | 節見出しを **「ロールを 5 つに分ける」**へ。**管理関数所有用**と**移行バッチ用**を表の行として明示。**アプリ用の権限**(業務表への `SELECT`/`INSERT`/`UPDATE`・`TRUNCATE` と `REFERENCES` は与えない・越境が必要な操作だけ関数の `EXECUTE`)を書いた。12-8 節の「4 ロール」も直した |
| 8 | **参加上限の競合防止が実効的に直列化されていない** — 同一トランザクションだけでは異なるテナントの同時受諾を防げない | **P1** | 非起因 | (A) | **採用** | **グループ行を計数の直列化点にする**(上限検査の前に排他ロックし挿入まで保持)**または直列化可能分離で再試行**、と規則を置いた。**「同一トランザクション」だけでは不足であることは本書が定める**と明記 |
| 9 | **管理者操作ログの必須範囲が文書内で一致していない** — 8-2-B は全呼び出しを `Must`、12-6 は `Should` の器 | **P1** | 非起因 | (A) | **採用** | 8-4 節の見出しを **「必須範囲は破壊的操作 = `Must` / 全操作の網羅は `Should`」**へ。**線引きの表**を置き(破壊的操作と制御資源の 7 操作 = `Must`・記録は同一トランザクション / 参照系 = `Should`・同じ器で必須にしない)、8-2-B 節の記述を 8-4 節へ寄せた。**優先度のずれ自体は `U-12` が要件改訂へ送っている**ことも明記 |
| 10 | `NFR-018 (b)③` の「適用範囲の明示なし・不明」という記録が要件書と一致しない | **P2** | 非起因 | (-) | **次周へ繰り越し** | **7.3-2 の終端遷移表に従い、P2 は P0/P1 ゼロの周で一括反映する**。結論(対象外)は妥当との評価なので、**裁定理由の記録だけを直す** |
| 11 | `FR-030` の申し送りの受け取り先が「`Could` 採用時」でタスク・文書・担当のいずれでもない | **P2** | 非起因 | **(B)** | **次周へ繰り越し**(**(B) の送り先を記録**) | 同上。**(B) = 本書の射程外**。**送り先は `Could` 採用を判断する製品判断であり、起票の要否を PO へ確認する** |
| 12 | 「識別子を 5 種」の見出しが表の 6 種・12-8 節の「6 種」と食い違う | **P2** | 非起因 | (-) | **次周へ繰り越し** | 同上(見出しの数だけの不整合) |

**採用 9 件(P0 4 / P1 5)・繰り越し 3 件(P2)・不採用 0 件。**

### 検査(反映後)

| 検査 | 結果 |
| --- | --- |
| ④ ID の在/不在 | **rc=0** |
| ① 引用形式 | `()` → rc=0 |
| ② 文書検査 3 本 | rc=0 |
| 発火条件の再記述 | **0 件**(維持) |
| ロール表 | **5 行** |
| 業務的一意性の全数表 | **18 行** |

**`確定ゲート周回` を 1 へ。** 本文は 2,498 → **2,618 行**。

---

## 確定ゲート 2 周目(**差分レビュー**)+ **PO スコープ裁定(7.3-6)**

**判定: 否決(P0: 0 / P1: 8 / P2: 0)** — **8 件すべて `起因`**(1 周目の修正が生んだもの)。
**繰り越し P2 3 件はレビュー側が「維持」を明示**(今周の件数には再算入しない)。
**参加区分 #1〜#12 × `D2` の有無は同期プロトコル設計 11-2 と全行一致**との確認を得た。

### PO スコープ裁定を発火条件を待たずに起動した(7.3-6)

**7.3-6 の発火条件は「前周修正起因の指摘が 2 周連続で過半」**だが、
**今周は起因率 8/8 = 100%** であり、**同条は「射程変更の必要が判明したら発火条件を待たず
即時に PO 裁定を起動してよい」**と定めている。→ **起動した。**

**指摘の性質が 2 種類に分かれていた**:

| 種類 | 件数 | 性質 |
| --- | --- | --- |
| **伝播漏れ** | **6 件** | 1 周目で入れた構造(イベントスロット・`D5` 台帳)が `T2`・`T7`・`M08`・`C14`・変更イベント表・`P`/`T` 対応表・全数表へ通っていない。**機械的・有限** |
| **DDL レベルの新規設計** | **2 件** | ① `D2` の局所再採番と即時部分一意の両立手順 ② `D5` 台帳の経路排他の物理契約。**「論理データモデルと制約」を越える** |

**PO 裁定(2026-09-08・山田正輝)= 「範囲確定」**。

> **確定した線引き**: **本書は不変条件を定め、それを満たす手順・機構は実装が選ぶ。**
> (**`D3` の後退禁止で既に採っている形と同じ** — 「後退させない責務を DB 側に置く」ことだけを定め、
> 条件付き `UPDATE` か `BEFORE UPDATE` トリガかは実装が選ぶ)

**射程が変わったので、変更範囲と理由を変更履歴と本 worklog に記録した。次周は全文レビュー**(7.3-4)。

### 採否記録(7.3-2)

| # | 要旨 | 重大度 | 起因 | 分類 | 採否 | 反映内容 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **アプリ用ロールの権限が同じ節で矛盾**(表は業務表への直接権限・「呼び出し許可」は関数の `EXECUTE` だけ) | P1 | **起因** | (-) | **採用** | 「**直接権限を与えない」の対象は越境関数が読む表だけ**であり、**自テナントの業務表への直接権限はアプリ用ロールが持つ**と明記(通常経路を関数越しにするわけではない) |
| 2 | **undo 用の世代順序キーが物理化されていない** — 6-1 節に発行順が無く、`D4` は非再利用識別子なので値順 = 発行順を仮定できない | P1 | **起因** | (A) | **採用** | 6-1 節へ **`発行順`(試合内で単調増加する整数・`UNIQUE (tenant_id, 試合, 発行順)`)** を追加。**`D4` の値順で代用しない**理由も明記。**適用水位 `D3`** の行も同時に明示した |
| 3 | **`D2` の部分一意と `O2` の範囲更新を両立させる更新方式が無い** — 部分索引は `DEFERRABLE` にできず、`100,200 → 200,300` は中間状態で衝突する | P1 | **起因** | (A) | **採用(射程確定)** | **`#### `D2` の局所再採番と一意性の両立`** を新設。**本書は 4 つの不変条件**(`T5` の内側で完結・**各文の完了時点で一意**・値に意味を持たせない・局所)**を定め、手順は実装へ送る**。**満たし方の例 3 つ**(退避値域の二段更新 / 更新順の保証 / 順序値の別表化)と**採らないもの 3 つ**(一意性を落とす・`DEFERRABLE` にする・アプリ層だけで守る)を書いた |
| 4 | **全数表に新設した `D2` の一意性が入っていない**(「9 行追加」に対し実差分は 8 行) | P1 | **起因** | (-) | **採用** | `D2` の行を追加。**指摘のとおり 1 周目の追加は 8 行だった**(記録の数え間違い)。**全数は 20 行**になった |
| 5 | **イベントスロットが `T2` の物理写像へ伝播していない**。版 → スロットの複合 FK も未確定 | P1 | **起因** | (-) | **採用** | `T2` の書き込み先へ**イベントスロット表**を追加 / `P2` の表列挙を **4 表 → 5 表**へ / **版 → スロットの複合 FK**(スロットの主キーと同じ 4 列)を明記し、**孤立版を DB が許さない**形にした。**`D1` なしの行はスロット参照も NULL**・`CHECK` で表す |
| 6 | **確定版の所在がスロットへ統一されていない**(`M08`・`C14`・変更イベント表・`T7` が対象イベント行のまま) | P1 | **起因** | (-) | **採用** | 4 箇所すべてを**イベントスロットの列**へ統一。**改訂で有効な版が変わっても確定版の所在が動かない**理由を書いた |
| 7 | **`D5` 台帳書き込みの `T` 要素が経路表と矛盾**(`P4=T8`・`P5=T9` のみだが台帳規則は「まず台帳へ書く(`T1`)」) | P1 | **起因** | (-) | **採用** | **`P4` = `T1`・`T8` / `P5` = `T1`・`T9`** へ統一(同 9-2 の `P4` 行が「`V12` 前に `D5` を照合し保存済み結果を先に再掲する」と定めるため、**`D5` の記録は退避・拒否と同じ境界の内側**)。`T8`・`T9` の書き込み先からは台帳を外し、`T1` へ寄せた |
| 8 | **`D5` 台帳は「第 2 の原本を作れない」関係を構造化できていない**(FK だけでは複数の子行が同じ台帳行を参照できる。全数表とも矛盾) | P1 | **起因** | (A) | **採用(射程確定)** | **経路排他(1 対 1 の強制)**を規則として置いた — **各原本表の側に `(tenant_id, D5)` の一意制約**を置く(**「自前の `D5` 一意性」ではなく「台帳行への参照が 1 対 1 であること」の強制**)。**経路と種別の一致**も規則にし、**強制の機構は実装へ送った**。全数表の拒否原本の行を直し、**退避原本の行も追加** |
| 10・11・12 | 繰り越し P2 3 件 | P2 | 非起因 | (-)/(B)/(-) | **維持(次周以降へ)** | **7.3-2 の終端遷移表に従い P0/P1 ゼロの周で一括反映**。レビュー側も「維持」を明示 |

**採用 8 件(P1 8)・不採用 0 件・繰り越し 3 件(P2)。**
**うち 2 件は「射程確定」として、不変条件を本書が定め手順・機構を実装へ送る形で閉じた。**

### 実装へ送った 2 件(**12-8 節の「実装時に確定する範囲」へ追記**)

| 範囲 | 受け取り先 | 本書が定めたもの |
| --- | --- | --- |
| **`D2` の局所再採番の手順** | **同期・記録権の最初の実装タスク** | 4 つの不変条件(5-3 節) |
| **`D5` 台帳の「経路と種別の一致」の強制機構** | **TSK-343**(`migration` を書く側) | 「1 対 1 であること」と「経路と種別が一致すること」(5-4 節) |

### 検査(反映後)

| 検査 | 結果 |
| --- | --- |
| ④ ID の在/不在 | **rc=0** |
| ① 引用形式 | `()` → rc=0 |
| ② 文書検査 3 本 | rc=0 |
| 発火条件の再記述 | **0 件**(維持) |
| 業務的一意性の全数表 | **20 行** |

**`確定ゲート周回` を 2 へ。** 本文は 2,618 → **2,663 行**。

---

## 確定ゲート 3 周目(**全文レビュー** — 射程変更直後の周)

**判定: 否決(P0: 2 / P1: 7 / P2: 1)**。**起因 4 件 / 非起因 6 件 = 起因率 44%**(**過半でない** —
7.3-6 の 2 周連続条件は発火しない。2 周目 100% → 3 周目 44% で下がった)。
**繰り越し P2 は #10・#11・#12 いずれも「維持」**(レビュー側が明示)。**今周の P2 1 件を加えて 4 件**。

### 採否記録(7.3-2)

| # | 要旨 | 重大度 | 起因 | 分類 | 採否 | 反映内容 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **キャッシュ無効化の適用述語が通常集計を取りこぼす** — 3-0 節が波及先を「実効参加のテナント」に限っているが、**設定値変更(トリガー 8)は選手通算・チーム集計・チャートにも効く**。グループ未参加テナントで古い値が返る | **P0** | 非起因 | (-) | **採用** | 3-0 節の適用先を **「④ 共有集計だけが実効参加を条件にする」**へ限定し、**①〜③・⑤ の波及先は自テナントで実効参加を条件にしない**と明記 |
| 2 | **選手統合時のカルテ所見を表現できない** — 統合元と統合先が同じカルテ種別の所見を持つと `(選手, カルテ種別)` の一意制約に違反する | **P0** | 非起因 | (A) | **採用** | **統合先の所見を現行の 1 件として残し、統合元の所見は移動記録を通じて変更履歴へ世代として積む**(10-1 節と同じ器)。**片方を捨てる / 一意制約を外す のどちらも採らない**理由を書いた。**分割で統合前の帰属へ戻せる**ことも明記 |
| 3 | **アプリ用ロールの直接権限の 2 周目修正が実効的でない** — 表単位の権限では「自テナント行だけ直接許可」はできない | P1 | **起因** | (-) | **採用** | **アプリ用ロールは業務表への直接権限を持ち、行の絞り込みは RLS のポリシーが行う(権限では絞らない)**と線引きし、**「直接権限を与えない」の対象は越境関数所有ロールの権限をアプリ用ロールへ継承させないこと**と定義した |
| 4 | **`D2` の再採番手段に「別表化」を許したのが確定範囲と矛盾** — 本書は `D2` の列・NULL 性・部分一意・`T5` の書き込み先を固定済み | P1 | **起因** | (A) | **採用** | **選択肢から「別表化」を削除**し、**採らないもの**へ移した。**別表化は手順の選択ではなく確定済み物理写像の変更**であり、**手順として実装へ送れる範囲に入らない**と明記 |
| 5 | **`T8`・`T9` の意味を同期正本から変更している** — 同 8-1 は「**`T8` の退避永続化は退避原本と `D5` ごとの保存済み結果を含む**」と定義しており、`T1`+`T8` へ分けると原子境界とクラッシュ注入点の意味が正本間で食い違う | P1 | **起因** | (-) | **採用** | **`P4` = `T8` / `P5` = `T9`** へ戻した。**`T8`・`T9` は台帳の書き込みを自身に含む**。**2 周目に `T1`・`T8` へ分けたのは同正本の定義の取り違え**だった(2 周目の指摘文が「**同期正本どおり前者〔= `T8`/`T9` に内包〕へ統一**」と言っていたのを、私が逆に読んだ)。**`T` 要素の名前は同正本が正であり本書が分解し直さない**と明記 |
| 6 | **`D5` 台帳の 1 対 1 不変条件と全数表が一致しない** — 全数表に操作イベント側が無く、直前の規則は「各表で自前の一意性を持たない」とも読める | P1 | **起因** | (-) | **採用** | **3 表(操作イベント・拒否原本・退避原本)すべてに `UNIQUE (tenant_id, D5)` を置く**と明記し、**全数表へ操作イベントの行を追加**(全数 **22 行**)。**「台帳だけが持つのは全経路・全試合・全世代の一意性」「各表が持つのは参照が 1 対 1 であることの一意制約」**と書き分けた |
| 7 | **招待コードから招待を一意に解決する不変条件が無い** | P1 | 非起因 | (-) | **採用** | 招待表へ **`UNIQUE (コードのハッシュ)`** を追加。**テナントに閉じない**(受諾者は受諾するまで対象グループを知らない)ため**規則 1 の例外として理由を明記**し、**全数表にも `➖ 対象外` として 1 行載せた**。**衝突は発行時に検出して再発行する** |
| 8 | **移行用記録権世代の `D3` が自己矛盾** — 6-1 節は全世代行に `D3` を要求するのに、移行世代は「確定水位を持たない」としていた | P1 | 非起因 | (A) | **採用** | **移行世代も `D3` を `NOT NULL` で持つ**。**値は「取り込んだ最後の `D1`」**で、取り込み完了時点では**前進しきった値**になる。**保持端末は NULL 可**(端末が存在しない) |
| 9 | **移行再実行の「バッチ単位で消去」が物理削除の全面禁止と整合しない** | P1 | 非起因 | (A) | **採用** | **「消去」を「退役」へ改め、不変条件 6 点**を置いた — 行を消さない / 退役済みは正史の連結から外し投影再生成の入力にしない / **`D5` は退役後も消費済みのまま**(やり直しは新しい `D5` で) / **業務的一意性の述語へ `退役していない` を加える**(そうしないと再投入が一意制約で失敗する)/ 移行世代も退役させる / **`FR-038` の「何度でもやり直せる」は初期化とバッチ退役の 2 経路がある** |
| 10 | 88 列対応表の 86–87 行が Markdown 表から外れている | P2 | 非起因 | (-) | **繰り越し** | 7.3-2 に従い P0/P1 ゼロの周で一括反映 |

**採用 9 件(P0 2 / P1 7)・不採用 0 件・繰り越し P2 4 件。**

### 起因 4 件の性質(**記録**)

**#3・#4・#5・#6 はいずれも「1〜2 周目で入れた構造の詰めが足りなかった」型**であり、
**#5 は私の読み間違い**(2 周目の指摘文が「同期正本どおり `T8`/`T9` に内包へ統一」と言っていたのを
逆に読んで `T1`+`T8` へ分けた)。**起因率は 100% → 44% へ下がった。**

### 検査(反映後)

| 検査 | 結果 |
| --- | --- |
| ④ ID の在/不在 | **rc=0** |
| ① 引用形式 | `()` → rc=0 |
| ② 文書検査 3 本 | rc=0 |
| 業務的一意性の全数表 | **22 行** |

**`確定ゲート周回` を 3 へ。** 本文は 2,663 → **2,691 行**。
