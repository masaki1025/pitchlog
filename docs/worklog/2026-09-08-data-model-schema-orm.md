---
date: 2026-09-08
topic: データモデルのスキーマ本体と ORM 導入(TSK-342)
branch: feature/data-model-schema-orm
---

# 作業ログ: 2026-09-08 データモデルのスキーマ本体と ORM 導入(TSK-342)

## やったこと

### 着手(`/task-start`)

| 項目 | 値 |
| --- | --- |
| Notion | [TSK-342](https://app.notion.com/p/3d593b75e68781958074ed04dff7dab7)(`進行中`) |
| ブランチ / worktree | `feature/data-model-schema-orm` / `../pitchlog-worktrees/feature-data-model-schema-orm`(`f0eeae1` 起点) |
| 開始条件 | **満たしている** — 前タスク TSK-335 は PR #49 でマージ済み(`f0eeae1` / 2026-09-08) |

### 計画段階の調査(`/investigate`)

**3 観点を並列で委任**(spec-checker = 要件突合 / decision-tracer = 決定経緯 / legacy-analyst = 旧システムの事実)
+ **自分で 3 件を実測**。統合先は [research.md](../features/data-model-schema-orm/research.md)(488 行)。

**自分の実測 3 件**:

1. **候補案の規模と節構成** — 1,866 行。**1〜12 節が移送対象**。**13 節「禁止事項の照合」は対象外**
   (T250P の `DM-MT-01` が `absent-section: "13"` で**正本に 13 節が無いこと**を検査する)
2. **引用形式の分布** — `REQ:<行>` **286 件** / その他の `<file>:<行>` 5 件 / `docs/legacy/` への行引用 **0 件** /
   安定 ID 142 件。**参照行番号の範囲は 83〜1231 で要件書 1295 行を超えない**(= 行番号だけ見ると壊れていないように見える)
3. **`FR-015` の逐語の位置と射程** — 現行 357 行目の受け入れ基準の中。**禁止対象は「同一チーム内の背番号の重複」に限定**

**decision-tracer が Bash を持たず辿れなかった履歴を、自分で埋めた**(research.md 1-1)。これが**本調査で一番大きい発見**。

### 自分の誤りの是正 3 件(research.md 0 節に記録)

| # | 誤り | 是正 |
| --- | --- | --- |
| 1 | 調査依頼に「旧 DB は 13 → 33 テーブル」と**旧側の事実として**書いた | **33 テーブルは版固定アーカイブではなく旧リポの現在位置**(`dd03160`)。`docs/legacy/` は `ed6a20f` 時点の 12(13)テーブルのみ |
| 2 | TSK-271 が「列 9 のみ・UNIQUE なし」と**実測**したと伝えた | **旧 DDL の読解**であって本番データの実測ではない。**重複行の実測件数はどこにも無い** |
| 3 | 引用の行ずれを「**−636 〜 +36**」と測った | **抽出の癖による誤り**(`REQ:n` の直前の逐語を機械的に拾い、別の引用のものを当てた)。条項単位では **+14 〜 +36 で全部プラス** |

## 決定

**まだ決定していない。** 調査の結論は research.md の「結論(要約)」5 件。計画は次の `/plan` で書く。

**調査で確定した、計画に効く事実**:

1. **ORM の矛盾は「同じ日に別タスクが逆向きに決めた」もの** — `2026-08-31 01:00` に TSK-250 が 3 値を固定
   (`a320568`)、**同日 13:19 に検査が実装され**(`3b731ba`)、**23:19 に 10.1 の箇条が入った**(`50b38d0`)。
   **TSK-250 が 10.1 を無視したのではなく、裁定の時点で箇条が存在しなかった。**
   しかも 10.1 の箇条は **TSK-250 自身の調査が出した「ORM と独立にする」という着想**を TSK-270 が正本へ書いたもの。
   **同じ意図から出発して逆の条文になった。**
2. **5.1 の ORM 留保は設計書の初版(v0.9・2026-08-07 02:19)から存在**(decision-tracer の「特定不能」を確定)
3. **`FR-015` との衝突は成立しない** — 一意制約 18 件 + 複合 FK 6 件のうち重なるのは背番号 1 件で、候補案の結論も「張らない」
4. **RLS は要件から導出されない設計判断** — 要件書・改善台帳ともに 0 hits。**本タスクが決める**
5. **`WAIT-01`〜`WAIT-08` の全 8 件が本タスクの射程**。ただし `waiting-data-model.json` は**未作成**

## 未決・次の一歩

### 人間の裁定を計画承認前に取る(4 件・research.md 申し送り)

| # | 論点 |
| --- | --- |
| **A-1** | **`game_lineup_snapshot` の受け皿を作るか** — 旧資料が矛盾(`DL` は 12 種で存在を認めず / `DB` は 13 個目として挙げる。**両者とも `ed6a20f` 時点**)。一方**要件書 `FR-038` が名指しして「別に保全する」と要求**。precedence は 88 列の意味に限定で射程外 |
| **A-2** | **越境テスト再実行ゲートの所有者** — TSK-270 が「実スキーマ適用後に再実行するゲートを TSK-250 の完了条件に置く」と決めたが、**スキーマ本体を作るのは本タスク**になった |
| **A-3** | **移行バッチの書き込み経路** — 決定が存在しない。かつ **probe の実測が候補案 3-2 のロール表と形が違う** |
| **A-4** | **`NFR-018` (b)③「動的コード生成の禁止」が ORM の実行時 SQL 生成に及ぶか** — 要件書に適用範囲の明示が無い |

### 次の一歩

- **`/plan`** で計画書を書く(**コア領域なので敵対レビュー必須**)。上の裁定 4 件は**計画承認前**に取る
- 並行トラックの **TSK-235** は計画段階まで(実装は本タスクのマージ後 — 競合 7 ファイル)


---

## 計画レビュー(2〜5 周目)と承認

| 周 | 指摘 | 反映の要点 |
| --- | --- | --- |
| 2 | 8(P0 2 / P1 6) | fixture 固定位置 / `WAIT-*` closure の所有 / 件数是正 / 対応表 2 層化 / 7.3-7 射程宣言 / 7.4 順守 / 独自停止条件の削除 / 意味条件の `[手動・外部]` 化 |
| 3 | 5(P0 0) | 検査 ①④ の rc 化 / `game_lineup_snapshot` 条件を逐行確認へ / `TSK-` 0 件条件の撤回 / `WAIT-04` 送り先の実タスク化 |
| 4 | 1(P0 0) | fixture の固定を確定ゲート後へ |
| 5 | 1(P0 0) | その固定を**ステップ 13** として表内へ(`tests/fixtures/` と `scripts/` は文書パスでないため、記法なしでは現在地導出が `unknown` に落ちる) |

**人間の承認 2026-09-08・山田正輝**(コミット `48ec1a8`)。13 ステップ。
実行順は **ステップ 1〜12 → 確定ゲート(7.3)→ ステップ 13**。

### 3 周目 `P1-4` の実測(規約の解釈を是正した)

**承認済み正本は送り先の目印として裸の `TSK-<番号>` を使っている** — `docs/design/sync-protocol.md` に
**56 箇所**・要件書に **6 箇所**。**いずれも `app.notion.com` は 0 件**。
→ 設計書 7.4「正本は Notion を参照しない」が禁じるのは **URL 参照と内容の複製**であり、
**タスク ID の目印は禁じていない**。2 周目に自分が置いた「`TSK-` 0 件」条件は過剰だったので撤回した。

---

## ステップ 1: 正本の新設・素材移送・引用の貼り替え・対応表

### 引用の貼り替えをどう機械的に解決したか(**行番号のずれは一定でない**)

下調べの時点では「ずれは **+14 〜 +36** で一定でないため機械的な一括置換では移せない」としていた。
**解決の鍵は、候補案を書いた 16 コミットすべてで要件書が同一内容だったこと**である
(`git show <sha>:docs/requirements/...` の md5 が **1 種**・**1,249 行**)。
→ **行番号引用の解決基準は単一**なので、次の 3 段で全件を機械的に同定できた:

1. **旧行本文の取得** — 基準版(1,249 行)の当該行をそのまま読む
2. **現行版での同定** — その行本文を現行の要件書 v2.7(1,295 行)から完全一致で探す
   (**173/177 が一意に一致**。残り 4 件は個別に解決 — 下記)
3. **囲む安定 ID の算出** — 基準版側と現行版側の**両方**で最内の安定 ID を計算し、**一致することを確認**
   (母集合の正は `scripts/design_relations/req-universe.json` の 8 カテゴリ 212 件)

**下調べで個別照合していた 8 件のずれ(+14・+18・+18・+18・+18・+19・+21・+36)は、
この機械的解決の出力と全件一致した**(独立な再現)。

**個別に解決した 4 件**:

| 参照 | 状況 | 解決 |
| --- | --- | --- |
| 素材 303 行 | 行本文が改稿されていた | `FR-013`(現行 321 行)。「適用されず、破棄されず退避され」は不変 |
| 素材 355 行 | 行本文が `- **受け入れ基準**:` で 25 箇所に重複 | 囲む条で解決し `FR-017`(現行 373 行)。区分 3 固定・拡張ラベル Should・全区分可逆はいずれも現行にある |
| 素材 722 行 | **規範が変わった** | `FR-035`(現行 740 行)。**v2.4 で「閲覧・書き出し = Must」「挿入位置を指定して取り込む = Should」へ優先度分割**。素材が 1 つの Must として書いていた記述は**ステップ 9(`C16`)で分ける** |
| 素材 1055 行 | v2.6 で追補 | `付録A/A-1`(現行 1093 行)。「割合・率の表示には常に母数を併記する」は不変(併記値の定義が加わって強まった) |

### 対応表の構造(`scripts/design_relations/citation-map-data-model.json`)

| 層 | 件数 | キー |
| --- | --- | --- |
| **① `references`** | **145** | 旧参照の文字列(`REQ` 形式 141 + 未修飾パスへの行引用 4)。**候補案 1〜12 節のユニーク集合と exact-set 一致を機械確認済み** |
| **② `occurrences`** | **291** | `(source_line, ordinal)`。**291 組すべて一意**を機械確認済み |
| `companion_lines` | 36 | 「素材 597-606」「素材 722・724・744」型の**2 個目以降の行番号**。検査器の正規表現には掛からないが規範としては引用されている |
| `bare_line_citations` | 5 | `ADR-003 :93-104` 型の裸の行番号(`ADR-003 D-1` / `D-6・D-8` / `D-6` / `決定記録 D-43` ×2 へ) |

**未修飾パスへの行引用 4 件はすべて `docs/legacy/` だった** — 引用規約は版固定アーカイブへの行番号引用を
**許容する**ので、パスを修飾して残した(`docs/legacy/baseball-scoring-db-structure.md:168`・`:176`・`:286` /
`docs/legacy/research/data-layer.md:402`)。**未修飾だと検査器の legacy 判定に落ちて違反になる**のが原因。

### 移送で変えたこと / 変えていないこと

**変えたこと(文書同一性の言い換えのみ)**: 「本書は候補案であり決定ではない」→ 正本の位置づけ /
`**候補案**:` → `**本書の設計**:`(15 箇所)/ 見出しの `候補案: ` を落とす(12 箇所)/
`本候補案`・`本起草` → `本書` / `正本化タスク` → 内容に応じて `TSK-343`・`実機確定タスク`・`本書` /
`docs/features/` 配下(`plan.md`・`research.md`)への参照を**全廃**(6 箇所は事実を残して出所表記を落とした)。

**変えていないこと**: 技術内容は 1 文字も変えていない。**移管欠陥 ID 群の是正はステップ 2〜10** が行う。

**節の移送は全数確認済み** — 候補案 1〜12 節の見出し **84 件**がすべて正本にあり
(改稿した 19 件は 1 対 1 で対応)、正本側の追加は `## 変更履歴` と `### 1-1. 参照規約` の **2 件**だけ。

### 検査の結果(是正前の実測を含む)

| 検査 | 結果 |
| --- | --- |
| **①** 引用形式 | **正本 `()` / 候補案 3 形式** → **rc=0** |
| **②** 文書検査 3 本 | `check_docs_status` / `check_design_propagation` / `check_doc_coverage` すべて **rc=0**(**索引行を入れる前は `check_docs_status` が「docs/README.md の正本一覧に載っていない」で rc=1** — 番人が効いていることを確認した) |
| **③** core-guard | `tests/test_core_guard.py` **45 passed** |
| **④** ID の在/不在 | **rc=1(必須 54 件すべて不在)** — ステップ 1 直後の期待値そのもの。是正の出発点 |
| `## 13.` の不在 | **0 件**(T250P の `DM-MT-01` が検査する条件) |

### 気づいたこと(後続ステップの制約)

- **射程宣言(ステップ 12)は `## 13.` にしてはいけない** — T250P の `DM-MT-01` が
  `absent-section: "13"` を検査する。**`### 12-8.` の下に置く**
- **`P-01`〜`P-60` の台帳に恒久的な置き場が無い** — 正本は 49 件を 104 箇所で参照するが、
  **`docs/design/sync-protocol.md` にあるのは 44 行分**で、`P-04`・`P-16`・`P-40`・`P-45` 等 31 件は
  候補案の `research.md` 3-2 にしかない。**本書の 1-1 節に「台帳の恒久的な置き場は本書では定めず
  受け取り先へ送る」と書いた**。**ステップ 12 の射程宣言で送り先を明示する**
- **正本本文に `REQ:<数字>` と書くと検査 ① が落ちる**(`\bREQ:\d+\b` に当たる)。
  変更履歴で件数を説明する際も「`REQ` 形式の行番号引用」と書く

---

## ステップ 2: RLS の設計判断とテーブル制約

### 本文へ入れたもの

1. **3-1 節に「この採否は要件から導出されない — 本書の設計判断である」を新設**
   - **独立再実測(2026-09-08)**: `RLS` / `row level security` / `行レベルセキュリティ` は
     **要件書 0 件・要件対話の決定記録 0 件・改善台帳 0 件**
   - `NFR-010` が課すのは **① 結果 ② 適用範囲 ③ 検証**の 3 つだけで**実現層を指定していない**
   - 改善台帳 `I-2` が指名する手段も `team_id` 列
2. **採る 3 つの理由**(旧「RLS を採れる根拠」を理由として整理し、2 点を足した)
3. **この判断の帰結**を明文化 — 3-4 節の 3 規則は RLS 採用に**従属して決まる**。要件から独立には導けない
4. **3-4 節に「本書が置く業務的一意性の全数」表(8 行 + 置かない 3 件)**を新設
5. **規則 3「制約違反の詳細を一律に秘匿する」を新設**(旧・規則 2 の手段 (c) から昇格)。
   手段 (c) は**規則 3 への参照だけ**にした(同じ規則を 2 箇所で定義しない — 3-0 節と同じ規律)

### 理由 2 に足した典拠(**旧システムが実際にその状態だった**)

テナント境界は `game.owner_team_id` の 1 列とアプリ層のガードだけで、
**所有権チェックはオプショナル引数を省略すると全データへ到達できた**
(`docs/legacy/baseball-scoring-db-structure.md:176` / `docs/legacy/research/data-layer.md:402`)。
改善台帳 `I-2` が「分離の穴が後追い是正になった」ことを課題に挙げ「利用が 1 チームの段階から組み込む」と
定めた対象そのものである。

### 理由 3 に使った条項の位置を計画から是正した

計画 4 節は「**`NFR-019(b)` の越境テストが『API 直叩きを含む全経路』を要求する**」と書いていたが、
**「API 直叩き含む」は `NFR-019` ではなく `NFR-010` の測定方法にある**(要件書 846 行 —
`NFR-010` の節内。`NFR-019` は CI 常設先として参照されている側)。
→ **本文では `NFR-010` の測定方法として引いた。**

### `tenant_id` を先頭に含まない業務的一意制約が 0 件 — 逐項の証跡

**本書の業務的一意性の全数 8 件**(代理キーは対象外。節番号は本文で機械確認した。**ステップ 3 の裁定 A-1 で 9 件目を追加した** — 下記):

| # | 節 | 一意性 | キーの形 | 判定 |
| --- | --- | --- | --- | --- |
| 1 | 5-3 | べき等キー | `UNIQUE (tenant_id, べき等キー)` | ✅ |
| 2 | 5-3 | (試合, 世代, 連番) の有効な行が 1 件 | `UNIQUE (tenant_id, 試合, 世代, 連番) WHERE 置換されていない` | ✅ |
| 3 | 6-1 | 記録権の現世代が 1 件 | `UNIQUE (tenant_id, 試合) WHERE 失効していない` | ✅ |
| 4 | 7-1 | テナント自身のチームレコードは高々 1 件 | `UNIQUE (tenant_id) WHERE kind = 'self'` | ✅ |
| 5 | 9-1 | 参加は `active` が最大 1 件 | `UNIQUE (グループ, 参加テナント) WHERE 状態 = active` | ✅ **グループの内側**(制御資源 — `NFR-010` が「記録・集計とは別のクラス」と定める) |
| 6 | 10-3 | 語彙(チーム拡張可の層) | `UNIQUE (tenant_id, キー名)` | ✅ |
| 7 | 10-3 | 語彙(システム固定の層) | `UNIQUE (キー名)` | ➖ 対象外(テナントに属さない) |
| 8 | 10-4 | 付録C のシステム設定値 | `UNIQUE (キー名)` | ➖ 対象外(同前) |

**意図的に置かない 3 件**: 背番号(7-2 — `FR-015` / `P-15`)/ チーム名(7-1 — `FR-039`)/
試合の業務的一意性(5-1 — 要件に明示がない)。

→ **✅ が 6 件・➖(テナントに属さないもの)が 2 件で、`tenant_id`(またはテナントに閉じた親)を
先頭に含まない業務的一意制約は 0 件。**

**節番号の同定方法**: 見出しを走査して各行の所属節を計算し、`部分一意索引` / `WHERE 状態 = active` /
`一意制約はキー名でグローバル` 等の実文言の位置から節を機械的に取った
(当初 `kind = 'self'` を 3-6 節、設定値を 10-5 節と書いていたが、**実測で 7-1 と 10-4 に是正**した)。

### 移送の残滓 3 件を同時に直した

**`Web 調査 6-2 節`・`Web 調査 6-3・6-4 節`** という参照が 3 箇所残っていた(候補案 `research.md` の節番号で、
ステップ 1 の置換は `research.md` の文字列がある行しか拾えていなかった)。
→ **`PostgreSQL 公式ドキュメント` / `Alembic・SQLAlchemy の公式ドキュメント`** へ改めた。

### 検査

| 検査 | 結果 |
| --- | --- |
| ① 引用形式 | 正本 `()` / 候補案 3 形式 → **rc=0** |
| ② 文書検査 3 本 | **rc=0** |

---

## ステップ 3: 裁定 A-1・A-3 の反映

### A-3(移行バッチの書き込み経路)— 3-2 節

**候補案の内部矛盾を閉じた**: 3-2 節は「マイグレーション用ロール(テーブル所有者)は**バイパスする(所有者)**」と
書きながら、同じ節の末尾で「**全テーブルへ `FORCE ROW LEVEL SECURITY` を推奨**」していた。
**FORCE は所有者もポリシーに従わせる**ので、この 2 つは両立しない。

**反映内容**:

- 節見出しを **「ロールを 4 つに分ける(論点 3・裁定 A-3)」** へ
- ロール表の**マイグレーション用**の RLS 欄を「バイパスする(所有者)」→
  **「従う — 全テーブルへ `FORCE ROW LEVEL SECURITY` を掛けるため、所有者でもポリシーの適用を受ける」**
- **`#### 移行バッチの書き込み経路(裁定 A-3)`** を新設 — 専用ロール + `BYPASSRLS`。
  **`BYPASSRLS` は FORCE でも拘束されない**ので、**全テーブルに FORCE を掛けたまま一括投入できるのはこの経路だけ**
- **8-2-B 節の「常時バイパス経路を作らない」と矛盾しない理由を表で明示**
  (稼働: 常時 / 移行期間のみ・ログイン: `NOLOGIN` / `LOGIN` 必須・到達経路: 関数の `EXECUTE` / バッチの接続のみ)
- **このロールに課す条件 6 件**(期間限定・アプリから到達しない・DDL を持たない・書き込み先の限定・監査・検査対象)
- **却下案 4 件**(所有者で投入 / 移行中だけ FORCE を外す / ポリシーに従わせて `set_config` で切り替え / superuser)

### A-1(移行元の最終オーダーの保全先)— 12-3 節

**`#### 移行元の最終オーダーの保全先(裁定 A-1)`** を新設し、**物理実体として定義**した
(表・所属・キー・FK・列・由来・読み方の禁止・突合・削除の 9 項目)。

**要点**:

- **キーは `UNIQUE (tenant_id, 試合, チームレコード)`**・**FK は複合参照** — 3-4 節の規則 1・2 に従う
  (**ステップ 2 で作った「業務的一意性の全数」表への追加が必要**。下記「次のステップへの持ち越し」)
- **選手 ID へ解決しない** — 旧側は選手を名前・背番号の JSON 文字列で持つため、
  **原本のまま保持**し、解決結果は別の nullable 列(88 列写像で列 63-71 に採った原則と同型 — 裁定 `R-1`)
- **7-4 節のスタメンとは別の表**である。混ぜると先発として読まれる経路ができる(**P-45** に反する)
- **突合の相手がこの表**である — 「最初のプレイ行と食い違う試合は交代のあった試合として警告レポートへ列挙」
  (`FR-038`)の比較対象

**旧側の存在矛盾の扱い**: `docs/legacy/research/data-layer.md:17` は全 12 種として挙げず、
`docs/legacy/baseball-scoring-db-structure.md:176` は 13 個目として挙げる(**両者とも同一リビジョン**)。
→ **存在しなければ移行 0 件で終わるだけ**で、要件が名指しする保全先を持たない方が害が大きい、という
裁定の理由を本文に書いた。

### 機械検査の実測(**計画の「是正前 1 件」は誤りだった**)

計画 4 節のステップ 3 は「**「テーブル所有者」+「バイパス」が同一文に現れる箇所が 0 件**
(**是正前 = 候補案に 1 件**)」としていたが、**実測では候補案に 2 件**あった:

| # | 箇所 | 性質 | 処置 |
| --- | --- | --- | --- |
| 1 | 3-2 節冒頭「テーブル所有者は既定で RLS をバイパスするため…」 | **実効する設計記述**(矛盾の本体) | 書き換えた |
| 2 | 12-7 (1)「① 『`SECURITY DEFINER` は所有者権限で走るため RLS をバイパスする』」 | **過去の誤りの記録**(誤った記述の引用) | 「RLS の適用を受けない」へ語を改め、記録としての意味は保った |

**判定方法**: 本文を `。` `改行` `|`(表セル)で切って文に分け、
**両語が同一の文に現れる件数**を数えた。**正本 0 件 / 候補案 2 件**。

### 検査

| 検査 | 結果 |
| --- | --- |
| 所有者バイパスの同一文 | **正本 0 件 / 候補案 2 件** → rc=0 |
| ① 引用形式 | 正本 `()` → **rc=0** |
| ② 文書検査 3 本 | **rc=0** |

### 次のステップへの持ち越し

- **ステップ 2 で作った「本書が置く業務的一意性の全数」表へ、A-1 の
  `UNIQUE (tenant_id, 試合, チームレコード)` を足す**(表自身が「表にない一意性を後から足すときは
  この表へ行を足して同じ判定を通す」と定めている)。**本ステップで足した**

---

## ステップ 4: 順序系の写像(`C01`〜`C05`・`C08`)+ `WAIT-05`

### 同期プロトコル正本との逐行照合(**`U-7` の要求**)

**同 11-4 `U-7`**(sync-protocol.md 2077 行): 「**D1 と D2 の用途分離のデータモデルへの伝播**(5-1)。
**出場区間の境界・下流再計算の起点には D2 を使い、D1 を使わない**ことを製品データモデルへ反映する」/
本書の境界外 = 「列・索引・表分割」/ 受け取り先 = TSK-250(→ TSK-335 の裁定 `R-1` で本タスクへ移管)。

**同 5-1** の規範(逐語): 「**同期連番(D1)は同期の順序・欠落の判定と undo の逆順に用い、
論理順序(D2)はプレイの投影・出場区間の境界・下流再計算の起点に用いる**」/
「過去位置へ挿入するイベントは、**D1 では生成時点の末尾**に置かれ、**D2 では指定された過去位置**に置かれる」/
「**出場区間の境界または再計算の起点に D1 を使ってはならない**」。

**同 5-2** の確定(起草時は本書の (B) 送りだった): 順序ラベルの方式は **整数の間隔(gap)方式**。
**有理数(分母が指数的に伸びる)・LexoRank(単一書き手なので衝突耐性が不要)・相対参照(範囲集計ができない)
はいずれも却下**。

### 4 箇所 + 識別子表の照合結果

| # | 箇所 | 是正前 | 是正後 | ID |
| --- | --- | --- | --- | --- |
| 0 | **4-3 節 識別子表** | 「**イベント連番**」1 本だけ。役割は「順序と欠落の判定」 | 「**同期連番(`D1`)**」へ改称し**「出場区間の境界・再計算の起点には使わない」を明記** + **「論理順序(`D2`)」の行を新設**((試合, 記録権世代)内で全順序・プレイ投影/境界/起点が役割) | `C01` / `C02` |
| 1 | **5-2 節 用途対応表** の「論理順序」 | 「同期連番とは別の順序値を持つ。**具体の順序ラベル方式は (B)**」 | **`D2`**。**物理表現は整数の間隔(gap)方式で確定**(同 5-2 を取り込み。**(B) 送りを解除**) | `C03` |
| 2 | **5-2 節 用途対応表** の「出場区間の境界」 | 「**イベント連番**で持つ」 | **`D2`** で持つ。**`D1` を使ってはならない**(同 5-1) | `C04` |
| 3 | **5-2 節 用途対応表** の「再計算の起点」 | 「**イベント連番**。行識別子でもプレイ番号でもない」 | **`D2`**。**`D1` でも行識別子でもプレイ番号でもない**。**undo の逆順だけは `D1`** | `C05` |
| 4 | **6-2 節 出場区間(投影)** | 有効開始・有効終了 = 「**イベント連番またはプレイ番号**」 | **`D2`**。**`D1` でもプレイ番号でもない** | `C04` |
| 5 | **6-3 節 状態補正の位置** | 「以降の再計算の起点」だけ | 「**`D2`** で表す(**`D1` を使わない**)」 | `C05` |
| 6 | **12-3 節 移行** | 「**イベント連番** — 旧行の行識別子の順序で振る。**これが論理順序の正になる**」 | **`D1` と `D2` の両方**を旧行識別子の順序で振る。**移行では並びが一致するが、同じ値を 1 本の列で兼用しない**(移行後に `FR-007` の任意位置挿入が入ると分岐するため**列は最初から 2 本**) | `C08` |

**「到着順」の語は全廃した**(是正前 5 件 → **0 件**)。`D1` を説明する語は
**「キュー追記時に採番される順」**へ統一した(同 5-1 の「生成時点の末尾に置かれる」と整合)。

### 12-7 (2) を「解消済み」へ

起草時は **「イベントの順序」を収束しなかった 3 領域の (2)** として、
「順序ラベルの方式 / 挿入時に再採番するか / 同期連番の意味の正」を未確定のまま同期設計へ送っていた。
**同期プロトコル設計が `v0.3` で `approved` になり 3 件すべてが確定した**ので、
**(2) を「解消済み」と明記し、決まっていないことを「なし」にした**。
「なぜ文書だけでは閉じなかったか」は**記録として過去形で残した**。

### 検査

| 検査 | 結果 |
| --- | --- |
| ④ ID の在/不在 | **`C01` 3 / `C02` 1 / `C03` 1 / `C04` 2 / `C05` 4 / `C08` 3 / `WAIT-05` 3**(是正前は全件 0)。`C13` は **0 件**を維持(裁定 `R-6` — TSK-250 の所有) |
| 「到着順」 | **0 件**(是正前 5 件) |
| ① 引用形式 | 正本 `()` → rc=0 |
| ② 文書検査 3 本 | rc=0 |
| ④ 全体 | **rc=1** — 残り 47 件はステップ 5〜10 の担当分(期待どおり) |

### 気づいたこと(重要)

**欠陥 ID(`C01`〜`C16` 等)の定義そのものはリポジトリに存在しない。** T250P にあるのは
**群の件数と所有**だけで、**ID ごとの `source` / `location` / `invariant` は TSK-250 のステップ 4 が
これから作る**(`defects-data-model.json` のベースライン 78 件)。
→ **本タスクは計画書の各ステップ行が書いている「何を直すか」と、同期プロトコル正本の該当節を
典拠にして是正している。** ID と是正内容の対応は**本 worklog が唯一の記録**であり、
**ステップ 12 の `closure-handoff-data-model.json` でそれを機械可読にして TSK-250 へ渡す**。
ステップ 12 の設計時に、**`closure_evidence` に「反映節」だけでなく「本 worklog の該当行」も入れる**。

---

## ステップ 5: 削除経路の分離(`C06`・`M06`)

### 典拠(同期プロトコル設計 11-2 — **データモデル影響差分**)

**この節が本タスクのステップ 5〜8 の一次典拠である。** 逐語で確認したもの:

- **`W1`**: 確定済みイベントの修正・削除は**新しい変更イベントとして追記**し、**既存イベントを書き換えない**
- **参加区分表**(11-2 の直後): **#10 プレイの修正 / #11 プレイ行の論理削除 / #12 交代イベントの修正** —
  いずれも **従属**・**変更版順を持つ**・**自身の `D2` を持たず、対象参照と確定版が必須**
- **`W3`**: `P3`(= `D1` なし変更イベント)は **`D1` を持たない** / **`W3-c`**: `P3` は **prefix コミットの対象外**
- **`W3-a`**: `V11` は対象の期待版で、**一致するときだけ受理**して**対象の確定版を進める**
- **`W3-b`**: **変更版順は `D1`・`D2` とは別の順序**で、**論理再生順に使わない**
- **`W4`**: **進行中の変更操作では `V12` を照合**し、**終了後の変更操作では `V12` を前提にしない**
- **`V3`/`V6`/`V10`/`V11`**: `P3` は **`D4` を持たず**復旧世代だけ照合 / **`D2` を持たず**対象に従属 /
  **`V10` は必須** / **`V11` は必須**
- **影響差分 9**: 対象は **`(試合, 対象 D4, 対象 D1)`** で名指す。**対象 `D1` だけの列では不足**
- **影響差分 13-a**: **対象側に「確定版」の列**が要る。**`D1`・`D2` とは別**
- **影響差分 4**: **`D2` を持たない種別がある** → **`D2` を `NOT NULL` にできない、または別表に分ける判断**

### 是正内容

| 箇所 | 是正前 | 是正後 | ID |
| --- | --- | --- | --- |
| **4-2 節の箇条** | 「『プレイ行の論理削除』は投影側のフラグであり、**正は墓標・改訂イベント**」 | 「**正は変更イベント(参加区分 #11)**。投影側のフラグはその投影」+ **墓標(#8)・改訂版(#9)を流用しない**理由(同期の拒否と再送の仕組みであって訂正ではない) | `C06` |
| **5-3 節に新設** | 変更イベントの実体が無かった | **`#### 変更イベント(参加区分 #10〜#12)`** を新設 | `C06` / `M06` |

**「正は墓標・改訂イベント」は 0 件**(是正前 1 件)。

### #10〜#12 に対応する実体・属性・制約(**逐項の証跡**)

| # | 事項 | 本文での定義 | 典拠 |
| --- | --- | --- | --- |
| 1 | 置き場 | **操作イベント表に同居**。種別で識別。別表に分けない | `W1` / `4.0-2` |
| 2 | `D1` | **持たない**(採番しない)。**列は NULL 可**。prefix コミットの対象外 | `W3` / `W3-c` |
| 3 | `D2` | **持たない**(対象に従属)。**列は NULL 可** | `V6` |
| 4 | `D4` | **持たない**。要求時の**復旧世代だけを照合** | `V3` |
| 5 | 対象参照 | **`V10` = `(試合, 対象 D4, 対象 D1)` の複合参照**が必須 | `V10` / 影響差分 9 |
| 6 | 期待版 | **`V11`** が必須。**一致するときだけ受理**し対象の確定版を進める | `V11` / `W3-a` |
| 7 | 対象側の確定版 | **対象イベント側に確定版の列**。`D1`・`D2` とは別系列 | 影響差分 13-a |
| 8 | 変更版順 | **`K4`**。対象ごとの期待版・確定版の連鎖。**論理再生順に使わない** | `K4` / `W3-b` |
| 9 | 記録権証明 | **進行中は `V12` を照合・終了後は前提にしない** → 経路を分ける | `W4` |
| 10 | べき等キー | **`V1`** で無条件。**`(テナント, D5)` で一意**(3-4 節 規則 1) | `V1` |
| 11 | 原子境界 | **`P3`** の経路(`T1`・`T2`・`T4`・`T6`・`T7`) | 影響差分 10 |

**11 項目すべてを本文の表として置いた。**

### `M06`(削除機構の混同)の是正

**11-1 節の削除 4 系統**(試合・選手・テナント・制御資源の**エンティティのライフサイクル**)と、
**#11 の確定済みプレイ行の訂正**は**別の話である**ことを明記し、**同じ機構にまとめないと書いた**。

### `NOT NULL` にできないことの帰結を明記(影響差分 4 の判断)

**本書は変更イベントを操作イベント表に同居させる**ので、**`D1`・`D2`・`D4` は NULL 可**になる。
その帰結として:

- **`(試合, 世代, 連番)` の部分一意インデックスの述語に `D1 IS NOT NULL` を加える**
  (PostgreSQL の `UNIQUE` は NULL を重複と見なさないが、**述語で明示して意図を読めるようにする**)
- **`D1` が NULL のイベントの順序は変更版順で決まる**。**`D1` の欠落として扱ってはならない**(`W3-c`)

### 検査

| 検査 | 結果 |
| --- | --- |
| ④ | **`C06` 3 件 / `M06` 3 件**(是正前 0) |
| 「正は墓標・改訂イベント」 | **0 件**(是正前 1 件) |
| ① 引用形式 | `()` → rc=0 |
| ② 文書検査 3 本 | rc=0 |

---

## ステップ 6: 識別子と一意性(`M01`〜`M04`・`M07`・`M08`・`M17`)+ `WAIT-03`

### 前提の再確認

同期プロトコル設計 **4-1** は「**列名・型・NULL 性・FK・索引・表分割は決めない**」と明記している。
**それを決めるのが本節である**(同 11-2 が「物理構造の決定は各受け取り先が担う」と書いている側)。

### 5-3 節へ「識別子と一意性の物理写像」を新設(7 行の表)

| 値 | 型 / NULL 性 | 一意性・制約 | ID |
| --- | --- | --- | --- |
| `D1` | `bigint` / **NULL 可**(変更イベント #10〜#12 は持たない) | `UNIQUE (tenant_id, 試合, D4, D1) WHERE D1 IS NOT NULL AND 置換されていない` | `M01` |
| `D2` | `bigint` / **NULL 可**(変更イベント・その場登録 #6・墓標 #8 は持たない) | **一意制約を置かない**(`O2` の局所再採番で値が動く) | `M02` |
| `D4` | `bigint` / **NULL 可**(`V3`) | 記録権世代表側で `UNIQUE (tenant_id, 試合) WHERE 失効していない` | `M04` |
| `V10` | `(試合, 対象 D4, 対象 D1)` の 3 列 | **複合 FK**。**対象 `D1` だけの単一列にしない**(影響差分 9) | `M07` |
| `V11` + 確定版 | `V11` は変更イベント側 / **確定版は対象イベント側** | 受理時に「`V11` = 対象の確定版」を検査し確定版を進める。**`D1`・`D2` とは別系列** | `M08` |
| 一時 ID 写像 | 一時 ID は UUID | `UNIQUE (tenant_id, 一時 ID)`。**写像行の有無が状態の一部**(`C4`) | `M17` |

**業務的一意性の全数表(3-4 節)へ 2 行を反映**: 5-3 の `(試合, D4, D1)` を
**`D1 IS NOT NULL` 付きの述語へ精緻化**し、**一時 ID 写像の `UNIQUE (tenant_id, 一時 ID)` を追加**した
(表自身の規律「表にない一意性を後から足すときはこの表へ行を足す」に従った)。**全数は 10 行**になった。

### `WAIT-03`(`D2` の数値型)— **`bigint`** と根拠 4 点

1. **gap 方式は値の間に隙間を空ける**ので**行数よりはるかに広い値域が要る**。1 試合 250〜350 行
   (`6.1/毎球データ`)で、`integer`(約 21 億)だと**初期間隔 2^20 で 1 試合 2,000 行程度で枯渇域**
2. **`numeric` は採らない** — 有理数方式を却下した以上**任意精度が要らず**、索引サイズと比較コストが重い
3. **文字列ラベルは採らない**(同 5-2 が却下)
4. **`bigint` は標準型**で `7.1` に適合

**初期間隔・再採番の閾値は本書では決めない** — 同期プロトコル設計 5-2 が
「**実クエリが 1 本も無い段階で定数を確定させない**」として **(B) 論点 26** へ送っている。
**型だけを決める理由**: 型が決まらないと `models`(TSK-343)が書けない。

### `M03`(`D2` に不変前提の索引を張らない)— 11-3 節の索引原則 **8** として追加

**`D2` は `O2` の局所再採番で値が動く**(影響差分 2・3・26)。したがって:

- **一意キー・主キー・FK の参照先にしない**
- **他表へコピーしない**(投影行・サイドカー・キャッシュの結合キーに使わない。結合キーは
  `(試合, D4, D1)` または行識別子)
- **`D2` 値を埋め込んだ部分索引の述語や永続カーソルを作らない**
- **`D2` を含む索引は範囲・ソート用に限る**

### 手動条件の充足

| 条件 | 充足 |
| --- | --- |
| `(試合, D4, D1)` の部分一意がある | ✅ `M01` の行(`WHERE D1 IS NOT NULL AND 置換されていない`) |
| `D2` に不変前提の索引を張らない旨が索引原則にある | ✅ 11-3 節の原則 **8** |
| `WAIT-03` の数値型と根拠が本文にある | ✅ `bigint` + 根拠 4 点 |

### ID の割り当てについて(**申し送り**)

**ステップ 4 の worklog に書いたとおり、欠陥 ID の定義はリポジトリに存在しない。**
本ステップの 7 ID は**計画書ステップ 6 行の記述**(「D2 の列・部分一意・索引の禁止、V10 の複合参照列、
V11 と確定版列を物理写像として置く」)を満たす単位へ**本タスクが割り当てた**:

| ID | 割り当てた内容 |
| --- | --- |
| `M01` | `D1` 列と `(試合, D4, D1)` の部分一意 |
| `M02` | `D2` 列(型・NULL 性・一意制約を置かないこと) |
| `M03` | `D2` に不変前提の索引を張らない(索引原則 8) |
| `M04` | `D4` 列 |
| `M07` | `V10` の複合参照列 |
| `M08` | `V11` と対象側の確定版列 |
| `M17` | 一時 ID → 正式 ID の写像(`C1`〜`C4` の物理表現) |

**`M17` の割り当ては本タスクの判断である**(他ステップが請求していない「識別子」の残余として置いた)。
**ステップ 12 の `closure-handoff-data-model.json` に本割り当てを載せ、TSK-250 のステップ 4
(欠陥台帳のベースライン固定)で突合してもらう。**

### 検査

| 検査 | 結果 |
| --- | --- |
| ④ | `M01` 2 / `M02` 1 / `M03` 2 / `M04` 2 / `M07` 2 / `M08` 2 / `M17` 2 / `WAIT-03` 2(是正前は全件 0) |
| ① 引用形式 | `()` → rc=0 |
| ② 文書検査 3 本 | rc=0 |
