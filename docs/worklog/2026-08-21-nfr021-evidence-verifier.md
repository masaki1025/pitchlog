---
date: 2026-08-21
topic: Phase 4-5 NFR-021 証跡の機械検証器と append-only の CI 検査
branch: feature/nfr021-evidence-verifier
---

# 作業ログ: 2026-08-21 Phase 4-5 NFR-021 証跡の機械検証器と append-only の CI 検査

## やったこと

- **/task-start**: Notion タスク起票(優先度 高・見積 8)→ worktree 作成 → 計画書・worklog の雛形 → Notion を `進行中` へ遷移 + ブランチ名をコメント記録。手順どおり **Git 側の成功後に遷移**した

- **/investigate**: 調査サブエージェント **3 本を並列**(要件突合 = spec-checker / 決定経緯 = decision-tracer / 実装面の実査 = Explore)。**legacy-analyst は外した** — 題材が検証器の実装と Git の意味論で、旧システムの事実が関与しないため(4-4 と同じ判断)。**Web 調査(/research)も不要と判断** — 外部技術ではなく自リポジトリの正本と Git CLI の挙動に閉じるため、Web より自分の実測の方が強い典拠になる。結果を [research.md](../features/nfr021-evidence-verifier/research.md) へ統合

- **自分の実測(失効判定の Git 挙動)**: 10.1 の変更パス集合の 4 規定が Git の既定挙動で満たされるかを本リポジトリの実履歴で測った。**すべて満たされない**ことが判明(詳細は research.md 4 節):
  1. **2 点差分は範囲内で作成→削除されたパスを落とす** — `frontend/src/lib/display_geometry_263_v1.json` が 2 点差分で 0 件・`git log` で 1 件
  2. **マージコミットの差分は既定で出ない** — `ed4f745..c11376c` で **109 → 121 パス**(`-m` 付与)。差の 12 件に `AGENTS.md`・`CLAUDE.md`・`.claude/agents/*` = 失効対象が含まれる。`--diff-merges=combined` / `dense-combined` / `remerge` は 109 のままで取りこぼす。`--diff-merges=all` は 2.53.0 では**無効値**
  3. **リネーム検出は既定 on で旧パスが消える** — `d21620b`(JSON を `frontend/src/lib/` → `contracts/` へ移設)の `--name-only` は新パスのみを出力。`--no-renames` で両方出る。**ただし範囲の和集合では偶然カバーされることがある**(同範囲で 2 件 vs 4 件)ため、和集合はこの取りこぼしを構造的に塞がない
  4. **`merge-base --is-ancestor` は 0 / 1 / 128 の 3 値** — `returncode != 0` の一括判定は「失効」と「判定不能」を混同する
  - 結論: **`git log --format= --name-only -m --no-renames <T>..<C>`** の 3 オプションを全部明示して初めて規範どおりになる
  - 副産物: **`onboarding.md` は現在 `status: draft`**(blob `402f50a…`)。合格条件 ④ は approved を要求するので**現 develop の phase4 証跡は必ず ④ で不合格**であり、4-6 が前提であることの機械的裏付けになる。**検証器の負例テストに実 blob が使える**

- **調査エージェントの報告を原典で 2 件裁定した**:
  1. **NFR-018 の射程** — Explore が命名文法の重複を「NFR-018 の思想」として提示、decision-tracer は「ドメイン計算に限定され重複を禁じる決定は見つからない(不明)」と報告。要件書 `:867` を読んで**閉じた列挙**(状況判定・座標変換・捕球選手推定・成績集計の前処理・終了判定)であることを確認 → **NFR-018 違反ではない**。ただし両者は同一の述語ではない(索引検査は `.md` 限定・真偽値のみ / 4-5 は拡張子を問わず fail-closed・成分抽出が必要)ため、共通化するなら正規形パーサの新設が筋
  2. **`.claude/skills/release/SKILL.md` の不整合** — `:17` に v1.8 で除去された表現「Phase 4 PR」が残存、`:13`・`:16` が改訂を「v1.6」と記載(正は v1.7)。**原典で確認済み**。4-5 では触らず `P4-後` へ申し送る

- **/plan**: 計画書 + 詳細設計を起票し、`codex_run.py review normal` で **4 周**回して収束(下記)。**人間承認 2026-08-21**

## 決定

### 計画レビュー 4 周の内容

| 周 | 指摘 | 主な反映 |
| --- | --- | --- |
| 1 | 否決(P0×1 / P1×7 / P2×2) | **候補 SHA の可変参照を禁止**(10.1 `:639`)/ **13 章の宿題への結論から JSON 出力を撤回**(記録項目 4 つを超えると規範追加)/ **(a) 完全性検査を予約へ適用しない**(適用すると規範どおりの予約 PR が恒久的に落ちる)/ `result` の列挙値検査 / 非 PR イベント経路 / **設計書 10.1 の CI 表への追随が必要**(「反映なし」は誤り)/ ステップ 5 の分割 |
| 2 | 否決(P0×3 / P1×2 / P2×2) | **`git cat-file -t` で生のオブジェクト種別を確認**(`^{commit}` は注釈付きタグを剥がすため 40 桁のタグ OID が通る)/ **採番・排他プロトコルの機械化**(e)を新設 / **`push` の事後検査を追加** / 未知キーの拒否・`started_at` の一致は検査しない / **非 `A` 状態をすべて拒否**(`T` = type change)/ 典拠行の訂正 |
| 3 | 否決(P0×2 / P1×3) | **(e) を 4 検査へ再設計し base のツリー基準へ**(統合後基準では 10.1 の順序規範と等価にならない)/ **`push` の事後検査を撤回** / 検査対象を **`develop` を base とする PR のみ**へ限定 |
| 4 | **条件付き可決**(P0・P1 なし・P2×1) | 件数表記の同期のみ |

### 方針転換 1 件(2 周目で入れて 3 周目で撤回)

**`push` イベントの事後検査**。2 周目に「非 PR イベントのスキップは検査の穴」(ブランチ保護が未適用のため直接 push が塞がれていない)と指摘されて追加したが、3 周目に「赤くしても統合は止まらない・統制が未定義」「`main` 宛 PR で正当なマージが落ちる」と指摘され、正本を読み直して撤回した。

**根拠**: 10.1 `:640`(残余リスク)が同じ問題を既に処理していた — 「ブランチ保護が未導入…なので**人間管理の排他区間**を置く」「**これは機構による強制ではなく運用規律であり、所有者・他メンバーの UI / API 操作を防げない残余リスクが残る**」「マージ後の第 1 親・第 2 親・ツリー検査は**事後検出**であり…手遅れになりうる」。**正本は事後検出を対策として位置づけておらず、この穴を残余リスクとして受容済み**だった。

**教訓**: レビューの指摘に反応して機構を足す前に、**正本が既にその穴をどう扱っているかを確認する**。2 周目の P0 は「穴がある」という事実としては正しかったが、「4-5 が塞ぐべき穴」ではなかった。

### H-68(閉じた輪)への対処

3 周目のレビューは**自身が 5 件中 4 件を「H-68 型(前の周の修正に対する指摘)」と自己申告**した。台帳 H-68 の実例(設計書 v1.7 の確定ゲート 12〜15 周目で P1 11 件中 11 件が直前の周への指摘)と同型なので、**4 周目のプロンプトで判定基準を事前固定**した — 「**新規の (A)(方式が分岐する)指摘が 1 件でもあれば否決 / (B) のみなら条件付き可決**」。これは台帳 H-68 の**対応案 (c) が「有効性を実測済み」としている手法**そのもの。結果、4 周目は P0・P1 が消えて収束した。

### 人間の裁定を受けた判断(5 件・2026-08-21 承認)

1. **CI に新ジョブ `nfr021-append-only` を追加**(既存ジョブへ相乗りしない・paths filter なし = フィルタ誤りで黙って通る経路を作らない)
2. **13 章の宿題は既存規定の適用で閉じる**(新しい媒体・永続キー・記録項目・出力契約を作らない → 設計書の規範改訂も確定ゲートも不要)
3. **統合時検査 (a) を予約へ適用しない**(README の解釈。適用すると予約 PR が恒久的に落ちる)
4. **採番・排他プロトコルを機械化する**((e-1)〜(e-4)。既存規範 10.1「試行の排他と採番」の機械化であり規範追加ではないと判断)
5. **`push` 経路の検査を実装しない**(10.1 `:640` が残余リスクとして受容済み)

### 塞がない残余リスク(記録)

- **直接 push・GitHub UI / API による `develop` の更新** — 10.1 `:640` が受容済み。対策は「人間管理の排他区間」(運用規律)
- **base が古い 2 つの PR が同じ `attempt_seq` を別々に通す競合** — (d) の一意性検査が後から入る側で落ちるが、**先に入る側は止められない**
- どちらも**ブランチ保護が適用可能になった時点で再検討**する。台帳へ follow-up として記録する(クローズ処理)

## 未決・次の一歩

- **次は /implement のステップ 1**(正規形パーサとスキーマ適合検査)。ステップ 1〜7 は Codex 委任(重さ分類 通常 → terra / max)、**ステップ 8 は `ci.yml` が `guard_paths` のため Claude が起草し人間の逐行確認を受ける**
- **各ステップで変異テストを PR 作成者が自分で再現する**(委任先の自己申告で代替しない — 台帳 H-49)
- **実装差分には `review normal` の後に `review adversarial` を 1 周追加する**(誤りの帰結が「ゲートの恒久閉塞」または「ゲートの迂回」であるため)
- **4-6 への申し送り**: `win-setup` のランナー選定 / 要件書 10 章の未決事項「NFR-021 の継続検証基盤」の決着記録が要件書に反映されていない / **ルートへの ruff・ty 導入(F4)は `/pyproject.toml` が失効対象パスなので証跡作成より前に済ませる**
- **`P4-後` への申し送り**: `.claude/skills/release/SKILL.md` の 2 件の不整合(「Phase 4 PR」の残存 / 改訂の版番号)
