---
date: 2026-08-31
topic: 文書検査機構の多文書対応(プロファイル化 + 不変条件 DSL)— TSK-250 の先行 A
branch: feature/doc-check-multi-doc
---

# 作業ログ: 2026-08-31 文書検査機構の多文書対応(プロファイル化 + 不変条件 DSL)

## やったこと

- /task-start: 既存の Notion タスク **TSK-269** を取得(TSK-250 の計画レビューで切り出し・起票済み —
  重複起票はしていない)・`feature/doc-check-multi-doc` の worktree 作成・計画書雛形と本ログの作成

- /investigate: 調査サブエージェント **3 本**(伝播検査の責務棚卸し / 帰属検査と CI 配線の棚卸し /
  検査機構の決定経緯)を並列で投げ、`docs/features/doc-check-multi-doc/research.md` へ統合した。
  **重い指摘は原典を Claude が直接確認**(research.md の ◎印)
- **基準線を自分で測定**: `uv run pytest tests/` = 784 passed / 3 検査すべて green /
  ruff・ty ともクリーン。これが「既存の振る舞いを変えない」ことの判定基準になる

## 決定

- 調査は 3 本(既定どおり)。要件突合(spec-checker)と旧システム(legacy-analyst)は本タスクの
  射程外なので使わず、**コードの棚卸し 2 本 + 決定経緯 1 本**に振り替えた

## 計画レビュー 1 周目(sol xhigh)= 否決 / P0 6 件・P1 7 件

**反映せずに寝かせる**(下記「保留」)。指摘は失わないよう要点を残す。

- **P0: fail-closed にすると現行の同期正本が実際に fail する** — `MT-01` の `scope` は
  `冒頭、1節、2-2、…`(`scripts/design_relations/defects.json:445`)だが、**approved の同期正本に
  `## 1.` は存在しない**。**いま green なのは `_heading_section` が空文字列を返すため**
  (`scripts/check_design_propagation.py:297`)。**ステップ 3 の共通条件「引数なしで 3 検査 green」と
  両立しない**。同期本文も oracle も変更禁止としているのでその場で直せない
  → **ステップ 3 より前に `MT-01` を裁定する**(「存在してはならない節」を別の意味型にするか、
  oracle 改訂を独立の承認済みステップにするか)
- **P0: 「機械 17 件の検出集合の完全一致」は移行の正しさを証明しない** — fixture は 17 欠陥を同時に
  含むため、**ルールを交換しても / 全ルールが同じ広い条件を検査しても / 17 ID を一律返しても**集合は一致する。
  既存テストで個別の正常・異常対を持つのは 8 件のみ(`tests/test_check_design_propagation.py:155`)
  → **欠陥別の characterization corpus を先に固定し、旧分岐と新宣言を shadow 実行して
  `(defect_id, check_id, violation 有無, reason 種別)` で差分比較**してから旧分岐を削除する
- **P0: 移行後の宣言を保存する資産が計画に無い** — `defects.json` の `invariant` は 4 項目しか
  受け取らず、かつ本計画は同ファイルを不変としている。**`invariants-sync-protocol.json` 等の
  文書固有ルール資産**を明示するか、oracle 改訂を独立ステップにする
- **P0: `profile-*.json` の命名が `profile-schema.json` と衝突する** — 列挙がスキーマを
  プロファイルとして読む。**専用ディレクトリか明示レジストリ**にする
- **P0: TSK-250 が固定した CLI(`--manifest` / `--defects-file`)を追加するステップが無い**。
  ステップ 2 は `--profile` しか足していない
- **P0: TSK-250 の 4 検査の入力契約がプロファイルに無い** — claims / WAIT / AUTH / FORB /
  baseline digest の資産パスも共通正規化 ID のスキーマも定義していない。
  **サンプルだけで満たせるのは受け渡し契約 4(ランナーが実プロファイル非依存)であって、
  契約 5(実入力の互換性)ではない**
- **P1: 「新規機構を発明しない」への非抵触は主張できない** — 型体系・JSON Schema・汎用評価器・
  合成規則・コンフォーマンスランナーはいずれも新規機構。**「発明していない」という主張を削除し、
  2026-08-31 の裁定を前決定の明示的な例外・上書きとして記録すべき**
- **P1: 1 PR に確定単位が 3 つある**(一般化 / 既存 16 分岐の全量移行 / TSK-250 用の新検査)。
  ステップ 13(`GLOBAL_CHECK_IDS` の整理)は受け渡しに不要な別リファクタ
- **P1: TSK-250 のステップ 22(明示引数の CI step 追加)と本計画の列挙方式が再衝突する**
  → 再開時は **TSK-250 側の再レビュー**(A のマージ時に予定済み)で「CI は引数なし列挙・明示引数は
  ローカル検証コマンドだけ」へ揃える
- **P1: 「784 件以上」は既存テストを削除しても達成できる** → **着手前の pytest node ID 集合を固定し、
  既存 node ID が全件残ることを検査する**へ改める

## 保留(2026-08-31)

**人間の判断により本タスクを保留し、TSK-270(認可構成の実機検証)を先行させる。**
理由は**製品コードの着手を最短にするため** — 先行 3 本のうち**スキーマ実装を実際にブロックするのは
TSK-270 だけ**で、本タスク(文書検査の機構)と TSK-271(88 列・移行)はブロックしない。

- **worktree とブランチは残す**(`feature/doc-check-multi-doc`)。計画書は**承認しない**まま
- 再開時は上記 P0 6 件・P1 7 件の反映から始める。**とくに `MT-01` の裁定は着手前に必要**
- **`research.md` の棚卸しは再開時にそのまま使える**(検査機構の実態・型の不足・基準線)

## 未決・次の一歩

- /investigate で下調べ。**本タスクは既存コードの一般化**なので、`scripts/check_design_propagation.py` と
  `scripts/check_doc_coverage.py` の**現在の責務の切れ目**を先に把握する必要がある
  (`if defect_id == "SP-01"` の手書き分岐が 432-573 行、`ASSIGNMENT_KINDS` と `LEDGER_SECTION_ID` の
  ハードコードが coverage 側)
- **受け渡し契約は TSK-269 の Notion 本文と TSK-250 の計画書 1 節が正**。とくに
  「**機械で保証すべき 4 検査**」(FORB の構造判定 / 直接要件の「対象外」禁止 / ベースライン digest 拘束 /
  WAIT・AUTH・FORB の交差検査)は、**人間の逐行確認へ回して契約を満たすことはできない**
- **既存の同期正本に対する検査結果が変わらないこと**が回帰の要。fixture と `defects.json` は
  同期側の oracle なので触らない

## 保留中の追記 — `codex_run.py` の見出しスコープ欠陥を本タスクへ回した(2026-08-31)

**人間の裁定 2026-08-31**: TSK-270 の実装委任で実地に踏んだ `codex_run.py:63-78`(`has_step_table`)の
欠陥を、**本タスク(TSK-269)へ回す**。調査メモ **1-5 節**に記録した。

- **症状**: `### 実装ステップ` の配下に `#### 第 1 群: …` のような**小見出しを置くとステップ表が
  見えなくなり、実装委任が拒否される**。`in_section = "実装ステップ" in line` が
  **見出しレベルを見ずに再計算される**ため
- **踏む条件は特殊ではない** — **計画を群に分割すると必ず踏む**。TSK-270 は敵対レビュー 3 周否決を受けて
  承認範囲を第 1 群に絞ったため分割が必然だった
- **1-4 節(`extract_scope` の黙殺)と同型**の欠陥。ただし**壊れ方の向きが逆**で、
  `has_step_table` は fail-closed(拒否して止まる)なので実害は手戻りに留まる。
  **根は同じ「文書構造の解釈が暗黙の前提に依存し、前提が外れたことを報告しない」**
- **TSK-270 側は暫定回避のみ**(小見出しに語「実装ステップ」を残す + 計画書へ機構要件の注記)。
  **回避であって修正ではない**
- **/plan で射程に含めるかを決める**。含めないなら**独立タスクとして起票する**(論点 5 に追加済み)

**本タスクは `ブロック中` のまま**。TSK-270 の第 1 群が終わってから再開する。
