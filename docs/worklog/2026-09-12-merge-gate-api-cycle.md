---
date: 2026-09-12
topic: マージゲートと API の循環を解く — ゲート解除の道筋を調べて選択肢を出す
branch: feature/merge-gate-api-cycle
---

# 作業ログ: 2026-09-12 マージゲートと API の循環を解く

## やったこと(ステップ 1〜3)

調査(`docs/features/merge-gate-api-cycle/research.md`)を踏まえ、
`design.md` に選択肢の比較・12-4 の条文判定・PO 裁定 3 件を置いた。

### 途中で前提が 2 つ崩れた

**1. Notion カードの「TSK-344 の取り下げ」はリポジトリにも Notion にも無い。**
ステータスは `未着手`(最終更新 2026-09-08)。DoD 6 項目は Notion 側に実在する。

**2. 選択肢 ⑩(予定済みの 12-4 改訂へ相乗り)の相乗り先が存在しない。**
`S-4` は「TSK-348 / TSK-353 へ申し送る」と書いているが、
**TSK-348 は完了済みで射程は `:240` の 1 行の是正だけ**、
**TSK-353 は `data-model.md` の本文を改訂せず検出機構を作るタスク**である。

### 「送り先が空手形」が本タスクで 2 件そろった

| # | 送ったもの | 送り先 | 実態 |
| --- | --- | --- | --- |
| 1 | **製品 HTTP 経路の実装** | TSK-317 のクローズ処理が「新規起票」へ | TSK-367 は「受取先を**決める**」タスクで、実装の所有者ではない |
| 2 | **12-4 冒頭の旧文言の現況化** | `S-4` が「TSK-348 / TSK-353」へ | **TSK-348 は完了・射程外 / TSK-353 は本文を触らない** |

運用評価台帳の既存候補「`H-68` 型で機械条件を先送りするとき、送り先が空手形になる」の
**昇格条件に該当する実測**である。ステップ 5 で追記する。

## PO 裁定の依頼(3 件 — 独立に否決可能)

**詳細と典拠は [`design.md` 4 節](../features/merge-gate-api-cycle/design.md) が正。**
本節は依頼の要旨だけを持つ。

| 裁定 | 問い | 推奨 | 反映先 | 裁定しないと何が止まるか |
| --- | --- | --- | --- | --- |
| **A** 適用単位 | 12-4 のゲートは**何を単位に**判定するか | **当該 PR が開く経路について**判定する。対象横断の網羅と「`NFR-010` 適合」の宣言はリリース候補時の受入で行う | `data-model.md` 12-4(条文化タスクへ) | **FR の実装 PR が 1 本も通せない** |
| **B** 測定経路 | 「アプリ経路だけの検証では通過にしない」を**どう充足**したことにするか | **HTTP 経路を開く PR は、その経路の直叩きテストを同一 PR に含める**。開かない PR には空の要求として掛かる | `data-model.md` 12-4(条文化タスクへ) | **A が通っても最初の API PR が通せない** |
| **C** 所有者 | **製品 HTTP 経路の実装を誰が持つか** | **各 FR の実装タスクが自経路の越境テストごと持つ**。TSK-217 は対象横断の判定へ残し、**前提「FR-041 の実装が先」を「FR-041 の実装 PR と同一変更」へ訂正** | **正本ではない** — Notion のタスク定義(設計書 7.4 の作業管理面。**確定ゲート不要**) | **A・B が通っても着手する人がいない** |

**裁定 A・B を採る場合は条文化の乗り物も選ぶ**(`design.md` 4-1 節):
**⑦ ADR-004 + 12-4 の追随を同一ゲート(推奨・軽量版可)** か **② 12-4 の単独改訂**。
**どちらでも `S-4` の現況化を同乗させる。**

### 律速は裁定ではない

**3 件すべてが通っても、通過条件①(RLS の DDL が実スキーマへ適用)が未充足なら
誰も通過できない**(`backend/migrations/` に `CREATE POLICY` / `CREATE ROLE` /
`ROW LEVEL SECURITY` は 0 件)。**①の産出者は TSK-317(進行中)。**
**裁定は①が充足した直後に詰まらないための先回りであり、今すぐ何かが動き出すわけではない。**

## 決定

### PO 裁定(2026-09-12・山田正輝)

**3 件とも推奨どおり。条文化の乗り物も決まった。**

| 裁定 | 決定 | 不採用案とその不採用理由 |
| --- | --- | --- |
| **A** 適用単位 | **経路単位** — 12-4 のゲートは**当該 PR が開く経路について**判定する。対象横断の `NFR-019(b)` 網羅と「`NFR-010` 適合」の宣言は**リリース候補時の受入**(要件書 `:957`)で行う | **「対象横断のまま」を不採用**。採ると最初の 1 本が永久に条件を満たせず、循環が解けない。別の手(例外規定・段階化)が必要になり、いずれも確定ゲートを伴う |
| **B** 測定経路 | **同一 PR に含める** — 「アプリ経路だけの検証では通過にしない」は、**HTTP 経路を開く PR がその経路の直叩きテストを同一 PR に含める**ことをもって充足とする。**HTTP 経路を開かない PR には空の要求として掛かる** | **「全経路の網羅を要求」を不採用**(`data-model.md:246` の読み)。採ると API が無い現在は通過できず、最初の 1 本を通すのに例外規定か段階化が別途必要になる |
| **C** 所有者 | **各 FR の実装タスク**が自経路の HTTP 越境テストごと持つ。**TSK-217 は対象横断の判定とハーネスの所有者として残し**、前提「FR-041 の実装が先。実装がないと構成できない」を**「FR-041 の実装 PR と同一変更」へ訂正**する | **「単一の API 実装タスク」を不採用**。契約の一貫性は 1 タスクで担保できるが、**そのタスク自身が最初のゲート通過の当事者になり、A・B が否決されると詰まる**。今回は A・B が通ったので致命ではないが、**per-PR の裁定 A と揃う方が構造が単純**である |
| **条文化の乗り物** | **⑦ ADR-004 の新設 + 12-4 の追随を同一の確定ゲートで通す** | **② 12-4 の単独改訂を不採用**。確定ゲートの回数は同じ 1 回だが、**ADR は 7.2 の表で「必須(軽量版可)」**なのでレビュー面が小さい。加えて**裁定の理由が ADR という恒久記録に残る**(単独改訂だと変更履歴にしか残らない)。先例は ADR-003 の「本 ADR と**同一の確定ゲート**で要件書 v2.2 として改訂する」 |

**`S-4` の「12-4 節冒頭の旧文言の現況化」を同乗させる**(⑩ の却下で受け取り手が無いことが
判明したため)。

### 裁定の射程 — 今すぐ何かが動き出すわけではない

**通過条件①(RLS のポリシーとロールの DDL が実スキーマへ適用)は未充足**であり、
**その産出者は TSK-317(進行中)**である。**律速は裁定ではなく TSK-317。**
本裁定は**①が充足した直後に詰まらないための先回り**である。

## 未決・次の一歩

- ~~PO 裁定 A / B / C~~ → **2026-09-12 に取得済み**(上記「決定」)
- ~~台帳への実測追記~~ → 完了(ステップ 5)
- ~~後続タスクの起票~~ → 完了(ステップ 6)。**TSK-379 / TSK-380 / TSK-381** を新規起票し、
  **TSK-367 と TSK-346** へはコメントで引き渡した(**二重起票なし**)
- クローズ処理(ステップ 7)

### 引き渡しの一覧

| 先 | 内容 |
| --- | --- |
| **TSK-379**(新規) | ADR-004 + 12-4 の追随改訂を同一ゲートで。`S-4` の現況化を同乗 |
| **TSK-380**(新規) | TSK-217 の前提の訂正(**射程は変えない**) |
| **TSK-381**(新規) | TSK-344 の DoD 4・6 の見直し |
| **TSK-367**(既存) | 裁定 C = 本カードの候補 3。**射程は変えず方向だけ確定**。二重化を避けるためコメントで引き渡し |
| **TSK-346**(既存) | 入力 3 件(12-4 の解釈は書かなくてよい / 37 経路と矛盾できない / 論点 18 の引き取り) |

**製品 HTTP 経路の実装は単独タスクとして起票しない** — 裁定 C により各 FR の実装タスクが持つ。

## 結果サマリ

**循環を解く道筋が決まった。ただし本 PR はゲートを解除していない。**

裁定 A(経路単位)と B(同一 PR に含める)を採れば、**最初の API PR は自分の越境テストを
同じ変更として持つのでマージ時点でその経路は green** になり、12-4 の「越境テストが green に
なるまでマージしない」に抵触しない。**例外規定は要らない。**

**ただしこれは条文化されて初めて効く。** 当初この節は「循環は解けた」「即時解除」と書いていたが、
**誤りだった**(敵対レビュー 1 周目 `P0`)。`design.md` 3-3 節自身が「**条文だけでは決まらない**」と
判定している以上、裁定 A・B は**現行条文の読みではなく新しい規範**である。
**`docs/features/` は正本ではなく**(設計書 7.2)、**ゲート定義の正は `data-model.md` 12-4**
(`:2748`)なので、**`TSK-379` が確定ゲートを通ってマージされるまで現行の 12-4 が効く。**

**本 PR が達成したのは「裁定を取ったこと」と「条文化の受け皿を作ったこと」である。**

### 何を作ったか

| 成果物 | 中身 |
| --- | --- |
| `research.md` | 18 節。調査サブエージェント 3 本 + Notion 精査の統合。**すべての事実に典拠** |
| `design.md` | 選択肢 10 案の比較(確定ゲート数の数え方の定義つき)/ 12-4 の条文判定 / PO 裁定 3 件 / 起票の一覧 |
| 台帳 | 既存候補 2 件へ実測追記 + 反証 1 件。**`H-*` の新規採番なし・版上げなし** |

### 正本への反映

| 正本 | 反映 |
| --- | --- |
| `docs/development/harness-evaluation.md` | 既存候補 2 件へ実測 + 反証 1 件 + 変更履歴 1 行 |
| `docs/README.md` | 台帳行の要約を現行化 |
| `docs/design/data-model.md` | **反映なし**(決定 `E-1` — 条文化は TSK-379 へ) |
| 要件書 / `sync-protocol.md` | **反映なし** |

### ハーネス運用評価台帳への追記の判断

**該当した。** 既存候補「`H-68` 型で機械条件を先送りするとき、送り先が空手形になる」の
**昇格条件が成立**(別タスクで実害 2 件)。**`H-*` の採番は PO 判断事項として上げた**
(自分で昇格させない)。反証 1 件(`data-model.md:2793` の「上表に受け取り先が無い範囲は
存在しない」が不成立)も記録した。

### 途中で崩れた前提

| # | Notion カードの記述 | 実測 |
| --- | --- | --- |
| 1 | 「TSK-344 の**取り下げ**」 | **リポジトリにも Notion にも無い。** ステータスは `未着手` |
| 2 | 選択肢 ③「TSK-217 を先に立てる」 | **不可。** TSK-217 の前提が FR-041 の実装 |
| 3 | (計画段階の私の推奨)⑥「TSK-346 に畳む」 | **却下。** 12-8 が 12-4 を `data-model.md` の確定範囲と宣言しており射程侵犯 |
| 4 | (計画段階の私の推奨)⑩「予定済みの改訂へ相乗り」 | **却下。** 相乗り先が存在しない(TSK-348 は完了・射程外 / TSK-353 は本文を触らない) |

**③④ は原典の確認だけで消えた。** 台帳の既存候補「順序の付記に理由を書かないと循環を作る」の
教訓「まず各辺の典拠を確認する」がそのまま効いた。

### 残っていること

**次の 1 手は `TSK-379`(ADR-004 + 12-4 の追随改訂)。** これが済むまで裁定は効かない。

**律速は本タスクではない。** 12-4 の通過条件①(RLS のポリシーとロールの DDL が実スキーマへ
適用)は未充足で、**産出者は TSK-317(進行中)**である。裁定は①が充足した直後に詰まらない
ための先回りであり、**今すぐ何かが動き出すわけではない。**

## 敵対レビュー(2026-09-12・sol xhigh)— 判定 P0 4 / P1 5 / P2 1・否決

**10 件中 9 件が妥当。全件を反映した。**

| # | 指摘 | 反映 |
| --- | --- | --- |
| **P0-1** | 裁定 A が `NFR-019` の「PR ごと全グリーンでないとマージ不可」(要件書 `:920`)と矛盾。`TSK-379` は要件書を対象にしないので後続でも解消されない | **裁定 A の反映先に「要件書 `NFR-019` との整合」を追加**し、`TSK-379` の射程に含めた |
| **P0-2** | 正本未改訂の裁定を「即時解除」「循環は解けた」として運用可能にしている | **撤回した。** `design.md` に 2-1 節「裁定はいつから効くか」を新設し、**`TSK-379` の確定まで現行 12-4 が効く**と明記 |
| **P0-3** | ゲート数の数え方が**一括検証の規則**(設計書 `:437`「複数正本の一括検証は単一のゲートとして扱い」)に反する。⑦ のレビュー面が② より小さいも不成立 | **数え方の柱書に `:437` を追加**し、③ を 2 → 1 ゲートへ訂正。**⑦ を採る理由を「レビュー面」から「理由の残り方」へ書き換えた** |
| **P0-4** | `research.md` の「不明として残るもの」が 16 節で解決済みの項目を再び不明としている | **未解決表を現行化**し、解決済み 3 件を明示。4 節の「リポジトリに出現しない」にも注記を付けた |
| **P1-5** | 裁定 B の読み①を `:161-163` は支持していない | **「性質」行を足し、現行条文の読みではなく条文化を要する新しい適用規則**と明記。対立案の `:246` が**字面に忠実**であることも書いた |
| **P1-6** | 案③の却下根拠が原典から出ない | **「正面から反する」を撤回**。不採用の理由を 3 点(文案が無い / 波及面が広い / ⑦ で足りる)へ差し替え |
| **P1-7** | 裁定 C は所有者変更であって前提訂正ではない。`http-route-matrix.json` が 37 経路の `test_owner` を TSK-217 に固定している | **「これは所有者変更である」行を新設**。`TSK-380` の射程に**契約資産の再割り当て方式の決定**を追加 |
| **P1-8** | 台帳の「実害 2 件」のうち 1 件目が昇格条件を満たさない | **1 件へ訂正**。過大だった理由(TSK-367 は実在し `着手可`・実装 PR が止まった事実は無い)も台帳に記録 |
| **P1-9** | `:2793` への反証が射程を取り違え | **反証を撤回した**(12-8 は data-model.md 自身の射程の閉包であって、全製品タスクの所有者一覧ではない) |
| **P2-10** | 見出し「却下の根拠(4 件)」に 5 件 | 「却下・不採用の根拠(5 件)」へ |

**裁定 A・B・C 自体は否定されていない。** 否定されたのは「**それが今すぐ効く**」という書き方と、
**判断材料の 3 箇所の誤り**(ゲート数・案③の却下根拠・台帳の実害)である。

**`P0-3` は私の見落としだった** — 設計書 `:437` の「複数正本の一括検証は単一のゲート」は
本タスクの調査中に読んでいながら、ゲート数の数え方に反映していなかった。
