---
date: 2026-09-07
topic: 同期プロトコル — サーバー適用の処理段階と原子性(後続 γ)
branch: feature/sync-server-apply
---

# 作業ログ: 2026-09-07 同期プロトコル — サーバー適用の処理段階と原子性(後続 γ)

## やったこと

### /task-start

- Notion タスク **TSK-321**(TSK-280 の /pr クローズ処理で 2026-09-04 に起票済み)を取得。**重複起票はしていない**
- worktree `../pitchlog-worktrees/feature-sync-server-apply`(ブランチ `feature/sync-server-apply`・
  起点 `origin/develop` = `56e4180`)を作成
- 計画書雛形 `docs/features/sync-server-apply/plan.md` を作成(重さ分類 = **コア領域**)

### /investigate

調査サブエージェント **5 本を並列**で投げた(コア領域・射程裁定が絡むため既定 3 から増やした):
spec-checker(要件突合)/ decision-tracer(決定経緯)/ Explore ×2(正本 6・8 章の精読 / 実装資産の現況)/ legacy-analyst(旧システムの制約)。
結果を `docs/features/sync-server-apply/research.md` に統合した。

**裁定した矛盾**: `B3a` の除外理由が 4 典拠で 2 系統に割れていた(計画書「6 章の処理段階に依存」vs
allow-list「P5・T9 処理に依存」)。**正本 `:1248`(`B3a` = `P5 + T9`)と `:1201`(`T9` = 保存を伴う原子境界)で裁定**し、
**`B3a` は 6 章の段階列挙だけでは閉じない**と確定した。`DI1`・`DI5`・`I1` は 4 典拠すべてが一致する。

**最大の発見**: **「スキーマ」の語が 2 種を指していた。** TSK-250 が決めるのは
**DB スキーマ**(列名・型・NULL 性・FK・索引・表分割 — `:1774`)、後続 β が γ へ送ったのは
**wire / JSON / API スキーマ**(`sync-ack-contract/plan.md:246`)。カード本文の「スキーマ不要で先行できる」は
**DB スキーマ**を指すと読むのが正本の用語法と整合する。

### /plan(計画書の作成と計画レビュー 1 周目)

`docs/features/sync-server-apply/plan.md`(6 節すべて記入)と、密度の高い機構設計を
`docs/features/sync-server-apply/design.md` へ分離して起票。

**計画レビュー 1 周目**(`codex_run.py review adversarial` = `sol xhigh`・2026-09-07):
**否決(P0 5 件・P1 5 件・P2 0 件)。全 10 件を採用**した。

| # | 指摘(要旨) | 採否 | 反映内容 |
| --- | --- | --- | --- |
| **P0-1** | `applicationPath` 省略時に期待フィールドを基底 5 件へ減らす設計は**正本にない例外** | **採用** | 10-3 `:1691` は「**各 JSON は**…prefix・べき等結果・一時 ID 写像・記録権…を表す」で、条件付きの部分は**加算**だった。**集合を共通 9 件(無条件)にし、省略可否だけを条件付き**へ改めた。既存 4 資産は `_v2` へ原子移行(人間の裁定 2026-09-07) |
| **P0-2** | P5 の条件付き契約が分岐名だけで判定証拠を必須化していない | **採用** | `P5分岐` を **discriminated union** にし、`B3a` = 未使用 D5 + 拒否原本、`B3b` = 不変な先着原本 + 同一 D5 異内容の後着を exact-key 必須に |
| **P0-3** | 2 資産では正本が明示する必須シナリオ集合を満たせない | **採用** | **未作成資産の繰り延べ表(exact-set)**を新設。復元系 8 シナリオ・`p3-invalidation-consumed-before-complete`・`P1`〜`P5` の故障行列を ID 単位で列挙し **TSK-330** を名指し |
| **P0-4** | ステップ 7 はステップ 8 の成果物なしに合格条件を成立させられない | **採用** | 繰り延べ表・資産・scenarioId を**ステップ 7 で原子的に追加**する形へ再構成(8 → 7 ステップ) |
| **P0-5** | `DI5`/`B3` の新規決定器が既存 D5 分類器とコピー実装になる(**`NFR-018` = P0**) | **採用** | D5 分類を **`decideIdempotencyCollision` の一系統に限定**。新モジュールは**分類済み結果の D1 順停止だけ**を担う。**分類を自前で持たないことを機械的に確認**する合格条件を追加 |
| **P1-1** | `B3a` を実装済みへ移す境界が正本の安定 ID と一致しない | **採用** | **`B3a` は射程外に残す**(正本の帰結は不可分な `P5 + T9`)。移動は 8 ID → **7 ID** へ。理由を **TSK-330** 名指しへ |
| **P1-2** | 注入点の語彙化に実行時検査がなく任意文字列が green のまま残る | **採用** | **18 組の `(経路, T 要素)` と P3 の 3 注入点を閉じ**、値と組合せを実行時検査。18 組はステップ 1 の reader から導出し**二重定義しない** |
| **P1-3** | コア領域登録が製品 4 本だけで spec を漏らしている | **採用** | 現行 `core-areas.json` は製品と spec を**個別登録**していた。**新規 3 モジュール + 各 spec = 6 パス**を登録する形へ |
| **P1-4** | wire スキーマが名前のない「別タスク」へ再送されている | **採用** | **TSK-331 を起票**し相互リンク。DoD に加えた。あわせて **TSK-330(γ')も起票** |
| **P1-5** | 6-2 の順序が正本ドリフトを検出できず「実装済み」移動と釣り合わない | **採用** | レビュアが提示した**第 3 案(スナップショット・ドリフト検査)**を採用(人間の裁定)。正本 6-2 の 2 表を実際に読んで逐語照合する。**正本を 1 行も改訂しない**ので確定ゲートを回さずに済む |

**起票した受け取り先 2 件**:

- **TSK-330** — 8 章の原子性の実行検証と backend 適用の実装(後続 γ')。開始条件は TSK-250 の完了
- **TSK-331** — wire 形式・プロパティ名・JSON / API スキーマの確定(後続 β の宿題)

**副産物**: 本レビューは**バックグラウンド実行で完走した(exit 0)**。TSK-322 で 23 回連続して
強制終了していた経路であり、`~/.claude/settings.json` の `env` ブロックに置いた
`CLAUDE_CODE_DISABLE_BG_SHELL_PRESSURE_REAP=1` が **claude 自身のプロセス環境にも届いていて、
再起動なしで既に効いている**ことが実測で確定した
(`BASH_MAX_TIMEOUT_MS` は起動時に固定されるため、このセッションでは未適用のまま)。

### 計画レビュー 2 周目

**否決(P0 5 件・P1 2 件・P2 1 件)。全 8 件を採用**した。

| # | 指摘(要旨) | 採否 | 反映内容 |
| --- | --- | --- | --- |
| **P0-1** | 非後方互換な契約変更なのに `schemaVersion` の繰り上げがない | **採用** | 必須フィールドを 5 → 9 に増やすと旧構造は新 validator を通らない = 非後方互換。10-3 `:1689` に従い**全資産を `schemaVersion: 2`** にし、**validator が構造と `schemaVersion` の組を照合**する |
| **P0-2** | 既存 D5 分類器の判定不能時が**正本 4-5 と乖離**している | **採用** | 正本 `:404` は「**判定不能は…D1 付き経路は B3b、D1 を持たない変更イベントは B13 を返す**」。現行実装は例外・`INDETERMINATE` を `REJECT_LATER`(第 3 の値)にしていた。**このまま `B3b` を実装済みへ移すと非正本の結果を封印する**。**人間の裁定で既存実装を正本へ是正**する |
| **P0-3** | `batchStopBoundary.ts` の入力だけでは `DI5` を決定できない | **採用** | `NOT_DUPLICATE` の A5 は後段(⑤記録権 → `B4` / ⑥連番 → `B2` / ⑦内容 → `B3a`)で分かれる。**人間の裁定で後段候補を遅延評価のコールバックで注入**し、**gap 後は呼ばれないこと**を保証する |
| **P0-4** | P5 の discriminated union が**分岐証拠の値関係**を検査しない | **採用** | `B3b` は先着原本が初期永続状態と一致・先着と後着の D5 が同一・内容が異なることを、`B3a` は対象 D5 が初期べき等集合に存在しないことを**実行時検査**する |
| **P0-5** | 未作成資産の繰り延べ表が **stable ID の exact-set になっていない** | **採用** | 「復元系 8 件」「P1〜P5」というカテゴリしか列挙しておらず母集合が無かった。**正本必須資産 ID の母集合**(10-2 の故障系シナリオ表 + 10-3 が名指しする ID)を **stable な小文字 scenarioId で全件定義**し、`作成済み ∪ 未作成繰り延べ = 母集合` を独立した exact-set にした |
| **P1-1** | 正本ドリフト検査が**正本だけの変更で CI 起動しない** | **採用** | `ci.yml:124-128` の `frontend-changes` フィルタは `frontend/**` のみ。一方 harness ジョブは「paths filter は付けない」と明記(`:88`)。**人間の裁定で 2 層構造**へ — harness の Python が正本↔スナップショット、Vitest がスナップショット↔TS |
| **P1-2** | 共通 9 件と「既存 runner は変更不要」が両立しない | **採用** | `validateFailureScenarioResult` も同じ `validateObservations` を使うため、5 キーしか返さない既存 runner が必ず red になる。**契約検査と結果検査を分離**し、結果側は契約が `omittedBecause` としたキーの不在だけを許可する |
| **P2** | `_v2` 原子移行の結論は成立するが**説明している不変条件が逆** | **採用** | 正しくは「**旧版参照が存在する間に旧資産を削除しない**」。手順を「新版追加 → 旧資産を残したまま全参照移行 → 旧版削除」と明記した |

**人間の裁定 2 件(2026-09-07)**:

6. **`DI5`・`B3b` は両方やる** — 既存分類器を正本 4-5 へ是正し、後段候補をコールバックで注入する
7. **ドリフト検査は 2 層**(harness の Python が正本↔スナップショット / Vitest がスナップショット↔TS)。
   `ci.yml` と `test_ci_wiring.py` はいずれも `guard_paths` なので触らない

**構成の変化**: 7 → **8 ステップ**。移動する ID は 7 件のまま(`B3a` は射程外に残す)。
**シナリオ資産は 2 件 → 9 件**へ(墓標・改訂 + `P1`〜`P5`・`D1` 混在バッチ・gap 後 `B3b`)。
**既存のコア領域コード(`idempotencyCollision.ts`)の振る舞いを変更する**ため、
逐行確認の最優先をステップ 4 に置いた。

計画レビュー周回: 1 → 2

### 計画レビュー 3 周目

**否決(P0 4 件・P1 4 件・P2 0 件)。7 件を採用・1 件を理由つきで不採用。**

| # | 指摘(要旨) | 採否 | 反映内容 |
| --- | --- | --- | --- |
| **P0-1** | `DI5` の入力に現在の `D3` がなく gap 境界を決定できない | **採用**(P0-2 の裁定に吸収) | `DI5` 自体を射程外へ戻したため解消 |
| **P0-2** | **`batchStopBoundary` の返す境界結果が `U-14` で決定不能** | **採用** | **裁定 3(`U-14` は射程外)と裁定 6(`DI5` をやる)が衝突していた**。A5 は外部境界結果を返す必要があり、そのとき保存済み退避の再掲を `B1`/`B4` のどちらにするかを決めねばならず、それが `U-14`。**人間の裁定 8 で `DI5` を射程外へ戻し、`batchStopBoundary.ts` を作らないことにした**(裁定 6 の部分撤回)。移動 ID は 7 → **6** |
| **P0-3** | 単一の `P5分岐` union と 1 資産では `B3a`・`B3b` の両方を固定できない | **採用** | **`p5-b3a-crash-boundaries` と `p5-b3b-unreached-t9` の 2 資産へ分割**。母集合は 23 → **24 ID**、γ が作る資産は 9 → **10 件** |
| **P0-4** | **故障系契約が T 要素の全確定/全非確定を比較面に含めていない** | **採用** | 正本 10-3 `:1694` の要求。**1 周目の計画にはあったが 2 周目の書き直しで落としていた退行**。期待フィールドに **`tElementCommitment`** を足して 9 → **10 件**にし、T 集合は経路 reader から導出、**部分確定が red** になることを合格条件にした |
| **P1-1** | 2 層ドリフト検査は正本とスナップショットを同時変更するとすり抜ける | **採用** | スナップショットが `scripts/` 配下だと `frontend/**` フィルタに当たらず Vitest が skip される。**人間の裁定 9 でスナップショットを `frontend/src/lib/sync/` 配下へ**置き、既存フィルタで起動させる。`ci.yml`・`test_ci_wiring.py`(`guard_paths`)を触らずに穴が閉じる |
| **P1-2** | 複数一致の扱いを実装時判断へ残すと結果語彙が閉じない | **採用** | **「一意な先着原本が無い破損」として内部エラーに固定**し、`B3b`/`B13` へ混ぜないことを計画段階で確定した |
| **P1-3** | 復元系の写像は現在の exact-set では正本行の被覆を証明できない | **採用** | `作成済み ∪ 繰り延べ = 母集合` は **ID の存在しか見ない**。**10-2 の観点にも stable key を与え、`観点集合 = 各 scenarioId の covers の和集合`** を 4 つ目の exact-set として追加した |
| **P1-4** | 台帳と索引が実装ステップに割り当てられていない | **不採用** | **ハーネスの読み違い**。台帳と索引の更新は **`/pr` のクローズ処理**が正規経路(pr スキル手順 1-3 — ①計画書 3 節の宣言 → ②台帳 → ③変更履歴 → ④索引を同一コミット)であり、**クローズ処理はステップ表の外側**にある(設計書 6.1)。ただし誤解を招いたので**計画書 3 節と design.md 7 章に明記**した |

**人間の裁定 2 件(2026-09-07)**:

8. **`DI5` を射程外へ戻す** — 外部境界結果には `U-14` の選択規則が要る。`batchStopBoundary.ts` は作らない
9. **スナップショットは `frontend/` 配下** — 既存の `frontend/**` フィルタで Vitest を起動させる

**構成の変化**: ステップ数は 8 のまま。**新規モジュールは 3 → 2**(`batchStopBoundary.ts` を削除)。
**移動する ID は 7 → 6**。**期待フィールドは 9 → 10**。**シナリオ資産は 9 → 10**。**母集合は 24 ID**。
**exact-set は 3 → 4**(観点被覆を追加)。

計画レビュー周回: 2 → 3

### 計画レビュー 4 周目

**否決(P0 0 件・P1 3 件・P2 1 件)。P0 がゼロになった。全 4 件を採用。**
すべて「実装前必須」3 件 +「実装中で可」1 件。

| # | 指摘(要旨) | 採否 | 反映内容 |
| --- | --- | --- | --- |
| **P1-1** | `tElementCommitment` の期待値と実測値の形が未定義で部分確定を確実に検出できない | **採用** | 単一の集約値にすると runner が T 要素ごとの実状態を示さず**部分確定を隠せる**。**資産側 `{ mode: "allCommitted" \| "allNotCommitted" }` / 結果側 T ID を exact-key とする状態 record** に分け、キー集合を reader 由来の T 集合と照合。**部分確定変異 = 結果 record の 1 要素だけ反転** |
| **P1-2** | 観点被覆の**和集合検査**だけでは誤写像と重複を検出できない | **採用** | 和集合だと ① 両方から同じ観点を落とすと green ② 別シナリオへ付け替えても不変 ③ 重複被覆で 1 辺消しても red にならない。**`scenarioId × 観点key` のペア集合そのものを exact-set 照合**へ。繰り延べ表には**交差が空**も要求。あわせて **10-2 の `(d)` 観点は 22 行**(残り 2 行は `(c)` の E2E)と確定し、**8 ID で覆えない 2 観点に ID を追加**して母集合 24 → **26** |
| **P1-3** | 閉じた 18 組は**許可語彙**しか保証せず、資産が全クラッシュ境界を覆う保証がない | **採用** | 各資産が有効な組を 1 件選ぶだけで通ってしまう。**故障注入を複数 `case` 構造**にし、**`⋃ 宣言 case = reader の 18 組`** と **`⋃ トランザクション外 = P3 の 3 点`** を exact-set 照合。`p1`〜`p4` と `p5-b3a` で 18 組を覆い切る |
| **P2** | P1-4 の不採用は妥当だが「同一コミット」の範囲が pr 手順とずれている | **採用** | **不採用の判断自体は妥当と認められた**。同一コミットの対象は**②台帳・③変更履歴・④索引**で、**①計画書への宣言はその前**。文言を是正 |

**構成の変化**: ステップ数 8 のまま。**母集合 24 → 26 ID**(`restore-all-write-paths-blocked`・
`restore-release-new-generation-boundary` を追加)。**exact-set 4 → 5**(注入被覆 2 種を追加、観点被覆をペア集合化)。
**観点 key を 22 種で確定**し、**26 ペアを列挙**した。

計画レビュー周回: 3 → 4

### 計画レビュー 5 周目 と 打ち切り

**否決(P0 1 件・P1 3 件・P2 1 件)。**

| # | 指摘(要旨) | 採否 |
| --- | --- | --- |
| **P0** | `_v2` 移行後に同じ版の `faultInjection` を破壊的変更する手順が版付け規則に反する | **TSK-332 へ送付**(**4 周目の私の修正が作った順序バグ** — ステップ 5 で `_v2` を作った後、ステップ 6 で配列化していた) |
| **P1** | `p5-b3b-unreached-t9` が「`T9` 未到達」を契約上表現できない | **TSK-332 へ送付** |
| **P1** | 複数 `case` と期待観測点の対応がなく宣言だけで境界被覆を満たせる | **TSK-332 へ送付** |
| **P1** | 2 つの注入被覆が和集合のままで重複と誤写像を検出できない | **TSK-332 へ送付** |
| **P2** | exact-set の本数と設計記述に残存不整合がある | **一部反映**(縮小後の計画で件数を整理)・残りは TSK-332 |

#### 打ち切りの判断(人間の裁定 10)

**指摘がどのステップに当たっているか**を集計したところ、**4 周目以降は全件が故障系契約(旧ステップ 5〜8)**で、
**オラクルと処理段階(旧ステップ 1〜4)は 2 周連続で 0 件**だった。

| 周 | 旧ステップ 1〜4 | 旧ステップ 5〜8 |
| --- | ---: | ---: |
| 1 | 2 | 8 |
| 2 | 3 | 5 |
| 3 | 4 | 4 |
| **4** | **0** | **4** |
| **5** | **0** | **5** |

さらに **4・5 周目の P0 はいずれも前周の私の修正が作った問題**であり、
故障系契約の詳細設計(`caseId`・到達不能の判別子・被覆のペア集合化)は
**実装しながら詰める方が精度が上がる領域**に入っていた。

→ **人間の裁定 10 で射程を分割**。**[TSK-332](https://app.notion.com/p/3d493b75e687816eb9fbd19dda889d5e)**
(NFR-019(d) 故障系テスト資産契約の再設計とシナリオ資産の作成)を起票し、旧ステップ 5〜8 を送った。

**本タスクは 4 ステップに縮小**:

1. `R-TXN-ROUTE` の reader(**TSK-332 が使う 18 組の唯一の出所**)
2. 6-2 の処理段階 + 2 層の正本ドリフト検査 + `DI1`・`DI4`・`I1`・`I4` を実装済みへ
3. `RG1` 共通前段ゲート + `RG1` を実装済みへ
4. **既存 D5 分類器を正本 4-5 へ是正** + `B3b` を実装済みへ

**5 周分のレビュー知見は失っていない** — `design.md` の 3 章・6 章に保存し、
**射程外の印と 5 周目の未反映指摘 4 件**を冒頭の表に明記した。TSK-332 はそこから始められる。

計画レビュー周回: 4 → 5

#### 起票した受け取り先(計 3 件)

| タスク | 内容 | 開始条件 |
| --- | --- | --- |
| **TSK-330** | 8 章の原子性の実行検証と backend 適用の実装(後続 γ') | **TSK-250 の完了** |
| **TSK-331** | wire 形式・プロパティ名・JSON / API スキーマの確定 | なし |
| **TSK-332** | `NFR-019(d)` 故障系テスト資産契約の再設計とシナリオ資産 10 件 | **TSK-321 の完了**(`R-TXN-ROUTE` reader を使う) |

### 計画レビュー 6 周目(射程縮小直後の全文レビュー)

**否決(P0 0 件・P1 2 件・P2 2 件)。全 4 件を採用。**

| # | 指摘(要旨) | 採否 | 反映内容 |
| --- | --- | --- | --- |
| **P1-1** | **射程分割が research.md と design.md の申し送り本文へ反映されていない** | **採用** | plan.md だけ直して他を放置していた。research.md に**現行の分担表**を追記し**失効した記述を明示**、design.md の「γ ステップ 7/8」「γ が作る」を **TSK-332 の計画対象**へ置換(10 箇所)、見出しの「母集合 24」を **26** へ是正。plan.md の順序根拠も「ステップ 2 以降が reader を使う」→「**使うのは TSK-332 であり、2〜4 は相互に独立**」へ |
| **P1-2** | **複数一致の機械条件が現行の `REJECT_LATER` をそのまま通す** | **採用** | 「`B3b`/`B13` に混ざらないことの検査」は、**現行実装が返す `REJECT_LATER` が既にどちらでもない**ため**コードを 1 行も変えなくても green** になる。**専用例外の送出へ一意化**し(`IdempotencyDecisionResult` では表現できなくする)、**2 件以上の一致で当該例外が送出されることを厳密に検査**、**判定器が呼ばれないことも固定**する形へ |
| **P2-1** | 新規資産のコア領域**重複帰属**が登録計画に落ちていない | **採用** | `sync-protocol` だけだとコアレビューは発火するが機械可読 paths 上の帰属が漏れる。**`processingStages` 系 3 件を `tenant-isolation`・`recording-rights` にも、`restoreAdjustmentGate` 系 2 件を `recording-rights` にも**登録し、`test_core_guard.py` の各 area 期待集合も追随させる |
| **P2-2** | **CI フィルタについての事実認識が現行設定とずれている** | **採用** | **私の事実誤認だった**。`ci.yml:124-130` の `frontend-changes` は `frontend/**` だけでなく **`contracts/**` と `scripts/design_relations/sync-protocol.json` も列挙**している(最初の grep がその行にマッチせず見落とした)。台帳の候補②を「**個別列挙方式なので、正本由来の橋渡し資産を新設するたびに `ci.yml`〔`guard_paths`〕の更新が要り、漏らすと黙ってすり抜ける**」へ事実修正した。**スナップショットを `frontend/` 配下へ置く判断(裁定 9)自体は有効**とレビュアも認めている |

**セッション中断**: 本周の反映作業中に Anthropic 側の API エラー
(`Server error mid-response`)でセッションが 1 度切れたが、**作業内容の損失はなかった**
(未コミットの編集が worktree に残っていたので、そこから再開した)。

計画レビュー周回: 5 → 6

### 計画レビュー 7 周目

**否決(P0 0 件・P1 2 件・P2 2 件)。全 4 件を採用。**
**6 周目とまったく同じ失敗型** — **反映が主要箇所だけで、波及先が漏れる**。

| # | 指摘(要旨) | 採否 | 反映内容 |
| --- | --- | --- | --- |
| **P1-1** | **`research.md` の失効指定が不足**し、旧射程の結論と現行分担が矛盾している。「本書の裁定 5」は同書の裁定表に存在しない | **採用** | `research.md` の裁定表は**着手時の 3 件(1・2・3)**だけで、私が書いた「裁定 1・5」の 5 は **`plan.md` 側の番号**だった。**失効表を全面改訂**し、①「射程の選択肢」②裁定 1 と派生 5 項目 ③裁定 2 の検査方式 ④裁定 3 の見立て ⑤「TSK-250 後の別タスク(γ')」→ TSK-330 ⑥本文の γ 表記 を**箇所ごとに列挙**。さらに**本文 7 箇所へ個別の失効印**を入れた |
| **P1-2** | **`design.md` の申し送りに旧 γ 責務が残り**、第 1・第 2 群も確定済みの段階に見える | **採用** | `:222`・`:400`・`:517`・`:520` の γ を **TSK-332 / TSK-330 / TSK-321** へ置換。**「第 1 群 / 第 2 群」は旧ステップ 7/8 由来の暫定分類**で、**実装順序・コミット単位・依存関係は TSK-332 の計画で確定する**と明記 |
| **P2-1** | コア領域の追加帰属は過剰ではないが、**テナント分離への帰属がまだ不足** | **採用** | `RG1` が「③認可後」を強制するので **`restoreAdjustmentGate` 系 2 件**を、その右辺を実装済みとして読むので **`canonOracle` 系 2 件**を `tenant-isolation` へ追加。**`tenant-isolation` へ 7 件・`recording-rights` へ 5 件**に |
| **P2-2** | CI フィルタの事実訂正が **`design.md` と記録文へ波及せず**、候補文も**条件を過度に一般化** | **採用** | `design.md` 5-1 の記述を**現行の 6 対象の列挙**へ書き換え。台帳の候補②も「**`frontend/**` や `contracts/**` 配下に置く限りフィルタ更新は不要で、それ以外の新しいファイル名を使うときだけ必要**」へ限定。worklog も追随 |

**あわせて worklog の「決定」「未決・次の一歩」を現行化**した — 着手時の裁定 3 件しか書いておらず、
その後の撤回・是正(裁定 6 の部分撤回・裁定 3 の見立ての誤り・裁定 5 の是正)が反映されていなかった。
**裁定 10 件の「その後」を追跡する表**に置き換え、`plan.md` の裁定表にも同じ追記を入れた。

**同じ失敗を 2 周続けた**: 6 周目の P1-1 も 7 周目の P1-1・P1-2 も「**主要箇所だけ直して波及先を放置**」。
7 周目は `grep` で全出現を列挙してから潰す手順に変えた。

計画レビュー周回: 6 → 7(**現在地 = 7**)

### 計画レビュー 8 周目

**否決(P0 0 件・P1 1 件・P2 3 件)。全 4 件を採用。P1 は 2 → 1 へ、P0 は 3 周連続でゼロ。**

| # | 指摘(要旨) | 採否 | 反映内容 |
| --- | --- | --- | --- |
| **P1** | **`research.md` にまだ旧 γ 責務が残る**(`:332-335`・`:345`・`:372-385`) | **採用** | **3 周続けて同じ場所を取り落とした**。`U-10`・(B) 論点 18・21 の「γ が受ける」を **TSK-321 / 330 / 331 へ分解**、「裁定 1 が確定させること」「裁定 2・3 で送るもの」に**着手時の記録である旨の印**を付け、**個別項目に取り消し線と現行の送り先**を入れた。**「本タスクの `/pr` で起票する」を削除**(TSK-330 は起票済み) |
| **P2-1** | 失効表が**裁定 7〜9 による是正まで「裁定 10 による失効」**としている | **採用** | 見出しを「**後続の裁定・レビューによる**」へ改め、**各行に実際の契機**(裁定 10 / 1 周目 P1-1 / 裁定 7・9 / 裁定 8 / 1 周目 P1-4 / 裁定 4・8・10)を列に追加。**射程分割とそれ以前の是正を分けた** |
| **P2-2** | 裁定の「その後」表が**既知の中間是正を追跡し切れていない** | **採用** | **裁定 1 に「1 周目 P1-1 で `B3a` の判定側だけ実装を撤回」**、**裁定 5 に「3 周目 P0-4 で `tElementCommitment` を足し 10 件へ」**を `plan.md` と worklog の両方へ追記 |
| **P2-3** | worklog の現行化で**旧周回数と重複段落が新たに残った** | **採用** | **Phase 4 / 移行制約の段落が二重化**していたので削除。worklog の周回数を **7** に、`design.md` の「5 周分の指摘を反映済み」を「**7 周分 — 1〜5 周目は詳細設計への指摘、6〜7 周目は射程分割の波及是正**」へ |

**3 周連続で同じ失敗型**(反映の波及漏れ)。8 周目は `grep` で全出現を列挙 → 1 件ずつ潰す → **再 grep で残存を確認**
という手順にした。

計画レビュー周回: 7 → 8

### 人間の承認(2026-09-07・山田正輝)

**計画レビュー 8 周を経て承認**。`plan.md` の frontmatter を `承認: 済(2026-09-07・山田正輝)` へ。

#### レビューの推移

| 周 | 判定 | P0 | P1 | P2 | 指摘の性質 |
| --- | --- | ---: | ---: | ---: | --- |
| 1 | 否決 | 5 | 5 | 0 | 計画の欠陥(正本の読み違い・射程の穴) |
| 2 | 否決 | 5 | 2 | 1 | 同上 + **既存コードの正本乖離を発見** |
| 3 | 否決 | 4 | 4 | 0 | 同上 + **裁定どうしの衝突を発見** |
| 4 | 否決 | 0 | 3 | 1 | 検査の実効性 |
| 5 | 否決 | 1 | 3 | 1 | 検査の実効性 → **射程分割の判断へ** |
| 6 | 否決 | 0 | 2 | 2 | 反映の波及漏れ |
| 7 | 否決 | 0 | 2 | 2 | 反映の波及漏れ |
| 8 | 否決 | 0 | 1 | 3 | 反映の波及漏れ + 自傷 |
| **累計** | | **15** | **22** | **10** | **全 47 件のうち 46 件を採用・1 件を理由つきで不採用** |

**P0 は 4 周目以降ほぼゼロ**(5 周目の 1 件は 4 周目の私の修正が作った順序バグ)。
**6 周目以降は計画の欠陥ではなく文書間の整合**が残課題だった。

#### 承認時点で残っている既知の不整合(承認者が承知のうえ)

- **`research.md` は着手時の調査メモ**であり、履歴として残す記述と現行の分担が混在する。
  **失効表(`:39-51`)と個別の失効印**で追跡しているが、**現行の正は `plan.md`**
- 8 周目の指摘は 4 件とも反映済みで、**未反映の指摘は無い**

#### 不採用にした 1 件(6 周目 P1-4)

「台帳と索引が実装ステップに割り当てられていない」— **ハーネスの読み違い**。
台帳と索引の更新は **`/pr` のクローズ処理**が正規経路(pr スキル手順 1-3)で、
**クローズ処理はステップ表の外側**にある(設計書 6.1)。7 周目でこの不採用は**妥当と認められた**。

#### 起票した受け取り先(3 件)

| タスク | 内容 | 開始条件 |
| --- | --- | --- |
| **TSK-330** | 8 章の原子性の実行検証と backend 適用の実装(後続 γ') | TSK-250 の完了 |
| **TSK-331** | wire 形式・プロパティ名・JSON / API スキーマの確定 | なし |
| **TSK-332** | `NFR-019(d)` 故障系テスト資産契約の再設計とシナリオ資産 10 件 | **本タスクの完了** |

#### 次の一歩

`/implement` でステップ単位に委任する(**ラッパーの受け入れ条件はすべて通過** —
`status: active` / `承認: 済` / 重さ分類 コア領域 → `gpt-5.6-sol` `xhigh` / ステップ表 4 本)。

## 決定

**人間の裁定は計画レビュー 6 周を通じて 10 件**(現行の全文は plan.md 4 節の裁定表が正)。
**着手時の裁定 1〜5 は、レビューを経て一部が撤回・是正されている**:

| # | 裁定 | 周 | その後 |
| --- | --- | --- | --- |
| 1 | 射程 = 案 A(契約・オラクル・注入点の固定に限定)。backend 変更なし・TSK-250 を開始条件に含めない | 着手時 | **1 周目 P1-1 で「`B3a` は判定側だけ実装」を撤回**(帰結が不可分な `P5 + T9`)→ **`B3a` ごと TSK-330 へ**。**裁定 10 で分割** — 契約と注入点は TSK-332 へ。**backend 不変と TSK-250 非依存は維持** |
| 2 | 6-2 の処理段階は関係マニフェストに登録しない | 着手時 | **維持**。ただし「TS 側の単体テストで固定」は**裁定 7・9 の 2 層ドリフト検査**へ強化 |
| 3 | `U-13`・`U-14` は射程外のまま進む | 着手時 | **維持**。ただし「契約・オラクルの固定は矛盾に触れず成立する」という見立ては**誤りだった** — `DI5` が触れるため**裁定 8 で `DI5` ごと射程外へ** |
| 4 | wire スキーマは含めない | 着手時 | **維持** → **TSK-331 を起票** |
| 5 | 期待フィールドは条件付き必須へ | 着手時 | **1 周目 P0-1 で「共通 9 件 + 省略可否を条件付き」へ是正** → **3 周目 P0-4 で `tElementCommitment` を足して 10 件へ再是正** → **裁定 10 で設計ごと TSK-332 へ** |
| 6 | `DI5`・`B3b` は両方やる | 2 | **裁定 8 で `DI5` を撤回**(`B3b` は維持) |
| 7 | ドリフト検査は 2 層 | 2 | 維持 |
| **8** | **`U-14` との衝突 → `DI5` を射程外へ戻す** | 3 | — |
| **9** | **スナップショットは `frontend/` 配下** | 3 | — |
| **10** | **打ち切り → 射程を分割**(故障系契約と資産を TSK-332 へ) | 5 | — |

**Phase 4 の論理スロット 4-6 には当たらない**ことを確定した — 要件書 `:956` が Phase 4 完了時の合格条件から
「**一致性・越境・E2E・同期故障系は実装期のため対象外**」と明示的に除外している。
→ `/task-done` の手順 4(`P4-後` の起票)は発火しない。

**旧システム側の移行制約は 1 件も無い**ことを確定した(legacy-analyst が `docs/legacy/` 全 11 ファイルを確認)。

## 結果サマリ(/pr クローズ処理)

### 何を実装したか(4 ステップ)

| # | コミット | 内容 |
| --- | --- | --- |
| 1 | `bc5b98c` | **`R-TXN-ROUTE` の専用パーサと reader** — 8-1 の経路表 `P1`〜`P5` と T 要素 `T1`〜`T9` の 14 要素。**経路 → T 集合の写像を公開**し、TSK-332 が 18 組の唯一の出所として使う |
| 2 | `d2a28b8` | **6-2 の処理段階 + 2 層の正本ドリフト検査** + `DI1`・`DI4`・`I1`・`I4` を実装済みへ |
| 3 | `69e990e` | **`RG1` 共通前段ゲート**(右辺 10 節を逐語固定)+ `RG1` を両関係で実装済みへ |
| 4 | `b7fbde5` | **既存 D5 分類器を正本 4-5 へ是正** + `B3b` を実装済みへ |

### 本タスクの一番の成果 — 既存コードが正本から乖離していた

正本 4-5 `:404` は「**判定不能は異内容として扱い**…**D1 付き経路は `B3b`、D1 を持たない変更イベントは `B13` を返す**」
と明記しているのに、TSK-280 が入れた `idempotencyCollision.ts` は
**`REJECT_LATER`(`'後着拒否'`)という正本に存在しない第 3 の語彙**を返していた。

**計画レビュー 2 周目でレビュアが発見**したもので、計画を 8 周回した価値がここに出た。
是正は正本の文言をそのまま実装する形にした — **新しい分岐を足さず** `contentIdentity` を
`DIFFERENT` へ写像して既存の規則表へ流すので、**規則表の単一性(`NFR-018`)も保たれる**。
結果 `REJECT_LATER` は到達不能になり**列挙ごと削除**、**再導入を防ぐテスト**も置いた。

### 2 層の正本ドリフト検査

```
docs/design/sync-protocol.md 6-2
   ↓ ① harness(Python・paths filter なし = 常時実行)
frontend/src/lib/sync/processingStages.snapshot.json
   ↓ ② Vitest(frontend/** フィルタで起動)
frontend/src/lib/sync/processingStages.ts
```

**スナップショットを `frontend/` 配下に置いた**のが要点。`scripts/` に置くと、
**正本と同時に変えた PR で Vitest がフィルタ外になり skip され、TS だけ旧状態でも気づけない**
(7 周目 P1-1)。`ci.yml`(`guard_paths`)を触らずに穴が閉じた。

### allow-list の移動 6 ID と、射程外に残した 4 ID

| 移動 | `DI1`・`DI4`・`I1`・`I4`(処理段階)/ `RG1`(両関係)/ `B3b`(B3 の分岐) |
| --- | --- |
| **射程外に残す** | `B3a`(`P5 + T9` が不可分)/ `DI5`(`U-14` 未解決)/ `I5`・`I6`(永続化を伴う)— **いずれも理由が TSK-330 を名指しし、名指しが消えたら red になる検査つき** |

用途別に 3 つの実装済み集合(D5 衝突規則 / 処理段階 / B3 の分岐)へ分け、
**`EXPECTED = 実装済み(3 つ) ∪ 射程外` の総和が正本の ID 集合と一致すること**を検査で固定した。

### 検証で重視したこと — 「テストがあるが実装を変えても通る」型の排除

計画レビューが繰り返し突いたのがこの型だったので、**各ステップで変異実験を実測**した(計 14 回):

| ステップ | 実験 | 結果 |
| --- | --- | --- |
| 1 | T 要素行の `=` 禁止検査を外す / 孤立 T の照合を外す | いずれも red |
| 2 | 正本 6-2 の 1 セルを変異 / スナップショットを変異 / 製品表の `DI1` 右辺を変異 | いずれも red |
| 3 | `RG1` の 1 節を変異 / 節の順序を入れ替え / fail-closed を反転 | 前 2 つは red・3 つ目は **TS2322 型エラー** |
| 4 | 判定不能を `SAME` へ / 比較例外を `SAME` へ / 破損例外を外す / `startsT9` を `true` へ / `B3b` の 1 節を変異 / `DI5` の理由から受け取り先を消す | いずれも red(`startsT9` は型エラー) |

**1 度は私のスクリプトが壊れていて偽の green を出した**(節の順序入れ替えが実際には起きていなかった)。
やり直して検出を確認した。

### 品質ゲート

| 層 | 結果 |
| --- | --- |
| frontend | prettier / eslint / vue-tsc green・**vitest 661 passed**(着手時 619 → +42) |
| harness | ruff / ty green・**pytest 1134 passed**(1129 → +5) |
| 新規検査器 | `check_processing_stages.py` **rc=0** |

### 台帳への追記

**該当あり** — `## 候補` へ **2 件**、変更履歴表へ 1 行、`docs/README.md` の台帳行を現行化。
**`H-*` の新規採番はしていない・版は上げない**(7.6-3 前段)。

1. **ハーネス設計書の `contracts/` 記述の食い違い**(`:181` は OpenAPI を含む / `:198` は NFR-019a のベクタに限定)
   — **TSK-331 の置き場裁定に直撃**する
2. **CI のパスフィルタが個別列挙方式**で、正本由来の橋渡し資産をフィルタ外へ新設すると同時変更ですり抜ける
   — 本タスクは置き場の選択で回避した

### 残っているもの(すべて人間の作業)

1. **人間の逐行確認**(コア領域 4 領域該当・**PR 作成者以外**)。CI が `- [x]` を機械検査する
2. **実施記録行の記入**(`対象= 範囲= 方法=`)
3. **マージ**

**逐行確認の優先度**: ① **ステップ 4(既存 D5 分類器の振る舞い変更)** ② ステップ 2 の段階順序
(9 段階 + 11 段階の逐語)③ `RG1` の右辺 10 節 ④ `R-TXN-ROUTE` の 14 要素。

**①が最優先である理由**: 本タスクで唯一**既存のコア領域コードの振る舞いを変えた**箇所であり、
`NFR-018`(単一実装)と `FR-012`(二重適用の防止)に直接触れる。

## 未決・次の一歩

**着手時の問い(TSK-250 依存の切り分け)は決着済み** — /investigate で「スキーマの 2 義」を確定させ、
**TSK-250 が決めるのは DB スキーマ**であり本タスクは依存しないと裁定した(裁定 1)。

**現在地**: 計画レビュー **6 周**を経て射程が確定。**人間の承認待ち**。

- **次の一歩**: 計画書の人間承認 → `承認: 済` へ更新してコミット → `/implement` でステップ単位に委任
- **本タスクの射程**: 4 ステップ(`R-TXN-ROUTE` reader / 6-2 の処理段階 + 2 層ドリフト検査 /
  `RG1` ゲート / **既存 D5 分類器の正本 4-5 への是正**)。allow-list からの移動は **6 ID**
- **送った先**: **TSK-330**(実行検証 + backend・開始条件 = TSK-250)/ **TSK-331**(wire スキーマ)/
  **TSK-332**(故障系契約と資産・開始条件 = 本タスク)
- **`/pr` のクローズ処理で行うこと**: 評価台帳の `## 候補` へ 2 件 + 変更履歴 1 行 + `docs/README.md` の現行化
