---
date: 2026-09-07
topic: 同期プロトコル — サーバー適用の処理段階と原子性(後続 γ)
branch: feature/sync-server-apply
---

# 作業ログ: 2026-09-07 同期プロトコル — サーバー適用の処理段階と原子性(後続 γ)

## やったこと

### /task-start

- Notion タスク **TSK-321**(TSK-280 の /pr クローズ処理で 2026-09-04 に起票済み)を取得。**重複起票はしていない**
- worktree `../pitchlog-worktrees/feature-sync-server-apply`(ブランチ `feature/sync-server-apply`・
  起点 `origin/develop` = `56e4180`)を作成
- 計画書雛形 `docs/features/sync-server-apply/plan.md` を作成(重さ分類 = **コア領域**)

### /investigate

調査サブエージェント **5 本を並列**で投げた(コア領域・射程裁定が絡むため既定 3 から増やした):
spec-checker(要件突合)/ decision-tracer(決定経緯)/ Explore ×2(正本 6・8 章の精読 / 実装資産の現況)/ legacy-analyst(旧システムの制約)。
結果を `docs/features/sync-server-apply/research.md` に統合した。

**裁定した矛盾**: `B3a` の除外理由が 4 典拠で 2 系統に割れていた(計画書「6 章の処理段階に依存」vs
allow-list「P5・T9 処理に依存」)。**正本 `:1248`(`B3a` = `P5 + T9`)と `:1201`(`T9` = 保存を伴う原子境界)で裁定**し、
**`B3a` は 6 章の段階列挙だけでは閉じない**と確定した。`DI1`・`DI5`・`I1` は 4 典拠すべてが一致する。

**最大の発見**: **「スキーマ」の語が 2 種を指していた。** TSK-250 が決めるのは
**DB スキーマ**(列名・型・NULL 性・FK・索引・表分割 — `:1774`)、後続 β が γ へ送ったのは
**wire / JSON / API スキーマ**(`sync-ack-contract/plan.md:246`)。カード本文の「スキーマ不要で先行できる」は
**DB スキーマ**を指すと読むのが正本の用語法と整合する。

### /plan(計画書の作成と計画レビュー 1 周目)

`docs/features/sync-server-apply/plan.md`(6 節すべて記入)と、密度の高い機構設計を
`docs/features/sync-server-apply/design.md` へ分離して起票。

**計画レビュー 1 周目**(`codex_run.py review adversarial` = `sol xhigh`・2026-09-07):
**否決(P0 5 件・P1 5 件・P2 0 件)。全 10 件を採用**した。

| # | 指摘(要旨) | 採否 | 反映内容 |
| --- | --- | --- | --- |
| **P0-1** | `applicationPath` 省略時に期待フィールドを基底 5 件へ減らす設計は**正本にない例外** | **採用** | 10-3 `:1691` は「**各 JSON は**…prefix・べき等結果・一時 ID 写像・記録権…を表す」で、条件付きの部分は**加算**だった。**集合を共通 9 件(無条件)にし、省略可否だけを条件付き**へ改めた。既存 4 資産は `_v2` へ原子移行(人間の裁定 2026-09-07) |
| **P0-2** | P5 の条件付き契約が分岐名だけで判定証拠を必須化していない | **採用** | `P5分岐` を **discriminated union** にし、`B3a` = 未使用 D5 + 拒否原本、`B3b` = 不変な先着原本 + 同一 D5 異内容の後着を exact-key 必須に |
| **P0-3** | 2 資産では正本が明示する必須シナリオ集合を満たせない | **採用** | **未作成資産の繰り延べ表(exact-set)**を新設。復元系 8 シナリオ・`p3-invalidation-consumed-before-complete`・`P1`〜`P5` の故障行列を ID 単位で列挙し **TSK-330** を名指し |
| **P0-4** | ステップ 7 はステップ 8 の成果物なしに合格条件を成立させられない | **採用** | 繰り延べ表・資産・scenarioId を**ステップ 7 で原子的に追加**する形へ再構成(8 → 7 ステップ) |
| **P0-5** | `DI5`/`B3` の新規決定器が既存 D5 分類器とコピー実装になる(**`NFR-018` = P0**) | **採用** | D5 分類を **`decideIdempotencyCollision` の一系統に限定**。新モジュールは**分類済み結果の D1 順停止だけ**を担う。**分類を自前で持たないことを機械的に確認**する合格条件を追加 |
| **P1-1** | `B3a` を実装済みへ移す境界が正本の安定 ID と一致しない | **採用** | **`B3a` は射程外に残す**(正本の帰結は不可分な `P5 + T9`)。移動は 8 ID → **7 ID** へ。理由を **TSK-330** 名指しへ |
| **P1-2** | 注入点の語彙化に実行時検査がなく任意文字列が green のまま残る | **採用** | **18 組の `(経路, T 要素)` と P3 の 3 注入点を閉じ**、値と組合せを実行時検査。18 組はステップ 1 の reader から導出し**二重定義しない** |
| **P1-3** | コア領域登録が製品 4 本だけで spec を漏らしている | **採用** | 現行 `core-areas.json` は製品と spec を**個別登録**していた。**新規 3 モジュール + 各 spec = 6 パス**を登録する形へ |
| **P1-4** | wire スキーマが名前のない「別タスク」へ再送されている | **採用** | **TSK-331 を起票**し相互リンク。DoD に加えた。あわせて **TSK-330(γ')も起票** |
| **P1-5** | 6-2 の順序が正本ドリフトを検出できず「実装済み」移動と釣り合わない | **採用** | レビュアが提示した**第 3 案(スナップショット・ドリフト検査)**を採用(人間の裁定)。正本 6-2 の 2 表を実際に読んで逐語照合する。**正本を 1 行も改訂しない**ので確定ゲートを回さずに済む |

**起票した受け取り先 2 件**:

- **TSK-330** — 8 章の原子性の実行検証と backend 適用の実装(後続 γ')。開始条件は TSK-250 の完了
- **TSK-331** — wire 形式・プロパティ名・JSON / API スキーマの確定(後続 β の宿題)

**副産物**: 本レビューは**バックグラウンド実行で完走した(exit 0)**。TSK-322 で 23 回連続して
強制終了していた経路であり、`~/.claude/settings.json` の `env` ブロックに置いた
`CLAUDE_CODE_DISABLE_BG_SHELL_PRESSURE_REAP=1` が **claude 自身のプロセス環境にも届いていて、
再起動なしで既に効いている**ことが実測で確定した
(`BASH_MAX_TIMEOUT_MS` は起動時に固定されるため、このセッションでは未適用のまま)。

### 計画レビュー 2 周目

**否決(P0 5 件・P1 2 件・P2 1 件)。全 8 件を採用**した。

| # | 指摘(要旨) | 採否 | 反映内容 |
| --- | --- | --- | --- |
| **P0-1** | 非後方互換な契約変更なのに `schemaVersion` の繰り上げがない | **採用** | 必須フィールドを 5 → 9 に増やすと旧構造は新 validator を通らない = 非後方互換。10-3 `:1689` に従い**全資産を `schemaVersion: 2`** にし、**validator が構造と `schemaVersion` の組を照合**する |
| **P0-2** | 既存 D5 分類器の判定不能時が**正本 4-5 と乖離**している | **採用** | 正本 `:404` は「**判定不能は…D1 付き経路は B3b、D1 を持たない変更イベントは B13 を返す**」。現行実装は例外・`INDETERMINATE` を `REJECT_LATER`(第 3 の値)にしていた。**このまま `B3b` を実装済みへ移すと非正本の結果を封印する**。**人間の裁定で既存実装を正本へ是正**する |
| **P0-3** | `batchStopBoundary.ts` の入力だけでは `DI5` を決定できない | **採用** | `NOT_DUPLICATE` の A5 は後段(⑤記録権 → `B4` / ⑥連番 → `B2` / ⑦内容 → `B3a`)で分かれる。**人間の裁定で後段候補を遅延評価のコールバックで注入**し、**gap 後は呼ばれないこと**を保証する |
| **P0-4** | P5 の discriminated union が**分岐証拠の値関係**を検査しない | **採用** | `B3b` は先着原本が初期永続状態と一致・先着と後着の D5 が同一・内容が異なることを、`B3a` は対象 D5 が初期べき等集合に存在しないことを**実行時検査**する |
| **P0-5** | 未作成資産の繰り延べ表が **stable ID の exact-set になっていない** | **採用** | 「復元系 8 件」「P1〜P5」というカテゴリしか列挙しておらず母集合が無かった。**正本必須資産 ID の母集合**(10-2 の故障系シナリオ表 + 10-3 が名指しする ID)を **stable な小文字 scenarioId で全件定義**し、`作成済み ∪ 未作成繰り延べ = 母集合` を独立した exact-set にした |
| **P1-1** | 正本ドリフト検査が**正本だけの変更で CI 起動しない** | **採用** | `ci.yml:124-128` の `frontend-changes` フィルタは `frontend/**` のみ。一方 harness ジョブは「paths filter は付けない」と明記(`:88`)。**人間の裁定で 2 層構造**へ — harness の Python が正本↔スナップショット、Vitest がスナップショット↔TS |
| **P1-2** | 共通 9 件と「既存 runner は変更不要」が両立しない | **採用** | `validateFailureScenarioResult` も同じ `validateObservations` を使うため、5 キーしか返さない既存 runner が必ず red になる。**契約検査と結果検査を分離**し、結果側は契約が `omittedBecause` としたキーの不在だけを許可する |
| **P2** | `_v2` 原子移行の結論は成立するが**説明している不変条件が逆** | **採用** | 正しくは「**旧版参照が存在する間に旧資産を削除しない**」。手順を「新版追加 → 旧資産を残したまま全参照移行 → 旧版削除」と明記した |

**人間の裁定 2 件(2026-09-07)**:

6. **`DI5`・`B3b` は両方やる** — 既存分類器を正本 4-5 へ是正し、後段候補をコールバックで注入する
7. **ドリフト検査は 2 層**(harness の Python が正本↔スナップショット / Vitest がスナップショット↔TS)。
   `ci.yml` と `test_ci_wiring.py` はいずれも `guard_paths` なので触らない

**構成の変化**: 7 → **8 ステップ**。移動する ID は 7 件のまま(`B3a` は射程外に残す)。
**シナリオ資産は 2 件 → 9 件**へ(墓標・改訂 + `P1`〜`P5`・`D1` 混在バッチ・gap 後 `B3b`)。
**既存のコア領域コード(`idempotencyCollision.ts`)の振る舞いを変更する**ため、
逐行確認の最優先をステップ 4 に置いた。

計画レビュー周回: 1 → 2

## 決定

**人間の裁定(2026-09-07)— 3 件**(詳細と根拠は research.md「人間の裁定」節)

1. **射程 = 案 A(契約・オラクル・注入点の固定に限定)**。backend 変更なし・TSK-250 を開始条件に含めない
2. **6-2 の処理段階は関係マニフェストに登録しない**(正本改訂・確定ゲートを回さない)。残るリスクは台帳の候補へ
3. **`U-13`・`U-14` は γ の射程外のまま進む**(TSK-329 が受領済み)

**Phase 4 の論理スロット 4-6 には当たらない**ことを確定した — 要件書 `:956` が Phase 4 完了時の合格条件から
「**一致性・越境・E2E・同期故障系は実装期のため対象外**」と明示的に除外している。
→ `/task-done` の手順 4(`P4-後` の起票)は発火しない。

**旧システム側の移行制約は 1 件も無い**ことを確定した(legacy-analyst が `docs/legacy/` 全 11 ファイルを確認)。

## 未決・次の一歩

- **開始条件に TSK-250(データモデル正本化・現在 進行中)の完了を含めるか**を最初に判断する。
  カード本文の制約どおり、6 章の処理段階だけならスキーマ不要で先行できる可能性がある。
  **8 章(適用・原子性)と `NFR-019(d)` のクラッシュ注入は `U-8` 経由で TSK-250 に依存する**
- 正本 `docs/design/sync-protocol.md` は 2026-09-07 に **v0.3 approved**(TSK-322)。
  本タスクは **6 章 / 8 章**が対象で、TSK-322 が 6-3 の段階番号(④ → ⑤)を是正済み
- 次の導線: /investigate(射程と TSK-250 依存の切り分け)→ /plan
