---
date: 2026-09-07
topic: 同期プロトコル — サーバー適用の処理段階と原子性(後続 γ)
branch: feature/sync-server-apply
---

# 作業ログ: 2026-09-07 同期プロトコル — サーバー適用の処理段階と原子性(後続 γ)

## やったこと

### /task-start

- Notion タスク **TSK-321**(TSK-280 の /pr クローズ処理で 2026-09-04 に起票済み)を取得。**重複起票はしていない**
- worktree `../pitchlog-worktrees/feature-sync-server-apply`(ブランチ `feature/sync-server-apply`・
  起点 `origin/develop` = `56e4180`)を作成
- 計画書雛形 `docs/features/sync-server-apply/plan.md` を作成(重さ分類 = **コア領域**)

### /investigate

調査サブエージェント **5 本を並列**で投げた(コア領域・射程裁定が絡むため既定 3 から増やした):
spec-checker(要件突合)/ decision-tracer(決定経緯)/ Explore ×2(正本 6・8 章の精読 / 実装資産の現況)/ legacy-analyst(旧システムの制約)。
結果を `docs/features/sync-server-apply/research.md` に統合した。

**裁定した矛盾**: `B3a` の除外理由が 4 典拠で 2 系統に割れていた(計画書「6 章の処理段階に依存」vs
allow-list「P5・T9 処理に依存」)。**正本 `:1248`(`B3a` = `P5 + T9`)と `:1201`(`T9` = 保存を伴う原子境界)で裁定**し、
**`B3a` は 6 章の段階列挙だけでは閉じない**と確定した。`DI1`・`DI5`・`I1` は 4 典拠すべてが一致する。

**最大の発見**: **「スキーマ」の語が 2 種を指していた。** TSK-250 が決めるのは
**DB スキーマ**(列名・型・NULL 性・FK・索引・表分割 — `:1774`)、後続 β が γ へ送ったのは
**wire / JSON / API スキーマ**(`sync-ack-contract/plan.md:246`)。カード本文の「スキーマ不要で先行できる」は
**DB スキーマ**を指すと読むのが正本の用語法と整合する。

## 決定

**人間の裁定(2026-09-07)— 3 件**(詳細と根拠は research.md「人間の裁定」節)

1. **射程 = 案 A(契約・オラクル・注入点の固定に限定)**。backend 変更なし・TSK-250 を開始条件に含めない
2. **6-2 の処理段階は関係マニフェストに登録しない**(正本改訂・確定ゲートを回さない)。残るリスクは台帳の候補へ
3. **`U-13`・`U-14` は γ の射程外のまま進む**(TSK-329 が受領済み)

**Phase 4 の論理スロット 4-6 には当たらない**ことを確定した — 要件書 `:956` が Phase 4 完了時の合格条件から
「**一致性・越境・E2E・同期故障系は実装期のため対象外**」と明示的に除外している。
→ `/task-done` の手順 4(`P4-後` の起票)は発火しない。

**旧システム側の移行制約は 1 件も無い**ことを確定した(legacy-analyst が `docs/legacy/` 全 11 ファイルを確認)。

## 未決・次の一歩

- **開始条件に TSK-250(データモデル正本化・現在 進行中)の完了を含めるか**を最初に判断する。
  カード本文の制約どおり、6 章の処理段階だけならスキーマ不要で先行できる可能性がある。
  **8 章(適用・原子性)と `NFR-019(d)` のクラッシュ注入は `U-8` 経由で TSK-250 に依存する**
- 正本 `docs/design/sync-protocol.md` は 2026-09-07 に **v0.3 approved**(TSK-322)。
  本タスクは **6 章 / 8 章**が対象で、TSK-322 が 6-3 の段階番号(④ → ⑤)を是正済み
- 次の導線: /investigate(射程と TSK-250 依存の切り分け)→ /plan
