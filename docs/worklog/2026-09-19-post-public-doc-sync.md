---
date: 2026-09-19
topic: public 化後の正本追随
branch: feature/post-public-doc-sync
---

# 作業ログ: 2026-09-19 public 化後の正本追随

## やったこと

- `/task-start` — TSK-429 起票・`feature/post-public-doc-sync` + worktree 作成
- `/investigate` — 調査サブエージェント 2 本(decision-tracer / spec-checker)+ GitHub API 実測 + 原典での裏取り → [research.md](../features/post-public-doc-sync/research.md)
- `/plan` — 計画書を記入(7 ステップ)、`review normal` を起動

## ステップ 1: gitleaks 検出の誤検知判定(2026-09-20)

**実行**: 正本(`github-setup.md` 4 章)の SHA 固定イメージ `ghcr.io/gitleaks/gitleaks:v8.30.1@sha256:c00b6bd0…` を `--redact=100` 付きで実行。**秘匿値は本記録に含めない。**

**検出は 2 件**(**メモリが持っていた「3 件」と食い違う** — 作業ツリーの `dir` スキャン結果。全履歴スキャンは下記の理由で未実施):

| # | RuleID | File | Line | キー | 値の形式 |
| --- | --- | --- | --- | --- | --- |
| 1 | `generic-api-key` | `tests/fixtures/profile-sample/profiles/registry.json` | 25 | `pins.asset_digests.auth_catalog` | 64 桁 hex(SHA-256) |
| 2 | `generic-api-key` | 同上 | 27 | `pins.asset_digests.auth_ddl_map` | 64 桁 hex(SHA-256) |

**誤検知と判定する根拠(4 点)**:

1. **値は資産から導出される内容ハッシュである。** `scripts/doc_check_profile.py:1846-1867` が `pins["asset_digests"]` を読み、`:491`・`:511` の `hashlib.sha256(...).hexdigest()` で**再計算して照合**し、不一致なら「`pins.asset_digests.<name>` が不一致です」で落とす。**再計算可能な導出値であり、秘匿を要する資格情報ではない。**
2. **同一ファイル・同一構造の兄弟 8 件は検出されていない**(`claims`・`ddl_elements`・`product_ddl_map`・`waiting`・`forbidden`・`direct_requirements`・`expected_ids`・`baseline_digest`)。**発火の差はキー名に `auth` を含むかどうかだけ**で、値の性質によるものではない。
3. **NFR-014 の対象は「DB接続情報・JWT署名鍵・管理者パスワード・チーム初期パスワード」の 4 種**。内容ハッシュはいずれにも該当しない。
4. **テストフィクスチャである**(`tests/fixtures/profile-sample/`)。`test_check_doc_coverage.py`・`test_doc_check_profile.py`・`test_ci_wiring.py` 等が消費する。

**全履歴スキャンの結果(人間が `!` で実行・1612 コミット走査・`ERR` なし・partial scan なし)**: **3 件**。作業ツリーの 2 件との差は、**`auth_catalog` のダイジェストが更新されて 2 つのコミットで別々に検出された**ことによる。**新しい種類の検出ではない。**

| # | Commit | 日付 | Line | キー |
| --- | --- | --- | --- | --- |
| 1 | `5c8f52c`(資産ローダー等を追加 ステップ 28/38) | 2026-09-04 | 25 | `auth_catalog` |
| 2 | `5c8f52c` | 2026-09-04 | 27 | `auth_ddl_map` |
| 3 | `190fc3a`(corpus version 台帳を追加 ステップ 8/12) | 2026-09-14 | 25 | `auth_catalog`(値の更新により再検出) |

**人間の判定(2026-09-20・山田正輝)**: 上記 4 点の根拠を提示し、登録方針 A / B / C を示したうえで **A(3 件を誤検知として `.gitleaksignore` へ登録)を選択**。**これをもって 3 件すべてを誤検知と判定した記録とする**(計画書 ステップ 1 の合格条件)。**真のシークレットは含まれないため、停止・失効・ローテーションの経路には入らない。**

### 登録方式の限界(ステップ 2 で正本へ明記する)

gitleaks の fingerprint は **`<コミット SHA>:<パス>:<ルール>:<行>`** で**コミット単位**。したがって **`.gitleaksignore` へ 3 件登録しても、当該資産のダイジェストが変わるたびに新しい fingerprint が生まれ `secrets` が再び落ちる**。実測として **2026-09-04 → 09-14 の 10 日間で 1 件増えている**。

**A を選んだ判断の内訳**: B(`.gitleaks.toml` のパス単位 allowlist)は恒久的だが**正本 4 章の手続を変える**うえ `[extend] useDefault` の挙動が未確認で、**組込みルールを置換すると走査が空洞化する**リスクがある(計画のスコープ外)。C(フィクスチャのキー名変更)は発火条件を消せるが**本タスクの射程外**。**A の再発コストは「新しい fingerprint を 1 行足す」だけ**である。

**→ ステップ 2 で `github-setup.md` 4 章へ「再発する」ことを明記する**(次に踏む人が同じ調査をやり直さないため)。

### 未了 — 全履歴スキャンが hook に阻まれる(**本タスクの射程内の発見**)

正本 `github-setup.md` 4 章が定めるローカル監査コマンドは **`docker run … gitleaks:v8.30.1 git --no-banner --no-color --redact=100 /repo`** だが、**`git_guard.py` がこれをブロックする**:

> ブロック: git のグローバルオプションまたはサブコマンドを解析できませんでした。安全側で遮断します。

**gitleaks の `git` サブコマンドを git コマンドと誤認**している。**approved 正本が定める手順が、同じリポジトリの hook によって実行不能**という状態。**4 章を編集する本タスクの射程内**なので、ステップ 2 で扱う。

暫定として `dir` サブコマンド(作業ツリーのみ)で 2 件を同定した。**全履歴に 3 件目が存在するかは未確認** — メモリの「3 件」との差がここで説明できる可能性がある。**人間に `!` 付きで実行してもらう必要がある。**

## ステップ 2: `.gitleaksignore` 新設と 4 章の追随(2026-09-20)

**ignore 機構が実際に効くことを正例で確認した**: コメント行を含む ignore ファイルを `--gitleaks-ignore-path` で与えて `dir` 走査 → **`no leaks found` / exit 0**。「パースエラーが出ない」だけでは**読まれている証明にならない**ため、抑止が働く側で確かめている。

**CI 実測(計画書ステップ 2 の合格条件)**:

| 項目 | 値 |
| --- | --- |
| run ID | **35513315849** |
| `event` | `workflow_dispatch` ✓ |
| `headBranch` | `feature/post-public-doc-sync` ✓ |
| `headSha` | **`9c6a8f75eab3e1383b79e2b1e44842f19e9e354b`** ✓(コミット `9c6a8f7` = ステップ 2 の HEAD と一致) |
| `secrets` | **success** |
| 全 9 ジョブ | **すべて success**(`secrets` `docs-lint` `core-guard` `harness` `nfr021-append-only` `frontend-changes` `backend-changes` `frontend` `backend`) |

**既定ブランチでの green ではなく、`.gitleaksignore` を含む本 feature の SHA 上で走ったことを 3 項目の照合で確認済み。**

**登録した fingerprint はステップ 1 の記録と 1 対 1 で対応**(`5c8f52c`:25 / `5c8f52c`:27 / `190fc3a`:25)。

## ステップ 6: NFR-019 (a)〜(d) の追跡先の解決(2026-09-20)

### TSK-202 の同定 — 実在するが**完了済み**

設計書 10.2 のリスク受容記録が追跡先として名指しする **`TSK-202`** は実在した: **「Phase 3: CI 先行分(gitleaks・docs-lint・hooksテスト)+ ブランチ保護」**(`ci-foundation/plan.md` の `notion:` と同一ページ)。**ステータス = 完了(2026-08-10)・URL プロパティ = PR #2**。

**したがって追跡先としては機能しない。** 閉じたタスクは (a)〜(d) の残作業を追えない。**特定不能ではなく「特定できたが完了済み」**である。

### 置換先 — **`TSK-217`**(PO 裁定で既に決まっていた)

**新たに決める必要はなかった。** `TSK-217`「NFR-019(b) 越境アクセステストの実装 — 認可行列の網羅」が、**PO 裁定 `C`(2026-09-12・山田正輝。`docs/worklog/2026-09-12-merge-gate-api-cycle.md:66`)**により「**対象横断の判定とハーネスの所有者**」と定められており、`contracts/authz/claim-mutant-map.json` の 10 owner のうち 2 件が本件に当たる:

- **owner 4 `NFR-019/paragraph-001`** — 「**比較単位と正解ベクタの定義((a)(c)(d) を含む)**」/ 根拠 = 柱書から届く・**ハーネスの所有**
- **owner 10 `SECTION-8/list_item-002`** — リリース判定基準 2「CI 全グリーン」(**全ランナー**)/ 根拠 = **CI ハーネスの所有**

### 対応表

| 項 | 追跡先 | 状態 | 担当 | DoD(要点) |
| --- | --- | --- | --- | --- |
| **(a)** 一致性 | **`TSK-217`**(柱書 owner 4)。実装基盤は ADR-003 の検査基盤(`TSK-235` — **本表では未検証**) | 未着手 | PO | 比較単位と正解ベクタの定義 |
| **(b)** 越境 | **`TSK-217`** 本体([URL](https://app.notion.com/p/3bc93b75e68781f2a613d791db2231ab))。見積 8・**コア領域(テナント分離)** | 未着手 | PO | 全組合せを pytest で実装し CI 常設 / 合否が「認可行列どおりに通り行列外は 404」/ 理由コード非漏洩 / 逐行確認の証跡 |
| **(c)** E2E | **`TSK-217`**(柱書 owner 4)。**専用の実装カードは見つからなかった** | — | PO | — |
| **(d)** 同期故障系 | **`TSK-324`**「同期プロトコル — NFR-019(d) を実入力経路と実ブラウザへ繋ぐ」([URL](https://app.notion.com/p/3d293b75e68781de9bcbd6774486322a))。加えて柱書 owner 4 = `TSK-217` | 未着手 | **未設定** | 4 シナリオが実入力経路を通る / `faultInjection` の全フィールド参照・未参照の変異が red / 複数タブ競合の実態検証 / Playwright で Web Locks の解放観測 |

### 申し送り(ステップ 8 の材料)

1. **`TSK-324` に担当者が設定されていない。**
2. **(c) E2E に専用の実装カードが無い** — 柱書 owner の `TSK-217` が抱えている形。`TSK-217` は**コア領域・見積 8・未着手**で、既に 10 owner を抱えている。
3. **`TSK-202` は完了済み**なので、設計書 10.2 の追跡先の記述は**そのままでは死んでいる**。ステップ 8 で解除するときに `TSK-217` へ差し替える。

## ステップ 8: 確定ゲート(設計書 10.2 の解除)— 開始(2026-09-21)

### 適用版の記録(7.3-1)

| 項目 | 値 |
| --- | --- |
| **適用版** | **設計書 v1.12 の 7.3**(2026-09-01 approved・敵対レビュー 反映 12 周で収束) |
| **条文コミット SHA** | **`56f8c104c6a30f5d38cd352ce50d4f108f2c8e13`** |
| 導出方法 | `git log -L <7.3 の行範囲>` で 7.3 を最後に変更したコミットを特定(`56f8c10`「確定ゲート 反映12周目」)。同日の変更履歴行 v1.12 が「確定ゲート通過(approved)」 |

**本ゲートは上記適用版で最後まで運用し、途中で切り替えない。** 初回敵対レビュー実行時点で異なっていれば更新してから開始する(暫定記録)。

### 対象と射程

**対象は設計書 1 本**(`dev-harness-design-2026-08-07.md` — frontmatter を `in-review` へ、変更履歴に v1.17 行を追加)。**射程宣言は v1.17 の変更履歴行に記載**(7.3-7 — 既存正本の改訂は当該改訂の変更履歴行に書く)。

**確定する範囲は 3 点**: ①解除手続の新設(5 項)②承認済み例外を `TSK-432` を持つ新しい未達へ切り替え ③リスク受容記録の解除と追跡先の `TSK-202` → `TSK-217` 差し替え。

**変更しない範囲**: NFR-019 (a)〜(d) の未達状態 / 10.1 の受入ゲートと残余リスク / `github-setup.md` 2 章 手続 3・4 の継続 / 補償統制の内容 / 6.4 の過去事実。

### 周回記録

#### 初周(全文)— 採否記録

**判定: 要修正 — P0 1 件 / P1 4 件 / P2 1 件**(判定行あり・件数一致 → 有効な周)。**6 件すべて採用・不採用 0 件。**

| # | 要旨 | 重大度 | 起因 | 分類 | 採否 | 反映内容 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **`integration_id` 未指定のまま解除すると統制が壊れる** — 必須 9 context とも発行元が未拘束で、書き込み権限者が同名の成功 status を立てれば必須チェックを満たせる。CI が落ちていても・走っていなくてもマージできる | **P0** | 非起因 | (A) | **採用** | **解除を撤回し射程縮小**(PO 裁定 2026-09-21)。リスク受容記録は**解除条件に「必須チェックの発行元が拘束されていること」を追加して存続**。あわせて**解除手続 6「形式的な充足で解除しない」を新設**し、本件を実例として条文へ埋め込んだ |
| 2 | **判定者を定めただけで肯定的な承認を前提にしていない。** 編集者が「判定者 = PO」と書けば成立する。「現任者」の決定元も空席時の fail-closed も無い | P1 | 非起因 | (A) | **採用** | 手続 2 へ**一意に同定できなければ解除不可(fail-closed)**、手続 3 へ**判定者の肯定的承認を得た後に限り成立**、手続 4 へ**承認した個人の識別**を追加 |
| 3 | **承認済み例外の切り替えが、新設した手続を満たしていない。** 独立した解除追記(判定者・判断・範囲)が無く、再確認条件はイベント名だけ | P1 | 非起因 | (A) | **採用** | 例外の切り替えを**手続適合の形へ全面改稿**。解除の対象・理由・判定(ゲート手順 5 の承認で成立)・解除後に残る未達を明記し、**再確認の主体・時期・記録先・未確認中の制限**を具体化した |
| 4 | **手続 5「実在する追跡先」が弱い。** 閉じた別タスクや残作業を含まないタスクも「実在」する | P1 | 非起因 | (A) | **採用** | 手続 7 へ**①未完了 ②解除後に残る未達を担当範囲または DoD に含む ③担当者または owner が定まっている**の 3 条件を明記。差し替え先の `TSK-217` が 3 条件を満たすことと、`TSK-324` が**担当者未設定のため差し替え先に用いない**ことを記録した |
| 5 | **10.1 の排他区間の記述が矛盾。** 「base を拘束しないので存続」「保護が利用可能になれば置換」「2026-09-19 適用済み」の併記で、維持か廃止か一意に判断できない | P1 | 非起因 | (A) | **採用** | **射程拡大**。「保護設定へ置き換える」を**撤回**し、`strict_required_status_checks_policy` は **topic を base へ追随させる規則であって base の更新を凍結する機能ではない**ため置換は成立しないと明記。**排他区間は存続**で一意化した |
| 6 | 正本索引が状態・更新日に追随していない(`check_docs_status.py` が 4 件検出) | P2 | 非起因 | (-) | **採用** | `docs/README.md` の設計書・台帳・onboarding・github-setup の 4 行を更新(版列 1.16 → 1.17 を含む)。**`check_docs_status.py` 合格を確認** |

**射程変更**(7.3-6 — 変更履歴 v1.17 行にも記録):

- **《縮小》リスク受容記録の解除を射程から外した**(P0 採用)。理由 = 形式的な充足はあるが実質的な強制が成立していないため
- **《拡大》10.1 の排他区間の「置き換える」見通しを撤回**(P1-5 採用)。理由 = 既存の規範文が本改訂の記述と矛盾し、実行者が一意に判断できないため

→ **次周は全文レビュー**(7.3-6)。

**エスカレーション判定**: 前周修正**起因**の指摘は 0 件(初周のため全件非起因)。7.3-6 の発火条件に該当しない。**6 周警告**: 現在 1 周目。

#### 2 周目(全文 — 射程変更直後)— 採否記録

**判定: 要修正 — P0 0 件 / P1 5 件 / P2 0 件**(判定行あり・件数一致)。**5 件すべて採用・不採用 0 件。5 件とも `起因`**(前周の修正が生んだ指摘)。

| # | 要旨 | 重大度 | 起因 | 分類 | 採否 | 反映内容 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **リモート強制の成否が本文内で矛盾。** 10.2 は「統制は成立していない」、一方 6.3 は「リモート強制済み」・10.1 は「有効化した」と断定。`github-setup.md` 2 章にも「達成した」が残る | P1 | **起因** | (A) | **採用** | **4 箇所を是正**(6.3 / 10.1 マージ条件 / `github-setup.md` 2 章 / v1.17 の変更履歴行)。**「Ruleset が適用され直 push 等が実際に止まること」と「CI の真正な発行元による green が強制されること」を全箇所で書き分けた** |
| 2 | **解除の成立時点と判定者が一意でない。** 手順 5(承認)と手順 6(追記)の間で状態が分岐し、手順 5 の「人間」は手続 2 の「一意に同定された現任 PO」に拘束されていない | P1 | **起因** | (A) | **採用** | **手順 5 の承認者が手続 2 の判定者と同一であることの確認を要件化**し、**成立時点を手順 6 の追記に一意化**。**手順 5〜6 の間は「未成立」**と明記 |
| 3 | **再確認条件が一回で失効する。** 「いずれか早い方」だと先に構成変更で再確認すると `TSK-432` 完了時の義務が消える。列挙も `integration_id`・`strict`・rule 内パラメータを捕捉しない | P1 | **起因** | (A) | **採用** | **「いずれか早い方」を撤回し、2 つを独立した契機**に(どちらを実施しても他方の義務は消えない)。**Ruleset の変更は種類を問わず契機**とし、列挙は**下限であり網羅ではない**と明記 |
| 4 | **手続 7 をリスク受容記録へ適用したという主張が成立しない。** 本記録は解除されないので「解除後に残る未達」が存在せず、`TSK-217` が条件②を満たすことも立証できていない(存続理由の発行元拘束は `TSK-433` に帰属) | P1 | **起因** | (A) | **採用** | **手続 7 は解除時の要件であり本記録にはまだ適用されないと明記**。死んだ参照の是正は別理由(解除時に突合できなくなる)として実施し、**追跡先を目的別に分離** — **解除条件の追跡 = `TSK-433`** / **解除後に残る未達の追跡 = `TSK-217`**。`TSK-324` は担当者未設定で条件③を満たさないため用いない |
| 5 | **射程縮小が承認済み計画書へ反映されていない。** 計画書は「リモート強制未達は解消」「リスク受容記録を解除」を要求したままで、v1.17 と同時に満たせず AGENTS.md 絶対規則 5 と衝突 | P1 | **起因** | (-) | **採用** | 計画書の **1 節の要件表・2 節やること 8・3 節の確定ゲート行・ステップ 8・DoD** を射程縮小へ同期 |

**起因比**: **5/5 = 100%(過半)**。7.3-6 のエスカレーションは「**2 周連続**で過半」が条件のため**本周では発火しない**。**次周も過半なら PO スコープ裁定へ上げる。**

**6 周警告**: 現在 2 周目(7.3-6 の警告は 6 周)。

#### 3 周目(差分)— 採否記録と**エスカレーション発火**

**判定: 要修正 — P0 0 件 / P1 4 件 / P2 1 件**(判定行あり・件数一致)。**5 件すべて採用・不採用 0 件。**

**エスカレーション(7.3-6)が発火した。** 母集団 = 当該周の P0+P1(P2 と (B) は算入しない):

| 周 | 母集団 | 起因 | 比率 |
| --- | --- | --- | --- |
| 2 周目 | 5 | 5 | **100%** |
| 3 周目 | 4 | 3 | **75%** |

→ **2 周連続で過半**。**PO スコープ裁定を起動し、裁定 = `A`(続行指示 + 機械的な全数掃き出し)を得た**(2026-09-21)。

**起因が続いた理由**: 初周の射程縮小が**事実の前提そのものを反転させた**(「リモート強制は達成した」→「していない」)。この前提はステップ 4・5(前段)で**既に 3 文書・6 箇所以上へ書き込まれており**、毎周 1〜2 箇所ずつ発見されていた。**「母集団を人が列挙する検査は、射程が動くたびに黙って古くなる」型**(台帳候補)。

| # | 要旨 | 重大度 | 起因 | 分類 | 採否 | 反映内容 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **6.2 に「三重の強制が揃った」の旧断定が残る。** さらに**計画書の検証仕様表自身が「三重の強制が揃った記述」を期待**しており誤断定を再生産する | P1 | 非起因 | (A) | **採用** | 6.2 を是正。**加えて計画書の検証仕様表の期待値を書き換えた**(誤断定の再生産元)。**裁定 `A` の機械的掃き出しにより、指摘に無かった台帳 `:449`(H-8 事象の追記)と台帳の変更履歴行にも同じ過大主張が見つかり是正**(1 件ずつ追う方式では出てこなかった箇所) |
| 2 | 手順 5 の承認者と現任 PO の同一性を**誰が・どの証拠で**確認するかが未定義。確認できない場合の停止経路も無い | P1 | **起因** | (A) | **採用** | **提案者が確認し、根拠(承認の場・日時・承認者の識別)を手順 6 の追記へ書く**ことを要件化。**確認できない/根拠を書けない場合は解除しない**。**役割名だけを書くことは根拠に当たらない**と明記 |
| 3 | **`TSK-433` の DoD が「指定可否の判断」に留まり、発行元拘束の成立を要求していない。** 判断だけで完了でき、**解除条件が未達のまま追跡先が再び閉じる** | P1 | **起因** | (A) | **採用** | 設計書へ**「追跡先が『判断のみ』で閉じる経路を塞ぐ」**を追加(指定しないと判断した場合は**解除条件を引き継ぐ新追跡先へ差し替えてから閉じる**。手続 7 の条件②は**存続中も継続して満たされる**ことを要求)。**`TSK-433` の DoD も更新**し「発行元拘束の成立」を含めた |
| 4 | 計画書の同期が部分的(影響正本表・ゲート区分・NFR-019 の書き分け・依存順がまだ解除を要求) | P1 | **起因** | (-) | **採用** | 4 箇所すべて同期 |
| 5 | `github-setup.md` の変更履歴に「未達は解消した」が残り、訂正の履歴行が無い | P2 | **起因** | (-) | **採用** | **既存行は履歴として残し、2026-09-21 の訂正行を追記**(追記の規律)。索引も同期 |

**裁定 `A` による機械的掃き出しの結果**: `三重の強制|リモート強制|強制が揃|強制済み|強制される|達成した|有効化した|CI 未達|拒否` で 6 文書を走査。**是正対象は 設計書 6.2 / 台帳 H-8 本文 / 台帳 変更履歴 / 計画書 検証仕様表 2 行 / 計画書 4 箇所 / `github-setup.md` 変更履歴** の計 10 箇所。**うち 3 箇所(台帳 2・計画書の検証仕様表)は今周の指摘に含まれていなかった。**

**検査**: `check_docs_status.py` / `check_plan_docs_sync.py` / `doc_check_invariants.py` **すべて合格**。

#### 4 周目(差分)— 採否記録

**判定: 要修正 — P0 0 件 / P1 3 件 / P2 1 件**(判定行あり・件数一致)。**4 件すべて採用・不採用 0 件。**

**起因比**: 母集団 3(P0+P1)/ 起因 2 → **67%**。**3 周連続で過半**だが、**裁定 `A`(続行指示)は 3 周目に取得済み**であり本周は同裁定の下で継続する。**推移は下降**: P1 件数 5 → 4 → 3、起因比 100% → 75% → 67%。

| # | 要旨 | 重大度 | 起因 | 分類 | 採否 | 反映内容 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **裁定 `A` の掃き出し漏れ。** `github-setup.md` 3 章が「Ruleset を有効化しても**防げる**のは『必須チェックの欠落・failure のままのマージ』と…」と断定。**走査語彙に「防げる」が無かった** | P1 | **非起因** | (A) | **採用** | 当該行を是正(防げるのは「直接 push・force push・削除」と「PR を経ないマージ」まで。**必須チェックの欠落・failure のままのマージは防げていない**)。**走査語彙を `防げる\|止まる\|効いている\|機構的に強制\|拒否する\|拒否される` へ拡張して再走査** — 新たな過大主張は 0 件 |
| 2 | **追跡先の存続規則が条文として実効でない。** 「手続 7 はまだ適用されない」と「条件②は存続中も適用」が矛盾。`TSK-433` を閉じる前に**誰が確認しどこへ記録するか**が無く、判断だけで閉じた場合の検知経路が無い | P1 | **起因** | (A) | **採用** | 矛盾を解消(**「解除時に差し替える」要件は未発動 / 条件①〜③は追跡先である限り継続して満たす**と書き分け)。**確認の主体と記録先を新設** — **閉じる者**が①DoD が解除条件の成立を含むこと ②成立していなければ差し替え先が定まっていることを確認し、**確認結果を 10.2 へ追記する**。**追記のない完了は追跡先の更新として認めない** |
| 3 | **`TSK-433` の契約が文書間で非同期。** 設計書は「発行元拘束の成立」を要求するが、`github-setup.md` 3 章・4 章と計画書 F-2 は実地検証だけを完了条件としている | P1 | **起因** | (A) | **採用** | **4 箇所を同期**(`github-setup.md` 3 章の二段階記述の見出しと完了条件・4 章の `integration_id` 行・計画書 F-2 の表・DoD)。完了条件を**①実測 2 件 ②二段階記述の解消 ③発行元拘束の成立(または解除条件を引き継ぐ追跡先への差し替え)**の 3 点に統一 |
| 4 | **表記崩れ 2 件**(設計書 `存続)。: hooks` / 台帳 `いない。****「解決」`) | P2 | **起因** | (-) | **採用** | 両方修正 |

**検査**: `check_docs_status.py` / `check_plan_docs_sync.py` / `doc_check_invariants.py` **すべて合格**。

#### 5 周目(差分)— 採否記録

**判定: 要修正 — P0 0 件 / P1 2 件 / P2 1 件**(判定行あり・件数一致)。**3 件すべて採用・不採用 0 件。**

**起因比**: 母集団 2(P0+P1)/ 起因 1 → **50%**。**過半を割り、連続記録は途切れた。** 推移: P1 件数 **5 → 4 → 3 → 2**、起因比 **100% → 75% → 67% → 50%**。

| # | 要旨 | 重大度 | 起因 | 分類 | 採否 | 反映内容 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **裁定 `A` の掃き出しが再び未完。** `github-setup.md` 3 章の適用手順が `gh ruleset check` を「両ブランチに全ルールが**効いている**ことを確認」としているが、同コマンドは**適用対象のルールを列挙するだけ**で、push/merge の拒否動作も発行元拘束も実証しない(存在しないブランチ名でも実行できる)。**拡張した走査語彙「効いている」に直接一致しており、前周の「0 件」という全数結果は成立していなかった** | P1 | **非起因** | (A) | **採用** | 当該行へ**限界の注記を追加**(適用対象の列挙にすぎないこと・拒否動作の実証は `TSK-433`・**本コマンドの合格をもって「保護が効いている」と判定しないこと**)。再走査し、残るヒットは是正後の正しい記述か無関係のみ |
| 2 | **追跡先の追跡継続規則がまだ閉じていない。** 確認が閉じる者自身の自己申告に留まり、Notion 側の完了を検知・阻止する主体が無い。また**発行元拘束が成立した場合に「条件が成立したので追跡不要」として閉じると、記録が存続したまま条件①(未完了)を失う** | P1 | **起因** | (A) | **採用** | **fail-closed を明記** — 本書は Notion 側の操作を阻止できないので、**追記なしに閉じられた場合は「追跡先を失った状態」となり、その状態では解除条件の充足を主張できない**(手続 6 と同じ効力)。**回復には条件①〜③を満たす追跡先を定めて追記する**。あわせて**拘束成立時の扱い**を追加 — 閉じる前に**本記録の解除(手続 1〜6)を実施するか、残る解除条件を担う追跡先を定める**。**「条件が成立したので追跡不要」として閉じることは認めない** |
| 3 | `github-setup.md:79` の強調記号が奇数個で範囲が崩れている | P2 | **起因** | (-) | **採用** | 当該行を書き直し(`**` が 10 個 = 対応 OK を機械確認) |

**検査**: `check_docs_status.py` / `check_plan_docs_sync.py` / `doc_check_invariants.py` **すべて合格**。

#### 6 周目(差分)— 採否記録 + **6 周警告の提示**

**判定: 要修正 — P0 0 件 / P1 4 件 / P2 0 件**(判定行あり・件数一致)。**4 件すべて採用・不採用 0 件。**

**起因比**: 母集団 4 / 起因 1 → **25%**。推移: P1 件数 **5 → 4 → 3 → 2 → 4**、起因比 **100% → 75% → 67% → 50% → 25%**。**起因は下がり続けたが件数が増加** — 指摘が「前周の後始末」から**新しい論点**へ移ったことを示す。

**6 周警告(7.3-6)を PO へ提示した**(周回・重大度推移・起因比の一覧)。**PO 判断 = 続行**。あわせて指摘 #3 について **PO 裁定 = `A`(提案者と判定者の同一人物を許し、自己承認である旨の明記を義務化)** を得た。

| # | 要旨 | 重大度 | 起因 | 分類 | 採否 | 反映内容 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **「直 push・force push・削除・PR 外マージは実際に止まる」も実証されていない。** `gh ruleset check` は拒否動作を実証せず、`TSK-433` の完了条件にも 4 経路の負例試験が無い。「実地検証は未完」と矛盾 | P1 | **非起因**(掃き出し不備) | (-) | **採用** | **4 箇所を「設定上は止まる(拒否動作そのものの実測は未実施)」へ書き分け**(設計書 6.2 / `github-setup.md` 2 章・3 章 / `onboarding.md`)。**`TSK-433` の完了条件へ「4 経路の負例試験」を追加**(正本側と Notion 側の両方) |
| 2 | **追跡先の無断完了を検知する主体がまだ無い。** fail-closed を定義しただけで、誰がいつ確認するかが無い | P1 | **起因** | (A) | **採用** | **検知の契機を 3 つ明記** — ①解除を提案する時点(**手続 1 へ「追跡先の状態を Notion で再取得し本節の記載と一致することを確認する」を追加**)②再確認条件の発火時 ③当該追跡先を参照する他の正本・計画書の改訂時。**いずれかで不一致を見つけた者が本節へ追記する** |
| 3 | **提案者と判定者が同一人物の場合の扱いが未定義。** 自己同定・自己確認・自己承認で全部の字面を満たせる | P1 | **非起因** | (A) | **採用** | **PO 裁定 `A` を条文化** — 手続 0 を新設し、**同一人物を許す**(1 人開発で職務分離を要求すると解除が永久に不能。`required_approving_review_count: 0` の正当化と整合)。**ただし手続 4 の追記へ「自己承認である」旨の明記を義務化し、明記のない解除は成立しない** |
| 4 | 計画書 75・244 行が「例外を閉じることはできない」のままで v1.17 と衝突 | P1 | **非起因** | (-) | **採用** | **「期限付きの承認済み例外」(期限到来により解除)と「自動再現要件の未達状態」(スクリプト未実装のため継続)を書き分け**。2 箇所へ伝播 |

**検査**: `check_docs_status.py` / `check_plan_docs_sync.py` / `doc_check_invariants.py` **すべて合格**。

#### 7 周目(差分)— 採否記録 + **2 回目の射程縮小(送り出し)**

**判定: 要修正 — P0 1 件 / P1 5 件**(判定行あり・件数一致)。**6 件すべて採用・不採用 0 件。** 起因比 4/6 = **67%**。

**推移**: P1 件数 **5 → 4 → 3 → 2 → 4 → 6**(**反転して増加**)/ 起因比 100% → 75% → 67% → 50% → 25% → 67%。**7 周・指摘 33 件・全件採用でも収束しなかった。**

**PO 裁定 = 射程縮小(送り出し)**(2026-09-21)。**理由**: 初周の `P0` で**解除そのものを射程から外した**ため、**新設しようとしていた解除手続に適用対象が存在しない**。**適用対象のない手続を机上で完成させようとすると、是正が次の穴を生む閉じた輪になる** — **設計書 v1.7 の確定ゲート 12〜15 周目**および **ADR-003 の PO 裁定(2026-08-19)**と同型の失敗。**7 周目の `P0` がその象徴**で、6 周目に私が追加した「4 経路の負例試験」が**新しい危険(拒否されなければ保護ブランチが実際に更新・削除される)を生んでいた**。

**縮小の内容**: 解除手続 0〜7 の 8 項 → **2 原則のみ**(①形式的な充足で解除しない ②範囲は当該記録が明示した対象に限る)。**詳細は初回の実解除を行う `TSK-435` へ送り出す**(理由・原則が分岐しない根拠・見直しトリガーを明記 — ADR-003 の送り出しと同型)。

| # | 要旨 | 重大度 | 起因 | 分類 | 採否 | 反映内容 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **負例試験に安全な対象・隔離・復旧が無い。** 拒否されなければ**保護ブランチが実際に更新・削除される** | **P0** | **起因** | (A) | **採用** | **安全条件を必須化** — `main`/`develop` を対象にしない(検証専用リポジトリか**同一 Ruleset を適用した使い捨てブランチ**で、**適用 rule 集合の一致を API 実測で立証**)/ 削除の負例は使い捨てのみ / force push・直 push は**直前 SHA を記録し再現可能な状態**で / **各試験の前に復旧手順を書き実施後に確認を記録**。正本・Notion 両方へ |
| 2 | 無断完了の検知契機のうち**義務化されたのは①だけ**。②③は取得・照合を命じていない | P1 | 起因 | (A) | **採用**(**送り出しへ**) | **`TSK-435` の射程に含めた**(「追跡先が満たすべき条件と無断完了の検知契機」) |
| 3 | **手続 0 の追加がゲート契約へ伝播していない**(射程宣言・索引・計画書が「7 項」のまま) | P1 | 起因 | (-) | **採用** | **射程縮小により手続 0〜7 自体が本改訂から外れた。** 射程宣言・索引・計画書を**新射程(2 原則 + 送り出し)**へ全面同期 |
| 4 | 負例試験が承認済み計画書の F-2 契約へ同期されていない | P1 | 起因 | (-) | **採用** | 計画書の F-2 定義と DoD へ**負例試験(安全条件つき)**を追加 |
| 5 | **掃き出しがまだ全数でない。** `github-setup.md` の見出しと**両書の変更履歴**に無限定な断定が残る | P1 | 非起因 | (-) | **採用** | 見出しへ「設定上は」を挿入 / `github-setup.md`・`onboarding.md` の変更履歴の断定を限定 |
| 6 | 計画書 80 行が「本タスクは一時例外を解除する」のままで、設計書がリスク受容を「一時例外」と呼んで存続させているのと混同 | P1 | 非起因 | (-) | **採用** | 「**解除するのは期限付きの承認済み例外だけ**」「**NFR-019 のリスク受容記録は解除しない**」へ書き分け |

**検査**: `check_docs_status.py` / `check_plan_docs_sync.py` / `doc_check_invariants.py` **すべて合格**。**次周は全文レビュー**(射程変更のため — 7.3-6)。

#### 8 周目(全文 — 射程変更直後)— 採否記録

**判定: 要修正 — P0 0 件 / P1 2 件**(判定行あり・件数一致)。**2 件すべて採用・不採用 0 件。** 起因比 2/2 = 100%(ただし**母集団が 2 まで縮小**)。

**推移**: P1 件数 **5 → 4 → 3 → 2 → 4 → 6 → 2**。**射程縮小により 6 → 2 へ急減。** 射程外への指摘は 0 件で、**依頼文で射程を明示した効果が出ている**。

| # | 要旨 | 重大度 | 起因 | 分類 | 採否 | 反映内容 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **承認済み例外の解除の成立時点が 2 つある。** 「手順 5 の承認をもって成立」と「手順 6 で記入されるまで成立しない」が同居し、**手順 5〜6 の区間が「解除済み」とも「例外存続」とも読める** | P1 | **起因** | (A) | **採用** | **成立時点を 1 つに一意化** — **本行へ承認日・承認者が記入された時点(手順 6)で成立**。**手順 5 の承認は成立の必要条件であって成立時点ではない**。手順 5〜6 の間は「未成立」で**承認済み例外は従前のまま存続**。記入欄も明示的に置いた。**7 周目の射程縮小で全面改稿した際、2 周目に一度解消した曖昧さを再導入していた** |
| 2 | **送り出しの理由が本改訂の射程と矛盾。** 「解除そのものを射程から外したため適用対象がない」としているが、**期限付き例外は本改訂で実際に解除している**。手続詳細の不足ではなく**適用対象の有無そのものの矛盾** | P1 | **起因** | (A) | **採用** | **理由を正した** — 本改訂が実施する解除は **1 件(期限付き例外)だけで、それは 2 原則だけで判定できる**(「期限が到来したか」という客観的事実 + 原則 2 による範囲の限定)。**送り出した詳細が必要になるのは条件の充足を判断する解除** = リスク受容記録の解除であり、**それを射程から外したため適用対象がない**。**あわせて「今回の例外解除にも 2 原則は適用される」(原則 2 で範囲を限定した)ことと「送り出しによって現在の解除に穴は開かない」ことを明記** |

**検査**: `check_docs_status.py` / `check_plan_docs_sync.py` / `doc_check_invariants.py` **すべて合格**。

#### 9 周目(差分)— **合格**

**判定: 合格 — P0 0 件 / P1 0 件 / P2 0 件。** 指摘なし。7.3-2 の終端遷移表により**反映なし・カウントせず、最終全文確認周へ進んだ**。

#### 最終全文確認周(1 回目)— 採否記録

**判定: 要修正 — P0 0 件 / P1 1 件**(判定行あり・件数一致)。**1 件採用・不採用 0 件。**

| # | 要旨 | 重大度 | 起因 | 分類 | 採否 | 反映内容 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **8 周目で本文(10.2)は是正した送り出し理由が、変更履歴 v1.17 行の射程宣言に古いまま残っていた。** 同じ行が「期限付き例外を本改訂で解除する」とも書いており**射程宣言が自己矛盾**。「文言は直したが実効がない」型の**伝播漏れ** | P1 | **起因** | (-) | **採用** | 射程宣言を本文と同期 — 原因を「**本改訂の解除は 1 件で 2 原則だけで判定できる一方、送り出した詳細が必要になるのは条件の充足を判断する解除(= リスク受容記録の解除)であり、それを射程から外したため詳細手続に適用対象が存在しない**」へ。**今回の解除にも 2 原則が適用されていること**と**送り出しによって現在の解除に穴は開かないこと**も追記。機械確認: 旧文言 0 件 / 新文言 2 箇所(本文・射程宣言) |

**7.3-2 の終端遷移**: 最終全文確認周で P0/P1 あり・採用ありのため、**周回 +1 して差分再レビューへ戻る**。

**検査**: `check_docs_status.py` / `check_plan_docs_sync.py` / `doc_check_invariants.py` **すべて合格**。

#### 10 周目(差分再レビュー)— **合格**

**判定: 合格 — 指摘ゼロ。** 反映なし・カウントせず、**最終全文確認周を再実施**(7.3-2)。

#### 最終全文確認周(2 回目)— **合格・収束**

**判定: 合格 — P0 0 件 / P1 0 件 / P2 0 件。** 指摘なし。**7.3-2 の終端遷移表により収束**(収束確認周のためカウントしない)。**→ 人間承認へ。**

---

### 確定ゲートの総括

| 周 | 種別 | 判定 | P0 | P1 | P2 | 起因比 | 備考 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 初 | 全文 | 要修正 | **1** | 4 | 1 | — | **P0 により射程縮小**(解除の取り下げ)+ **拡大**(10.1 の置換撤回) |
| 2 | 全文 | 要修正 | 0 | 5 | 0 | 100% | |
| 3 | 差分 | 要修正 | 0 | 4 | 1 | 75% | **エスカレーション発火 → 裁定 `A`**(続行 + 全数掃き出し) |
| 4 | 差分 | 要修正 | 0 | 3 | 1 | 67% | |
| 5 | 差分 | 要修正 | 0 | 2 | 1 | 50% | |
| 6 | 差分 | 要修正 | 0 | 4 | 0 | 25% | **6 周警告 → 判断 = 続行**。`P1#3` に**裁定 `A`**(自己承認を許し明記を義務化) |
| 7 | 差分 | 要修正 | **1** | 5 | 0 | 67% | **2 回目の射程縮小(送り出し)** |
| 8 | 全文 | 要修正 | 0 | 2 | 0 | 100% | 射程外への指摘 0 件 |
| 9 | 差分 | **合格** | 0 | 0 | 0 | — | 最終全文確認周へ |
| 最終(1) | 全文 | 要修正 | 0 | 1 | 0 | 100% | **伝播漏れ** → 差分再レビューへ戻る |
| 10 | 差分 | **合格** | 0 | 0 | 0 | — | 最終全文確認周を再実施 |
| 最終(2) | 全文 | **合格** | 0 | 0 | 0 | — | **収束** |

**合計: レビュー実行 12 回(全文 6 + 差分 6)/ 指摘 42 件 / 全件採用・不採用 0 件 / 反映周 9 回。**

**P0 は 2 件**、どちらも**正本へ誤りを書き込む寸前で止めた**:

1. **初周** — `integration_id` 未指定で**統制が成立していない**のに「解除した」と書こうとしていた。→ **解除を撤回し、条件を追加して存続**
2. **7 周目** — **保護ブランチを実際に更新・削除しうる負例試験**を安全条件なしで指示しようとしていた。→ **安全条件を必須化**

**射程変更 2 回・PO 裁定 3 回**(エスカレーション時の続行指示 / 自己承認の可否 / 送り出し)。**7 周目の送り出しが転機**で、P1 件数は 6 → 2 → 0 と落ちた。

### 人間承認と approved 化(手順 5〜7)

**PO 承認: 2026-09-21・山田正輝。** 採用/不採用の一覧を提示して明示的に承認を得た(**採用 42 件・不採用 0 件**)。

**手順 6 の実施**:
- 設計書 frontmatter を `in-review` → **`approved`**
- 変更履歴へ **v1.17 の「確定ゲート通過(approved)」行**を追加(1 版 = 2 行の既存の書き方に従う)
- **10.2 の承認済み例外の解除が成立**(承認日・承認者を本行へ記入した時点で成立 — 記入前は未成立という規定どおり)。**自己承認である旨を明記**(6 周目の PO 裁定 `A`)
- `docs/README.md` 索引を現行化(版 1.17・状態 approved)
- 検査 3 種すべて合格

**手順 7 のレビュー往復の要点**:

| 項目 | 内容 |
| --- | --- |
| **最も重い指摘** | **初周 `P0`** — `integration_id` 未指定で統制が成立していないのに解除しようとしていた。**この 1 件が「解除しない」という結論を生み、改訂の性格を変えた** |
| **2 番目** | **7 周目 `P0`** — 6 周目に自分で追加した負例試験が、**保護ブランチを実際に壊しうる**危険を持っていた。**是正が新しい危険を生んだ例** |
| **警告の発動** | **エスカレーション 1 回**(3 周目 — 2 周連続で起因が過半)/ **6 周警告 1 回**(6 周目)。いずれも PO 判断は**続行** |
| **射程変更** | **2 回**(初周後の縮小 + 拡大 / 7 周目の縮小 = 送り出し)。いずれも変更範囲と理由を変更履歴・worklog へ記録し、次周を全文レビューにした |
| **収束しなかった構造的原因** | **適用対象のない手続を机上で完成させようとしていた**。初周で解除を射程から外したため、新設中の解除手続に適用対象が無くなっていた。**v1.7 の確定ゲート 12〜15 周目・ADR-003 の PO 裁定(2026-08-19)と同型** |
| **転機** | **7 周目の送り出し**。P1 件数は 6 → 2 → 0 と落ちた |
| **繰り返し出た型** | **伝播漏れ**(本文を直して変更履歴・索引・計画書に残る)が 4 周で発生。**最終全文確認周が 1 回目で捕まえた**のもこの型 — 「approved になる版そのものを必ず全文確認する」規定が実際に効いた |

(以下、反映周ごとに追記)

## ステップ 9: 索引の現行化と、機械検査が覆わないファイルの明示的確認(2026-09-21)

**索引(`docs/README.md`)はステップ 8 の approved 化の中で更新済み。** 本ステップでは**突合と、機械検査の射程外にあるファイルの目視確認**を行った。

### 機械検査の射程外(計画書 4 節の是正どおり)

`check_plan_docs_sync.py` の対象は **`docs/**/*.md`** で、**`.md` 以外は `suffix` で除外**される。したがって次の 2 ファイルは**同スクリプトが一切見ない**:

| ファイル | 射程外の理由 | 目視確認の結果 |
| --- | --- | --- |
| `.gitleaksignore` | **`.md` ではない**(ルート直下) | **fingerprint 3 件・理由コメント 22 行**。3 件はステップ 1 の判定記録と 1 対 1(`5c8f52c`:25 / `5c8f52c`:27 / `190fc3a`:25) |
| `.claude/skills/release/SKILL.md` | **`docs/` 配下ではない** | 旧記述(「ブランチ保護が使えないため機構では強制できない」)が消滅し、**排他区間の存続**へ書き換わっている。**手順 0 の無条件中断は保持**(機械確認: 1 件) |

### 索引 vs frontmatter の突合

| 正本 | frontmatter | 索引の版 | 最終更新 |
| --- | --- | --- | --- |
| 設計書 | `approved` | **1.17** | 2026-09-21 |
| `github-setup.md` | `approved` | 1.2 | 2026-09-21 |
| `onboarding.md` | `approved` | 1.2 | 2026-09-21 |
| 台帳 | `approved` | 1.0 | 2026-09-21 |

### 検査

| 検査 | 結果 |
| --- | --- |
| `check_docs_status.py` | **合格** |
| `check_plan_docs_sync.py` | **合格** |
| `doc_check_invariants.py` | **合格** |
| `uv run ruff check .` | **All checks passed** |
| `uv run ty check` | **All checks passed** |
| `uv run pytest tests/` | **1394 passed**(634.89s) |

## 台帳候補(本タスクのスコープ外 — 起票は別途)

**型 1: 1 件見つけた時点で走査を止める(母集団の打ち切り)**
`420` タブが 3 回の実測を挙げている。本タスクでも 1 件発生 — 設計書の変更履歴表を 9 行目まで読んで「v1.16 は in-review」と結論したが、10 行目に同じ版の approved 行があった(**1 版 = 2 行**の構成)。

**型 2: 典拠として当てる規則を取り違える(規則の誤選択)— 走査を尽くしても防げない**

- **事象 1(本セッション)**: 上記の版状態の判定で、「変更履歴表が状態の正」という**存在しない規則**を当てた。設計書 **7.1-4** は逆を定める — 「冒頭の変更履歴表は状態遷移の『履歴』であり、**現在状態の正ではない**(現在状態の唯一の正は frontmatter — 5 項)」(設計書 `:413`)。AGENTS.md の「版の正は同書の変更履歴」は**版番号**の規定で状態の規定ではなく、2 つを混同した。
- **事象 2(`420` タブ)**: 同じ論点への反論として「10 行目に approved 行がある・v1.15 と同じ 2 行構成」と述べた。**結論は正しいが、根拠として同じ変更履歴表を権威に用いている**。正しい答えは「frontmatter が approved で、それが唯一の正」の一行。
- **型 1 との違い**: 事象 2 は 4 行を読み切っており、走査の打ち切りでは説明できない。**当てる文書の選択が誤っている**ため、全行読んでも同じ誤りに着く。
- **誘因**: 「それらしい表」が目の前にあること。2 セッションが同じ表に引かれ、片方は in-review と読み、片方は approved の行を探した。
- **対処の方向**: 母集団を広げても効かない。**「この問いの正はどこか」を先に確定してから読む**。

**型 3: 記録が欠けている箇所を、推定で埋めて肯定形で書く**(`420` タブ提供・2026-09-20)

- **事象**: `420` タブが TSK-421 のカード・計画書へ「落ちたのは証跡の仕組み(R-2)とその周辺**だけ**である」と断定形で記述。TSK-421 タブが全木検索したが典拠なし。**PR #63 の 3 周目は否決 P0 2 / P1 2 の計 4 件で、内容が残るのは worklog の 1 行要旨 1 件のみ**。他 3 件の記録は両木に存在せず、R-1・R-3〜R-6 の採否は**不明**。撤回済み。
- **型 1・型 2 との違い**(TSK-421 タブの指摘・`420` タブが受諾): 型 1 は**測っている**が範囲が狭い(対策 = 範囲を広げる)。型 3 は**測っていない・記録が最初から無い**(対策 = 広げようがない。**「不明」と書く規律だけ**)。
- **本セッションで確認した追加事実(原典)**: **規律は既に存在する** — 設計書 **8.5**「**出典必須**: 回答には必ず典拠(`ファイルパス:行` または節番号)を付ける。**典拠が見つからないことは「不明」**…」。ただし **射程が「調査サブエージェントの回答」に限定**されている。`.claude/skills/investigate/SKILL.md` と `research-template.md` も**調査メモ限定**。**計画書テンプレート・Notion カードには掛かっていない**。
- **したがって候補の言い方は「規律が無い」ではなく「規律の射程が調査メモに限定され、計画書・カードを覆っていない」**。対策も「規律を作る」ではなく「射程を広げる」になる。**型 2(機械検査の射程が狭い)と同根**だが、こちらは機械検査ではなく**人間向けの規約の射程**。

**型の採番(master の worklog が出どころ・`420` タブと合意 2026-09-20)**: 型 1 = 母集団の打ち切り(測る範囲)/ 型 2 = 規則の誤選択(当てる文書)/ 型 3 = 記録の欠落を推定で埋める(測っていない)/ **型 4 = 規律・検査の射程が、それが代表していると読める範囲より狭い**。実測は 型 1 = 4 / 型 2 = 4 / 型 3 = 1 / 型 4 = 4。

**型 4 の実測 4 件**: ① `core_guard` が `- [x]` の 1 行だけを見て `- 実施記録:` を見ない ② `feature_status.py` / `codex_run.py` が記入済み行 1 つで OK を返す(H-89 の fail-open 面)③ `check_citation_format` が registry の 1 文書(`docs/design/sync-protocol.md`)しか見ない ④ **設計書 8.5 の「典拠が無ければ不明と書く」が調査サブエージェントの回答に限定**され、計画書・カードを覆っていない。**本タスクでも計画レビュー 1 周目で同型を踏んだ**(`check_design_propagation.py` を設計書の合格条件に置いていた — 撤回済み)。

**H-77 の更新候補(2026-09-20 — `420` タブと合意)**: 型の採番が 2 セッション間で衝突した(先方が「型 2」を別の意味で使用)。台帳 **H-77「並行ブランチが台帳へ追記すると `H-*` 番号が衝突する」**と**同じ機構**(共有名前空間への採番に払い出し機構が無い)。**新規項目ではなく H-77 への追記**が適当 — 事象が同一で、別立てにすると再発回数が割れて頻度が読めなくなる。**既存記述は書き換えず追記**する(台帳 `:18` の先例「既存候補 1 件へ解消の追記」に倣う。既存の対応案を消すと「一度はこの 3 つで足りると考えた」判断の記録が失われる)。**番号は H-77 のまま**(台帳 `:107`「**番号は追記のみ — 振り直さない。欠番も再利用しない**」)。

更新点は 3 つで、**依存関係がある**:

| | 更新点 | ① への依存 |
| --- | --- | --- |
| **①** | **射程を「共有名前空間への採番」まで広げる**(現行の事象は「`H-*` の番号は各ブランチが台帳の最大値 + 1 を自分で決める運用」で、対象は**台帳の `H-*`**。今回衝突したのは**作業中の型番号**なので現射程では当たらない) | — |
| **②** | **「② 誤った振る舞いの実証 = なし(回避したため)」が覆る** — 今回は回避ではなく、誤った参照が**実際に書かれセッション間で伝達された**(起票前に master が指摘して停止) | **依存する**。①なしで②だけ書くと「台帳の `H-*` は 1 件も衝突していないのに実証あり」の形になる。①が通らなければ②は現射程では依然「なし」 |
| **③** | **既存の対応案 3 つ(最大値を見る手順の明文化 / ブランチ名由来 / NFR-021 型の予約方式)がいずれも効かない経路だった** — 3 つとも台帳上の番号を前提にしており、台帳に存在しない番号には届かない。**今回を捕まえたのは機構ではなく人間の指摘**。あわせて**広げた側の対策 ④「台帳へ入る前は番号を使わず内容で呼ぶ」**(機構不要) | **前半(効かない)は①なしでも成立**。**後半(④)は①の上でのみ意味を持つ** |

**優先度の再判定が要る** — 実証が出ただけなら優先度が上がるだけだが、**対応案が全滅**なら項目の書き直しが要る。

**射程で切る整理**(`420` タブが採用・この往復から適用): 台帳へ入る前(worklog・カード・セッション間)= **番号を使わず内容で呼ぶ**(機構不要。失敗の原因が共有名前空間そのものなので、使わなければ失敗しない)/ 台帳内(`H-*`)= **機構が要る**(相互参照に識別子が要るため回避案を適用できない)。**これにより射程拡張(①)と対策(④)が対応づく。**

## 結果サマリ(/pr クローズ処理)

**実装ステップ 9/9 完了・確定ゲート収束・PO 承認済み。**

| 何を | どこへ反映したか |
| --- | --- |
| **公開の根拠の記録** | `github-setup.md` 1 章(範囲と取得方法のみ。**個人を特定する情報は書かない**) |
| **プラン制約の解消と選択肢表** | 同 1 章(既存記述は**2026-08-10 時点の記録**と明示。**一般評価は変えず**採用した事実のみ追記) |
| **NFR-019 の書き分け・手続 5 の終了** | 同 2 章(**リモート強制は成立していない** — 発行元未拘束。手続 3・4 は継続) |
| **二段階記述と JSON の全現値同期** | 同 3 章(**`require_extra_approval_for_unattributed_changes` のキー欠落を是正** — 旧 JSON の再適用で実設定が失われる状態だった) |
| **`.gitleaksignore` の新設** | ルート直下 + 同 4 章(**再発することを明記**) |
| **縮退記述の現況化** | 設計書 9 節(6.4 の過去事実は保持) |
| **承認済み例外の解除・解除の 2 原則・排他区間の存続** | 設計書 10.1 / 10.2(**v1.17 approved**) |
| **現況化** | `onboarding.md` / 台帳 **H-8**(部分対応)/ `release/SKILL.md`(手順 0 は保持) |
| **索引** | `docs/README.md` 4 行 |

**起票した後続タスク**: `TSK-432`(再現スクリプト)/ `TSK-433`(実地検証 + 発行元拘束 + 4 経路の負例試験)/ `TSK-434`(ruleset へ `tenant-boundary-bypass`)/ **`TSK-435`(解除手続の詳細 — 確定ゲートからの送り出し)**。

**取り下げ**: `TSK-428`(本タスクと重複。内容は参照資料として残置)。

### 台帳への追記の判断(/pr 手順 1-3)

**追記した**: **H-68 へ 4 例目**。本タスクの確定ゲートが**レビュー 12 回・反映 9 周・指摘 42 件**を要し、**P1 件数が 5→4→3→2→4→6 と反転**した。**H-68 の対応策(射程宣言 7.3-7・エスカレーション 7.3-6)は入っており、エスカレーションは 3 周目に実際に発火した**が、**7.3-6 の裁定の選択肢に「実装時確定への送り出し」が明示されていない**ため作成者が候補として提示せず、**送り出しに至ったのは 7 周目で 4 周遅れた**。対応案 2 件を追加。

**追記しなかったもの(理由つき)**:

- **型 1〜4(母集団の打ち切り / 規則の誤選択 / 記録の欠落を推定で埋める / 規律・検査の射程が狭い)と H-77 の更新候補** — **`420` タブとの共同の材料**で、**起票者が未決**(本セッションから PO へ諮ったが回答待ち)。**両タブから重複起票するのを避けるため本 PR では追記しない。** 材料は本 worklog に逐語で残っている(「台帳候補」節)
- **`AGENTS.md` の「`ruff format` は未導入 — 走らせない」が root の話で backend では CI が実行している**件(`tsk235-dsl` タブの実測) — **本タスクの射程外**(backend を触らない)。型 4 の実例になりうるが**こちらでは確認していない**ため候補にも入れない

## PR 全体の敵対レビュー(PR #75 作成後・2026-09-21)

**確定ゲートは設計書 v1.17 のみを対象**としていたため、**PR に含まれる他の変更**(`github-setup.md` 全 4 章 / `.gitleaksignore` / `onboarding.md` / 台帳 / `release/SKILL.md` / `docs/README.md`)を **PR 全体の差分**(1063 追加 / 29 削除・10 ファイル)で敵対レビュー(sol xhigh)にかけた。

**判定: 要修正 — P0 0 件 / P1 4 件 / P2 1 件。4 件採用・1 件不採用。**

**レビュアが独立に検証して妥当と確認した点**(こちらが最も確認したかった箇所):
- **`.gitleaksignore` の誤検知判定** — 「3 件の値は**実際に資産から再計算できる SHA-256 と一致**しており、誤検知判定自体は妥当」
- **公開許諾の記録** — 「個人情報を記載せず範囲・条件・期限・取得方法を示せている」

| # | 要旨 | 重大度 | 採否 | 反映/理由 |
| --- | --- | --- | --- | --- |
| 1 | **`TSK-433` の完了条件が設計書 v1.17 と矛盾。** 設計書は「発行元拘束の成立を含み、判断だけでは満たさない」とするが、`github-setup.md`(2 箇所)と計画書(1 箇所)は**「成立しなければ別の追跡先へ差し替えて閉じる」経路を許す** → v1.17 が指定した追跡タスク自体を解除条件未達のまま完了できる | P1 | **採用** | **7 周目の射程縮小で設計書から除いた経路が下位 2 文書に残っていた**(伝播漏れ)。**3 箇所すべて削除**し「**成立しない限り閉じられない**(解除条件が満たされないだけで fail-closed でよい)」へ |
| 2 | **負例試験が最終 Ruleset 構成へ拘束されていない。** 試験後に設定を変更しても再試験を要求しない。隔離先も「rule 集合の一致」しか求めず、実効構成との同一性を証明しない | P1 | **採用** | **最終構成への拘束**を追加(試験後に Ruleset へ**種類を問わない**変更を加えたら再試験 / 試験時点の Ruleset を API 取得して証跡に含め完了時点と照合)。**隔離先の同一性**も強化(**API 投影の全 rule 全パラメータ・対象 ref の条件・実行主体**。立証できない項目は**その項目について試験が成立していないと記録**) |
| 3 | **H-68 の状態が「対応済み」のまま**なのに未実施の対応案を追加した → **追加対応が追跡対象にならない** | P1 | **採用** | 状態を **「対応中(2026-09-21 — 4 例目を受けた追加対応が未実施)」**へ。**旧状態は残置** |
| 4 | 台帳の自己規律は状態を「未対応/対応中/対応済み/見送り」に限定しているのに、H-8 で**新しい状態値「部分対応」を使った** = 分類の拡張であり版繰り上げ + 確定ゲートが要る | P1 | **不採用** | **「部分対応」は新しい値ではない。** 台帳内で**既に 5 箇所**が使用している(2026-08-16・F1 / 08-23・root-lint-typecheck / 09-01・gate-convergence-rules / 09-04・core-area-paths ほか)。**分類を拡張したのは本 PR ではない。** ただし**自己規律の列挙(`:120`)が実態に追いついていない**のは事実で、これは**「規律の射程が実態より狭い」型**に当たる → **台帳候補へ送る**(本 PR では扱わない) |
| 5 | **`dir` スキャンの記録が矛盾。** 正本は作業ツリーで「上記 3 件」が検出されるとするが実測は 2 件。また worklog は ignore で `dir` 検出ゼロとする一方、正本は形式違いで一致しないとする | P2 | **採用** | 正本を **「`dir` では作業ツリー上の 2 件が検出される」**へ是正(全履歴 3 件との差は `auth_catalog` のダイジェスト更新による再検出)。**抑止の実測は run `35513315849` の `secrets` success** と明記。**worklog の「検出ゼロ」は `--gitleaks-ignore-path` に dir モードの fingerprint を与えた機構確認**であり、実ファイルの検証ではない旨を本節に記録 |

**不採用 1 件は本タスクで初めて**(確定ゲート 42 件 + 計画レビュー 24 件はすべて採用だった)。

## develop(PR #72 マージ後)の取り込み(2026-09-23)

**経緯**: PR #72(U-T1 テナント境界の強制点)が develop へマージされ(`871fd97`)、本 PR と正本 3 件で衝突した。

**衝突の解決(2 件)**:

| ファイル | 衝突 | 解決 |
| --- | --- | --- |
| `docs/README.md` | 設計書行(v1.17 / v1.16)・台帳行(候補 63 件 / 66 件)・`github-setup.md` 行 | 設計書 = **v1.17**(本 PR 側が新しい)。台帳 = **66 件**(#72 が候補 3 件追加)に本 PR の `H-8` / `H-68` 追記を合成。`github-setup.md` 行は両者の追記を合成 |
| `docs/development/harness-evaluation.md` | 変更履歴表の先頭(本 PR 2 行 / #72 の 1 行) | **3 行とも残す**(本 PR の TSK-429 2 行を上、#72 の U-T1 行を下) |

`docs/development/github-setup.md` は自動マージで解決した(3 章 JSON は `tenant-boundary-bypass` を含む 10 件で整合)。

**#72 のマージで偽になった本 PR の記述を訂正した(3 件)**:

1. **3 章 二段階記述①** — 「下記 JSON は 2026-09-20 に GitHub API で実測した全現値と**一致する**」は、#72 が JSON へ 1 件追加したことで**偽になった**。「実測値へ `tenant-boundary-bypass` を加えたもの/同 context は未適用」へ改めた
2. **3 章へ実測を追記** — **実 Ruleset(id `23694095`)は 2026-09-23 時点で 9 context のままで `tenant-boundary-bypass` は未適用**(`gh api` で実測)。**同ジョブは現に必須チェックではなく、失敗しても走らなくてもマージを阻止しない**状態だった

   **→ 2026-09-24 に適用した(PO 指示)。** 適用前に**正本 JSON と実 Ruleset を全項目で突合**し、**差分が `tenant-boundary-bypass` の 1 件だけ**であることを確認してから `PUT /repos/masaki1025/pitchlog/rulesets/23694095` を実行(`conditions` / `bypass_actors: []` / `strict_required_status_checks_policy` / `require_extra_approval_for_unattributed_changes` / `allowed_merge_methods` はいずれも一致)。適用後の実測 = **必須 10 context**・`enforcement: active`・`bypass_actors: []`・`current_user_can_bypass: never`。**`integration_id` は追加分を含め全 10 context とも未指定**なので、**設計書 10.2 のリスク受容を解除しない結論は変わらない**。3 章の二段階記述①と同時更新の項を実態へ書き戻した
3. **件数を本文から外した** — `github-setup.md` の「`integration_id` は未指定(**9 context** とも)」と設計書 10.2 の「必須 **9 context** とも」を、いずれも「**全 context とも**」へ。**件数を書くと必須ジョブが増えるたびに古くなる**ため(本件がその 1 例目)。`integration_id` は追加される context も含めて未指定であることを実測で確認したので、**10.2 のリスク受容を解除しない結論は変わらない**

**台帳候補(本タスクのスコープ外 — 起票は別途)**:

- **正本が要求する「本書と Ruleset の同時更新」に機械検査が無く、PR のマージだけで文書側が先行する** — `github-setup.md` 3 章は「必須ジョブの**追加・削除・改名時は本書と Ruleset を同時更新**する」と定めるが、**#72 のマージで文書は 10 context・実設定は 9 context に分岐した**。**分岐したこと自体を検出する経路が無い**(CI は Ruleset を読まない)。**実害**: 新設した迂回検査が必須チェックとして効いていない状態が、誰にも気づかれずに続く

## 決定

## 未決・次の一歩

- **人間判断待ち 3 件**: ① `github-setup.md` 2 章へ実施記録の未検査ポインタを残すか ② 設計書を行番号で引く他タスクの棚卸しを入れるか(**外す側に寄っている** — `docs/features/**` は一時領域で射程外)③ 上記台帳候補と実施記録の穴を誰が起票するか
- PR #71 マージにより develop が `5e9ffd9` へ。**設計書 1033 → 1171 行**で計画書の行番号参照 12 箇所が全て無効化。内容ベースで再導出済み(反映はレビュー結果と同じ周で行う)
