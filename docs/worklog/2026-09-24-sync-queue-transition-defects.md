---
date: 2026-09-24
topic: 同期キュー遷移の矛盾 2 件(U-13・U-14)
branch: feature/sync-queue-transition-defects
---

# 作業ログ: 2026-09-24 同期キュー遷移の矛盾 2 件(U-13・U-14)

## やったこと

- /task-start: worktree 作成、Notion TSK-329 を `進行中` へ、計画書・worklog の雛形作成。
  カードに DoD 欄が無かったので追加した。重さ分類は**コア領域**(同期プロトコル)
- /investigate: 調査 3 本(spec-checker / decision-tracer / Explore)を並列実行し
  [research.md](../features/sync-queue-transition-defects/research.md) へ統合

## 決定

- 調査メモの記法: **[確認済]**(Claude が原典を読んだ)と **[報告]**(エージェント報告のまま)を区別する
- エージェント報告の誤りを 1 件訂正 — 7-2 節内の `B1` は **`:1011` と `:1017` の 2 箇所**(1 箇所ではない)。
  実施順の制約が変わるため research.md 7 節に裁定として記録した

- /plan: 計画書(6 節)+ 詳細設計を作成。密度が高いので design.md を分離した
- **U-14 の方式を人間が裁定(2026-09-24)**: 「選択規則を明文化」— 境界結果は未使用 D5 の V12 照合で決まり、
  保存済み退避結果の再掲は境界結果を決めない。`B1` 側 2 箇所・`B4` 側 3 箇所のどちらも削らず条件を補う
- ベースラインを自分で実測: `check_design_propagation` / `check_doc_coverage` / `check_docs_status` の 3 検査とも rc=0
- 敵対レビュー(`codex_run.py review adversarial`)を実行中

## 決定(追記)

- **U-14 の規則は新設ではなく、6-2「処理段階の列挙」から導かれる既存の意味論**だと判明した。
  段階 ④ が保存済み結果を「再掲する候補」へ分類し、**未使用 D5 だけ段階 ⑤ へ進む**。
  `B4` は段階 ⑤ でしか生じないので、**未使用 D5 を持たない要求は `B4` になり得ない**。
  → design.md 2-2 の根拠を 7-1 P4(二次)から **6-2 処理段階表(一次)** へ差し替えた。
  あわせて `U-14` の欠陥の所在が「**7-1 総括・7-3 手順 3・10-2 が `B4` を無条件に述べていて 6-2 段階 ⑤ と食い違う**」と特定できた
- 改訂ゲートは **finalize-doc**(版 v0.3 → v0.4 の構造的変更 — 設計書 7.6-3)と判定

## 未決・次の一歩

- **最重要発見**: 機械検査される不変条件 `SP-01` が「7-2 の遷移条件列は **A5 の結果だけ**をキーとし、
  `B4` を条件にすることを forbidden」と既に定めている。→ **`U-14` の未決はサーバーの応答分類・`D3`・通知に限られ、
  クライアントのキュー遷移条件は未決ではない**
- 実装(`queueTransition.ts` の `QT-02`/`QT-08`)も既に A5 のみで遷移しており、**正本の `B1` 限定文言だけが浮いている**
- `U-14` 未決が `DI5` の実装を実際にブロックしている(`canonOracle.ts` / `idempotencyCollision.ts`)
- 次: /plan。`U-14` の応答分類(C)は**人間の方式判断**なので、判断材料 3 軸を提示して裁定を仰ぐ

## 敵対レビュー(計画)

| 周 | 指摘 | 反映 |
| --- | --- | --- |
| 1 | P0 2 / P1 5 / P2 1 | **全件採用**。P0-1 = `U-14` の規則が `B2`/`B3` を落としていた(段階 ④ が `B3b` 候補を作り段階 ⑥ が確定させる)。P0-2 = キュー空判定の自己矛盾 → **「空」を 4 用途へ分離** |
| 2 | P0 2 / P1 3 / P2 1 | **P0 2 件・P1-1・P1-2 の正本側・P2-1 を反映**。P0-1 = **`B7` 経路の脱落**(7-1 の A3「ACK が返るのは `B1`〜`B4` に限る」・段階 ⑧ が `B7` へ分岐)→ 規則を **ACK 経路限定**にし `B5`〜`B7` 行を追加。P0-2 = **`Q5` は「未送信 0 件」で発火させられない**(7-5「拒否・退避・未処理が 1 件でも残れば完了としない」)→ ③ を再定義し **③-b** を新設 |

| 3 | P0 2 / P1 1 / P2 3 | **全件採用**。P0-2 = ③ を「当該試合に `退避済み` が無い」とすると**退避が一度起きた試合で `Q5` が恒久的に出なくなる**(`退避済み` は `U-3` まで自動破棄されない)→ **エッジ条件**へ。7-5 の完了判定は「試合終了宣言後の初回同期」限定であり流用不可と判明 |
| **4** | **P0 3 / P1 5 / P2 1 — 増加** | **閉じた輪と判定**。9 件中 8 件が 3 周目の反映そのものへの指摘 |

### 4 周目 — 閉じた輪の診断と PO 裁定

**周回 8 → 6 → 6 → 9 で増加に転じた。**原因は 3 周目に私が
「`N1`〜`N5` の全確定 + 発火ロジック + exact-set テスト」という**強い述語を足したこと**。
台帳 **H-22**(自分が新設した規定の穴を次の周が突く)・**H-68**(実機のない段階で細部まで確定させようとすると
閉じた輪に入る)・**「強い述語が細部を無限に呼ぶ輪」**(TSK-335 — 述語を弱めて収束した 1 例目)と同型。

**PO 裁定(2026-09-24)= (a) 範囲確定 + (d) 述語を弱める**:

- **通知契約 `N1`〜`N7` の確定と `syncNotices.ts` の実装を [TSK-441](https://app.notion.com/p/3e493b75e68781a09230e4ddea5b0438) へ分離**(新規起票)
- 正本で閉じるのは「各境界結果で通知が出るか出ないか」と「`B4` 専用文面を流用しない禁止」まで
- **各件に受入証跡の形(対象/範囲/方法)と「なぜ機械で閉じないか」を付けて送る**(design.md 4-2)
- **`changeOperationGate`(FR-007 の安全条件)は残す** — 乖離を残すと正史が壊れる経路が開くため
- **`DI5` は変更しない**と判定(`B4` は段階 ⑤ で確定し `DI5` の述べる段階 ⑥ の走査に入らない)。理由を 11-4 に残し逐行確認の対象にする

4 周目の P0 のうち**輪の外側だったのは P0-2(`DI5` 伝播)のみ**で、これは判定として処置した。
P0-1(③-b が ③ の否定になっていない → `E ∧ ¬X` / `E ∧ X` へ)と
P0-3(`B7` を一律ロールバックとした → 「クライアントへ返らない」まで。通信・ACK 消失の `B7` は部分確定がありうる)は反映した。

### 5 周目(最終周)— 収束

否決条件を「**射程に残した範囲の P0 のみ**」に限って実施(P1 以下は人間承認と逐行確認で代替 — TSK-278 が
(a)→(c) の組で収束した前例に倣う)。**否決対象の P0 は 0 件**で、次の 6 点に反例なしと判定された:
`Q5`/③-b の排他・網羅 / `B5`〜`B7` の扱い / `U-13`・`U-14` の規則 / `DI5` 不変判定 / 「空」の 4 用途 / ステップ 7 件の分離。

**収束確認周なので `計画レビュー周回` は +1 しない**(4 のまま)。

## 現況(2026-09-24 時点)

- 3 検査(`check_design_propagation` / `check_doc_coverage` / `check_docs_status`)は**編集後も rc=0**
- 計画レビュー周回: 4(1〜4 周目が指摘反映を伴う周。5 周目は収束確認)
- **`承認: 済(2026-09-24・山田正輝)`** — 計画レビュー 5 周(反映 4 周 + 収束確認 1 周)を経て承認
- 実装ステップ 7 件。射程外は TSK-441 へ受入証跡の形つきで送付済み

## 実装(/implement — 全 7 ステップ完了)

| # | ステップ | コミット | 備考 |
| --- | --- | --- | --- |
| 1 | `U-13` の規則確定 | `1906183` | 2 行・1 ファイル |
| 2 | `U-13` の帰結・「空」の 4 用途 | `8e685a9` | **差し戻し 1 回**(8-3 の過剰改変) |
| 3 | `U-14` の選択規則(5 箇所) | `f9a35d3` | 7-2 の `B1` を節本文で保持し `element-coverage` の落下を回避 |
| 4 | 11-4 クローズ + `U-15` → TSK-441 + `DI5` 判定 | `3504d4c` | 受け取り先整理表の `r8` → `r7` に台帳 10 行を追随 |
| 5 | v0.4 起案 + 索引現行化 | `3508f8b` | 1 版 2 行の形式に従い**起案行のみ**(`approved` は /finalize-doc) |
| 6 | `DI5` 射程外理由の現行化 | `e0f501c` | 射程は TSK-330 のまま |
| 7 | `changeOperationGate` を ④ へ | `7e5e77c` | **本タスク唯一の挙動変更**(FR-007 の安全条件) |

### 差し戻し 1 件(ステップ 2)

8-3(補正の通知)の行が `B2`〜`B7` → `B1`〜`B7` に広げられ、**6-3 の通知方針が丸ごと複写**されていた。
design.md 4 節が「8-3 は補正の通知(別軸)であり境界結果の通知と重ねない」と定めており、
複写すると 6-3 の改訂時に 8-3 が黙って古くなる。**主語を「止まった」のまま `B2`〜`B4` に限定し、
通知内容の正は 6-3 と参照する形**へ戻した。

### 総合検証(/check)

| 層 | 結果 |
| --- | --- |
| harness(ruff / ty / pytest) | **green**(1564 passed) |
| backend(ruff format / ruff / ty) | **green** |
| backend(pytest) | **374 passed / 211 errors** — **develop でも同一**。`PITCHLOG_TEST_*_DSN` が SQLAlchemy 形式(`postgresql+psycopg://`)で psycopg の生 DSN と噛み合わない**環境起因**。本ブランチは backend に 0 ファイル変更 |
| frontend(prettier / eslint / vue-tsc / vitest) | **green**(665 passed) |
| docs 3 検査 | **green**(全ステップで rc=0 を維持) |
| docs 相対リンク | **壊れ 0 件**(1 件は `<...>` 形式アンカーの誤検出・develop にも存在) |

### 申し送り

- **backend の DSN 環境変数の形式**は develop から続く既存問題。本タスクの射程外だが、
  ローカル手順の現行化が要るかもしれない(CI では通っている)

## 確定ゲート(/finalize-doc — 設計書 7.3)

### 手順 0: 射程宣言の確認(7.3-7)

**あり。** 既存正本の改訂なので、**v0.4 の変更履歴行(in-review)に記載**している
(①〜⑤ の確定範囲 + 範囲確定 + 人間の方式判断)。7.3-7 の配置要件を満たす。

### 手順 1: 適用版の記録(7.3-1)

- **適用版 = ハーネス設計書 `docs/development/dev-harness-design-2026-08-07.md` の v1.17(approved)**
- **条文コミット SHA = `3504d4c`** — 7.3-1 の定義「適用版が approved 状態で収録されている最新のコミット。
  通常は in-review 化コミットの第一親」に従う。in-review 化コミットは `3508f8b`(ステップ 5)、その第一親が `3504d4c`。
  当該コミット時点で設計書は `status: approved`・v1.17 であることを実測で確認した
- 参考: **7.3 の条文本体は `56f8c10`(2026-09-01)以降変わっていない**(`git log -L` で実測)

**逸脱の記録**: 7.3-1 は「in-review 化と**同一コミット**で worklog へ暫定記録する」と定めるが、
in-review 化(`3508f8b`)は実装ステップ 5 として先行しており、**適用版の記録は本コミットへ分離した**。
**初回敵対レビュー実行前に記録している**ため 7.3-1 の目的(先取り固定の排除)は満たすが、
同一コミット要件は満たしていない。**以後のゲートでは in-review 化の時点で記録する。**
