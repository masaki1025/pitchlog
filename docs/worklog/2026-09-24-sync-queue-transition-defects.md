---
date: 2026-09-24
topic: 同期キュー遷移の矛盾 2 件(U-13・U-14)
branch: feature/sync-queue-transition-defects
---

# 作業ログ: 2026-09-24 同期キュー遷移の矛盾 2 件(U-13・U-14)

## やったこと

- /task-start: worktree 作成、Notion TSK-329 を `進行中` へ、計画書・worklog の雛形作成。
  カードに DoD 欄が無かったので追加した。重さ分類は**コア領域**(同期プロトコル)
- /investigate: 調査 3 本(spec-checker / decision-tracer / Explore)を並列実行し
  [research.md](../features/sync-queue-transition-defects/research.md) へ統合

## 決定

- 調査メモの記法: **[確認済]**(Claude が原典を読んだ)と **[報告]**(エージェント報告のまま)を区別する
- エージェント報告の誤りを 1 件訂正 — 7-2 節内の `B1` は **`:1011` と `:1017` の 2 箇所**(1 箇所ではない)。
  実施順の制約が変わるため research.md 7 節に裁定として記録した

- /plan: 計画書(6 節)+ 詳細設計を作成。密度が高いので design.md を分離した
- **U-14 の方式を人間が裁定(2026-09-24)**: 「選択規則を明文化」— 境界結果は未使用 D5 の V12 照合で決まり、
  保存済み退避結果の再掲は境界結果を決めない。`B1` 側 2 箇所・`B4` 側 3 箇所のどちらも削らず条件を補う
- ベースラインを自分で実測: `check_design_propagation` / `check_doc_coverage` / `check_docs_status` の 3 検査とも rc=0
- 敵対レビュー(`codex_run.py review adversarial`)を実行中

## 決定(追記)

- **U-14 の規則は新設ではなく、6-2「処理段階の列挙」から導かれる既存の意味論**だと判明した。
  段階 ④ が保存済み結果を「再掲する候補」へ分類し、**未使用 D5 だけ段階 ⑤ へ進む**。
  `B4` は段階 ⑤ でしか生じないので、**未使用 D5 を持たない要求は `B4` になり得ない**。
  → design.md 2-2 の根拠を 7-1 P4(二次)から **6-2 処理段階表(一次)** へ差し替えた。
  あわせて `U-14` の欠陥の所在が「**7-1 総括・7-3 手順 3・10-2 が `B4` を無条件に述べていて 6-2 段階 ⑤ と食い違う**」と特定できた
- 改訂ゲートは **finalize-doc**(版 v0.3 → v0.4 の構造的変更 — 設計書 7.6-3)と判定

## 未決・次の一歩

- **最重要発見**: 機械検査される不変条件 `SP-01` が「7-2 の遷移条件列は **A5 の結果だけ**をキーとし、
  `B4` を条件にすることを forbidden」と既に定めている。→ **`U-14` の未決はサーバーの応答分類・`D3`・通知に限られ、
  クライアントのキュー遷移条件は未決ではない**
- 実装(`queueTransition.ts` の `QT-02`/`QT-08`)も既に A5 のみで遷移しており、**正本の `B1` 限定文言だけが浮いている**
- `U-14` 未決が `DI5` の実装を実際にブロックしている(`canonOracle.ts` / `idempotencyCollision.ts`)
- 次: /plan。`U-14` の応答分類(C)は**人間の方式判断**なので、判断材料 3 軸を提示して裁定を仰ぐ

## 敵対レビュー(計画)

| 周 | 指摘 | 反映 |
| --- | --- | --- |
| 1 | P0 2 / P1 5 / P2 1 | **全件採用**。P0-1 = `U-14` の規則が `B2`/`B3` を落としていた(段階 ④ が `B3b` 候補を作り段階 ⑥ が確定させる)。P0-2 = キュー空判定の自己矛盾 → **「空」を 4 用途へ分離** |
| 2 | P0 2 / P1 3 / P2 1 | **P0 2 件・P1-1・P1-2 の正本側・P2-1 を反映**。P0-1 = **`B7` 経路の脱落**(7-1 の A3「ACK が返るのは `B1`〜`B4` に限る」・段階 ⑧ が `B7` へ分岐)→ 規則を **ACK 経路限定**にし `B5`〜`B7` 行を追加。P0-2 = **`Q5` は「未送信 0 件」で発火させられない**(7-5「拒否・退避・未処理が 1 件でも残れば完了としない」)→ ③ を再定義し **③-b** を新設 |

| 3 | P0 2 / P1 1 / P2 3 | **全件採用**。P0-2 = ③ を「当該試合に `退避済み` が無い」とすると**退避が一度起きた試合で `Q5` が恒久的に出なくなる**(`退避済み` は `U-3` まで自動破棄されない)→ **エッジ条件**へ。7-5 の完了判定は「試合終了宣言後の初回同期」限定であり流用不可と判明 |
| **4** | **P0 3 / P1 5 / P2 1 — 増加** | **閉じた輪と判定**。9 件中 8 件が 3 周目の反映そのものへの指摘 |

### 4 周目 — 閉じた輪の診断と PO 裁定

**周回 8 → 6 → 6 → 9 で増加に転じた。**原因は 3 周目に私が
「`N1`〜`N5` の全確定 + 発火ロジック + exact-set テスト」という**強い述語を足したこと**。
台帳 **H-22**(自分が新設した規定の穴を次の周が突く)・**H-68**(実機のない段階で細部まで確定させようとすると
閉じた輪に入る)・**「強い述語が細部を無限に呼ぶ輪」**(TSK-335 — 述語を弱めて収束した 1 例目)と同型。

**PO 裁定(2026-09-24)= (a) 範囲確定 + (d) 述語を弱める**:

- **通知契約 `N1`〜`N7` の確定と `syncNotices.ts` の実装を [TSK-441](https://app.notion.com/p/3e493b75e68781a09230e4ddea5b0438) へ分離**(新規起票)
- 正本で閉じるのは「各境界結果で通知が出るか出ないか」と「`B4` 専用文面を流用しない禁止」まで
- **各件に受入証跡の形(対象/範囲/方法)と「なぜ機械で閉じないか」を付けて送る**(design.md 4-2)
- **`changeOperationGate`(FR-007 の安全条件)は残す** — 乖離を残すと正史が壊れる経路が開くため
- **`DI5` は変更しない**と判定(`B4` は段階 ⑤ で確定し `DI5` の述べる段階 ⑥ の走査に入らない)。理由を 11-4 に残し逐行確認の対象にする

4 周目の P0 のうち**輪の外側だったのは P0-2(`DI5` 伝播)のみ**で、これは判定として処置した。
P0-1(③-b が ③ の否定になっていない → `E ∧ ¬X` / `E ∧ X` へ)と
P0-3(`B7` を一律ロールバックとした → 「クライアントへ返らない」まで。通信・ACK 消失の `B7` は部分確定がありうる)は反映した。

### 5 周目(最終周)— 収束

否決条件を「**射程に残した範囲の P0 のみ**」に限って実施(P1 以下は人間承認と逐行確認で代替 — TSK-278 が
(a)→(c) の組で収束した前例に倣う)。**否決対象の P0 は 0 件**で、次の 6 点に反例なしと判定された:
`Q5`/③-b の排他・網羅 / `B5`〜`B7` の扱い / `U-13`・`U-14` の規則 / `DI5` 不変判定 / 「空」の 4 用途 / ステップ 7 件の分離。

**収束確認周なので `計画レビュー周回` は +1 しない**(4 のまま)。

## 現況(2026-09-24 時点)

- 3 検査(`check_design_propagation` / `check_doc_coverage` / `check_docs_status`)は**編集後も rc=0**
- 計画レビュー周回: 4(1〜4 周目が指摘反映を伴う周。5 周目は収束確認)
- **`承認: 済(2026-09-24・山田正輝)`** — 計画レビュー 5 周(反映 4 周 + 収束確認 1 周)を経て承認
- 実装ステップ 7 件。射程外は TSK-441 へ受入証跡の形つきで送付済み

## 実装(/implement — 全 7 ステップ完了)

| # | ステップ | コミット | 備考 |
| --- | --- | --- | --- |
| 1 | `U-13` の規則確定 | `1906183` | 2 行・1 ファイル |
| 2 | `U-13` の帰結・「空」の 4 用途 | `8e685a9` | **差し戻し 1 回**(8-3 の過剰改変) |
| 3 | `U-14` の選択規則(5 箇所) | `f9a35d3` | 7-2 の `B1` を節本文で保持し `element-coverage` の落下を回避 |
| 4 | 11-4 クローズ + `U-15` → TSK-441 + `DI5` 判定 | `3504d4c` | 受け取り先整理表の `r8` → `r7` に台帳 10 行を追随 |
| 5 | v0.4 起案 + 索引現行化 | `3508f8b` | 1 版 2 行の形式に従い**起案行のみ**(`approved` は /finalize-doc) |
| 6 | `DI5` 射程外理由の現行化 | `e0f501c` | 射程は TSK-330 のまま |
| 7 | `changeOperationGate` を ④ へ | `7e5e77c` | **本タスク唯一の挙動変更**(FR-007 の安全条件) |

### 差し戻し 1 件(ステップ 2)

8-3(補正の通知)の行が `B2`〜`B7` → `B1`〜`B7` に広げられ、**6-3 の通知方針が丸ごと複写**されていた。
design.md 4 節が「8-3 は補正の通知(別軸)であり境界結果の通知と重ねない」と定めており、
複写すると 6-3 の改訂時に 8-3 が黙って古くなる。**主語を「止まった」のまま `B2`〜`B4` に限定し、
通知内容の正は 6-3 と参照する形**へ戻した。

### 総合検証(/check)

| 層 | 結果 |
| --- | --- |
| harness(ruff / ty / pytest) | **green**(1564 passed) |
| backend(ruff format / ruff / ty) | **green** |
| backend(pytest) | **374 passed / 211 errors** — **develop でも同一**。`PITCHLOG_TEST_*_DSN` が SQLAlchemy 形式(`postgresql+psycopg://`)で psycopg の生 DSN と噛み合わない**環境起因**。本ブランチは backend に 0 ファイル変更 |
| frontend(prettier / eslint / vue-tsc / vitest) | **green**(665 passed) |
| docs 3 検査 | **green**(全ステップで rc=0 を維持) |
| docs 相対リンク | **壊れ 0 件**(1 件は `<...>` 形式アンカーの誤検出・develop にも存在) |

### 申し送り

- **backend の DSN 環境変数の形式**は develop から続く既存問題。本タスクの射程外だが、
  ローカル手順の現行化が要るかもしれない(CI では通っている)

## 確定ゲート(/finalize-doc — 設計書 7.3)

### 手順 0: 射程宣言の確認(7.3-7)

**あり。** 既存正本の改訂なので、**v0.4 の変更履歴行(in-review)に記載**している
(①〜⑤ の確定範囲 + 範囲確定 + 人間の方式判断)。7.3-7 の配置要件を満たす。

### 手順 1: 適用版の記録(7.3-1)

- **適用版 = ハーネス設計書 `docs/development/dev-harness-design-2026-08-07.md` の v1.17(approved)**
- **条文コミット SHA = `3504d4c`** — 7.3-1 の定義「適用版が approved 状態で収録されている最新のコミット。
  通常は in-review 化コミットの第一親」に従う。in-review 化コミットは `3508f8b`(ステップ 5)、その第一親が `3504d4c`。
  当該コミット時点で設計書は `status: approved`・v1.17 であることを実測で確認した
- 参考: **7.3 の条文本体は `56f8c10`(2026-09-01)以降変わっていない**(`git log -L` で実測)

**逸脱の記録**: 7.3-1 は「in-review 化と**同一コミット**で worklog へ暫定記録する」と定めるが、
in-review 化(`3508f8b`)は実装ステップ 5 として先行しており、**適用版の記録は本コミットへ分離した**。
**初回敵対レビュー実行前に記録している**ため 7.3-1 の目的(先取り固定の排除)は満たすが、
同一コミット要件は満たしていない。**以後のゲートでは in-review 化の時点で記録する。**

### 採否記録(7.3-2)

#### 1 周目(全文・2026-09-24)— 判定行: **否決 — P0 0 件 / P1 1 件 / P2 0 件**

| # | 要旨 | 重大度 | 帰属 | 分類 | 採否 | 理由 / 反映内容 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `U-15` の送付先が **Notion URL を直接参照**している(11-4 の `U-15` 行と受け取り先整理表の 2 箇所)。設計書 **7.4**「**正本は Notion を参照しない(Git 単独で完結)**」と矛盾する | **P1** | **起因** | `(-)` | **採用** | **2 箇所の URL を削除**し、既存の受け取り先と同じ `**TSK-441**(同期の通知契約の確定と実装)` の形へ揃えた。**原因は実装ステップ 4 の委任文で URL を書かせたこと**(私の指示ミス)。develop に同型の是正前例 `732a64c`「正本から Notion URL を除き設計書 7.4 違反を是正する」があり、**同じ型を 2 度踏んだ** |

**レビューの所見**: `U-13`・`U-14` の段階規則、「空」の 4 用途 + ③-b、指定 5 箇所、`DI5` の据え置きには
**反例・不整合を認めない**と判定された。文書検査 3 件も成功。

**遷移**(7.3-2): P0/P1 あり・1 件以上採用 → **周回 +1(0 → 1)**・`反映1周目` コミット 1 件 → **差分再レビューの次周へ**。

**再発防止の申し送り**: **委任文に「正本へ Notion URL を書かない(設計書 7.4)」を常駐させる**
(同じ型を 2 回踏んだため — 無人時の品質約束 5)。

#### 2 周目(差分・2026-09-24)— 判定行: **可決 — P0 0 件 / P1 0 件 / P2 0 件**

**指摘なし。** Notion URL 2 箇所の削除と `TSK-441` 表記の統一を確認、影響節との不整合もなし。
文書検査 3 件も成功。

**遷移**(7.3-2): P0/P1 ゼロ・P2 ゼロ(通常周)→ **反映なし・カウントしない** → **最終全文確認周へ**。
(本項の追記コミットは**反映周コミットにしない** — 7.3-2 の規定)

#### 3 周目(**最終全文確認周**・2026-09-24)— 判定行: **否決 — P0 0 件 / P1 1 件 / P2 0 件**

| # | 要旨 | 重大度 | 帰属 | 分類 | 採否 | 理由 / 反映内容 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **保存済み結果の再掲とバッチ全体の境界結果が 3 箇所で混同**されている。7-1 の総括・7-3 手順 3 は「受理済みの再送は `B1`、拒否済みは `B3`」と**断定**し、7-7 の `X2` は確定済みの取り込みを「`B1` の重複」と**断定**する。しかし 6-2 の処理段階と `DI5` では保存済み結果は**候補**であり、同一要求内の未使用 D5・欠番・先行する拒否により境界結果は `B2`/`B3`/`B4` になり得る。**`U-14` の「再掲そのものは境界結果を決めない」が実効化されていない** | **P1** | **非起因**(レビュア判定) | `(A)` | **採用** | **3 箇所とも是正**。7-1・7-3 は「受理済みは **A5 の「重複」**、拒否済みは **A5 の「拒否」と理由**」へ改め、**境界結果は 6-2 の段階列挙が決める**ことを明示(全件が重複の再送なら `B1` を例として残す)。7-7 の `X2` も「**A5 の「重複」**」へ改めた |

**帰属についての所見**: レビュアは非起因としたが、**7-1 と 7-3 は本改訂(ステップ 3)が触った箇所**であり、
**同一文の中に「受理済みは `B1`」という断定と「再掲そのものは境界結果を決めない」という一般規則が同居する
自己矛盾を残した**点は**本改訂に起因**する。7.3-3 の「**文言は直したが実効がない**」型に該当する。
7-7 の `X2` は本改訂が触っていない v0.1 由来だが、**同一規則の適用漏れ**であり ③ の実効化として同時に是正した
(新しい論点を開かないため射程変更にはあたらない)。

**遷移**(7.3-2): 最終全文確認周・P0/P1 あり・1 件以上採用 → **周回 +1(1 → 2)**・`反映2周目` コミット 1 件
→ **差分再レビューへ戻る**。

**警告・エスカレーション**: いずれも**未発火**。`確定ゲート周回` = 2(6 未満)。
起因件数 × 2 > 母集団 の連続成立も無し(本周はレビュア判定で起因 0 件)。

#### 4 周目(差分・2026-09-24)— 判定行: **否決 — P0 0 件 / P1 1 件 / P2 0 件**

| # | 要旨 | 重大度 | 帰属 | 分類 | 採否 | 理由 / 反映内容 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 7-1 の冒頭が「**ACK と独立応答の内容は再送しても同じものが返る**」と断定する一方、同じ段落が受理済みの再掲を **A5「受理」→「重複」**へ変えると定め、境界結果も処理段階で再決定される。実装が「ACK 全体を同一にする」のか「保存済みのイベント結果だけを基に A5・境界結果を再構成する」のか選べない。**反映箇所内に旧総括が残った「文言は直したが実効がない」型** | **P1** | **起因** | `(A)` | **採用** | 冒頭を「**再送で同じものが返るのは、D5 ごとに保存した確定結果である**」へ限定し、「**ACK と独立応答の全体が同一になるわけではない**」を明示。受理済みの再掲が A5「重複」を返すこと(初回は「受理」)も併記した |

**遷移**(7.3-2): P0/P1 あり・1 件以上採用 → **周回 +1(2 → 3)**・`反映3周目` コミット 1 件 → **差分再レビューの次周へ**。

**エスカレーション監視**(7.3-6): 母集団 = 当該周の P0+P1 件数。
- 3 周目: 母集団 1・起因 0 → `0 × 2 > 1` は**不成立**
- 4 周目: 母集団 1・起因 1 → `1 × 2 > 1` は**成立**

**連続 1 周目。次周も起因が過半なら 2 周連続成立となり PO スコープ裁定へエスカレーションする。**
`確定ゲート周回` = 3(6 周警告は未発火)。

#### 5 周目(差分・2026-09-24)— 判定行: **否決 — P0 0 件 / P1 1 件 / P2 0 件**

| # | 要旨 | 重大度 | 帰属 | 分類 | 採否 | 理由 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 7-1 の総括は**保存済み確定結果が再送時に返ると無条件に読める**が、混在バッチでは**先行する `B2`/`B3` より後ろの保存済み結果候補も A5「未処理」**になる(`DI5`・6-2 段階 ⑥・7-1 の `A5` 行)。したがって**受理済み再送が常に A5「重複」を返すわけではない**。実装者は「保存済み結果を常に再掲する」か「停止境界より後ろでは未処理にする」かをなお選べず、**前周の是正が実効化されていない** | **P1** | **起因** | `(A)` | **PO 裁定待ち(未処置)** | 指摘は正当(7-1 の `A5` 行が「最初の B2 または B3 で停止し、それ以降は事前分類済み B3b を含めすべて「未処理」を返す」と定める)。**ただし 7.3-6 のエスカレーション条件が成立したため、処置方針を PO 裁定に委ねる** |

### エスカレーション(7.3-6)— **発火**

| 周 | 母集団(P0+P1) | 起因 | `起因 × 2 > 母集団` |
| --- | --- | --- | --- |
| 3 | 1 | 0 | 不成立 |
| **4** | 1 | 1 | **成立** |
| **5** | 1 | 1 | **成立** |

**2 周連続で成立** → **PO スコープ裁定へエスカレーションする**(7.3-6)。

**作成者の所見**: 3〜5 周の指摘は**すべて 7-1 の「再送の総括」段落 1 箇所**に集中している。
是正するたびに次の未限定条件が見つかる形で、**台帳 H-22(自分が新設した規定の穴を次の周が突く)・
「強い述語が細部を無限に呼ぶ輪」と同型**である。原因は、**規範がすでに 7-1 の `A5` 行と `DI5` にあるのに、
総括段落が散文で規則を言い直していること**(重複した述語が毎周ほころびる)。

### PO スコープ裁定(7.3-6 — 2026-09-24)= **続行の明示指示 + 述語を弱める**

エスカレーション(4・5 周目の 2 周連続成立)に対する裁定。

**裁定**: **続行の明示指示**。射程は変更しない(①〜⑤のまま)。
あわせて、**5 周目の指摘を「7-1 の総括から規則の言い直しを外す」形で反映する**。

**判断の理由**: 3〜5 周の指摘は**すべて 7-1 の「再送の総括」段落 1 箇所**で、内容は毎回
「言い直した規則に条件が足りない」だった(3 周目 = 受理済み → `B1`・拒否済み → `B3` の断定 /
4 周目 = 「ACK の内容は再送しても同じ」の断定 / 5 周目 = 停止境界より後ろは保存済み候補も「未処理」)。
**規範は 7-1 の `A5` 行・4-5 の `DI5`・6-2 にすでにあり、総括が散文で重ねて定義していた**ことが原因。
**重複した述語を消せば輪から出る**(計画レビューで採った (d) 述語を弱める と同型。台帳に収束実績あり)。

**射程縮小・範囲確定を採らない理由**: 縮める対象がない(`U-13`/`U-14` の規則は 1〜3 周目で確定済み)。
射程宣言は既に①〜⑤へ限定済みで更新内容がない。

**代償**: 総括から具体例(「全件が重複の再送なら `B1`」)が消える。ただし 6-3 の 3 例表が同じ例を持つため
情報は失われない。

**反映内容**: 7-1 の総括を「保存した確定結果は再送でも変わらない / **ただし ACK 全体が同一になるわけではない** /
**A5 の結果も境界結果も保存済み結果の有無では決まらない** / **正は 7-1 の `A5`・4-5 の `DI5`・6-2 の処理段階** /
**本段落は要約であり規則を重ねて定義しない**」へ改めた。

**カウンタ**: 指摘の採用反映を伴うため **周回 +1(3 → 4)**・`反映4周目` コミット 1 件。
**エスカレーションの連続カウントは裁定によりリセット**(7.3-6)。
**次周は差分再レビュー**(射程が変わっていないため — 7.3-6 の表)。

#### 6 周目(差分・裁定後の最初の周・2026-09-24)— 判定行: **可決 — P0 0 件 / P1 0 件 / P2 1 件**

| # | 要旨 | 重大度 | 帰属 | 分類 | 採否 | 理由 / 反映内容 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 7-1 の総括が挙げる典拠 **4-4 の `C3`** は「**ACK 消失後の再送でも別の正式 ID を重複生成しない**」= 一時 ID 写像に限定であり、**補正差分・P5 の拒否結果・退避結果までは支えていない**。挙動は `T6`・`T8`・`T9` で一意なので**参照範囲のみの問題** | **P2** | **非起因** | `(-)` | **採用** | **典拠を是正**。保存の原子性は **8-1 の `T6`**(確定結果と一時 ID 写像)・**`T8`**(退避)・**`T9`**(拒否原本・拒否結果・理由)、一時 ID の重複生成防止は **4-4 の `C3`** と書き分けた。原典で `C3`・`T6`・`T8`・`T9` の定義を確認済み |

**レビューの所見**: **PO 裁定どおり、総括への規則再記述は要求しない**と明記された(裁定が尊重されている)。

**遷移**(7.3-2): P0/P1 ゼロ・P2 あり(通常周)→ 残存 P2 を一括裁定し**採用・一括反映** →
**周回 +1(4 → 5)**・`反映5周目` コミット 1 件 → **最終全文確認周へ**。

**エスカレーション**: 裁定によりリセット済み。本周は起因 0 件で再発火なし。
`確定ゲート周回` = 5(**6 周警告まであと 1**)。

#### 7 周目(**最終全文確認周**・2026-09-24)— 判定行: **否決 — P0 0 件 / P1 1 件 / P2 0 件**

| # | 要旨 | 重大度 | 帰属 | 分類 | 採否 | 理由 / 反映内容 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **7-3 手順 3 が保存済み結果を無条件に A5 へ再掲する記述**になっている(受理なら「重複」・拒否なら同じ拒否結果・退避なら同じ退避結果と断定)。一方 7-1 の `A5` 規範と 4-5 の `DI5` は、保存済み結果を**内部候補**とし**先行する最初の `B2`/`B3` 以後の候補は「未処理」に上書き**すると定める。実装者が「保存済みなら常に再掲」と「停止境界後なら未処理」のどちらを採るか迷う | **P1** | **非起因** | `(A)` | **採用** | 7-3 手順 3 を「**保存済み結果を内部候補として再掲する**」へ改め、「**外部へ返る A5 の確定順序と停止境界は本節ではなく 7-1 の `A5` と 4-5 の `DI5` が正**であり、**最初の `B2`/`B3` 以降は保存済み結果候補を含めすべて「未処理」になる**」を明示した。**PO 裁定の方針(述語を弱め、正を指す)を 7-3 にも適用** |

**レビューの所見**: 本指摘は「7-1 の総括に規則の再掲を求めるもの**ではなく**、実処理を規定する 7-3 自体の矛盾」であり、
**PO 裁定とは競合しない**と明記された。

**遷移**(7.3-2): 最終全文確認周・P0/P1 あり・1 件以上採用 → **周回 +1(5 → 6)**・`反映6周目` コミット 1 件
→ **差分再レビューへ戻る**。

**エスカレーション**(7.3-6): 本周は起因 0 件のため `0 × 2 > 1` は不成立。**再発火なし。**

### 6 周警告(7.3-6)— **発火**

`確定ゲート周回` が **6 に達した**ため、周回・重大度推移・起因比の一覧を PO へ提示する
(**判断を求めるが強制打ち切りはしない**)。提示内容は下表。

| 周 | 種別 | 判定 | P0 | P1 | P2 | 母集団 | 起因 | `起因×2>母集団` |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 全文 | 否決 | 0 | 1 | 0 | 1 | 1 | 成立 |
| 2 | 差分 | 可決 | 0 | 0 | 0 | 0 | — | — |
| 3 | 最終全文 | 否決 | 0 | 1 | 0 | 1 | 0 | 不成立 |
| 4 | 差分 | 否決 | 0 | 1 | 0 | 1 | 1 | **成立** |
| 5 | 差分 | 否決 | 0 | 1 | 0 | 1 | 1 | **成立** → PO 裁定 |
| 6 | 差分 | 可決 | 0 | 0 | 1 | 0 | — | — |
| 7 | 最終全文 | 否決 | 0 | 1 | 0 | 1 | 0 | 不成立 |

- **P0 は全 7 周でゼロ**
- **不採用 0 件**(全周とも全件採用)
- **裁定後(6・7 周)は起因ゼロ** — 述語を弱めた効果は出ている
- 残る指摘は**すべて同一の型**(「保存済み結果の再掲を無条件に書いた箇所の残り」)で、
  **7-1 → 7-3 と順に潰れてきている**

#### 8 周目(差分・2026-09-24)— 判定行: **可決 — P0 0 件 / P1 0 件 / P2 0 件**

**指摘なし。** 7-3 手順 3 が保存済み結果を内部候補に限定し、外部 A5 の確定順序・停止境界を
7-1 の `A5` と 4-5 の `DI5` へ正しく委譲していること、6-2・6-3・7-7 との矛盾がないこと、
**PO 裁定にも適合している**ことが確認された。

**PO の判断**: 6 周警告に対し **続行を指示**(2026-09-24)。編集なし・カウンタなし(7.3-6 の表)。

**遷移**(7.3-2): P0/P1 ゼロ・P2 ゼロ(通常周)→ **反映なし・カウントしない** → **最終全文確認周へ**。
(本項の追記コミットは**反映周コミットにしない**)

#### 9 周目(**最終全文確認周**・2026-09-24)— 判定行: **否決 — P0 0 件 / P1 1 件 / P2 0 件**

| # | 要旨 | 重大度 | 帰属 | 分類 | 採否 | 理由 / 反映内容 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **7-7 の `X2` と `X3` が矛盾**。`X2` は既存 D5 を保存済み結果として扱い、6-2 の段階 ④〜⑤ も現在の D4・V12 を照合するのは**未使用 D5 だけ**と定めるが、`X3` は「D4 または V12 が不成立なら**無条件に** `B4`・退避」と規定する。ACK 消失前に受理済みだった書き出しイベントを記録権移動後に取り込むと、保存済みの「受理」を再掲するのか現在の記録権で `B4` にするのかが分岐する。**`U-14` の是正が 7-7 では実効化されていない** | **P1** | **非起因** | `(A)` | **採用** | `X3` を「**未使用 D5 のイベントを取り込めるのは…に限る**」へ限定し、「**既存 D5 の保存済み結果は `X2` に従って再掲され、現在の D4・V12 の照合対象にならない**」を明示。**あわせて節全体に正を指す一文**を追加: 「`X1`〜`X4` は取り込み固有の規則だけを定める。D5 の照合位置・記録権の照合対象・A5 の確定順序と停止境界・境界結果の決定は、**取り込みでも 6-2 の処理段階・7-1 の `A5`・4-5 の `DI5` が正**」 |

**同型の 4 例目のため、節ごと閉じる形を採った。** 1〜3 例目は 7-1 の総括・7-3 手順 3 で、
いずれも「保存済み結果の再掲を無条件に書いた箇所」だった。**個別に潰すと次の箇所が出る**ため、
7-7 では `X3` の限定に加えて**節全体が正を指す一文**を置き、`X1`〜`X4` のどれにも同型が残らないようにした。

**遷移**(7.3-2): 最終全文確認周・P0/P1 あり・1 件以上採用 → **周回 +1(6 → 7)**・`反映7周目` コミット 1 件
→ **差分再レビューへ戻る**。

**エスカレーション**(7.3-6): 本周は起因 0 件のため `0 × 2 > 1` は不成立。**再発火なし。**
**6 周警告は 6 周目到達時に発火済みで、PO は続行を指示している**(2026-09-24)。

#### 10 周目(差分・2026-09-24)— 判定行: **可決 — P0 0 件 / P1 0 件 / P2 0 件**

**指摘なし。** `X3` の限定が 6-2・7-1 の `A5`・4-5 の `DI5`・6-3 と整合し、前周の矛盾が解消されたことを確認。
伝播・文書状態検査も通過。

**遷移**(7.3-2): P0/P1 ゼロ・P2 ゼロ(通常周)→ **反映なし・カウントしない** → **最終全文確認周へ**。
