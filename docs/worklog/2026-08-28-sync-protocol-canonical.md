---
date: 2026-08-28
topic: 同期プロトコル設計の正本化(docs/design/ へ新設 + 確定ゲート)
branch: feature/sync-protocol-canonical
---

# 作業ログ: 2026-08-28 同期プロトコル設計の正本化(docs/design/ へ新設 + 確定ゲート)

## やったこと

- /task-start: 既存の Notion タスク **TSK-265** を取得(前タスクのクローズ処理で起票済み — 重複起票はしていない)・`feature/sync-protocol-canonical` の worktree 作成・計画書雛形と本ログの作成
- /investigate: 調査サブエージェント 4 本(spec-checker 要件突合 / decision-tracer 決定経緯 / legacy-analyst 旧システム事実 / 表間突合)を並列で投げ、`docs/features/sync-protocol-canonical/research.md` へ統合した。**重い指摘は原典を Claude が直接確認**(research.md の ◎印)

## 決定

- **調査は 4 並列**とした(既定 3 + 1)。4 本目「表間突合」は、前タスクが 3 周収束しなかった主因(**「表 A の変更を表 B へ写したか」を見る検査が無い**)に対して、**その突合を人手で 1 回回す**もの。DoD の「構造の伝播漏れを検出する検査」の設計材料を兼ねる
- **エージェント間の食い違いを 1 件裁定した** — D1 の用途について、誤っているのは **2-1 の定義(`design.md:41`)側**であり `:222` ではない(要件書 `:286` が「重複排除 = べき等キー / 順序と欠落 = 連番」と役割を分離しており、候補案 4-2 の `:228` も同じ切り分けを取る)。**是正の向きを逆にすると誤りを正本へ固定する**

## 計画レビュー(16 周・2026-08-28〜29)

P0 は **4 → 4 → 5 → 5 → 2 → 3 → 3 → 2 → 3 → 2 → 2 → 2 → 2 → 4 → 3 → 2** と推移し、**収束しなかった**。**16 周目のレビュー自身が「追加の敵対レビュー周は設けず、P0/P1 を最小修正して人間確認で承認へ進む」と判定**し、実装を止める 4 項目を名指しした(すべて反映済み)。

**人間の判断で 3 度の設計し直しと 2 度の射程縮小を行った**:

| 周 | 捨てたもの | 分かったこと |
| --- | --- | --- |
| 5 | ベースライン(ratchet)機構 | **新しい検査機構を発明すると、その機構自身が次の P0 を生む** |
| 6 | 「1 ステップ = 1 伝播関係」の分割 | **関係は伝播先の表を共有するので、関係単位では所有が交差する** |
| 9 | 人間照合欠陥の機械化(アンカー → 構造化不変条件) | **意味の判断は機械化できない** — 機械化すると第二の正本になる |
| 10・11 | U-1 を本タスクで閉じること | **「要件を 1 行足すだけ」に見えたものが、状態機械に入口がなく新規設計だった**。**先行タスクにすると成果の取り込み経路が無く版も衝突する**ため後続へ |
| 13 | 部分置換による編集 | **大きな文書への部分置換は取り残しを作り、それ自体が P0 になる**。全面書き直しへ |

**台帳 H-78 への追記材料**: 前タスクの診断は「**検査が無いから収束しない**」だった。本タスクで分かったのは **「検査を作れば閉じる」わけではない** — **機械化できない領域があり、それを機械化しようとすると発明が増えて収束が遠のく**。**H-78 の対応案に「何を機械化しないかを先に決める」を加える必要がある**。


## 人間照合の記録(実装ステップ)

**機械では判定できない欠陥は、各所有ステップで該当箇所を原典と照合し、結果をここへ記録する**(計画書 4 節)。**最終的な担保は確定ゲートでの人間の逐行確認**である。

### ステップ 10(ACK とキューの状態)

| 欠陥 | 照合先の原典 | 結果 |
| --- | --- | --- |
| **SP-04** | 本書 7-1 の A5 の結果集合(受理 / 重複 / 拒否 / 退避 / 未処理) | **是正**。A1 の射程が「受理され連続 prefix に入ったイベント」となっており、**5 状態のうち「重複」が肯定側にも否定側にも現れなかった**。「**A5 が『受理』を返したイベント**」へ改め、A5 の語彙へ揃えた |
| **SP-05** | research.md D-1 ⑤(A5 が A4 より前に置かれている構造の痕跡) | **是正**。ACK 要素表を **A1〜A5 の番号順**へ並べ直した。2 周目・3 周目に A5 を後から挿入した痕跡だった |
| **CI-02** | 要件書 FR-012「Given **同期完了済みイベント** / When 24時間以内 / Then 端末に保持され続ける」+ FR-013 の退避規定 | **是正**。6-3 の B4 行が退避イベントを 24 時間保持の対象に含めており、**要件の Given(同期完了済み)を超えていた**。退避イベントは定義上サーバーへ適用されておらず「同期完了済み」ではないため、**24 時間規定の流用をやめた**。保持期間そのものは **U-3** として受け取り先へ送る |

### ステップ 12(変更イベントの契約)

| 欠陥 | 照合先の原典 | 結果 |
| --- | --- | --- |
| **SP-15** | 本書 5-3 の実行前提 3 行(前段・記録権の保持・後段の「挿入イベントの D1 が D3 + 1」) | **是正**。W4 が「5-3 の実行前提と同じ」と一括参照していたが、**3 行のうち 2 行が変更イベントへ適用できない** — 後段は D1 に依存するが変更イベントは D1 を持たず、記録権の保持は W4 自身が「終了後修正は記録権の外」と定めるため終了後経路に適用できない。**「W4 と 5-3 の実行前提の照合」節を新設し、3 行 × 進行中 P3 / 終了後 P3 の表で 1 行ずつ判定**した |
| **CI-04** | 要件書 FR-012 の墓標の受入基準(Given = **内容起因で拒否された**)/ 4.0-2(黙殺しない)/ NFR-015 | **是正**。6-3 の B2 行と 8-3 の通知表が「どこで止まったかを明示」の典拠に FR-012 の墓標の受入基準を引いていたが、**その Given は内容起因の拒否に限られ、gap・認証失効・認可・一時障害には及ばない**。一般の顕在化規範である 4.0-2 と NFR-015 へ典拠を差し替えた |

### ステップ 13(参加区分と群構造)

| 欠陥 | 照合先の原典 | 結果 |
| --- | --- | --- |
| **SP-17** | 本書 7-2 のキュー遷移(墓標・改訂版は「要操作 → 未送信」で積まれる)/ 8-1 の経路分類(#8・#9 は「FR-012 のキューに載る D1 付きイベント」) | **是正**。「表は 2 群に分ける」と宣言しながら**平坦な 1 表**だった。**群 A(D1 付きで端末内キューに載る種別 #1〜#9)/ 群 B(D1 を持たないオンライン専用の変更イベント #10〜#12)**の小見出しで表構造として実現した。**群定義の誤りも是正** — 旧定義は墓標・改訂版を「オンライン専用」に含めており、キューに載る事実と矛盾していた |
| **CI-06** | 要件書 FR-006(直前の取消可能な操作 1 件・undo を undo しない)/ FR-007(下流再計算) | **是正**。5-5 の undo 行が「下流再計算の起点は対象の D2 になる」の典拠に **FR-006 を引いていた**が、同条は下流再計算に触れていない。典拠を FR-006 が実際に定める範囲(undo の対象)へ限定した |
| **CI-08** | 要件書 NFR-015(データ保存・同期・バックアップ・出力の失敗、スキップ、自動補正の顕在化) | **是正**。O4 の「投影と再計算を止める」(fail-closed)を NFR-015 由来のように書いていたが、**同条が求めるのは顕在化であって停止ではない**。「**NFR-015 が要求するのは顕在化であり、停止自体は要求していない。停止は本書の判断である**」と分離して明記した |

### ステップ 15(記録権との境界のテスト種別)

| 欠陥 | 照合先の原典 | 結果 |
| --- | --- | --- |
| **CI-01** | 要件書 NFR-019 の (a)〜(d)((a) 一致性 / **(b) 越境アクセステスト** / (c) 主要分岐の E2E〔記録権の通常・緊急引き継ぎと退避経路を含む〕/ (d) 同期プロトコルの故障系) | **是正**。9-1 の列定義が「**(c) = 越境**」と書いており、**越境は (b)** であるため誤りだった。列定義の誤りにより **9-2 の境界表 11 行の当該列が汚染**されていた。全 20 行を要件書の原文と逐語照合して判定し直し、分布は **(a) 1 / (b) 1 / (c) 10 / (d) 10** となった。越境 (b) は「退避イベントの保存と閲覧・書き出し」に付く(閲覧・書き出しが認可を伴うため)。**同じ本書の 10-2 は (c) を正しく引いており内部矛盾していた**が、これも解消した |

**この欠陥は「引用の意味照合を全件やらないと見つからない型」の実例**である。列定義という 1 か所の誤りが列全体へ波及し、しかも同じ文書の別の節が同じ要件を正しく読んでいた。**記録権 × 同期という 2 つのコア領域の交点**にあった。

### ステップ 16(U-6 の決着)

| 欠陥 | 照合先の原典 | 結果 |
| --- | --- | --- |
| **CI-10** | 要件書 FR-012 の永続ストレージに関する受入基準(全件) | **是正**。10-1 の論点 19 が「要件語は『永続ストレージ API』のみ」と書いていたが、要件書には「永続ストレージ」という別表記も存在し、**「のみ」は過少**だった |
| **CI-11** | ADR-003 D-6 の契約表(**① 対象計算ベクタ契約 9 行 / ② 参照データ契約**) | **是正**。10-3 が「ADR-003 の契約表は**対象計算**が対象」と書いていたが、同表は②参照データ契約の行も持つ。**「①②のいずれにも (d) の行が無い」**へ改めた。あわせて主張の精度を上げ、「発効規則が要件書に存在しない」ではなく「**発効ゲートは存在するが、その対象は対象計算に紐づくベクタ契約に限られており (d) に及ばない**」とした |

**U-6 の決着**: NFR-019(d) のテスト資産について、**版付け**(1 シナリオ = 1 つの版付き JSON・`<scenarioId>_v<N>.json`・ファイル内に `scenarioId`/`version`/`schemaVersion`)、**置き場**(`tests/fixtures/sync-protocol-failures/`。`contracts/` はハーネス設計書 4 章が NFR-019(a) の共通正解に限定しているため使わない)、**契約**を本書 10-3 で確定した。

**CI ジョブ名は本書では決めない** — ジョブ表の正本はハーネス設計書 10.1 であり、本書が決めると 1 テーマ 1 正本の原則に反する。**台帳 H-57 へ本書の安定節 ID(`10-3`)で受け渡し**、残件を「Phase 4 の CI 拡張で 10.1 へ (b)(d) のジョブ行を追記」とした。**`H-*` の新規採番はしていない**(H-77 が未対応のため)。

### ステップ 18(旧システムを典拠とする記述)

| 欠陥 | 照合先の原典 | 結果 |
| --- | --- | --- |
| **LG-01** | 本書 2-3 の禁じる別名表(`プレイ番号 / プレイの番号` = 旧システムの 88 列の列名) | **是正**。退避取り込み手順の「相当するプレイ番号」が **D2 の意味での使用**で、自ら定めた別名規律に違反していた。**「論理位置(D2)」**へ改めた |
| **LG-02** | `services-shell.md:151`・`:153`・`:157`・`:158` / 改善台帳 **I-26**(`docs/improvements-from-baseball-scoring.md:212-216`) | **是正**。W1 の「旧システムはこれが無く」は**無典拠かつ不正確** — 旧システムには**保存前の差分プレビュー**と**編集行以降の前方再計算**が存在する。無いのは**永続化される履歴**。主張を永続履歴に限定し、I-26(手動状態補正は「履歴に残らない生の上書き」)を典拠として付けた |
| **LG-03** | `services-shell.md:150`(編集可能 14 項目)/ `:151`・`:158`(いずれも「上書き」を含む) | **是正**。別名表の「上書き / スキップ」行が `:150` を典拠にしていたが、同行は編集可能項目の記述で**どちらの語も含まない**。「上書き」の典拠を実際に該当する `:151`・`:158` へ差し替え、**「スキップ」は旧システム由来の欄から外して NFR-015 の要件語として別行に分けた**(旧調査 9 本での「スキップ」の出現は `domain-logic.md:167` の pytest 実行の文脈 1 件のみで、**旧システムの挙動を指す用法は 0 件**) |
| **LG-04** | `data-layer.md:285-286`(`get_play_list` は `プレイの番号` 順・`delete_last_play` は `MAX(id)`)/ `baseball-scoring-db-structure.md:243`(索引 `(試合_id, プレイの番号)` の用途 = 再開・undo 時の最終プレイ特定) | **是正**(典拠の追加のみ)。「旧システムも時刻列を持ちながら順序決定には使っていない」の典拠が**時刻列の存在しか示していなかった**。主張自体は正しいため、**順序決定に何を使っているか**を示す 3 典拠を補った |
| **LG-05** | `research/README.md:3` / `baseball-scoring-db-structure.md:3`(**対象リビジョンは双方 `ed6a20f`**)/ `README.md:36`(移行に関しては `data-layer.md` が事実の正) | **是正**。**両原典と逐語一致する文は作れない**(同一リビジョンを対象と宣言しており時点差で説明できない)ため、**裁定を落として「未裁定の矛盾」として送る形**へ書き換えた。従前の裁定は README:36 の優先規定と**逆向き**だった。**未申し送りだった第 3 の食い違い(PG 接続プール — `SimpleConnectionPool(1,3)` 対 `ThreadedConnectionPool` min1/max10 で**クラス名と最大接続数の両方**が食い違う)を追加**。「同期規則の結論には用いない」という判断は全数確認済みのため維持 |
| **LG-06** | `services-shell.md:150`・`:156-157` / `data-layer.md:283-284` | **(a) 正しい典拠へ差し替え**。「訂正は既存行の UPDATE で番号は変わらない」の典拠が別内容(UX 指摘)を指していた。**編集可能 14 項目に `プレイの番号` を含まない**ことと `update_play` / `update_plays` の UPDATE を典拠とした |
| **LG-07** | `services-shell.md:153`(差分プレビュー) | **(a) 正しい典拠へ差し替え**。「訂正は上書きで履歴を残さない」の典拠が**主張と逆方向**(差分プレビューの存在)だった。主張自体は正しいため、**LG-02 と同じ形で「永続履歴が残らない」へ限定**し、`:153` は**存在する機能の側**として引き直した |
| **LG-08** | `data-layer.md:402`(`プレイの番号` に UNIQUE 制約はなく重複し得る) | **(b) 主張を「不明」へ弱めた**。原典が支えるのは**重複のみ**で、**欠番については記述が無い**。支持典拠が存在しないため (a) では閉じられず、**「欠番があり得るかは原典から確認できず不明」**として移行仕様タスクへの申し送りに含めた |

**既知欠陥 40 件がこれですべて閉じた**(機械検出 17 件は検出器が green、人間照合 23 件は上表と既出の記録で照合済み)。

### ステップ 19(引用の識別子参照化)

**射程を「可変文書に限る」へ絞った(人間の判断 2026-08-29)。** 計画の DoD は「3 形式すべて 0 件」だったが、次の 3 点から**版固定アーカイブ `docs/legacy/` への行番号引用は残す**と決めた。

- 禁止の根拠は台帳 **H-20**(行番号引用が上位改訂で無言に壊れる)だが、`docs/legacy/` は **AGENTS.md 絶対規則 3 で変更禁止**であり、**行番号が腐らない**。禁止の射程外である
- 本書 **2-6 の第 3 項**が既に「調査記録の引用では行番号が証拠として機能する」と扱いを分けており、**検査器が規約より広く禁じていた**(検査器側の不足)
- 識別子化すると `services-shell.md` への **8 件がすべて同一の安定 ID「5」へ潰れ**(ステップ 18 で `:150` 編集可能 14 項目 / `:151` 前方再計算 / `:153` 差分プレビュー / `:156-157` 直前・過去修正 / `:158` 手動状態補正 と書き分けた区別が消える)、**3 件が `document`(識別子なし)へ落ちる**(`README.md` の優先規定と対象リビジョン・`db-structure.md` の対象リビジョン = **LG-05 の「同一リビジョンだから時点差で説明できない」という論証の根拠**)

**「参照先の規範が同一か」の全件確認を機械化した。** 帰属抽出器 `extract_citations` が**行番号引用と識別子参照の双方を同じ 4 要素キーへ解決する**ため、置換前の引用集合 **315 件**をベースラインに取り、置換後のダンプと `diff` して**無差分**を確認した。計画は「1 件ずつ確認して変わった箇所を次ステップへ引き継ぐ」としていたが、**引き継ぎは 0 件**である(粒度が行から条・節へ変わっても、解決先の安定 ID がすべて一致した)。

| 対象 | 件数 |
| --- | --- |
| `REQ:<行番号>` → 識別子リンク | **279 → 0** |
| 可変文書パスの `.md:<行番号>` → 識別子リンク | **8 → 0**(要件書 4・改善台帳 2・`harness-evaluation.md` 1・ハーネス設計書 1) |
| `docs/legacy/` への行番号引用(**残す**) | **18**(`data-layer.md` 8・`db-structure.md` 5・`services-shell.md` 3・`README.md` 2) |
| 引用集合 | **315 件で不変** |

**副次の是正 2 件**:

- **`check_citation_format` を文脈依存にした** — `_claim_references` と同じ「直前の引用のパスを引き継ぐ」規則で裸の行番号を判定し、`docs/legacy/` 配下なら違反にしない。**参照先が不明な裸の行番号は違反**として扱う(行単位で引き継ぎを打ち切るため、従前より厳しい)
- **`STABLE_ID_RE` に `付録[A-F]` の単独形を追加した** — 可変文書側の相異なる安定 ID **39 種**のうち**`付録C` だけがラベル・フラグメントで表現できず**、該当 4 件がリンク化できなかった

**回帰テスト 4 本を追加**(legacy は不検出 / 可変文書は検出 / 裸の行番号の引き継ぎ / `付録C` がラベル・フラグメントの双方で読める)。テストは 702 → **709 件**。

## 未決・次の一歩

- **N-5(短縮案の採否)・N-6(`H-*` の新規採番)は人間の判断事項**。計画確定前に決着が要る
- ~~N-1(U-1 の要件改訂)は本タスクの前提~~ → **失効(人間の判断 2026-08-28)**。計画レビュー 8〜11 周で **U-1 は新規設計になる**ことが判明し、**後続タスクへ送った**(要件書 v2.5 + 同期正本 v0.2 を同一ゲート)
- 次は /plan(スコープ確定と実装ステップの設計)
