---
date: 2026-09-11
topic: backend CI の残り red 1 件 — 文言走査が追跡済みの生成物(.coverage)を読んで落ちる
branch: fix/coverage-artifact-scan
---

# 作業ログ: 2026-09-11 backend CI の残り red 1 件(追跡済み生成物の走査)

## やったこと

- `/task-start` — worktree `../pitchlog-worktrees/fix-coverage-artifact-scan` を
  `origin/develop`(`964e0ce`)から作成。Notion タスクを `進行中` へ。
- **着手前に原因を原典で確定させている**(TSK-317 fix の PR #53 のあと・TSK-343 との突合を経て)。

## 決定

## 未決・次の一歩

## 計画段階(2026-09-11)

### `/investigate` を省いた

原因が PR #53 のレビュー往復と TSK-343 との突合で**実測まで確定していた**ため。
**未確定なのは `--cov` の消去機構と競合の有無だけで、それは本計画の判断に要らない。**

### 計画レビュー(コア領域 — `sol xhigh`)5 周

| 周 | 判定 | P0 | P1 | P2 | 起因 | 主な指摘 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 否決 | 0 | 5 | 3 | **全件 非起因** | `grep -c coverage` の母集団が誤り / コミット前に成立しない条件 / 負例が循環 / 単一正本性が未確定 / 到達不能な DoD |
| 2 | 否決 | 0 | 5 | 1 | **全件 起因** | **`--no-index` 欠落で検査が空洞** / 「実装が名前を知らない」は検証不能 / **`core-guard` で到達不能** |
| 3 | 否決 | 0 | 3 | 1 | **全件 起因** | 出力の事後選別ではローカル設定を排除できない / **ステップ 2 の負例を Claude が回せない** / 申し送りが DoD と不同期 |
| 4 | 否決 | 0 | 2 | 3 | 起因 4 / 非起因 1 | **母集団は index なのに述語を `HEAD` で評価** / PO 裁定の省略 / `NFR-018` の引用範囲の誤り |
| 5 | **可決** | **0** | **0** | **1** | — | `NFR-018` の範囲説明(**8 種は (β) の列挙で、(α) は別**) |

**採用 20 件 / 不採用 0 件**(**射程の訂正 1 件を含む** — 下記)。

### 実測で確定させたこと(計画段階で 5 件)

| # | 実測 | 効果 |
| --- | --- | --- |
| 1 | **`git ls-files \| grep -c coverage` は 7 を返す**(無関係な 6 件 + `backend/.coverage`) | **「0 になる」という合格条件は永久に満たされない**。完全パスの述語へ統一 |
| 2 | **`git rm --cached` 後に `D  backend/.coverage` が `git status --porcelain` に出る** | **コミット前には成立しない条件だった**ので前後に分けた |
| 3 | **追跡済みのまま `git check-ignore -v` は rc=1・出力なし。`--no-index` で rc=0** | **通常形で書くと恒久検査が追跡済みファイルを永久に検出できず空洞になる** |
| 4 | **DSN を外して `pytest tests/test_package.py` は 1 passed・`tests/db/...` は fail** | **母集団の契約と負例を `backend/tests/` 直下に置けば Claude が回せる** |
| 5 | **`.gitignore` へ `.coverage` を足しても `ls-files --cached --others --exclude-standard` は列挙し続ける** | **修正の本体は追跡の解除**であり `.gitignore` は再発防止にすぎない |

### 規範の射程の訂正(4 周目 `P1-2`)

**レビューは「PO 裁定の省略は 7.3-6 違反」と指摘した。原典を読むと 7.3-6 は計画レビューを拘束しない。**

- **7.3 の見出しは「正本確定ゲート」**で、フローは `draft → in-review → 人間承認 → approved`、**担い手は `/finalize-doc`**
- **変更履歴の先例(v1.13)も確定ゲートの事案**
- **`/plan` には終端条件の規定が無い** — これ自体が台帳の候補で **TSK-340 として起票済み**

**ただし私が 4 周目の依頼文で「7.3-6 に形式的に該当する」と書いたのが不正確な引用であり、
それを拘束規則として読んだのは自然である。** 引用の側を訂正した。

**実質は指摘のとおりだったので判断を PO へ上げ、裁定を得た** —
**「5 周目を回して可決を目指す」(2026-09-11・山田正輝)**。**その周で可決した。**

### 承認

**2026-09-11・山田正輝。** 実装(3 ステップ)へ進む。
