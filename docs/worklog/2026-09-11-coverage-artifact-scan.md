---
date: 2026-09-11
topic: backend CI の残り red 1 件 — 文言走査が追跡済みの生成物(.coverage)を読んで落ちる
branch: fix/coverage-artifact-scan
---

# 作業ログ: 2026-09-11 backend CI の残り red 1 件(追跡済み生成物の走査)

## やったこと

- `/task-start` — worktree `../pitchlog-worktrees/fix-coverage-artifact-scan` を
  `origin/develop`(`964e0ce`)から作成。Notion タスクを `進行中` へ。
- **着手前に原因を原典で確定させている**(TSK-317 fix の PR #53 のあと・TSK-343 との突合を経て)。

## 決定

## 未決・次の一歩

## 計画段階(2026-09-11)

### `/investigate` を省いた

原因が PR #53 のレビュー往復と TSK-343 との突合で**実測まで確定していた**ため。
**未確定なのは `--cov` の消去機構と競合の有無だけで、それは本計画の判断に要らない。**

### 計画レビュー(コア領域 — `sol xhigh`)5 周

| 周 | 判定 | P0 | P1 | P2 | 起因 | 主な指摘 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 否決 | 0 | 5 | 3 | **全件 非起因** | `grep -c coverage` の母集団が誤り / コミット前に成立しない条件 / 負例が循環 / 単一正本性が未確定 / 到達不能な DoD |
| 2 | 否決 | 0 | 5 | 1 | **全件 起因** | **`--no-index` 欠落で検査が空洞** / 「実装が名前を知らない」は検証不能 / **`core-guard` で到達不能** |
| 3 | 否決 | 0 | 3 | 1 | **全件 起因** | 出力の事後選別ではローカル設定を排除できない / **ステップ 2 の負例を Claude が回せない** / 申し送りが DoD と不同期 |
| 4 | 否決 | 0 | 2 | 3 | 起因 4 / 非起因 1 | **母集団は index なのに述語を `HEAD` で評価** / PO 裁定の省略 / `NFR-018` の引用範囲の誤り |
| 5 | **可決** | **0** | **0** | **1** | — | `NFR-018` の範囲説明(**8 種は (β) の列挙で、(α) は別**) |

**採用 20 件 / 不採用 0 件**(**射程の訂正 1 件を含む** — 下記)。

### 実測で確定させたこと(計画段階で 5 件)

| # | 実測 | 効果 |
| --- | --- | --- |
| 1 | **`git ls-files \| grep -c coverage` は 7 を返す**(無関係な 6 件 + `backend/.coverage`) | **「0 になる」という合格条件は永久に満たされない**。完全パスの述語へ統一 |
| 2 | **`git rm --cached` 後に `D  backend/.coverage` が `git status --porcelain` に出る** | **コミット前には成立しない条件だった**ので前後に分けた |
| 3 | **追跡済みのまま `git check-ignore -v` は rc=1・出力なし。`--no-index` で rc=0** | **通常形で書くと恒久検査が追跡済みファイルを永久に検出できず空洞になる** |
| 4 | **DSN を外して `pytest tests/test_package.py` は 1 passed・`tests/db/...` は fail** | **母集団の契約と負例を `backend/tests/` 直下に置けば Claude が回せる** |
| 5 | **`.gitignore` へ `.coverage` を足しても `ls-files --cached --others --exclude-standard` は列挙し続ける** | **修正の本体は追跡の解除**であり `.gitignore` は再発防止にすぎない |

### 規範の射程の訂正(4 周目 `P1-2`)

**レビューは「PO 裁定の省略は 7.3-6 違反」と指摘した。原典を読むと 7.3-6 は計画レビューを拘束しない。**

- **7.3 の見出しは「正本確定ゲート」**で、フローは `draft → in-review → 人間承認 → approved`、**担い手は `/finalize-doc`**
- **変更履歴の先例(v1.13)も確定ゲートの事案**
- **`/plan` には終端条件の規定が無い** — これ自体が台帳の候補で **TSK-340 として起票済み**

**ただし私が 4 周目の依頼文で「7.3-6 に形式的に該当する」と書いたのが不正確な引用であり、
それを拘束規則として読んだのは自然である。** 引用の側を訂正した。

**実質は指摘のとおりだったので判断を PO へ上げ、裁定を得た** —
**「5 周目を回して可決を目指す」(2026-09-11・山田正輝)**。**その周で可決した。**

### 承認

**2026-09-11・山田正輝。** 実装(3 ステップ)へ進む。

---

## 結果サマリ(`/pr` クローズ処理・2026-09-11)

### 実装(3 ステップ・差し戻し 0 回)

| # | 変更 | 自分で確認したこと |
| --- | --- | --- |
| 1 | **`backend/.coverage` の追跡解除 + `.gitignore`** | **6 条件**(完全パスでの追跡・`check-ignore` の一致・`D ` の 1 行・母集団から消失・ファイル残存・**ハッシュ一致**)+ **コミット後に `git status` が空** |
| 2 | **文言走査の母集団の絞り込みと、読めないパスの明示的な扱い** | **3 変異すべてが red**(`--others` 落とし / 黙って飛ばす / テキスト判定外し) |
| 3 | **追跡済み生成物の恒久検査を新設** | **3 変異すべてが狙った負例で red**(追跡状態の無視 / HEAD 評価 / 実リポジトリ評価) |

### 実機の測定値(**CI と同一コマンド**・DSN の実値が必要なため人間が実行)

```
cd backend && uv run pytest -c pyproject.toml --cov
→ 168 passed in 421.91s (0:07:01)
```

| 対象 | 結果 |
| --- | --- |
| **CI と同一コマンドの無選別全件** | **168 passed / 421.91s(7 分 01 秒)・failed と error が 0** |
| 件数の整合 | **develop の CI が `1 failed / 162 passed = 163`** → **本ブランチ 168**(**失敗 1 件が消え、新設 5 件が増えた**) |
| カバレッジ | **40% → 71%**(前回は DB テストが全部 setup error で本体に到達していなかった) |
| `tests/test_repository_artifacts.py` | **8 passed** |
| `backend/tests/test_wording_scan.py` | **5 passed**(**DSN 無しでも回る**) |
| ルート `uv run pytest tests/` | **1202 passed**(既存 1194 + 新設 8) |
| ルート `ruff check` / `ty check` | green |
| backend `ruff format --check` / `ruff check` / `ty check` | green |
| **`contracts/` の差分** | **0** |

**DoD の「無選別で全件」を守った** — `-k` / `-m` / `--deselect` / パス指定を使っていない。

### 3 段それぞれの実効(**実測で確認**)

| 段 | 何を止めるか | 実測 |
| --- | --- | --- |
| **1**(混入) | 生成物が index へ入ること | **恒久検査 8 passed**・**本番経路の 3 変異すべてが狙った負例で red** |
| **2**(churn が広い add に乗る) | 追跡済み生成物の更新が status に出ること | **`--cov` が `.coverage` を 53248 → 69632 bytes へ書き換えたのに `git status` が空**(**修正前ならここで `M backend/.coverage` が出て、それが `71c3d86` の混入の起き方そのものだった**) |
| **3**(走査が踏む) | 読めないものを母集団に入れること | **168 passed**(**失敗 1 件が消えた**)・**3 変異が red** |

### 途中で踏んだこと 2 件

#### ① 委任文の設計を誤った(ステップ 1)

**`git rm --cached` を Codex へ投げた。** **CLAUDE.md の役割分担で git 操作は Claude の担当**であり、
**加えて Codex は sandbox で `uv` のキャッシュすら read-only なので `.git/index` にも書けない**。

**Codex は「期待値 `D` には未到達」と正直に報告した** — **黙って通さなかったので機構は健全に働いた**。
**ステップ 2・3 の委任文には「リポジトリの git index を操作しないでください(一時リポジトリの中は除く)」を明記した。**

#### ② 台帳候補①(SQLAlchemy 形式の URL)が 2 例目として再発した

**最初の `--cov` 全件実行が `103 errors` で落ちた。**
全件 `missing "=" after "postgresql+psycopg://..."` で、**DB へ 1 回も到達していない**
(**65 passed は DB を使わないテスト**)。

**TSK-343 が ORM 作業で DSN を SQLAlchemy 形式にしたため。**
**切り分けは候補①に書かれた手順どおり、スキームだけ外して実行すれば 1 回で付いた。**

**候補①の昇格条件は「別の環境で再発したら `H-*` を与える」。**
**今回は同じマシン・別 worktree なので厳密な別環境ではないが、別タスク・別セッションでの 2 例目である。**
**採番の判断は台帳の運用者へ委ねる。**

**この事象は本タスクの変更とは無関係**であり、**スキームを外した実行は 168 passed で green**。

### 台帳と申し送り

**台帳へ候補 2 件を追記**(`H-*` の新規採番なし・版は上げない):

1. **検証コマンドを人が選ぶと、CI が走らせるコマンドとの差分が黙って残る**(**本文は TSK-343 が起草**)
2. **クローズ処理の一括 add が生成物を正本リポジトリへ取り込み、3 週間後に別の検査の母集団に入って壊れた**

**既存候補「SQLAlchemy 形式の URL を DSN に渡すと DB テスト全件が到達不能と区別できない形で落ちる」へ
2 例目を追記**(**本タスクの `--cov` 全件実行で再発**)。

**申し送り 3 件を起票**:

| # | タスク | 優先度 |
| --- | --- | --- |
| 1 | **検証手順へ「CI 緑を主張するときは CI のコマンドで確認する」を明記する** | 中 |
| 2 | **`/task-done` へ「マージ後に develop の run が全ジョブ green」を明記する** | 中 |
| 3 | **backend のカバレッジ設定(`[tool.coverage]`)を整備する** | 低 |
