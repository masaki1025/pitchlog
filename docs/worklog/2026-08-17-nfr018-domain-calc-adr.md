---
date: 2026-08-17
topic: NFR-018 実現方式 ADR の確定(状況計算の配置 + 付録A 契約化)
branch: feature/nfr018-domain-calc-adr
---

# 作業ログ: 2026-08-17 NFR-018 実現方式 ADR の確定

## やったこと

- /task-start: 引数を既存 Notion タスク TSK-227(Phase 4-3 のクローズ処理で起票済み)と同定し、新規起票はしていない → `feature/nfr018-domain-calc-adr` ブランチ + worktree 作成(origin/develop = 28cc738 起点)→ 計画書雛形・本 worklog 作成 → Notion を 進行中 へ

## 決定

- slug は `nfr018-domain-calc-adr`(対象の NFR 番号 + 主題。前例 `req-v1-9-nfr021-wsl2` に倣い NFR 番号を含める)
- **(人間裁定 2026-08-17)** 論点 A = **(B) ADR 様式に変更履歴表を導入する** — 台帳の候補 (7) をこの経路で決着させる。テンプレート改訂 + 既存 ADR-001・002 への遡及付与 + `check_docs_status.py` の grandfather 解除。**設計書の版繰り上げを誘発しない**(免除区分を 7.1-3 に書く案 (A) だと設計書の確定ゲートが別途必要になる)。候補 (8) の是正経路も開く
- **(人間裁定 2026-08-17)** 論点 B = **(ii) 単一の規範的契約 + 検証された 2 実行系**を基調とする — Python と TS で各々実装し、共通ゴールデンベクタ・差分テスト・プロパティテスト・変異テストの 4 層で縛る。**要件文言「単一の共有実装」の解釈変更を伴う**ため、ADR 本体で根拠を積み確定ゲートの敵対レビューに掛ける

- /investigate: 調査サブエージェント 3 本(spec-checker / legacy-analyst / decision-tracer)を並列実行 + Web 調査を /research(Codex)へ委任 → 決定的な引用と食い違いを原典で裁定 → `research.md` へ統合

## 調査で判明した重要事項

- **「配置」は既に要件で決着していた** — 断中のクライアント計算は FR-005 の受入基準、サーバー再計算は 4.0-2 の確定記録、両側に計算が存在することは R-3 で受容済み。**ADR が決めるのは「実現方式」だけ**
- **ADR の核心は要件解釈** — NFR-018 の「単一の共有実装」と R-3 の「両方に存在する」が緊張関係にあり、その解決が NFR-018 自身から ADR へ委任されている
- **⚠ 着手前に PO 裁定が要る**(台帳の候補 (7))— ADR 様式が未決で、テンプレートのまま ADR-003 を作ると `check_docs_status.py` が落ちる。`scripts/check_docs_status.py:56` のコメントが「将来 ADR を標準テンプレートから作ると検査が落ちる」と**意図した挙動**として明記している
- **正本の記述誤りを 2 件検出・裁定した** — ① 設計書 6.3 と台帳 H-54 の「3 箇所コピー = 改善台帳 I-2」は誤りで**正しくは I-4**(I-2 はテナント分離)② 台帳 H-55 の「WHIP/FIP の二重実装」は版固定資料と矛盾し、**実際に二重なのは被打率・投球回**(WHIP/FIP は `cal_stats.py` にしか存在しない)。**H-55 の主張自体は成立する — 例が違うだけ**
- **旧システムの最重要の教訓** — 旧要件書は NFR-011(Should)で単一実装を要求したが**1 年以上解消しなかった**。ADR は「方針」ではなく「機構」を決めないと同じ結末になる

## 結果サマリ(/pr クローズ処理)

**当初の目的(ADR-003 の起案)は達成していない。** 計画レビュー 1 周目の P0 により、**先に要件書 NFR-018 を改訂する**方針へ転換し(人間裁定 2026-08-17)、ADR 起案を分離した。本 PR が納品したのは**その前提整備**である。

**実装したもの**(2 ステップ・2 コミット):

1. **ADR 様式の正規化**(`e6c8a99`)— テンプレートに frontmatter 3 行と変更履歴表を追加(変更履歴表はメタ表より前)/ ADR-001・002 へ遡及付与(版 1.0 据え置き・正規化自体も 1 行として記録)/ `check_docs_status.py` の免除辞書から ADR 2 件を削除 + コメント更新 / テストを残る免除対象へ付け替え + **テンプレート由来の ADR が検査を通ることを固定するテストを新規追加** / 索引の ADR 行を版 `1.0`・最終更新 `2026-08-17` へ
2. **正本の記録の是正**(`c96cb25`)— 設計書 6.3 と台帳 H-54 の典拠を I-2 → I-4 / 台帳 H-55 の事例を「被打率・投球回の二重実装」へ是正し責任投手モデルの片側修正を併記 / **候補 (7) を `H-62` へ昇格し対応済みへ** / 候補 (8) の昇格条件を現況化 / 変更履歴表に各 1 行

**正本への反映**: 5 本(ADR-001・ADR-002・設計書・台帳・索引)。要件書は反映なし。**うち ADR-001・002 と設計書 6.3 は approved 正本への変更**で、**7.3 の「軽微な誤字等は人間判断で確定ゲートを省略できる」に基づく裁定**(2026-08-17・山田正輝)のもと PR レビューに載せた。裁定の 3 条件は機械的に確認済み — ADR の `## 文脈` 以降は完全無差分、設計書 6.3 の境界表は当該典拠文字列の置換のみ(「含む/含めない」欄と発効条件は不変)。

**検証**: pytest 355 passed(+1)/ `check_docs_status.py` 緑 / 索引が全 9 文書で変更履歴表の実値と一致 / 変更 markdown のリンク切れ 0。**新規テストの実効性を変異 2 種(frontmatter 削除・変更履歴表の後置)で実測**し、いずれも落ちることを確認して復元した。

**frontend の品質ゲートは意図的に未実行**。`frontend/` と `contracts/` への変更が 0 件であることを差分で確認しており、CI 側も paths filter により `frontend` ジョブは skipped になる。

## 評価台帳への追記判断(/pr 手順 1-3)

**該当する → `H-63` を追記した(機能した点)。**

計画レビュー 1 周目が P0 として「ADR が解釈だけで NFR-018 を実質的に弱めようとしている」を検出し、**調査 4 本(spec-checker / legacy-analyst / decision-tracer / Codex の Web 調査)と Claude の推奨と人間の裁定が揃って傾いていた方向を、要件原典を典拠に覆した**。手戻り(ADR を書いてから要件改訂へ戻る)を着手前に回避できた事例として記録価値がある。

なお **`H-62`(ADR 様式の決着)はステップ 2 で既に追記済み**。本クローズ処理での追記は H-63 のみ。

## 未決・次の一歩

このタスクは**正本(ADR)の新設**であり、確定ゲート(/finalize-doc — 敵対レビュー + 人間承認)が必須。実装コードの比重は小さく、大半が設計判断になる見込み。

着手前に確かめるべき論点(/investigate で下調べ):

- **状況計算の実現方式の選択肢と実例** — 共有実装(単一言語で書いて両側から使う)/ 生成(1 つの定義から両言語へ生成)/ その他。pitchlog は backend = Python・frontend = TypeScript なので、言語をまたぐ共有の手段が論点になる
- **付録E マトリクスの完成状況** — 要件書は「全表の完成は設計フェーズの必須成果物」と書くが、どのスロットの担当かが未規定(Phase 4-3 の research.md の「不明」項目)
- **付録A 集計定義の契約化(固定ベクタ)の具体形** — 台帳 H-55 の必須項目
- **設計書 6.3 の条件付き除外の発効条件** — 「付録A 契約の実装完了後に発効」の「実装完了」を何で判定するか
- **旧システムの二重実装の実例**(WHIP/FIP・被打率/投球回・scoringReview.ts の判定表)— 何を防げばよいかの具体
