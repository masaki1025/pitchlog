---
date: 2026-08-15
topic: ガードの誤検知と fail-open の修正(F1)
branch: fix/guard-false-positive
---

# 作業ログ: 2026-08-15 ガードの誤検知と fail-open の修正(F1)

## やったこと

- /task-start: 既存タスク F1 に着手 → `fix/guard-false-positive` ブランチ + worktree 作成(origin/develop 起点・`cb15d85`)→ 計画書雛形・本 worklog 作成

## 本タスクで扱うもの(着手時点の把握)

台帳で `対応案` が **F1** の項目は 4 件。**すべて追跡優先度 高・状態 未対応**。

| 項番 | 事象 | 型 |
| --- | --- | --- |
| H-2 | カレントが保護ブランチだと `git push` を内容に関わらず全ブロックする | 誤検知 |
| H-9 | `git_guard` はブランチ解決に失敗すると **fail-open** する(docstring の宣言と実装の不一致) | **fail-open** |
| H-10 | `codex_guard` が「`codex` を含む正当な読み取り専用コマンド」を誤遮断する | 誤検知 |
| H-11 | `git_guard` の過剰遮断は `push` 以外にも及ぶ — 読み取り専用コマンドまで落ちる | 誤検知 |

あわせて台帳には F1 の完了条件に関する指定が 2 件ある(**3 種を分けてテストする** / **「止めてはいけないものを止めていないか」の負例を増やす**)。

## 本タスクの進め方(PO 指示 2026-08-15)

**軽く回す。** 直前の F5 は確定ゲート 11 周・差し戻し 2 回を要し、成果に対して工数が過大だった。
本タスクは **非コア領域のコード修正**で、現時点の見立てでは**正本改訂を伴わない**(伴うなら計画段階で申告する)。

- 計画レビューは**周回を絞る**
- **確定ゲートは不要**(正本の版繰り上げがない前提)
- 実装は**ステップ単位**を維持する(1 委任 = 1 ステップ = 1 コミット)。ここは安全に関わるので崩さない
- **変異テストは維持する** — F1 はガードそのものの修正であり、「壊したら落ちるか」を確認しないと**通したいものを通し、止めたいものを止めない**変更が素通りする

## 調査(2026-08-15)

実挙動を**ガードへ直接 payload を与えて再現**し、決定経緯は decision-tracer へ委任した。結果と典拠は [research.md](../features/guard-false-positive/research.md)。

**着手時の見立てを 2 点、覆した。**

- **F1 の対象は 4 件ではなく 5 件**だった。`worklog/2026-08-12-harness-evaluation.md:155-168`(F-ID の定義元)が **H-17 の負例・故障系テスト 3 種**を F1 の完了条件に含めており、**実装方針も確定ゲート 2 周目 P1-4 で既に確定済み**だった(サブコマンド位置での動詞判定 / `current_branch` を fail-closed へ)
- **「正本改訂を伴わない」は誤り**だった。設計書 `:308`・`:431` が「保護ブランチ**上での** push をブロック」という**カレントベース**の文言で、H-2 の修正と食い違う。**PO 裁定で PR レビュー扱い**(7.6-3 前段・版は上げない)とした

**台帳の記述より正確な原因が 1 件。** H-10 の「二重引用符ネストが shlex で解析不能」は**不正確**で、実測ではネストはすべて解析できる。真因は **`re.split` が引用符を無視してセグメント分割する**こと。`git_guard` にも同型があり、`echo "git push; ls"` のような**実行すらしない文字列**まで遮断されていた。

## 計画(2026-08-15)

計画レビュー 1 周で収束。**P0 が 2 件出て、どちらも実装前に閉じるべき欠陥だった。**

- **P0-1**: `push` をカレント判定から**単に外す**と、`git push`(引数なし)・`origin` のみ・`--all`・`--mirror` が **develop へ通る**。上段の宛先判定は明示 refspec しか見ていない → **宛先ベースの三値判定**(非保護と証明できたときだけ許可 / 保護・force は遮断 / **宛先未解決も遮断**)へ
- **P0-2**: 引用符対応だけを入れると、`bash -lc 'echo ok; codex exec x'` が **1 セグメントになって見逃される**(現行は粗い `re.split` が偶然遮断していた)→ **`shell -c` 本文の再帰評価を同じステップで**入れる

## 実装(ステップ 1〜5)

| # | 内容 | 差し戻し |
| --- | --- | --- |
| 1 | `guard_common.shell_segments` 新設(引用符を考慮した分割) | **1 回** |
| 2 | `codex_guard` の差し替え + `shell -c` 本文の再帰評価 | **1 回** |
| 3 | `git_guard` の差し替え + Git 呼び出し解析 + 宛先ベース push 判定 | **1 回** |
| 4 | `current_branch` の三値化と fail-closed | — |
| 5 | 正本の節更新 + 台帳更新 | — |

テスト **285 → 354 passed**。

### 差し戻し 3 件 — いずれも委任先の自己申告は「green・変異も期待どおり」だった

| # | 欠陥の型 | 内容 |
| --- | --- | --- |
| 1 | **under-block** | 語中の `#` をコメント開始として扱い、`a#b; codex exec x` の後半が飲み込まれて**遮断が外れる**。bash は語頭の `#` だけをコメントにする |
| 2 | **テスト欠落** | fail-closed 経路(`shell_segments` が `None`)にテストが無く、**外しても 305 passed**。しかも外すと exit code が 2 → 1 になり、hooks は 2 でしか遮断しないため**素通り**する |
| 3 | **over-block** | **`git push -u origin <branch>` が遮断**される。`/pr` 手順 3-1 の実形であり、**マージしていれば以後すべての PR が作れなくなっていた** |

**3 件目は変異テストでは捕まらなかった。** 指示した 18 件は全件通り、委任先の変異 4 種もすべて期待どおり落ちていた。検出できたのは、**指示に入れていない日常的な push 形式を PR 作成者が独自に試したため**。→ **H-53 として起票**。

### 検証の方法

各ステップで **Codex の自己申告を受け取らず、PR 作成者が再現**した。

- **実挙動**: ステップ 3 では指示 18 件 + **独自の境界 14 件** + **日常的な push 形式 7 件**
- **変異**: 全ステップ合計 11 種。とくにステップ 3 の「`PROTECTED` を空集合にする」で **19 failed**(遮断側の全無効化を検出できる)

**検証中に自分の判断を 1 度訂正した。** ステップ 4 で `/tmp` 配下に一時ディレクトリを作り「非リポジトリなのに遮断された」と欠陥だと考えたが、調べると**このマシンには `/tmp/.git` が実在**していた。git は非リポジトリと診断する一方でファイルシステム走査は `.git` を見つけるため、実装が保守的に「不明」へ倒していた。**実装が正しく、検証側の前提が誤り**だった。

## 正本への反映

- **設計書**(節更新・**版は 1.5 のまま**): 6.2 と 8.3 の `git_guard` 行を**宛先ベース**へ是正し、**`commit`・`merge`・`rebase` はカレント判定のまま**と**書き分けた**
- **台帳**: H-2・H-9・H-10・H-11 を `対応済み`、**H-10 の事象を真因へ全面訂正**、H-17 を `部分対応`。**H-51**(ヒアドキュメント本文の誤検知)・**H-52**(設計書 12.1 の記述欠落)・**H-53**(指示の盲点)を新設し、**H-42 の再発を 4 件目**として記録。**版は 1.0 のまま**
- **索引**: 両者の最終更新を現行化

## 作業中に踏んだ誤検知 — 計 7 回

F1 の調査・実装中に、**私自身のコマンドが 7 回ガードに止められた**。台帳 H-28 の「**ブロックされたら別経路を探す**が既定動作になる」が、F1 の作業中にそのまま再現した形になる。

うち **2 件は本タスクの `shell_segments` で解消**する(引用符内のセパレータ)。**残りはヒアドキュメント本文の扱い**で、**本タスクでは直らない** → **H-51** として起票した。

## ステップ 5 で H-42 の型を自分で再発させた

台帳へ H-51・H-52 を追記する際、`## 候補` を**文字列検索**して挿入したところ、**最初の出現が変更履歴の文中**(バックティックで囲まれた言及)だったため**変更履歴の行を割って差し込まれ**、`check_docs_status.py` が exit 1 になった。**F2 のステップ 1 で同じ型の破損を修復したのは私自身**である。復元し、**行単位の完全一致かつ一意**で挿入し直した。

**F2 で機構化した「見出し行のバックティック検査」が即座に検出した**ため、今回は台帳に破損が残らずに済んだ。前回は人力レビューが拾うまで残っていた。

## 決定

- **設計書の節更新は PR レビューで通す**(7.6-3 前段・版は上げない — PO 裁定 2026-08-15)
- **`push` はカレントブランチで判定しない**。宛先で判定し、**解決できないものは遮断**する
- **`current_branch` は三値**。非リポジトリと**積極確認**できた場合のみ許可し、それ以外の不明はすべて遮断する
- **委任結果の受け取りでは、指示に列挙していない境界を PR 作成者が独自に立てて実測する**(H-53)

## 未決・次の一歩

- **H-51**: ヒアドキュメント本文の誤検知(F1 中に 4 回踏んだ)。任意の heredoc を一律データ扱いにすると実行可能本文まで隠しうるため、安全に広げられる条件の設計が先
- **H-52**: 設計書 12.1 に脅威モデルの節を設ける(正本の新設 → **確定ゲートが要る**)か、変更履歴の参照先を訂正する
- **H-17 の残り 2 件**: `format_on_save` のテスト 0 件 / `session_context` の worklog 注入テスト
- 本 PR は **`guard_paths` に該当しない**(触ったのは `.claude/hooks/**`)。**core-guard は発火しない**
