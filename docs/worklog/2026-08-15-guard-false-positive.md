---
date: 2026-08-15
topic: ガードの誤検知と fail-open の修正(F1)
branch: fix/guard-false-positive
---

# 作業ログ: 2026-08-15 ガードの誤検知と fail-open の修正(F1)

## やったこと

- /task-start: 既存タスク F1 に着手 → `fix/guard-false-positive` ブランチ + worktree 作成(origin/develop 起点・`cb15d85`)→ 計画書雛形・本 worklog 作成

## 本タスクで扱うもの(着手時点の把握)

台帳で `対応案` が **F1** の項目は 4 件。**すべて追跡優先度 高・状態 未対応**。

| 項番 | 事象 | 型 |
| --- | --- | --- |
| H-2 | カレントが保護ブランチだと `git push` を内容に関わらず全ブロックする | 誤検知 |
| H-9 | `git_guard` はブランチ解決に失敗すると **fail-open** する(docstring の宣言と実装の不一致) | **fail-open** |
| H-10 | `codex_guard` が「`codex` を含む正当な読み取り専用コマンド」を誤遮断する | 誤検知 |
| H-11 | `git_guard` の過剰遮断は `push` 以外にも及ぶ — 読み取り専用コマンドまで落ちる | 誤検知 |

あわせて台帳には F1 の完了条件に関する指定が 2 件ある(**3 種を分けてテストする** / **「止めてはいけないものを止めていないか」の負例を増やす**)。

## 本タスクの進め方(PO 指示 2026-08-15)

**軽く回す。** 直前の F5 は確定ゲート 11 周・差し戻し 2 回を要し、成果に対して工数が過大だった。
本タスクは **非コア領域のコード修正**で、現時点の見立てでは**正本改訂を伴わない**(伴うなら計画段階で申告する)。

- 計画レビューは**周回を絞る**
- **確定ゲートは不要**(正本の版繰り上げがない前提)
- 実装は**ステップ単位**を維持する(1 委任 = 1 ステップ = 1 コミット)。ここは安全に関わるので崩さない
- **変異テストは維持する** — F1 はガードそのものの修正であり、「壊したら落ちるか」を確認しないと**通したいものを通し、止めたいものを止めない**変更が素通りする

## 決定

## 未決・次の一歩

- 次の一歩: `/investigate` で 4 件の実挙動と典拠を確認 → `/plan`
