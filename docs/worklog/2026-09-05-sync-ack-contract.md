---
date: 2026-09-05
topic: 同期プロトコル — ACK の応答契約と再送・undo の同期上の表現(後続 β)
branch: feature/sync-ack-contract
---

# 作業ログ: 2026-09-05 同期プロトコル — ACK の応答契約と再送・undo の同期上の表現(後続 β)

## やったこと

### /task-start

- Notion タスク(**後続 α の /task-done で起票済み**。重複起票はしていない)を取得
- worktree `../pitchlog-worktrees/feature-sync-ack-contract`(ブランチ `feature/sync-ack-contract`・
  起点 `origin/develop` = `d357f1f`)を作成
- 計画書雛形 `docs/features/sync-ack-contract/plan.md` と本ログを作成
- Notion のステータスを **進行中** へ・ブランチ名をタスクへコメント

## 決定

- **重さ分類はコア領域**(同期プロトコル)。sol xhigh・敵対レビュー・**人間の逐行確認必須**
- **後続 α の作法を再発明しない** — オラクル照合(期待集合を TS 側に持たない)/ 注入境界は未注入で fail-closed /
  値の形式を検査しない / ID は正本 ID / 単一 locus / 射程外の不在 assert を壊さない /
  **永続変更の公開経路は receipt・preparation を要求する**(`prohibitions.spec.ts` の H-78 検査が既にある)

## 着手時点で分かっている前提

**後続 α(PR #44・`d357f1f`)が注入境界を 7 点空けて待っている。** 本タスクがその供給側を実装する。

| α 側の注入境界 | 本タスクで供給するもの |
| --- | --- |
| A5 結果(受理 / 重複 / 拒否 / 退避 / 未処理) | **7-1 の ACK 応答契約** |
| B3 の理由分類(内容起因 / `O4`) | 同上 |
| A4 の写像確定(`C4` のゲート) | **4-4 の `C1`**(応答で写像を返す) |
| `accepted_at`(`I6` の起点) | **7-1 の P3 応答** |

**α のコードは現状どの遷移も発火しない**(すべて fail-closed)。本タスクで初めてキューが回る。

## 未決・次の一歩

- **/investigate** — 7-1(`A1`〜`A5`・`B3a`/`B3b`)/ 7-3(ACK 消失後の再送)/ 7-6(undo の `U1`〜`U6`・`U2-a`〜`U2-c`)の全数把握。
  **`DI1`〜`DI5`・`I1`・`B3a` は後続 γ(6 章依存)との境界**をどこで切るかの確認。
  **`C1` の写像返却がクライアント側だけで閉じるか**(サーバー適用は γ)
- **/plan** — 実装ステップ表と「やること / やらないこと」の確定 → 計画レビュー → 人間承認
- **α の申し送り**: `NFR-020` の 6 前提不成立時の挙動 / `U-3` の保持期間 / **6-3 の B5 通知の持ち主**が未定。
  本タスクの射程に入るかを /investigate で判定する

## /plan(2026-09-05)

- 計画書を作成 → **敵対レビュー(コア領域)1 周目は P0 5・P1 4・P2 1**。**射程の取り違えが 3 件**あり、
  いずれも原典で確認して是正した(供給元マトリクス / `B3` の 2 択 / `U3`・`U4`・`U6` は状況判定)
- **10 → 12 ステップ**へ全面改訂 → **人間承認 2026-09-05・山田正輝**

## /implement(2026-09-05〜06)

| ステップ | 内容 | テスト |
| --- | --- | --- |
| 1 | D1 ACK 封筒の型と fail-closed パーサ | 444 |
| 2 | 境界結果の受け取り側(`B1`〜`B4` に限定) | 462 |
| 3 | 拒否理由の分類(「内容起因 / `O4`」まで) | 474 |
| 4 | **`classifyB3` の型是正**(α の変更) | 481 |
| 5 | `A4` 写像の受け取り(当該イベントへ結合) | 496 |
| 6 | P3 独立応答の封筒(5 要素の全組) | 524 |
| 7 | **A5 の永続遷移 mutation**(α の変更) | 533 |
| 8 | アダプタ統合(**4 点だけ供給**) | 538 |
| 9 | 再送範囲の決定(`D3` は範囲決定にだけ) | 549 |
| 10 | `U1` の取消イベント投入 | 559 |
| 11 | B5 の通知記述子(6 → 7 種)**Claude 直接実装** | 561 |
| 12 | ハーネス登録 | — |

### ステップ 11 を Claude が直接実装した理由と手続

**Codex の実行が環境要因で 12 回連続して強制終了した**ため、CLAUDE.md の例外規定に従って直接実装し、
**`codex_run.py review normal` を通した**(**P0 なし・P1 なし・P2 1 件**)。
P2(**単語の存在だけを見るテストは劣化した文面でも green になる**)を反映し、
`再ログインしてください` と `未送信の記録はそのまま保持` という**句で固定**した。

### 実測: 背景実行の強制終了(**通算 13 回**)

**後続 α から持ち越した候補の観測を更新する。**

| 実行 | 種別 | 結果 |
| --- | --- | --- |
| ステップ 8 | `--resume`(約 64 万トークン) | 失敗 ×2 |
| ステップ 8 | 新規 | **成功**(15 万) |
| ステップ 9 | `--resume` | 成功(24 万) |
| ステップ 10 | `--resume` | 失敗 |
| ステップ 10 | 新規 | 失敗 → **2 回目で成功** |
| ステップ 11 | 新規 | 失敗 ×2 → **Claude 直接実装へ** |
| B5 の差分レビュー | — | 失敗 → **2 回目で成功** |

**「セッションを切ると通る」という見立ては誤り**だった(**新規セッションの 1 回目でも落ちる**)。
**効いているのは再実行だけ**であり、**判定条件は依然として未特定**。
私自身、途中で「`--resume` の失敗率が高い」と誤った相関を述べたため、**その訂正も記録する**。

### `[手動・外部]` H-59 逐語照合表(**機械 green に数えない**)

**α の 79 行に加え、本タスクで次を人間の逐行確認へ送る。**

| 正本 | 逐語の要点 | 実装 |
| --- | --- | --- |
| **`A1`** | P1・P2 の受理で T 要素が原子的に確定。**P3・P4・拒否・退避・未処理には掛からない** | **実装しない**(サーバーが保証する性質。封筒に入れない) |
| **`A2`** | D3 以下は以後の再送で二重適用されない | **同上** |
| **`A3`** | 境界結果 `B1`〜`B7` のどれか。**ACK が返るのは `B1`〜`B4` に限る** | `boundaryResults.ts`(`B5`〜`B7` は別区分) |
| **`A4`** | 選手登録イベントを含む場合の一時 ID → 正式 ID の写像 | `playerIdMapping.ts`(**当該イベントへ結合**) |
| **`A5`** | `(D4, D1, D5)` ごとの結果。**D1 ≤ D3 だけでは結果種別を特定できない** | `ackEnvelope.ts`(語彙は `R-ACK-STATE` から) |
| **`B1`〜`B4`** | 要求終端 / gap / 恒久的な内容拒否 / 記録権不一致 | `boundaryResults.ts` |
| **`B5`〜`B7`** | 認証失効 / 認可 / 一時障害。**ACK 自体が返らない** | `boundaryResults.ts` の別区分 + `syncNotices.ts` の B5 記述子 |
| **`B3a` / `B3b`** | 未使用 D5 の内容拒否 = P5+T9 / 既存 D5 との衝突 = T9 開始なし | `rejectionReason.ts`(**受け取るだけ**) |
| **`C1`** | 応答で写像を返す | **実装しない**(後続 γ・allow-list に残す) |
| **`C2`・`C3`** | 決定的解決 / 再送で別 ID を重複生成しない | **α が実装済み**(無変更) |
| **`C4`** | 写像確定まで同期済みにしない | **α が実装済み**。本タスクは `A4` を供給 |
| **`U1`** | 常に取消イベントを積む。**同期済み・未送信を問わない** | `undoQueueing.ts` |
| **`U5`** | 「未送信 / 同期済み」判定に依存する分岐を置かない | 同上(raw 走査で固定) |
| **`U3`・`U4`・`U6`** | 即時の巻き戻し / 操作結果の区分 / 次の取消対象 | **実装しない**(**状況判定 = NFR-018 の (α)**) |
| **7-3 の手順 1・4** | 未送信の先頭から再送 / 返ってきた A5 で遷移判定(**D3 は再送範囲の決定に使う**) | `resendRange.ts` |
| **7-3 の手順 2・3** | ③認可後・V12 前の D5 照合 / 保存済み結果の再掲 | **実装しない**(後続 γ) |
| **6-4 の 2 択** | **B3 からの再開だけを 2 択に限定**。改訂版はローカル生成 / 墓標はオンライン記録権確認後 | **ラベルは操作者が選ぶ**。`classifyB3` は決めない(**後続 δ が供給**) |
