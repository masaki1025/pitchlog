---
date: 2026-09-13
topic: ADR-004 — マージゲートの適用単位と測定経路を条文化する
branch: feature/merge-gate-clause
---

# 作業ログ: 2026-09-13 ADR-004 マージゲートの条文化

Notion: TSK-379。計画書: `docs/features/merge-gate-clause/plan.md`(承認: 済 2026-09-13・山田正輝)。
調査: `docs/features/merge-gate-clause/research.md`。

## 確定ゲートの適用版(設計書 7.3-1 — 暫定記録)

**適用版 = 初回敵対レビュー実行時点で approved の 7.3 の版。** 当該ゲートはこの版で最後まで運用し、
途中で切り替えない。**本記録は暫定であり、初回敵対レビュー実行時点で異なっていれば更新してから開始する。**

| 項目 | 値 |
| --- | --- |
| **7.3 の適用版** | **ハーネス設計書 v1.14**(`docs/development/dev-harness-design-2026-08-07.md`・`status: approved`) |
| **条文コミット SHA** | **`31654ab`** — 本 in-review 化コミットの**第一親**であり、適用版が approved 状態で収録されている最新のコミット |
| 記録の時点 | ステップ 1(3 正本の in-review 化)と**同一コミット**。初回敵対レビューはまだ実行していない |

**本ゲートは 3 正本の一括検証であり、単一のゲートとして扱う**(設計書 `:437`
「複数正本の一括検証は単一のゲートとして扱い、適用版の記録はゲート単位で 1 件」)。
したがって**適用版の記録は本表 1 件のみ**で、正本ごとには持たない。

| 一括検証の対象 | 種別 | 目標版(暫定) |
| --- | --- | --- |
| `docs/adr/ADR-004-merge-gate-scope.md` | **新設** | 0.1 |
| `docs/design/data-model.md`(12-4 節ほか) | 既存正本の改訂 | 0.3 |
| `docs/requirements/requirements-pitchlog-2026-07-22.md`(`NFR-019(b)`) | 既存正本の改訂 | 2.8 |

**版はいずれも暫定**である。版番号の確定は確定ゲートの人間承認時に行う(ステップ 8)。

## やったこと

### ステップ 1: 3 正本を in-review 化する

- **`ADR-004-merge-gate-scope.md` を新設**した。frontmatter `status: in-review` / 変更履歴表 /
  メタ表 / **射程宣言の節**(設計書 7.3-7 — **新設の正本は文書内の節**に置く)まで。
  本文 5 節(文脈・決定・理由・帰結・参照)は**ステップ 6 で記述する**
- **`data-model.md` を `approved → in-review`**、変更履歴の先頭へ**起案行(版 0.3)**。
  射程宣言は**変更履歴行**に置いた(7.3-7 — **既存正本の改訂は当該改訂の変更履歴行**)
- **要件書を `approved → in-review`**、変更履歴の末尾へ**起案行(版 2.8)**。同じく射程宣言を行内に置いた。
  要件書の変更履歴表は**古い順**・4 列目が **`変更者`**(`状態` ではない)で `data-model.md` と体裁が異なる
- **`docs/README.md`** に **ADR-004 の索引行を新設**し、要件書・データモデル設計の 2 行を
  **in-review / 目標版 / 2026-09-13** へ現行化した

**索引の版セルは平文の数値でなければならない**(`check_docs_status.py:597` が
`INDEX_VERSION_RE` で全一致を要求し、`:694` が索引の版と変更履歴表の最大版の一致を要求する)。
そのため**「暫定」は版セルではなく変更内容セルに書いた**。

### ステップ 1 の是正: 要件書への行番号引用が自分の編集で全部 1 行ずれた

要件書の変更履歴表は**古い順**なので、起案行は**表の末尾**に入る。その結果
**以降の全行が +1 ずれた**。実測: `NFR-018` の正解ベクタ条項 `:915` → **`:916`** /
`NFR-010` の測定方法 `:846` → **`:847`** / 8 章のリリース判定基準 2 `:1035` → **`:1036`**。

**自分がステップ 1 で書いた 4 箇所の引用が、書いた直後に古くなっていた**
(`docs/README.md` の要件書行 / 要件書の起案行 2 箇所 / `data-model.md` の起案行)。
**行番号を条項名 + 逐語の参照へ置き換えて是正した** — `NFR-018` の**正解ベクタ**条項 /
`NFR-010` の**測定方法**条項(逐語「API直叩き含む」)。

**これは本改訂の全体に効く制約である。** 本ゲートは要件書と `data-model.md` を
同時に改訂するので、**以後も行番号は編集のたびにずれる**。したがって
**正本の本文へ書く要件書参照には行番号を使わない**(条項名 + 逐語 + アンカー)。
行番号は worklog・research のような**日付つきの記録**にだけ残す。
`data-model.md` が v0.1 で**行番号引用 296 件を安定 ID 参照へ貼り替えた**のと同じ理由である。

**既存の feature 文書・worklog の要件書行番号引用も現時点で 1 ずれている**が、
**過去記録は書き換えない**(TSK-348 の PO 裁定)。**本 feature の
`research.md` は本ゲートの依頼文が参照する生きた文書**なので、
**ステップ 6 の ADR 本文で典拠を引くときに条項名 + 逐語で引き直す**。

**台帳候補**(ステップ 10 で追記する): **正本の行番号引用は、その正本自身を編集した瞬間に
書き手自身の手で古くなる**。`data-model.md` は安定 ID で解いたが、**要件書は解いていない**。

### ステップ 2: 12-4 のゲート表に裁定 A・B を条文化する

ゲート表へ **2 行を新設し 3 行を改訂**した。

| 行 | 何をしたか |
| --- | --- |
| **適用単位**(新設) | 裁定 A。**当該 PR が開く経路ごとに判定する**。**開いていない経路を理由に不合格と判定しない** |
| **通過条件** | ② の末尾へ**括弧参照**を足した(「②の green は本表の「適用単位」の範囲で判定する」) |
| **最低要求 4 件と測定経路の関係**(新設) | **4 件はすべて DB ロール・関数 ACL・`search_path` への要求で HTTP 経路への要求を 1 件も含まない**。**測定経路の充足条件は 4 件に足す別の要求**である |
| **測定経路** | 裁定 B の**充足条件**を追記。**HTTP 経路を開かない PR には空の要求として掛かる** |
| **判定の記録** | **どの経路について判定したか**を記録項目へ足した |

**「判定」と「成立」を別の語で使い分けた**。**判定** = 本ゲートを通す/通さないを決める行為(**PR ごと**)。
**成立** = `NFR-019(b)` の網羅が完成した状態。**網羅がいつ成立していなければならないかは
要件書 8 章のリリース判定基準 2 が定める**と書き、**本節では判定時点を新設しなかった**。
`NFR-021` はこの 2 語の混同で `P0` を 2 回踏んでいる(台帳 `H-79`)。

**最低要求 4 件の文言は 1 文字も変えていない**(`git diff` で当該行が差分に出ないことで確認)。
**12-7 (1) が実機確定タスクへの要求として同じ 4 件を列挙している**ためで、実測すると
**両者の差は 12-4 側の ② が「越境関数」・12-7 側が「関数」の 1 語だけ**である(既存の差・本改訂では触らない)。

**計画の 4 項目に対し「判定の記録」行の改訂を 1 件足した**。適用単位を経路ごとにした以上、
**記録に経路が残らないと何を判定したのかが後から分からない**ためで、裁定 A の直接の帰結である。
**射程の追加としてここに記録する**。

### ステップ 3: 「経路」と「経路を開く」を 12-4 で定義する

**12-4 へ「経路の定義」の項を新設**し(non-serving 宣言より前 — **定義してから使う**)、
**1-1 の定義の所在表へ 1 行足した**。

- **経路** = **製品の外からの要求が DB のデータへ到達する、識別子を持った 1 本の入口**。
  **識別子の正は `contracts/authz/http-route-matrix.json` の `route_id`**
- **「開く」の述語**は **3 条件の同時成立**(入口が存在する / 外から到達できる / DB のデータを読むか書く)
  の **閉から開への遷移**として定義した
- **境界例を 6 件表にした**(読み取りのみ = 開く / 内部経路 = 開かない / バッチ = 開かないが最低要求 4 件は掛かる /
  ハンドラだけで未登録 = 開かない / `/health` のように DB へ到達しない = 開かない / 既存経路の変更 = 開かない)

**書きかけを実測で潰した 1 件**: 最初 `route_id` の書式を **`ROUTE:FR-nnn` と
`ROUTE:SHARED:...` の 2 系統と書いた**。実測すると **49 件中 12 件が
`ROUTE:CONTROL:*` / `ROUTE:MANAGEMENT:*`** で、**列挙が最初から不完全だった**。
**列挙をやめて「集合も体系も同ファイルが正・本節は列挙しない」へ書き換え**、
**書式は網羅でない例示として 4 件**だけ残した(4 件はいずれも同ファイルに実在することを確認済み)。
**台帳が繰り返し記録している「母集団を人が列挙する検査は射程が動くと黙って古くなる」型**を、
**書いている最中に自分で踏んだ**ということである。

**射程の境界**: **機械判定(CI での検出)は送る**(ADR-004 の射程宣言・ステップ 6 で実 ID)。
**意味定義は本改訂で置いた** — 所在を指すだけで終わらせない(計画レビュー `P0-2`)。
**同ファイルに識別子が無い入口を開く PR は同一 PR で `route_id` を与える**という要求を 1 つ置いた。
**識別子が無いと「どの経路について判定したか」が記録できず適用単位が定まらない**ためである。

### ステップ 4: 要件書 `NFR-019(b)` へ発効条項を置く

`NFR-019(b)` の網羅の列挙の**末尾へ 1 文を追記した**(**純粋な追記**であることを実測で確認 —
旧 `(b)` の 910 字がそのまま新 `(b)` の先頭に残っており、**列挙は 1 件も減っていない**)。

**`NFR-018` の正解ベクタ条項と同型だが、向きが逆である**ことを本文に書いた。

| 条項 | 禁止していること | なぜ |
| --- | --- | --- |
| `NFR-018` の正解ベクタ | **未完成のあいだ合格と判定してはならない** | **未整備を適合と偽らない**ため |
| `NFR-019(b)`(本改訂) | **未発効の経路を理由に不合格と判定してはならない** | **実装が存在しない経路の越境テストは書けない**ため。そう読むと**本項を実装する PR 自身を含めてどの PR もマージできない** |

**この非対称は隠さずに条文へ書いた。** 隠すと敵対レビューで「同型と言いながら向きが逆だ」と
必ず突かれるし、実際に**同型なのは「実体が無いものについて判定を下さない」という原則の方**であって、
**禁止の向きは実体が無いことの帰結が逆だから逆になる**。

**網羅の判定時点は新設していない** — 「**8 章のリリース判定基準 2 が定め、本項は判定の時点を
新たに定めない**」と書いた。**12-4 への参照を本文に一切置いていない**ことも確認した
(要件書本文中の `12-4` / `data-model` の出現は**変更履歴行の 1 件のみ**)。**上位文書が下位文書へ
規範を委ねる形にはしない**。

**行数は増えていない**(既存行への追記)。**要件書の行番号はステップ 1 以降さらにはずれていない**。

### ステップ 5: 参照へ寄せ、現況化し、誤引用を是正する

**6 箇所**を直した。

| 箇所 | 何が壊れていたか | どう直したか |
| --- | --- | --- |
| 12-4 の**見出し** | **履行済みの予告**(「実装は TSK-343」) | 「TSK-343 で完了済み」へ |
| 12-4 の**冒頭** | **非文**(「設計書 5.1 の「…」は **…追随は TSK-343 が行う**」)+ **引用元が消滅**(v1.14 が当該文言を削除) | 5.1 と本節の分担(向きは一方向)を書き、**予告は履行済みと明記**。**採用理由は書き写さない**(7.1-1) |
| **ORM 行** | **「ADR の起票判断は TSK-343」が未決のまま** | v1.14 で決着 —「**起票しない**」 |
| **non-serving 2 項・3 項** | **通過条件を散文で言い換えていた**(片方だけ直る事故を招く) | **ゲート表への参照へ寄せた**。**1 項は変えていない**(`git diff` で確認) |
| **3-2 節の `NFR-010` 引用** | 鉤括弧で「**API 直叩きを含む全経路**」— **要件書にこの語は無い** | 逐語「**API直叩き含む**」へ |
| **12-4 の non-serving「なぜ宣言が必要か」** | 鉤括弧で `NFR-010`「テナント分離は**初日から全機能に適用**する」— **要件書にこの語は無い** | 出所は**改善台帳 `I-2`**。`NFR-010` は別の文で書く |

**計画が挙げていたのは 3 箇所(12-4 冒頭 / ORM 行 / `:246`)だが、同型の誤引用をもう 1 件見つけた。**
**12-4 の non-serving 節が `NFR-010` を鉤括弧で引きながら、要件書に無い「初日から全機能に適用」を
引用していた** — 出所は**改善台帳 `I-2`「マルチテナント分離を初日から完全実装する」**である。
**`:246` と同じ型で、しかも本改訂が触っている当の節にあった**ので同時に直した。**射程の追加として記録する。**

**射程外として残した同型 2 件**(3-1 節・**12-4 の外**): `:143`「② 適用範囲(初日から全機能)」と
`:155-156`「`NFR-010` は**初日から全機能に適用**を求めるが」。**鉤括弧の引用ではない**が、
**要件書に無い語を `NFR-010` に帰属させている点は同じ**である。**本改訂の射程宣言に入っていないので触らない** —
**確定ゲートで指摘が出たら 7.3-6 の射程変更として PO 裁定に上げる**。

**設計書 v1.14 との受け取り先の食い違いを 1 件記録する。** 設計書 v1.14 の変更履歴は
**「当該引用の現況化は次の改訂を持つ TSK-353 の射程とする」**と宣言している。
**本改訂(TSK-379)が先に履行した。** 計画の DoD が「`S-4` 第 2 部(12-4 節冒頭の現況化)を同乗させる」と
定めており、**TSK-378 の裁定に基づく承認済みの射程**である。**TSK-353 の側にこの項目が空手形として
残る**ので、**ステップ 6 で TSK-353 へ「本項は TSK-379 で履行済み」をコメントする**。

### ステップ 6: ADR-004 の本文を書き、送り先 2 件を起票する

**本文 5 節**(文脈・決定・理由・帰結・参照)を書いた。**決定は `D-1`〜`D-5` の 5 件**で、
**各行に「規範の所在」列を持たせて過去形で書いた** — 所在列を外すと**過去に何を決めたかの記録**として
残るだけで、**規範としては読めなくなる**。これが 4 節に置いた検証条件である。

**典拠の是正 1 件**: 主典拠を **8 章のリリース判定基準 1・2** に置き、**`NFR-021` の 2 時点は従**とした
(同項は開発環境の受入プロファイルの条項で、**`NFR-010` に一言も言及していない**)。
**裁定 B の根拠に `:161-163` を使っていない**ことも本文に明記した — 同箇所は **DB 側既定拒否を採る理由**を
述べたもので、**API 直叩きを DB 層テストで代替できるとは言っていない**。

**行番号引用を全件その場で検証した。** **1 件ずれていた** — ADR-003 の
「未完成のあいだ適合と判定しないであって、実装を禁じるものではない」は **`:389` ではなく `:390`**
(`:389` は別の箇所)。**TSK-378 の `design.md` が `:389` と書いており、そのまま写していたら誤引用になっていた。**
逐語も原典に合わせて直した(「判定しないのであって」→「判定しない**であって**」)。
他の 6 件(設計書 `:64`・`:437`・`:461`・`:897` / legacy `:397` / TSK-317 計画 `:157`)は**一致**。

#### 起票 2 件(重複が無いことを Notion 検索で確認済み)

| ID | 内容 | 受領範囲 |
| --- | --- | --- |
| **TSK-382** | `SP-06`「RLS を初日から全テーブルに掛ける」と 12-4 の射程外宣言・non-serving の**字面衝突**を裁定する | **この 3 者の衝突だけ**。**裁定 A・B の再議はしない**・**「経路」の意味定義も受け取らない** |
| **TSK-383** | 「経路を開く」の**機械判定**を CI へ入れる | **機械化だけ**。**意味の正は 12-4 節「経路の定義」**で、本タスクで再定義しない(7.1-1) |

**どちらも受領範囲を先方の本文に書いた** — 台帳の「**送り先が空手形になる**」型を自分で再生産しないためである
(計画レビュー `P1-6`)。**ADR-004 の射程宣言と帰結に実 ID で記録した。**

**TSK-353 へ申し送りをコメントした**(設計書 v1.14 が同タスクの射程と宣言していた
「12-4 節冒頭の現況化」を本改訂が先に履行した件)。**TSK-379 のカードにもステップ 6 の完了を記録した。**

## 確定ゲート(ステップ 7)

### 1 周目 — 否決 `P0` 0 件 / `P1` 7 件 / `P2` 2 件(初周 = 全文レビュー)

**判定行と件数は整合**しており、様式(重大度・起因・(A)/(B)/(-)・判定行)も充足していた。
**全 9 件について原典を当たって事実を確認した。** 採否は次のとおり(**採用 8 件・不採用 1 件**)。

| # | 重大度 | 起因 | 分類 | 要旨 | 採否と理由 |
| --- | --- | --- | --- | --- | --- |
| 1 | P1 | 起因 | (A) | **PR を経ない有効化(設定・feature flag)にゲートの適用単位と記録先が無い**。2 項は「マージ**または有効化**」を条件にしているのに、適用単位・測定経路・記録はすべて PR 基準 | **採用**。適用単位行へ「**PR を経ずに経路を開く変更も、版管理された PR として行い同じ単位で判定する**」を追加。**判定の帰属先を持たない有効化を認めない** |
| 2 | P1 | 起因 | (A) | **`route_id` に method / path が無く、経路の識別粒度が決まらない**。1 ハンドラが複数 `route_id` を処理する場合に開いた経路集合が一意にならない | **採用**(**実測で確認** — `routes[]`・`cells[]` のいずれにも method / path のキーが無い)。**判定の単位は `route_id`(論理経路)であり method / path ではない**と明記し、**1 対多・多対 1 の両方向の扱い**を書いた |
| 3 | P1 | 起因 | (A) | **`NFR-019(b)` の発効条件(「実装の完成」)と 12-4 の「経路を開く」3 条件がずれる**。到達可能だが機能が未完成の PR で判定が分かれる | **採用**。要件書側を「**対象となる経路が実装され、製品の外からの要求がその経路を通ってデータへ到達できるようになった時点**」へ改めた。**12-4 を参照せずに 3 条件と同じ状態を指す**書き方にした(上位が下位へ規範を委ねない) |
| 4 | P1 | 起因 | (-) | **要件書の同一条項内で「判定の時点を新たに定めない」と「判定の時点だけを定める」が正面から矛盾**している | **採用**。**私の書き分けの失敗**である。「**網羅の成立時点**は 8 章が定め本条項は変更しない」/「**本条項が定めるのは各経路の越境テストの発効時点だけ**」へ分離し、「判定の時点だけを定める」を削った |
| 5 | P1 | 起因 | (-) | **再実行の契機に「経路を開く PR」が無い**。スキーマも本書も変わらない通常の経路追加でゲート自身が発火せず、「**文言は追加したが実効がない**」 | **採用**。契機を **2 つ → 3 つ**にした。**最も効く指摘**で、これが無ければ裁定 A・B を条文化しても運用に接続しなかった |
| 6 | P1 | 起因 | (-) | **ADR-004 の 1 文が規範として読め、しかも `NFR-019` 柱書と食い違う** —「越境テストが全部通っていることを求めているのはリリース候補時であって、個々の PR ではない」 | **採用**。**事実としても誤り**だった。**リリース時点で求められるのは網羅の「成立」**、**個々の PR で求められるのは発効済みテストの全グリーン**。両者を分けて記述形へ書き直し、「**本決定はそこを緩めていない**」と明記した |
| 7 | P1 | 起因 | (-) | **TSK-343 を完了済みとした現況化が 12-7・12-8 の受け取り先表へ伝播していない**。完了済みと未実施の双方に読める | **採用**。2 行へ「**TSK-343 は完了済み**(設計書 v1.14 で 5.1・10.1 を改訂)。**本行は受け取り先の記録として残す**」を追記した |
| 8 | P2 | 起因 | (-) | **ADR-004 の鉤括弧引用 5 件が逐語でない**(語順・対象項・空白の省略) | **採用**。5 件すべてを原典の逐語へ差し替え、機械照合で一致を確認した |
| 9 | P2 | **非起因** | (-) | **`data-model.md` に既存の `NFR-010` 引用ずれが残る**(`:163` の鉤括弧内の括弧と空白 / `:1617`「初日から全経路へ」/ `:2494`) | **不採用** — **本改訂の射程宣言に含まれない**。射程宣言 ⑦ は **3-2 節の 1 箇所**に限っており、**射程変更は 7.3-6 の PO 裁定**による。**PO 裁定(2026-09-13・山田正輝): 射程外として不採用**。なお `:2494` は本改訂が置いた「API直叩き含む」で、**断片としては原典と逐語一致している**(全文でないだけ) |

**エスカレーション判定**(7.3-6): 母集団 = P0+P1 = **7 件**、うち起因 **7 件**。
**`起因件数 × 2 > 母集団` が成立**(14 > 7)。**ただし成立は 1 周目のみで、2 周連続ではない**ため
**エスカレーションは発火しない**。初周は全文レビューであり、**本改訂が触った箇所に指摘が集中するのは
想定どおり**である。2 周目も成立したら PO スコープ裁定へ上げる。

**6 周警告**(7.3-6): `確定ゲート周回` = **1**。未到達。

### 2 周目 — 否決 `P0` 0 件 / `P1` 3 件 / `P2` 1 件(差分再レビュー)

**判定行と件数は整合**、様式も充足。**4 件すべて `起因`**(1 周目の反映が原因)で、**全件採用**した。

| # | 重大度 | 起因 | 分類 | 要旨 | 採否と理由 |
| --- | --- | --- | --- | --- | --- |
| 1 | P1 | 起因 | (A) | **1 周目 P1-2 の是正が穴を開けた** —「複数 endpoint が 1 つの `route_id` を実装するなら 1 本の経路」と書いたため、**既存 `route_id` へ第 2 endpoint を足す PR が、新しい外部入口を作るのにゲートも直叩きテストも発火しない** | **採用**。**その経路へ新しい外部入口を足すことは「開く」に含める**を追加し、境界例を**「同じ入口の処理変更 = 開かない」/「新しい入口の追加 = 開く」の 2 行に分割**した。**`route_id` は method / path と結線していないので既存テストが新しい入口を覆う保証が無い**ことも書いた |
| 2 | P1 | 起因 | (-) | **1 周目 P1-1 の是正が対象行と矛盾**。適用単位には有効化を足したのに、**ゲートの対象は「実装 PR」のまま**で、有効化だけの設定 PR を対象外と読む余地が残った | **採用**。対象行を「**実装 PR および、その機能または経路を有効化する PR**(**実装 PR でなくても対象である**)」へ |
| 3 | P1 | 起因 | (-) | **1 周目 P1-7 の伝播が同じ表の中で midway だった** — 12-8 の別の行が `D5` の強制機構を今も TSK-343 へ未決として送っている | **採用**(**実測で確認** — `backend/migrations/versions/0007_idempotency_originals.py` が `kind = 'rejected'` の `CHECK` と `(tenant_id, d5, kind)` の複合 FK を実装済み。`design.md` の 3 案比較でも同案が「採用」)。当該行へ「**完了済み — `CHECK` + 種別を含む複合 FK を採った**」を追記 |
| 4 | P2 | 起因 | (-) | **1 周目 P2-8 の引用是正で `****` が生じた**(閉じ強調と開き強調が空白なしで連結) | **採用**。空白を入れ、**3 正本を機械走査して `****` の残存 0 件**を確認した |

**「是正が次の同型を生む」型が 3 件**(#1・#2・#3 はいずれも 1 周目の反映が原因)。
**台帳が繰り返し記録している連鎖**であり、本ゲートでも同じ形で出た。

**エスカレーション判定**(7.3-6): 母集団 = P0+P1 = **3 件**、うち起因 **3 件**。
**`起因件数 × 2 > 母集団` が成立**(6 > 3)。**1 周目に続き 2 周連続で成立したため、
7.3-6 のエスカレーション条件が発火する** — **PO スコープ裁定へ上げる**。

**6 周警告**(7.3-6): `確定ゲート周回` = **2**。未到達。

### PO スコープ裁定(2026-09-13・山田正輝)— **続行の明示指示**

**7.3-6 のエスカレーション条件**(`起因件数 × 2 > 母集団` が 2 周連続)が
**1 周目(7/7)・2 周目(3/3)で成立**したため PO 裁定へ上げた。

**裁定: 続行の明示指示。** **文書への編集なし・カウンタなし・コミットなし**(7.3-6 の表)。
**連続カウントは 0 へリセットする。** 次周は**差分再レビューを続行**する。

**上げたときの材料**: **`P0` は 2 周ともゼロ**・**件数は 9 → 4 と減少**。
**起因比が高いのは射程の発散ではなく、「12-4 の中で条文が複数行に分かれており、
1 箇所を直すと連動先を落とす」形**である(2 周目の 4 件はすべてこの形だった)。

### 3 周目 — 否決 `P0` 0 件 / `P1` 3 件 / `P2` 0 件(差分再レビュー)

**判定行と件数は整合**、様式も充足。**3 件すべて `起因`・すべて `(A)`(方式が分岐する)**で、**全件採用**。

**3 件は同じ根に生えていた。** 私は 2 周目に**境界例の表へ行を足して**穴をふさいだが、
**「開く」の 3 条件は経路 `R` について書かれたまま**だった。**定義と例が矛盾し**(#1)、
**例だけを直したのでゲート表へ伝播せず**(#2)、**「有効化」も対象へ足しただけで
判定集合・測定条件が無かった**(#3)。**例を足して定義を直さなかったのが原因である。**

| # | 重大度 | 起因 | 分類 | 要旨 | 採否と理由 |
| --- | --- | --- | --- | --- | --- |
| 1 | P1 | 起因 | (A) | **「開く」の 3 条件は経路 `R` の遷移として書かれており、「既存 `route_id` へ第 2 入口を足す = 開く」という境界例と矛盾する**。経路 `R` は変更前から 3 条件を満たすため、定義上は開かない | **採用**。**3 条件を入口ごとの遷移として定義し直した**(レビュアが示した 2 択のうち後者)。例外を足すのではなく**定義の主語を変えた** |
| 2 | P1 | 起因 | (A) | **入口についての判定・テストがゲート表へ伝播していない**。表は「その**経路**の直叩きテスト」「どの**経路**について判定したか」しか要求せず、**同じ `route_id` の旧証跡で新入口を通過できる**(「文言は直したが実効がない」) | **採用**。適用単位・測定経路・本書の責務・判定の記録を**入口単位へ揃えた**。**記録には `route_id` と入口(method / path)の両方**を要求し、**同じ `route_id` に既存テストがあっても開いた入口を覆っていなければ充足しない**と明記した |
| 3 | P1 | 起因 | (A) | **「有効化する PR」を対象へ足しただけで適用集合と測定条件が無い**。3 条件上の閉→開を伴わない有効化は、**対象なのに判定経路ゼロ・測定要求ゼロ・再実行契機なし** | **採用**。**「既存の入口が到達するデータの範囲を広げること」を「開く」に含める**と定義し、境界例へ **2 行**(資源が広がる有効化 = 開く / 入口も到達範囲も変わらない有効化 = 開かないが対象ではある・測定要求は空)を足した |

#### 裁定の粒度を精密化した — **PO へ報告する**

**裁定 A は「経路」単位で下されたが、条文は「入口」単位になった。**
`route_id` は認可マトリクス上の論理経路の識別子で、**HTTP の method / path と 1 対 1 に対応しない**
(同ファイルは method / path を持たない — 実測済み)。**経路単位のままだと、既存の `route_id` へ
2 本目の入口を足す PR が新しい外部入口を作るのにゲートが発火しない。**

**判定が細かくなる方向の精密化であり、裁定 A の意図を緩めていない。**
`data-model.md` の起案行・ADR-004 の `D-1` と帰結に**明記した**。

**エスカレーション判定**(7.3-6): 母集団 **3**・起因 **3** で `起因件数 × 2 > 母集団` が成立。
**ただし PO 裁定(続行の明示指示)で連続カウントは 0 へリセットされている**ため、
**本周は連続 1 周目**であり**発火しない**。

**6 周警告**(7.3-6): `確定ゲート周回` = **3**。未到達。

### 4 周目 — 否決 `P0` 0 件 / `P1` 4 件 / `P2` 0 件(差分再レビュー)

**判定行と件数は整合**、様式も充足。**4 件すべて `起因`**、**うち 3 件が `(A)`**。**全件採用**。

**3 周目で定義の主語は正したが、「到達性」の中身が粗かった。** 「到達するデータの範囲」としか
書かなかったため、**同じデータへの操作の拡大(読取 → 更新)と主体の拡大(通せる役割を増やす)が
ゲートを素通り**した。**軸を明示しないと「広がった」の判定が主観になる。**

| # | 重大度 | 起因 | 分類 | 要旨 | 採否と理由 |
| --- | --- | --- | --- | --- | --- |
| 1 | P1 | 起因 | (A) | **「開き直す」を資源の拡大だけに限定していた**。同一 method / path・同一行集合のまま**読取のみ → 更新可能**にする変更や、**通せる役割だけを広げる**変更が「開かない」と読める | **採用**。**到達性を〈主体 × 資源 × 操作〉の 3 軸で比べる**と定義し、3 軸の表を置いた。**境界例へ 2 行**(操作の拡大 / 主体の拡大)を足して 11 行にした |
| 2 | P1 | 起因 | (A) | **拡大を「開く」に入れたのに、測定条件がその差分を検証しない**。**同じ入口の従来資源だけを叩くテストを足せば文言上は充足**でき、false green が残る | **採用**。測定経路の充足条件へ「**開き直した入口は、広がった〈主体 × 資源 × 操作〉の組そのものを覆うテストでなければ充足しない**」を追加した |
| 3 | P1 | 起因 | (A) | **非 HTTP の外部入口が定義域に入るのに、識別子も測定条件も無い**。`route_id` の正は `asset_kind: "authz_http_route_matrix"` の HTTP 専用資産で、測定経路は「HTTP の入口を開く PR」と「1 つも開かない PR」しか規定せず、**非 HTTP だけを開く PR はどちらにも該当しない** | **採用**。測定経路を「**入口を開く PR は、同一 PR にその入口を製品の外から直接叩くテストを含む**(HTTP なら API 直叩き、他のプロトコルならそのプロトコルで)」へ一般化。**非 HTTP の入口を開く PR は同一 PR で識別子の置き場も定める**とし、**別タスクへ送らなかった** — 送ると識別子が無いまま入口だけが開くからである |
| 4 | P1 | 起因 | (-) | **ADR に足した帰結 2 項目が、条項参照を外しても規範として読める**。`route_id` の意味を現在形で定義し、記録項目を再掲していた | **採用**。**私が 4 節に置いた検証条件に自分で反していた**。語の定義と記録項目の再掲を落とし、**過去形 + 12-4 への参照**へ書き直した |

**表記の追随**: 起案行・ADR の射程宣言・`D-2` の文言も「入口」単位へ揃えた。
**TSK-383 のカード名は起票時の「経路を開く」のまま**なので、ADR にその旨を注記した。

**エスカレーション判定**(7.3-6): 母集団 **4**・起因 **4** で `起因件数 × 2 > 母集団` が成立。
**3 周目に続き 2 周連続で成立したため、エスカレーションが再発火する** — **PO スコープ裁定へ上げる**。
(1 回目の発火は 1・2 周目で、PO 裁定「続行の明示指示」により連続カウントは 0 へリセットされていた。)

**6 周警告**(7.3-6): `確定ゲート周回` = **4**。未到達。

### PO スコープ裁定 2 回目(2026-09-13・山田正輝)— **続行の明示指示**

**7.3-6 のエスカレーション条件が 3・4 周目で 2 周連続成立**したため再度上げた。

**裁定: 続行の明示指示。** **文書への編集なし・カウンタなし・コミットなし**(7.3-6 の表)。
**連続カウントは 0 へリセットする。** 次周は**差分再レビューを続行**する。

**上げたときの材料**: **`P0` は 4 周ともゼロ**。件数は 9 → 4 → 3 → 4 で**減っていない**。
ただし**指摘は 4 周とも「ゲートを回避できる抜け道」という同じ問い**であり、
**回避経路は経路 → 入口 → 到達性の軸と層を下りきっている**(3 軸の内側に層は無い)。
**件数が減らないのは論点の発散ではなく、1 周に 1 層しか下りられなかったため**と判定した。

**代替案(射程縮小)を採らなかった理由**: 「入口」の意味定義を TSK-383 へ寄せる形になり、
**計画レビュー `P0-2`「所在を指すだけでは定義にならない」で一度否決した形**へ戻る。
**今回塞いだ穴(操作の拡大・主体の拡大・非 HTTP の入口)は、定義が無いと塞げない**ものだった。

**打ち切り条件を先に決めた**: **5 周目の指摘が 3 軸の内側の精密化なら続行**、
**別の箇所への伝播漏れ(作成者の取りこぼし)なら、周回では解けないので打ち切り 3 案を PO へ出す。**

### 5 周目 — 否決 `P0` 0 件 / `P1` 3 件 / `P2` 1 件(差分再レビュー)

**判定行と件数は整合**、様式も充足。**全件採用**。

**本周でレビュアが明示した 2 点が、打ち切り判断の材料である**:

> **3 軸の集合比較そのものに、未封鎖の回避経路や「次の層」はありません。**
> **「広がった組そのものを覆う」も、変更前後の差分とテストの主体・資源・操作を照合できるため、
> 人手で判定可能です。否決理由は伝播漏れです。**

**4 周かけて下りてきた「ゲートを回避できる抜け道」の層は、ここで底に着いた。**
**残っているのは私の伝播漏れである。**

| # | 重大度 | 起因 | 分類 | 要旨 | 採否と理由 |
| --- | --- | --- | --- | --- | --- |
| 1 | P1 | 起因 | (A) | **非 HTTP の入口では判定記録が書けない**。測定条件は一般化したのに、**記録形式が `route_id` と method / path に固定**されたままだった | **採用**。記録を「**経路の識別子と、入口を一意に指す値の両方**」へ一般化し、**HTTP / 非 HTTP それぞれの書き方**を示した |
| 2 | P1 | 起因 | (-) | **ADR の `D-2` が自己矛盾**。前半で「入口を開く PR は…」と一般化した直後、後半が「**HTTP 経路**を開かない PR には空の要求」のままで、**非 HTTP だけを開く PR が両方に該当**する | **採用**。「**入口を 1 つも開かない PR**」へ |
| 3 | P1 | 起因 | (-) | **ADR の帰結に手続規範が残っている** —「**この 1 点は人間承認の際に明示的に確認する**」は、条項参照を外しても人間承認時の必須行為として読める | **採用**。**4 周目に P1-4 を直した際、その同じ段落へ新しい規範を書き足していた**。ADR から落とし、**確認の申し送りは本 worklog が持つ**(下記) |
| 4 | P2 | 非起因 | (-) | **ADR の見出し参照が旧称**(`12-4 節「経路の定義」`)のまま。実際の見出しは「経路と入口の定義」 | **採用**。**3 箇所**(射程宣言・`D-4`・帰結)を是正し、**全文走査で旧称の残存 0 件**を確認した |

#### 人間承認時に明示的に確認していただく 1 点(ADR から本 worklog へ移した)

**裁定 A・B は「経路」単位で下されたが、条文は「入口」単位である。**
`route_id` が HTTP の method / path と 1 対 1 に対応しないため、経路単位のままでは
**既存の経路へ 2 本目の入口を足す PR でゲートが発火しない**(確定ゲート 3 周目 `P1`)。
**判定が細かくなる方向の精密化であり、裁定の意図を緩めていない**と判断したが、
**裁定の文言からは動いている。** **ステップ 8 の人間承認でこの 1 点を確認する。**

**エスカレーション判定**(7.3-6): 母集団 **3**・起因 **3** で成立。
**ただし PO 裁定 2 回目(続行の明示指示)で連続カウントは 0 へリセットされている**ため、
**本周は連続 1 周目**であり**発火しない**。

**6 周警告**(7.3-6): `確定ゲート周回` = **5**。**次の反映周で 6 に達し、警告の提示義務が生じる。**

### 6 周目 — 否決 `P0` 0 件 / `P1` 3 件 / `P2` 1 件(差分再レビュー)

**判定行と件数は整合**、様式も充足。**3 件を反映し、1 件は PO 裁定待ちで保留**。

**本周で初めて起因比が反転した** — 母集団 3 のうち **起因は 1 件**、**非起因が 2 件**。
非起因の 2 件は**より前の周の反映が残していたもの**で、直近の反映が生んだものではない。

| # | 重大度 | 起因 | 分類 | 要旨 | 採否と理由 |
| --- | --- | --- | --- | --- | --- |
| 1 | P1 | 起因 | (A) | **非 HTTP の記録形式が定義節へ伝播していない**。ゲート表は一般化したのに、**定義節は「経路 = 同じ `route_id` を持つ入口の集合」「記録には `route_id` と入口の両方」のまま**で、非 HTTP では両方を同時に満たす記録が書けない | **採用**。定義節を「**同じ経路識別子を持つ入口の集合**」へ一般化し、**HTTP の経路識別子の正 = `route_id`**、**HTTP 以外は同一 PR で定める契約資産が持つ**と書き分けた。非 HTTP の PR への要求も「**経路識別子と入口を一意に指す値の両方を置ける契約資産**」へ精密化(**片方だけの資産では記録が書けない**) |
| 2 | P1 | **非起因** | (-) | **版 0.3 の射程宣言 ⑥ が実変更を覆っていない**。⑥ は「12-4 冒頭と ORM 行の現況化」に限るのに、実差分には **12-7 の申し送り表・12-8 の受け取り先表 2 行**(TSK-343 完了の伝播)が入っている。**7.3-7 のスコープ統制が実効を失っている** | **保留 — PO 裁定へ上げる**。**確定ゲート中の射程変更は 7.3-6 の PO 裁定によってのみ行える**(7.3-7)。**指摘は正しい** — 1 周目 `P1-7` と 2 周目 `P1-3` を採用した時点で、**宣言より広い範囲を実際に触っていた** |
| 3 | P1 | **非起因** | (-) | **ADR にまだ規範が残っている** —「**発効済みのテストは個々の PR でも全グリーンでなければならない**」は、`NFR-019` への参照を外しても現在形のマージ条件として成立する。**1 周目 `P1-6` を直したときに自分で書いた文**である | **採用**。「**個々の PR に対する `NFR-019` 柱書の要求を本決定は緩めていない**」へ書き換え、**規範ではなく本決定の射程の記述**にした |
| 4 | P2 | **非起因** | (-) | **入口集合が空のときの記録形式が無い**。「対象入口なし」・空配列・項目省略のどれが正しいか判別できない | **採用**。「**入口を 1 つも開かない PR は『対象入口なし』と書く — 項目を省かない**」を追加。**省くと判定したのか書き忘れたのかが区別できない** |

**レビュアの確認結果**: 旧見出しの完全一致は **0 件**、`D-2` の空要求の一般化と
帰結の人間承認規範の削除は**反映を確認**した。

**エスカレーション判定**(7.3-6): 母集団 **3**・起因 **1**。`1 × 2 > 3` は**不成立**。
**6 周を通じて初めて発火条件が成立しなかった周である。**

**6 周警告**(7.3-6): `確定ゲート周回` が **6 に達した**。**周回・重大度推移・起因比の一覧を PO へ提示する義務が生じる** — 下記。

### PO スコープ裁定 3 回目(2026-09-13・山田正輝)— **範囲確定**

**6 周目 `P1-2`**(版 0.3 の射程宣言 ⑥ が実変更を覆っていない)に対する裁定。

**裁定: 範囲確定。射程宣言 ⑥ を実態へ合わせる。**

| | 内容 |
| --- | --- |
| **変更範囲** | ⑥ を「12-4 冒頭と ORM 行の現況化」から「**TSK-343 の完了に伴う現況化**」へ広げ、対象を **⓵ 12-4 節の見出しと冒頭 ⓶ 12-4 節の方針表の ORM 行 ⓷ 12-7 節の申し送り表の該当行 ⓸ 12-8 節の受け取り先表の該当 2 行**と明示した |
| **理由** | **⓵⓶ を直した結果、同一文書内で「TSK-343 は完了済み」と「TSK-343 へ未実施として送る」が併存した。** ⓷⓸ はその後始末であり、**新しい判断を持ち込んでいない**。1 周目 `P1-7`・2 周目 `P1-3` を採用した時点で、**宣言より広い範囲を実際に触っていた** |
| **カウンタ** | **加算しない**(7.3-6 — カウンタは指摘の採用反映を伴う周のみ。PO 編集単独では加算しない)。`確定ゲート周回` は **6** のまま |
| **コミット** | 反映周が立たないので**独立コミット**(反映周記法・ステップ記法を付けない) |
| **次周** | **全文レビュー**(7.3-4 — 射程変更直後の周) |

**連続カウントは 0 へリセットする**(7.3-6 — PO が判断した事実で流す)。

**変更履歴への記録**: `data-model.md` の版 0.3 の起案行に、裁定名・裁定日・裁定者を明記した。

## 決定

## 未決・次の一歩
