---
date: 2026-08-12
topic: ハーネス運用評価台帳の新設
branch: feature/harness-evaluation
---

# 作業ログ: 2026-08-12 ハーネス運用評価台帳の新設

## やったこと

- /task-start: Notion タスク起票(優先度 中・DoD 8 点)→ `feature/harness-evaluation` ブランチ + worktree 作成(origin/develop 起点)→ 計画書雛形・本 worklog 作成
- マージ済みブランチの後片付け(人間の指示・2026-08-12): ローカル `feature/req-v1-9-nfr021-wsl2`・`feature/dev-harness` を削除、リモート `feature/req-v1-9-nfr021-wsl2`・`feature/legacy-db-doc`・`feature/requirements-definition` を削除(いずれも develop にマージ済みを `git branch --merged` で確認)。**walter97 が作った 3 本(`codex-plan-status-guard`・`feature-status`・`harness-baseline-merge`)は共有リポジトリへの影響があるため残置**(人間の判断)

### 台帳の材料として今回追加で見つかった 2 件

- **`/task-done` がブランチを消さない** — worktree の除去までで、**ローカル・リモートのブランチ削除はどのスキルもやらない**。リポジトリ設定 `deleteBranchOnMerge` も **false** のため、マージ済みブランチが両側に溜まり続ける(今回 6 本溜まっていた)。※ 自動削除の有効化は人間の判断で**見送り**(ブランチの寿命は都度判断する方針)→ 台帳では「`/task-done` に後片付けの案内を入れるか」を論点として起票する
- **`git_guard.py` の誤検知(実害あり)** — `git_guard.py:96-99` の最終ルールが「`push` を含むコマンドは**カレントブランチが保護ブランチなら**すべてブロック」になっており、**`git push origin --delete <マージ済み feature>` まで止まる**。`develop` に触らないコマンドで、ガードの本来の射程(保護ブランチ上での commit/merge/rebase・保護ブランチ**宛て**の push・force push)には当たらない。今回は worktree 側(カレント = `feature/harness-evaluation`)から実行して通した。**ガードが正しい操作を止めると、迂回の習慣がつくリスクがある**ため優先度は高め

- **/investigate(3 並列)**: decision-tracer(様式・正本要求・過去決定の重複)/ Explore ×2(全 worklog+全 feature 文書+変更履歴表の走査 / 機構の穴 8 件の実コード検証)。結果を `docs/features/harness-evaluation/research.md` に統合。**legacy-analyst は旧システム担当のため今回は使わない**
  - **成果**: 様式は改善台帳を流用可能と確定 / `H-*` の衝突 0 件 / **再発の集計が可能になった**(ゲート区分の判定ミス 5 件・approved 正本間の矛盾 9 件・レビュー反映が不十分で再指摘 7 件 ほか)/ **起票候補 8 件はすべて事実と確認**、加えて**未認識の穴 11 件**を発見
  - **私が裁定した訂正 3 件**: ① エージェントの「要件書の変更履歴に同一版 1.8 が 2 行 = 腐りの実例」は**誤り**(2 件目は「frontmatter 追加・本文の内容変更なし」で 7.6-3 前段の正常な結果)→ 不採用。ただし「版列が一意キーでなくその意味を定義した規則が無い」観察自体は有効なので既知の別タスク「文書版番号規則の明文化」へ接続 ② 私が書いたゲート典拠「新設 = 7.6-3 後段」は不正確 → `AGENTS.md:14` + 設計書 7.2 表へ差し替え ③ 前タスク worklog `:142` の周回数の帰属誤り(harness-baseline-merge の 5 周は計画レビューでなく確定ゲート)→ **削除せず注記で訂正**(旧裁定の扱いと同じ作法)

- **/plan(計画レビュー 5 周で収束・全 17 件採用・不採用 0 件)**: 1 周目 P1×3/P2×3 → 2 周目 P1×2 → 3 周目 P1×4 → 4 周目 P1×4 → 5 周目 P1×1(参照 1 行)。**2026-08-12 に PO 承認**
  - **1 周目**: **`/task-done` は PR マージ後に develop 上で走るため台帳(正本)を編集できない**(絶対規則 1 に反する)→ **主経路を `/pr` のクローズ処理へ**(worktree 側・feature ブランチ上なので同一 PR に載せられる = 7.6-2)。索引の状態セルに矢印(`draft→approved`)を書くと完全一致比較で不一致になる → リテラル 1 語へ。ステップ 1 が計画系コミットなのに実装ステップ表内にあり進捗導出が不整合 → 表外へ移して連番を振り直し。要件書 v1.8 の先例は「版列が一意キーでない」ことの例に限る(2 行目は本文変更なしで H-* 追記と性質が異なる)。「不採用 0 件」は**機構有効性の直接証拠でない**(追認傾向でも同値)→ 観測指標として記録
  - **2 周目**: 「追記を**検討する**」では誰がいつ判断するか決まらない → **判断主体 = PR 作成者・時点 = 最終コミット前・非該当時は worklog に理由を残す**。**突合が片方向**(宣言済み未反映だけを中断)→ 台帳を追記したのに宣言し忘れると素通りするので**双方向化**。「高優先度」に基準がなく DoD を客観的に ON にできない → `追跡優先度` を導入
  - **3 周目**: **正本に Notion URL を必須記載する設計が設計書 7.4 `:351`「正本は Notion を参照しない」に反する**(実在の規約違反)→ 台帳は F-ID + worklog リンクのみ・URL は worklog 側。全 diff 逆突合では**この PR 自身の feature 文書・worklog・スキルで誤中断**する → 対象範囲と除外リストを定義し、処理順を `/sync-docs` と揃える。基準が 19 件に一意適用できない(**N-11 は制御目的の典拠自体が未確認**)→ 2 条件の同時成立へ改め、**既知 19 件を対応表で固定**。ステップ 2 の目視確認が合格条件に入っていない → 移動+確認者不在時は停止。**密度が上がったため `design.md` を分離**
  - **4 周目**: 4 件すべて**3 周目で自分が作った不整合** — 「follow-up タスク **URL**」の表現が Notion URL 再混入を招く / `通常` と `候補` の定義が重複して一意に決まらない / F2 の主眼に双方向突合の機械化が入っていない / **詳細設計の内容を plan に複製していた(7.1-1 違反)**。→ 用語統一・排他的 3 分割・F2 に追加・**plan を受入条件だけに縮約**
  - **5 周目**: `design.md:9` が「将来項目は plan.md の基準側で判定」と書いており、plan の「design.md 1 節が唯一の正」と矛盾 → 本書 1 節参照へ
  - **周回が伸びた構造(台帳の材料)**: 3 周目 → 4 周目が「**前周で自分が書いた文言の穴**」で、前タスクの設計書ゲート 5 周目以降と同じパターン。**文書を厚くするほど検証面が増える**。ただし蒸し返しは 1 件もなく、各周の反映は次周で解消確認されている

## 決定

- **本タスクの発端**: 直前タスク(TSK-206 = 要件書 v1.9 + 設計書 v1.4)の作業中に 2026-08-11 に合意。本プロジェクトでハーネスを回すのが初回で、**タスク単位の worklog には詳細が残るが横断的な傾向が残らない**ため、改善台帳(`docs/improvements-from-baseball-scoring.md`)と同じ**蓄積台帳型**で別建てする
- **ゲートは `/finalize-doc`**(敵対レビュー + 人間承認)。**典拠は `AGENTS.md:14`**(「新設・版繰り上げ = 敵対レビュー+人間承認〔/finalize-doc〕」)**と設計書 7.2 表の「`docs/development/` … 確定ゲート 必須」(`:327`)**
  - ※ 当初 worklog に「新設 = 7.6-3 後段」と書いたが**不正確**(`/investigate` で判明)。7.6-3 の条文(`:369`)は「実装追随の節更新・変更履歴追記 = PR レビュー / **版繰り上げを伴う構造的変更** = 確定ゲート」で、**「新設」という語を含まない**。結論は変わらないが典拠を上記へ差し替える

## 未決・次の一歩

- 次: /investigate で改善台帳の様式(連番・列構成)と既存 worklog 群からの材料の追加洗い出し → /plan
- **様式の未決**: 改善台帳が I-* の連番なので台帳は H-* を想定しているが、列構成(事実 / 影響 / 対応案 / 状態)は /plan で確定する
- **運用導線の未決**: どのスキル・どのタイミングで追記するか(`/task-done` 時が候補)。決めた導線を設計書に書くか台帳冒頭に書くかも含めて /plan で決める。**設計書へ書くなら設計書側も改訂ゲートの対象**になる点に注意
- **材料の初期リスト(14 件)は Notion タスクの「背景・メモ」に記録済み** — プロセス設計の問題 5 件 / 機構の穴 6 件 / 機能した点 3 件。/investigate で既存 worklog(feature-status・harness-baseline-merge・ci-foundation ほか)から追加分を洗う
