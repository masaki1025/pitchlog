---
date: 2026-09-17
topic: U-01 DTO 基盤
branch: feature/u01-dto-base
---

# 作業ログ: 2026-09-17 U-01 DTO 基盤

## やったこと

## 決定

## 未決・次の一歩

## やったこと

- `/task-start`: TSK-388 を着手(**新規起票せず TSK-363 が起票済みのカードを再利用**・worktree `feature-u01-dto-base`・Notion を 進行中 へ)
- `/investigate`: **3 並列**(spec-checker / decision-tracer / Explore)。**legacy-analyst は外した** —
  `U-01` は base 型で、旧システムの 88 列構造と接する面は `U-D2` / `U-X5` 側にあり `data-model.md` が間に立つため。
  **裏取りが要る主張 3 件は私が原典で確認**した → `research.md`
- `/plan`: `plan.md`(契約)と `design.md`(詳細設計)を作成。**通常レビュー 5 周**で収束 → 承認

## 決定

**`U-01` は「決める側」になる。** ページングの形・ID の表現・時刻の保持形式は**要件書にも正本にも決定が存在せず**、
決めると**下流 5 単位への事実上の先決**になる。**許した条文も禁じた条文も無い**ので、**本計画書の承認をもって線引きとした**。

| 決めたこと | 内容 |
| --- | --- |
| **ページング** | **形だけ。値は決めない**(PO 判断 2026-09-17)。`limit` 必須・既定値なし・下限 1 / **不透明な継続文字列**。`total` を持たない |
| **ID** | `EntityId` = `uuid.UUID`。**実装事実に合わせた判断であって正本の帰結ではない**。**認可判定に使わない** |
| **時刻** | `Timestamp` = aware。**精度・丸め・保持形式は決めない** |
| **基底** | `BaseSchema`(`extra="forbid"`)/ `ReadSchema`(`from_attributes`) |
| **写像** | `location` はドット連結。**現行実装の形を確定してテストで固定するだけで `errors.py` を触らない** |

**`ResponseValidationError` の 500 は別タスクへ送る**(塞ぐと射程が器へ戻る)。
**禁止語検査は二重に守る** — 上流 7 語 × 差分行(本計画書の式)/ 実装 5 語 × 全文(既存テスト)。

## 計画レビュー 5 周の推移と、私の失敗

| 周 | 判定 | 性質 |
| --- | --- | --- |
| 1 | 否決 `P0 4 / P1 2 / P2 1` | **根拠を強く書きすぎ** — 要件の帰結でないものを (A) にした |
| 2 | 否決 `P0 4 / P1 3` | **7 件中 5 件が追随漏れ** |
| 3 | 否決 `P0 2 / P1 2` | **4 件中 3 件が追随漏れ** |
| 4 | 否決 `P0 2 / P1 1` | 2 件が同テーマの残り + **私の主張が成立しないもの 1 件** |
| 5 | **可決** `P0 1`(残余)→ **その場で解消** | — |

### 出続けた 2 テーマ(**どちらも私の書き方の癖**)

**1. 根拠の射程を強く書く。** 条文を読むと必ず「そこまでは言っていない」が出た。撤回した主なもの:

- **`NFR-014` を「ログに出さない」の根拠にした** — 実際は「**リポジトリに含めず環境変数で管理する**」であり**ログの規定ではない**
- **`NFR-005` を `total` を置かない根拠にした** — **`COUNT` は同条が禁じる「全件読み込み型」ではない**(むしろ是とする側)
- **`FR-041`/`FR-034` の「切り捨てず 400」を件数上限一般へ広げた** — **比較対象テナント集合の規則**
- **`set_config` を根拠に「DTO にテナント識別子を載せるのは正本と逆向き」とした** — **DB の RLS 文脈の規律**であり DTO の禁止ではない
- **DoD を「3 本の原典の具体化」と書いた** — **どの原典も DTO のテナント識別子を禁じていない**ので導出できない

**2. 同じ事実が複数ファイルにある。** 1 箇所直して別ファイルに古い記述を残す型が **3 周連続**。
**TSK-317 の 2〜3 周目とまったく同じ**で、そこで原因まで特定していたのに再発させた。
**手当ても 2 度中途半端にした**(2 周目に結論だけ外し、3 周目に詳細節を残した)。
**最終的に `research.md` の「未解決・申し送り」節を丸ごと落とし、「本書は判断を持たない」と宣言**して畳んだ。

### 撤回した「検査が守る」という書き方

**「上限が無いこと」を標本で証明しようとした。** `AfterValidator` で閾値を置かれると有限個の標本は抜ける。
**「すべてを覆う」を撤回し、網羅でないことを限界として明示**した。
**TSK-317 でも同型の指摘を受けており、2 度目**である。

## 未決・次の一歩

`/implement` でステップ 1 から委任する。申し送りは `plan.md` 7 節が正。

---

## 結果サマリ(`/pr` クローズ処理・2026-09-18)

### 実装したもの — 3 ステップ・3 コミット

| ステップ | コミット | 中身 |
| --- | --- | --- |
| 1/3 | `1b48bc3` | `BaseSchema`(`extra="forbid"`)/ `ReadSchema`(`from_attributes=True`)/ `EntityId = UUID` / `Timestamp = AwareDatetime` |
| 2/3 | `55a842a` | `PageRequest`(`limit` 必須・既定値なし・`ge=1` / `cursor` は不透明文字列)と `Page[ItemT]`(`items` + `next_cursor`)。**値は決めない**(PO 判断) |
| 3/3 | `0f2a031` | バリデーション写像(`location` のドット連結)の**現行の形をテストで固定**。`errors.py` は 1 行も触らない |

**`backend/tests/conftest.py` は 1 行も変えていない。** **依存追加なし。**

### 検証(全層を実測)

**1 回目(2026-09-18・develop 取り込み前)はハーネス層が 7 件 red** だった。
**全件が `tests/test_check_authz_catalog.py`** で、**develop が抱えていた差分閉包の欠陥**(PR #69 が是正)。
**本単位の差分は `backend/**` と `docs/**` だけ**で `scripts/` にも `contracts/` にも触れていないため、原因ではない。
**`/pr` 手順 2-4 に従って一度中断**し、**PR #69 のマージ後に develop を取り込んで再実行**した。

**2 回目(#69 マージ後 — `origin/develop` = `6ca2d33` を取り込み済み)**:

| 層 | コマンド | 結果 |
| --- | --- | --- |
| ハーネス | `ruff check .` / `ty check` | **green** |
| ハーネス | `pytest tests/` | **1390 passed**(7 件の red は解消) |
| backend | `ruff format --check .` / `ruff check .` / `ty check` | **green**(117 files already formatted) |
| backend | `pytest --ignore=tests/db` | **254 passed** |
| backend | `pytest`(全量) | **263 passed / 194 errors** |

**残る 194 errors は本単位と無関係の環境事象。** 全件が `tests/db/` の**セットアップ時 `psycopg.ProgrammingError`**で、
**ローカルの DSN が SQLAlchemy 形式(`postgresql+psycopg://`)であるために接続文字列を解釈できない** — **台帳に既出**。
**`tests/db/` を外すと 254 passed** で、**本単位が触る範囲に落ちるテストは 1 件も無い**。**DB 層の実ゲートは CI。**

**限界を 1 つ明記する** — 「ページングに上限が無いこと」は**標本では証明できない**(`AfterValidator` で閾値を置かれると有限個の標本は抜ける)。テストは**現行実装の形を固定するだけ**であり、網羅ではない。

### 正本への反映

| 正本 | 反映 |
| --- | --- |
| `docs/development/harness-evaluation.md` | **`H-89` 新設**(機構の穴・**追跡優先度 高**・`F9`)+ **既存候補「タスクカードの DoD に、原典を持たない項目が混ざる」へ 3 件目** + 変更履歴表 1 行。**版は上げない**(7.6-3 前段) |
| `docs/README.md` | 台帳行の要約と最終更新日を現行化 |

### ハーネス運用評価台帳への追記: **あり**(`H-89` + 既存候補へ 3 件目)

判断者 = PR 作成者(Claude)。クローズ処理の最終コミット前に判断し、**計画書 3 節へ宣言を追記してから**台帳・変更履歴表・索引を同一コミットで更新した。

**`H-89` を `高` とした根拠**(判定条件節の 2 条件):

- **① 制御目的の典拠 = あり** — ハーネス設計書 6.1「`codex_run.py implement` は実装ステップ表の無い計画書を**拒否**する」
- **② 誤った振る舞い = あり(a 正しい操作を止める)** — **実測で確認**した:

  | 入力 | `step_table_status` |
  | --- | --- |
  | 現行の `u01-dto-base/plan.md` | `OK` |
  | **その番号セルを太字にしただけの版** | **`NO_TABLE`** |
  | さらに 2 節「やること」に素の数字で始まる 3 セルの表がある版 | **`TABLE_OUTSIDE_SCOPE`(`'やること'`)** |

  **3 つ目では診断が無関係な表を名指しし、「実装ステップ見出しの配下へ移せ」と返す。** 本物のステップ表は正しい見出しの直下にあるので、**指示どおり動かすと計画書の構造のほうが壊れる**。

**候補のまま据え置いたもの(DoD の原典欠落 — 3 件目)**: **昇格条件(「別の単位のカードでも同型が出たら `H-*` を与える」)は成立した**。
本件は **カード TSK-388 の DoD「`tenant` を含む識別子を DTO に持たない」がどの原典からも導出できなかった**もので、
**`U-00` の 2 件が「原典に対応する記述が無い」型だったのに対し、本件は「原典が別のことを言っている」型**である
(`NFR-014` / `NFR-005` / `FR-041` / `set_config` を順に当たり、**それらしい条文が引けてしまう**ぶん危険が高い)。
**それでも `H-*` は与えなかった** — 台帳の分類規則が条件 ①(制御目的の典拠)を要求し、**設計書 11.1 は依然としてカード本文の典拠の持ち方を定めていない**ため。
**候補自身の昇格条件が分類規則より緩い**という食い違いは、**`fix/receiving-task-closure-baseline` が別の候補について PO 判断へ送ったものと同型**で、**2 つ目の候補でも表面化した**。台帳に記録して PO 判断へ送った。

### follow-up タスク一覧

| F-ID | 内容 | 出所 | Notion |
| --- | --- | --- | --- |
| **F9** | **`codex_run.py` のステップ表検出の是正** — (a) 番号セルから装飾を落としてから数える / (b) 書式要求を規範文書へ明記する / (c) 両方。**あわせて「見出し配下に 3 セルの表が実在するのに数字セルだけが読めていない」場合を `TABLE_OUTSIDE_SCOPE` と区別し、別の文言を返す** | [台帳 `H-89`](../development/harness-evaluation.md) | https://app.notion.com/p/3de93b75e687815eaed3e984121a9ec1 |

**起票済み**(2026-09-18)。**台帳側には F-ID と worklog への相対リンクだけを張り、URL は本 worklog に置く**(設計書 7.4 — 正本は Notion を参照しない)。

### frontmatter の訂正

`計画レビュー周回` を **4 → 5** に直した。**5 周すべてが指摘反映を伴っている**(5 周目も `P0` 1 件を反映してから収束)ため、記入規則の「指摘反映を伴うレビュー 1 周ごとに +1」に照らすと 4 は 1 周ぶん少ない。承認コミットの件名(「通常レビュー 5 周で収束」)とも食い違っていた。

### 申し送り(`plan.md` 7 節が正)

1. **`ResponseValidationError` が 500 に落ちる** — ハンドラの手当ては別タスク(決着先の候補は `TSK-346`)。**別途起票が要る。**
2. **述語 4(経路ちょうど 2 本)は将来必ず壊れる** — `U-T1` と葉の単位が最初に踏む。
3. **`U-T1` が並行着手中** — 依存は無いが、**先に型を決めた側が後を縛る**。
