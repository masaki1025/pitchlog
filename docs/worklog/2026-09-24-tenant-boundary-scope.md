---
date: 2026-09-24
topic: 迂回検査 条件 5・条件 2 の射程判定(TSK-440)
branch: fix/tenant-boundary-scope
---

# 作業ログ: 2026-09-24 迂回検査 条件 5・条件 2 の射程判定(TSK-440)

## やったこと

- `/task-start` — TSK-440(既存カード・ステータス `着手可`)から着手。
  `fix/tenant-boundary-scope` + worktree を `origin/develop`(`bf8ba5b`)起点で作成
- `/investigate` — 調査サブエージェント 3 本(spec-checker / decision-tracer / 実装構造)+ 自分の実測
  → [research.md](../features/tenant-boundary-scope/research.md)

## 決定(調査の結論として計画へ渡すもの)

- **条件 5 と条件 2 は別の問題として扱う。** 条件 2 はこの検査器の脅威モデルのどの項目にも対応せず、
  由来は上流分割計画の「面を混ぜない」規律(`product-impl-unit-split/plan.md:270` `:258`)
- **除外の単位をモジュールにしない。** 到達可能性は母集団の導出根拠には使えるが、
  モジュール除外の形にすると U-T1 が P0 で否決した「パスで丸ごと除外」と同じになる
  (`tenant-boundary-enforcement/design.md:463` `:470-471`)
- **他タブと合意した資産の境界**: 導出規則を資産に置くのは可 / **導出結果を資産に置くのは不可**。
  除外集合は検査の実行時に毎回導出する

## 未決・次の一歩

- `/plan` で計画書。**射程を絞るときは、絞った結果として落ちてはいけないものを同時に名指しする**
  (`worklog/2026-09-17-tenant-boundary-enforcement.md:240-247`)
- **既存の未承認履歴 7 件**(`contracts/tenant_boundary/*.json` 各 `:61-64`)の扱いは人間の判断が要る

## 実装(2026-09-24〜25)

`/implement` で 10 ステップ(当初 12・うち 2 つは撤回)。**1 委任 = 1 ステップ = 1 コミット**。

| # | 内容 | 性格 |
| --- | --- | --- |
| 1 | センサスの土台(merge-base 比較・永続 golden 無し)+ CI 実経路の回帰 | 土台 |
| 2 | 検査 visitor の訪問漏れ 7 位置(現行バグ) | 厳しくする |
| 3 | flow の網羅性 + **Call 被覆の不変条件** | 厳しくする |
| 4 | 相対 import の絶対化 | 厳しくする |
| 5 | (iv) 末尾名一致 + (v) 再輸出写像 | 厳しくする |
| 6 | **既定値の反転** | **緩和** |
| 7 | 条件 2 の裁定 exact-set(候補は狭めない) | 部分緩和 |
| — | ~~コア paths 拡大~~ | **撤回** |
| 8 | 保証単位を `design.md` 6-0 / 1-1 へ逐語で置く | 文書 |
| 9 | 申し送り・起票・Notion | 文書 |
| 10 | 凍結基準の受理 | 手続 |

### 効果(現行 contract + 現行 checker を TSK-235 の `backend/src` へ当てた全文走査)

| | 適用前 | 適用後 |
| --- | --- | --- |
| **TB007** | **794** | **4** |
| **TB002** | **146** | **35** |
| TB005 | 137 | **137** |
| TB004 | 15 | **15** |

**TB001〜TB006 は不変。減ったのは TB007 と、裁定した TB002 だけ。**

### 見つけた現行検査器のバグ 3 種(緩和と無関係に利得)

1. **`_visit_function` がデフォルト引数・注釈・戻り注釈を訪問しない** →
   `def run(c=TenantContext(X))` が **0 件**
2. **PEP 695 の `type_params` を scanner も flow も訪問しない** →
   `def run[T: TenantContext(X)]()` が **0 件**(backend は Python 3.12 固定)
3. **flow 登録の漏れ 12 件** — lambda default / annotated assignment / class base /
   `except` handler type / 終端文の後。**`authz/catalog.py` が 10 件**

### 差し戻し 4 回

| 対象 | 何を直したか |
| --- | --- |
| ステップ 1 | センサスの基準が `origin/develop` の**先端**だった → **merge-base** へ。TSK-431 が同じ検査器を変えるため |
| ステップ 5 | 再輸出写像が**未供給だと (v) が黙って飛ぶ**。本番経路が必ず供給することをテストで固定 |
| ステップ 6 | **`dataclasses.replace` の `unknown` を免除に加えていた**。負例へ注釈を足して**緑になった経路を覆い隠して**いた |
| ステップ 6 | 残る 18 件のうち 6 件が `str` / `enumerate` / `dict`。**名前は読み取れている**のに拒否しており、**宣言より厳しく既存の (i) と不整合**だった |

**3 つ目は「テストを実装に合わせて緩める」型**で、本 PR で 7 周かけて潰してきたものである。

### コア paths 拡大の撤回(2026-09-25)

**設計書 6.3-⑤ の敵対レビューで承認不可(P0×1 / P1×4)。**

- **P0**: `api/**` と `services/**` は**構築可能性ではなく「置かれそうな場所」**を選んだヒューリスティック。
  保証外にした `registry[k].make_context(t)` は**配置場所に制約がない**ので釣り合わない
- **実測で否定された私の主張 3 点**:
  ① 「どちらのマージ順でも通る」→ **PR #74 の `AREA_PATH_ADDITIONS` に `tenant-isolation` が無い**ので
     #74 が先なら拒否される ② 「過剰包含ではない」→ `api/` にヘルスチェックと DTO 基盤、
     **`services/` は 0 ファイル** ③ **`services/context_issuer.py` という例は引用先に存在しない**
     (**自分で作った例を典拠つきのように書いていた**)

**したがって本 PR は「代償なしの保証縮小」**であり、**人間が残余リスクを明示的に受容する案件**である。

### TSK-235 への申し送り(実測で特定した 12 件)

**2 ファイルに集中している。**

    TB002  domainmut/engine.py:262,263,265,265,273,273,291,296,298   9 件
             局所変数 generation / missing_generation。MutationGeneration を受ける変数名
    TB007  domaingen/pregen_checks.py:582                            1 件
             _CHECKS[check_id](...) の添字呼び出し
    TB007  domainmut/operators_display.py:469,479                    2 件
             dataclasses.replace(source.invocation, ...)

**いずれも規則どおりの検出で、真の脆弱性ではない。**
TB002 9 件は**変数名の変更**で消える。TB007 3 件は**直接呼び出しへの書き換え**で消える。

### 別タスクを起票した(2026-09-25)

**[TenantContext の発行を専用モジュールへ機械的に封じ込める](https://app.notion.com/p/3e693b75e6878136891fca40b09db799)**
(優先度 高・コア領域)

**DoD は「ファイルを移す」ではなく「そこ以外では有効なインスタンスを発行できない」まで含めた。**
`TenantContext.__init__` が公開のままでは、合法な発行コードを移しただけでは閉じないためである。

- **発行専用モジュール以外から有効な `TenantContext` を作れない実行時 capability**
- **発行モジュール外からの constructor / import / registry 登録を拒否する検査**
- **その強制点・検査器・テストだけをコア paths に登録**(ディレクトリ全体を足さない)

**着手は TSK-440 のマージ後**(保証縮小が入ってから代償を作る)。
**U-A1 / TSK-217 と射程が重なりうる** — `tenant-context-allowlist.json` の
`allowed_product_modules` は**現在 0 件が機械的に強制**されており、
**製品の入口が開く時点で本タスクの機構が要る**。

### 凍結基準の受理で踏んだ罠 2 件(2026-09-25)

**どちらも機構が自分で検出したが、どちらも「先に算出した値をそのまま書く」と静かに間違う形だった。**

1. **未承認 marker が PR 番号を焼き込んだ単一定数である。**
   `scripts/check_tenant_boundary_bypass.py:64` の `PENDING_APPROVAL` は
   `"未承認(PR #72 のレビュー待ち)"` という完全一致の定数で、
   `:552` が「これ以外の `approved_on` は `YYYY-MM-DD`」を要求する。
   **後から受理する側は、自分の出所を名乗れず PR #72 の名前を借りるしかない。**
   定数を一般化する案は取れない — 既存 6 資産の merge-base 履歴が同じ文字列を使っており、
   `_validate_history_append_only` が merge-base 履歴の変更を拒否するため。
   **本 PR は marker をそのまま使い、出所が TSK-440 であることと
   marker が機構上の定数であることを各履歴の `reason` へ明記した。**

**marker の送り先**:
**[凍結履歴のセンチネル記録を訂正する経路を定める](https://app.notion.com/p/3e593b75e68781c4aadad43595dbaccc)**
(TSK-450・未着手)。既存記録の訂正経路が設計書 7.7 に無く(7.7-2 は追記のみを定める)、
**条文の改訂か PO 裁定が先**という整理である。

**当初カードは対象を「v1 の既存 7 件だけ」と件数で固定していた**ため、
**本 PR が足す 2 件が入っていなかった**。これを指摘し、**TSK-431 側が是正した**(原典で確認):

- タイトルから「7 件」を外し、対象を **「センチネル値を持つ v1 記録すべて」** という導出される定義へ
- **「着手時に実数を数え直し、件数を前提にした手順を書かない」を DoD へ追加**
- **起票時 7 件 / 本 PR マージ後 9 件**という見込みは本文に残した

**カードのタイトルに件数を焼き込んだこと自体が、本タスクが扱う問題と同じ型**だった
(**訂正経路を定めるカード自身が、訂正できない形で古くなりかけた**)。
また 431 は v2 で `approved_by` / `approved_on` を予約語の包含
(`PENDING` / `TODO` / `TBD` / `未承認` / `未定` / `レビュー待ち`)で拒否するため、
**本 PR の 2 件は 431 の merge-base 側 prefix に入って v1 扱いになる**としている。
**431 のマージ後に実際そうなっているかは人間の確認事項として残す。**

2. **凍結射影は `excluded`(`baseline_control` と `source_digest`)以外の全トップレベル欄を含むので、
   `contract_revision` / `fixture_set_revision` 自身が射影に入る。**
   よって受理履歴の `change.after` は **revision を上げた後の状態**で算出しなければならない。
   revision を上げる前の値を書くと `_validate_baseline_transition` が
   「履歴の変更後射影が実スナップショットと不一致」で落ちる。
   **`change.before` は merge-base 側の射影**であって、作業ツリーの revision 更新前の値ではない。
   `baseline_control` 自体は excluded なので、履歴を追記しても射影は動かず、1 回で収束する。

| 欄 | 何を入れるか | 間違えやすい値 |
| --- | --- | --- |
| `change.before` | **merge-base** の資産 + 外部凍結対象の射影 | 作業ツリーの revision 更新前の射影 |
| `change.after` | **revision を上げた後**の作業ツリーの射影 | revision を上げる前の射影 |

### 現在地導出が読めなくなっている(未是正 — 人間の判断待ち)

**ステップ 8 / 9(コア paths 拡大)の撤回で総数を 12 から 10 へ詰めた結果、
先行するコミット 1〜7 の件名が名乗る `/12` が計画書の総数 10 と一致しなくなった。**
`scripts/feature_status.py` の `extract_step_tokens` は
`/<N>` が総数と一致しない件名を不正形として扱うため、**ステップ 1〜7 が進捗に算入されない**。
撤回の `Revert "... (ステップ 8/12)"` 2 件も同じ理由で不正形になる。

- **実体は 10 ステップすべて完了**である
- `/pr` の現在地導出突合は**非ブロッキング**(警告が出ても中断しない)で、CI も参照していない
- 是正するなら**件名の書き換え = 履歴改変 = force-push** が要る(**人間の操作**)

**併せて、計画書のステップ 2 の行が末尾のセル区切り `|` を欠いており、
ステップ表の解析からその行だけ落ちていた**(`STEP_ROW_RE` は閉じの `|` を要求する)。
**これは本コミットで是正した。** 表の総数が 10 と正しく読めるようになった結果、
上の `/12` 不一致が表面化した。

### 本番経路への結線を変異で確認した(2026-09-25)

**TSK-431 から「関数は正しいが本番経路へ結線されていない」型の P0 が 2 件出たとの連絡を受けた。**
**「関数を直接呼ぶテストでは 1 件も捕まらない」**という指摘なので、
本 PR が新設した 4 機構について同じ型がないかを**変異で測った**。
検査器へ変異を 1 件ずつ入れ、`tests/test_check_tenant_boundary_bypass.py` の 173 件を回した。

| 変異 | 壊した機構 | 落ちた本番経路テスト | 落ちた専用テスト | 計 |
| --- | --- | --- | --- | --- |
| M1 | 再輸出写像を常に未供給扱い((v) 無効) | `..._real_commit_diff[5]` / `..._population_is_nonempty_and_green` / `..._always_supplies_reexport_maps_to_source_change` | 2 件 | 6 |
| M2 | 相対 import の `level` を常に 0 | `..._population_is_nonempty_and_green` | 2 件 | 3 |
| M3 | Call 被覆の不変条件を無効化 | `..._population_is_nonempty_and_green` | 3 件 | 4 |
| M4 | 条件 2 の裁定を全シンボルへ拡大 | `..._real_commit_diff[2]` / `..._population_is_nonempty_and_green` | 5 件 | 7 |

**4 機構とも本番経路に結線されている。** 結線されていなければ、関数本体を壊しても
本番経路のテストは緑のままになる。**M4 では「落ちてはいけないもの」を守る
`test_recording_generation_remains_red_in_product_tree` と
`test_condition_2_unadjudicated_generation_is_red` が両方落ちた** — 裁定を広げた瞬間に赤が出る。

### develop 由来の既存欠陥を 1 件確認した(本 PR の射程外)

**TSK-431 の P0-2「比較元と HEAD の資産マップをどちらも HEAD の `FROZEN_BASELINE_ASSETS` から
作っていた」は、431 の結線に起因するものではなく develop に既にある構造である。**

- merge-base `e62aced` の検査器も `:3749` で `FROZEN_BASELINE_ASSETS` だけをループしている
  (現 HEAD では `:4593`)
- `test_frozen_baseline_asset_paths_are_an_exact_set` は
  **実ファイル集合と定数の等式**を見るだけなので、**定数と実ファイルから同時に消せば一致して通る**

**つまり資産を 1 件まるごと凍結検査の外へ出す経路がテストでも捕まらない。**
**TSK-431 が `git ls-tree` による比較元の独立列挙で是正済み**なので本 PR では触らない。

### 台帳へ足す候補(/pr のクローズ処理で判断する)

**制御目的の典拠が確認できるものだけ `H-*` へ、確定していないものは `## 候補` 節へ。**

| # | 候補 | 典拠 |
| --- | --- | --- |
| ① | **新設した任意パラメータが未供給のとき、機構が黙って無効化される。** 「本番経路が必ず供給すること」をテストで固定する手当てが有効だった | ステップ 5 の差し戻し。**TSK-431 の同型は 1 周目 `P0-3`** — `GITHUB_WORKSPACE` と検査対象 root が違うだけで不変量モードへ降格し、**環境変数の値を変えるだけで遷移・記録・`acceptance_id` の検査が丸ごと飛んだ**。**しかもその穴は、前の差し戻しで是正を指示した側が作った**(431 の自己申告) |
| ② | **「関数は正しいが本番経路へ結線されていない」型は、機構を壊す変異を入れて本番経路テストが落ちるかで検出できる。** 関数を直接呼ぶテストでは 1 件も捕まらない | 本 PR の M1〜M4 の実測。**TSK-431 の 1 周目 `P0-1`(既存の検査を結線から外して弱めた)と `P0-2`(union 走査の関数が本番から一度も呼ばれない死んだコード)** がこの型。**431 も是正後に同じ判定基準(資産本体を変異させて本番検査器が red になり戻すと green になる)で実測した** |
| ③ | **marker に固有名詞(PR 番号)を焼き込むと、後続の未承認記録が自分の出所を名乗れない。** append-only 制約により定数の一般化も塞がれる(**⑦ の対策が効かない例**) | `check_tenant_boundary_bypass.py:64`。**TSK-450 の種別 A の射程**(原典で確認) |
| ④ | **凍結射影に revision 欄自身が含まれるため、受理の `change.after` は revision 更新後に算出しなければならない** | 本 PR の受理。**Codex と独立に同じ罠を踏みかけた** |
| ⑤ | **保証範囲外として宣言した内容は撤回されやすい** | 本 PR で 4 例(条件 2 の絞り込み・コア paths 拡大ほか) |
| ⑥ | **母集団を人が列挙する検査は、射程が動くと黙って古くなる** | 既存の型。**本 PR の負例 exact-set もこの型**であり、目視の主対象として逐行確認シートへ明記した |
| ⑦ | **時点の事実を記録へ焼き込むと後の変更で古くなり、訂正経路が無いと直せない。** 対策は件数や固有名詞でなく**導出される定義**で書くこと | 実例 3 件: (a) **TSK-450 のカードが対象を「7 件」と固定していた** — **この型を定義したカード自身が、起票と同じ日(2026-09-24)に同じ型を踏んでおり、気づいたのは起票者でなく本 PR からの指摘だった**(TSK-431 の自己申告・是正済み) / (b) **本 PR の `movement_fact`** が資産の宣言を根拠として引いている(TSK-450 の種別 B として射程に入った) / (c) **③ の marker** — **⑦ の一般則が効かない例**。導出される定義で書いても、append-only が定数の一般化を塞ぐため解けない |

**⑥ の系として、資産を定数と実ファイルから同時に消すと凍結検査の外へ出せる**構造が
develop に存在する(**TSK-431 が是正中**)。**これは本 PR の射程外。**

### TSK-431 の 2 周目 P0 は本 PR に効かない(実測)

**431 の報告**: `_validate_baseline_control` が `pass_fail_mapping` を含む 7 token を
`required_triggers` としてハードコードしており、**`movement_triggers` から
`pass_fail_mapping` を外した宣言は現状の検査器では拒否される**。

**本 PR は `movement_policy` を一切変更していない**(実測: `movement_policy` /
`movement_triggers` の差分は 0。履歴の `movement_fact` の**本文中**に
`pass_fail_mapping` という語が出るだけ)。**したがって影響しない。**

**ただし** `movement_fact` は「資産が `pass_fail_mapping` を宣言していること」を
根拠として参照している。**431 が下限外 trigger を宣言から足し引きできるようにした後、
資産から `pass_fail_mapping` が消えれば本 PR の記述は時点の記録として古くなる。**
**append-only により訂正できない。**

**431 の補足**: **431 が外すのは実装側の定数だけで、資産の宣言からは `pass_fail_mapping` を
消さない**(7 資産すべてが引き続き宣言する)。**したがって現時点では顕在化しない。**
顕在化するのは将来どこかのタスクが実際に資産から外したときである。

**射程**: 当初 TSK-450 の DoD は承認欄のセンチネルに限定されており、
`movement_fact` の陳腐化は含まれていなかった。**「DoD を広げるのは人間の判断」と整理して
TSK-431 側から PO へ上げ、射程を広げる裁定が出た**(**カードに「PO 判断 2026-09-25」として
記録されている** — 裁定の場は TSK-431 のセッションであり、本セッションは立ち会っていない)。

TSK-450 は **2 種別**を扱う形になった。

| 種別 | 中身 |
| --- | --- |
| **A. 承認欄のセンチネル** | 起票時の主旨 |
| **B. 記録内容の陳腐化** | 記録本文が参照する事実が後の変更で成り立たなくなる。**実例が本 PR の `movement_fact`** |

**A は値を差し替えれば済むが、B は「いつ時点の事実として正しかったか」の保持が要る** —
当時正しかった記述を単に誤りとして消すと訂正が履歴の意味を書き換えるため。
**本 PR の `movement_fact` は書かれた時点では正確である。**
この論点は TSK-450 の DoD へ独立項目として入った。

## 結果(2026-09-26・`/pr` クローズ処理)

### 実装したもの

**10 ステップ・42 コミット。** `scripts/check_tenant_boundary_bypass.py` の条件 5 判定を
**由来種別(provenance)駆動から名前駆動へ反転**し、条件 2 に**裁定 exact-set** を置いた。
**緩和は条件 5 の 1 箇所と条件 2 の裁定だけ**で、**ステップ 2〜5 はいずれも検査を厳しくしている**。

| | 適用前 | 適用後 |
| --- | --- | --- |
| **TB007** | **794** | **4** |
| **TB002** | **146** | **35** |
| TB005 | 137 | **137** |
| TB004 | 15 | **15** |

**副産物として現行検査器のバグ 3 種を是正した**(デフォルト引数・注釈・戻り注釈の未訪問 /
PEP 695 `type_params` の未訪問 / flow 登録の漏れ 12 件)。**負例は 68 → 87 件。**

### 正本へ反映したもの

| 正本 | 反映 |
| --- | --- |
| `docs/development/harness-evaluation.md` | **既存候補「母集団を人が列挙する検査」へ 20・21 件目** + **`## 候補` へ新設 2 件** + 変更履歴表へ 1 行。**`H-*` の新規採番なし・版は上げない**(7.6-3 前段) |
| `docs/README.md` | 台帳行の候補数を 71 → 73、最終更新日を現行化 |
| その他の正本 | **反映なし**(計画書 3 節の宣言どおり) |

**`docs/features/tenant-boundary-enforcement/design.md` は feature 設計書であり正本ではない。**
6-0 と 1-1 へ保証の逐語ブロックを置いた。

### 台帳への追記の判断

**該当すると判断した。** 候補 7 件を worklog で整理したうえで、**乱立させずに 3 つへ集約**した。

- **新設 ①** 機構は正しいのに本番経路へ結線されていない(**①未供給パラメータ**を有効だった手当てとして内包)
- **新設 ②** 時点の事実を記録へ焼き込むと訂正経路が無く直せない(**③marker**・**④revision**・**⑤保証範囲外の撤回**を実例として内包)
- **既存候補へ 2 例目** 母集団を人が列挙する検査(**⑥**)

**`H-*` は採番していない** — いずれも**制御目的の典拠が無い**(設計書は「新機構が本番経路に載っていること」も
「記録内容の正確さの維持」も定めていない。後者は TSK-450 自身の対象である)。

### 残した状態

- **コミット件名のステップ記法**: ステップ 1〜7 が旧総数 `/12` を名乗ったまま。
  **撤回でステップ総数を 10 へ詰めた結果**で、**実体は 10 ステップすべて完了**である。
  **書き換えを 2 回試みたが適用できなかった**(履歴改変は分類器が遮断し、人間が打った 2 度とも反映されなかった)。
  **現在地導出は「ステップ記法が不正」と出る**が、**`/pr` の突合は非ブロッキングで CI も参照していない**。
- **未承認 marker**: 契約資産 2 件の `approved_by` / `approved_on` は marker のまま。
  **承認は人間の逐行確認の結果として行う。**

## 敵対レビュー 1 周目と差し戻し(2026-09-26)

**判定はマージ不可。P0 2 件。** どちらも**同じ根**で、
**module-wide な名前解決が字句スコープの shadow を無視する**というものだった。

| # | 重大度 | 内容 |
| --- | --- | --- |
| 1 | **P0** | **条件 5**: `from external.helpers import safe` の後に `def build(safe, t): return safe(t)` と書くと**緑になる**。引数は実行時に何でも渡せるので `TenantContext` を渡せば構築できる。**属性式でない解決不能 callable なので宣言 (iii) では赤であるべき**で、保証外として明示した `registry[k].make_context(...)` 形でもない |
| 2 | **P0** | **条件 2**: 裁定シンボルを引数で shadow すると免除される。**裁定が exact-set として機能していない** |
| 3 | P1 | 受理履歴の `reason` の「狭めただけの受理を作れなくする」は**機構が保証していない**(資産・宣言・検査器を協調改変すれば通る) |
| 4 | P2 | 逐行確認シート末尾の余分な空行(`git diff --check` が失敗) |

**P0 2 件は対照群つきで独立に再現した。**

    import した名前を引数で shadow して呼ぶ        -> 0 件   ← 穴
    対照 A: import 無しの裸名を呼ぶ                -> TB007  (正しい)
    対照 B: import した名前をそのまま呼ぶ          -> 0 件   (正しい)
    裁定シンボルを引数で shadow                    -> 0 件   ← 穴
    対照 C: 引数 generation                        -> TB002  (正しい)
    対照 D: 未裁定 RecordingGeneration を shadow   -> TB002  (正しい)

**修正後は 4 形すべてが期待どおりになった**(shadow 2 形が赤、対照 B と裁定のそのまま利用は緑)。
**局所代入・`for` target・`with`/`except` の as・内包表記・関数内 import も同じ扱い**にし、
**`global` は module 束縛を参照するため免除を維持**、**`nonlocal` は外側の字句束縛**として扱う。
**負例 2 形を追加して exact-set へ登録**し、**「import した名前をそのまま呼ぶ形が緑のまま」もテストで固定した**
(過剰拒否へ倒れたことに気づけないため)。負例は **87 → 89 件**。

**凍結基準**: 検査器が射影の外部対象なので修正で射影が動いた。
**新しい履歴を足さず、既存の末尾 1 件の `change.after` を再算出した値へ更新**した
(同じ受理の中の修正であるため。revision は 14 / 6 のまま据え置き)。
**before/after とも自分で独立に算出して一致を確認した。**

### レビューが「問題なし」と確認した観点

既存負例 68 件は全件赤のまま / 追加負例も全件赤 / **裁定 5 件はいずれも記録権世代でない**(原典を
`origin/feature/domain-calc-dsl` で確認)/ **4 機構の本番結線は成立**(変異確認は「死んだコード」型の
確認として十分と評価)/ Call 被覆の不一致は差分打ち消しの外 / **凍結連鎖はすべて一致**。

### 再レビューができない(人間の判断が要る)

**Codex が使用上限に達した**(次回は 2026-09-30)。**修正後の敵対レビューを実施できない。**
設計書 6.3 はコア領域に敵対レビューを必須としているため、**このまま人間の逐行確認へ進めるかは
人間の判断事項**である。**修正自体は完了しており、全ゲートは green。**

### 台帳候補に足すもの(次の `/pr` で判断)

**⑧ 敵対レビューが外部サービスの使用上限で止まり、修正後の再レビューができなくなる。**
**`H-69`(sandbox の制限で委任できない検証)と同族だが、あちらは実行環境の制限、こちらは残量である。**
**残量は作業の途中で尽きるため、どこで尽きたかによって手当てが変わる**
(本件は「修正は適用済み・検証は自分で実施済み・再レビューだけが欠けた」状態で尽きた)。

## develop の取り込みで自分のテストが同じ型で落ちた(2026-09-26)

**PR #79(製品認可面)が develop へ入ったため取り込んだ。** 競合は
`harness-evaluation.md` と `docs/README.md` の 2 箇所(どちらも追記行の並び)。
**凍結射影の外部対象は検査器 1 本だけで develop 側は触っていないため、
merge-base が変わっても `change.before` は不変**だった(実測: 取り込み後も CLI は exit 0)。

**取り込み後の全件実行で 1 件落ちた** — `test_product_call_coverage_sets_are_complete`。

- **不変条件そのものは守られていた**(4 集合の一致は全ファイルで通った)
- **落ちたのは総数をべた書きした `assert totals == [2261, 2261, 2261, 2261]`**。
  develop が `backend/src/pitchlog/authz/*` を増やし、**Call が 2261 → 2874 になった**

**これは本 PR が 2 度直した型そのものである。** ステップ 10 の `assert len(history) == 1` と、
台帳候補 ⑦「時点の事実を記録へ焼き込むと後の変更で古くなる」。**自分が書いたテストが同じ型を踏んだ。**

**是正**: 総数のべた書きを外し、**「母集団が空でない」ことと「4 集合が一致する」ことだけを固定**した。
**緩めていない** — 総数の正確な一致が守っていたのは母集団の増減の検知だが、
**増減は develop の変更で正常に起きる**。**fail-open を防ぐ「非空」は保っている。**

**台帳候補 ⑦ の実例に (d) として足す**(次の `/pr` で判断): **自分のテストが、
同じ型を台帳へ提出した同じ PR の中で、同じ型で落ちた。**

## 敵対レビュー 2 周目(2026-09-26)

**判定はマージ不可。`P0` 3 件・`P1` 2 件・`P2` 1 件。**
**前周の `P0` 2 件はどちらも未閉塞**と判定された(`P1` `P2` は閉じた)。
**新しい 5 形をすべて自分で再現した**(いずれも緑)。

| # | 形 | なぜ通るか |
| --- | --- | --- |
| P0-1a | `def build(safe: safe, t)` | **引数注釈**から `known_callable` が得られると字句束縛判定を素通りする。**宣言「型の解決可否は保証の条件にしない」にも反する** |
| P0-1b | 遅延束縛クロージャ | 定義時は `imported_safe`、**実行時のクロージャは後から代入した `factory`** を参照する |
| P0-2a/b | `GenerationError = other` / walrus / 注釈付き代入 | 条件 2 の**候補抽出が別名解決後の名前**を見るため、**候補名自体が消える** |
| P0-3 | 別関数の `global` writer | **module binding は可変**なのに、各関数を初期 module 環境から独立に解析している |

`P1` は ① **添字代入先を空環境で解析するため `authz/catalog.py:648` の `str` を TB007 と誤検知**
(baseline 相殺で今は隠れるが、当該行を将来編集すると表面化する)
② **クラス本体の裸名をメソッドの閉包環境として渡しており過剰拒否**
(**クラス属性をメソッドより後ろへ移すと緑になる** — 実行時の意味が同じなのにソース順で判定が変わる)。

### 方針を変えた — 列挙をやめて否定形で閉じる

**1 周目は「束縛の種類を列挙して閉じる」形だった。2 周目でさらに 5 形が出た。**
**このまま列挙で追うと 1 周に 1 件ずつ出続ける**(台帳の既知の型。**U-T1 は 5 周続いた**)。

> **import された名前の裸名呼び出しを module 側の情報で無罪にしてよいのは、
> その名前がそのモジュール内のどこでも再束縛されない場合に限る。**

**再束縛が 1 箇所でもあれば不明扱い(赤)。束縛の種類を数え上げないので、
新しい構文が来ても穴が開かない。** 条件 2 も同じ考え方にする
(**候補抽出は常に構文上の `node.id` から行い、別名解決は裁定の突合にだけ使う**)。

### 過剰拒否の代償を先に測った — 両ツリーとも 0 件

| 型 | `feature/domain-calc-dsl` | `develop`(authz 込み) |
| --- | --- | --- |
| 注釈に同名 | **0** | **0** |
| クロージャで外側の import 名を呼ぶ | **0** | **0** |
| `global` 宣言名の呼び出し | **0** | **0** |
| 裁定名の再束縛 | **0** | **0** |
| import 名の再束縛 | **0** | **0** |

**最初の測定ではクロージャ 2 件が出たが、中身は外側で `def record(...)` と定義した関数の呼び出しで
import 由来ではなく、測定条件が広すぎただけだった**(`catalog_independence.py:311,317`)。
**import 名に絞ると 0 件。**

**したがって fail-closed へ倒しても製品コードへの影響は無い。** この実測を委任プロンプトへ入れ、
**安全側へ倒してよい**ことを明示した。

### レビューが確認した良い点

- **凍結基準は現 HEAD に対して正しい**(before/after とも一致・履歴増分は各 1 件・revision 13→14 / 5→6)。
  **「未受理の同一 PR 内の是正で新規履歴を足さず末尾の `after` だけ直す」判断は 7.7 と整合**すると明示された
- **総数 assert の削除は指摘しない** — 「visitor 被覆という本来の不変条件は維持されている」
- `P1`(reason の過剰主張)と `P2`(末尾空行)は**閉じた**

## 射程の裁定(2026-09-26・人間)と 3 周目へ向けた状態

**2 周目の `P0` 5 形をすべて閉じたところ、TSK-235 の TB007 が 4 → 23 件へ増えた。**
内訳を分類して人間へ提示し、**保証範囲を 1 項縮小する裁定**を得た。

| 種別 | 件数 | 実行時に `TenantContext` を渡せるか | 裁定 |
| --- | --- | --- | --- |
| **引数として渡された callable**(`clock()` `check()` `handler()`) | **9** | **できる** | **保証範囲外** |
| 局所定義関数(`visit()` `record()`) | 5 | 再代入すれば | 保証範囲外 |
| 局所変数(`replacement()`) | 2 | できる | 保証範囲外 |
| `patch` 対象(`open` `Path.open`) | 2 | できる | 保証範囲外 |
| `cls(...)`(classmethod) | 2 | できない | 保証範囲外(過剰拒否だった) |
| 既存の残件(`dict[...]()` `dataclasses.replace`) | 3 | — | **赤のまま** |

**決め手は「TSK-235 側では直せない」こと。** `scope.py:283` の `clock: Clock = time.monotonic` は
**テストで時計を注入するための引数**で、**型注釈を付けても由来は確定しない**
(`Callable` は具体的な関数を指さない)。**計画書が挙げていた回避策が効かない形**である。

### 線引き(赤のまま維持するもの)

| 対象 | 扱い | 理由 |
| --- | --- | --- |
| 引数・局所変数・クロージャ変数の callable | **保証範囲外**(緑) | 上記 |
| **`global` で再束縛される名前** | **赤** | **module 束縛は字句束縛ではない。モジュール全体を見れば静的に追える** |
| **条件 2 の裁定** | **赤** | **裁定の exact-set 性は保証範囲内** |

**保証範囲外にした 3 形は負例から外し、正例として「緑であること」を固定した。**
**黙って消すと、後から見て漏れなのか判断なのか区別できないため。**

### 実測(裁定の反映後)

| | 反映前(2 周目修正後) | 反映後 |
| --- | --- | --- |
| **TB007** | 23 | **3** |
| TB002 | 35 | **35** |
| TB004 / TB005 | 15 / 137 | **15 / 137** |

**TB007 の 3 件は既知の残件のみ**(`pregen_checks.py:582` の `dict` 1 件、
`operators_display.py:469,479` の `dataclasses.replace` 2 件)。
**TSK-235 への申し送りは 12 件のまま変わらない**(TB007 3 + TB002 9)。

**DoD の「TB007 794 → 4」は 3 へ更新した** — 4 件目だった `authz/provisioning.py:457` の
`handler` は develop 由来の引数呼び出しで、保証範囲外になったため。

### 本 PR の保証縮小は 2 項目になった

1. 条件 5 の未解決 callable を**由来種別から名前駆動へ**(当初の主旨)
2. **引数・局所変数・クロージャ変数として外から渡された callable を経由した構築**(本裁定)

**逐行確認シートの A 節(保証の宣言)の判定項目が 1 つ増える。**

### 既存負例が 1 件、保証範囲外になって外れた(重要)

**`C5_CONTEXT_UNKNOWN_FACTORY` は develop の既存 68 件に含まれていた負例**で、中身はこれである。

    def forge_context(factory, tenant_id):
        return factory(tenant_id)

**まさに「守らないもの 4」の形**であり、**射程の裁定により緑になった**。
**したがって「既存 68 件は全件赤のまま」という本 PR の主張は、67 件へ改める必要がある。**

**削除して終わりにはしていない** — `test_parameter_bare_call_is_outside_condition_5_scope`
として**「保証範囲外なので緑である」ことをテストで固定**した。
**黙って消すと、後から見て漏れなのか判断なのか区別できないため。**

**これは保証縮小の直接の帰結であり、逐行確認の対象**である
(シートの `D-2` に「この 1 件を落とすことを受け入れるか」として入れた)。
**U-T1 が守っていた範囲を 1 件、本 PR が明示的に落とした**という事実は、
PR 本文にも書く。
