---
date: 2026-09-10
topic: develop の red を解消する — 判定注記の追随漏れ 1 件と CI の浅いクローン 2 ジョブ
branch: fix/authz-decision-note-followup
---

# 作業ログ: 2026-09-10 develop の red の解消(fix/authz-decision-note-followup)

## 発端 — TSK-343 が develop の red を報告

**PR #52(TSK-317・マージ済み `67e06a2`)のあと、develop が red になっていた。**
**報告は TSK-343(`tsk343-orm`)から**で、**私は気づいていなかった**。

**欠陥は 2 つで、どちらも `14973f6`(判定注記 7 件の追加)に起因する。**

| # | 欠陥 | 症状 |
| --- | --- | --- |
| **①** | テスト側 2 箇所が `MANAGEMENT_TARGET_GRANT_GROUP_MATCH` に追随していない | **手元の `tests/db` で 11 件 red**(exact-set が 8 対 7) |
| **②** | **`ci.yml` の `harness` と `backend` が浅いクローン** | **CI red** — `harness` 19 failed / `backend` 15 failed + 85 errors |

## ② が ① を隠していた

`provisioned_catalog` fixture が **`source_commit を解決できない`** で死ぬので、
**`_assert_failure_case_contract()` に到達する前に error** になる。
**CI ログに `sha256 exact-set 一致しない` は 0 件**(TSK-343 の実測)。

**機械は ① を捕まえる設計だった。その手前で別の理由で倒れていた。**

## PR #52 は red のままマージされた

| 時刻 | 事象 |
| --- | --- |
| 10:05:19 | `backend` ジョブ開始(1 回目) |
| 10:06:44 | **`backend` FAILURE 確定** |
| **10:09:37** | **PR #52 マージ** |
| 10:10:57 | `backend` FAILURE(2 回目) |

**チェックの状態**: `backend` FAILURE ×2 / `harness` FAILURE + CANCELLED / `core-guard` FAILURE + SUCCESS。

**PR 本文に「CI の run id 付きの数値は本 PR の CI 結果が正」と書いて CI に委ねながら、
私は結果を見なかった。** `/pr` の導線は「**CI 全グリーン → 人間がマージ**」である。

## 私の失敗は 3 段だった

1. **注記を追加したあと、回すテストを自分で選んだ**
   (検査器・新設テスト・mutation 231 件は回したが、`test_authz_management_probe.py` と
   `test_authz_toctou.py` は回していない)
2. **PR の CI を確認しなかった**
3. その CI は**別の理由で倒れていて 1 を隠していた**

**2 を守っていれば 1 と 3 の両方に気づけた。**

## 時系列の確定(TSK-343 の実測との突合)

**私の「DB 全件 110 passed」はステップ 19 が 1 件足す前の数字だった。**
develop の `tests/db` の収集は **111 件**(私も `--collect-only` で確認)で、
差分の 1 件は **`595af5c`(ステップ 19)が追加した `test_all_asset_mutations_are_killed_...`**。

→ **最後の全件 green は注記追加(`14973f6`)より前**であり、
**注記追加後に全件を回した回数はゼロ**だった。

## 計画レビュー(敵対レビュー 4 周・不採用 0 件)

| 周 | 判定 | P0 | P1 | P2 | 主な指摘 |
| --- | --- | --- | --- | --- | --- |
| 1 | 否決 | 2 | 1 | 2 | **toctou は注記だけ不足**(条件は既にある — 私の事実誤認)/ **全件実行が検証手順に未配線** / DoD の範囲が自己矛盾 / 件数「102」が誤り / **日付が未来** |
| — | **PO 判断** | | | | **CI 設定を射程に含める**(develop が数週間 red になるのを避ける) |
| 2 | 否決(全文) | 2 | 1 | 0 | **`mutation_composition.py` の変更は不要**(射程の過剰拡大)/ **`grep -c` の期待値が誤り** / 条件付き正本宣言が機構と不一致 |
| 3 | 否決 | 1 | 1 | 0 | **正本宣言が逆転していた** / ジョブ集合の導出条件が解釈分岐 |
| **4** | **可決** | **0** | **0** | **0** | 指摘なし。「**実装着手できる水準**」 |

**人間の承認**: 2026-09-10・山田正輝。

## レビューで直った、私の推測 2 件

**① `actions/checkout` の挙動を実装を読まずに推測していた。**
私は「PR では merge ref を取るので `fetch-depth: 0` でも `origin/develop` が無いことがある」
と考え、**`verify_frozen_oracle_unchanged()` の base 解決を変える計画**にしていた。
**レビューがピン留めした実装(`3d3c42e5`)の原典を確認**し、
**`fetch-depth: 0` なら全ブランチを取得し `+refs/heads/*:refs/remotes/origin/*` で
remote-tracking ref を生成する**と示した。**射程を 1 ファイル分狭められた。**

**② `grep -c 'fetch-depth: 0'` の期待値を 7 と書いたが実際は 6。**
**`:90` のコメントを数えるため**である。
**しかも私はこのセッションの前半で `:90` がコメントだと自分で気づいていた**
(awk が誤読したときに確認済み)。**それでも grep 由来の件数を書いた。**

## 3 周目の P0 — **機構の説明が機構のトリガになった**

3 節の台帳行に**機構の説明**を書いたところ、
**説明文に含めた語が `check_plan_docs_sync.py:478` の `if "反映なし" in line:` に当たり**、
**`harness-evaluation.md` が `no_change` へ分類された**。

**「この機構は同じ行に不反映の語があると不反映と判定する」という説明が、
まさにそう判定される形になっていた。**

**台帳候補 D(誤検知の記録が同じ誤検知で妨げられる)と同じ自己参照の型**であり、
**本日 3 回目**(`secret_guard` で 2 回・本件)。

**是正**: 説明を 4 節へ移し、3 節の行はパスと変更内容だけにした。
**実際の抽出器を import して検算**し、`reflected` に 3 パス・`no_change` が空になることを確認。
**4 節へ「機構が読む節に、機構のトリガ語を書いてはならない」を教訓として明記した。**

## 「人手列挙が避けられない場合の正しい形」— 本日の学びの結論

**`ci.yml` から「履歴を要するジョブ」を機械導出する標識は存在しない**
(履歴依存は `source_commit` 検査や三点差分へ**推移した先**にあり YAML には現れない)。
私は「人手列挙を避けよう」として**「不能なら理由を書いて列挙」という逃げ道**を作ったが、
**母集合のオラクルを定めていなかった**(3 周目 `P1-2`)。

**是正 = 列挙するが exact-set にする。**

- **`fetch-depth: 0` を持つ集合(7)と持たない集合(2)の両方**を宣言と exact-set 照合する
- **ジョブが増減したら必ず red** になり、
  **新設ジョブが `fetch-depth` を持たずに追加されたらそこで止まって明示的な判断を強制する**
- レビューが**実データで全経路**(追加・削除・ステップ 3 の巻き戻し・架空宣言)を確認した

**本日 11 回踏んだ「人が列挙した集合が母集団になっている」型に対する、
列挙が避けられない場合の正しい形である。**

## 未決・次の一歩

- **実装**: ステップ 1〜4 を委任(1 委任 = 1 ステップ = 1 コミット)
- **実機の DB テスト**: **無選別の全件**(`cd backend && uv run pytest -c pyproject.toml tests/db`)。
  **部分実行で済ませない**(本欠陥の原因)。所要は約 6 分 40 秒・収集は 111 → **112 件**になる見込み
- **CI の全ジョブ green を私が確認してから人間へ引き渡す**(PR #52 で欠けた手順)
- **台帳へ候補 2 件**(3 節で無条件に宣言済み): ① SQLAlchemy URL と libpq conninfo の混同が
  「到達不能」と区別できない形で失敗する(**典拠は TSK-343 の実測・本文は先方が起草**)/
  ② **fail-closed な setup error が実体的な red を飲み込む**
- **申し送り(PR #2 へ)**: `conftest.py` の `_required_db_execution_error` の強化
  (**収集した DB 必須テストのうち call フェーズに到達しなかったものがあれば red** —
  TSK-343 の提案。**今回の「fixture で死んで assertion に到達しない」型が機構で見える**・
  **定数を要しない形**)

---

## 結果サマリ(`/pr` クローズ処理・2026-09-10)

### 実装(4 ステップ・差し戻し 0 回)

| # | 変更 | 自分で確認したこと |
| --- | --- | --- |
| 1 | `test_authz_management_probe.py` へ 8 件目の失敗ケース(`MANAGEMENT_TARGET_GRANT_GROUP_MATCH` / `changed_field_id="grant_group_id"`) | **全 8 件が単独変更**(baseline との差分フィールドが 1 個ずつで `changed_field_id` と一致)・body の 8 件と **exact-set 一致** |
| 2 | `test_authz_toctou.py` へ **注記のみ**追加 | `_DECISION_NOTE_RE` が **8 件**を拾い exact-set 一致・**`ON` 条件は 1 回のまま**(重複追加なし) |
| 3 | `ci.yml` の `harness` と `backend` へ `fetch-depth: 0` | YAML パース OK・**持つ 7 / 持たない 2** をジョブ名で確認(**`grep -c` は使わない** — `:90` のコメントを数える) |
| 4 | `test_ci_wiring.py` へ両集合の exact-set 検査 | **自分で 4 パターン壊して全部 red**・無変更なら通る |

### 実機の測定値(ローカル・WSL2・CI runner ではない)

| 対象 | 結果 |
| --- | --- |
| **`tests/db` の無選別全件** | **112 passed / 410.59s(6 分 50 秒)・failed と error が 0** |
| 収集件数 | **112**(現行 111 + 8 件目のパラメータ 1。**予測どおり**) |
| ファイル別の増分 | `test_authz_management_probe.py` が **8 → 9**・他は不変 |
| `tests/test_ci_wiring.py` | **21 passed**(既存 16 + 新設 5) |
| backend `ruff format --check` / `ruff check` / `ty check` | green |
| ルート `ruff check` / `ty check` | green |
| `check_docs_status.py` | rc=0 |
| **`contracts/` の差分** | **0** |

**DoD の「無選別で全件」を守った** — `-k` / `-m` / `--deselect` / パス指定を使っていない。
**本欠陥の原因が「回すテストを人が選んだ」ことなので、ここが本 fix の中心である。**

### ステップ 4 の負例 — **本欠陥の再発形を機械で止める**

| 壊し方 | 結果 |
| --- | --- |
| `harness` の `fetch-depth` を外す | **red** |
| `backend` の `fetch-depth` を外す | **red** |
| **`fetch-depth` を持たない新設ジョブを足す** | **red** ← **本欠陥の再発形** |
| `checkout` が 2 個 | **red** |
| 無変更 | 通る |

**`test_ci_wiring.py` には `checkout` の語が 1 件も無く、
浅いクローンを検知する機構がそもそも存在しなかった。** そこを埋めた。

### 正本へ何を反映したか

| 正本 | 反映 |
| --- | --- |
| `docs/development/harness-evaluation.md` | **`## 候補` へ 2 件追記**(`H-*` の新規採番なし・版は上げない) |
| `docs/README.md` | 台帳行の要約と最終更新日を現行化 |
| その他の正本 | **反映なし**(テストと CI 配線の追随のみで、規範も設計も変わらない) |

### 台帳へ出した候補 2 件

**① SQLAlchemy 形式の URL を DSN に渡すと DB テスト全件が「到達不能」と区別できない形で落ちる**
(**典拠は TSK-343 の実測・本文は先方が起草**。**二次情報として出るのを避けるため**)

**② fail-closed な setup error はその先の実体的な red を全部飲み込む — CI の中でも起きる**
(本欠陥の三段構えを記録。**機械は ① を捕まえる設計だったが、その手前で別の理由で倒れていた**)

### 申し送り

| 送り先 | 内容 |
| --- | --- |
| **TSK-317 PR #2** | `conftest.py` の `_required_db_execution_error` の強化(**収集した DB 必須テストのうち call フェーズに到達しなかったものがあれば red**。TSK-343 の提案・**定数を要しない形**・**今回の型が機構で見える**) |
| **申し送り(未起票)** | **`/pr` の手順へ「push 後に CI の全ジョブ green を確認してから人間へ引き渡す」を明記する** — **現在の導線は「CI 全グリーン → 人間がマージ」だが誰が確認するかを書いていない**ため、**PR 作成者が飛ばせる**(本欠陥がそれ) |

### マージ順序

```
TSK-348(完了)→ TSK-317 PR #1(#52 完了)→ 【本 fix】 → TSK-343 → TSK-317 PR #2 → TSK-355 → TSK-235
```

**TSK-343 が「backend DB テスト green」を合格条件に使えないため、本 fix が先に入る**
(先方の計画書 4-6 節へ挿入済み・挿入理由も明記済み)。

### このタスクで確立したこと — **人手列挙が避けられない場合の正しい形**

`ci.yml` から「履歴を要するジョブ」を機械導出する標識は存在しない
(履歴依存は `source_commit` 検査や三点差分へ**推移した先**にあり YAML には現れない)。
**したがって列挙は避けられない。**

**ただし両方の集合(持つ 7 / 持たない 2)を exact-set で固定すれば**、
**ジョブが増減したら必ず red** になり、
**新設ジョブが `fetch-depth` を持たずに追加されたらそこで止まって明示的な判断を強制する**。

**本日 11 回踏んだ「人が列挙した集合が母集団になっている」型に対する、
列挙が避けられない場合の答えである。**
**片方だけを固定すると新設ジョブが素通りする**ことを、実際に壊して確認した。
