---
date: 2026-09-09
topic: docs/design/data-model.md v0.2 の改訂ゲート(TSK-348)
branch: feature/data-model-v0-2
---

# 作業ログ: 2026-09-09 data-model.md v0.2 の改訂ゲート(TSK-348)

## やったこと

- `/task-start 348` — worktree を `origin/develop`(`1424d67`)から作成。
- 是正箇所を**委任前に実測して確定**した(TSK-317 の差し戻し 2 回の反省 — 下記)。
- `/plan` — 計画書を起草。**5 ステップ**。

## 決定

- **③ 定義の言い換えによる重複を射程外へ出した**(PO 裁定 2026-09-09)。
  正本自身が `:2775-2778` で「**13 周目までに 98 件を是正したが毎周 5〜6 件出る**」
  「**走査は字面を探すが、残滓は言い換えで残る**」「**2,800 行規模の自然言語文書では原理的に尽きない**」
  と書いている。射程へ入れると **TSK-342 と同じ 13 周の輪**に入る。
  → **本タスクは ①② の 4 行以内の是正 + 版繰り上げに限る。**
- **TSK-317 と並行で進める**(PO 判断 2026-09-09)。本タスクは TSK-317 のマージ前提なので、
  後から着手すると確定ゲートの周回分だけ待ちが増える。
  **注意**: どちらもコア領域で逐行確認が必須。**確認待ちを 2 本同時に溜めない**。

## 実測(委任前に確定させた是正箇所)

| 件 | 是正対象 | 走査すべき概念名(`H-79` 対策) |
| --- | --- | --- |
| ① `search_path` の P0 | **`:240` の 1 行** | 「一時スキーマ」「`pg_temp`」「`search_path`」「名前解決」 |
| ② 返却契約 | **`:479` / `:484`**(集計値に限定 — 是正)/ **`:486` / `:528`**(返す経路を持たない — **維持**) | 「集計値に限定」「集計は関数に書かない」「返す経路を持たない」 |

**`:2437`・`:2661`・`:2670`・`:2750` は「乗っ取りが効かないことを要求する」側**で**是正対象ではない**。
この区別をステップ 1 の合格条件に機械検査として置いた。

## 未決・次の一歩

- 計画書を**コア領域の敵対レビュー**へ。収束したら人間の承認。
- 承認後 `/implement` → ステップ 4 の後に `/finalize-doc`(7.3 の確定ゲート)→ 確定ゲート通過後にステップ 5。
- **確定ゲートは 1〜2 周で閉じる見込み**(射程を 2 件・4 行以内に絞ったため)。
  7.3-6 が発火したら PO 裁定を起動する。**③ を射程へ戻さない。**
- **申し送り**: `AUTH-*` と `CATALOG:*` の ID 体系の食い違い / `ddl-elements.json` に DB レベル権限の記述が無い件。
