---
date: 2026-09-09
topic: docs/design/data-model.md v0.2 の改訂ゲート(TSK-348)
branch: feature/data-model-v0-2
---

# 作業ログ: 2026-09-09 data-model.md v0.2 の改訂ゲート(TSK-348)

## やったこと

- `/task-start 348` — worktree を `origin/develop`(`1424d67`)から作成。
- 是正箇所を**委任前に実測して確定**した(TSK-317 の差し戻し 2 回の反省 — 下記)。
- `/plan` — 計画書を起草。**5 ステップ**。

## 決定

- **③ 定義の言い換えによる重複を射程外へ出した**(PO 裁定 2026-09-09)。
  正本自身が `:2775-2778` で「**13 周目までに 98 件を是正したが毎周 5〜6 件出る**」
  「**走査は字面を探すが、残滓は言い換えで残る**」「**2,800 行規模の自然言語文書では原理的に尽きない**」
  と書いている。射程へ入れると **TSK-342 と同じ 13 周の輪**に入る。
  → **本タスクは ①② の 4 行以内の是正 + 版繰り上げに限る。**
- **TSK-317 と並行で進める**(PO 判断 2026-09-09)。本タスクは TSK-317 のマージ前提なので、
  後から着手すると確定ゲートの周回分だけ待ちが増える。
  **注意**: どちらもコア領域で逐行確認が必須。**確認待ちを 2 本同時に溜めない**。

## 実測(委任前に確定させた是正箇所)

| 件 | 是正対象 | 走査すべき概念名(`H-79` 対策) |
| --- | --- | --- |
| ① `search_path` の P0 | **`:240` の 1 行** | 「一時スキーマ」「`pg_temp`」「`search_path`」「名前解決」 |
| ② 返却契約 | **`:479` / `:484`**(集計値に限定 — 是正)/ **`:486` / `:528`**(返す経路を持たない — **維持**) | 「集計値に限定」「集計は関数に書かない」「返す経路を持たない」 |

**`:2437`・`:2661`・`:2670`・`:2750` は「乗っ取りが効かないことを要求する」側**で**是正対象ではない**。
この区別をステップ 1 の合格条件に機械検査として置いた。

## 計画レビューの採否記録

### 1 周目(P0×1 / P1×5)— 全件採用

`P0-1` を追跡した結果、**② 返却契約の是正方向そのものが誤り**だと判明したため
**PO 裁定 2026-09-09 で ② を取り下げた**(要件書が集計値を定めており、
`aggregation_contract: none` は probe の性質。probe と製品を同じ層として比べていた)。
`P1-2`〜`P1-6` は反映。**5 → 6 ステップ**。

### 2 周目(P1×8)— 全件採用・**8/8 が前周修正起因**

ゲート順序(`P1-1`)/ 射程宣言の対(`P1-2`)/ 走査スクリプト化(`P1-3`)/ 受け取り先の実 ID(`P1-4`)/
`guard_paths` 登録(`P1-5`)/ `CREATE` 条件の精密化(`P1-6`)/ 安定 ID 引用(`P1-7`)/
`sha256sum -c` の是正(`P1-8`)。コミット `1a4a4ca`。

### 3 周目(P1×6 / P2×1)— **7/7 が前周修正起因** → **7.3-6 発火**

| ID | 内容 | 実データでの確認 |
| --- | --- | --- |
| `P1-1` | ステップ 1 の `check_docs_status.py` green が確定していない | **実在**。`:694` が索引の版と変更履歴の最大版の**一致**を常時要求(`latest_version = max(versions)` — `:615`)。**版繰り上げを確定ゲートまで遅らせられない** |
| `P1-2` | 確定ゲートが走査表を必ず陳腐化させる | **実在**。確定行の追記で行番号がずれ、確定行に `search_path` を書けば出現集合も増える |
| `P1-3` | 「1 語減らすと red」が行集合の exact-set から導けない | **実在。指摘より深刻**。下表のとおり **5 語のうち 3 語**は削っても行集合が変わらない |
| `P1-4` | CI で `origin/develop` 比較を実行できる保証がない | **実在**。`ci.yml` の `harness` ジョブの checkout に `fetch-depth: 0` が**無い**(直上の core-guard ジョブには有る) |
| `P1-5` | `guard_paths` の保護集合が不足 / 分類表を `docs/features/` に置くのは 7.2 に反する | **実在**。負例テストと分類表が未登録。`core_guard.py:222` は `guard_paths` を完全一致で突合 |
| `P1-6` | `CREATE` 禁止を「呼び出しロール」に限定すると資産より弱い | **実在**。`check_authz_catalog.py:2775`「**列挙 schema への CREATE は空でなければならない**」= 主体は**所有者以外の全ロール** |
| `P2-1` | ステップ参照が旧番号のまま | 実在 |

**`P1-3` の実測**(`docs/design/data-model.md` の走査語ヒット行):

| 走査語 | ヒット行 |
| --- | --- |
| `search_path` | 238 239 240 245 2437 2631 2661 2670 2750 |
| `一時スキーマ` | **240 のみ** |
| `pg_temp` | **0 件** |
| `完全修飾` | **238 のみ** |
| `名前解決` | 238 242 1374 |

`一時スキーマ`・`pg_temp`・`完全修飾` は**いずれも `search_path` が同じ行を拾う**ため、
**この 3 語を削っても行集合は変わらない**。2 周目に私が書いた合格条件
「走査語を 1 つ減らすとテストが red」は **5 語のうち 3 語で成立しない**。

## PO 裁定 2026-09-09(7.3-6)— **機構を落として正本の是正だけにする**

**発火条件**: 前周修正起因が 2 周連続で過半(2 周目 **8/8**・3 周目 **7/7**)。

**裁定に至った観察**: **3 周の指摘 21 件のうち、正本の 1 行そのものへの指摘は 0 件**。
すべて**「是正が完全であることを証明するために足した機構」**への指摘であり、
**機構を足すたびに新しい検査面が生まれ、それが次周の指摘になっていた**。

| 提示した案 | 裁定 |
| --- | --- |
| **A. 機構を落として承認** | **採用** |
| B. 7 件を反映して 4 周目 | 不採用(機構がさらに膨らみ 4 周目も前周修正起因が出る見込み) |
| C. 現計画のまま打ち切り承認 | 不採用(実効のない合格条件を承認済み計画書に残すため — 7.3-3 の P1 定義そのもの) |

**帰結**:

- **3 ステップ・新設ファイル 0 件**へ縮小(`in-review` 化 / `:240` の是正 / 層の明記と 12-8 節)
- **機構一式を [TSK-353](https://app.notion.com/p/3d693b75e6878142862ef503afc270fe) へ切り出した** —
  走査スクリプト・分類表・退行 fixture・`guard_paths` 登録・`ci.yml` の `fetch-depth`。
  **3 周目の `P1-2`〜`P1-5` を「TSK-353 で最初から設計する穴」としてカードに転記済み**
- **`H-79` の機械保証は本タスクでは付けない**。`[手動・外部]` の逐行確認 + worklog 記録に降ろした。
  根拠は正本自身の `:2775-2778`(「原理的に尽きない」)
- **`P1-1` は TSK-342 の先例で確定** — 変更履歴の起案行は**目標版 `0.2`** を持ち、索引も同時に `0.2` へ。
  **確定ゲートでは状態だけが `approved` になり版は動かない**
- **`P1-6` は資産の不変条件と同じ主体集合へ戻した** —
  「`search_path` に載せるスキーマには**所有者以外のいかなるロールにも `CREATE` を与えない**」

## `H-79` 対策の 5 語走査と逐行分類(`[手動・外部]`)

**PO 裁定により機械保証は TSK-353 へ切り出したため、本タスクでは逐行で行って記録する。**
**この記録を機械 green の一部として数えない**(数えると「実効のない合格条件」= 7.3-3 の P1 になる)。

**走査語**: `search_path` / `一時スキーマ` / `pg_temp` / `完全修飾` / `名前解決`
**対象**: `docs/design/data-model.md`(**確定ゲート 反映 1 周目まで適用後**)
**出現行**: **14 件**(走査出力と下表が exact-set 一致することを機械で確認済み)

| 行 | ヒット語 | 分類 | 差分 | 判断理由 |
| --- | --- | --- | --- | --- |
| 10 | `search_path` / `一時スキーマ` / `pg_temp` | **是正済み** | 新規 | 本改訂の**起案行**(射程宣言) |
| 239 | 5 語すべて | **是正済み** | 変更 | **確定ゲート 1 周目 `P1-2` で是正**。「または全オブジェクトを完全修飾する」が `:241` の無条件要求の**代替として読める逃げ道**だった(`H-79` と同型)。`search_path` 設定を必須にし、完全修飾を補助策に降ろした |
| 240 | `search_path` | 要求側 | 未変更 | 「スキーマ検査」の**検査要求**。`search_path` を検査対象に挙げているだけ |
| 241 | `search_path` / `一時スキーマ` / `pg_temp` | **是正済み** | 変更 | ステップ 2 の本命。`pg_temp` の末尾明示へ |
| 242 | `search_path` / `pg_temp` | **是正済み** | 新規 | ステップ 2 で新設し、**1 周目 `P1-1` で集合を是正**(列挙スキーマのうち `pg_temp` を除く) |
| 244 | `名前解決` | 要求側 | 未変更 | 「ACL と名前解決を固定しないと越境できる」— 前提条件の説明 |
| 247 | `search_path` | 要求側 | 未変更 | `core-areas.json` の対象範囲に `search_path` の設定を含める要求 |
| 1377 | `名前解決` | **無関係** | 未変更 | **選手の名寄せ**(付録 D/D-6)。別概念 |
| 2440 | `search_path` | 要求側 | 未変更 | 越境テストの最低要求③「乗っ取りが効かないこと」 |
| 2634 | `search_path` | 要求側 | 未変更 | TSK-317 が実機確定する対象の列挙 |
| 2664 | `search_path` | 要求側 | 未変更 | 実機で確認する構成の列挙 |
| 2673 | `search_path` | 要求側 | 未変更 | 2440 と同じ要求の再掲(12-4 節側) |
| 2753 | `search_path` | 要求側 | 未変更 | 12-8 節の受け取り先表(TSK-317) |
| 2785 | `search_path` | **是正済み** | 変更 | ステップ 3 で 12-8 節の受け取り先を TSK-352 / TSK-353 へ是正した行 |

**内訳**: 是正済み **5** / 要求側 **8** / 無関係 **1**。
**要求側と無関係の 9 行すべてが `origin/develop` の同一行として実在することを機械で確認した**(= 未変更)。

**この走査で 1 周目 `P1-2` を先に見つけられなかった**: 初回の走査では `:239` を
「一般契約であり誤った対処方法を書いていない」として**要求側**に分類した。
しかし `:241` を無条件要求へ是正した時点で、`:239` の「または」が**代替として読める逃げ道**に変わっていた。
**是正が他の行の意味を変えることを走査では見ていなかった** — `H-79` の変種として記録する。

## 確定ゲートの適用版(設計書 7.3-1)

**適用版 = 初回敵対レビュー実行時点で approved の 7.3 の版。** 当該ゲートはこの版で最後まで運用し、
途中で切り替えない。**運用は下記の条文に従う**(スキルの手順と食い違う場合は条文が優先)。

| 項目 | 値 |
| --- | --- |
| **7.3 の適用版** | **ハーネス設計書 v1.13**(`docs/development/dev-harness-design-2026-08-07.md`・`status: approved`) |
| **7.3 条文の最終変更コミット** | **`cc76032b3942e07a4fce3641df3fc0833a847207`**(2026-09-02「設計書 v1.13 を approved 化」) |
| **設計書ファイルの現行コミット** | **`1bf690c58c8de30a2bbe9d7b1dadc3b24d1683f0`**(2026-09-04。**10.1 の実装追随のみで 7.3 条文は不変** — 節本文の差分で確認) |
| **記録日** | 2026-09-09 |

**手順の逸脱を 1 件記録する**: 7.3-1 は適用版の暫定記録を**手順 1 の `in-review` 化と同一コミット**へ
含めることを求めるが、`in-review` 化は実装ステップ 1(`ae270af`)で先に行っており、
**本記録は別コミットになった**。**初回敵対レビューの実行前に記録している**ので適用版の同定自体は成立する。

## 確定ゲートの採否記録

### 1 周目(全文レビュー)— 判定: **否決(P0×0 / P1×2 / P2×2)**・**全 4 件採用**

**母集団**(7.3-6)= 当該周の P0+P1 = **2**。**起因の P0/P1 = 1 件**なので `1 × 2 > 2` は**不成立**。
**エスカレーションは発火しない。**

| ID | 重大度 | 起因 | 分類 | 要旨 | 採否と反映内容 |
| --- | --- | --- | --- | --- | --- |
| `P1-1` | P1 | **起因** | (-) | `CREATE` 禁止のスキーマ集合が凍結資産と一致しない | **採用**。実測で確認 — `probe_data` は列挙スキーマだが `search_path` に無く、`pg_catalog`/`pg_temp` は `search_path` にあるが列挙スキーマではない。本文の条件を「**`search_path` に列挙するスキーマのうち `pg_temp` を除くすべて**」へ限定し、**候補構成側の機械検査は本条件より広い**と正確に書き直した(当初の括弧書きは両者を同一集合として説明していた) |
| `P1-2` | P1 | 非起因 | (-) | 完全修飾を `search_path` 設定の代替にできる記述が残っている | **採用**。`:239` の「または全オブジェクトを完全修飾する」を塞ぎ、**`search_path` の設定を必須**・**完全修飾は併用可能な補助策**へ。理由も明記(`:241` の契約は無条件であり、完全修飾しても**暗黙の `pg_temp` による演算子・関数の解決**は残る) |
| `P2-1` | P2 | **起因** | (-) | 追加した `1.2` 引用の参照先が誤り | **採用**。ただし**レビューの示した修正先(`1.1`)も不正確**だった — 実測すると「付与範囲の集計データ」は `1.1`(背景・目的)ではなく **`:122` の 3 章(ステークホルダー)の表**と **`:844` の `NFR-010`** にある。**規範である `NFR-010`** へ差し替えた |
| `P2-2` | P2 | 非起因 | (-) | 12-7 節の表で受け取り先欄に機能名が入っている | **採用**。`FR-030` 行の第 1 列を **12-8 節と同じ受け取り先(`PO の Could 採否判断`)**へ揃え、機能名を内容列へ移した |

**繰り越し・不採用・(B) 送り: いずれも 0 件。** 未終結の不採用一覧は空。

**レビューが確認した点**: 版・状態の整合(frontmatter `in-review` / 変更履歴最大版 `0.2` /
索引 `in-review` `0.2` `2026-09-09`)、`check_docs_status.py` green、設計伝播・文書カバレッジ・
認可カタログ・`git diff --check` green。

### 2 周目(差分と影響節)— 判定: **否決(P0×0 / P1×1 / P2×1)**・**全 2 件採用**

**母集団**(7.3-6)= 当該周の P0+P1 = **1**。**起因の P0/P1 = 1 件**なので `1 × 2 > 1` は**成立**。
**ただし 1 周目は不成立**(母集団 2・起因 1 で `2 > 2` は偽)だったため、
**2 周連続には達しておらずエスカレーションは発火しない**。
**3 周目も起因が過半なら 7.3-6 が発火する。**

| ID | 重大度 | 起因 | 分類 | 要旨 | 採否と反映内容 |
| --- | --- | --- | --- | --- | --- |
| `P1-1` | P1 | **起因** | (-) | `CREATE` 契約が構成検査へ伝播していない | **採用**。2 点あった。① 1 周目に書いた「候補構成側の機械検査は**本条件より広い**」は誤り — 実データでは**包含関係ではなく互いに差分**を持つ(`probe_data` は列挙スキーマだが `search_path` に無く、`pg_catalog` は `search_path` にあるが列挙スキーマではない)。包含の主張を撤回し「**候補構成の検査で本条件が満たされると読んではいけない**」と明記した。② **是正した契約に対応する検査要求が無かった** — `:240` の「スキーマ検査」の列挙に **`search_path` に列挙したスキーマの `CREATE` ACL** を追加した |
| `P2-1` | P2 | **起因** | (-) | `pg_temp` の名前解決対象の説明が誤っている | **採用**。**PostgreSQL 17 公式で確認した** — 「the temporary schema is only searched for relation … and data type names. **It is never searched for function or operator names.**」。1 周目に私が書いた「暗黙の `pg_temp` による**演算子・関数**の解決は残る」は誤りだった。必須化の根拠を差し替え、**① 凍結契約が無条件であること ② 完全修飾の網羅性は人手の保証にしかならず、`search_path` はカタログで観測できる単一属性として機械検査できること**の 2 点に置いた(結論は変えていない) |

**繰り越し・不採用・(B) 送り: いずれも 0 件。** 未終結の不採用一覧は空。

**レビューが確認した点**: `NFR-010` が実際に「明示的に付与された範囲の集計データ」を定めており
安定 ID として有効 / `FR-030` の受け取り先が 12-7 節と 12-8 節で一致し列形式も揃っている /
`in-review`・v0.2・索引の整合と `check_docs_status.py`・認可カタログ検査の成功。

**この 2 周で分かった型**: **是正のたびに「是正した契約と、それを検査する要求・機構との対応」が
ずれる**。1 周目 `P1-2`(是正が隣の行を逃げ道に変えた)・2 周目 `P1-1`(是正に対応する検査要求が無い)は
同じ型で、**`H-79` の「同種の欠陥を全経路へ適用しない」の隣接版**である。
**台帳候補として /pr のクローズ処理で判断する。**

### 3 周目(差分と影響節)— 判定: **否決(P0×0 / P1×1 / P2×0)**・**7.3-6 発火 → PO 裁定「範囲確定」**

**母集団** = P0+P1 = **1**。**起因の P0/P1 = 1 件**で `1 × 2 > 1` が**成立**。
**2 周目も成立していたため 2 周連続となり、設計書 7.3-6 の PO スコープ裁定が発火した。**

| ID | 重大度 | 起因 | 分類 | 要旨 | 採否 |
| --- | --- | --- | --- | --- | --- |
| `P1-1` | P1 | **起因** | (-) | 2 周目に `:240` へ追加した検査要求(`search_path` に列挙したスキーマの `CREATE` ACL)が、`:242` の契約対象(**`pg_temp` を除く**集合)と一致しない。`:241` が `pg_temp` の列挙を必須にしているため両者は必ずずれ、実装者がどちらにも読める | **採用**(下記の範囲確定で解消) |

**レビューが確認した点**: 2 周目 `P1-1` ① の「互いに差分を持つ」は実データと一致(列挙側だけ = `probe_data` /
`search_path` 側だけ = `pg_catalog`)/ `P2-1` の仕様訂正は正しい / `pg_proc.proconfig` により
機械検査可能という根拠も成立 / 版・状態・索引の整合。

#### 3 周の推移(PO へ提示した実測)

| 周 | 指摘 | 判定 | 起因比 | 争点 |
| --- | --- | --- | --- | --- |
| 1 | P1×2 / P2×2 | 否決 | 1/2 | 3-2 節の集合定義が凍結資産と不一致 |
| 2 | P1×1 / P2×1 | 否決 | **1/1** | 包含の主張が誤り + 是正に対応する検査要求が無い |
| 3 | P1×1 / P2×0 | 否決 | **1/1** | 追加した検査要求の集合が契約とずれる |

**`P0` は 3 周連続ゼロ**。**指摘総数は 4 → 2 → 1 と単調減少**。
**3 周とも争点は 3-2 節の契約表の同一の集合定義 1 箇所**であり、
**原因は同じスキーマ集合が 3 行(`:240` 検査要求 / `:241` `pg_temp` / `:242` `CREATE`)に散って
書かれていて、片方を直すともう片方とずれること**。
**TSK-342 が 13 周で踏んだ「定義が散っていて片方だけ直る」と同型。**

#### PO 裁定 2026-09-09(7.3-6)— **範囲確定**

| 提示した選択肢 | 裁定 |
| --- | --- |
| **範囲確定** | **採用** |
| 射程縮小(集合定義の精密化を射程から完全に外す) | 不採用(1 周目の「集合が凍結資産と不一致」が未解決のまま残る) |
| 続行の明示指示(1 語句足して 4 周目) | 不採用(3 周連続で同一箇所が回っており 4 周目も同型が出る見込み) |

**確定した線引き** = **本書は「関数の名前解決経路へ、所有者以外のロールがオブジェクトを注入できてはならない」
という不変条件までを定め、検査の対象集合の厳密な列挙と合格述語は実装(TSK-317)が確定する。**
`D3` の後退禁止・`D2` の局所再採番と同じ線引きであり、TSK-342 の「範囲確定」裁定と同型。

**文書への編集**(7.3-6 の表に従う):

1. **`:242` を「名前解決経路の非注入(不変条件)」へ書き換え** — 厳密な集合列挙と包含関係の説明を撤去し、
   守り方 2 つ(`pg_temp` は末尾明示 / それ以外の列挙スキーマは `CREATE` を所有者以外へ与えない)を残した
2. **`:240` の検査要求を「名前解決経路への注入可能性」へ** — **各項目の検査対象集合と合格述語は実装が確定する**と明記
3. **起案行の射程宣言の「確定しない範囲」へ TSK-317 を追記**
4. **12-8 節の「実装時に確定する範囲」へ 1 行追加**(受け取り先 = TSK-317)

**カウンタ**: 本周は指摘の採用反映を伴うので `確定ゲート周回` を **+1(→ 3)**。
**連続カウントは 0 にリセット**(PO が判断した事実で流す — 7.3-6)。
**射程が変わったため次周は全文レビュー**(7.3-4)。

### 4 周目(**射程変更直後なので全文レビュー**)— 判定: **否決(P0×0 / P1×1 / P2×0)**・**1 件採用**

**母集団** = P0+P1 = **1**。**起因の P0/P1 = 1 件**で過半だが、
**3 周目の PO 裁定で連続カウントは 0 にリセット済み**なので本周が新しい連続の 1 周目。
**エスカレーションは発火しない。**

| ID | 重大度 | 起因 | 分類 | 要旨 | 採否と反映内容 |
| --- | --- | --- | --- | --- | --- |
| `P1-1` | P1 | **起因** | (-) | **worklog の「未決・次の一歩」が計画レビュー段階のまま残り、現在地が自己矛盾**していた(「4 周目は回さない」「承認後 `/implement` でステップ 1〜3」)。射程変更後は「次周は全文レビュー」と記録している一方で、**この記録から全文レビューを省略して人間承認へ進める経路が残る**。記録の不整合だが**規約(7.3-4・7.3-6)の実効性を損なう**ため P2 ではなく P1 | **採用**。「未決・次の一歩」を**確定ゲート段階の現在地**へ書き換え、**残っている手順を 7.3-2 の終端遷移に沿って明示**した。「4 周目は回さない」は**計画レビュー**の 4 周目を指していたもので、確定ゲートには適用されない — この混同が起きない書き方にした |

**レビューが確認した点(全文レビューとして)**:

- **範囲確定の線引きは成立**。不変条件が保護対象・攻撃主体・禁止状態を特定しており、
  守り方 2 つは固定列挙ではなく分類ごとの制約。**検査項目自体は列挙されているので実装への委譲は逃げ道になっていない**
- **TSK-317 は受け取り可能** — 同タスクは schema ACL と `search_path` の実カタログ検査を責務に含み、
  **`CATALOG:ACL:*` と `CATALOG:SEARCH-PATH:*` を既に実装済み**
- **射程変更の範囲と理由が変更履歴と worklog の双方に記録されている**(7.3-6・7.3-7 の要求)
- **全文中に旧「厳密な集合列挙」を前提とする残存箇所は無い**
- **3 つの実変更が揃っている**。`place_pg_temp_explicitly_last` が `REJ-003.corrected_expectation` と**逐語一致**
- `check_docs_status.py` / `check_design_propagation.py` / `check_doc_coverage.py` /
  `check_authz_catalog.py` / `git diff --check` がすべて成功し、**凍結 3 資産の差分も 0**

### 5 周目(差分)— 判定: **否決(P0×0 / P1×1 / P2×0)**・**1 件採用** → **7.3-6 再発火 → PO 裁定「続行 + 構造的な是正」**

**母集団** = P0+P1 = **1**。**起因 1 件**で過半。**4 周目も過半だったため 2 周連続となり 7.3-6 が再発火した。**

| ID | 重大度 | 起因 | 分類 | 要旨 | 採否と反映内容 |
| --- | --- | --- | --- | --- | --- |
| `P1-1` | P1 | **起因** | (A) | 4 周目で書いた「残っている手順」が**線形**で、**7.3-2 の終端遷移の分岐を落としていた** — ① 5 周目に P0/P1 があれば採用反映後に差分再レビューへ戻る ② P2 は「P0/P1 ゼロかつ P2 あり」で一括**裁定**し採用分だけ反映 ③ 最終全文確認周で P0/P1 なら差分へ戻る・P2 のみで採用反映ありなら**最終全文確認周を再実施** ④ 人間承認へ進む条件は「最終全文確認周で収束した場合のみ」。また「収束後に必須」は**逆**で、最終全文確認周の結果によって初めて収束する | **採用**(下記の構造的な是正で解消) |

**レビューが確認した点**: 計画レビューと確定ゲートの区別は明確 / 1〜4 周の採否記録は必須項目を備え
全 8 件採用・(B) 送りなしを復元できる / `確定ゲート周回: 4` と 3 周目裁定後のリセットの記録が一致 /
**`反映1周目`〜`反映4周目` が各 1 コミット・各件名 1 マーカーで揃い、ステップ記法もない**
(`feature_status.py` の突合も「一致」)/ **`cd87314` 前後で `data-model.md` の blob SHA が同一 =
正本本文は変更していない** / 正本・索引ともに `in-review` / `0.2` / 文書検査 4 種と空白検査が成功。

#### PO 裁定 2026-09-09(7.3-6 再発火)— **続行の明示指示 + 構造的な是正**

**4 周目・5 周目は 2 周とも同じ型**だった: **私が規範表(7.3-2 の終端遷移)を worklog に再掲し、
その劣化コピーが指摘になった**。**正本本体への指摘は両周とも 0 件**で、
`P0` は 5 周連続ゼロ、指摘総数は 4 → 2 → 1 → 1 → 1。

| 提示した選択肢 | 裁定 |
| --- | --- |
| **再掲をやめて参照へ**(続行 + 構造的な是正) | **採用** |
| 4 分岐を書き足して手順表を維持 | 不採用(7.3-2 は 7 行の遷移表で、再掲を維持する限り次周も別の落としが出る) |
| 打ち切って最終確認周へ | 不採用(記録が不正確なまま残る) |

**確定した線引き** = **worklog は「現在地の事実」だけを記録し、遷移規則を再掲しない。**
**遷移の正は設計書 7.3-2 と `/finalize-doc` スキルであり、worklog はそれを参照する。**
**正本 1-1 節の「定義の所在」(同じ不変条件を 2 箇所で定義しない)と同じ考え方**で、
**この型を根から閉じる**。
**裁定の種別は 7.3-6 の表の「続行の明示指示」行**である(**射程は変わらない** —
worklog の記録方法の変更であり、確定する範囲・確定しない範囲はどちらも不変)。
**連続カウントは 0 にリセット**(PO が判断した事実で流す — 7.3-6)。
**次の周の種別は 7.3-6 の当該行と 7.3-4 が定める**(6 周目 `P1-1` の是正 —
本 worklog は遷移を定義しない)。

### 6 周目(差分)— 判定: **否決(P0×0 / P1×2 / P2×0)**・**全 2 件採用**

**母集団** = P0+P1 = **2**。**起因 2 件**で過半だが、**5 周目の PO 裁定で連続カウントは 0 にリセット済み**
なので本周が新しい連続の 1 周目。**エスカレーションは発火しない。**

**2 件はいずれも 5 周目の構造的是正そのものの書き損じ**だった。

| ID | 重大度 | 起因 | 分類 | 要旨 | 採否と反映内容 |
| --- | --- | --- | --- | --- | --- |
| `P1-1` | P1 | **起因** | (A) | **撤去対象だった operative な遷移記述を、撤去した直後に書いていた** — 「次周は差分レビューを続行する」。さらに根拠を **7.3-6 の「範囲確定」行**としたが、**今回の裁定は「続行の明示指示」**であり参照先が誤り | **採用**。当該記述を削除し、**「次の周の種別は 7.3-6 の当該行と 7.3-4 が定める」**へ置換。裁定の種別も**「続行の明示指示」行**へ是正した |
| `P1-2` | P1 | **起因** | (-) | 「現在地」の**正本本文の最終変更を `8ac40cd`(ステップ 3)と書いていたが誤り**。確定ゲート反映 1〜3 周目で本文を毎回変更しており、**最終は `1bd4b3c`**。同じセル内の「反映で本文を変更したのは 1〜3 周目」とも自己矛盾していた。**中断後に差分の基点を誤認させる** | **採用**。**`1bd4b3c`** へ是正し、**blob SHA の推移を実測値で併記**した(`dacd6839…` → `1a6d0c60…` → `04be4453…` → `33a0a276…`。**4〜6 周目は `33a0a276…` で不変**) |

**レビューが確認した点**: 1〜5 周の採否記録が全 9 件について必須項目を備え、全件採用・未終結不採用 0 件・
`(B)` 送り 0 件を復元できる / `確定ゲート周回: 5` と `反映1周目`〜`反映5周目` が各 1 コミット /
5 周目裁定による連続カウントのリセットが記録されている / 本文・変更履歴・索引が
`in-review` / `0.2` / `2026-09-09` で一致し文書検査と空白検査が成功。

**この 3 周(4・5・6)で分かったこと**: **記録側の指摘が 3 周続いた**。
4・5 周目は「規範表の再掲」、6 周目は「その是正自体の書き損じ」。
**正本本文への指摘は 4 周目以降 0 件**で、`P0` は 6 周連続ゼロ。
**記録の精度が周回の律速になっている** — 台帳候補 3 件目の実測として追記した。

### 7 周目(差分)— 判定: **否決(P0×0 / P1×1 / P2×0)**・**1 件採用** → **7.3-6 が 3 度目の発火 + 6 周警告**

**母集団** = 1。**起因 1 件**で過半。**6 周目も過半だったため 2 周連続**で 7.3-6 が発火。
**あわせて `確定ゲート周回` が 6 に達したため 6 周警告の PO 提示**を行った。

| ID | 重大度 | 起因 | 分類 | 要旨 | 採否と反映内容 |
| --- | --- | --- | --- | --- | --- |
| `P1-1` | P1 | **起因** | (-) | **「現在地」が反映 6 周目を取り込んでいない** — 6 周目の 2 件採用は採否記録に書いたのに、現在地の数値が「5 周目・9 件・`P0` 5 周連続」のままだった。実値は「6 周・11 件・`P0` 6 周連続」。**中断後に周回を誤認し、6 周到達時の PO 提示要件も見落とし得る** | **採用**(下記の構造的是正で解消) |

**レビューが確認した点**: **operative な遷移記述は 0 件**で参照へ置き換わっており裁定種別も
7.3-6「続行の明示指示」と一致 / **blob SHA が記録どおり**(`dacd6839…` → `1a6d0c60…` →
`04be4453…` → `33a0a276…`。**反映 4〜6 周目もすべて `33a0a276…` で正本全体が不変**)/
採否表は 1〜6 周・計 11 件が揃い全件採用・未終結不採用と `(B)` 送りとも 0 件 /
`反映1周目`〜`反映6周目` が各 1 コミットで機械突合も一致 / 版・状態・索引の整合と文書検査 3 種の成功。

#### 6 周警告の PO 提示(7.3-6)

| 周 | 指摘 | 起因比 | 争点 |
| --- | --- | --- | --- |
| 1 | P1×2 / P2×2 | 1/2 | **正本本文**(集合定義が凍結資産と不一致) |
| 2 | P1×1 / P2×1 | 1/1 | **正本本文**(包含の誤り・是正に対応する検査要求の欠落) |
| 3 | P1×1 | 1/1 | **正本本文**(追加した検査要求の集合がずれる)→ PO 裁定「範囲確定」 |
| 4 | P1×1 | 1/1 | worklog の現在地が計画レビュー段階のまま |
| 5 | P1×1 | 1/1 | worklog が規範表を再掲 → PO 裁定「続行 + 構造的是正」 |
| 6 | P1×2 | 2/2 | **その是正自体の書き損じ** |
| 7 | P1×1 | 1/1 | **派生値の更新漏れ** |

**`P0` は 7 周連続ゼロ。正本本文は反映 3 周目(`1bd4b3c`)以降 4 周連続で無変更。
正本本文への指摘は 4 周目以降 0 件** — **4 周続けて作業記録の書き損じをレビューしている**。

#### PO 裁定 2026-09-09(3 度目)— **続行の明示指示 + 派生値の撤去**

| 提示した選択肢 | 裁定 |
| --- | --- |
| **派生値を撤去して参照へ**(続行 + 構造的是正) | **採用** |
| 打ち切って最終確認周へ(今周の P1 を不採用支持で終結) | 不採用(派生値が古いまま残り中断後に周回を誤認する) |
| 数値を書き直して 8 周目 | 不採用(手書き維持を続ける限り同型の更新漏れが再発する) |

**確定した線引き** = **5 周目の裁定「再掲をやめて参照へ」を数値にも適用する。**
**worklog は派生値(周回・指摘件数・`P0` の連続・反映コミットの本数)を持たない。**
**それらの正は `plan.md` の frontmatter・本節の採否記録の行・`git log`・`feature_status.py` であり、
worklog はその所在だけを書く。**

**5・6・7 周目の指摘は 3 周とも「worklog が別の場所にある事実を写して劣化させた」型**だった。
5 周目は遷移規則、6 周目はその是正の書き損じ、7 周目は数値。**写す対象を無くすことで型を閉じる。**

**連続カウントは 0 にリセット**(PO が判断した事実で流す — 7.3-6)。**射程は不変。**

### 8 周目(差分)— 判定: **否決(P0×0 / P1×1 / P2×0)**・**不採用(PO 支持)** → **打ち切り裁定**

| ID | 重大度 | 起因 | 分類 | 要旨 | 採否と理由 |
| --- | --- | --- | --- | --- | --- |
| `P1-1` | P1 | 起因 | (-) | 「現在地」節から派生値が撤去し切れていない。**「台帳候補」と申し送りが次の見出しを持たないため構造上 `## 現在地` 節の内側**であり、そこに残る件数(計画レビュー 3 周 21 件・確定ゲート 1〜3 周目の 3 連続・4・5 周目の 2 連続)も派生値である | **不採用(PO が支持 — 2026-09-09・山田正輝)**。**7.3-2 の全件不採用行 (i)** により**当該指摘は終結**し、最終全文確認周へ進む。理由は下記 |

#### 不採用の理由(**打ち切り裁定 2026-09-09・山田正輝**)

**7.3-3 は P2 を「表現・語彙・記録(実装の挙動を変えない)。ゲート進行自体が生む記録・参照の
追記要求を含む」と定義している。** 確定ゲート 4〜8 周目の指摘は**すべてこの型**であり、
**本周の指摘は「見出しの境界の引き方で件数が当該節の内側に入る」という構造上の分類の話**で、
**情報の欠落ではない**。

**レビュー自身が今周、記録から現在地が復元できることを確認している**:

- 「**採否記録は 1〜7 周・全 12 件が揃い、PO 裁定 3 回も復元可能**」
- 「`feature_status.py` は**確定ゲート 7 周・3/3・反映周コミット突合: 一致**を出力した」
- 「`data-model.md` の blob SHA は `1bd4b3c`〜`03ad494` で **`33a0a276…` のまま**」
- 「本文・索引はともに `in-review` / v0.2 で整合し、文書状態検査も成功」

**ゲートの目的(正本の品質)は達している**:

| 指標 | 実測 |
| --- | --- |
| `P0` | **8 周連続ゼロ** |
| 正本本文の変更 | **反映 3 周目(`1bd4b3c`)以降 5 周連続で無変更** |
| 正本本文への指摘 | **4 周目以降 0 件** |
| 争点の所在 | **1〜3 周目 = 正本本文 / 4〜8 周目 = 作業記録** |

**「台帳候補」の件数は知見の内容そのもの**である(「3 周 21 件すべてが機構への指摘で、
正本の 1 行への指摘は 0 件だった」という**実測が知見の中身**)。**これを撤去すると知見が失われる。**
派生値の撤去は**現在地の同定に使う数値**を対象とした裁定であり、**知見の記述には及ばない**。

**この打ち切りをもって当該指摘は終結し、次は最終全文確認周(7.3-2)である。**

### 最終全文確認周(1 回目)— 判定: **否決(P0×0 / P1×1 / P2×0)**・**1 件採用**

**7.3-2 の「最終全文確認周: P0/P1 あり」行**に従い、**採否裁定して反映**し、
**採用があるので差分再レビューへ戻る**。

**3 周目以来はじめての正本本文への指摘**である(4〜8 周目はすべて作業記録側だった)。

| ID | 重大度 | 起因 | 分類 | 要旨 | 採否と反映内容 |
| --- | --- | --- | --- | --- | --- |
| `P1-1` | P1 | 非起因 | (-) | **射程宣言が実変更をすべて覆っていない** — 確定範囲③を「12-8 節の受け取り先是正」に限定していたが、実際の差分には**12-7 節の `FR-030` 行の受け取り先列の変更**も含まれる(確定ゲート 1 周目 `P2-2` の採用で入った)。**7.3-7 の必須射程との矛盾**であり、ゲート進行の記録ではない | **採用**。③ を**「12-7 節と 12-8 節の受け取り先の是正」**へ拡張し、索引の説明も追随した。**あわせて① も同じ欠陥を持っていたので是正**(3 周目の範囲確定で `:242` を「名前解決経路の非注入(不変条件)」へ書き換えたのに、起案行の① が旧文言「所有者以外のいかなるロールにも `CREATE` を与えない」のままだった)。① を**契約表 4 行の是正**として書き直した |

**差分 7 hunk と射程宣言の突合(全数)**:

| hunk | 所在 | 射程宣言のどこが覆うか |
| --- | --- | --- |
| `-2 +2` | frontmatter `approved` → `in-review` | ゲートの機構(射程宣言の対象外) |
| `+10` | 変更履歴の起案行 | 射程宣言そのもの |
| `-238,3 +239,4` | 3-2 節の契約表(3 行 → 4 行) | **確定する範囲 ①** |
| `+531` | 3-6 節の層の明記 | **確定する範囲 ②** |
| `-2634 +2637` | 12-7 節の `FR-030` 行 | **確定する範囲 ③**(本周で追加) |
| `+2753` | 12-8 節「実装時に確定する範囲」へ TSK-317 の行 | **確定しない範囲**(範囲確定の PO 裁定) |
| `-2782 +2786` | 12-8 節の申し送りの受け取り先(TSK-352 / TSK-353) | **確定する範囲 ③** |

**レビューが確認した点**: 3-2 節の契約表 5 行が相互に整合し `REJ-003` の正解値・3 関数の末尾
`pg_temp`・スキーマ 3 件の空の `create_role_ids` と一致 / 3-6 節の製品と probe の分離が
凍結資産の `candidate_probe_only`・`aggregation_contract: none` と `FR-041`・`FR-034`・`NFR-010` の
集計データ契約に整合 / 確定しない範囲が TSK-317・TSK-352・TSK-353 へ接続され「未定(要起票)」は 0 件 /
**8 周目の PO 支持済み指摘は終結時点から変化していないため再指摘していない**(7.3-5 の封鎖が機能した)/
文書検査 4 種と空白検査の成功。

### 9 周目(差分)— 判定: **否決(P0×0 / P1×0 / P2×1)**・**採用** → **P2 一括裁定 → 最終全文確認周へ**

**7.3-2 の「P0/P1 ゼロ・P2 あり(通常周)」行**に従い、**残存 P2 を一括裁定**して採用分を反映し、
**最終全文確認周へ進む**。**`P0`・`P1` がともにゼロになった最初の周**である。

| ID | 重大度 | 起因 | 分類 | 要旨 | 採否と反映内容 |
| --- | --- | --- | --- | --- | --- |
| `P2-1` | P2 | 起因 | (-) | **最終全文確認周の記録の挿入位置が 8 周目の指摘表のセルを分断していた**。8 周目の要旨セルに **`` `## 現在地` 節の内側 ``** という文字列があり、`## 現在地` を目印に挿入したためそこで切れて、**8 周目の採否行と最終全文確認周の節構造がともに壊れていた**。`f6155ec` が終結後に新たに導入した記録欠陥であり、終結済み指摘の再提示ではない | **採用**。**セルを組み直し**、挿入位置を**行頭が厳密に `## 現在地` である見出し行**の直前へ移した。**節構造を機械で確認した**(見出し一覧で 1 周目〜9 周目と「現在地」が順に並ぶこと) |

**原因**: 文字列一致で挿入位置を決めていた。**採否記録の本文に見出しと同じ文字列が現れると分断する。**
以後は**行頭が厳密に一致する見出し行**を探す。

**レビューが確認した点(hunk 全数突合の検算)**: **7 hunk すべてが射程宣言の項に対応**する
(私の突合表と一致)/ **① の実変更は正確に 4 行**(既存 3 行の改訂 + 「名前解決経路の非注入」1 行の追加。
契約表全体は未変更の「既定 `EXECUTE` の剥奪」を含めて 5 行)/ 確定しない範囲が 3 周目の裁定と一致 /
索引が 12-7・12-8 節の双方へ追随。

### 最終全文確認周(2 回目)— 判定: **可決(P0×0 / P1×0 / P2×0)** → **収束**

**7.3-2 の「最終全文確認周: 指摘ゼロ」行**により**収束**。カウントしない(収束確認周)。

**レビューが実データで確認した内容**:

- **`:240` の是正が `REJ-003` の `corrected_expectation` と逐語一致**し、3 関数の末尾 `pg_temp`・
  列挙スキーマ 3 件の空の `create_role_ids` と整合
- **probe と製品の層の説明が `FR-034` の共有粒度・`FR-041` の集計値比較・`NFR-010` の集計データ限定と整合**
- **射程宣言が実変更 3 箇所・7 hunk をすべて覆っている**
- 確定しない範囲が **TSK-317 / TSK-352 / TSK-353** に接続。**`FR-030` は `Could` 採否前なので
  受け取り先を「PO の採否判断」としているのが要件と一致**。**「未定」「要起票」は 0 件**
- **8 周目に終結した指摘は状態不変のため再提示なし**(7.3-5 の封鎖が機能)。
  **9 周目に導入・修正した記録構造にも新たな欠陥なし**
- **改訂射程外を含む全文に `P0`/`P1` 級の欠陥なし**。TSK-352/353 対象の残滓に新規の P2 (B) もなし
- 文書検査 4 種と空白検査がすべて成功

## 確定ゲートの通過(2026-09-10)

**PO 承認 2026-09-10・山田正輝。** `/finalize-doc` 手順 6 として次の 3 点を同時に反映した:

1. 正本 frontmatter を **`status: approved`**
2. 変更履歴の先頭へ **版 `0.2` の確定行**(ゲート結果・PO 裁定 4 回・争点の所在・PO 承認を記録)。
   **版は `0.2` のまま**
3. `docs/README.md` を **`approved` / `0.2` / 2026-09-10** へ。「確定ゲート未通過」を通過の記述へ置換

### レビュー往復の要点(手順 7)

**重要な指摘と対応**:

| 指摘 | 対応 |
| --- | --- |
| **`CREATE` 禁止の集合が凍結資産と一致しない**(1・2・3 周目) | 3 周目の **PO 裁定「範囲確定」**で、本書は**不変条件までを定め**検査対象集合の厳密な列挙は **TSK-317** が確定する線引きにした。**同じ集合が 3 行に散っていて片方を直すともう片方とずれる**のが原因だった |
| **完全修飾が `search_path` 設定の代替として読める**(2 周目) | `search_path` の設定を**必須**にし完全修飾を補助策へ。根拠は **① 凍結契約が無条件 ② 完全修飾の網羅性は人手の保証にしかならず `search_path` はカタログで観測できる単一属性として機械検査できる**(1 周目に書いた「暗黙の `pg_temp` による演算子・関数の解決」は**PostgreSQL 公式で誤りと確認**して差し替えた) |
| **射程宣言が実変更を覆っていない**(最終全文確認周 1 回目) | ③ を **12-7 節と 12-8 節の双方**へ拡張。**あわせて指摘外の ① も同型の欠陥を持っていたので是正**(3 周目の範囲確定を起案行へ反映していなかった)。**差分 7 hunk と射程宣言の対応表を worklog に全数で記録**した |

**警告・エスカレーションの発動**:

| 規定 | 発動 |
| --- | --- |
| **7.3-6 のエスカレーション**(起因が 2 周連続で過半) | **3 回発動**(3 周目 / 5 周目 / 7 周目)。いずれも PO 裁定を取得し連続カウントを 0 にリセット |
| **7.3-6 の 6 周警告** | **7 周目に発動**。周回・重大度推移・起因比の一覧を PO へ提示した |
| **7.3-2 の全件不採用** | **8 周目に発動**。PO が不採用を支持して終結(カウントなし・反映周コミットにしない) |

## 結果サマリ(/pr のクローズ処理・2026-09-10)

### 何を実装したか

**正本 `docs/design/data-model.md` を v0.2 で `approved` にした。実変更は 3 箇所・差分 7 hunk。**

| # | 節 | 変更 |
| --- | --- | --- |
| ① | **3-2 節の契約表(4 行)** | 「一時スキーマ」を **`pg_temp` の末尾明示**へ(`REJ-003.corrected_expectation` と逐語一致)/ 「名前解決の固定」で **`search_path` の設定を必須**にし完全修飾を補助策へ / 「スキーマ検査」へ**名前解決経路への注入可能性**を追加 / 「書き込み可能スキーマ」を**「名前解決経路の非注入」という不変条件**へ書き換え |
| ② | **3-6 節** | **probe と製品の層の明記**を 1 箇条(引用は安定 ID: `FR-034/認可行列/↳ 内容`・`FR-041`・`NFR-010`) |
| ③ | **12-7 節・12-8 節** | 受け取り先を実 ID へ(`FR-030` 行の列形式の是正 / 残滓は **TSK-352** / 機械検査は **TSK-353** / 検査対象集合の厳密化は **TSK-317**) |

**是正の根拠**: `contracts/authz/rejected-configs.json` の **`REJ-003`** が
`remove_temporary_schema_from_search_path` を **PostgreSQL 17.11 実機で `incorrect` と判定**しており
(`observed_result: temporary_relation_hijack_returned_attacker_rows` /
`rejection_reason_id: IMPLICIT_PG_TEMP_PRECEDES_LISTED_SCHEMAS`)、
**approved 正本に実機で反証済みの契約が 1 行残っていた**。実装側(関数 3 件の `search_path`)は既に正しく、
**食い違っていたのは正本の 1 行だけ**だった。

### 何を正本へ反映したか

| 正本 | 反映 |
| --- | --- |
| `docs/design/data-model.md` | **v0.2 で `approved`**。上記 3 箇所 + 起案行(射程宣言 7.3-7)+ 確定行 |
| `docs/README.md` | 索引を **`approved` / `0.2` / 2026-09-10** へ。台帳行も現行化 |
| `docs/development/harness-evaluation.md` | **`## 候補` へ 2 件追記 + 既存候補 1 件へ実測**。変更履歴 1 行。**`H-*` の新規採番はなし・版は上げない**(7.6-3 前段) |
| `docs/requirements/**` / `docs/adr/**` / `docs/ops/**` / 設計書 / `contracts/**` / `backend/**` / `frontend/**` / `.claude/**` / `tests/**` / `scripts/**` / `.github/**` | **反映なし**(宣言どおり) |

### ハーネス運用評価台帳への追記の判断(手順 1-3)

**該当する。** `## 候補` へ **2 件**を追記し、既存候補 1 件へ**実測**を追記した。
**`H-*` の新規採番はしていない**(いずれも単発のゲート内観測で、昇格条件を明記した)。

1. **是正の完全性を証明する機構を同一タスクの射程へ入れると輪になる** —
   計画レビュー 3 周・指摘 21 件の**すべてが機構への指摘**で、**正本の 1 行への指摘は 0 件**
2. **ある場所の記述を別の場所へ写すと写し先が劣化する** —
   確定ゲート 10 周のうち **6 周がこの型**。**正本内の契約 → 検査要求 / 規範表 → 作業記録 /
   派生値 → 作業記録**の **3 層で観測**
3. **実測の追記** — 既存候補「字面の走査では、自然言語の言い換えによる定義の重複を捕まえられない」の
   **対応案が TSK-353 として起票**され、その設計の穴 4 件(行集合の exact-set では走査語を守れない /
   全件「無関係」でも通る / 確定ゲートの追記で分類表が陳腐化する / CI で `origin/develop` が解けない)を
   同カードへ転記した

### 申し送り(起票済み)

| 送るもの | 受け取り先 |
| --- | --- |
| 定義の言い換えによる残滓の是正 | **TSK-352** |
| その機械検査の新設(走査スクリプト・分類表・退行 fixture・`guard_paths` 登録・`ci.yml` の `fetch-depth`) | **TSK-353** |
| 3-2 節「名前解決経路の非注入」の検査対象集合の厳密な列挙と合格述語 | **TSK-317**(範囲確定の PO 裁定) |

### 未起票の申し送り(/pr で扱う)

- **`AUTH-*` と `CATALOG:*` の ID 体系の食い違い** — TSK-250 の計画書が期待する `AUTH-*` は実在しない
  (実測 0 件・`auth-catalog.json` の全 187 件が `CATALOG:*`)
- **`ddl-elements.json` に DB レベル権限の記述が無い** — TSK-317 のステップ 4 で顕在化
  (実測: `ON DATABASE` / `CREATE ON` / `connect` が各 0 件)

## 現在地

**確定ゲート(7.3)の途中。** 実装ステップ 1〜3 は完了(`ae270af` / `27179a6` / `8ac40cd`)。
**計画レビューは 3 周 + PO 裁定で終了**しており、上記は**確定ゲートの周回**である。

**本節は現在地の同定に使う派生値を持たない**(7 周目 `P1-1` の PO 裁定)。
**下記「台帳候補」の件数は知見の内容そのものなので本裁定の対象外である**
(8 周目 `P1-1` の打ち切り裁定 — 撤去すると知見が失われる)。**各事実の正は次の場所にある**:

| 知りたいこと | 正の所在 |
| --- | --- |
| 確定ゲートの周回数 | `docs/features/data-model-v0-2/plan.md` の frontmatter `確定ゲート周回` |
| 各周の指摘・重大度・起因・採否 | **本 worklog の「### <n> 周目」の採否記録**(周ごとに追記される) |
| 反映周コミットの一覧と件名 | `git log --oneline origin/develop..HEAD`(件名に `反映<r>周目` を含むもの) |
| 正本本文が変わった周 | `git rev-parse <commit>:docs/design/data-model.md`(blob SHA の推移) |
| 段階・ステップ進捗・PR 状態・反映周コミット突合 | `uv run python scripts/feature_status.py` |
| 次の周の種別とその後の遷移 | 設計書 **7.3-2 の終端遷移表**と `/finalize-doc` の手順(5 周目 `P1-1` の PO 裁定) |
| PO 裁定の内容と根拠 | **本 worklog の各周の採否記録内の「#### PO 裁定」**(3 周目・5 周目・7 周目) |

**依存**: **TSK-317 の PR は本タスクのマージを待つ**(`data-model.md` 12-4 節のマージゲート)。
**マージ後に TSK-353** へ着手(機械検査一式 — 計画レビュー 3 周目の指摘 4 件が設計の入力)。

**申し送り**: `AUTH-*` と `CATALOG:*` の ID 体系の食い違い / `ddl-elements.json` に DB レベル権限の記述が無い件。

**台帳候補**(/pr のクローズ処理で判断):

1. **「是正の完全性を証明する機構を同一タスクに入れると、機構自身が新しい検査面を生み、
   前周修正起因の輪になる」** — 計画レビュー 3 周 21 件すべてが機構への指摘で、正本の 1 行への指摘は 0 件
2. **「是正した契約と、それを検査する要求・機構との対応がずれる」** — 確定ゲート 1〜3 周目の 3 連続。
   **`H-79` の隣接版**
3. **「規範表を作業記録に再掲すると、劣化コピーが指摘を生み続ける」** — 確定ゲート 4・5 周目の 2 連続。
   **`H-79` / 定義の所在の型が、正本ではなく worklog で再発した**。
   **是正は「再掲をやめて参照にする」**(PO 裁定 2026-09-09)
