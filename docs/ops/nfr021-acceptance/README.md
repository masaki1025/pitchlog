---
status: in-review
---
| 版 | 日付 | 変更内容 | 状態 |
| --- | --- | --- | --- |
| 1.0 | 2026-08-19 | 新設(Phase 4-4)。敵対レビュー 6 周 → PO 承認 | approved |
| 1.1 | 2026-08-24 | **ブートストラップ手順へ「Phase 4 完了時受入の前提 PR」を挿入(起案 — 版繰り上げ)**: 設計書 v1.9 が `onboarding.md` の完成と v1.0 approved 化を Phase 4 の成果物から**前提**へ移し、論理スロットの外へ出したことへの追随。従来の実務手順は `4-5` → `4-6` で**前提 PR を飛ばしており**、「規範として競合した場合は設計書が優先する」という注記があっても**実行用手順に旧経路が残る**状態だった(設計書 v1.9 確定ゲート 1 周目 P1-2)。旧手順 5・6 を 6・7 へ繰り下げた。**受入の実施主体・証跡の様式・検査項目は変更していない**。**確定ゲート 1 周目の反映(P1×1 を採用・不採用 0 件)**: 手順 5 が「統合してから onboarding の approved を確認する」という順序で、**設計書 13 章「Phase 4 完了時受入の前提 PR」のマージ条件を欠いていた**。**本書 v1.1 自身もその対象**であるため、従来の文言では本書が `in-review` のまま前提 PR を統合できた(手順 1 の 4-4 確定ゲートは v1.0 の確定であり v1.1 を代替しない)。**マージ条件を満たしたことを確認してから統合する**順序へ是正した。**確定ゲート 2 周目の反映(P1×1 を採用・不採用 0 件)**: 1 周目の反映で当該マージ条件の**条文を本書へ書き写しており 7.1-1(複製せず参照する)に違反**していた(かつ抜粋のため設計書側の「通常 PR 要件」「`gate_kind: phase4` は課さない」が落ちて**条件の説明としても不完全**だった)。**条文を削り節参照に留めた**。計画: `docs/features/onboarding-approval/plan.md` | in-review |
| 1.1 | 2026-08-24 | **確定ゲート通過(approved)**: 敵対レビュー **3 周**(sol xhigh — 1 周目 P1×1・2 周目 P1×1 を**全件採用・不採用 0 件**、3 周目は P0/P1 いずれも 0 件で収束)→ **PO 承認(2026-08-24・山田正輝)**。**確定内容**: ブートストラップ手順へ**手順 5 として「Phase 4 完了時受入の前提 PR」を挿入**し旧手順 5・6 を 6・7 へ繰り下げ(設計書 v1.9 が同 PR を論理スロット外に位置づけたことへの追随)/ **マージ条件を満たしたことを確認してから統合する**順序に固定し、**本書自身も同節の対象**であることを明示(条文は複製せず設計書 13 章の同名節を参照 — 7.1-1)/ **Phase 4-5 の実装状態を「実装済み」へ是正**(3 箇所)。**受入の実施主体・証跡の様式・命名文法・検査項目・append-only の扱いは変更していない**。計画: `docs/features/onboarding-approval/plan.md` | **approved** |
| 1.2 | 2026-08-26 | **ブートストラップ手順の手順 5 を第 2 の前提 PR へ対応させ、onboarding の版の焼き込みを撤去(起案 — Phase 4-6)**: 設計書 v1.10 が「Phase 4 完了時受入の前提 PR(第 2 号)」を新設したことへの追随。手順 5 を**第 1 号 → 第 2 号の順に統合する**形へ改め、**「版が v1.0 であることを確認する」という版の焼き込みを撤去**して「**その時点で `develop` に統合済みの approved 版**」へ改めた — `onboarding.md` が v1.1 へ改訂されたことで**現行の実行用手順が腐っていた**(設計書 v1.10 確定ゲート 2 周目 P1。同じ版焼き込みは設計書 10.1 の実施順序 ① にもあり同時に撤去した)。**受入の実施主体・証跡の様式・命名文法・検査項目・append-only の扱いは変更していない**。計画: `docs/features/phase4-6-acceptance/plan.md` | in-review |
| **1.2** | 2026-08-26 | **確定ゲート通過(approved)**: 設計書 v1.10・`onboarding.md` v1.1 と**同一ゲート**(敵対レビュー 5 周・全件採用・不採用 0 件)→ **PO 承認(2026-08-26・山田正輝)**。**確定内容**: ブートストラップ手順 5 を**第 1 号 → 第 2 号の順に統合する**形へ改め、**onboarding の版の焼き込み(v1.0)を撤去**して「**その時点で `develop` に統合済みの approved 版**」とした。**本書 v1.2 自身が第 2 号の対象 3 本の 1 つである**ことも明記。**受入の実施主体・証跡の様式・命名文法・検査項目・append-only の扱いは変更していない**。計画: `docs/features/phase4-6-acceptance/plan.md` | **approved** |
| 1.3 | 2026-08-27 | **ブートストラップ手順 5 を前提 PR(第 3 号)へ対応**(前提 PR 第 3 号): 第 1 号・第 2 号の 2 号立てから **第 1 号 → 第 2 号 → 第 3 号**の 3 号立てへ / 設計書 13 章の参照を「同名 2 節」→「**同名 3 節**」へ / **本書 v1.3 が第 3 号の対象 4 本の 1 つである**ことを明記（**確定ゲート 1 周目の反映**: 「証跡本文の完全性」へ**人手で確認する項目**を追加 — ログ成果物の参照先が候補ツリーに収録されていることと 6 ラベルの実在は、検証器の射程外なので判定者が目視で確認する）（**確定ゲート 2 周目の反映**: 人手確認項目を「参照先の収録」から「**6 ラベルの実値が証跡本文に記載されていること**」へ改めた） | **draft**(確定ゲート中) |

# NFR-021 受入証跡の運用

## 位置づけと文書分類

このディレクトリは NFR-021 の受入証跡を運用する場所である。正本はこの
`README.md` と、`reservation-template.md`、`evidence-phase4-template.md`、
`evidence-release-template.md` の 4 件だけである。個々の予約レコードと結果証跡は、
変更不可の監査記録であり、正本ではない。

文書分類・証跡の保持・正本の確定手続は、[ハーネス設計書](../../development/dev-harness-design-2026-08-07.md) 10.1「NFR-021 受入ゲート」の
「文書分類」および「保持」を正とする。証跡の必須項目、合格項目、受入プロファイルは
[要件定義書](../../requirements/requirements-pitchlog-2026-07-22.md) の NFR-021 を正とし、
本書では複製しない。失効の意味論と検証器の契約も、ハーネス設計書 10.1 の該当項を
参照する。

## 実務手順

受入は、予約を確定してから実施し、結果証跡で予約を閉じ、排他を解除する順に進める。
開始条件、採番、PR への統合、排他解除の規範は、ハーネス設計書 10.1「NFR-021 受入
ゲート」の「試行の排他と採番」および「実施順序」を参照する。予約が未閉塞の間は、
同一ゲートキーの次の試行を始めない。

## レコード種別の閉じた命名文法

すべて `docs/ops/nfr021-acceptance/` の直下ファイルに限る。**このディレクトリに置いてよいのは
下表の 3 種だけ**であり、サブディレクトリを作らない。**下表のいずれにも一致しないツリー項目は、
拡張子の有無・種類を問わずすべて不正**とする(無視ではなく不合格 — 未知の項目を黙って通すと、
非正規名の証跡が列挙から漏れる)。

| 種別 | 正規形 | 備考 |
| --- | --- | --- |
| 正本(除外集合) | `README.md` / `reservation-template.md` / `evidence-phase4-template.md` / `evidence-release-template.md` の 4 件のみ(完全一致) | 索引必須。検証器の列挙対象から除外する。 |
| 予約レコード | `YYYY-MM-DDTHHMMSSZ-<gate_kind>-<phase4\|vX.Y.Z>-seq<NNN>-reservation.md` | `<NNN>` は `attempt_seq` の最低 3 桁ゼロ詰め。1000 以降は桁が増える。 |
| 結果証跡 | `YYYY-MM-DDTHHMMSSZ-<gate_kind>-<phase4\|vX.Y.Z>-seq<NNN>-<short_sha>.md` | `<short_sha>` は `tested_commit_sha` の短縮形。`seq<NNN>` により形式として一意にする。 |
| 上記のいずれにも一致しないツリー項目 | fail-closed | **拡張子を問わない**。`.MD` などの大文字違い、`.yaml`・拡張子なし、サブディレクトリとその配下の全項目を含む。検証器は無視せず不合格にする。 |

| 要素 | 規則 |
| --- | --- |
| `YYYY-MM-DDTHHMMSSZ` | 秒精度の UTC。区切りは日付部のみハイフン、時刻部は区切りなし、末尾は `Z` 固定。 |
| `<gate_kind>` と第 2 要素の組 | `phase4-phase4` または `release-vX.Y.Z` のみ。その他の組合せは不正。 |
| `vX.Y.Z` | `v` + 数値 3 連のセマンティックバージョン。 |
| `<short_sha>` | `tested_commit_sha`(完全な小文字 40 桁の commit OID)の**先頭 12 文字**。桁数を固定するのは、Git の動的な一意短縮に委ねると実装ごとに桁が変わるため。 |
| `seq<NNN>` | `seq` + `attempt_seq` の 10 進表記。**3 桁未満なら 3 桁へゼロ詰めし、3 桁以上はそのまま書く**(`zfill(3)` 相当)。したがって `seq001` は正しく、`seq0001`・`seq1` は不正。 |

### ファイル名と frontmatter の対応

**予約レコードと結果証跡でキーの持ち方が異なる**ため、種別ごとに定める。

**結果証跡**: ファイル名の `<gate_kind>` は frontmatter の `gate_kind` と一致する。第 2 要素が
`phase4` なら `release_version` は**不在**、`vX.Y.Z` なら `release_version` と一致する。
`seq<NNN>` は `attempt_seq`、`<short_sha>` は `tested_commit_sha` の短縮形に対応する。

**予約レコード**: 予約は**ゲートキーを合成形の単一キー `gate_key` で持つ**(`phase4` または
`release-vX.Y.Z`)。ファイル名の `<gate_kind>-<phase4|vX.Y.Z>` は、`gate_key` から次の規則で
導いた値と一致する — `gate_key` が `phase4` なら `phase4-phase4`、`release-vX.Y.Z` なら
`release-vX.Y.Z`。`seq<NNN>` は `attempt_seq` に対応する。**予約は `gate_kind` と
`release_version` をキーとして持たない**(`gate_key` が両者を保持するため。別々に持つと
不一致の余地を作る)。

### 二重記録の一致

結果証跡は同じ事実を frontmatter と本文の両方に持つ。**機械検証の正は frontmatter とし、
本文の記載は frontmatter と完全に一致していなければならない**。少なくとも次の 2 組は
一致を必須とし、食い違えば fail-closed とする。

- 本文「commit SHA」 = frontmatter `tested_commit_sha`
- 本文「onboarding blob SHA」 = frontmatter `onboarding_blob_sha`

これを課さないと、frontmatter を T、本文を別のコミットとした証跡でも機械検査を通ってしまう。

**本文「onboarding 版」は機械照合の対象にしない。** 要件書 NFR-021 が人間可読の記録として
「onboarding 版」を必須にする一方、ハーネス設計書 10.1「NFR-021 受入ゲート」は「版番号では内容を
一意に識別できない」(版を上げない節更新があるため)としており、版番号と blob を機械的に突き合わせる
規則は成立しない。**そこで本文には版と blob SHA の両方を欄として持たせ、機械照合は blob SHA だけで
行う**。版は人間が読むための記録として残す。

以上の突合は Phase 4-5 で実装済みの検証器が行う。

## `attempt_id` の生成規則

`attempt_id` は次の合成形式で発行する。

```
<ゲートキー>-<attempt_seq の最低 3 桁ゼロ詰め>-<発行時刻 YYYYMMDDTHHMMSSZ>
```

- ゲートキーは `phase4` または `release-vX.Y.Z` に固定する。予約レコード、結果証跡、
  `attempt_id` のいずれでも別表記を許さない。
- 例は `phase4-001-20260819T101500Z` とする。
- 発行主体は、[要件定義書](../../requirements/requirements-pitchlog-2026-07-22.md) 8 章の
  判定者である。採番主体もハーネス設計書 10.1「NFR-021 受入ゲート」の「試行の排他と
  採番」に従う。

ゲートキーと `attempt_seq` を ID 自身に埋め込むことで、`attempt_id` とレコード本文の
不一致を目視でも検出できる。

## レコード間の一致契約

同一の `attempt_id` を持つ予約レコードと結果証跡では、ゲートキー、`attempt_seq`、
`release` の場合は `release_version` がすべて一致しなければならない。1 つでも
食い違えば fail-closed とし、検証器は対応不能として不合格にする。新しいキーは増やさない。

## 索引の必須・除外集合と監査 PR の受入条件

正本 4 件は `docs/README.md` の正本一覧に載せる。予約レコードと結果証跡は正本ではないため、
索引カバレッジ検査の対象外とする。

除外の実装は `docs/ops/nfr021-acceptance/` 全体の接頭辞単位にしてはならない。直下にあり、
かつ予約レコードまたは結果証跡の正規形に一致するものだけを除外する。正本 4 件、未知の
`.md`、サブディレクトリ配下の `.md` は索引漏れとして fail-closed にする。

**この除外規則の正本は本書である**(設計書 10.1 は本書を参照するに留める — 7.1-1)。

実装(`scripts/check_docs_status.py` の変更)は Phase 4-4 の責務ではない。**予約レコード監査 PR**
が実装し、正規形の予約・結果証跡を索引不要とする**正例**と、正本 4 件・未知のツリー項目・
サブディレクトリ配下の項目を索引漏れとして拒否する**負例**の両方を受入条件に含める。

## 証跡本文の完全性

結果証跡は、要件書 NFR-021 の証跡項目とゲート種別に応じた合格項目を、**欄として漏れなく持ち、
空欄やテンプレートのプレースホルダ(`<...>` の形)を残さない**。これは**作成時に満たすべき運用規律**である。

**人手で確認する項目(機械検査の射程外 — v1.3)**: 要件書 NFR-021 は「標準出力またはログ成果物への参照」欄に
**`host_raw` / `host` / `wsl_uname_raw` / `wsl_uname` / `wsl_dpkg_raw` / `wsl_dpkg` の 6 ラベルの実値を
証跡本文へ直接記載すること**を要求する(実施したアーキテクチャの識別)。**別ファイルへの参照だけにしない** —
**証跡本文は本節の改変禁止・append-only の保護下にあるが、参照先のファイルは後続の試行で上書きされうる**ため。
**検証器はこの欄が非空でプレースホルダでないことしか検査しない**(ゲート時の完全性検査 ⑩)ので、
**6 ラベルの実在と、値が受入プロファイルに適合していることは判定者が目視で確認する**。
採取手順は `docs/development/onboarding.md` の 2-2 節が定める。

機械検査は**二段**で行う。**両者は別物であり、互いに代替しない。**

| 検査 | 時点 | 対象 | 実装 |
| --- | --- | --- | --- |
| **統合時の受入検査** | PR が `develop` へ統合される前 | **その PR で新規に追加された**予約レコード・結果証跡だけ | append-only の CI 検査(Phase 4-5) |
| **ゲート時の完全性検査(⑩)** | 受入判定の実行時 | `evidence_path` で**名指しされた 1 件**だけ | 検証器(Phase 4-5) |

**統合時の検査が要る理由**: 統合された時点で証跡は変更も削除もできなくなる。`failed` の結果証跡は
監査 PR で、`release` の証跡は検証器の実行前に `develop` へ統合されるため、**不可逆化の前に検査
できる場所はここしかない**。ここを欠くと、欄不足やプレースホルダの残った証跡が PR レビューの
見落としで統合された時点で、二度と直せなくなる。

**どちらも過去の証跡を再検査してはならない理由**: 証跡は append-only で訂正も削除もできず、同一
`attempt_id` の訂正版を足すことも 1 対 1 の対応契約に反する。したがって過去の不完全な `failed`
記録が 1 件でもあると、**そのゲートキーが恒久的に閉塞する**。

**統合時の受入検査が見るもの(4 項目)**:

1. **完全性** — ⑩ と同じ(証跡項目・ゲート別合格項目の存在、空欄・プレースホルダの不在)
2. **命名文法への適合** — 正規形・字句規則・直下ファイル限定
3. **スキーマ適合** — 必須キーの存在と型、ファイル名と frontmatter の対応、レコード間の一致契約
4. **孤児・多重対応の拒否**
   - **結果証跡側** — 新規の結果証跡には対応する予約が統合後のツリーに**ちょうど 1 件**存在すること /
     同一の `attempt_id` に対する結果証跡が**2 件目にならない**こと
   - **予約側** — 統合後のツリーにおいて `attempt_id` と (ゲートキー, `attempt_seq`) の組が**それぞれ一意**で
     あり、**同じ値を持つ予約が 2 件目にならない**こと

**3 と 4 が要る理由**: これを欠くと、本文と名前だけ正しい**孤児の結果証跡**・**同一予約への 2 件目の
結果証跡**・**同じ `attempt_id` を持つ 2 件目の予約**が統合されてしまう(予約の重複は、ファイル名の時刻
だけを変えれば命名文法もスキーマ適合も一致契約もすべて満たせるため、専用の検査が要る)。統合後は変更も削除もできないため、検証器の合格条件 ⑨(対応する予約がちょうど 1 件・
予約を持たない結果証跡が 0 件)により、**以後の正しい試行も永久に不合格**になる。

**上記 2 つの検査は Phase 4-5 で実装済みである**(v1.1 — 設計書 v1.9 確定ゲート 2 周目 P1。従来は 4-5 の受入契約として未来形で書いていた)。

**実測値の内容が妥当かどうかの判断は人間(判定者)が行う。** 検証器が見るのは記入の欠落だけである。

## 改変禁止と append-only

予約レコードと結果証跡は、いずれも削除・改変しない。既存証跡の変更・削除を拒否する
append-only の CI 検査は **Phase 4-5 で実装済み**である。**同検査は「証跡本文の完全性」節が定める
統合時の受入検査も担う**(新規追加分のみが対象)。

## Phase 4 のブートストラップ手順

**順序の正は[ハーネス設計書](../../development/dev-harness-design-2026-08-07.md) 13 章「Phase 4 の分割」の「順序」**であり、
経路の正は同 10.1「NFR-021 受入ゲート」の「Phase 4 のブートストラップ」である。本節は
それらを実務の並びに写したものであり、**規範として競合した場合は設計書が優先する**。

1. **4-4 の確定ゲート**で、この README とテンプレート 3 枚を確定する(受入は行わない)。
2. 確定した内容に準拠して、**初回の予約レコード監査 PR** を `develop` へ統合する。この PR では
   ディレクトリの新設と、CI 受理に必要な最小限の索引除外規則およびそのテストを扱う。
3. **4-4 をマージ**する(予約の統合点を取り込む)。
4. **4-5** で検証器と append-only の CI 検査を実装する(**実装済み**。この時点でも受入は行わない)。
5. **Phase 4 完了時受入の前提 PR(第 1 号・第 2 号・第 3 号)** は、設計書 13 章の同名 3 節の
   **マージ条件を満たしたこと**(**本書 v1.2 は第 2 号の対象 3 本の 1 つ・本書 v1.3 は第 3 号の対象 4 本の 1 つである**)**を確認してから**、
   **第 1 号 → 第 2 号 → 第 3 号の順に** `develop` へ統合する。統合後の `develop` で
   `docs/development/onboarding.md` の frontmatter が `status: approved` であることを
   確認する(**版は焼き込まない** — 要求するのは「**その時点で `develop` に統合済みの
   approved 版**」であり、特定の版ではない)。
   **統合前に 4-6 の受入を開始してはならず、`tested_commit_sha`(T)も採ってはならない**
   — 同書は失効対象パスであり、T を採った後に触れば証跡が失効する。
6. **4-6** で受入を実施する。予約を含む候補ツリーで onboarding の手順を完走し、結果証跡を
   追加して検証器に掛ける。**受入を行うのは 4-6 だけ**である。
7. 合格時は 4-6 をマージする。失敗時は、実装を含まない**結果証跡だけの監査 PR**で予約を
   閉じてから次の試行を予約する。

**「Phase 4 PR」という表現は用いない** — 4-4 と 4-6 のどちらを指すか一意に読めないため。
