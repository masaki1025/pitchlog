# 個人設定(共有しない)
.claude/settings.local.json
CLAUDE.local.md

# Codex セッション追跡(codex_run.py が feature 単位で保存)
.codex-session

# シークレット(NFR-014)
.env
.env.*
!.env.example

# Python
__pycache__/
.venv/
.ruff_cache/
.pytest_cache/
*.egg-info/
# coverage.py の生成物が一括 add で再び追跡されるのを防ぐ
.coverage
.coverage.*

# Node
node_modules/
dist/
coverage/

# OS
.DS_Store
Thumbs.db

# BOOT-REPORT は CI が毎回出力する生成物。正本リポジトリへ取り込まない。
backend/domain/boot-report.json
